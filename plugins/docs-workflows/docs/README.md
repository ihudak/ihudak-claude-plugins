# docs-workflows documentation

| I want to… | Go to |
|---|---|
| install this and set it up | [Getting started](getting-started.md) |
| see how the pieces fit together | [Workflow](workflow.md) |

## Commands

No commands ship yet. This plugin is an empty, registered skeleton; later tasks in the marketplace split move `/document`, `/docs-profile` and `/release-notes` into it from `dev-workflows`, and each will get a page here as it lands.

## Reference

- [Agents](reference/agents.md) — the agents this plugin bundles, once any ship.
- [References](reference/references.md) — the reference corpus this plugin bundles, once any ship.
- [Environment](reference/environment.md) — the environment variables this plugin reads, once any are read.

## Status

This plugin ships 0 slash commands, 0 agents, and 0 reference files — it ships 0 bundled skills. It is an empty, registered skeleton: later tasks in the marketplace split move the documentation half of the `dev-workflows` pipeline into it without changing how any of it behaves.
