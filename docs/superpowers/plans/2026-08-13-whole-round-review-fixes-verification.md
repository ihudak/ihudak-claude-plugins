# Verification record — whole-round review fixes (sub-project I)

STATUS: COMPLETE — 47/47 PASS, 0 FAIL (tally and coverage at end of file)

Date: 2026-08-13. Written after the final fix wave, per R10 (this record is that rule's first application). If a later fix wave lands, this record is rewritten, not patched.

Spec: `docs/superpowers/specs/2026-08-13-whole-round-review-fixes-design.md`
Ledger: `.superpowers/sdd/2026-08-13-whole-round-review-fixes/progress.md`
Branch merge-base for could-fail proofs: `41bfe52`.

Conventions used in this record:
- All counts are whitespace-normalized where the checked phrase can wrap (`tr -s '[:space:]' ' '` before grepping), because these files hard-wrap prose.
- `grep -c` counts lines; occurrence counts use `grep -o ... | wc -l`.
- Every expected value was re-derived against the tree under verification, not copied from the spec, plan, or ledger.
- "Verified by reading" rows are named in the coverage section; no PASS below is recorded for a check that was not run.

## Three-repo state (Step 1 + Step 2)

Captured 2026-08-13 against the trees this record verifies. **Re-captured 2026-08-13, post-record fix wave** — see `## Post-record fix wave (whole-branch review)` below for why. Every value in this table and its bullets was re-derived against the current tree, not carried forward from the first capture.

| Repo | Branch | HEAD | plugin.json | CHANGELOG head | Catalog | Tree |
|---|---|---|---|---|---|---|
| canonical `/workspace/ihudak-claude-plugins` | `iv-gu/whole-round-review-fixes` | `bbf22d6` | 2.51.0 | `## [2.51.0] — 2026-08-13` | 2.51.0 | clean |
| mgd `/workspace/mgd-claude-plugins` | `iv-gu/whole-round-review-fixes` | `602f8fe` | 2.51.0 | `## [2.51.0] — 2026-08-13 (ported from ihudak-claude-plugins)` | 2.51.0 | clean |
| copilot `/workspace/ihudak-copilot-plugins` | `iv-gu/whole-round-review-fixes` | `c428387` | 2.21.0 | `## [2.21.0] — 2026-08-13` | 2.21.0 | clean |

- Clean = `git status --porcelain | grep -v '.superpowers' | wc -l` → 0 in all three repos (re-run).
- These heads are one commit past the ledger's original `ALL TASKS SETTLED` line (`canonical 5dc7905 / mgd 4b70d0a / copilot f6d529f`) — each repo gained exactly one further commit, the post-record fix wave (`bbf22d6` / `602f8fe` / `c428387`), applying the five whole-branch-review Minors documented below. The ledger itself is not rewritten; this table now tracks the branch, which has moved past it.
- JSON catalogs parse (re-run): canonical `.claude-plugin/marketplace.json` and mgd `.claude-plugin/marketplace.json` both load via `python3 json.load` (dev-workflows entry = 2.51.0 in each). Copilot's `.github/plugin/marketplace.json` and `dev-workflows/.plugin/plugin.json` also both parse (2.21.0 each) — three copilot version sites (plugin.json, CHANGELOG, catalog) agree.

### mgd parity gate — full output

Command: `diff -rq /workspace/ihudak-claude-plugins/plugins/dev-workflows /workspace/mgd-claude-plugins/plugins/dev-workflows`

```
Files /workspace/ihudak-claude-plugins/plugins/dev-workflows/.claude-plugin/plugin.json and /workspace/mgd-claude-plugins/plugins/dev-workflows/.claude-plugin/plugin.json differ
Files /workspace/ihudak-claude-plugins/plugins/dev-workflows/CHANGELOG.md and /workspace/mgd-claude-plugins/plugins/dev-workflows/CHANGELOG.md differ
Files /workspace/ihudak-claude-plugins/plugins/dev-workflows/LICENSE and /workspace/mgd-claude-plugins/plugins/dev-workflows/LICENSE differ
Files /workspace/ihudak-claude-plugins/plugins/dev-workflows/README.md and /workspace/mgd-claude-plugins/plugins/dev-workflows/README.md differ
Files /workspace/ihudak-claude-plugins/plugins/dev-workflows/references/dependencies.md and /workspace/mgd-claude-plugins/plugins/dev-workflows/references/dependencies.md differ
```

Exactly the five identity files (`plugin.json`, `CHANGELOG.md`, `LICENSE`, `README.md`, `references/dependencies.md`); zero `Only in` lines. **Method + time note (per ledger):** this is working-tree `diff -rq` on today's already-converged trees, which normalizes line endings on checkout. At ship-time commit pairs the same comparison showed 47–54 differing files (raw-blob and on-disk agreed); the 5-vs-47 difference is a *time* difference, not a method difference — quote ship-time values only with their commit pair. **Re-run post-record fix wave** (canonical `bbf22d6` / mgd `602f8fe`): identical result — same five files, zero `Only in` lines. The fix wave edited both trees identically (`vi-reviewer.md`, `risk-planner.md`, `diff-summarizer.md`, `session-hygiene.md` all live under `plugins/dev-workflows/` and are not identity files), so the gate's outcome is unchanged; only `CHANGELOG.md`'s divergent content (already one of the five, already differing) grew by one more line in both trees.

## Requirement rows (Step 3)

Paths below: `CN` = `/workspace/ihudak-claude-plugins/plugins/dev-workflows`, `CM` = `/workspace/ihudak-claude-plugins/CLAUDE.md`, `MG` = `/workspace/mgd-claude-plugins`, `CP` = `/workspace/ihudak-copilot-plugins/dev-workflows`, `SP` = `/workspace/ihudak-claude-plugins/docs/superpowers`.

### Critical + Important (R1–R10, R45)

| ID | Required | Check | Expected | Observed | Verdict |
|---|---|---|---|---|---|
| R1 | copilot `skills/upgrade/README.md` deleted; two unique passages folded into `SKILL.md`; rule 6 exemption sharpened | (a) `test -f CP/skills/upgrade/README.md`; (b) `find CP/skills -name README.md \| wc -l`; (c) `grep -n sdkmanrc CP/skills/upgrade/SKILL.md`; (d) `grep -ni incompatible` same file; (e) read rule 6 in `CP/skills/_shared/next-phase-offer.md` | (a) absent (b) 0 (c/d) both passages present (e) narrative exempt, "any text that tells a reader what to type" bound | (a) ABSENT (b) 0 (c) `:27` — `.sdkmanrc` in the Java version-file list (d) `:65` — "Incompatible explicit versions … `upgrade: gradle:9 java:11`" example (e) rule 6 now reads "Narrative prose that describes the pipeline's shape … may keep the short form — but ANY text that tells a reader what to type (usage blocks, example invocation tables, quick-starts, tips) is bound by this rule regardless of which file it appears in" | PASS |
| R2 | `/document` "no refresh" made functional (D3): `fetch:` varies; `:196` label sharpened | `grep -n 'fetch:' CN/commands/document.md`; `grep -n 'no refresh'` same | dispatch block varies fetch on Phase 1 choice; label warns un-fetched PR won't resolve | `:396` `fetch: [false if Phase 1 chose 'no refresh'; true otherwise]`; `:196` choice reads "no refresh — resolve PRs from local objects only; a PR not yet fetched will not resolve, and a dirty clone stops blocking" | PASS |
| R3 | `read-only-repos.md` prep promise narrowed to `code-scanner`+`diff-summarizer` (D1); `docs-grounder` documented as §1–§4 + digest; `CLAUDE.md` paragraph matches | `grep -n docs-grounder CN/references/read-only-repos.md`; read `CM` read-only paragraph (whitespace-normalized) | both files scope `prep` to the two emitters and describe `docs-grounder` as digest-returning | `read-only-repos.md:5` "the first two also emit the §6 `prep` block; `docs-grounder` consumes §1–§4 only"; `:69` "`docs-grounder` … returns a digest — `status` / `retrieval` / … — not a `prep` block; its staleness signal is the 14-day clause in `docs-grounding.md`". `CM` paragraph: "Consumed by `code-scanner` and `diff-summarizer`, which emit the `prep` block; `docs-grounder` also consumes it (§1–§4 only …) but returns a digest, not a `prep` block. Cited by the eight commands…" | PASS |
| R4 | `/ready` preflight moved to immediately after `$SPECS_PATH` resolves, ahead of the dirty-tree prompt | `grep -n specs-preflight CN/commands/ready.md`; `sed -n '44,62p'` | executable preflight invocation appears once, after `$SPECS_PATH` resolution and before the dirt test (corrected form per ledger — the plan's original "expect exactly 1 grep match" never held: 5 matches exist, 4 narrative + 1 executable) | 5 matches at `:17,:52,:110,:555,:560`; the single executable invocation is `:51-52` ("execute its `specs-preflight` entry point (§3) inline"), sitting after step 2 "Resolve `$SPECS_PATH`" (`:47-49`) and before step 3's dirty/non-main test (`:59-61`) | PASS |
| R5 | `CLAUDE.md` no longer claims a direct-mode `doc-reviewer` gate (D4) | `grep -n '/document (direct)' CM`; read direct-mode invariants | map edge has no `doc-reviewer`; invariant states no reviewer gate | `CM:139` `/document (direct) → [docs-style-checker] → [doc-fixer] → impl-maintenance → commit-artifacts`; `CM:204` "**No `doc-reviewer` gate** — direct mode is deliberately lightweight: a mandatory style check (Phase 3.5) and `doc-fixer`, but no Opus review. Two rules in `document.md` (`:1490`, `:1494`) depend on that absence" | PASS |
| R6 | `document.md:35` no longer says both modes share `doc-reviewer` (D4) | `sed -n '30,40p' CN/commands/document.md` | the shared-agents sentence names only style-checker/fixer as shared; `doc-reviewer` Jira-mode-only | Live `:36`: "The two modes share the same `docs-style-checker` / `doc-fixer` agents; only Jira mode also runs `doc-reviewer` (each mode emits its own final report)." | PASS |
| R7 | Grill-bounded invariant narrowed to `/idea` (D5) | `grep -n bounded CM` | claim scoped to `/idea`; other five named relentless | `CM:247` "Only `/idea`'s embedded grill is **bounded** (≤10 questions; `--deep` switches it to relentless) …; `/create-vi`, `/create-ard`, `/specify`, and `/design` (and `/update-vi`) grill **relentlessly** to convergence with no cap" | PASS |
| R8 | `/idea` round 2 + §8.5 state the round-1 `refresh:` block is reused verbatim | `grep -n refresh CN/commands/idea.md`; `grep -n refresh CN/references/model-routing/classification.md` | both sites carry the verbatim-reuse sentence | `idea.md:164` "Round 2 reuses round 1's `refresh:` block verbatim — `switch_to_default_branch: false`, `pull: false` — so the read-only posture … hold for both rounds"; `classification.md:376` "dispatch — **including `refresh:`** — is reused verbatim" (Task 4 sentence intact) | PASS |
| R9 | F's record: V5 → 7 consumers, V8 → 3 choices, each annotated as superseded same-day by `7142976` | `grep -n 'V5\|V8\|7142976' SP/plans/2026-08-11-environment-guards-verification.md` | corrected values + supersession notes present | V5 row: "7 consumer commands: idea, create-vi, create-ard, epics, release-notes, update-vi, specify … **Corrected 2026-08-13: original recorded 6 … re-derived at `7142976` = 7 — superseded same-day by 7142976**"; V8 row: "3 `choices:` … **Corrected 2026-08-13: original recorded 4 … re-derived at `7142976` = 3 … superseded same-day by 7142976**" | PASS |
| R10 | Repo `CLAUDE.md` conventions gain: verification record written last, after the final fix wave | `grep -ni 'verification record' CM-of-repo (/workspace/ihudak-claude-plugins/CLAUDE.md)` | rule present | `CLAUDE.md:59` "**A sub-project's verification record is written last** — after the final fix wave, never before it. … Re-derive every expected value against the tree being verified, and never copy an `expect N` from another plan" | PASS |
| R45 | `/idea` grill cap 5 → 10; `--deep` fully uncapped; `grilling-technique.md` + `CLAUDE.md` updated together; overflow path still fires at the new bound; CHANGELOG entry | `grep -n '≤10\|deep\|cap' CN/commands/idea.md CN/references/grilling-technique.md`; `grep -n grill CN/CHANGELOG.md`; CM row R7 | 10 as the bound everywhere `/idea`'s cap is stated; `--deep` = no cap; overflow markers bound-relative; changelog bullet | `idea.md:15` "`--deep` switches the grill from bounded (≤10 questions) to relentless"; `:132` "≤10 question slots"; `:186-187` "`--deep`: relentless — … the cap does not apply"; `:184-185` overflow intact: leftover gaps become `- [NEEDS CLARIFICATION: <question>]`, capped at 3 — mechanism is bound-relative, not tied to the literal 5; `grilling-technique.md:27` "Used by `/idea` (≤10; `--deep` switches to relentless) and `/prompt-grill-me` (≤5)"; `:32` names "their stated bound", no hardcoded number (Task 4 fix-round correction); `CM:247` ≤10; `CHANGELOG.md:20` carries the user-visible entry incl. the fix-round correction **— re-verified post-record fix wave (`bbf22d6`): `CHANGELOG.md:20` unchanged (the wave's new bullet was inserted at `:27`, after this line, so no shift here)** | PASS |

### Minor — caller lists and rosters (R13, R16, R17, R19–R30, R33)

| ID | Required | Check | Expected | Observed | Verdict |
|---|---|---|---|---|---|
| R13 | `cost-emission.md:3-5` names `/update-vi` | `sed -n '1,6p' CN/references/cost-emission.md` | `/update-vi` in the VI-lifecycle roster | `:4` roster is `/idea, /create-vi, /update-vi, /create-ard, /specify, /epics, /design, /implement, /ready, /document, /release-notes` (11 commands, `/update-vi` present) | PASS |
| R16 | `CLAUDE.md` model-routing roster 13 → 14; `skills/model-routing/SKILL.md:3` matches | whitespace-normalized extract of the roster parenthetical; count items; `sed -n 3p` SKILL.md | 14 names incl. `/update-vi`; SKILL says 14 | roster = `/implement, /document, /epics, /release-notes, /vuln, /upgrade, /docs-profile, /idea, /create-vi, /update-vi, /create-ard, /specify, /design, /ready` — comma-split count 14; SKILL.md:3 "the 14 pipeline commands" | PASS |
| R17 | `CLAUDE.md` source-truth consumer list 3 → 5 (`doc-writer`, `risk-planner` added) | `grep -n source-truth CM` | 5 names | `CM:111` "consulted by `doc-planner`, `doc-writer`, `doc-reviewer`, `release-notes-writer`, and `risk-planner`" | PASS |
| R19 | `feedback-emission.md:4,:191` + `README.md:128-130`: twelve → thirteen `emit-auto` callers | `grep -ni 'twelve\|thirteen'` both files | thirteen at all three sites, zero twelves | `feedback-emission.md:4` "all thirteen workflow", `:191` "the thirteen commands' maintenance phases", `README.md:128` "all thirteen workflow commands"; no `twelve` hit in either file | PASS |
| R20 | `risk-planner.md:11` drops `/vuln` | `sed -n '9,13p' CN/agents/risk-planner.md` | callers = `/implement`, `/upgrade` | `:11` "Invoked from the dev-workflows commands (`/implement`, `/upgrade`) only when …" **— re-verified post-record fix wave (`bbf22d6`): unchanged; the wave edited this file's line 22 (see DM2, now CLOSED), not the line 9-13 window this row checks** | PASS |
| R21 | `CLAUDE.md` risk-planner ledger line gains `/upgrade` | `grep -n 'risk-planner (used' CM` | `/upgrade` present | `CM:156` "risk-planner (used by /implement plan critique, /upgrade)" | PASS |
| R22 | `CLAUDE.md` doc-fixer line stops crediting `/release-notes` (it uses `dt-doc-fixer`) | `grep -n 'doc-fixer (used' CM` | `/document, /epics` only | `CM:159` "doc-fixer (used by /document, /epics)"; map line `CM:145` routes `/release-notes` through `dt-style-checker → dt-doc-fixer (optional)` | PASS |
| R23 | `vi-reviewer.md:12` gains `/update-vi` | `sed -n '10,14p' CN/agents/vi-reviewer.md` | both callers named | `:12` "Invoked from `/create-vi` Phase 4 after authoring and `/update-vi` Phase 4 after updating" **— re-verified post-record fix wave (`bbf22d6`): unchanged; the wave edited this file's frontmatter `description` (line 3) and body opening (line 8), not the line 10-14 window this row checks** | PASS |
| R24 | `jira-reader.md:11,15-18` gains `/create-ard` and `/ready` | `sed -n '9,19p' CN/agents/jira-reader.md` | both present in the invocation list | `:11` "Invoked from `/document` …, `/epics` …, `/specify` …, `/create-ard` (Phase 2, `depth: vi-only` VI-level / `full` Epic-level), and `/ready` (Phase 2, `depth: vi-plus-epics`)" | PASS |
| R25 | `CLAUDE.md` docs-style-checker: stale "Jira mode" qualifier → both modes | `grep -n 'docs-style-checker (used' CM` | both modes | `CM:163` "docs-style-checker (used by /document, both modes)" | PASS |
| R26 | `grilling-technique.md:27-28` bounded list gains `/prompt-grill-me`; relentless list gains `/update-vi`, `/create-ard` | `sed -n '27,28p' CN/references/grilling-technique.md` | both lists complete | `:27` "Used by `/idea` (≤10; `--deep` switches to relentless) and `/prompt-grill-me` (≤5)"; `:28` "Used by `/create-vi`, `/update-vi`, `/create-ard`, `/specify`, `/design`" | PASS |
| R27 | `jira-input-resolution.md:153` gains `idea-reader` | `grep -n idea-reader CN/references/jira-input-resolution.md` | consumer named | `:153` "Consumed by `/idea` (source typing), `idea-reader` (export …" | PASS |
| R28 | `README.md:256` jira-reader row lists all 7 dispatchers | `grep -n jira-reader CN/README.md` | 7 named | `:256` "Used by `/document`, `/epics`, `/release-notes`, `/implement` (multi-source input), `/create-ard`, `/specify`, and `/ready`" — 7 | PASS |
| R29 | `CLAUDE.md` test-baseliner line gains `/upgrade` + `/vuln` direct invocations | `grep -n 'test-baseliner' CM` | direct + via-agent phrasing | `CM:154` "test-baseliner (used by /implement, and by /upgrade and /vuln both directly and via upgrade-executor / vuln-fixer)" | PASS |
| R30 | `ard-resolution.md:57` + `CLAUDE.md` ARD-respect line gain `/ready` | `grep -n 'Consumers\|/ready' CN/references/ard-resolution.md`; `grep -n 'respect the applicable ARD' CM` | `/ready` in both | `ard-resolution.md:57` "## Consumers (informative)" with `/ready` entry at `:63`; `CM:251` "`/design`, `/implement`, `/specify`, `/epics`, `/ready` respect the applicable ARD" | PASS |
| R33 | `CLAUDE.md` vi-reviewer ledger line gains `/update-vi` | `grep -n 'vi-reviewer' CM` | `/update-vi` present | see command output below the table batch: `CM:164` (checked next batch) | see next batch |

### Minor — stale claims, terminal order, routing/cost, dead gate, formats (R11, R12, R14, R15, R18, R31–R34, R41–R43)

| ID | Required | Check | Expected | Observed | Verdict |
|---|---|---|---|---|---|
| R11 | `/release-notes` Phase 9/10 swapped: feedback before follow-ups (D6) | `grep -n '^## Phase' CN/commands/release-notes.md` | feedback phase numerically and positionally before follow-ups | `:313` `## Phase 9 — Session maintenance & feedback`, `:355` `## Phase 10 — Emit follow-up tasks`, `:380` `## Phase 11 — Session cost` — canonical emitter-tail order | PASS |
| R12 | `session-hygiene.md` rule 2 amended: the binding sequence is the emitter tail; deliverable-side finish may precede it; `document.md` NOT touched for this | `sed -n '110,130p' CN/references/session-hygiene.md`; ledger Task 8 | amended rule; no phase move | **Re-verified post-record fix wave (`bbf22d6`) — wording changed, requirement still holds.** Rule 2 now reads "What binds every command is the **emitter tail** — feedback → follow-ups → cost → `resume.md` → `commit-artifacts`. A command's deliverable-side finish may **sit anywhere relative to the tail, including between its members**, at whatever point suits it: `/document`'s conditional `Phase 8.5 — Finish & handoff` **sits between feedback and follow-ups** — it is the docs-repo git finish, while `emit-auto` writes into `$SPECS_PATH`, so the two touch different repositories and their relative order carries no consequence" (bold = the fix-wave reword: the original "may precede the tail" contradicted its own `/document` example, whose Phase 8.5 finish sits *between* the tail's members, not before all of it). `document.md` still untouched by this rule — re-confirmed `emit-auto` at `:1035` precedes `Phase 8.5 — Finish & handoff` at `:1054`, unchanged from the original capture | PASS |
| R14 | §8.5's `absent`-with-outside-deferral precedence scoped to `/implement`; `/idea` rule unconditional | whitespace-normalized `grep -o 'for `/implement` only'` on `CN/references/model-routing/classification.md`; read `:380-391` | 1 occurrence (wrap-aware — a single-line grep returns 0 by construction, per plan defect #4); four-state table consistent | normalized occurrence count = 1; `:387` "caller-scoped. For **`/implement`** it is resolved only when that `absent` …" — `/idea` resolves unconditionally (ledger Task 10: all 4 cells of the state table agree) | PASS |
| R15 | `cost-prices.yaml` drops the "Opus 4.5" comment; `cost-emission.md` stops claiming Haiku is routing-policy-reachable | `grep -ni 'opus 4.5' CN/references/cost-prices.yaml`; whitespace-normalized `grep -o 'and Haiku;' CN/references/cost-emission.md \| wc -l` (the spec's token "routing-policy-reachable" is its own shorthand, not the file's literal text — a literal grep for it returns 0 on BOTH trees and proves nothing, so it was replaced with the pre-fix reachable-list phrase "…the Opus chain, the Sonnet chain, and Haiku;") | yaml: 0 hits; emission: 0 occurrences | yaml grep returns nothing — `:22` now reads "Opus 5 and Opus 4.6-4.8 all bill at $5 / $25" (pre-fix: "Opus 4.5-4.8"); "and Haiku;" occurrences = 0 — `:154-157` now reads "the Opus chain and the Sonnet chain — **plus Haiku, priced as a harmless defensive entry even though no routing path in `classification.md` currently reaches it**" | PASS |
| R18 | `source-truth.md` §4.1 no longer assigns `diff-summarizer` duties (D2); verification duty sits with the consulting agent | `sed -n '210,228p' CN/references/source-truth.md`; `grep -n diff-summarizer` same | §4.1 names the consulting agent reading shipped source | §4.1 now: "The consulting agent (doc-planner, §4.2) verifies these the same way as every other user-visible claim — by reading shipped source directly with the §3 techniques, not by relying on a PR summary to have surfaced them." Remaining `diff-summarizer` mention (`:229`) is an unrelated orchestrator-input note. Ledger: duty genuinely performed — `verification_warnings[]` consumed at `document.md` Phase 5.8 | PASS |
| R31 | `CLAUDE.md` branch policy rewritten (no phantom `git_repo` context) | `grep -n 'Branch policy' CM` | current contexts only | `CM:215` "`/epics` never branches. `/document` classifies its write context against the resolved `docs_repo_path` (not necessarily cwd) — … `obsidian` (never branch); else `git rev-parse` plus docs signals → `docs_repo` (branch opt-in, confirmed at plan approval) or `non_docs_repo` (…); else `plain_dir` (never branch)" — no `git_repo` token anywhere in `CM` (`grep -c 'git_repo' CM` = 0, run) | PASS |
| R32 | `CLAUDE.md` `document-as-jira` → `document-as-spec` | `grep -n 'document-as' CM`; `grep -c 'document-as-jira' CM` | 0 stale hits | `CM:222` "for `document-as-spec` / `skip-and-report` decisions, and for a `document-as-code` decision …"; `document-as-jira` count = 0 | PASS |
| R33 | `CLAUDE.md` vi-reviewer ledger line gains `/update-vi` (completes the row deferred from the previous table) | `grep -n vi-reviewer CM` | both callers | `CM:167` "vi-reviewer (used by /create-vi, /update-vi)" | PASS |
| R34 | `CLAUDE.md` `/upgrade` map edge gains `risk-planner@Opus` | `grep -n '^/upgrade' CM` | edge present between planner and executor | `CM:145` `/upgrade → upgrade-planner → [risk-planner@Opus] → upgrade-executor → [code-review@Opus] → review-fixer → tests → impl-maintenance → commit-artifacts` | PASS |
| R41 | copilot: "§2.1 Sonnet [detection] chain" fixed at `agents/epic-writer.md:3`, `skills/docs-profile/SKILL.md:255`, `skills/epics/SKILL.md:341` | `grep -rno '§2.1 [A-Za-z ]*chain\|Sonnet detection chain' CP --include='*.md'` (CHANGELOG excluded), plus reading the 3 named sites | 0 `§2.1 Sonnet` survivors; named sites read "§2.1 detection chain" | phrasing census: 80 × "§2.1 detection chain", 1 × "§2.1 chain" (`docs-profile/SKILL.md:70`, model-agnostic — acceptable), 3 × bare "Sonnet detection chain" (`design:343`, `release-notes:318`, `specify:450` — these mirror canonical's own prose verbatim at `design.md:335` / `specify.md:442` / `release-notes.md:317` and are outside R41's copilot-dialect scope), 0 × "§2.1 Sonnet"; all 3 named sites now read "§2.1 detection chain"; pre-fix `epic-writer.md:3` at copilot merge-base `128a38e` read "§2.1 Sonnet detection chain" | PASS |
| R42 | `idea-format.md:14` provenance enum gains `doc-grounding` | `sed -n '12,16p' CN/references/idea-format.md` | enum complete | `:14` `provenance: rfe \| vi \| markdown \| community-post \| prompt \| doc-grounding` | PASS |
| R43 | Unpriced-model dominance warning surfaced + maintainer checklist; `session-cost.py` pricing logic untouched | `grep -ni 'unpriced\|checklist' CN/references/cost-emission.md`; `git diff --stat 41bfe52..HEAD -- plugins/dev-workflows/scripts/session-cost.py` | warning section wired into emit-cost; §12 checklist; empty script diff | `§6.1 Unpriced-model dominance warning` at `:236` with printed line "⚠ Cost estimate is a lower bound — <model-id> is unpriced (…)" at `:255` and a §12 pointer at `:258`; `## 12. Maintainer checklist — onboarding a new model generation` at `:394` (ties routing + prices files, `:411`); `session-cost.py` diff vs merge-base is empty (ledger Task 9 traced reachability `session-cost.py:217-218` → per-model JSON → §6.1 trigger → §11 emit) | PASS |

### Minor — dispatcher count and specs-repo bookkeeping (R35–R40, R44) + found-in-execution (R46, R47)

| ID | Required | Check | Expected | Observed | Verdict |
|---|---|---|---|---|---|
| R35 | "cited by the seven commands" → eight in canonical `CLAUDE.md`, mgd `CLAUDE.md`, copilot `dev-workflows/README.md` | case-insensitive whitespace-normalized `grep -o 'cited by the [a-z]* commands'` per edition + repo-wide survivor sweep | eight at all three sites; no live survivors | canonical `CM` "Cited by the eight commands"; mgd `CLAUDE.md` "Cited by the eight commands"; copilot `README.md:369` "Cited by the eight skills that dispatch them" (correct dialect: skills). Survivor sweep: the only remaining "cited by the seven commands" strings tree-wide are `plugins/dev-workflows/CHANGELOG.md:103`→**`:104` (re-verified post-record fix wave, `bbf22d6`/`602f8fe`)** in canonical and mgd — the historical 2.45-era release entry, which describes that release's state and is never rewritten (same immutable-history class the R39/R40 annotations document). Line shifted because the wave's own new CHANGELOG bullet was inserted above the `[2.50.0]` header, pushing every later line down by one; content unchanged, confirmed by direct grep, not by arithmetic on the old number. The CM (`CLAUDE.md`) citations in this row and every other row are unaffected — `CLAUDE.md`'s fix-wave edit was a same-line replace (1 insertion, 1 deletion), so its total line count did not shift | PASS |
| R36 | D's V18 breakdown made to sum; G's tally corrected 25 → 27 PASS | `grep -n 'V18\|Corrected 2026-08-13' SP/plans/2026-08-11-verification-results.md`; `grep -n '27 PASS\|25 PASS' SP/plans/2026-08-11-vault-prior-art-discovery-verification.md` | breakdown sums to its stated total; tally = 27 at both sites | D (`2026-08-11-verification-results.md:30`): corrected breakdown "2 + 5 + 16 + 3 + 2 = 28" with recount commit `f02eee3` named; G (`2026-08-11-vault-prior-art-discovery-verification.md:43`): "Tally: 27 PASS, 2 FAIL … original tally recorded 25 … 29 rows − 2 = 27; V11a/V11b were left uncounted", and the `:79` echo now also reads "27 PASS / 2 FAIL (corrected 2026-08-13 …)" (the stale echo was Task 13's fix-round Important) | PASS |
| R37 | C's plan (`2026-08-10-specs-repo-git-completeness.md`) wrong `expect N` / wrong-target checks corrected | `grep -o '\[R37 correction, 2026-08-13\]' … \| wc -l`; read sampled annotations | every correction dated, tagged, and re-derived at a named commit | 6 `[R37 correction, 2026-08-13]` annotation blocks (some correcting multiple values, e.g. `:2043` "Four of the above are wrong", `:952`/`:1199` wrong-target preflight counts re-derived at `9e663bf`, copilot values at `04273a4`) | PASS |
| R38 | B1's plan (`2026-08-08-document-gate-enforcement.md`) 4 wrong + 1 stale-at-ship corrected | `grep -oi 'corrected 2026-08-13' … \| wc -l`; read the parity annotation | corrections present with ship-commit re-derivations | 5 correction markers; the mgd-parity one (`:1876`) re-derives 47 differing items at ship pair canonical `c7bdac2` / mgd `fce902f`, with the fix-round method note (real `git worktree` checkout agrees with raw-blob; today's live trees show 5 — time difference, not method) | PASS |
| R39 | B2's plan (`2026-08-10-document-authoring-placement.md`) 6 wrong + 2 wrong-target corrected | `grep -oi 'corrected 2026-08-13' … \| wc -l`; read sampled annotations | corrections tagged (R39) with re-derived values | 9 correction markers tagged `(R39)`, incl. the structurally-unsatisfiable `images_wanted` substring check (`:480`), the unscoped gate-ledger table count (`:539`), the `^| ` separator-row miscount (`:655`), the phase-heading over-match (`:727`), the `^## 2\.4` never-matches pattern (`:778`), and the CHANGELOG-narrative false positive (`:891`) — each re-derived at `25b3628`/`c25eab7` | PASS |
| R40 | A's plan (`2026-08-07-release-notes-field-hygiene.md`) wrong values corrected (spec predicted 5; execution found 4, reported not forced, + fix-round parity corrections) | `grep -oi 'corrected 2026-08-13' … \| wc -l`; read the two parity annotations | corrections present; parity values consistent with R38/R39's | 6 correction markers; `:1056` re-derives 54 differing items at ship pair `4720e28`/`f64a69b` and `:1100` re-derives 47 at `4720e28`/`d404a75`, both with the method note aligning all three plans (47/54/47/47 at ship pairs vs 5 on today's converged trees); `:880` annotates the unsatisfiable `change_type` grep as WRONG-TARGET | PASS |
| R44 | Feedback entry `2026-08-12-implement-broad-then-narrow-candidate` annotated CLOSED by 2.50.0 | `grep -n -i 'CLOSED\|2.50.0' /workspace/specs/dev-workflows-feedback/2026-08-12.md`; confirm the cited CHANGELOG section | CLOSED status citing the shipping release | **Re-verified post-record fix wave (`bbf22d6`).** Feedback file unaffected (it lives outside the three repos the wave touched) — entry heading still "… — CLOSED"; `:9` still "**Status: CLOSED (2026-08-13).** `/implement` adopted `classification.md` §8.5 — see `CHANGELOG.md`'s `[2.50.0] — 2026-08-12` entry…". The cited CHANGELOG section shifted by one line: the wave's own new bullet was inserted above the `[2.50.0]` header, so the adoption bullet now sits at `CHANGELOG.md:33` (was `:32`), inside the `[2.50.0]` section now spanning `:29`–`:44` (was `:28`–`:43`) — re-confirmed by direct grep, content identical, only the line numbers moved | PASS |
| R46 | `CLAUDE.md` doc-reviewer ledger line gains the "Jira mode" qualifier its list-mates carry (found in Task 3, fixed in Task 13) | `grep -n 'doc-reviewer *(used' CM` | qualifier present | `CM:158` "doc-reviewer (used by /document Jira mode)" | PASS |
| R47 | `source-truth.md`'s "Every sub-agent that synthesises documentation (…)" list gains `doc-writer` in all three editions; `risk-planner` deliberately excluded | whitespace-normalized `grep -o 'Every sub-agent that synthesises documentation ([^)]*)'` per edition | 4 names in each | all three editions (canonical + mgd `references/source-truth.md`, copilot `skills/_shared/source-truth.md`) read "(`doc-planner`, `doc-writer`, `doc-reviewer`, `release-notes-writer`)"; `risk-planner` exclusion is deliberate and justified in the ledger (its sole source-truth use is a NEVER-rule citing §7; its deliverable is a plan, not documentation) — it remains in `CLAUDE.md`'s broader 5-name "consulted by" catalog (R17) but not this narrower sentence | PASS |

## Could-fail proofs (Step 4)

Every check below was re-run against the pre-fix tree and returned a **different** value than against HEAD, proving the check is capable of failing. Pre-fix baselines: canonical `41bfe52` (the last spec-only commit before implementation; verified `bcffb4c..41bfe52` — the range above the true merge-base `bcffb4c` — touches only `docs/superpowers/specs/…-design.md`, so plugin content at `41bfe52` equals the merge-base state), mgd `a437f5c` (merge-base), copilot `128a38e` (merge-base). **21 proof rows below, covering 22 distinct requirements and spanning all three repos** — 18 canonical checks (R2, R4, R5, R7, R10, R11, R13, R14, R15, R16, R19, R20, R22, R29, R32, R34, R42, R45), 2 mgd probes (R35, R47), 3 copilot probes (R1, R35, R41):

| Row | Check (as in the row above) | Pre-fix (OLD) | HEAD (NEW) |
|---|---|---|---|
| R2 | `grep -n 'fetch:'` + count of `false if Phase 1 chose 'no refresh'` | `:396` `fetch: true` hardcoded; count 0 | `:396` `fetch: [false if Phase 1 chose 'no refresh'; true otherwise]`; count 1 |
| R4 | line order: executable preflight vs dirty-tree test in `ready.md` | preflight `:72` AFTER dirt test `:54` | preflight `:52` BEFORE dirt test `:61` |
| R5 | `grep -n '/document (direct)'` in `CLAUDE.md` | `:138` `… → [doc-reviewer] → [doc-fixer] → …` | `:139` `… → [docs-style-checker] → [doc-fixer] → …` |
| R7 | grill-bound line | `:246` "bounded (≤5 questions; `--deep` on `/idea` relaxes it)" — flow-wide claim | `:247` "Only `/idea`'s … (≤10 …)" — scoped |
| R10 | `grep -ci 'verification record is written last'` repo `CLAUDE.md` | 0 | 1 (`:59`) |
| R11 | `grep -n '^## Phase 9\|^## Phase 10'` release-notes.md | Phase 9 = Emit follow-up tasks, Phase 10 = Session maintenance & feedback (inverted) | Phase 9 = Session maintenance & feedback, Phase 10 = Emit follow-up tasks |
| R13 | `/update-vi` in `cost-emission.md:3-5` roster | ABSENT | present |
| R14 | normalized `grep -o 'for `/implement` only' \| wc -l` on classification.md | 0 | 1 |
| R15 | yaml `grep -i 'opus 4.5'`; normalized `grep -o 'and Haiku;' \| wc -l` | `:22` "Opus 4.5-4.8" (1 hit); "and Haiku;" = 1 | 0 hits; 0 |
| R16 | roster item count + `SKILL.md:3` | 13; "the 13 pipeline commands" | 14; "the 14 pipeline commands" |
| R19 | `grep -ci twelve` feedback-emission.md | 2 | 0 (three "thirteen" sites) |
| R20 | `risk-planner.md:11` caller list | "(`/implement`, `/vuln`, `/upgrade`)" | "(`/implement`, `/upgrade`)" |
| R22 | `doc-fixer (used by …)` ledger line | "/document, /epics, /release-notes" | "/document, /epics" |
| R29 | `test-baseliner (used by …)` ledger line | "upgrade-executor, vuln-fixer, and /implement" | "/implement, and by /upgrade and /vuln both directly and via upgrade-executor / vuln-fixer" |
| R32 | `grep -c 'document-as-jira'` CLAUDE.md | 1 | 0 |
| R34 | `grep -n '^/upgrade'` map edge | no `risk-planner` | `… → [risk-planner@Opus] → …` |
| R42 | `idea-format.md:14` enum | `rfe \| vi \| markdown \| community-post \| prompt` | `… \| prompt \| doc-grounding` |
| R45 | grill-cap sites (with R7's line) | ≤5 at `CLAUDE.md:246`, "≤5 bound" at `:259` | ≤10 at `:247`, "≤10 bound" at `:260` |
| R35 (mgd) | normalized case-insensitive `grep -o 'cited by the … commands'` on mgd `CLAUDE.md` | "cited by the seven commands" (at `a437f5c`) | "Cited by the eight commands" |
| R47 (mgd) | normalized `grep -o 'Every sub-agent that synthesises documentation (…)'` on mgd source-truth.md | 3 names (no `doc-writer`) at `a437f5c` | 4 names |
| R1 / R35 / R41 (copilot) | `ls-tree 128a38e dev-workflows/skills/upgrade/`; pre README phrasing; pre `epic-writer.md:3` | `README.md` existed; copilot README said "cited by the seven skills"; epic-writer said "§2.1 Sonnet detection chain" | README absent; ":369" says "Cited by the eight skills"; "§2.1 detection chain" |

One check was **replaced** during this step because it failed the could-fail test: R15's literal `grep routing-policy-reachable` returned 0 on both trees (the spec's phrase was shorthand, not file text — the same wrong-target class R37–R40 annotate). The row now uses the discriminating "and Haiku;" probe (pre 1 / post 0).

## What is NOT closed (Step 5)

### The 9 deferred minors — 2 CLOSED by the post-record fix wave, 7 still open; none quietly dropped

| # | Ledger entry | Where | Status today (verified) |
|---|---|---|---|
| DM1 | Task 1: `task-1-report.md` R31 entry contains a leftover thinking-aloud fragment | `.superpowers/sdd/…/task-1-report.md:63` | OPEN — fragment still present ("…wait — this is the R31 compound assert…"); report hygiene only, product unaffected |
| DM2 | Task 2: `agents/risk-planner.md:22` bare-word "upgrade/vuln work" in prose | `CN/agents/risk-planner.md:22` | **CLOSED by the post-record fix wave (canonical `bbf22d6`, mgd `602f8fe`, copilot `c428387`).** Line 22 now reads "For upgrade work, this includes…" — `/vuln` never dispatches `risk-planner` (confirmed via `grep -l 'risk-planner' commands/*.md` → `implement.md`, `upgrade.md` only), so the bare-word prose no longer contradicts the caller-list rows (R20/R21) it sits beside |
| DM3 | Task 5: `diff-summarizer.md:90` says "per the Refresh step's read-only detection **above**" but the Refresh step (`:100`) is BELOW | `CN/agents/diff-summarizer.md:90` vs `:100` | **CLOSED by the post-record fix wave (canonical `bbf22d6`, mgd `602f8fe`; copilot already correct — see below).** `:90` now reads "per the Refresh step's read-only detection, item 2 below" — re-verified `## Refresh step` is still at `:100`, i.e. genuinely below, so the corrected direction is accurate, not just relabeled |
| DM4 | Task 6: `read-only-repos.md:69` — the docs-grounder aside sits between the two-emitter sentence and the `prep:` fence, so the trailing colon follows the wrong clause | `CN/references/read-only-repos.md:69` | OPEN — verified the aside still ends "…the 14-day clause in `docs-grounding.md` instead:" directly before the yaml fence; readability only, no factual error |
| DM5 | Task 6: `task-6-report.md:123` misattributes `CHANGELOG.md:82` to header `[2.46.1]` (correct: `[2.47.0]`) | `.superpowers/sdd/…/task-6-report.md` | OPEN — the report still says `[2.46.1]`; report narrative only |
| DM6 | Task 11: `task-11-report.md` overstates the Step-3 grep output (lists two `docs/superpowers` spec files that do not match the literal string) | `.superpowers/sdd/…/task-11-report.md:71-72` | OPEN — listing still present; the survivor conclusion itself remains correct |
| DM7 | Task 11: copilot commit message says "eight **skills**, not seven" where the brief's literal was "eight commands" — sensible dialect judgment, undisclosed as a deviation | copilot commit `99429af` | OPEN-PERMANENT — commit messages are immutable; verified: `99429af fix(dev-workflows): read-only-repos is cited by eight skills, not seven` |
| DM8 | Task 13: the new R40 prose locates the 42 CRLF files under `references/guidelines/*` and `api-guidelines/**` but 5 are under `references/fix-vuln/` and `references/upgrade/` (R38's annotation names all four) | `SP/plans/2026-08-07-release-notes-field-hygiene.md:1056,:1100` | OPEN — verified the directory list still names only the two dirs; the numbers (54/47/42) are correct, only the directory enumeration undercounts |
| DM9 | Task 16: `task-16-report.md` ready-count reconciliation narrative inaccurate in 3 specifics; conclusion (12) correct, real arithmetic 14−3+1 | `.superpowers/sdd/…/task-16-report.md` | OPEN — report unedited; ledger warns "do not reuse the narrative as a template" |

### Routed findings — all verified CLOSED

- **R30 second half** (`ard-resolution.md:57`) routed Task 1 → Task 2: closed — verified in row R30.
- **R46** (doc-reviewer "Jira mode" qualifier) found in Task 3, routed to Task 13: closed — verified in row R46.
- **R47** (source-truth "synthesises documentation" list) found in Task 16's window, fixed in all three editions at the final heads: closed — verified in row R47.
- **mgd `README.md:333`** (identity file, excluded from Task 15's copy step — the ONLY tree-wide survivor of both the prep claim and "seven commands"): hand-fixed in Task 15 (ledger: "README:337 hand-fix confirmed; zero survivors"); today's mgd README carries neither stale string (row R35 sweep).
- **Task 14's canonical README fixes not carried by the port** (9 catalog edit sites + env-var section; flagged "MUST ROUTE BEFORE TASK 17"): closed — mgd via Task 15 fix round 1 (`5a63d05`, 16 lines classified: 12 ported re-derived, 2 identity kept), copilot via Task 16 fix round 1 (`8b19bfa`, 6 README mirrors re-derived from copilot's own tree).
- **mgd README follow-up, 4 fronts** (Task 14 sweep, env-var section, twelve→thirteen, ≤5→≤10): closed by Task 15 fix round 1; spot-confirmed today — mgd README has no "cited by the seven" and mgd's grill/emit-auto counts came through the re-derived port.
- **KNOWN TRANSIENT** (README "eight" vs CLAUDE.md "seven" divergence until Task 11): resolved — both say eight (row R35).
- **Task 16 concern 4** (doc-writer/risk-planner briefly added to a copilot source-truth "top consumer list" then reverted): investigated, no gap — neither canonical nor copilot source-truth.md declares that consumer list; the revert was correct. (R47's later 4-name fix is the narrower "synthesises documentation" sentence, a different list.)
- **Task 13 CRLF concern**: first wrongly rejected via filesystem `cmp` (working trees normalize on checkout), corrected to blob-level-real, finally settled by worktree checkouts as a **time** difference (47/54 at ship pairs; 5 today). This record quotes both values with method + commit-pair context (parity-gate note, rows R38–R40).

### Plan defects recorded during execution — adjudicated, and this record obeys their corrected forms

1. **Task 7 Step 4** `grep -n 'specs-preflight' # expect exactly 1` — never held on any tree (5 matches pre and post; 4 narrative + 1 executable). Row R4 uses the corrected executable-invocation form.
2. **Plan defect #4** — appendix R14 ASSERT2 greped a single line for a phrase the file hard-wraps; unsatisfiable by construction. Row R14 (and this record throughout) uses wrap-aware, whitespace-normalized checks.
3. **Plan defect #5** — appendix R35a's OLD anchor was invalidated when Task 6/R3 rewrote the sentence before Task 11 ran. No action needed here; noted as provenance for why Task 11 re-derived its own values.

### Deliberately out of scope (spec non-goals) — still open by design

- **I3 / phase-handoff gates** (`/design`'s "spec is on main" gate; PR creation in `commit-artifacts`; §3.6 revision) — deferred to sub-project J. Untouched, as required.
- **The ~100 per-VI feedback entries** — deliberately deferred; only `2026-08-12-implement-broad-then-narrow-candidate` was annotated (R44), because 2.50.0 actually shipped its ask.

## Post-record fix wave (whole-branch review)

**Why this record is being amended rather than left as-is:** R10 (row R10 above; `CLAUDE.md:59`) requires a verification record to be written last, after the final fix wave — never before it, because a record written earlier is stale the moment a later wave lands. That is exactly what happened here: this record was committed as canonical (`72aed7f`) with `STATUS: COMPLETE`, and a **whole-branch review** run afterward against the resulting branch found five new Minors (no Critical, no Important — verdict **MERGE**). A single fix wave (canonical `bbf22d6`, mgd `602f8fe`, copilot `c428387`) cleared them. Per R10, a record that predates a real fix wave must be brought current — not superseded by a second file, and not left to describe a branch state that no longer exists. This section is that update, applied in place: the state block above and the six requirement rows below were re-verified (not assumed unaffected), and the amendment is itself the rule's first self-application — R10 governs the record you are reading.

### The five findings and what was applied per edition

| ID | Finding | Canonical (`bbf22d6`) | mgd (`602f8fe`) | Copilot (`c428387`) |
|---|---|---|---|---|
| F1 | `agents/vi-reviewer.md`'s frontmatter `description` (line 3) and body opening (line 8) still said the reviewer is for VIs "authored by `/create-vi`" — only the separate "Invoked from" line (checked by R23, line 12) had earlier been corrected to name `/update-vi` too | Fixed — both sites now name `/create-vi` **or** `/update-vi` | Fixed — identical edit | Fixed — `create-vi:` or `update-vi:` (copilot dialect) |
| F2 | `CLAUDE.md`'s canonical-terminal-order bullet (line 272) had not received the R12 "emitter tail" clarification, so a reader could still misread `/document`'s Phase 8.5 finish as a rule violation | Fixed — bullet now states the emitter tail explicitly, same wording pattern as R12's session-hygiene.md fix | Fixed — identical edit | **N/A — verified absent.** `.github/copilot-instructions.md` has no equivalent terminal-order statement; nothing to fix |
| F3 | `references/session-hygiene.md` rule 2 said a deliverable-side finish "may precede the tail," contradicting its own `/document` example, whose Phase 8.5 finish sits *between* the tail's members, not before all of it | Fixed — reworded to "may sit anywhere relative to the tail, including between its members" (verified against `/document`'s actual phase order: `emit-auto` at `:1035` precedes `Phase 8.5` at `:1054`, unchanged) | Fixed — identical edit | Fixed — `skills/_shared/session-hygiene.md`, same reword (copilot's canonical-order bullet differs slightly — no `cost` step — but the emitter-tail clause and the fix are structurally identical) |
| F4 | `agents/risk-planner.md` said "For upgrade/vuln work…" though `/vuln` never dispatches `risk-planner` (only `/implement` and `/upgrade` do, per R20/R21) | Fixed — "For upgrade work…" | Fixed — identical edit | Fixed — identical edit |
| F5 | `agents/diff-summarizer.md:90` said the Refresh step's read-only detection was "above" when `## Refresh step` (`:100`) is below | Fixed — now "item 2 below" | Fixed — identical edit | **N/A — verified already correct.** Copilot's `agents/diff-summarizer.md` already read the corrected form from an earlier port adaptation; checked and left unchanged |

All three commits are single-commit fix waves (`git show --stat` confirms each touches only the files named above: `CLAUDE.md`, `plugins/dev-workflows/CHANGELOG.md`, `agents/diff-summarizer.md`, `agents/risk-planner.md`, `agents/vi-reviewer.md`, `references/session-hygiene.md` — canonical/mgd; the copilot equivalent minus the two N/A files, using its own path/dialect conventions). Each also appends one CHANGELOG bullet summarizing the wave — this is the source of the CHANGELOG line-number shifts re-verified in R35 and R44 below.

### New heads

Canonical `bbf22d6`, mgd `602f8fe`, copilot `c428387` — all three trees clean, all three still on `iv-gu/whole-round-review-fixes`. Full re-verification of the three-repo state block (versions, CHANGELOG heads, catalogs, JSON parse, mgd parity gate) is above, re-run against these heads, not copied from the original capture.

### Existing rows re-run against the post-wave tree

Six requirement-table rows check a file the wave touched; each was re-run (not assumed unaffected) and is updated in place above:

- **R20** (`risk-planner.md:11` caller list, window lines 9-13) — unchanged; the wave edited line 22 (DM2), outside this row's window. Still PASS.
- **R23** (`vi-reviewer.md:12` "Invoked from" line, window lines 10-14) — unchanged; the wave edited lines 3 and 8 (F1), outside this row's window. Still PASS.
- **R12** (`session-hygiene.md` rule 2) — **content changed** (F3's reword). Requirement still holds (rule still amended, `document.md` still untouched), but the row's quoted evidence was stale and has been replaced with the current wording. Still PASS.
- **R45** (CHANGELOG.md:20 grill entry) — unchanged; the wave's new bullet was inserted at `:27`, after this line. Still PASS.
- **R44** (CHANGELOG.md `[2.50.0]` section + adoption bullet) — **line numbers shifted** by the wave's new bullet (inserted above the `[2.50.0]` header): adoption bullet `:32`→`:33`, section range `:28`-`:43`→`:29`-`:44`. Content identical; citations updated. Still PASS.
- **R35** (CHANGELOG.md survivor sweep, "cited by the seven commands") — **line number shifted**: `:103`→`:104` in both canonical and mgd, same cause as R44. Content identical; citation updated. Still PASS.

`CLAUDE.md`'s own fix-wave edit (F2) was a same-line replace — 1 insertion, 1 deletion, net zero line-count change, confined to line 272 — so no other row's `CM:NNN` citation anywhere in this record shifted or needed re-derivation; this was checked, not assumed (no requirement row cites line 272, and no row's grep pattern matches the new line-272 wording).

**Zero rows FAILED on re-run.** All six re-verified rows remain PASS; three needed evidence updates (wording or line numbers), three were confirmed byte-identical.

### Two deferred minors closed

DM2 and DM3 (see the updated table above) are now CLOSED by this wave — F4 closes DM2, F5 closes DM3. The other seven deferred minors are untouched by this wave and remain OPEN as originally recorded.

## Coverage statement — what was verified how

**Tally: 47 rows (R1–R47), 47 PASS, 0 FAIL.** Every observed value in the rows above came from a command run against today's trees during this verification session; none was copied from the spec, plan, or ledger.

**Verified primarily by targeted reading** (the check is a read of specific prose, not a count — deterministic, but a human/model judgment that the text says what the row claims): R1(e) rule-6 wording, R3's two contract sentences, R5's invariant bullet, R6, R12's rule-2 text, R18's §4.1 text, R31's rewritten policy line, R36–R40's annotation content, R44's CLOSED note, R47's list. For each, the exact live text is quoted in the row so a reviewer can re-check the judgment without re-running anything.

**Backed by ledger claims I additionally re-verified mechanically this session:** R9 (V5 re-derived at HEAD = 7 files: create-ard, create-vi, epics, idea, update-vi, release-notes, specify; V8 = the 3 choices quoted); R12's "no phase move" (`document.md` Phase 8 with `emit-auto` at `:1035` still precedes `Phase 8.5 — Finish & handoff` at `:1054`); R18's consumer (`verification_warnings` genuinely consumed at `document.md:616-660`, Phase 5.8); R43's wiring (§11 step at `:385` runs the "§6.1 dominance trigger … and print its warning").

**Accepted from the ledger / task reviews without independent re-derivation** (named per the honesty rule — these are the record's residual trust points):
- R14: the four-state `/idea`×`/implement` deferral table's semantic agreement (Task 10 + its reviewer). I mechanically verified only the scoping sentence (normalized count 1, pre 0).
- R37–R40: the ~29 individual corrected values' re-derivations at historical ship commits (`9e663bf`, `04273a4`, `c7bdac2`/`fce902f`, `25b3628`/`c25eab7`/`8bc8862`, `4720e28`/`f64a69b`/`d404a75`). I verified every annotation exists, is dated, requirement-tagged, and commit-anchored, and spot-read ten blocks including all four mgd-parity corrections — but I did not repeat the historical `git worktree` checkouts at the four ship pairs. Task 13's fix-round re-review did, independently.
- R45: the overflow rule's byte-identity across Task 4's diff (`3d6abcb` vs `eb36aa3`) — Task 4 diff-proved it; I verified the live overflow text is bound-relative and fires (capped `[NEEDS CLARIFICATION]` at `idea.md:184-185`).
- R1: "zero content lost" from the deleted copilot README — Task 12's review read the deleted file in full from git history; I verified the two named unique passages landed in `SKILL.md` but did not re-diff the entire deleted file.

**Not verifiable mechanically at all:** nothing in scope. The closest is R47's `risk-planner`-exclusion *decision* — a judgment, recorded with its justification in the ledger and restated in the row; the mechanical part (4-name list, three editions) is verified.

**Check-form corrections made during this verification:** one — R15's literal `routing-policy-reachable` grep (0 on both trees, proves nothing) replaced with the discriminating "and Haiku;" probe. This is exactly the wrong-target class R37–R40 annotate; it is recorded rather than silently swapped.

STATUS: COMPLETE
