# Task 17 — Canonical residue sweep (phase-handoff gates)

Sub-project J (phase-handoff gates + PR-on-completion), branch `iv-gu/phase-handoff-gates`, baseline `fd7a147`. This is the Step-17 exhaustive sweep required by the plan's spec §7 (the ten numbered checks in Task 17 Step 1) plus the deferred residue candidates carried forward by Tasks 5/8/9/11/13, plus the Step-3 "what did I make false?" prose re-read. Every row below reports a verdict — `fixed` or `checked and correct` — never only the fixes.

## Part A — Spec §7 rows (Step 1's ten checks)

| # | Check | Expected | Observed (pass 1) | Verdict |
|---|---|---|---|---|
| 1 | Four-prefix authority residue (`design)/` and `(vi/ ard/ spec/ design/`) | 0 everywhere outside CHANGELOG; both anchor forms provably able to fail | CLEAN both patterns, outside CHANGELOG. Can-fail probe: `design)/` → 1, `(vi/ ard/ spec/ design/` → 1 on a synthetic probe file, confirming both patterns are live, not vacuous | checked and correct |
| 2 | Never-opens-a-PR family | 0 | The brief's literal command (no `\| grep -v CHANGELOG`) returns non-clean: `CHANGELOG.md:2`. Read the two matching lines (CHANGELOG.md:2, :792) — both are historical/changelog prose *documenting* the fix (the reworded capability-limit statements and a pre-existing Increment-3c entry), not residue. Re-run with `\| grep -v CHANGELOG` (matching checks #1 and #3's own convention) → CLEAN | checked and correct — **and a brief defect found**: check #2's own command omits the `\| grep -v CHANGELOG` filter that checks #1 and #3 both carry, so it flags legitimate CHANGELOG prose as if it were residue. Filed under "pattern matching prose instead of a dispatch" |
| 3 | `/create-vi` relocates `idea.md` | 0 outside CHANGELOG history | The broad `relocat` grep (correctly) returns 16 real hits (README ×4, phase-handoff.md, create-vi.md ×3, idea.md ×5, CLAUDE.md ×3) because relocation moved to `/idea` in Tasks 6/7 and is now described correctly in many places. Read every hit: all 16 either describe `/idea`'s own relocation or explicitly state `/create-vi` does **not** relocate (`"do not relocate, /idea already did"`, `"since /create-vi no longer moves it"`, `"/create-vi ... never relocating"`, `"never relocated"` ×2). Zero hits claim `/create-vi` relocates `idea.md` | checked and correct (read-every-hit style, like rows 5–6 — a bare match count is not the signal here) |
| 4 | `/idea` no-specs-deliverable | 0 | CLEAN | checked and correct |
| 5 | `/ready` never-commits family | read every hit | 6 hits in `ready.md` + 2 in `CLAUDE.md`. `ready.md:16` — commits `_readiness.md` only via §4.3 consent, branches only via that same consent (`ready/<KEY>-<slug>`), `specs-preflight` creates none. `ready.md:97` — same, re-stated for the branching-context section. `ready.md:482`, `:514` — Phase 7/7b's own additive sub-phases "NEVER commit their own output." `ready.md:575` — "NEVER auto-commit `_readiness.md`... offered at that same §4.3 choice, never automatic." `CLAUDE.md:217` — `/epics` never branches (unrelated to `/ready`, correctly a different command). `CLAUDE.md:243` — `/release-notes`'s own no-branch/no-commit invariant (unrelated command, correctly scoped). None of the 8 hits claims `/ready` auto-commits or auto-branches | checked and correct |
| 6 | `/ready` never-branches family | read every hit | Covered by the same 8 hits as row 5 (single combined grep per the brief). `ready.md:16` and `:97` both state the *only* branch `/ready` ever creates is `ready/<KEY>-<slug>`, and only behind the §4.3 consent choice at Phase 5's handoff — never unprompted, never at `specs-preflight` | checked and correct |
| 7 | `/design` worktree-vs-ref test | the ref test, not a worktree check | `design.md:3` (frontmatter) — "merged specification.md from the specs repo's main branch." `design.md:50` — "verified against `origin/<default>` by ref — never a worktree file-existence check," driven by `require-on-main`. `design.md:59` — the Epic-picker's own ref test, `git cat-file -e "origin/<default>:...specification.md" 2>/dev/null`, explicitly "never a worktree file-existence check." Both sites are ref-based | checked and correct |
| 8 | `/implement` `:718`'s claim scoping | 1 occurrence of "handed off separately"; no stale `:718` citation | `grep -c "handed off separately"` → 1 (implement.md:737). `grep -n ":718"` → no hits — Task 11's fix round already replaced the stale line-number citation with the stable phrase "Phase 7's never-commits-the-deliverable claim" (implement.md:597) | checked and correct |
| 9 | `ard-resolution.md` caller list | brief says "expect 5" | Actual: **6** — `` Cited by `/create-ard`, `/design`, `/implement`, `/specify`, `/epics`, and `/ready` `` (ard-resolution.md:4-5) | checked and correct — **and a brief defect found**: the brief's own "expect 5" is stale. Task 8 (progress.md:181-205) deliberately promoted `/create-ard` from a non-consumer to a 6th consumer, and Task 15's parallel-catalog sweep independently re-verified 6 in three mirrors (README:316/359, CLAUDE.md:255). The file is correct; the brief's expected value predates Task 8 and was never updated. Confirmed via `Execute \`require-on-main\`` case-insensitive imperative-form count: 7 files (create-ard, create-vi, design, epics, implement, ready, specify) — `ard-reviewer`'s consumer count of 6 excludes `/epics` (which gates a VI-level spec, not an ARD) and is otherwise consistent |
| 10 | `next-phase-offer.md` merge step | ≥1 | 1 — `next-phase-offer.md:35`: "A next-step offer that names a downstream command must also name the merge," with worked prose about `require-on-main` stopping on an open PR | checked and correct |

## Part B — Known residue candidates (carried from Tasks 5/8/9/11/13)

| # | Candidate | Verification | Verdict |
|---|---|---|---|
| 1 | `update-vi.md:26`/`:40` say the off-main grounding report sits "beside the existing divergence notice," but that notice is in Phase 2 (`:53`), not Phase 1 | Confirmed still present at both sites. The divergence notice (Jira-import vs. frozen-draft disagreement) is written in Phase 2 (`update-vi.md:53`, "surface a notable divergence to the user"), which runs *after* Phase 1's confirmation step — a Phase 1 report cannot sit "beside" output Phase 2 hasn't produced yet. Placement (both reports staying in Phase 1) was already correct per Task 9's review; only the cross-reference was wrong | **fixed** — removed the stale "beside the existing divergence notice" clause at both `:26` and `:40`; the off-main report now stands on its own in Phase 1 with no false adjacency claim |
| 2 | `implement.md`'s ARD clause attributes "unreachable when no ARD resolves" to `ard-resolution.md`'s No-regression rule section; it actually sits in the Output section, one heading above | Confirmed: the sentence "`unmerged` is reachable only when an ARD file resolves. An absent ARD is `none`, unchanged" lives in `ard-resolution.md`'s `## Output — the ARD context, or none` section (line 45), immediately before the `## No-regression rule (central)` heading (line 47) — one heading above, exactly as reported. `implement.md:236` cited "the no-regression rule" | **fixed** — `implement.md:236` now cites "`ard-resolution.md`'s Output section" |
| 3 | `docs-profile.md:194` ("Do not push, do not open the PR via any CLI") — should it get the same capability-vs-policy restatement the five other no-PR sites got? | `docs-profile.md` has **zero diff** on this branch (confirmed via `git diff main...HEAD`) — it is entirely outside sub-project J's scope; it operates on a docs/tooling repo path, never `$SPECS_PATH`. The five sites Task 5 fixed all shared one shape: an ambiguous absolute claim ("the plugin never opens the PR itself") that reads as global and became false once `phase-handoff.md` §2.6 shipped real `gh pr create` execution for the specs repo. `docs-profile.md:194`'s wording is structurally different — an imperative directive to the executing agent about *this step's own action* ("Do not push, do not open the PR via any CLI"), immediately followed by "present the branch name + the drafted message for the user to push and open themselves," which scopes it to the current action rather than making a claim about the plugin's behavior elsewhere. It is also *stricter* than `document.md`'s docs-repo flow (which does offer a `gh pr create` command as text on GitHub; `docs-profile.md` never even offers the command) — applying the five-site restatement verbatim would misdescribe it as looser than it is | **checked and correct — deliberately left unchanged.** Decided not to touch it: different repo class (docs/tooling, not `$SPECS_PATH`), different claim shape (scoped imperative, not an ambiguous global "never"), and a stricter policy than the pattern it would be borrowing |
| 4 | `ready.md:3`'s frontmatter compresses "never stops on an unmerged **or missing** artifact" into one phrase, implying both produce the same finding, when the body distinguishes unmerged (⚠, caps PARTIAL) from missing (❌, coverage gap) | Confirmed in the body (`ready.md:86`): unmerged (rows D/E) → one of three named ⚠ reasons capping the verdict at PARTIAL; missing (row F, `on_main: absent`) → recorded as **missing**, a distinct outcome, "exactly as before this feature." The frontmatter's single clause did not distinguish these. **Sharper finding while verifying**: `CLAUDE.md:254` (written by *this* branch, not pre-existing — confirmed via `git diff main...HEAD -- CLAUDE.md`) had the same compression, but worse: it cited `references/phase-handoff.md` §3.4 as the source of the PARTIAL-capping finding for "an unmerged or missing artifact" as a pair. §3.4 is *specifically* "Row F delegates" — the **missing**-artifact case, whose own table entry for `/ready` says "records the artifact as missing in its coverage roll-up," never PARTIAL-capping. The PARTIAL-capping behavior is rows D/E (unmerged, off-`<default>`) — a different table location, §3.3, not §3.4. `ready.md:86` itself gets this exactly right ("§3.4's `/ready` row — this is row F only, never rows D/E, which also read `absent`... but return `stopped: true`") | **fixed** — both `ready.md:3` and `CLAUDE.md:254` now name unmerged (§3.3 rows D/E, PARTIAL-capping finding) and missing (row F, §3.4, coverage gap) as two distinct outcomes with the correct section citations |
| 5 | A sibling task flagged `phase-handoff.md` §3.4's `/create-ard` row's closing generalisation ("Each passes `invariants` to its reviewer as `applicable_ard`") as possibly not describing `/create-ard`'s real mechanism | **The candidate's own file citation is wrong** — the closing generalisation lives in `ard-resolution.md`'s `## Consumers (informative)` section (line 74), not in `phase-handoff.md` §3.4 (which is Row F's delegation table for `/create-ard`'s *idea/VI* dependency — an unrelated mechanism). Read `create-ard.md` to settle the substance: Phase 0 (`:76`) inherits VI-level `AD-N` **read-only**; Phase 4 (`:116`) authors "list inherited VI-level ADs read-only and never contradict them"; the Phase 5 `ard-reviewer` dispatch (`:132-138`) passes only `ARD path` and `Scope` — **no `applicable_ard` field**, confirmed absent from `agents/ard-reviewer.md` too. `ard-resolution.md`'s own `/create-ard` consumer row already said this correctly ("`ard-reviewer` checks non-contradiction directly against the drafted file — no separate deviation-record path"), but the closing sentence generalised over all six consumers as if all six passed `applicable_ard`, which is true for only 5 (design, implement, specify, epics, ready — all confirmed via grep to pass `applicable_ard` to their reviewer/writer) | **fixed** — `ard-resolution.md`'s closing sentence now reads "The other five pass `invariants` to their reviewer as `applicable_ard`... `/create-ard` alone does not: it inherits VI-level `AD-N` read-only straight into its own grill and drafting (Phase 4), and `ard-reviewer` checks non-contradiction directly against the drafted file, never via that field." Also noting: the brief's own citation of "phase-handoff.md §3.4's /create-ard row" was itself the kind of stale reference this task exists to catch — resolved by reading the actual source, `ard-resolution.md` |

## Part C — Step 3: "what did I make false?"

Per-file findings for the frontmatter `description` of every command this branch touched, `README.md`'s reference list + command table, and `CLAUDE.md`'s invariant blocks.

### Frontmatter descriptions (10 commands touched by this branch)

Only `idea.md` and `ready.md` had their frontmatter text edited by this branch (confirmed via `git diff main...HEAD` scoped to each `description:` line); the other 8 (`create-ard`, `create-vi`, `design`, `document`, `epics`, `implement`, `specify`, `update-vi`) were re-read against their new bodies for staleness introduced indirectly.

| Command | Finding | Verdict |
|---|---|---|
| `idea.md` | "on a completed handoff it relocates `idea.md`... and opens a pull request for it" reads as a single conditional clause governing *both* verbs. The body (Phase 5, `idea.md:264`) shows relocation happens unconditionally once a Jira key resolves and the disposition is refined — **before** the §4.3 consent choice is even presented — while only PR-opening is conditioned on a completed handoff; declining still leaves `idea.md` relocated. README.md's own row 16 for `/idea` already carries the correct disambiguation ("declining leaves it relocated but not on the default branch"); the frontmatter lacked it | **fixed** — reworded to "once a Jira key resolves it relocates `idea.md`..., and on a completed handoff also opens a pull request for it... — declining leaves it relocated but not on the default branch" |
| `idea.md` | "opens a pull request for it" — checked against the established plugin convention for describing the PR-open capability probe (§2.6: attempt `gh pr create`, fall back to manual instructions on failure). `finish-and-handoff.md:74` (Task 5, reviewed and shipped) uses the identical certainty-toned phrasing ("the plugin does open the pull request... on a host with a CLI"), so this is consistent established shorthand, not a new overclaim | checked and correct |
| `ready.md` | see Part B row 4 | fixed (documented there) |
| `create-ard.md`, `create-vi.md`, `design.md`, `document.md`, `epics.md`, `implement.md`, `specify.md`, `update-vi.md` | Re-read each against its new body (require-on-main / handoff-to-main additions from Tasks 7, 8, 9, 10, 11, 12). None make a claim the new gating behavior falsifies — the frontmatter summaries simply don't mention the new gates (a compression choice, not an inaccuracy) except `design.md`, which already accurately says "Takes over a merged specification.md from the specs repo's main branch" (matches the new strict ref-based gate) | checked and correct (all 8) |

### `README.md` reference list + command table

| Site | Finding | Verdict |
|---|---|---|
| `references/ard-resolution.md` entry (`:316`) | Names 6 consumers (`/create-ard`, `/design`, `/implement`, `/specify`, `/epics`, `/ready`) | checked and correct — matches Part A row 9's corrected count |
| `references/specs-repo-git.md` entry (`:348`) | "seventeen commands," six-prefix `^(idea\|vi\|ard\|spec\|design\|ready)/` | checked and correct — 21 total commands − 4 exempt (`/api-guideline-reviewer`, `/guideline-reviewer`, `/statusline`, `/docs-profile`) = 17, confirmed by `ls commands/*.md \| wc -l` = 21 |
| `references/phase-handoff.md` entry (`:349`) | "Eight producers... seven consumers" | checked and correct — re-derived independently via case-insensitive `` `Execute \`handoff-to-main\`` `` (8 files) and `` `Execute \`require-on-main\`` `` (7 files); my first pass with case-sensitive `Execute` under-counted the consumers to 4 by missing lowercase "execute" in `implement.md`, `epics.md`, `ready.md` — self-caught and corrected, an instance of "a pattern crossing a case variant" from the nine-ways-a-check-misreports list |
| Command table row 16 (`/idea`) | "on a completed handoff (Phase 5) it relocates `idea.md`... — declining leaves it relocated but not on the default branch" | checked and correct — this is the version idea.md's own frontmatter was missing (now aligned, see above) |
| Command table row 17 (`/create-vi`) | "`<KEY>` alone derives `idea.md`... in-contract and gated via `require-on-main`... an explicit `@<path>` is out-of-contract... never relocated and never gated (`/idea` alone relocates)" | checked and correct |
| Command table row 21 (`/design`) | "Takes over a merged `specification.md` from the specs repo's main branch... hard repo gate" | checked and correct |
| Command table row 22 (`/ready`) | "an artifact resolved via `require-on-main`... but not yet on the default branch is recorded as a finding capping the verdict at `PARTIAL`, never a stop" | checked and correct — correctly scoped to the unmerged case only, does not conflate with missing (unlike the `ready.md:3`/`CLAUDE.md:254` defect found in Part B row 4) |
| "Architecture (ARD) consumption" section (`:359`) | Names the same 6 ARD consumers | checked and correct |
| `/epics` workflow-map entry — docs-grounder before `require-on-main` | `epics.md:126`'s docs-grounding runs in Phase 2, ahead of the Phase 2.5/2.6 gates, annotated in `CLAUDE.md:144` as "a deliberate exception to §5 rule 2's ordering." Verified against `phase-handoff.md` §5 rule 2 (a consumer gates "before its first subagent dispatch, code scan, **docs-grounding retrieval**, or grill question") — this genuinely is an exception, and it is labeled as such rather than silently violating the rule | checked and correct — deliberate, documented, matches Task 12/14's recorded decision |

### `CLAUDE.md` invariant blocks

| Block | Finding | Verdict |
|---|---|---|
| VI-creation-flow bullets (`:246-259`) | `/ready` bullet (`:254`) — see Part B row 4 | fixed |
| VI-creation-flow bullets | Producer/consumer counts, relocation-ownership bullets (`:252-253`), ARD bullet (`:255`) | checked and correct |
| specs-repo-git block (`:268-279`) | `:275` "never open a PR" — checked for contradiction against the newly inserted `:273` ("`references/phase-handoff.md` extends this reference... `handoff-to-main` DOES open a pull request"). Not a contradiction: `:268`'s heading scopes the whole block to `specs-repo-git.md`'s own two entry points (`specs-preflight`, `commit-artifacts`), and `:273` explicitly marks `phase-handoff.md` as a *diverging, extending* reference — the divergence is stated immediately above `:275`, not left implicit | checked and correct |
| workflow-relationships diagram (`:139-179`) | `/implement`'s `[handoff-to-main: escalated spec/design notes, when any]` claim | checked and correct — confirmed live at `implement.md:595` |
| Command/agent/hook counts (`:30`) | "twenty-one slash commands... thirty-three reusable subagents, four hooks" | checked and correct — 21 commands confirmed by directory listing (unchanged by this branch, which added/edited references and command bodies but no new command or agent files) |

## Part D — Fixes applied (summary)

1. `plugins/dev-workflows/commands/update-vi.md:26`, `:40` — removed the stale "beside the existing divergence notice" cross-reference (Phase 1's off-main report and Phase 2's divergence notice are not adjacent; placement was already correct).
2. `plugins/dev-workflows/commands/implement.md:236` — re-cited `ard-resolution.md`'s Output section instead of its No-regression rule section for the "unreachable when no ARD resolves" claim.
3. `plugins/dev-workflows/commands/ready.md:3` (frontmatter) — distinguished the unmerged (PARTIAL-capping finding) and missing (coverage-gap) outcomes instead of compressing them into one clause.
4. `CLAUDE.md:254` — same distinction, plus corrected the section citation (§3.3 rows D/E for unmerged/PARTIAL, §3.4/row F for missing/coverage-gap — the original cited §3.4 for both).
5. `plugins/dev-workflows/references/ard-resolution.md:74` — scoped "passes `invariants` to its reviewer as `applicable_ard`" to the five consumers where it's actually true; `/create-ard` inherits `AD-N` read-only into its own grill instead.
6. `plugins/dev-workflows/commands/idea.md:3` (frontmatter) — decoupled relocation (unconditional, once a Jira key resolves) from PR-opening (conditional on a completed handoff), matching `README.md`'s already-correct phrasing for the same fact.

`plugins/dev-workflows/commands/docs-profile.md` — reviewed, deliberately left unchanged (Part B row 3).

## Part E — Pass 2 (re-run after fixes)

| # | Check | Result |
|---|---|---|
| 1 | Four-prefix authority | CLEAN both patterns; can-fail probe still 1/1 |
| 2 | Never-opens-a-PR family (corrected form, `\| grep -v CHANGELOG`) | CLEAN |
| 3 | `/create-vi` relocates `idea.md` | Same 16 correct hits, content unchanged in substance, `idea.md:3`'s wording now also matches README's disambiguated phrasing |
| 4 | `/idea` no-specs-deliverable | CLEAN |
| 5–6 | `/ready` never-commits / never-branches | Same 8 hits, all still correct; `ready.md:3` now carries the distinguishing clause (does not match the row-5/6 grep patterns, unaffected) |
| 7 | `/design` ref test | Unchanged, still correct |
| 8 | `implement.md` `:718` scoping | `handed off separately` still 1; no stale `:718`; `implement.md:236` now cites "Output section" (verified present) |
| 9 | `ard-resolution.md` caller list | Still 6 (`Cited by` line unchanged — this task fixed the *closing generalisation* below it, not the caller list itself); closing sentence now correctly scoped to 5 of 6 |
| 10 | `next-phase-offer.md` merge step | Still 1 |
| — | `update-vi.md` stale cross-reference | `grep -n "beside the\|Beside the" update-vi.md` → no hits |
| — | `implement.md` no-regression mis-citation | `grep -n "no-regression rule"` in implement.md → no hits (only "Output section" remains) |
| — | Hard-wrap check on every edited line | All 6 edited lines confirmed single unbroken lines via direct read-back after edit; `ard-resolution.md`'s two-sentence replacement (previously wrapped across two source lines) is now one line |
| — | Producer/consumer counts re-derived | 8 producers / 7 consumers, unchanged and confirmed case-insensitively |

No new residue found on the re-run; all fixes hold.

## Part F — Nine-ways-a-check-misreports self-audit

- **Phrase that already existed** → not applicable to any fix landed here; each fixed sentence was demonstrably new/stale text (verified against `fd7a147` or the specific task's own commit range in `progress.md`), not a pre-existing phrase misattributed to this task.
- **Tool examining nothing** → ruled out; every grep/read above returned real content from the live tree, cross-checked against `git diff main...HEAD` for provenance.
- **Discrimination proof returning identical counts on both sides** → checked for row 1 (design)/`(vi/ ard/ spec/ design/` can-fail probe, 1/1 on a synthetic file, not 0/0).
- **Pattern matching prose instead of a dispatch** → found in the brief itself (check #2's missing `\| grep -v CHANGELOG`, Part A row 2) and self-caught in my own producer/consumer count (case-sensitive `Execute` under-counting consumers to 4, Part C).
- **Pattern crossing a line-wrap** → not applicable; every prose block touched follows the no-hard-wrap convention and was verified as one unbroken line post-edit.
- **Line-range diff falsely failing as a paragraph grew** → not applicable; no line-range diffs used in this task's checks (full-file/whole-tree greps and direct reads only).
- **Recorded line number invalidated by a later edit in the same task** → guarded against: `ard-resolution.md`'s closing-sentence edit was re-read fresh after landing (Part D item 5), not cited from memory; all `implement.md`/`ready.md`/`CLAUDE.md` line numbers in this report were re-derived from the current tree, not copied from `progress.md`.
- **Raw count identical on both sides while content moved** → checked for the ARD-consumer count (still 6 pre/post this task's fix, correctly — this task fixed the closing generalisation's *scope*, not the roster).
- **Pattern whose format was wrong (missing brackets a file actually uses)** → not applicable; no `[bracket]`-form patterns used in this sweep.

## Part G — Notes on the brief itself

- Check #2's literal command needs `\| grep -v CHANGELOG` to avoid a false non-clean result against legitimate CHANGELOG prose (Part A row 2). Not fixed in this file's own text since the brief is a historical task artifact, not a shipped reference — documented here instead, per the "report rather than comply" rule for a defective brief.
- Check #9's "expect 5" is a stale expectation; the correct, current, verified value is 6 (Part A row 9). The shipped file is correct.
- Residue candidate #5 (Part B row 5) cited the wrong source file (`phase-handoff.md` §3.4 instead of `ard-resolution.md`'s Consumers section); resolved by reading the actual file rather than trusting the citation.

## Commit

Committed on `iv-gu/phase-handoff-gates`. Files: this sweep file, plus the six fixed source files listed in Part D.
