# Example default owners, unioned into the pages a profile scopes the owners
# rule to (in the built-in example profile, self-hosted/_content/** pages).
# Both consumers read THIS file at its bundled path: the
# changelog-owners-reminder hook and the docs-frontmatter skill each resolve it
# under ${CLAUDE_PLUGIN_ROOT}. A profile's frontmatter.default_owners names this
# same bundled file: it is the value both of its writers, /docs-profile and
# /docs-init, are instructed to put there, and no consumer resolves it, so it
# redirects neither of them.
# One ID per line; blank lines and `#` comments are ignored.
docs-owner-placeholder
