{
  "description": "product-workflows plugin hooks — specs-context injection for /epics",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/preload-context.sh"
          }
        ]
      }
    ]
  }
}
