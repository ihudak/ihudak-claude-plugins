# Changelog

All notable changes to the **product-workflows** plugin are recorded here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Versions follow semver at the plugin level.
A section headed `— Unreleased` has not been published yet; where more than one of them stands, they all ship together in the next release.

## [3.8.3] — 2026-09-24

**Update `workflows-core` to 1.7.6 with this release**: its `grounding-format` §8 supersedes an on-file finding a verifier contradicts, and §5 supersedes one whose horizon moves; `/prd-ground` now writes both and `/brd-interview` reads them. Its `phase-handoff` §3.4 carries the `/prd-ground` and `/brd-package` stops below. An older `workflows-core` describes an in-place rewrite and row-F stops this release no longer takes.

### Upgrade notes — what an existing tree meets

- **A deferred re-put `[V]` question written by 3.8.1 or 3.8.2 carries no `- **Re-puts:**` line.** From this release on, the write that first records a `[V]` tag at a question's address writes the line, reading `none` where the question puts no decision again. That is the question's first write where it is written `[V]`, and otherwise the write recording its re-tag from `[G]` or its rewrite out of *untagged*. A question whose `[V]` tag is on file with no line is therefore exactly one an earlier version wrote. When `/brd-interview` resumes its round, it puts one tie picker per such question before the queue opens. The picker asks which reopened, held or reverted decision the question puts again, offering only the decisions that fit, or none. The answer is written as the line, so each question is asked about once. A `[V]` carrying the line, `none` included, is never asked about, and nor is a question the run itself tags `[V]`.
- **A self-review written before this release carries no `- **Re-escalates:**` line.** An answer to an `[SR#n]` finding of such a package is frozen fresh, as it was before, and is never tied to an answer the customer gave to the same finding in an earlier package. That is the safe direction: the worst case is a second record where one would have superseded the first. Packages built from this release on write the line where the operator confirms two findings are one.
- **An earlier `/brd-interview` run that was cancelled, aborted or interrupted may have left torn writes.** Before this release, the `[C]` entries and defect lines a run held were written to `interview/customer-questions.md` before its round record existed. A Phase 8 *Cancel* or an abort left them there, with no round record naming them. Such an item is now a torn write (`decision-register-format` §8), and no reader counts it. The next `/brd-interview` run reports each one in Phase 0 and removes it in Phase 9, before appending its own. A run that opens no new round removes them too, and reports what it removed. Until one does, `/brd-package` refuses the folder with `BRD_PACKAGE_TORN_WRITES`, naming each item. Where the slice kept no requirement of its own, `/brd-interview` refuses it, and the stop says so. Two leftovers are not torn writes and stand. A defect line appended to an entry whose round record predates both requirement-defect lines is counted under §8's reading of such a record. And an earlier version wrote an entry for each `[C]` of the round on every run, resumes included, so one `## Round <N>, question <p>` heading can head more than one entry; §8 counts each, so `/brd-interview` reports every such heading in Phase 0, and the operator reduces the entries to one by hand. A re-decision an interrupted run wrote onto a record already on file cannot be undone, so it stands. The round it belongs to, when next opened or resumed, records it as a question already disposed *decided* and puts it to nobody.
- **A package built before this release has no `Package reviewed` line in its prompt**, so a review of it cannot repeat one in section 1 (`customer-review-schema` §4 now requires it). `/brd-reconcile` then finds the package the review answers in this order: a date section 1 names that matches exactly one prompt on file, the only package on file, or else the operator's pick from the dated prompts. Where the operator cannot tell, no self-review is handed over, and every `[SR#n]` answer comes back `unmatched` for a human. Packages built from this release on print the line, and a review that copies it is matched on it.

### Fixed — behaviour

- **`/brd-interview` wrote held `[C]` entries and defect lines before the run was finished** (ii, N2, R23–R30). A Phase 8 *Cancel*, an abort or an interruption between Phase 7 and Phase 9 left entries in `interview/customer-questions.md` that no round record named. Phase 9 is now the only phase that writes a round's deliverables. It writes `code-defect-log.md`, then `decisions.md`, then `customer-questions.md`, then `interview/round-<N>.md` last. The round record is the commit point: an item stamped with a round whose record does not exist or does not name it is a **torn write**. `decision-register-format` §8 defines it once, and every reader cites it and never counts one: `/brd-interview`'s *asked* test and question generation, `/brd-package`, `/brd-reconcile`, `/brd-split`, `/prd-ground`, `/create-prd`, `/create-ard`, `/specify`, `/prd-proposal`, `brd-package-reviewer` and `proposal-reviewer`. `customer-review-reader` reads the files it is handed as they stand, and `/brd-reconcile`, its caller, applies §8 to what it returns. Every phase before Phase 9 holds its part in memory: the `[V]` answers, the held `[C]` entries, a defect line added to a held entry, a `--round N` re-open and a question the round-1 test adds. So a *Cancel* or an abort now writes nothing the run decided, and `docs/commands/brd-interview.md`'s "writes nothing" is true. A round record written before its `code defects:` and `requirement defects:` lines existed first gets a baseline line naming what was on file. A `[CDF#n]` re-disposition is applied after the round record names it, and the next run's Phase 0 completes one a stopped run left. `code-defect-log-format` makes a removed torn write the one exception to never reusing an id.
- **A `[CDF#n]` re-disposition one run completed could be left uncommitted** (M7, M8). Phase 0 completes a re-disposition an interrupted run left, which writes `code-defect-log.md`; a stop, an abort or a *Cancel* before this run's own handoff then left that write in the working tree. The handoff now stages the log wherever `git status --porcelain` reports it changed, and the step 9 report, the abort paragraph and the *Cancel* paragraph say it stays uncommitted until a run reaches its handoff. A run that opens no new round hands the log off too wherever it completed a re-disposition or found a change an earlier run left uncommitted.
- **`/brd-package` could ship a torn write, and inferred two files' merge from the register's** (N16). It now stops with `BRD_PACKAGE_TORN_WRITES` before the register gate, so that gate's remedy never lands a torn write first. A round record that exists but cannot be read stops the run with `BRD_PACKAGE_ROUND_UNREADABLE`. `interview/customer-questions.md` and `code-defect-log.md` ship in the bundle whole, and each is now gated with `require-on-main` where it is in the folder, stopping with `BRD_PACKAGE_SHIPPED_NOT_HANDED_OFF`. The round gate stops with `BRD_PACKAGE_ROUND_NOT_RECORDED` for a round whose record is not even in the folder, named only by a re-decision an interrupted run left. Its remedy names `/brd-interview`: once every round is closed, the round that run opens records each such re-decision, and while a round is still open it resumes that round, the re-decision being recorded in the round it opens once that one closes.
- **A decision reopened by another command had no question source, so it stayed `reopened` for good** (iii, N1, R31, R35, R62). `/brd-reconcile` reopens a decided `[VD#n]` or `[CD#n]` when a customer answer contradicts or constrains it without replacing it, and its propagation sweep reopens one in a dependent BRD. No `/brd-interview` source put the question again, so `/brd-reconcile` kept offering a round that asked nothing. A new source, *A decision reopened elsewhere*, puts every `reopened` record whose question no round still holds unanswered. It is put in the round the run opens, under the record's own tag, against the current findings, with each `Reopened` paragraph quoted as context. A record reopened while a round is open waits for the next round, and the run says so. *Every round is closed* and Phase 11's `nothing-to-review` no longer say that a decision another command moved raises nothing. Now a move to `reopened` raises its question, and a move to `superseded` or `withdrawn` raises nothing. Every question that puts a decision again, whatever its tag, carries a structured `- **Re-puts:** [VD#n]` (or `[CD#n]`) line, and every other `[V]` carries `- **Re-puts:** none`, written by the write that first records its `[V]` tag, so a question re-tagged to `[V]` gets it too. Every reader resolves a re-put through that line and never through prose. See the upgrade notes for a `[V]` an earlier version wrote without it.
- **A reopened decision a propagation sweep reverted while its re-put question waited had no exit** (R63). The sweep's *Reverted* restores a reopened `[VD#n]` to `open` or `decided`, and the question already putting it again then answered a record `/brd-interview` would neither re-decide nor supersede. Such a record is now superseded by the answer, as a record the will-change rule held is, `Superseded <YYYYMMDD>: by [VD#m]` naming the new record, and the tie picker counts it among its candidates. Where that supersession is itself a torn write, `decision-register-format` §8 now restores the `status` the record held before it, read off the record by §6, instead of a fixed `open`: `open` where it carries no `conditional_on` and every finding it rests on reads `horizon: will-change`, and `decided` otherwise.
- **`--round <highest+1>` could open a round beside one still holding unanswered questions** (R34). It now stops with `BRD_INTERVIEW_ROUND_STILL_OPEN`, naming the open round and the step each of its holding states needs, in the order they must run. The two deliberate concurrent openings stand: Phase 11's `defects-unasked` and a `--round 1` re-open. `/brd-reconcile` offers *Work another round* for a decision it reopened only where the bare `/brd-interview` run would move something (R32): where no round is open, and where the open round holds a *deferred*, *untagged* or *needs grounding* question, which that run resumes, naming `/prd-ground` first for the last. Where the open round holds only questions *held for the customer*, the offer is not made, and the reopened decision is named as waiting behind *Package again*. A record its propagation sweep reopened in a dependent BRD is named with `/brd-interview` on that BRD's key.
- **`/brd-interview` Phase 12's key events listed "a cancelled `[V]` queue"** (iv), which no path that reaches Phase 12 produces. The entry is removed.
- **`/brd-interview`'s legacy anchor saw only history reachable from HEAD** (xii, N3). A round record written before the `generated against:` line was compared at `git log -1 -- <record>`. That missed a record committed only on another branch, and could anchor on a squash commit whose grounding files already carried later changes. The anchor is now the earliest commit on any ref (`log --all`) whose blob for the record equals the record as it stands on disk, resolved against the whole candidate set. Where no candidate matches, every finding reads as unchanged, and the report says no commit on any ref holds the record as it stands. Where the matched commit itself changed a file under `grounding/`, the report names it as a commit that may postdate the record's write. The final report states the anchor's outcome either way.
- **A `/prd-ground` verifier `contradict` rewrote an on-file finding in place** (vii, D2, R68, R72). The rewrite reopened nothing, and a later `prior_verdict` recorded the rewritten verdict. Phase 7 now splits on origin. An own-run finding, which nothing cites yet, is still rewritten under the same id. An on-file finding is superseded, keeping its verdict as `prior_verdict`, and a successor with the next id carries the verifier's verdict, the same `claim`, `commit`, `altitude`, `horizon` and `prerequisite`, a `control` where it owes one, `consumed_by: none`, `outcome: contradict` and a `supersedes` note. `/brd-interview`'s successor tests hold for it unmodified. A class-4 `[DG#n]` citing a `[CG#n]` superseded this way is superseded by Phase 8's cascade, its note naming the run that replaces it: `--no-code` where the `[CG#n]` has a successor, and a plain re-run where it has none. As after a `--rebaseline` pass, citations into the old id still resolve, the coverage ledger's `evidence` column lists both ids, and a `consumed_by` stamp stays on the block it was written to. `decision-register-format` §4 cause 1 names the route. **A `contradict` whose finding owes a control and whose return carries no `own_control` writes no outcome at all.** On an own-run finding the run stops with the new `PRD_GROUND_VERIFY_INCOMPLETE` before Phase 8 writes anything, and fires `emit-block`. On an on-file finding nothing is superseded and no successor appended, the block keeps every field, and the Final report names it as not verified by this run.
- **A decision resting on several findings was reopened only when every one of them was superseded** (N12, R15). `decision-register-format` §4 cause 1 fires on any superseding finding. *A decision the re-grounding moved* now takes a record where **any** `evidence` finding reads `SUPERSEDED`. The confirmation test runs per superseded finding, a finding not superseded stands as cited, and the record is confirmed only where every superseded finding confirms.
- **Phase 6 moved an on-file finding's `horizon` and `prerequisite` in place** (N14, R18, R21). It now supersedes the finding and appends a successor with the same verdict, evidence and control and the new horizon. That successor is own-run, so Phase 7 verifies it. An on-file `[DG#n]` no frame set can place is superseded with no successor, and its note names the horizon Phase 6 would have written.
- **A design finding could lose its frame citations and so never be placed in a frame set** (N11, N15, R16, R19, R75). A `[DG#n]` rewritten in place, and any `[DG#n]` successor, keeps every frame citation of the evidence it replaces beside the verifier's own evidence. A held record resting on a superseded finding **no successor will come to** is put again at once, naming that finding and which of two cases applies, rather than waiting on a successor: it cannot be matched to one — its source cannot be decided, or it is a frame-only finding carrying no field token — or the finding, or one of its successors, each since superseded, carries the note `superseded: frame set <frame-set> re-ground`, which `/prd-ground`'s frame-set rule now writes on every block it supersedes. Any other finding with no successor yet waits, the class-4 finding the cascade retired among them, since its note names the run that re-derives it.
- **`/brd-interview` could not confirm a frame-only class-1 finding re-emitted by a later run** (R67, R73, R80). Successor test 3 matched a finding's `claim` by the requirement id it opens with, and a class-1 `[DG#n]` has none. `design-grounder` now writes a class-1 `claim` as `none — frame-only: <field>`, `<field>` being the label of the one unasked-for field exactly as the frame displays it, so two such fields on one frame are two findings. A successor of such a finding reads the same `claim`, token included, compared exactly, and cites the same frames: the evidence paths its set's index names equal the superseded finding's. A plain re-run that re-emits the finding therefore confirms the decision resting on it, and a finding about another field or frame never does. A legacy class-1 finding carrying no field token cannot be matched, so a decision resting on it is reopened, or put again where held, once; the answer is recorded against the current findings, which carry the token.
- **A plain `/prd-ground` re-run retired class-4 `[DG#n]` findings that nothing regenerated** (N13, R17, R65, R66). The frame-set supersession rule now retires a prior class-4 `[DG#n]` only where Phase 5 re-ground its claim against the repository its cited `[CG#n]` is pinned to, or under `--no-code`. Otherwise the finding stands, and only the cascade retires it. On a repository whose `HEAD` still matches its recorded pin, a plain re-run re-grounds only the claims no `[CG#n]` on file answers at that pin, a re-cut row among them, and each dispatch carries only the claims its repository still has to answer; a new `--depends-on` no longer re-grounds anything, since Phase 6 supersedes the on-file findings for their horizon. A frame set the design pass reconciled counts as re-ground whether or not it yielded a finding, so an agreeing re-run supersedes the set's prior class 1–3 findings rather than leaving them standing. Phase 7's opening set is now stated: every unsuperseded on-file `[CG#n]` pinned to a repository whose `HEAD` matched, on a plain re-run and a rebaseline alike, none under `--no-code`, and never an on-file `[DG#n]`.
- **A `/prd-ground` run that stopped after Phase 3 left its new pin recorded without its findings** (R74). Phase 3 appended `baselines.md` before Phase 5 and Phase 7 could stop, so a `--rebaseline` re-run then saw `HEAD` match the new pin and never superseded the old-pin findings. Phase 3 now holds each dated entry, and Phase 8 appends it after the two finding files, in the same write as the findings pinned to it; nothing is appended under `--no-code`. A run that stops in Phase 5 or 7 leaves the recorded pin as it found it, so a `--rebaseline` re-run supersedes the old findings, and a plain re-run stops with `PRD_GROUND_NEEDS_REBASELINE` as before.
- **`/prd-ground` Phase 8's field list** omitted `prerequisite` (viii) and wrote `consumed_by: none` on every block (N10). It now lists `prerequisite` on every finding reading `horizon: will-change`. `consumed_by: none` goes only on a block this run appends, and an on-file block keeps its stamps.
- **`/prd-ground`'s two Phase 0 gates had no stated order** (ix). The order changed which stop was printed. The ledger's gate resolves first, including whether the file is in the folder at all on row F, and the inventory's second. Where both stop, the ledger's stop prints, save one case: the ledger is in the folder and on no ref and the inventory is missing, where the inventory's `PRD_GROUND_NO_INVENTORY` prints. Under that order, rows of `PRD_GROUND_NO_INVENTORY`'s remedy table that could no longer be reached, and whose "no command has committed this slice's folder" was false where they could, are replaced (N9). That stop now prints only where the ledger was written, so a slice's inventory was written and lost. A parent re-run never re-creates it, so the remedy is to restore it.
- **`/prd-ground` could not tell an unfinished carve from a declined handoff** (xi, R4, R6). `/brd-split` writes a slice's three files before its walk places a row, so a carve stopped mid-walk leaves the same files as a finished one whose handoff was declined. `PRD_GROUND_NOT_HANDED_OFF` told the operator to commit them, and said a re-run was a no-op. Row F branch (b) now reads the parent's ledger first. Where the parent still holds an `unallocated` row, the new stop `PRD_GROUND_CARVE_UNFINISHED` says, by what the ledger shows and not why, that the slice's files are provisional. It says nothing in the folder is to be committed, and names the instructed `/brd-split <PARENT-KEY> "<how to cut it>"`. A `/brd-intake` re-run over the parent leaves the same state. `PRD_GROUND_NOT_HANDED_OFF` stands only where the parent is fully allocated and the slice agrees with it.
- **A slice out of step with its parent passed every `/prd-ground` gate and was grounded** (N5, R3). A carve cancelled mid-walk whose provisional files were committed by hand passed both gates, and so did a slice whose `claims:` named a `[BR#n]` its inventory did not hold. On the BRD route, once both gates pass, the run compares the slice's `claims:`, its inventory's rows and the parent's rows reading `covered-by: <this slice>`. Where they differ it stops with `PRD_GROUND_SLICE_UNRECONCILED`, naming each id by the set that lacks it, and grounds nothing. The remedy is the parent's instructed re-run where an `unallocated` row remains, the bare `/brd-split <PARENT-KEY>` otherwise, and no form where the parent's ledger cannot be read. The same stop now covers a `brd-link.md` that names no `parent:`.
- **`/prd-ground` took interrupted and damaged slice states for ordinary ones** (M5, M6, R77, NB3). A `PRD-` folder inside a BRD folder — a `BRD-` one, or a legacy unprefixed one §5.1's positive evidence places — with no `brd-link.md` was read as an idea-route PRD folder and grounded. Where it is empty, it is the folder a carve created and never linked, and it now stops with `PRD_GROUND_CARVE_INTERRUPTED`, whose remedy is to delete the empty folder and re-run the instructed `/brd-split`, never `/create-prd`. Where it holds anything, it is a slice that lost its `brd-link.md`, and it stops with `PRD_GROUND_LINK_MISSING`, which never names removal: it names the commit on any ref that wrote the file to restore it from, or, where none did, what the file must say. A slice's inventory or ledger that is on the default branch but missing from the working tree stopped with a remedy for a lost file; step 6a now stops with `PRD_GROUND_RESTORE_FROM_DEFAULT`, naming the restore.
- **`/prd-ground` had no stop for a slice's own inventory or ledger that is present and cannot be read** (area C re-review). `/brd-split` marks such a child unreconcilable, and `/prd-ground` passed it on to fail at Phase 8's ledger write. Step 6a now stops with `PRD_GROUND_SLICE_FILE_UNREADABLE`, naming the file and the error, and saying no `/brd-split` run repairs it.
- **`/brd-split` Phase 3 could propose a slice key an existing folder already carries** (NB3). A key now counts as used wherever an immediate subdirectory is named `PRD-<candidate>` or begins `PRD-<candidate>-`, linked or not, tested per candidate and never parsed out of a name, and an operator-supplied key that collides is re-prompted.
- **`/brd-split` reconciled only the children its own walk touched** (N7, R5, R7, R71). A stale provisional claim from an interrupted run survived. Once the parent was fully allocated, a bare re-run was a no-op, so nothing repaired a slice out of step. Phase 4 Step 3 now reconciles every standing child against the parent's `covered-by` rows, and writes nothing for a child already in step. A new reconcile path runs where no row is `unallocated` but a child stands empty or out of step: Step 3 first, then the empty-child resolution. Running Step 3 first gives back a claim to a child a cancelled carve left empty, so that child is not offered for removal. A child whose inventory or ledger is missing or unreadable is unreconcilable: it is reported and never reconciled, and it is never a walk's target, a re-cut's donor or a re-cut's receiver, the run naming it and its missing file beside each list that leaves it out. The run names `/prd-ground <CHILD-KEY>` as the command whose stops tell that child's states apart and name the repair — `PRD_GROUND_NO_INVENTORY`, `PRD_GROUND_NEEDS_SPLIT` and `PRD_GROUND_RESTORE_FROM_DEFAULT` among them. The *Cancel* paragraph says a slice's files stay provisional until Step 3 reconciles them, and that `/prd-ground` refuses the slice until then. A re-cut interrupted between its two ledger writes leaves the donor's row `deferred-to`; the next run's Step 3 leaves it standing, and §6.1 counts it as an orphan row through the parent. The command's `description:`, the docs page and `coverage-ledger-format` no longer call a bare re-run on a fully-allocated parent a no-op.
- **An ordinary `/brd-split` walk could assign a row to a child holding an orphan row for it** (N8, R11, R12). That is reachable after a `/brd-intake` re-run over the parent, and the child then claimed a row its own ledger gave to another owner. The walk now applies §3.2's per-row receiver exclusion to orphan rows: a child holding a row for a `[BR#n]` it does not claim is not offered that `[BR#n]`, and the walk says why. Nor is a child that still claims the `[BR#n]` while its own row reads `covered-by`, the trace of a withdrawal a re-cut began and never finished, nor an unreconcilable child. Every other child that still claims the `[BR#n]` is offered it whatever its own row reads, so an intake re-run strips no settled claim. `coverage-ledger-format` §2 names three routes to an orphan row, and §6.1 counts every orphan row through the parent's current disposition. §5.2's "never `covered-here`" is corrected: an orphan row left as the slice's own earlier decision can read `covered-here`, and the gate-set narrowing is what keeps it from making a slice eligible. The Final report of `/prd-ground`, `/brd-interview`, `/brd-package` and `/brd-reconcile` now counts orphan rows from all three routes, through the parent's current disposition.
- **`/brd-reconcile` had no disposition for a corrected resend answering a `decided` record the same way** (x, N4, D1, R37, R40, R43). Phase 4 now skips a candidate whose confirmed `chosen` equals the target record's and whose reason is byte-equal to the record's after whitespace collapse. The same applies to the same `chosen` with no reason stated, since a missing reason is not a different one. It is reported as *already reconciled, re-affirmed by* the review. It mints nothing, marks nothing answered and adds nothing to the propagation sweep. The skip covers `[C]`, `[AS#n]` and `[SR#n]` targets, and runs only on a confirmed mapping. Step 3's "replaces" now means an answer to the record's own question, whatever it chooses, so the same option with a different reason supersedes. The section-7 matching table, and `customer-review-reader`'s matching rule, describe a question whatever its entry's state, instead of reading as a filter on held questions, which had made the corrected-resend branch dead.
- **`/brd-reconcile` froze an answer against a record that was no longer live** (N17, N18, N19, R38, R39, R44, R45, R70). Every target now resolves to its question's live record: the earliest record frozen against it, followed through every supersession, `- **Re-puts:**` lines included, to the successor that stands. The skip and supersede rules compare against that record. An answer whose live record is `withdrawn` freezes nothing. A held entry whose `- **Re-puts:**` line names a record since withdrawn closes on the first review that answers it, whichever option the operator takes: *Record it for a human* closes it naming the withdrawal, and *Reject* closes it with the reason recorded. `/brd-package` renders only held entries, so no later package carries it. A candidate whose chain holds a record this same review froze is skipped, whatever its status now, so re-running an earlier review never reverts a later one's answer. A record frozen `open` for want of its reason is completed by the same `chosen` with a reason, skipped on the same `chosen` with none, and superseded by a different one. The same `chosen` with no reason never reaches the missing-reason picker. A different `chosen` with no reason, on a question an earlier review already answered, is not held again: its `open` record is named under *what still needs a human*, since no package re-asks it.
- **`/brd-reconcile` matched `[SR#n]` answers against the wrong package's self-review** (N20, R42, R46, R48). Every package numbers its findings from `[SR#1]` again. Now the reader is handed only the self-review of the package the review answers, resolved as the upgrade notes above describe, and never another. `/brd-package` prints a `Package reviewed: <BRD-KEY> <YYYYMMDD>` line into the prompt, and `customer-review-schema` §4 requires section 1 to repeat it. `customer-review-reader` returns it as `package_reviewed`. Where no package can be determined, every `[SR#n]` answer is `unmatched` and goes to a human. The reconciliation record names the package and how it was determined.
- **A finding re-escalated in a later package was frozen beside the customer's earlier answer to it** (N21, R47, R49). When a re-package's finding takes `escalated-to-customer`, `/brd-package`'s disposition gate checks it against earlier packages' escalated findings. A match needs the same class and the same single-record target, compared as whole values. Each match is shown beside the earlier finding's words. Only where the operator confirms the two are one finding does the self-review write `- **Re-escalates:** self-review-<YYYYMMDD>.md [SR#k]`. `/brd-reconcile` follows that operator-confirmed line to the earlier answer's live record. `brd-package-reviewer` now writes a target that is one record as that record's bracketed id alone, which is what makes the comparison mechanical.
- **`/create-prd` did not say whether a `prd.md` with no `kind: prd` counts as found** (v, D3). Step 6 now says any file at `<feature-folder>/prd.md` counts as found, whatever its frontmatter, as `/update-prd` treats it. It also says why `workflows-core:addressing` §5's three kind-gating commands differ: they consume a PRD's content, and `/create-prd` must not overwrite one. A `prd.md` that exists but cannot be read stops Phase 1 step 2 with `CREATE_PRD_PRIOR_UNREADABLE`, before any grill or write. `/create-prd`'s `CREATE_PRD_EPIC_FOLDER` remedy and `/epics`' `EPICS_NO_PRD` rows name `/update-prd` beside `/create-prd` where the folder's `prd.md` asserts no `kind: prd`.
- **Every `/create-prd` Overwrite lost the prior PRD** (N6, N22, R50, R52, R54). The three Overwrite labels promised "(archives the current one)", but no step took an archive, and Phase 5 wrote `prd.md` with no existence guard. Immediately before the run's first write to `prd.md`, now usually Phase 3's live authoring, the run copies an existing file into `revisions/` under `/update-prd`'s naming. This happens on every route, with no exemption, once per run. On the BRD route it also comes before the first write to `decisions.md`. A failed copy stops with `CREATE_PRD_ARCHIVE_FAILED` before anything is written. The archive path is staged with the PRD and reported. The Phase 3 pointer to "(Phase 0)" now names Phase 1 step 2.
- **`/update-prd` lost the prior revision on every update, and on a second update the same day** (N23, N24, R51, R53). The grill wrote `prd.md` live in Phase 3, before Phase 5's archive. The archive is now taken immediately before the run's first write to `prd.md`, once per run. Where the dated name is taken by any file, it uses the first free name with `-2`, `-3`, … inserted before `.md`, for both name forms. `revision_of:` records the name actually written. A base that exists and cannot be read stops with `UPDATE_PRD_BASE_UNREADABLE`, and a failed copy with `UPDATE_PRD_ARCHIVE_FAILED`, both before anything is written. `proposal-format` §2's archive rule takes the same first-free rule and cites `/update-prd` Phase 3, and `/prd-proposal` and `/brd-proposal` now hand off the archive at the name that rule actually wrote, suffix included, instead of the unsuffixed dated name; their docs pages say so.
- **`/epics` refused on files it read before the specs-repo preflight had settled the checkout's branch** (N26, N27, N29). Step 1a listed a BRD container's slices from their `brd-link.md`, and step 1b read `prd.md` for `EPICS_NO_PRD`, both before the preflight. So a stale plugin branch could hide a slice or make a merged PRD look absent. Step 1 now fixes the run key set from carrier frontmatter alone, including the parent's key on an Epic-level folder, and runs the preflight before step 1a or 1b reads anything. `EPICS_PRD_NO_REQUIREMENTS` fired only after Phase 1's questions and Phase 2's grounding. It now runs in Phase 0, right after step 1b accepts the run.
- **`/create-ard`, `/specify`, `/update-prd` and `/idea` refused on files they read before the specs-repo preflight** (R69). The same shape as `/epics`': `CREATE_ARD_BRD_NOT_SLICED` and `SPECIFY_BRD_NOT_SLICED` listed slices by their `brd-link.md`, `UPDATE_PRD_NO_PRD` and `UPDATE_PRD_BASE_UNREADABLE` read `prd.md`, `UPDATE_PRD_EPIC_FOLDER`'s remedy read the folder above, and `IDEA_NOT_AN_IDEA_FOLDER` read a slice's `brd-link.md`, `prd.md` and ledger, each before the preflight. `/create-ard`, `/specify` and `/update-prd` now fix the run key set from carrier frontmatter once resolution returns `found`, run the preflight, and only then place the folder and take their refusals; `/idea` runs it before it resolves the folder at all. In `/create-ard`, `/specify` and `/update-prd` an address that resolves to nothing still stops before the preflight, as before.
- **`/prd-proposal` and `/brd-proposal` ran the specs-repo preflight before resolving their address** (R81). With no key set stated, the preflight ran keyless, and could switch away from the slice's own unmerged `prd/` branch on an `@<path>` run. Each now resolves the address first (step 2), `@<path>` included, fixes the run key set from what resolution returned and carrier frontmatter alone — the resolved key, and on an Epic-level folder the key its parent's carrier asserts — and runs the preflight (step 3) before its container, slice or Epic refusal or any gate reads the folder; nothing between the resolution and the preflight reads the folder. An address that resolves to nothing stops with no preflight. The docs pages say so.
- **A slice's `coverage-ledger.md` that exists and cannot be read had no row in any remedy table** (M5). `coverage-ledger-format` §5.2 now says it is the same state as a missing one: its rows are `unresolved` (§6.2), neither data refusal can be evaluated, and a consumer names no option and reports the file by path with the read error. `/create-prd`, `/update-prd`, `/epics`, `/idea`, `/prd-proposal`, `/create-ard` and `/specify` widen their missing-ledger rows to it, and the docs pages of `/update-prd`, `/epics`, `/idea` and `/prd-proposal`, which name the missing case, name this one beside it.

## [3.8.2] — 2026-09-23

### Changed
- `docs/reference/model-routing.md`'s fallback chain now starts at `claude-opus-5-5`, matching `workflows-core` 1.7.5.

## [3.8.1] — 2026-09-23

**Update `workflows-core` to 1.7.4 with this release**: its `phase-handoff` §3.4 register row and `next-phase-offer`'s `/brd-reconcile` offers describe the `decisions.md` gate these commands now run; an older one says none of those offers carries `<merge-clause>`, its `grounding-format` defines no `prior_verdict`, the field `/prd-ground` now writes on a superseded finding and `/brd-interview` reads, and its `escalation-rules` does not list `/brd-reconcile`'s new conflicting-answer picker among the pickers whose free-text answer is normalised.

### Changed
- The design-spec citations in `/epics` and in the `brd-format`, `bundle-packaging`, `code-defect-log-format`, `coverage-ledger-format`, `customer-review-schema`, `decision-register-format`, `interview-tagging`, `linked-sources` and `proposal-format` references now say the spec was removed from the tree on 2026-09-23 and name the `git show 62e791e8:<path>` that retrieves it; the section and decision ids they cite are unchanged. Wording only, no behaviour.

### Fixed — behaviour

- **`/create-prd`, `/create-ard` and `/specify` read a BRD slice's `decisions.md` ungated.** A declined or unmerged handoff of `/brd-reconcile`, `/brd-interview` or `/create-prd` let a PRD, ARD or specification freeze against `[VD#n]`/`[CD#n]` records nobody had merged, and the stated reason for leaving the register ungated — that each command lands its deliverable and the next refuses to start until it is — was false at that hop. Each now runs `require-on-main` on the register in Phase 0 (`/create-prd` step 7a; right after the PRD gate in the other two): a stopping row stops `*_REGISTER_NOT_ON_MAIN`, a register in the folder but on no ref stops `*_REGISTER_NOT_HANDED_OFF`, and a register absent from the folder is reported and the run proceeds as before. `/brd-reconcile`'s offers of all three, `/create-ard`'s `/create-prd <SLICE-KEY>` option and `/specify`'s `/create-prd` recommendation now carry `<merge-clause>`. The command pages, `brd-workflow.md`, `workflow.md` and `docs/commands/brd-reconcile.md` describe the gate, and no longer say nothing downstream refuses an unsettled register. The same reason in `/create-ard` and `/specify` also covered the grounding files, and was false for them too: a `/prd-ground --rebaseline` run, which `/brd-reconcile` sends a challenged code claim to, rewrites them with nothing gating them before these commands. Neither command re-gates them — grounding is optional to both and read as evidence, which is now the stated reason — and Phase 2 reports a findings file it finds uncommitted.
- **`/brd-reconcile` applies the will-change rule (D19) before freezing a `[CD#n]`.** It wrote every confirmed answer `decided` whatever its evidence, so a customer answer resting only on findings a prerequisite is about to falsify entered the register as settled. It now runs `/brd-interview`'s own test on each record it would write `decided`, a completed `open` record included. Where every finding carries `horizon: will-change` the answer is still recorded verbatim, as `conditional_on: <BRD-KEY>/<decision-id>` where every such finding names one prerequisite carrying its BRD key, and as `status: open` where they name more than one or any names an unqualified prerequisite — a bare id is never resolved or parsed into a key, and the value is named for a human — and it is listed under *what still needs a human*. A record held `open` this way closes its question as *answered by the customer*, so its round can close and the later `/brd-interview` round that is its exit can be proposed — unless a propagation sweep's citation pass, reaching it by a changed id it names, withdraws or reverts it first; a record open for want of its reason alone stays held. A later answer against a record the rule held open mints a new `[CD#n]` that supersedes it rather than being written onto it as a missing reason, and a re-run over the same review skips it as already reconciled. `decision-register-format` §6 and its opening consumer paragraph, `docs/commands/brd-reconcile.md` and `docs/reference/references.md` say so.
- **A settled decision a re-grounding moved was never reopened, and the will-change rule's exit did not exist.** `decision-register-format` §4 named a superseding finding as a cause that may reopen a `decided` record, and nothing reopened one: a record whose every finding a `--rebaseline` pass superseded stayed `decided` silently. And a `[VD#n]` the will-change rule held — `open`, or `conditional_on` its prerequisite — had no exit a question source raised once the prerequisite shipped, the `[CD#n]` this release's `/brd-reconcile` holds the same way none either. `/brd-interview` now has one source for both (*A decision the re-grounding moved*). It takes a record every `evidence` finding of which reads `SUPERSEDED` — never one whose `evidence` names no finding (`evidence: []`, or any `[AS#n]`), and never a `[CD#n]` frozen `open` for want of its reason, whose question still travels in the next package. **A superseded finding's successors** are read off the grounding files: a finding of the same prefix, not itself `SUPERSEDED`, whose `claim` opens with the same requirement id, grounded against the same source — for a `[CG#n]` the repository whose `grounding/baselines.md` entry records a pin equal to its `commit`, for a `[DG#n]` the frame set whose index names every frame its `evidence` cites — and minted after it; a test that cannot be decided is not passed, so a sibling repository's or frame set's finding on the same requirement is never a successor. **A held record** is put again once every finding it rests on is `SUPERSEDED`, each has a successor, and no successor still carries `horizon: will-change`: a `--rebaseline` pass supersedes every finding it re-grounds whether or not the prerequisite shipped, and keeps `will-change` on the successor until it does, so the observation that it shipped is a successor no longer `will-change`, and until then the record is reported as waiting on its prerequisite rather than put to the customer on every pass. **A plain `decided` record** is reopened, its `Reopened` paragraph naming the successor findings, unless the successors confirm its premise: every superseded finding carries the `prior_verdict` `/prd-ground` now keeps on it and has a successor, and every successor carries that verdict and the superseded finding's own horizon — so a successor that moved from `will-change` to `current` reopens it, the prerequisite having shipped. One mechanical test for a `[VD#n]` and a `[CD#n]` alike; a record whose successors confirm raises nothing, and one resting on a finding with no `prior_verdict` — superseded before the field existed — is reopened rather than confirmation assumed. The record's question is raised in the next round this run opens, under the tag it had, which never moves, against the successor findings; a re-put `[C]` carries a `- **Re-puts:**` line naming the record, and, for a held record, its still-open `settles` defect, which is how that defect reaches the customer a second time. What the answer does turns on the record's `status` as the answer's writer reads it — `/brd-interview` for a `[VD#n]`, `/brd-reconcile` for a `[CD#n]`, reading the register as it stood before that run wrote anything: `reopened` is re-decided in place; `open` or `decided` is superseded by the new record; `withdrawn` or `superseded`, terminal, does not move, and the new record is written beside it, both named under *what still needs a human*. Where one `/brd-reconcile` run's answers reach one record more than once, a `- **Re-puts:**` line naming it decides it. No other question source raises a question on those successors, whatever their verdict and horizon. *The will-change rule*'s *Defer it until the prerequisite ships* now says the question stays *decided* and its round closes, the record being what waits; it is not the *deferred* holding state. `decision-register-format` §4 and §6, `docs/brd-workflow.md`, `docs/commands/brd-interview.md` and `docs/commands/brd-reconcile.md` state the exits, and say a record written `conditional_on` also leaves by the propagation sweep, which may revert or reopen it in place, and that the sweep's citation pass reaches a held record of either kind that names an id its reconciliation changed — one held `open` included, which it may withdraw or revert. `decision-register-format`'s consumer paragraph and `docs/reference/references.md` said `/brd-reconcile` "writes the `[CD#n]` records — the only command that does", true until `/brd-interview`'s reopen wrote onto one; both now say it *mints* them, and name the reopen among `/brd-interview`'s writes.
- **Superseding a finding kept no record of the verdict it carried**, which the reopen test above needs to tell a re-grounding that came back as it stood from one that moved a decision's ground. `/prd-ground` now writes the finding's verdict as `prior_verdict` (`workflows-core:grounding-format` §2) wherever it marks one `SUPERSEDED` — the `--rebaseline` pass, the class-4 supersession beside it, the Phase 7 sweep's no-design-pass branch, a `--no-code` re-ground of a frame set, and a `contradict` whose re-derived verdict is `SUPERSEDED` — and `/brd-interview`'s test reads it from the grounding files for both registers, never from a `[C]` entry's `- **Findings:**` line, which stays for `/brd-package` to render and count and for `/brd-reconcile` to copy into a `[CD#n]`'s `evidence`. `code-grounder` and `design-grounder` listed `SUPERSEDED` among the verdicts they return; each now says it never emits one, a re-grounding run being what writes it with its `prior_verdict`. `/brd-split`'s and `/brd-interview`'s closed-field-set gates admit the field unchanged, since it is one of §2's. `decision-register-format` §4, `docs/brd-workflow.md` and `docs/commands/brd-interview.md` state the test, and `docs/commands/prd-ground.md` says a `--rebaseline` pass keeps each superseded finding's verdict as `prior_verdict`.
- **`/brd-interview` opened a later round with nothing to ask.** With every round closed it opened round `<highest + 1>` whenever a finding had been added or superseded, a verifier outcome had changed or a decision had moved to `reopened` or `superseded` since the last round closed — and a `--rebaseline` pass supersedes every finding it re-grounds, so a round opened after one could hold no question, and a round record with nothing in it reads as a round nobody finished. It now generates that round's questions first, from what changed (a new *A later round is generated from what changed* rule: the round-1 sources applied to each finding added, or whose verdict or verifier outcome changed, since the last round was generated, less every superseded finding and every successor the reopen source reads, plus that source and the requirement-defect test), and opens the round only where some source puts a question. Otherwise it opens no round and writes no round record, and reports each change and why it raised nothing — a re-grounding as *re-grounded, nothing moved*, naming each record its successors confirmed and each held record still waiting. A decision another command moved to `reopened` or `superseded` raises nothing by that move alone, so `/brd-interview`'s *Work another round now* option, which said to take it "only if findings or decisions have changed", and its nothing-to-review report, which named a decision reopening as a way a round becomes askable, drop the decisions, and `/brd-package`'s `BRD_PACKAGE_NOTHING_TO_REVIEW` no longer says a decision reopened or superseded in `decisions.md` makes a round askable as a `--rebaseline` does. `docs/commands/brd-interview.md`, which said a run finding nothing changed proposes no new round, and `docs/commands/brd-package.md`, which said a decision reopened or superseded in the register can make a round askable, say so.
- **`/brd-interview` anchored what had changed on the last round's record, and so lost a finding changed while that round was open.** It proposed a later round from a finding added or superseded "since that round's record was written", or "since the last round closed", but a round's record is re-written by every run that works it: a finding a `--rebaseline` pass added or changed while that round was still open predated both its last write and its closure, and no later round ever asked about it. The first write of every round record now carries one `generated against:` line — every finding on file under `grounding/` not reading `SUPERSEDED` when the round's questions were generated, each with the verdict and verifier outcome it then carried, or `none` — which no later write adds or edits, and the *Every round is closed* branch compares the grounding against the last round's line: a finding on file that the line does not list and that does not read `SUPERSEDED` was added, one whose verdict or outcome differs from the line changed, and one the line lists that now reads `SUPERSEDED` was superseded, whenever the change happened. A round record written before the line existed is compared at its last write instead — the grounding files as they stood at the commit that last wrote it — which still misses a change made while that round was open, and reads every finding as unchanged where no commit carries the record. *A later round holds only what became askable* said "once the previous round was answered", twice; it now says after the previous round's questions were written. `/brd-reconcile`, which appends to a round record, said only that it never rewrites what the round asked; it now says every line an earlier write put there, the `generated against:` line among them, stands as written. `/brd-package`'s two citations of that test and `docs/commands/brd-interview.md`'s `--round` entry name the new anchor, and that page's round-record entry documents the line.
- **`superseded` named its replacement in no field.** `decision-register-format` §3 said a superseded record names the decision that replaced it, and no writer had a form for it. §4 now fixes a closing `Superseded <YYYYMMDD>: by [XD#m]` paragraph appended to `argumentation`, beside `Reopened`, `Reverted` and `Withdrawn`, the replacement always a record of the same BRD's `decisions.md`, and says superseding moves `status` and adds that paragraph and nothing else. `/brd-interview`, for a `[VD#n]` the will-change rule held, and `/brd-reconcile`'s steps 2 and 3, for an `[AS#n]` and for a `[VD#n]` or `[CD#n]` a later customer answer replaces, write it, and `bundle-packaging`'s list of plugin-written paragraphs a quotation never includes now names it.
- **`/brd-reconcile` reopened any record a frozen answer contradicted or constrained.** Its *Freeze the customer decisions* step 3 wrote `status: reopened` on every record in the slice's register the answer contradicted or constrained without replacing, but `reopened` follows `decided`: a `[CD#n]` frozen `open` for want of its reason, one the will-change rule held `open`, one already `reopened`, and a `superseded` or `withdrawn` one cannot take it. Now only a `decided` record is reopened there, and a record the answer replaces is superseded — one the will-change rule held `open` by any answer to its own question, and a `reopened` one by an answer whose entry does not name it on a `- **Re-puts:**` line, as a corrected resend's does not — while one frozen `open` for want of its reason is completed by an answer to its question, minting nothing, and a `reopened` one a `- **Re-puts:**` line names is re-decided in place. A record not `decided` that the answer contradicts or constrains without replacing keeps its status and is named, with the constraining `[CD#n]`, under *what still needs a human*; a terminal record takes nothing; and one the rule wrote `conditional_on` its prerequisite is `decided` and may take either. Where one run's answers reach one record more than once, the outcome does not depend on the order the candidates are taken in: a `- **Re-puts:**` line naming the record decides it by the status it held before the phase, whatever the other answers do; where no entry names it on such a line, the answer replacing it decides it by that status — superseded by that `[CD#n]`, completed by it where the record was frozen `open` for want of its reason, and nothing where it is terminal; and two or more answers that only contradict or constrain a `decided` record reopen it once, its `Reopened` paragraph naming every one. The reconciliation record's *what changed* lists every `[CD#n]` completed from `open` and every record step 3 left at its status, and its *what still needs a human* every record left at its status, every terminal record a `- **Re-puts:**` line named and every record reached more than once, with every `[CD#n]` that reached it. `docs/commands/brd-reconcile.md` says so.
- **`/brd-reconcile` could freeze two customer answers to one question.** Nothing stopped two candidates of one review, each confirmed onto the same held `[C]` question, `[AS#n]` or escalated `[SR#n]`, from both being frozen, which left *Freeze the customer decisions* two answers replacing one record with nothing to choose between them. *Confirm every candidate* now shows a candidate confirmed onto a target another candidate of the walk already took beside that one, and puts a conflicting-answer picker — freeze this one, keep the earlier one, or ask the customer and freeze neither — triggered by the shared target whether or not the reader flagged a `conflict`. The candidate set aside is not frozen and moves nothing, Phase 5 mints no `[CD#n]` for it, and the reconciliation record names it under *what still needs a human* with the customer's words and the candidate frozen in its place (or that the one kept went back to the customer for its reason); a third candidate onto the same target is put against the one kept, and where none was kept a later candidate goes back to the customer with it; the picker is put before the missing-reason picker, and a candidate that picker already sent back to the customer triggers nothing. `workflows-core` 1.7.4's `escalation-rules` lists the picker among those whose free-text answer is normalised, and `docs/commands/brd-reconcile.md` says so.
- **`/brd-reconcile`'s propagation sweep never swept the dependents of a `[CD#n]` it reopened or superseded, or of a `[VD#n]` it superseded.** Its changed-id set named every `[CD#n]` frozen and every `[VD#n]` reopened in the run, so a `[CD#n]` its step 3 reopened or superseded — and one a `- **Re-puts:**` line re-decided in place, or an answer completed from `open` — and a `[VD#n]` a customer answer superseded moved without a dependent BRD being told. The set now names every `[CD#n]` frozen, completed from `open`, re-decided in place, reopened or superseded in the run, and every `[VD#n]` reopened or superseded.
- **`/brd-reconcile`'s propagation-sweep picker offered writes a dependent's record could not take.** It offered *Reopened* on any `[VD#n]` or `[CD#n]` item, though `reopened` follows `decided`, and all four dispositions on a `superseded` or `withdrawn` record, which is terminal. *Reopened* is now unavailable on an item whose record is not `decided`, the item being named under *what still needs a human* where the change is something it must be decided against; and an item whose record is `superseded` or `withdrawn` is not presented at all — its sweep row is written `inherited-unchanged`, the record terminal and nothing reaching it, and it is named under *what still needs a human* where the change would have moved it. The reconciliation record's *what still needs a human* list, which the sweep already told the run to name an item in where the array dropped the disposition it would have taken, listed no such item; it now lists those and the terminal items, each with the dependent's record, the changed id or prerequisite that reached it, and what the register cannot carry — for a withheld *Reverted*, the earlier position the record no longer preserves or the findings the will-change rule fired on.
- **`/brd-reconcile`'s reconciliation record listed nowhere a row the customer asked to be built that was still `unallocated`.** *Update the coverage ledger* names such a row as `/product-workflows:brd-split <BRD-KEY>`'s — a `--sent` review of a slice never interviewed can find one — but the record's *what still needs a human* listed only a row that already carried a fate. It now lists the unallocated row too, naming `/product-workflows:brd-split <BRD-KEY>` as the run that allocates it.
- **`/brd-reconcile`'s propagation sweep could revert a dependent's decision onto will-change ground unresolved.** `decision-register-format` §4 writes `evidence` and `conditional_on` under §6's will-change rule, but the sweep's `reverted` write never tested the restored `evidence`. It now does, and where the rule fires on a restored position that carries neither a `conditional_on` nor `status: open`, *Reverted* is unavailable for that item and the item is named under *what still needs a human* — a reversion restores and decides nothing, so it cannot pick a resolution for the dependent.
- **The standing-empty-child remedy named a bare `/brd-split <PARENT-KEY>`**, which stops with `BRD_SPLIT_NEEDS_INSTRUCTION` whenever the parent still holds an `unallocated` row. `coverage-ledger-format` §5.2 and the tables in `/idea`, `/prd-proposal`, `/update-prd`, `/epics`, `/create-ard` and `/specify` now take the form from the parent's own ledger: with a slicing instruction where an `unallocated` row remains, bare where none does, and neither — the ledger reported by path — where it cannot be read. `/create-prd` step 7 gains the unreadable case, and `/create-ard`'s array splits in two. The command pages say the same.
- **`/prd-ground`'s `PRD_GROUND_NO_INVENTORY` on a slice named `/brd-split <PARENT-KEY> "<how to cut it>"` unconditionally, as the run that writes the slice's inventory from the rows the parent delegated to it.** A parent run's reconcile step touches a slice's inventory only for the rows its walk moves onto or off the slice, so the rows a slice already claimed are never written back, and on a fully-allocated parent that run carves nothing. The remedy is now a table on the slice's `claims:` and the parent's ledger: a slice claiming nothing takes `PRD_GROUND_EMPTY_INVENTORY`'s remedy; one claiming rows, with no `coverage-ledger.md` in its folder and none of those rows recorded `covered-by` it on the parent's ledger, was never written — a parent split interrupted after writing its `brd-link.md`, a folder no command has committed — and, since a parent re-run writes a slice's rows only for the rows it adds, is sent to remove that folder, in a commit on every branch carrying it where it was committed by hand, the default branch included, and re-run the split with an instruction where a claimed row is still `unallocated`, or to empty the left-over `claims:` by hand and re-run the parent in the form its ledger calls for where none is; one the parent's walk did write lost the file afterwards, and is told to restore it from the ref that carried it, since no parent re-run re-creates the rows it held; and an unreadable parent ledger names no `/brd-split` form. `PRD_GROUND_NEEDS_SPLIT` takes the same table, on two of its three reads — the third, whether a ledger stands in the slice's folder, being the one that stop has already answered: it had no remedy for a slice claiming nothing under a fully-allocated parent — a standing empty child, which a parent re-run does resolve — nor for an unreadable parent ledger. That stop also said the slice's inventory and ledger exist on no ref and in no folder, and that the slice's files were lost and should be restored, where it tests the ledger alone and the inventory may stand; it now names the ledger, says the ledger was lost with the inventory where that is missing too, and asks for what is missing to be restored. Its never-written row adds that removing the folder discards any inventory the interrupted run had copied into it, which loses nothing, the parent's inventory still carrying every row. `docs/commands/prd-ground.md` and `workflows-core`'s `phase-handoff` §3.4 row say so.
- **`/brd-interview` and `/brd-split` treated a slice with no inventory file as a slice with an empty one**, telling the operator the slice claims nothing. A `/brd-split` on the parent interrupted after writing the slice's `brd-link.md` leaves claims with no inventory, and so does a file lost after it was written. The absent case now stops with `BRD_INTERVIEW_NO_INVENTORY` or `BRD_SPLIT_NO_INVENTORY`, each naming both causes, whose remedy is `/prd-ground`'s `PRD_GROUND_NO_INVENTORY` table, cited.
- **`PRD_GROUND_EMPTY_INVENTORY` named a `/brd-split` form even where the parent's ledger could not be read**, and so did its copies in `/brd-interview` and `/brd-split`. Which form runs is that ledger's to say, so each now names neither there, and every remedy table handing that stop's remedy on inherits the case.
- **`/prd-ground` dispatched findings already reading `SUPERSEDED` to its verifier.** A `[CG#n]` a `--rebaseline` pass had superseded is pinned to a commit the repository has left, so the verifier stopped the run with `COMMIT_MISMATCH`, and the `--rebaseline` re-run that stop names dispatched the same finding again and stopped the same way; any other re-derivation returned a live verdict, which the `contradict` branch would write — a retired finding brought back beside its successor. Phase 7 now leaves every `SUPERSEDED` finding out of the dispatch set, whichever run superseded it, and out of the class-4 sweep, its outcome as the retiring run left it. `docs/commands/prd-ground.md` says so, and the command's description, that page and `docs/README.md`, which said it re-derives every finding, now say every live finding.
- **`/create-ard` and `/specify` stopped on a folder `workflows-core:addressing` §4.1 places at no level naming only the folder and what it carries.** Where that folder holds an `idea.md` and no `prd.md` — a legacy idea-route folder handed off before the kind prefixes — the stop now also names `/create-prd <KEY>`, whose `prd.md` places it (`workflows-core` 1.7.4's §4.1).
- **`/brd-proposal`'s readiness walk had no row for a slice whose proposal is current**, so a priced, current slice had no recommendation and no defined question. A fourth row includes it without asking, and the report names it as included rather than as the operator's decision. `docs/commands/brd-proposal.md` covers the stale and the current slice.
- **`/brd-interview`, `/brd-package`, `/brd-reconcile` and `/brd-split` had no refusal for an idea-route PRD folder** — a `PRD-` folder carrying no `brd-link.md`. The three route commands' root refusal accepts every `PRD-` folder, so `/brd-interview` reached an empty-inventory stop naming `/brd-intake`, or, where idea-route `/prd-ground` had written grounding, interviewed a folder with no ledger or inventory; `/brd-package` sent it to `/brd-interview`; `/brd-reconcile --sent` reached its inputs with nothing to reconcile into; and `/brd-split` read the folder as a root owning a source document. Each now stops before any gate — `BRD_INTERVIEW_NOT_A_SLICE`, `BRD_PACKAGE_NOT_A_SLICE`, `BRD_RECONCILE_NOT_A_SLICE`, `BRD_SPLIT_NOT_A_BRD` — deciding the folder by `/prd-ground`'s own `route: idea` test and naming `/create-ard` and `/specify` as the way that route goes on; `/brd-reconcile`'s also names `/update-prd` where the folder holds a `prd.md`, since `/update-prd` stops on one that holds none. Their docs pages, their capture-at-block lists and `brd-workflow.md` say so.
- **`/specify` staged the grounding files and the slice's `decisions.md` at bare paths on a per-Epic run**, where `feature_folder` is the `EPIC-` subfolder and those files sit in the PRD folder above it, so the `consumed_by` stamps went uncommitted. It now names them at `prd_dir`. `/create-ard`'s Epic-level run staged the slice's register the same way, and now names it at the slice folder.
- **`/brd-intake` took any folder its key resolved to for a re-run over a BRD.** `resolve-address` searches every level, so a key naming an idea-route PRD folder or an Epic folder resolved there, and only a slice was refused. Neither holds an inventory, so the re-run confirmation was skipped silently and Phase 2 would copy the customer's document into it, with an inventory and a coverage ledger written beside its `prd.md` or `epic.md`. Phase 0 step 7 now places a found folder as `workflows-core:addressing` §4.1 does and runs only on a root: an Epic folder stops with `BRD_INTAKE_EPIC_LEVEL`, and a `PRD-` folder no BRD carved, or a folder §4.1 places at no level, with `BRD_INTAKE_NOT_A_BRD`, before anything is copied, each leaving the folder as it stands and asking for an unused key. `docs/commands/brd-intake.md` and `brd-workflow.md` say so.
- **`/brd-interview`, `/brd-package`, `/brd-reconcile` and `/brd-split` had no refusal for an Epic folder.** Their level test read only the `BRD-` and `PRD-` prefixes, so an `EPIC-` folder passed it and the idea-route test, then reached a row-F remedy written for a BRD — `/brd-intake` over the Epic from `/brd-interview` and `/brd-split`, `/brd-interview` from `/brd-package` — or, under `/brd-reconcile --sent`, went past the gate with nothing to reconcile into. Each now stops before any gate with `BRD_INTERVIEW_EPIC_LEVEL`, `BRD_PACKAGE_EPIC_LEVEL`, `BRD_RECONCILE_EPIC_LEVEL` or `BRD_SPLIT_EPIC_LEVEL`, tested on the `EPIC-` prefix or, with no prefix, a resolved `kind: epic`. The remedy names the command on the slice above the Epic where there is one, and no command where there is none. The capture-at-block lists, the four command pages and `brd-workflow.md` list them.
- **`/create-ard`'s Epic-level run never read or stamped the grounding it was built on.** Its `grounding/` read, theme seeding, `consumed_by: ARD` stamps, staged paths and final report all tested the resolved folder, which on an Epic-level run is the `EPIC-` folder, and `/prd-ground` never grounds one. They now name `prd_dir`, the PRD folder above the Epic (the slice, on the BRD route), which is how `/specify` stages the same files. The optionality gauge alone still sizes the resolved folder, since it sizes the Epic and not its PRD. `docs/commands/create-ard.md` says so.
- **`/specify` addressed at an Epic never read or stamped the grounding it was built on.** Its `grounding/` read, theme seeding, `consumed_by: specification` stamps, staging condition, Phase 1 display and final report all tested the resolved folder, which on `/specify <EPIC-KEY>` is the `EPIC-` folder — and `/prd-ground` refuses an Epic, so none ever holds a `grounding/`. Phase 0 step 1 now defines `prd_dir` — the resolved folder on a `PRD-` address, the folder above it on an `EPIC-` one — as `/create-ard` does, and every one of those sites, and the BRD route's reads of the slice's `spec-seed.md` and `grounding/baselines.md`, names it. A per-Epic run reached through the picker on a PRD address was unaffected. Phase 0 step 0 also said the BRD route is a `PRD-` folder carrying `brd-link.md` "and nothing else", beside step 3's rule that an Epic addressed inside a slice is on the BRD route; it now names that Epic and reads the route off `prd_dir`.
- **Four stops sent an operator to re-run `/brd-intake` over a folder `/brd-intake` refuses.** `/brd-interview`'s no-parent `BRD_INTERVIEW_EMPTY_INVENTORY`, `/prd-ground`'s no-parent `PRD_GROUND_NO_INVENTORY` and `PRD_GROUND_NEEDS_INTAKE`, and `/brd-split`'s full-mode `BRD_SPLIT_EMPTY_INVENTORY` on a folder carrying neither BRD file all named that re-run, while `/brd-intake` now re-runs only over a BRD container and stops a folder carrying neither file with `BRD_INTAKE_NOT_A_BRD`. Each such stop now names the folder and what it carries, and a fresh intake under a new key — and, where the folder holds an `idea.md` and no `prd.md`, the legacy idea-route folder `workflows-core:addressing` §5 resolves on its name, `/create-prd <KEY>` too, which accepts that folder and writes the `prd.md` a re-run then routes on, as `PRD_GROUND_NEEDS_PRD` and `BRD_SPLIT_NOT_A_BRD` already do for a `PRD-` folder in the same state; `/brd-split`'s full-mode stop keeps the re-run for a container. The justification that such a folder "may be a legacy root BRD whose intake was interrupted" no longer decides the remedy: since 3.7.0 `/brd-intake` writes the inventory's header before it copies anything, and a folder an earlier intake left with `brd/source/` alone carries neither BRD file and is refused by `/brd-intake` too, so the remedy is the same. The stop classifications of `/brd-interview` and `/prd-ground` now file those no-parent forms — and `/brd-interview`'s key, level and idea-route refusals with them — as argument halts, a key naming the wrong folder — `/prd-ground`'s save on a legacy folder holding only an `idea.md`, whose missing input is the PRD. `docs/commands/brd-interview.md`, `docs/commands/prd-ground.md` and `docs/commands/brd-split.md` say so.
- **`/prd-ground`'s `PRD_GROUND_INVENTORY_NOT_HANDED_OFF` told the operator to commit and merge an inventory whose handoff was never offered.** A `/brd-split` on the parent interrupted after writing the slice's `brd/brd-inventory.md` and before its `coverage-ledger.md` leaves exactly that folder, and the stop called it a declined handoff. The stop now fires only where a `coverage-ledger.md` stands in the folder too; otherwise the run takes the ledger's branch, whose never-written row disposes of the inventory. The branch headings for a missing inventory or ledger now say *never produced, or lost after it was written*, and row F is described as conflating three states, not two; `docs/commands/prd-ground.md` and `workflows-core:phase-handoff` §3.4 say the same.

### Fixed — claims the wider exclusivity probe found false

- `/brd-reconcile` Phase 14 said each option "carries its own condition in its own text", `/create-prd`'s the one dropped rather than annotated. Every re-entry option whose trigger did not fire is dropped too; it now says an option whose condition does not hold is dropped, never annotated, and `docs/commands/brd-reconcile.md` agrees. That page also said nothing in the route runs after `/brd-reconcile`: a reopened decision goes to `/brd-interview`, a held question to `/brd-package`, a challenged claim to `/prd-ground --rebaseline`.
- `/brd-reconcile` said a review of a hand-authored package "can never be reconciled by any route". `--sent` exists for exactly that case.
- `docs/commands/brd-reconcile.md` said the command runs again on the same review only once a recorded-not-written dependent has its register on the default branch; a run cancelled or stopped partway is re-run on the same review too, skipping what it already froze.
- `/brd-reconcile`'s propagation sweep said any BRD at any level may declare `depends-on:`, "so … a BRD depending on a sibling" is found. Only a slice declares it — `/prd-ground`, `/brd-package` and `/brd-reconcile`'s own correction phase write the field and all three refuse a root — while the key it names may sit at any level; the scan still searches every level for a hand-edited root. The same passage said a child declares "never `depends-on:`", which a slice's own `--depends-on` falsifies; what is true is that a child is carved with no `depends-on:` naming its parent. `/brd-package`, `docs/commands/brd-package.md`, `docs/commands/brd-reconcile.md` (which also said "at either level") and `docs/brd-workflow.md`'s Parameters table, which said `/brd-package`'s `--depends-on` is repeatable "at either level" in the cell saying only a slice is packaged, are corrected with it.
- `/brd-reconcile`'s propagation sweep said "two of the four" dispositions are unavailable on some items and the array is presented "without the one that is"; on an `[AS#n]` whose earlier position a re-decision overwrote, *Reopened* and *Reverted* both are. It now says the array is presented without whichever of them is unavailable — one, or both — and that a terminal record's item is not presented at all. `docs/commands/brd-reconcile.md`'s list of withheld dispositions named `reopened` only on an assumption and `reverted` only where the earlier position is lost; it now also names `reopened` on a decision that is not `decided`, `reverted` where the restored evidence fires the will-change rule and the restored position carries none of its resolutions, and a terminal record's item. The page also said a withheld disposition's item is named for a human instead; it takes one of the others, and is named for a human as well where the withheld one is what it would have taken.
- `/brd-split`'s Handoff listed five readers of `brd-link.md` as if complete; eighteen commands name it. It now says "among them" and gives the `grep -l` recipe, which lists them. It also cited `commands/design.md` Phase 0, a `dev-workflows` file, now `/dev-workflows:design` Phase 0; said the empty-inventory stops stop on `claims:`, when they count inventory rows; and said an `allocate-only` handoff declares "exactly two" files, when the no-op path declares none.
- `/brd-split` said it runs "at either of the two levels `<BRD-KEY>` can name"; it runs on a root BRD or one of its slices, Phase 0 step 5 refusing an idea-route PRD folder and an Epic folder and step 8 stopping any other folder that is not a BRD container.
- `/brd-split` Phase 0 step 8 said that on a slice "step 6 already covers this before this step is ever reached". Step 6 stops an empty inventory only where grounding is on no ref; a slice ground before its parent's walk withdrew every claim reaches step 8, and the sentence now says what happens to it there.
- `/brd-intake` said three of `BRD_INTAKE_SLICE`'s five slots were substituted; the stop has six and four are. It also said the three seed consumers read their seed only out of the `PRD-` slice and that seeds on the root were read by nothing; each falls back to the parent BRD folder. And it said `/create-ard` and `/specify` on the BRD route derive `ard/<SLICE-KEY>-<slug>` and `spec/<SLICE-KEY>-<slug>`; that is a PRD-level run, and an Epic-level run under a slice derives `ard/<EPIC>-<eslug>` or `spec/<EPIC>-<eslug>`.
- `/brd-interview`'s no-parent empty-inventory stop warned against `/prd-ground` because it "stops on the same emptiness". `/prd-ground` refuses that folder before it reads any inventory, and the stop now says so.
- `/brd-interview`'s and `/brd-split`'s `*_MALFORMED_FINDING` stops, `/brd-interview`'s closing option offering `/prd-ground --rebaseline` and `/brd-package`'s `BRD_PACKAGE_NOTHING_TO_REVIEW` said a `--rebaseline` pass re-derives every finding, or the findings, against current commits. It re-grounds every claim, superseding the findings it replaces, and its verifier re-derives only live ones; each now says it re-grounds every claim against current commits.
- `/brd-interview`'s Phase 8 said its `Cancel` stops the run "with the decision still `open`, which is itself one of the three" resolutions, and Phase 6 that an abort leaves every decision already taken that pass written. Both stop before the register phase, the only writer of `decisions.md` and the round record, so nothing that pass decided is written and the next run puts the question again. Both now say so, and what is already on disk — a held `[C]` entry and a defect line appended to one, which no round record yet names — and how a round this run opened is generated again.
- `/brd-package`'s final report listed "the four artifacts written"; the handoff also lands `brd-link.md` when a prerequisite was added.
- `/create-prd`, `/create-ard` and `/update-prd` said two of a declined, a push-failed and a nothing-to-commit handoff open no pull request; none of the three does, and §4.1 lists other outcomes.
- `/create-prd` step 7's "Only the second case" and "In the second" pointed at the missing-ledger row, not the empty-gate-set row; its Phase 7 cost key "(or `brd_key`…)" implied a second value, and a commit-prefix note cited a retired "key the handoff has not yet minted". Pre-feature "as today" wording in `/create-prd`, `/create-ard`, `/specify` and `/epics` is reworded, and `/create-ard`'s and `/specify`'s handoffs drop "matching today's branch names", a leftover from before `handoff-to-main` named the branches.
- `/epics` said "there are no PRs to diff"; it has no implementation record and no commit to diff. `/update-prd` said it "reads no BRD tree"; it reads `brd-link.md` and the ledger to word its stops, and now says it reads no BRD tree for content. `docs/commands/update-prd.md` said the same twice, and is corrected with it.
- `/prd-ground` Phase 11's capture-at-block list omitted Phase 0 stops, and now states the rule first and marks the list as examples; step 2 said every other flag carries over on either route, when `--depends-on` is refused on the idea route.
- `/prd-ground`'s `PRD_GROUND_INVENTORY_NOT_HANDED_OFF` told the operator not to re-run `/brd-intake` because it "would rewrite the inventory and orphan the coverage-ledger dispositions". `/brd-intake` refuses a slice (`BRD_INTAKE_SLICE`) and any folder that is not a container (`BRD_INTAKE_NOT_A_BRD`), and every container is refused as a root before this stop, so the stop and `docs/commands/prd-ground.md` now say it names no `/brd-intake` run for that reason.
- `/brd-interview`, `/brd-package`, `/brd-reconcile`, `/brd-split` and `/prd-ground` said `resolve-address` searches "either of the two levels" — a BRD folder or a slice — and their `*_NOT_FOUND` stops said "(both levels searched)". `workflows-core:addressing` §3 bounds the glob at three levels, and each command's own Epic refusal exists because an `EPIC-` folder resolves. Each lead-in now names the three levels and every folder kind they return, and each stop names every level §3 bounds plus §5's legacy fallback, as `SPECIFY_BRD_NOT_FOUND` already did. `/brd-reconcile`'s dependents scan and slice-folder note, `/brd-split`'s note on a child's key, `brd-workflow.md`, `workflow.md`, and the command pages for `/brd-interview`, `/brd-package`, `/brd-reconcile`, `/brd-split` and `/specify` carried the same two-level claim and are corrected with them.
- `coverage-ledger-format` §5.1's consumer list of the positive-evidence container test omitted `/idea`, `/update-prd` and `docs-workflows`' `/document` (keyed) and `/release-notes`; it adds them, the §4.1 recipe, and `/brd-intake` and `/brd-split`, which take the test without refusing a container: `/brd-intake` through `workflows-core:addressing` §4.1 to accept a re-run, `/brd-split` to refuse the legacy idea-route shape and to word its full-mode empty-inventory stop.
- `bundle-packaging`'s table of qualified structured fields omitted `rejected: <SLICE-KEY>/[CD#n]`.
- `brd-workflow.md`'s Parameters table and `workflow.md`'s handover table omitted `--docs <path>` on every row whose command parses it — five commands, eight rows across the two tables; `brd-workflow.md` said the root key "has no further step of its own", when it still takes `/brd-split`, `/brd-intake`, `/frames` and `/brd-proposal`; and it and `docs/commands/idea.md` now say `/document` reads an image already in `attachments/` but puts new screenshots in `Doc screenshots/`, so `/idea` stays that folder's only writer.
- `docs/reference/resume-and-checkpoints.md` gave the terminal order without follow-ups, twice.
- `docs/commands/brd-interview.md` described its empty-inventory stop as one whose `/prd-ground` "would stop on the same emptiness", named re-intake with a corrected source over a no-parent folder, and `/brd-split` on the parent for every slice with a missing or empty inventory — no parent re-run re-creates the inventory of a slice that claims rows and has lost it — and neither it nor `docs/commands/brd-split.md` named the `BRD_INTERVIEW_NO_INVENTORY` / `BRD_SPLIT_NO_INVENTORY` stops for a slice with no inventory file. Both pages now name all three row-F outcomes and follow `/prd-ground`'s remedy table, and the interview page says `/prd-ground` refuses a no-parent folder before it reads any inventory.
- `docs/commands/prd-ground.md` said `PRD_GROUND_NEEDS_SPLIT` names `/brd-split` on the parent; it does only where that run can still change the slice. The page also named a `/brd-intake` re-run for a "BRD with a source document of its own" at three places no container reaches, sent the legacy no-ledger shape to an "interrupted-intake" branch as though a re-intake were its remedy, and never named `PRD_GROUND_NO_INVENTORY` or `PRD_GROUND_INVENTORY_NOT_HANDED_OFF`; `docs/commands/brd-split.md` named the full-mode `/brd-intake` re-run unconditionally.
- `docs/commands/specify.md` said everything about pickers, Epic counts and the resolved folder describes the keyed route only and that the BRD route "runs none of it"; Phase 2's Steps A and B run on every route, a slice being a PRD folder that can hold Epics. The page now says the route adds to them, and names an Epic inside a slice as a BRD-route address. Its grounding, stamping and `/epics`-offer sentences now name the PRD folder rather than the resolved folder. It also gave the BRD-route branch as `spec/<SLICE-KEY>-<slug>` for every run, where an Epic-level run lands on `spec/<EPIC>-<eslug>`; `/create-ard` and `docs/commands/create-ard.md` said the same of `ard/<SLICE-KEY>-<slug>` and of the slice folder as the feature folder, which an Epic-level run under a slice also falsified.
- `docs/commands/brd-intake.md` said a slice's `brd/` directory is the parent's, one hop up. A slice holds its own `brd/brd-inventory.md`, created by `/brd-split` on the parent; what is the parent's is the source and the records beside it, which is what `BRD_INTAKE_SLICE` itself says.

## [3.8.0] — 2026-09-22

**Update `workflows-core` to 1.7.3 with this release**: its `phase-handoff` register still lists the rendered `.html` among `/specify`'s handed-off artifacts until then — wording only, no behaviour.

### Changed

- **`/brd-reconcile`'s stale cross-reference sweep never edits an effort proposal.** The sweep reads every markdown file under the parent BRD, and a slice's `proposal.md` and `proposal-brief.md`, the umbrella's, and every archived revision of either were in that scope with nothing to stop a hit in their prose being `updated` in place — rewriting a figure's argument in a document the customer may already have been sent, with no archived prior and no record that anything moved. A hit there is now always `needs-a-human` and the file is never edited: the sweep's table carries a row for it, and *what still needs a human* names the fix — a re-run of `/prd-proposal <SLICE-KEY>` for a slice's proposal and `/brd-proposal <PARENT-KEY>` for the umbrella, the slice's first where both carry the hit, since the umbrella reads each slice's figures from that slice's own file; a hit only in an archived revision names no re-run, a revision being history. **Why**: a proposal has its own revision model. The next run of the command that wrote it archives the prior revision and classifies each change as a correction or a re-estimate, corrections listed first and never netted into a total (`references/proposal-format.md` §12) — which an in-place prose edit bypasses entirely. The *Apply the required corrections* phase carries the twin row: a package `/brd-package` builds carries no proposal, but a `--sent` package can, so a section-12 row asking to edit any line of a proposal, its brief or an archived revision is `refused-with-reason`, the file is never edited, and the row names the same re-run and goes to *what still needs a human*. `docs/commands/brd-reconcile.md`'s Phase 6 bullet, which had called `code-defect-log.md` "the one exception" whose prose is not corrected in place, now names the proposal as the second and adds its re-run to the channels a refusal names. `references/proposal-format.md` §0, `docs/roles-and-phases.md`, `docs/reference/proposal-format.md`, both proposal commands and their docs pages now name this scan beside the proposal's own-folder reads, as a prose scan and never a proposal read, and `docs/commands/brd-reconcile.md` adds the proposal to the sweep's never-edited list.

### Removed — `/specify` no longer renders an HTML copy of the specification

Phase 6 used to run `scripts/specification-to-html.py` over the finished `specification.md` and write a self-contained `.html` beside it, and Phase 7 staged that file with the rest of the feature folder. It is gone: the script is deleted, Phase 6 now opens on the `spec-reviewer` dispatch (its steps renumbered 1–2), and neither the files Phase 7 writes nor its `deliverable_paths` name an `.html` any more. **Why it goes rather than growing**: it rendered one of the roughly ten kinds of document this family writes into `$SPECS_PATH`, every one of which is markdown that the forge and the editors people read the specs tree in already render, and `workflows-core:phase-handoff`'s register had classed the file **unread** — handed off, with no reader found. A renderer for one kind was an inconsistency, and one for every kind would be a product nobody asked for. **Population: every `/specify` run.** Existing `.html` files in a specs repository are left where they are — nothing in the family reads them, and `workflows-core:source-truth` and `doc-planner` already ignore any rendered HTML mirror they meet. `specification-format.md` keeps its test-case micro-format and its open-questions heading depths, restated on their own grounds: both were justified by the renderer's parser, and both still bind, because `spec-reviewer`, `/design` and `/implement` read a test case field by field and heading depth is what places an open-questions block under its story or its criterion.

### Removed — `specification-format.md`'s provenance section

The section named the repository the format was first imported from and told a reader to re-sync the file from it by hand. That repository is not public, so the instruction was one no reader of this plugin could follow; the format is maintained here, and `specification-format.md` now says nothing about where it came from. `dev-workflows`' `design-format.md` drops the same comparison (4.2.1).

### Fixed — behaviour

- **`/idea` wrote its deliverable under `/specifications/` at the filesystem root when `$SPECS_PATH` was unset** (behaviour). Nothing checked the variable before `resolve-address` and Phase 4's write, and an empty value put `idea.md` under `/specifications/`. Phase 0 step 1 now stops first with `choices: ["Set SPECS_PATH (enter the path)", "Cancel"]` (`workflows-core:escalation-rules` *Required path environment variable unset*). `docs/commands/idea.md` said the variable was "not needed to start the run at all" and that an unresolvable path was "a silent no-op rather than a stop". It now lists the variable as required and states the stop.
- **`/create-prd` and `/update-prd` accepted an Epic folder** (behaviour). `resolve-address` searches every level, the Epic level included. On an `EPIC-` folder, `/create-prd` took the idea route and wrote `prd.md` into the Epic folder, below the PRD it was split from. `/update-prd` found no `prd.md` and named `/create-prd` on the same address, which then did the same. Population: every run of either command given an Epic key whose `EPIC-` folder exists. `/create-prd` step 5b now stops with `CREATE_PRD_EPIC_FOLDER`, and `/update-prd` step 3a with `UPDATE_PRD_EPIC_FOLDER`. Both test the directory prefix, or a resolved `kind: epic` on an unprefixed folder, and both name the PRD in the folder above as the likely target. Both docs pages now list the stop. The docs pages of `/create-ard`, `/specify`, `/epics` and `/update-prd` now say `/create-prd` refuses "three BRD-route shapes", which is how the commands put it. Where the folder above holds a PRD, the remedy's *re-refine this Epic* clause names `/epics <KEY>` only where the Epic folder holds an `epic.md`, since `/epics` refuses one that does not (`EPICS_NO_PRD`), and `/epics <PRD-KEY>` otherwise; and where the folder above holds none, the remedy says `/epics` reports that shape as `EPICS_EPIC_NOT_UNDER_PRD` or `EPICS_NO_PRD` by the same test, where it had named the first alone. `/prd-proposal`'s matching remedy says the same.
- **`/brd-package` classed `BRD_PACKAGE_REVIEW_UNACCOUNTED` as a stop that does not fire `emit-block`.** That stop means `brd-package-reviewer` broke its return contract twice on a dispatch this command owns. That is an agent-contract gap, the rule `/brd-reconcile` already applies to `BRD_RECONCILE_READER_CONTRACT`. It now fires `emit-block`, beside the three reference-integrity gaps and — with the reclassification below — two record-integrity gaps, six qualifying stops in all. The invariant now states its test as a rule, so every other stop — the bundle-integrity checks that report what the assembled bundle holds (`BRD_PACKAGE_DEAD_CITATION` and the two beside it) among them — is classed by it rather than by a list that had left them out.
- **`/prd-proposal` and `/brd-proposal` accepted an Epic folder** (behaviour). `resolve-address` searches every level, the Epic level included, and neither command refused an `EPIC-` folder. `/prd-proposal` reached its PRD gate, found no `prd.md`, and `PRD_PROPOSAL_NEEDS_PRD` named `/create-prd` with the Epic's key, which refuses that folder (`CREATE_PRD_EPIC_FOLDER`). `/brd-proposal` refused only a `PRD-` prefix, so an `EPIC-` folder passed, held no slices, and `BRD_PROPOSAL_NO_SLICES` named `/brd-split` with the Epic's key. Population: every run of either command given an Epic key whose `EPIC-` folder exists. `/prd-proposal` step 4a now stops with `PRD_PROPOSAL_EPIC_FOLDER`, naming the PRD above as the one to price, in the remedy shape `/create-prd` step 5b and `/update-prd` step 3a use. `/brd-proposal` step 4a stops with `BRD_PROPOSAL_EPIC_FOLDER`, naming `/prd-proposal` for the PRD above and itself for the programme above that, each only where a file on disk records the key. Both test the directory prefix, or a resolved `kind: epic` on an unprefixed folder; both are user halts in the capture-at-block list; both docs pages list the stop.
- **`/specify`'s Epic-level `### Next step` could offer `/create-prd <EPIC-KEY>`** (behaviour). It tested the resolved folder for a `prd.md` and, finding none, named `/create-prd <ADDRESS>`. An Epic folder holds no `prd.md` by construction, and `/create-prd` refuses it. Population: narrow — an Epic-level run whose parent PRD gate took the BRD route's absent branch, an Epic under a slice holding no `prd.md`. The offer now tests the folder Phase 0's PRD gate tested (the parent, on an Epic-level run) and names that folder's own key. `/create-ard`'s matching offer now says an Epic-level array names neither `/epics` nor `/create-prd`, so the Epic folder is never tested. Both now say `/create-prd` refuses "three BRD-route shapes", its Epic-folder refusal being a fourth refusal outside that set.
- **`/epics` sent an `EPIC-` folder with no `epic.md` to `/create-prd`, and `EPICS_EPIC_NOT_UNDER_PRD` said "No command in this plugin authors a PRD above an Epic folder that already exists"** (behaviour). The first fell to `EPICS_NO_PRD`'s idea-route row, which named `/create-prd <KEY>` with the Epic's key; `/create-prd` refuses it. That row is now its own, naming `/epics <PRD-KEY>` where the folder above holds a PRD, `/create-prd <PARENT-KEY>` where the folder above is a `PRD-` folder with no PRD, and no command anywhere else. The second was false where the folder above is a `PRD-` folder holding no `prd.md`: `/create-prd <PARENT-KEY>` authors one there. The stop now names that run in that case, subject to the slice rows of `EPICS_NO_PRD`'s table applied to that folder, and keeps the `git mv` remedy only for a folder with no `PRD-` folder above it. It is also classed as a user halt. The note that step 6 commits nothing now names `_coverage.md` beside the `epic.md` files. The new row names `/epics <PARENT-KEY>` as the run after `/create-prd <PARENT-KEY>`, and no longer says outright that the folder may be removed: the stop says what it holds, and that it may go once the operator has checked nothing in it is wanted.
- **Four stops are now classed as plugin gaps and fire `emit-block`** (behaviour). `/brd-interview`'s `BRD_INTERVIEW_MALFORMED_FINDING` and `/brd-split`'s `BRD_SPLIT_MALFORMED_FINDING` were user halts, but the keys they find in practice are `grounding-verifier`'s return fields, `own_verdict` above all, which reach a finding block only through a plugin command transcribing a return into the record — a record-integrity gap of the plugin's own making, the test `/brd-reconcile` applies to `BRD_RECONCILE_READER_CONTRACT`. `/brd-package`'s `BRD_PACKAGE_CORPUS_UNREADABLE` and `BRD_PACKAGE_SET_UNREADABLE` were classed with the bundle-integrity checks as facts about the bundle, but each stop's own text calls it a parse or read of the plugin's own records failing on content that is there, which no sentence in the package can fix. All four now fire `emit-block`; `/brd-package`'s invariant names six qualifying stops, and `/brd-split`'s no longer says none of Phase 0's stops qualify. `/brd-reconcile`'s `BRD_RECONCILE_EMPTY_DIGEST` was weighed by the same test and still does not fire — its return is well-formed, over the customer's own file rather than a plugin record, and the stop cannot tell a failed parse from a self-contradicting review — and the invariant now says that, where it had called the return a description of the file.
- **`/idea`'s `IDEA_NOT_AN_IDEA_FOLDER` and `/prd-proposal`'s `PRD_PROPOSAL_NEEDS_PRD` named runs that refuse the folder** (behaviour). `/idea` named `/create-prd <KEY>` for every BRD-route slice, though `/create-prd` refuses a slice whose gate set still has an `unallocated` row (`CREATE_PRD_BRD_UNALLOCATED`) or no `covered-here` row (`CREATE_PRD_BRD_NOT_ELIGIBLE`); named `/update-prd <PRD-KEY>` and `/epics <KEY>` for every Epic folder, though they stop `UPDATE_PRD_NO_PRD` where the folder above holds no `prd.md` and `EPICS_NO_PRD` where the Epic folder holds no `epic.md`; and offered `/brd-split` on a container without saying it is a no-op on a ledger with nothing `unallocated`. `/prd-proposal` said *"Author one first: /create-prd <KEY>"* on every folder. Both now resolve the remedy from the gate-set table `/update-prd` Phase 0 step 4 and `/epics` Phase 0 step 1b use, and the Epic rows from the conditionals `/create-prd` step 5b and `/update-prd` step 3a use, so every run a slice or Epic row names can itself take the folder — the `/create-prd <KEY>` after an allocate-only `/brd-split <KEY>` is named only where that walk leaves a claimed row `covered-here`. The BRD-container row is the one exception, and each command says so where it states the rule: it names each slice's `/create-prd <SLICE-KEY>` as where a PRD belongs, not as a run promised to start, and names `/brd-split <KEY>` with its no-op case said beside it. `/prd-proposal`'s Epic-folder remedy applies that table to the parent it names. Both docs pages say so.
- **The slice-remedy tables overlapped, and none covered a missing ledger** (behaviour). The row for a slice with no `covered-here` row and a non-empty gate set did not exclude `unallocated`, so a freshly carved slice with every claimed row `unallocated` matched both it and the `unallocated` row, and that row said nothing moves a terminal row back to `unallocated` — about rows that were never terminal. `/create-prd` tests `unallocated` first, so the `unallocated` row is the right one; the non-empty row now requires no row `unallocated` as well, in `/idea`, `/prd-proposal`, `/update-prd` step 4, `/epics` step 1b, the `/specify` and `/create-ard` next-step tables (and `/create-ard`'s matching array and final-report sentence), and `coverage-ledger-format.md` §5.2's refusal table. No copy covered a slice whose `coverage-ledger.md` is absent while its `brd-link.md` claims rows, which `/create-prd` step 7 and §5.2 answer by naming no command and reporting the missing file; each command table now carries that row, and so does `/brd-reconcile`'s Phase 14 offer test, reachable there on a `--sent` run; §5.2 states the rule the rows cite. The sentences in `/prd-proposal`, `/update-prd` and `/epics` saying the table's stop is the one place the command opens a coverage ledger now also name the Epic-folder stop that applies the table to the folder above.
- **`/create-prd`, `/update-prd` and `/epics` named `/create-prd <PARENT-KEY>` on a `PRD-` folder above an Epic without testing it** (behaviour). Where that folder is a slice, `/create-prd` applies its two data refusals there. `/create-prd` step 5b, `/update-prd` step 3a and `EPICS_EPIC_NOT_UNDER_PRD` now name it subject to the slice rows applied to that folder, as `/idea`, `/prd-proposal` and `EPICS_NO_PRD` already did, so the remedy `/prd-proposal` calls "the one both stops give" is one remedy. The `/create-prd`, `/update-prd` and `/idea` docs pages say the same.

### Fixed — claims the wider exclusivity probe found false

- **`brd-package-reviewer` called `package.defects` "the one input above that is optional".** It refuses only without `brd_dir`, `package.decisions` and `package.grounding`, so seeds, ledger, questions and prior reviews are not required either. It now says `package.defects` is optional, as is every input but those three.
- **`prd-reviewer` said "nothing mints keys any more".** `/brd-split` mints slice keys and `/epics` mints Epic keys. It now says no tracker mints keys, and the keys the plugin mints itself are minted against the same grammar.
- **`proposal-reviewer` called a verdict read as class-1 evidence "the one thing §8 of the format forbids"** — an unnamed format, and neither candidate §8 forbids only that. It now names `workflows-core:grounding-format` §8 and says it forbids exactly that.
- **`coverage-ledger-format.md` §1 called the ledger "the only place a requirement's fate is recorded".** `/brd-split`'s `slices.md` writes down each fate its run assigned, with a rationale. The ledger is now "the only authoritative record" of a fate, and `slices.md` is named as a narrative nothing gates on.
- **`coverage-ledger-format.md` §2 gave `evidence` "exactly one writer once the ledger exists", `/prd-ground`.** A `/brd-intake` re-run over a source-owning BRD rewrites an existing ledger whole and empties the column. The rule is now about the one writer *of a value*, and the re-run is named beside the two creators as writing the column empty.
- **`idea-format.md` called `/idea` "the command that creates `PRD-<KEY>-<slug>/`".** `/create-prd` creates the folder on its first write where none exists, and `/brd-split` creates slice folders. It now says `/idea` creates the folder whenever it is the first command to write there.
- **`docs/commands/brd-split.md` called `/brd-intake` "the only other command that writes" a child's ledger or inventory.** `/prd-ground` writes the ledger's `evidence` and `/brd-reconcile` writes dispositions. It now says `/brd-intake` is the only other command that *creates* either.
- **`docs/commands/create-ard.md` named the next-step offer as "the one place the run does open the ledger".** The final report opens it too, to leave `covered-by` rows' findings out of the unconsumed list. The page now names both places, neither gating the run, as the command does. `docs/commands/specify.md` said "two places" and now names the same three the command does: the zero-Epics choice, the next-step offer and the final report.
- **`docs/commands/create-prd.md` called the sibling re-cut "the one way" an empty slice stops claiming nothing, and offered the bare `/brd-split <PARENT-KEY>` without conditions.** Where the parent's ledger still has an `unallocated` row, the ordinary walk can offer `covered-by` against the slice, and a run with rows to place stops with `BRD_SPLIT_NEEDS_INSTRUCTION` without an instruction. The page now depends on the parent's ledger in the same way `BRD_SPLIT_EMPTY_INVENTORY` does, and names both ways out.
- **`docs/reference/proposal-format.md` said "every other artifact this plugin writes is read by you".** `/brd-package`'s bundle goes to the customer. It now says most of what the plugin writes is read by you, and that the bundle a customer sees comes with a prompt that explains the plugin's records.
- **`docs/reference/model-routing.md` called `/brd-package`'s rendered prompt the one artifact "somebody outside the organisation reads with nobody present to correct it".** A proposal is customer-read too. It now uses the command's wording: the one artifact an outside party pastes into an agent and runs, with nobody from the delivery team present to correct it.
- **`docs/commands/create-ard.md` and `docs/commands/specify.md` opened the BRD route with "No input outside the specs tree".** Both routes read code through `$REPOS_PATH`. The heading is now "No tracker is consulted". `specify.md` also said `SPECIFY_NEEDS_KEY` was "unreachable" because it reported a retired front-end. It is a live Phase 0 stop on a prompt with no address, and the page now says so. Two more stale sites on that page are fixed too: "no non-tracker behaviour" now reads "no direct-prompt behaviour", and "specifying from the export directly" now reads "from the resolved folder".
- **`docs/commands/create-prd.md` and `docs/commands/update-prd.md` said no command consumes `brd_key`, `brd_parent` or `depends_on`.** `/brd-proposal` reads `depends_on` for slice order, and `prd-reviewer` checks the `brd_key`/`brd_parent` pair. Both pages now say no command of the build ladder reads any of the three, and name those two readers.
- **This changelog's 3.6.0 entry said the "four-part stop contract" `/prd-ground` cited "exists nowhere in the tree".** Its recipe, `grep -rn 'stop contract'`, was case-sensitive and missed `workflows-core:phase-handoff` §4.4 *Stop contract*, which was in the tree then. The entry keeps its text and now carries a note: that contract governs only `require-on-main` stops, so the cut pointer stays cut, and the recipe should be run as `grep -rni`.
- **Three present-tense claims in older entries, now annotated, their original text kept:** 3.4.0's "the one reader is the sibling umbrella `/brd-proposal`" carries a *(Corrected in 3.8.0: …)* note saying it is the one command that reads *another folder's* proposal as a proposal (`/brd-reconcile`'s stale cross-reference sweep reads one only as prose). 3.3.1's "`prd.md`, the only name the plugin writes" carries one saying it is the only canonical PRD name, because `/update-prd` also archives under `revisions/`. 3.6.0's "the one place that name is re-derived" carries an *(at this release; …)* note, because 3.7.0 moved the rule to `brd-format.md` §1.1, which has two readers.
- **`/prd-ground` said grounding "happens at the slice and nowhere else"** — in its opening and in Phase 0 step 5a's heading — though the same step grounds an idea-route PRD folder. Both now say a root BRD is never ground and grounding happens at a `PRD-` folder, a slice or an idea-route PRD folder. `/brd-split` steps 6 and 7 carried the same sentence and now say grounding happens at a `PRD-` folder, on that route a slice. The command's docs page says the same.
- **The root-level stops of `/prd-ground`, `/brd-interview`, `/brd-package` and `/brd-reconcile` said a leftover root-level artifact "is left in place and nothing reads it".** `/brd-proposal`'s defect sweep and `/brd-reconcile`'s stale-cross-reference sweep read root artifacts. Each stop now says the command itself never reads it (`/brd-reconcile`: never reconciles against it).
- **Five refusal-inference sentences, in three commands, called a gated file "written exclusively by" one command.** `grounding/code-grounding.md` (`/brd-interview`, one site) is also stamped `consumed_by` by `/create-ard`, `/specify` and `/update-prd`; and `customer-review-prompt-<date>.md` (`/brd-reconcile`, three sites) is bannered by `/brd-reconcile` itself. Each of those four now says the file is *created* only by its producer, names the later writers, and keeps the inference, since every one of them also refuses a root. The fifth, `/brd-package`'s, is about `decisions.md`, which `/create-prd` (an `[AS#n]`, and its own `consumed_by` stamps), `/brd-reconcile` and the `/create-ard` and `/specify` stamps also write; it now names every command that writes the file — `/brd-interview` among them — and says each refuses a root, which is what the inference needs.
- **`/brd-package` called the rendered customer prompt "the one artifact … a person outside the organisation reads without anyone present".** A proposal is read by the customer too. It is now the one artifact an outside party pastes into an agent and runs, with nobody from the delivery team present — a proposal is read, not run.
- **`/create-ard` named Phase 7's offer as "the one place this run does open" the ledger, and `/specify` said "two places".** Both Final reports open the ledger as well, to leave `covered-by` rows' findings out of the unconsumed list. `/create-ard` now names two places and `/specify` three, none gating the run. `/brd-reconcile`, which said neither command reads the claims or the ledger "at all", now says not as an authoring input.
- **`/create-ard`, `/specify` and `/epics` said, in their container-refusal remedy, that "this command reads no coverage ledger".** Each opens a ledger elsewhere (the offers and Final reports above; `/epics` in one data stop). Each now says the *remedy* opens none. `docs/brd-workflow.md` (the parameter table and the next-step paragraph), `docs/workflow.md`'s handoff table and `docs/roles-and-phases.md` said `/create-ard` and `/specify` read no ledger, and now say neither reads or authors from it.
- **`/create-prd`'s empty-gate-set row called the sibling re-cut "the one way" an empty slice stops claiming nothing, and named the bare `/brd-split <PARENT-KEY>` unconditionally.** The ordinary walk can offer `covered-by` against the slice where the parent still holds an `unallocated` row, and a run with rows to place stops with `BRD_SPLIT_NEEDS_INSTRUCTION` without an instruction. The row now splits on the parent's ledger and names the instruction form where one is needed.
- **`/brd-split` called the re-cut the only path on which a standing child's ledger enters `deliverable_paths` because a row was taken off it.** An ordinary walk after a cancelled run can give a still-`unallocated` row of a provisionally keyed slice to a sibling. It is now the only path that takes off a row the child had *committed* to, its ledger reading `deferred-to`.
- **`/brd-split` Phase 0 step 5 called `brd-link.md`'s `parent:` "the same signal `/prd-ground` uses to tell a slice from a root, and the only reliable one".** `/prd-ground` tells a slice from a root by the directory prefix and reads `parent:` at step 6. The step now says so, and calls `parent:` reliable unlike a segment count. The command's docs page, which said "the only reliable signal", now says the same.
- **`/brd-split` called `--no-design` the one way a slice reaches build with a frame set unreconciled.** The pre-section pass can let one through unknown. It is now the one way with a frame set *known* to be unreconciled.
- **`/brd-intake` called a revised source's copy "the one thing a later intake writes again".** `brd-format` §1's rule is about when a copy under `brd/` is written again; the command now quotes it: a re-run over a revised source is the one time.
- **The PRD's single filename.** `/create-ard` and `/specify` gated "the only PRD name", `/create-prd` called `prd.md` "the only name this plugin writes", and `/update-prd` (its description and step 4) called it "the only copy"; `/update-prd` archives dated copies under `revisions/`. Each now says the canonical name, or the only *current* copy, and names the archive as never read as a PRD. The `/update-prd` docs page carried the same "only copy there is" and now matches.
- **`/create-ard` and `/specify` said grounding "on this route is optional and nothing gates on it".** On the BRD route `/brd-split` and `/brd-interview` gate on grounding. Both commands, and both docs pages (which said "grounding upstream … is optional"), now say it is optional to this command and nothing in this run gates on it.
- **`/update-prd` said no command consumes `brd_key`, `brd_parent` or `depends_on`.** `/brd-proposal` reads `depends_on` for slice order and `prd-reviewer` checks the pair. It now says no command of the build ladder reads any of the three.
- **`/prd-proposal` said "nothing reads" the brief or the archived revisions.** `proposal-reviewer` reads the brief inside the same run. It now says no later run reads either. Its sibling-census paragraph used *current* without defining it; it now gives `/brd-proposal` Phase 3's modification-time test and says that test opens no sibling's proposal.
- **`/prd-proposal` and `/brd-proposal` grill a proposal profile without declaring a depth**, which `workflows-core:grilling-technique` requires of every caller. Both now declare it relentless, done when every profile field has been asked and none is left as a recorded gap, and unable to close while `roles`, `productivity` or `calendar` lacks an answer — the three a team, a schedule and a productivity basis are stated from. `/brd-proposal`'s copy points at the profile `/prd-proposal` Phase 2 defines, since it carries no profile block of its own.
- **`/idea` said `grounding-format` §6.1 makes a missing frame-set index "unrecoverable".** §6.1 makes it a refusal, recovered by writing an index — `/frames`, or by hand. The sentence now says so.
- **The `/brd-package` docs page was headed "Everything it emits is read outside your organisation".** The run also writes an adversarial self-review that never leaves the folder. The heading now says *sends*, and the paragraph names what stays behind.
- **`docs/roles-and-phases.md` called `/dev-workflows:ready` "the one caller allowed to keep going past a stop like this", and said an absent artifact always falls back.** This plugin's `/brd-reconcile` also reads a stopping row without stopping, at two sites and never at its ordinary Phase 0 gate — its cross-BRD write guard, and a `--sent` run's redundancy check at Phase 0 step 6, which reads the gate inverted (`workflows-core:phase-handoff` §3.7) — and the commands whose gated input was never optional stop on its absence (§3.4). `/ready` is now the one caller whose own gate never stops it, `/brd-reconcile`'s two sites are named, and the absent-artifact bullet names the §3.4 exceptions.
- **`/brd-reconcile`'s next-step offer said `/create-ard` and `/specify` "neither reads outside the specs tree", and its docs page's offer table said each "reads only the specs tree" and reads neither `claims:` nor the ledger.** Both reach code through `$REPOS_PATH`, and both open the ledger to shape their offers and report. The command now says neither consults a tracker and each resolves its own code reads, and the table says each consults no tracker and reads neither `claims:` nor the ledger as an authoring input.
- **`docs/brd-workflow.md`'s parameter table said of `/prd-ground` "Only a slice is ground".** An idea-route PRD folder is ground too. The row now says a root BRD is never ground and grounding runs at a `PRD-` folder — a slice, or an idea-route PRD folder.
- **`/create-prd`'s empty-gate-set row said an instructed `/brd-split` run "can instead re-cut", and `/prd-ground`'s matching stop said the same**, while `docs/commands/create-prd.md` said *additionally*. The instructed run still runs its Phase 4.5 on the re-cut path and can offer to remove or keep the slice, so both commands now say *additionally*.
- **`code-defect-log-format.md` named `brd-package-reviewer` as the log's one reader.** `/brd-package`'s Phase 0 gate, `/brd-reconcile` (its inputs and its propagation sweep), `/prd-proposal`'s defect sweep and `proposal-reviewer` read it too. The header now names them as "among them" and gives a `grep -l` over the plugin's commands, agents and references that lists every file naming the log, saying it is no list of readers — `/prd-proposal` reads the log through `references/proposal-format.md` §7 and its own command file never names it. `docs/reference/references.md`'s entry for the file now names the same readers, `/brd-package`'s Phase 0 gate among them.
- **`bundle-packaging.md` named `/brd-package`, `brd-package-reviewer` and `/brd-reconcile` as its consumers.** `/brd-interview` and `/prd-ground` cite its §1.1, §6.2 and §6.3 for what they write that a bundle later carries. The header now names both and gives the `grep -l` recipe. Its list of "every other working record" excluded from a bundle, which omitted several such files, now reads "among them".
- **`coverage-ledger-format.md` §3.2 said `/brd-split`'s Step 3 "today visits only rows that were `unallocated`", calling the widening to a re-cut row "the command's change to make".** The command made it: Step 3 visits the rows Step 2R re-pointed too. The paragraph now says so.
- **`coverage-ledger-format.md` said a slice's `covered-by` row "is written by the parent's Phase 4 walk".** Phase 4.5's removal repair also writes `covered-by: <PARENT-KEY>` onto another child's row. It now names both.
- **`coverage-ledger-format.md` §3.1 said every consumer that puts a requirement "in front of a human" never reads a `covered-by` row.** The sibling re-cut puts one in front of the operator as an allocation. The rule is now about putting a requirement to a customer or into an interview round, and the re-cut is named beside it.
- **`coverage-ledger-format.md` said "two commands write a disposition once the ledger exists" and "exactly two commands write" the BRD bookkeeping.** `/brd-intake` re-run rewrites an existing ledger (the paragraph's own exception), and `/prd-ground` and `/brd-reconcile` write into a ledger. They now say two commands *allocate or settle* a row and two commands *create* the bookkeeping.
- **`coverage-ledger-format.md` §5.1 said only `/prd-ground`, `/brd-split` and `/brd-package` ever write `brd-link.md`.** `/brd-reconcile` also adds a `depends-on:` entry where a returned review's correction asks for one (its *Apply the required corrections* table's `brd-link.md` row: "a row asking to add one is `applied`"). It now names four, all but `/brd-split` writing only `depends-on:`; the argument the sentence serves is unchanged.
- **`coverage-ledger-format.md` §5.1's consumer paragraph listed product-workflows' consumers only.** `dev-workflows`' `/design`, `/implement` and `/ready` take the same positive test through `workflows-core:addressing` §4.1. They are now named, with a note that the paragraph's recipe reaches them only through their remedy text.
- **`decision-register-format.md` named the register's writers and three readers as its consumers**, while its own §1 `altitude` row names `/create-prd`, `/create-ard` and `/specify` as readers. The header now names those and the other readers, and gives the `grep -l` that lists every command and agent naming the register, saying it is no list of readers — `customer-review-reader` is handed the register by path and never names it.
- **`interview-tagging.md` omitted `/create-prd`**, which applies §2's test to triage a gap its grill could not close, **and `customer-review-reader`**, which cites §2. Both are named now.
- **`proposal-format.md` said "a `decisions.md` has exactly two writers", `/brd-interview` and `/brd-reconcile`.** `/create-prd` writes an `[AS#n]`, and the build ladder stamps `consumed_by`. It now says only those two put a decision in it, and that every other write also happens only on a BRD slice, so the tier-1 cap holds either way.
- **`proposal-format.md` §11 said "every other artifact the plugin writes is read by the operator or pasted into a tracker".** `/brd-package`'s bundle goes to the customer. It now says most is read by the operator, and that what a customer sees arrives with a prompt explaining the records. This matches the docs page that round 1 fixed.
- **`epic-reviewer` said three dimensions are conditioned on `refinement_targets`.** *Epic independence*'s refinement-mode exception reads it too. It is now named as a fourth.
- **`docs/workflow.md` said all twelve BRD edges appear "unchanged, in style and in label".** The two edges through `/prd-ground` carry different labels from `docs/brd-workflow.md`'s. It now says so, and says the labels do not contradict each other. The same page said `/create-ard` and `/specify` add no condition because "neither reads a resolved folder, gates a PRD, or reads the ledger". It now uses the reason round 1 wrote into `docs/brd-workflow.md`.
- **`docs/brd-workflow.md` said nothing in the handover box "opens a question" and that each "only reads what this route already wrote".** `/create-prd` writes an `[AS#n]`, and all three read `$DOCS_PATH` (`/create-ard` and `/specify` also read `$REPOS_PATH`). The box paragraph now says so. Its *What lands where* tree drew every artifact at the root's level. It is now split: the root holds `brd/`, the ledger, `slices.md`, `design/` and bookkeeping, and grounding, the register, the interview, the package and the reconciliation sit in the `PRD-` slice. A note covers a root written before grounding moved. The tree's `brd-link.md` and `decisions.md` comments now name `/brd-reconcile` and `/create-prd` as writers.
- **`docs/reference/references.md`**: the `coverage-ledger-format.md` entry said §5.1 is shared "by the four commands that refuse a container", which is more than four. It now points to §5.1 for the list. The `brd-format.md` entry adds `figure-reader` and `/brd-package`, and the `decision-register-format.md` entry adds `/create-prd`'s `[AS#n]`.
- **`docs/roles-and-phases.md` gave the companion plugins "five more lifecycle phases — three and two", and its Dev paragraph listed no `docs-audit`.** `workflows-core:cost-emission` §7 gives `docs-workflows` three (`documenting`, `docs-scaffold`, `docs-audit`). Both sentences now say six, and the page cites §7.
- **`docs/reference/agents.md` said three further agents ship elsewhere.** Commands also dispatch `workflows-core:doc-fixer` and `prose-style:prose-style-checker`. It now names five and the plugin each ships in.
- **`docs/commands/brd-split.md` called `/brd-split` "the only command of the route that runs more than once"** — every route command re-runs. It now says the only one that runs at both levels. It also counted "the three stops" naming a bare re-run, which is more than three; the count is dropped. It called the re-cut "the one case" the command re-allocates a fated row, twice, while Phase 4.5's removal repair rewrites fated rows too. Both sites, and the same claim on `docs/commands/prd-ground.md`, now name that repair as the exception.
- **`docs/commands/prd-ground.md` said the `prd` prefix is shared with `/create-prd` and `/update-prd`.** `/prd-proposal` opens on it too. It is named now.
- **`docs/commands/create-prd.md` called the `consumed_by` stamp "the only write it makes into any BRD file"**, which the same page contradicts with the `[AS#n]` it writes. It is now the only edit to an *existing* record, and the page says a new `[AS#n]` may be added.
- **`docs/commands/brd-interview.md` said a row "its holder is still committed to is moved by no command".** `/brd-reconcile` moves rows on a frozen `[CD#n]`. It now says no command re-points such a row onto another slice.
- **`docs/commands/epics.md` described seventeen commands as offering "a branch + commit + push + pull-request handoff for their own deliverable".** That set omits `/vuln`, `/upgrade` and the docs commands, which draft pull requests too. The seventeen it names are the commands that hand their deliverable onto the specs repo's default branch through `handoff-to-main`, and it now says that.
- **`docs/commands/update-prd.md` called `/update-prd` "the one authoring command deliberately excluded from `require-on-main`".** `/idea` and `/brd-intake` never run it either. It now names both, and says it is the one authoring command that works from a PRD earlier phases wrote while gating on none of them.
- **`docs/getting-started.md` said the marketplace also ships `obsidian-llm-wiki` and `acli`.** It ships `guideline-reviewers` too. It is named now.
- **Seed files had "one writer" on the `/create-prd`, `/create-ard` and `/specify` docs pages** (`/brd-intake --sort-existing`). `/brd-reconcile` corrects a seed's prose in place. Each page now calls `--sort-existing` the one command that *creates* a seed, and says `/brd-reconcile` only corrects one. `docs/commands/create-prd.md`'s container bullet likewise says `/brd-intake` and `/brd-split` *create* the ledger and inventory, rather than being their only writers.
- **`docs/commands/brd-split.md` said that on an empty root `/prd-ground` "would stop on the same emptiness".** `/prd-ground` refuses a root outright (`PRD_GROUND_ROOT_LEVEL`). The page now says that.
- **`docs/commands/brd-reconcile.md` said a decision moves "only through the four sweep dispositions or the two reopening causes".** That leaves out the command's own freeze and the `consumed_by` stamps `/create-prd`, `/create-ard` and `/specify` write. The page now names both.
- **`coverage-ledger-format.md` §5 said "only the parent's walk writes" a slice's orphan row.** The parent's Phase 4.5 removal repair writes one too. It now names the parent's *run*: its walk, or that repair.
- **`docs/commands/epics.md` called the positive BRD test "the one rule `/create-prd`, `/create-ard` and `/specify` share".** More commands share it. It now says every command that tests a folder's level shares it.
- **Three `/brd-intake` claims.** Its empty-root stop said `/prd-ground` stops with `PRD_GROUND_EMPTY_INVENTORY` and that `/brd-split` and `/brd-interview` "stop the same way". On a root, `/prd-ground` and `/brd-interview` refuse outright (`PRD_GROUND_ROOT_LEVEL`, `BRD_INTERVIEW_ROOT_LEVEL`), and `/brd-split` is the command that stops with an empty-inventory stop, `BRD_SPLIT_EMPTY_INVENTORY`. The stop now says so. `BRD_INTAKE_SLICE` said a slice's inventory and ledger are "for `/brd-split` to write"; other commands write the ledger later, and they are now *created by* `/brd-split` on the parent. The branch-prefix note said `brd` is shared by every `/brd-*` command; `/prd-ground` takes it on the BRD route too, as `workflows-core:phase-handoff` §2.9's `prefix` row says. It also counts no container refusals now.
- **`/brd-split`'s full-mode `BRD_SPLIT_EMPTY_INVENTORY` said `/prd-ground` "stops on the same emptiness"** — the command's own copy of the claim the docs page carried. `/prd-ground` refuses a root outright. The stop now says that. The same command also said "three stops" name it as the fix for an empty child, and that "only the parent's walk" writes a slice's orphan row. More stops and offers name it, so the count is dropped. Phase 4.5's removal repair writes orphan rows too, so both sites now name the parent's run. It also said `/brd-split` "is the only `/brd-*` command that can ever change" the `unallocated` term of the no-op test. `/brd-reconcile`'s ledger phase takes a row out of `unallocated` on a frozen `[CD#n]`, so it now says `/brd-split` alone puts a row in, and names what else takes one out. Its re-cut paragraph now names the key repair a removal performs as the other case in which it rewrites a fated row, as its docs page already does.
- **`/create-prd`, `/create-ard` and `/specify` named the root key in the routes for a contradicted decision.** They told the operator to run `/prd-ground <BRD-KEY> --rebaseline`, `/brd-interview <BRD-KEY>`, `/brd-package <BRD-KEY>` and `/brd-reconcile <BRD-KEY>`. Every one of those four refuses a root. Each route now names `<SLICE-KEY>`, and `--rebaseline` only where the pinned code has moved. `/create-prd`'s feedback emitter and Final report now name the slice key too.
- **`/create-prd`, `/create-ard` and `/specify` said only `/prd-ground`, `/brd-split` and `/brd-package` ever write `brd-link.md`.** `/brd-reconcile` merges a `depends-on:` too — the command-side copies of the `coverage-ledger-format.md` §5.1 claim fixed above. The argument they serve is about a root, and no command writes a `brd-link.md` into a root: `/brd-split` places one in each slice it carves, and every command that merges `depends-on:` refuses a root. Each now says that. `/create-ard` also listed `grounding/`, `interview/` and `decisions.md` in a `BRD-` folder, and `/specify` and `/epics` listed `grounding/`; under the current model those sit in a slice, and at root only in a tree written under the earlier two-level model. `/create-ard` now says that and the other two drop the entry. `/create-ard`, `/specify`, `/epics`, `/prd-proposal` and `/brd-proposal` named a closed set (three commands, or the family's — four, in `/prd-proposal`) as the takers of `coverage-ledger-format.md` §5.1's test, and now say every container or root refusal in this plugin takes it, directly or — as `/idea`'s does — through `workflows-core:addressing` §4.1, which cites it; `/prd-ground`, `/brd-interview`, `/brd-package` and `/brd-reconcile` now scope their "every consumer of this test" to this plugin, and say a consumer takes it directly or through `workflows-core:addressing` §4.1. The three authoring commands' seed notes now say `--sort-existing` *creates* a seed, as their docs pages already do.
- **`/create-ard` and `/specify` said the idea route has no grounding.** Each said the folder stays at tier 1 because "neither grounding nor a settled register exists", or "until the folder is grounded". `/prd-ground` runs on an idea-route folder too. Tier 2 also needs a settled register, which nothing on the idea route produces, so both now say the folder stays at tier 1 for that reason.
- **`/create-prd` said four other stops name it as the run that creates a `PRD-` folder.** `/epics`' `EPICS_NOT_FOUND` and `/prd-ground`'s `PRD_GROUND_NOT_FOUND` do too. The count is dropped and both are named.
- **Four `/prd-ground` claims.** It said `/create-ard` and `/specify` read `prd.md` "from the specs repo's default branch, never from a working tree". Each gates `prd.md` with its own `require-on-main`: one on a branch stops them, and one on no ref is reported as no authored PRD. It now says that. It called its verifier's non-`OK` statuses "the four statuses", and there are more; it now says every status other than `OK`. It called `/create-ard` and `/specify` "the two consumers" of what it gated and wrote, which other readers falsify — `/prd-proposal` grades on the same grounding; it now calls them the two downstream authors. A pointer "thirty lines below" now names the rule it points at.
- **`/specify` named "four" of its stops as user halts in its capture-at-block invariant.** It has more. The sentence now says every `SPECIFY_*` stop is argument- or tree-shaped.
- **Three `/brd-interview` claims.** It said "both rules" that read a question's state share one vocabulary. `/brd-package`'s gate on the rounds and `/brd-reconcile`'s closing of a round read it too, so it now says every rule. It said the `- **Requirement defect:**` line is the one "both readers" take. `/brd-package` renders it into the customer's copy of the question as well, so it now says every reader. Its capture-at-block invariant listed its user-halt stops and left out `BRD_INTERVIEW_BAD_ROUND`, `BRD_INTERVIEW_NO_FINDINGS` and `BRD_INTERVIEW_MALFORMED_FINDING`. It now states the test as a rule, so a stop added later is classed by it, and names the first two; the third, `BRD_INTERVIEW_MALFORMED_FINDING`, now qualifies under that rule (*Fixed — behaviour*).
- **Four `/brd-reconcile` claims.** Its *Reject* paragraph said "This command mints no `[C]`, writes nothing into the question set, and closes no round". The command as a whole closes each `[C]` it froze an answer to and a round those answers settle, so the sentence now names *Reject* as its subject. Its *Apply the required corrections* row said `depends-on:` is merged by "two other commands"; they are now named (`/prd-ground`, `/brd-package`), and the row says it is the one field of `brd-link.md` this command writes. Its register row said a record's fields move only through the sweep's dispositions and §4's reopening causes; it now names the command's own freeze and the `consumed_by` stamp as well, as its docs page already does. Its capture-at-block invariant listed its user-halt stops and left out `BRD_RECONCILE_SENT_NOT_READABLE`, `BRD_RECONCILE_SENT_REDUNDANT` and `BRD_RECONCILE_EMPTY_DIGEST`. It now states the test as a rule and names them.
- **`/prd-proposal` said three times that `/brd-proposal` is "the one command that reads another folder's proposal".** `/brd-reconcile`'s stale cross-reference sweep reads every proposal under the parent BRD too, as prose. All three now say *as a proposal*, which is the distinction the sentences beside them already drew; 3.6.0's quotation of `proposal-format.md` §0 carries a correction note saying the same.
- **`/create-ard` and `/specify` offered `/prd-proposal <KEY>` in their commercial-step prose.** On an Epic-level run `<KEY>` is the Epic's, which `/prd-proposal` step 4a refuses. Both now name `<PRD>`, the PRD folder's own key.
- **`/create-ard`, `/specify`, `/epics`, `/prd-proposal` and `/brd-proposal` said every container or root refusal in this plugin takes `coverage-ledger-format.md` §5.1's test from that section**, and `/prd-ground`, `/brd-interview`, `/brd-package` and `/brd-reconcile` said every consumer of the test does. `/idea`'s `IDEA_NOT_AN_IDEA_FOLDER` takes it through `workflows-core:addressing` §4.1. Each now says *directly or through* §4.1.
- **`/create-ard`'s next-step precondition put a clause about Epic-level runs between the test and its two outcomes**, so *where there is one* read as though it meant an Epic-level run. The clause now follows the outcomes. `/create-prd` step 5a now says, as step 5b already did, that the refusal is taken ahead of step 3's ladder.
- **`/brd-proposal` step 4a said step 4 "refuses only a `PRD-` prefix"** — true only among prefixed folders; step 4 also refuses an unprefixed slice by positive evidence. It now says so. Its report paragraph named "Price the slice first" as the answer that ends the walk; "Re-price it first" ends it too, and is named now.
- **`/specify`'s handoff `body_facts` named "the `<BRD-KEY>` this specification was seeded from".** `/specify` runs on a slice and reads its seeds from the slice folder. It now names the `<SLICE-KEY>`.
- **`docs/brd-workflow.md` said no command writes the retired root-level artifacts "any more".** A slice's `/brd-reconcile` stale cross-reference sweep reads every markdown file under the parent and can update an argumentation in a leftover root `decisions.md`. It now says no command *creates* them, and names that edit. The tree's root `design/` line now says `/frames` indexes it and no grounding reads it, a root never being ground.
- **`/brd-split`'s capture-at-block invariant was still a closed list, and left out `BRD_SPLIT_NEEDS_INSTRUCTION` and `BRD_SPLIT_NO_FINDINGS`.** It now states the test first, as `/brd-interview`'s, `/brd-package`'s and `/brd-reconcile`'s do — every stop but `BRD_SPLIT_MALFORMED_FINDING` reports the operator's argument, the tree or the environment and is classified by that test, never by a list — and names each of its `BRD_SPLIT_*` stops by its class, with the operator's `Cancel` halts in Phases 1.5, 2, 3 and 4 beside them as user halts.
- **`docs/commands/epics.md` said a stand-alone `EPIC-` folder is refused with `EPICS_EPIC_NOT_UNDER_PRD`.** Only where it holds an `epic.md`; one holding none is refused with `EPICS_NO_PRD` wherever it sits. The page now says so and describes that stop's own row for an empty `EPIC-` folder; the command's opening summary and its *NEVER partition* invariant, which said the same, now name both stops, and the page's input bullet names the empty folder among the refused shapes. The command's frontmatter description and Phase 0 step 1b's lead, which said `/epics` accepts "an EPIC- folder that has a PRD above it" and listed two refused shapes, now say an `EPIC-` folder holding its `epic.md`, and name the empty one among what is refused.
- **`docs/brd-workflow.md`'s tree put `attachments/` and `design/idea-sources/` "/idea vendored" inside a `PRD-` slice.** `/idea` refuses a BRD-route slice (`IDEA_NOT_AN_IDEA_FOLDER`), so nothing on this route writes either. The tree keeps `attachments/` as a reserved name no command writes here, and the paragraph beneath it says why.

## [3.7.0] — 2026-09-22

Design authority: `docs/superpowers/specs/2026-09-18-brd-figures-and-appendices-design.md`, whose §14 — one link walk shared by `/brd-intake` and `/idea` — governs where it and an earlier section of that file disagree. Every claim below is read off the shipped commands, agents and references; where the spec proposed something the tree does differently, the tree is what is recorded. **Update `workflows-core` to 1.7.1 with this release**: two of its references now name `figure-reader` and the two artifacts this release adds, one of them now defines a frame-set index row's `Linked from` the way `/idea` writes it, `escalation-rules` names the form of `/brd-reconcile`'s confirmation picker for an answer matching no question, `addressing` §4.1 now owns how a folder is placed at a level — the rule `/create-ard`, `/specify`, `/idea` and `/prd-ground` cite, and the definition of a prefix the container refusals of `/create-prd`, `/epics`, `/prd-proposal` and `/brd-proposal` take — `implementation-format` §1 now says where the implementation record `/specify` reads its candidate repositories from lives, and a machine updating this plugin alone reads the old wording beside the new behaviour. **One of the dependencies is behaviour, not wording**: this release gives `coverage-ledger.md`, which `/brd-intake` writes at a root BRD's top level, frontmatter carrying `key:` beside `kind: coverage-ledger`, and only 1.7.1's `addressing` §4 passes over a `kind:` naming a document. 1.7.0's §4 read a folder's kind off the first artifact carrying both fields, so beside it a root BRD would resolve as kind `coverage-ledger`.

### Fixed — the coverage ledger's `evidence` column had no writer, so every package told the customer nothing had been checked

`coverage-ledger-format.md` §2 defines `evidence` as the `[CG#n]`/`[DG#n]` findings bearing on a requirement, "empty until grounding has run"; `/brd-intake` Phase 5 and `/brd-split` Phase 3 each write it empty and name `/prd-ground` as whose job filling it is; and `/prd-ground` never wrote it. Its Phase 8 wrote the two grounding files and nothing else, its `deliverable_paths` on `route: brd` named no ledger, and its one mention of the column only forbade a documentation page in it. So the column was empty on every ledger in every tree for ever, however much grounding had run — and since `bundle-packaging.md` §1.1 puts the ledger **and** both grounding files in the customer bundle, a package handed the customer the one document that lists every requirement and cannot tell a checked one from an unchecked one, beside a grounding document showing which had been checked. A live `/brd-package` run shipped exactly that, its own adversarial reviewer raising it unprompted on both passes. **Population: every coverage ledger written on any tree, at every release the column has existed, and every bundle built from one.** `/prd-ground` Phase 8 now rebuilds the column as its last write, over every row of the ledger of the BRD it just ground: a row takes the id of every finding whose `claim` names that row's `[BR#n]`, a row no finding names keeps an empty cell, and nothing else in the row is touched — this command still allocates nothing and writes no disposition. It runs in every mode, `--no-code` included, since a design-only run still produces findings the column would otherwise never index, and not at all on `route: idea`, which has no ledger. **The column indexes and never adjudicates** — the verdict stays in the grounding file the id resolves to, which is why a `SUPERSEDED` finding stays listed rather than being filtered out — and the ledger joins that route's `deliverable_paths` only where the rebuild changed it, so an unchanged file is not named in a handoff. The Final report now states how many rows took an id and how many were left empty; the ledger line itself is unmoved, since §6 counts by `disposition`.

### Fixed — `/brd-interview` offered a `(Recommended)` push on a specs repo with no remote

`workflows-core:phase-handoff` §4.3 requires §2.1's push-target probe to run **before** its consent array is presented, and a one-line notice above the array where that probe finds no `origin`. `/brd-interview` Phase 10 said only "present its §4.3 choice array verbatim" and then quoted the array — which is what quoting an array invites — so a live run presented the array, ran no probe, and offered a `(Recommended)` push-and-open-PR that could not succeed, with nothing said: the exact state §2.1 records that probe as having been added for, met again. **Population: every `/brd-interview` run against a specs repository with no `origin` remote — on 3.6.x as well.** The phase now says to execute §4.3 in full rather than its array alone, names the probe and the notice, and carries the probe's `remote` value into `handoff-to-main`, which §2.1 expects carried rather than probed twice. **It was family-wide rather than this command's alone** — of the fifteen commands that quote a §4.3 array, only `/brd-package` named the probe, and it does so because it needs the `remote` value for its own delivery-route decision — and the half that fixes the other fourteen is `workflows-core` 1.7.1's, which names the step above the arrays where a reader who copies one will meet it.

### Fixed — an open requirement defect was never put to the customer

`/brd-intake` Phase 4 confirms each requirement defect into `brd/brd-defect-log.md` with resolution `open`, and `brd-format.md` §4 names two resolutions only the customer can supply, `customer-amended` and `withdrawn`. Nothing asked the customer for either. On 3.6.x `/brd-interview` generated its questions from grounding findings, horizons, design reconciliations, ledger dispositions and assertions without evidence — the strings `[DEF#` and `brd-defect-log` occurred in it zero times; `/brd-package` shipped the log in the bundle only so a ledger row's `rejected: [DEF#n]` would resolve for the reviewer, and no part of its prompt put an entry to anyone; and `customer-review-schema.md` §4 carries no defect section. So an `ambiguity` — two competent readings of one requirement — reached the customer only if they happened to open the log and comment on it, and the PRD was authored on whichever reading the delivery team assumed. **Population: every BRD intaken with a confirmed `ambiguity`, `conflict`, `duplicate`, `untestable` or `scope-leak` defect.** `unsourced` is not among them: grounding settles that class, and a finding that cannot settle it is already a question under the `NOT-PROVABLE` source.

An open requirement defect of those five classes is now a question source of `/brd-interview`'s own, the sixth bullet of Phase 3's *Round 1 is generated from the grounding*. It is asked **once across the slices**, by the slice holding the lowest-numbered of its rows still live — `covered-here` or `deferred-to` in that slice's own ledger — its rows being **every row it joins**, the one its entry is raised on and every one the entry names, so a split whose first row no slice builds is still owned through a sibling row one does; or, where none is live, by its **carrier** — the slice holding the lowest-numbered of those rows rejected citing it — on that row's own question (below), whatever the defect's class; and never again once any slice's `interview/customer-questions.md` records it. A defect whose owner cannot yet be decided, because a row it joins is still `unallocated` or a sibling's ledger or question set cannot be read, is withheld and reported rather than guessed. The question is always `[C]` (`interview-tagging.md` §1): it states what the defect records — the two readings, the two requirements that cannot both hold, the missing observable outcome — and names the image where the row was drawn from one. It travels in the package as every held `[C]` does — the review schema gains no section, and the answer comes back in its decision log — with its held entry naming the `[DEF#n]`. A row it names as context that the asking slice does not claim — a sibling's, or one the root settled — is written `<PARENT-KEY> [BR#n]`, by the rule the entry *a row a slice does not claim, cited bare, stopped its package* (below) records, which binds every question and everything written from one. `/brd-reconcile` copies that id into the answering `[CD#n]`'s new `settles` field (`decision-register-format.md` §1), from the held entry and never from the customer's prose, and its Phase 8, which wrote two of the log's resolutions, now writes a third — `resolved-by: <SLICE-KEY>/[CD#n]`, which `brd-format.md` §4 now admits beside the grounding resolution. That one was the bare `resolved-by: [CG#n]` on 3.6.x and is now qualified too, `resolved-by: <SLICE-KEY>/[CG#n]`: both carry the key of the slice whose decision or finding settled the defect, in the shape `conditional_on` already gives a record of another BRD, because each slice numbers its own `[CD#n]` and `[CG#n]` from 1 while the defect log is the parent's, so a bare `[CD#2]` there would name no one record. No command writes the `[CG#n]` form, so only a value written into a defect log by hand is affected, and it needs its slice key added: `/brd-package` reports a bare one rather than resolving it (`bundle-packaging.md` §6.2), and checks a qualified one naming the packaged slice against that slice's own register or grounding. `customer-amended` is qualified the same way, `customer-amended <SLICE-KEY> <date>`: the amended text is in the returned review of the slice that reconciled it, and once a BRD has two slices a bare date does not say whose. **Population: every defect a customer amends; a value 3.6.x wrote bare is read as it stands, naming no slice.** A settled defect takes `withdrawn` where the answer drops the requirement — every `open` defect raised on it, whether or not the answer's `settles` names it — which the ledger then records `rejected: [DEF#n]`; `customer-amended <SLICE-KEY> <date>` where the review supplied corrected text; and otherwise `resolved-by: <SLICE-KEY>/[CD#n]`. Every round record `/brd-interview` writes now carries a `requirement defects:` line saying which defects that round asked and which it withheld, and why — every open defect of the five classes joining a row of the slice's in-scope set, and every defect a rejected row of the slice carries, an `unsourced` one included, since the carrier reads the defect a rejection cites whatever its class.

**Where no slice owns a defect, this route never asks it, and the record says so rather than calling it settled.** A defect whose rows are all `superseded-by`, never delegated, or rejected citing something else has no live row and no carrier: it stays `open` in the parent's log, and the account line of a slice holding one of its rows withholds it as `no live row`. A defect every row of which the root's own walk settled — deferred, rejected or superseded there, never delegated — is in no slice's in-scope set: a slice that once claimed such a row keeps it only as an orphan row, which is out of that slice's scope as well (below), so no account line names it, and the root is never interviewed. `/brd-interview` states that limit beside the carrier rule; this release does not route such a defect to the customer.

**One question per rejected row, whichever slice asks.** Each rejected row's held entry now carries a `- **Rejected row:** [BR#n]` line. Where a defect this slice carries has a rejected row whose question is already on file and still *held for the customer* — a slice interviewed on 3.6.x, whose round-1 question predates the defect line, is the ordinary case — the run appends the `- **Requirement defect:**` line to that entry rather than raising a second question; an entry written before 3.7.0 is matched as the one held entry naming that row's `[BR#n]` and asking about its rejection, and where none or more than one could be it, a question is raised and the candidates are named. The question's text is not rewritten. Where that earlier question was already answered, the answer does not settle the defect — `settles` is copied from the entry's line when an answer is frozen, and that answer was frozen without one — so the defect is asked anew, by design, naming the earlier `[CD#n]` as context; and where it was frozen `open`, `/brd-reconcile` writes its `settles` from the line when it completes the record. And a rejected row whose defect any slice owns through a live row — this slice included — raises no question of its own: for a defect of the five classes, the defect's question names every row it joins as context, the rejected one among them, so a question of the row's own would put that row to the customer twice; an `unsourced` defect joins a live row only in a log entry written before the one-row listing, is grounding's to settle on that live row, and its rejected row raises nothing either. **Population: every slice holding a row rejected on a defect some slice owns through a live row, and 3.6.x slices holding a question about a row rejected on a defect no live row owns.** `brd-format.md` §4's `resolved-by` row, which named the answer to the question the defect raised, now names a rejected row's question that carries it as well.

**A slice interviewed before this release** has a round 1 whose record carries no such line, and its defects belong in round 1 — a question round 1 could have asked is never moved to a later round. Where round 1 is still open, the next bare `/product-workflows:brd-interview <SLICE-KEY>` appends them to it. Where round 1 is closed, the run names them, and its next-step offer is `/product-workflows:brd-interview <SLICE-KEY> --round 1` with the cause *requirement defects became a question source*, in place of the packaging step, until they are asked. The cost is one more interview run and one more package round with the customer for every such slice; what it buys is the questions the first package should have carried.

### Fixed — a defect whose rows were all rejected was never asked, and so never settled

`/brd-interview` raised a question for "an in-scope ledger row resolved `deferred-to` or `rejected` whose consequence for the rest of the BRD is unstated", and named no artifact where a consequence counts as stated and no tag. `/brd-split` records a rationale for a `deferred-to` row only, so a rejection's consequence was unstated by construction and whether to ask was left to judgement. And the question never carried the defect the rejection cites: no question carried a `[DEF#n]` at all, so `/brd-reconcile` could never copy one into a `settles` field, and a rejection's defect stayed `open` in the parent's log for good. The requirement-defect source this release adds (above) decides ownership over **live** rows only, so without the carrier below it would have left a `[DEF#n]` every row of which was rejected on it with no owner as well. **Population: every slice holding a row `rejected: [DEF#n]` on 3.6.x.** Phase 3 now asks every in-scope `rejected` row not already settled, always `[C]` (whether the customer accepts not getting what they asked for is theirs to say). A row is settled where it reads `rejected: <SLICE-KEY>/[CD#n]` (the customer's own withdrawal, below), or where the defect it cites is no longer `open`, is already asked, is owned through a live row by any slice, or is carried by another slice. Where no row a defect joins is live, its **carrier** (above) owns it: that row's question carries the defect on its `- **Requirement defect:**` line, states what the defect records, and counts as asking it, and `/brd-reconcile` resolves it `resolved-by: <SLICE-KEY>/[CD#n]` on the customer's answer — accepting the rejection or not; the row does not move, and `withdrawn` is not written, because the customer dropped nothing the delivery side had not already dropped. Where that defect is a `conflict` or `duplicate` the answer settles by keeping one row and dropping the other, the two rules meet, and Phase 8 states their order once: the rejected row never moves — kept, it goes to *what still needs a human* — while the defect's resolution and the other row's write follow that phase's rules for keeping one row and dropping the other. A defect with neither a live row nor a carrier is withheld as `no live row` (above). A `deferred-to` row raises a question only where no rationale was recorded — no `slices.md` block and no `[CD#n]` deferring it. **What it costs**: one more `[C]` question per rejected row a package now carries, and never a second one for the same row (above).

### Fixed — a slice asked the customer about rows its parent had withdrawn and settled

`/brd-interview` Phase 3 scoped a round to the ledger rows reading `covered-here`, `deferred-to`, `rejected` or `superseded-by`, read off the `disposition` column and never off `claims:`. An **orphan row** — a ledger row for a `[BR#n]` the slice's `claims:` no longer names (`coverage-ledger-format.md` §2) — carries the fate its parent's walk settled across unchanged where that fate is `rejected: [DEF#n]` or `superseded-by: [BR#n]` (§3's orphan table), so it read exactly like a row the slice had rejected or superseded itself: a requirement the root rejected was put to the customer by a slice that no longer claims it, and under this release's carrier rule its orphan row would have made that slice the carrier of the root's defect. **Population: every slice holding an orphan row its parent's walk rejected or superseded — on 3.6.x as well.** An orphan row is now out of the question scope whatever it reads, the scope reading `claims:` for that one test and for nothing else, and the carrier test counts only a row the parent's ledger records `covered-by` the slice. `/brd-package`'s review scope, which stated the same four dispositions as the package's scope, now leaves an orphan row out as well, naming it, cited `<PARENT-KEY> [BR#n]` (below), with the BRD that holds it — and no longer says every row out of scope is covered by a separate package, which a row the parent kept or settled itself never is; `coverage-ledger-format.md` §3.1, the rule both cite, now says why the one test that reads `claims:` is the orphan test. The all-delegated stop, which that scoping now reaches for a slice every one of whose rows is an orphan row, said every row was `covered-by` another BRD, that the inventory was not empty, and that an instruction on `/brd-split <BRD-KEY>` could re-cut a row onto "another child of" the slice — a slice has no child, its inventory is empty once every claim is withdrawn, and the re-cut runs on the parent. It now names the orphan rows, says why the empty-inventory gate does not see such a slice, and sends the operator to `/brd-split` on the parent, which resolves a child left claiming nothing; `/brd-package`'s account of a folder that stop leaves without a register, and the documentation page, say the same.

### Fixed — a row a slice does not claim, cited bare, stopped its package

A slice's package carries the slice's own inventory, which holds only the rows it claims (`brd-format.md` §2.1), and `/brd-package`'s citation-resolution check resolves a bare `[BR#n]` against that inventory alone (`bundle-packaging.md` §6.2, relation 1), so a bare mention of a sibling's row, an orphan row or a row the root settled stopped the run with `BRD_PACKAGE_DEAD_CITATION`. Four writers produced one: `/brd-interview`, which on 3.6.x let a question cite a delegated row as context with no qualifier; `/create-prd`, whose `[AS#n]` for a gap only the customer can settle goes into the same register; `/brd-reconcile`, whose freeze writes a `[CD#n]`'s `statement` as confirmed and copies its `options_considered` from the question, naming whatever row the question named; and `/brd-package` itself, whose review scope named each row out of scope with the BRD that holds it — `[BR#12] — held by ACME-90-02` — in a prompt part the run generates, which no operator edit can repair. **Population: every package of a slice holding a `covered-by` or orphan row — on 3.6.x as well — and every package whose questions or records cite a row the slice does not claim, 3.6.x's delegated context included.** Each now writes such a row `<PARENT-KEY> [BR#n]`, the one qualified prose spelling §6.2 gives another BRD's id, which relation 1 discharges, and reads which form a row takes off the slice's inventory: `/brd-interview` in every question and everything written from one (*A row this slice does not claim*); `/create-prd` in the `[AS#n]` it writes; `/brd-reconcile` in every field its freeze writes save the customer's own words, qualifying as it copies them the options of a question that names such a row bare, one recorded before 3.7.0 or put by a package the route did not build; and `/brd-package` in its review scope, a `superseded-by` row's successor included wherever the slice does not claim it. The customer's quoted reason, and an answer quoted outside the options, are never requalified: `bundle-packaging.md` §6.3 now counts both as verbatim customer content, so a bare id the customer wrote there is reported rather than stopped on, as one visible in their screenshot already was, and §2's de-Obsidianising pass leaves a link in either as written. **An entry already on file is not rewritten**: a question, a held entry or a register record written before 3.7.0 that cites such a row bare still stops the package, and `BRD_PACKAGE_DEAD_CITATION`'s message and the documentation page now say to qualify that id by hand where the stop names it, changing nothing else in the sentence. `bundle-packaging.md` §7.1, which said the review scope carries no requirement ids, now says it carries them for those rows, and §7.2 that its set-resolution check does not reach that list.

### Fixed — a slice whose rounds recorded no decision could not be packaged

`/brd-interview` wrote `decisions.md` as one block per `[VD#n]` and `[AS#n]` a round recorded, and a round whose questions were `[C]` or answered from findings records neither — its `[C]` questions go to `interview/customer-questions.md`. So no register was written; `/brd-interview`'s next-step offer, which checked `/brd-package`'s rounds gate and its something-to-review gate but not its register gate, offered packaging; and `/brd-package` Phase 0 step 6 stopped `BRD_PACKAGE_NEEDS_INTERVIEW`, saying no interview had ever run. Its remedy, another `/brd-interview` run, resumed the same round and again wrote no register — a loop. **Population: every slice whose rounds hold a `[C]` question and record no `[VD#n]` or `[AS#n]` — on 3.6.x any such slice, and with this release every slice owning an open requirement defect of the five classes above whose grounding raised no `[V]` question and no `[AS#n]`**, which the new question source turns into a round of `[C]` alone. `/brd-interview` now writes `decisions.md` on every run that records a round — the single header line `# Decision register: <BRD-KEY>` where no round recorded a decision (`decision-register-format.md` §1) — and hands it off with the round record, and its next-step offer checks `/brd-package`'s register gate as well as the other two. The no-new-round path writes it too, where none is on file, so a slice caught in the loop leaves it on its next `/brd-interview` run — writing the register as it resumes an open round, or on that path where every round is closed; only an all-delegated slice, which that command stops on, is left without one, and it has nothing to package. `/brd-package`'s rounds gate now takes the rounds it must find from the held `[C]` entries as well as from the register's records, so the record of a round of held `[C]` questions is gated too; before, such a round put nothing in the register and was never checked. `/brd-package` reads the header-only register as an empty corpus (`bundle-packaging.md` §6.1), and `/brd-reconcile` freezes its first `[CD#n]` into it.

### Fixed — the `/brd-interview` page said, twice, that an unchanged interview run hands off nothing

`docs/commands/brd-interview.md` said, in two sentences, that a run that finds every round closed and nothing changed "reaches the handoff with nothing to commit", and that `decisions.md` is written "on every run that records a round". Since the fix above that made a `[C]`-only slice packageable, that run writes `decisions.md` as its header line where none is on file and hands it off. **Population: docs readers only; the command already did this.** Both sentences now say so.

### Fixed — `/brd-interview` computed a next-step state it could never reach

Phase 11 tested for an absent `interview/` round record and offered `package_offerable: not-interviewed`, but every path that reaches Phase 11 has written a round record — a run that stops before writing one, the all-delegated stop among them, never gets there. **Population: none — the state was dead; the fix removes a branch no run could take.** The value and its list are gone, and Phase 11 says why `/brd-package`'s `BRD_PACKAGE_NOT_INTERVIEWED` can never meet an offer made there. Two sentences beside it were corrected with it: the claim that "only the all-delegated stop" leaves a folder without a register, and `/brd-package` step 7's claim that an empty round set "reaches step 8's `BRD_PACKAGE_NOTHING_TO_REVIEW`" — step 8 stops it unless `interview/` holds a round record and an open `[AS#n]` leaves something to review. The `/brd-package` docs page also said `BRD_PACKAGE_NOT_INTERVIEWED` needs "no `[VD#n]`"; it fires whatever the register holds. **Population, for those three corrections: every reader of them — no run's behaviour changed**, since the register claim and the round-set claim each described a path the commands already took differently, and the page described a stop its command already fired on a wider condition.

### Fixed — a first interview generated its round-1 questions twice

`/brd-interview` Phase 2's no-round-record branch runs *Round 1 is generated from the grounding* to decide whether there is a round to open, and Phase 3 then opened by generating the round's questions again, with nothing saying the second pass was the first one's set. **Population: every first `/brd-interview` run on a slice, where a literal reading writes each question twice.** Phase 3 now says that where Phase 2 already generated round 1 it works that set and generates nothing again — and that on a round it resumes or re-opens, whatever the cause, it generates nothing beyond the questions the round-1 test and the one-question-per-row rule append.

### Fixed — a customer decision's altitude was invented

`/brd-reconcile` Phase 5 filled a `[CD#n]`'s `altitude` with "the altitude the question carried", and no held `[C]` question carried one — `/brd-interview` wrote none into `interview/customer-questions.md` — so every `[CD#n]` got an altitude the run made up, and `/create-prd`, `/create-ard` and `/specify` filter the register on it. **Population: every `[CD#n]` frozen on 3.6.x.** Each held `[C]` entry now carries a `- **Altitude:**` line, decided by the test `decision-register-format.md` §1 now states as a mapping — `product` where the answer is the PRD's to state, `architecture` the ARD's, `implementation` the specification's, the value each of the three filters on; a `[CD#n]` copies it, or an `[AS#n]`'s own, or, for an escalated `[SR#n]`, the altitude of what its target names. Where there is nothing to copy — an entry written before this release, or a finding aimed at a document passage — the run decides it by the same test and says so in the reconciliation record. An answer matching no question the package put needs none: it is never frozen (below).

### Fixed — a customer's answer outside the options put could not be recorded

`decision-register-format.md` §1 required `chosen` to be one member of `options_considered`, and `/brd-reconcile` takes `options_considered` from what the package put and never from the answer — so a customer who chose neither reading, or dropped the requirement instead, broke one rule or the other, and Phase 4 gave the case no route. **Population: every customer answer outside the options its question, assumption or escalated finding put.** A `[CD#n]`'s `chosen` may now carry the customer's own answer after one fixed marker, `chosen: "outside the options: <the customer's answer, verbatim>"` — or, for an answer spanning lines or carrying a `"` or a `\`, the same marker opening a literal block scalar, so YAML reads the answer back exactly — with `options_considered` left as put; Phase 4 shows the operator whether an answer is outside the options and names *Confirm* as its route. A `[VD#n]` is unchanged: an option the operator names still joins `options_considered` before it is chosen.

### Fixed — a yes-or-no customer question had no recordable options

A `[CD#n]`'s `options_considered` is "what the package actually put in front of the customer, taken from the `[C]` question", and a question put as yes or no lists no options, so nothing said what the record should carry. **Population: every `[CD#n]` answering a yes-or-no question, an assumption or an escalated self-review finding on 3.6.x.** `decision-register-format.md` §1 now fixes it: such a question records `["yes", "no"]`, and one that listed its options records them as put; a `[CD#n]` answering an `[AS#n]`, which asserts and offers nothing, records `["as assumed", "not as assumed"]`, and one answering an escalated `[SR#n]` records `["as the package states", "as the finding argues"]`; `/brd-reconcile` Phase 5 and `/brd-interview`'s `[VD#n]` record cite the rule.

### Fixed — a decision resting on no finding had two spellings, and the will-change rule fired on it

`decision-register-format.md` §1 made only `defects`, `settles` and `conditional_on` "omitted when absent", and said nothing of a decision resting on no finding, so a writer could write `evidence: []` or leave the field out. And §6's test — the rule fires where *every* finding in the list is `will-change` — is vacuously true of an empty list, so read literally it refused to close a decision that rests on no finding at all. **Population: every `[VD#n]` or `[CD#n]` resting on no finding — a customer's business decision typically does.** §1 now fixes one form, `evidence: []`, never omitted — a known empty list, not the not-applicable case `workflows-core:grounding-format` §2.1 omits a field for, whose reading of an empty value `workflows-core` 1.7.1 scopes to finding records — and §6 and `/brd-interview`'s will-change phase say the rule does not fire on it; thinning a list to nothing is still the evasion §6 refuses.

### Fixed — a closed round's record still said it was open

`/brd-reconcile` Phase 5 closes a held `[C]` question by appending to `interview/round-<N>.md`, which is append-only, so a round record that stated its own state kept stating `open` above the closure, and nothing recorded that the round had closed. Its Phase 7 said the round records were "bannered too" while its own list of files to banner left them out. **Population: every round `/brd-reconcile` closed.** Every write of a round record now ends with one `Status:` line — `Status: open — waiting on <holding states>` or `Status: closed <YYYYMMDD> — <why>` — recording the round's state, which its questions' dispositions decide, a reader taking the **last** line and the dispositions winning where the two disagree; `/brd-reconcile` appends the closing line where its answers leave every question in the round with a terminal disposition, and none otherwise — a last line it leaves naming a holding state no question is in any more, such as *held for the customer* beside a question still *deferred*, is outvoted by the dispositions until the next `/brd-interview` write appends one that agrees. Round records are appended to, never bannered, and a stale-reference sweep hit inside one is `needs-a-human`, as in any dated snapshot, rather than an `updated` edit. A record carrying no `Status:` line is read off its questions' dispositions until its next write adds one. `/brd-interview` also no longer says a round holding a `[C]` "stays open indefinitely": it stays open until `/brd-reconcile` closes it with the customer's answer.

### Fixed — the review's traceability section had an open vocabulary

`customer-review-schema.md` §4's section 4 offered "confirmed, corrected, or missing entirely" without saying whether that set was closed, and had no value for a requirement the customer drops, or for one the package already said would not be built; two reader dispatches read byte-identical rows differently. **Population: every returned review.** Section 4 now takes exactly one of four values — `confirmed` (including a requirement the package says will not be built, where the customer agrees), `corrected`, `missing`, `withdrawn` — and a `withdrawn` row says why in the row itself, the answer to any question the package put about that requirement going in section 7 as every answer does. The row is rendered into every customer prompt and stays plugin-free. `/brd-reconcile` moves nothing on a withdrawal answering no question the package put — in section 4 alone or in a section-7 row matching nothing — and names it under *what still needs a human* with the customer's words: an answer matching no question is never frozen as a `[CD#n]`, since nothing records what it was offered, what it settles or which round it closes, and the operator records it for a human or rejects it — or, where the reader matched it to nothing although it answers a question the package put, names that question, which re-points the candidate onto the confirmation picker's four options and is recorded in the reconciliation record.

### Fixed — a customer's withdrawal of a requirement with no defect could not be recorded

`/brd-reconcile` Phase 9 wrote `rejected: [DEF#n]` only by citing a defect-log entry Phase 8 resolved `withdrawn`, so a customer withdrawing a requirement that carried no open defect left the row `covered-here`. **Population: every customer withdrawal, answering a question the package put, of a requirement carrying no open defect** — a withdrawal answering no question is never frozen (above). Phase 8 now resolves every `open` defect raised on a withdrawn requirement `withdrawn`, whether or not the answer's `settles` names it, so the new spelling is written exactly where no such defect exists. `coverage-ledger-format.md` §3's `rejected` now has two spellings: `rejected: [DEF#n]` as before, and `rejected: <SLICE-KEY>/[CD#n]`, citing the customer decision that withdrew the requirement, qualified by the slice's key as `resolved-by` is — written only by `/brd-reconcile`, onto its own slice's ledger. Both count as `rejected` in the ledger line, and §3 now says how to tell a customer's withdrawal from the delivery side's rejection: the citation — a `[CD#n]`, or a `[DEF#n]` the log resolves `withdrawn`. `/brd-interview` asks no question about a row the customer withdrew.

### Fixed — a customer asking for a row with a fate to be built was sent to a `/brd-split` run that moves nothing

`/brd-reconcile` Phase 8 said a customer answer asking for a `rejected` row to be built after all is an allocation, which Phase 9 names rather than writes, and Phase 9 named every row it would otherwise allocate in the reconciliation record with `/product-workflows:brd-split <BRD-KEY>` as the fix; its Phase 6 refusal of a section-12 row asking for a disposition named the same run as the channel. `/brd-split` on a slice walks only `unallocated` rows, and the row asked about carries a fate, so the named run moved nothing for it. **Population: every reconciliation whose customer asks for a row already rejected, deferred or superseded to be built — on 3.6.x as well.** The phases now say plainly that no command reverses such a disposition — no row of a slice's ledger returns to `unallocated`, and `/brd-split` on a slice walks no row that carries a fate, its sibling re-cut running on the parent and moving only a row a slice deferred, on an instruction a person gives — and record the row, with the customer's words, in the reconciliation record's *what still needs a human*, naming no command; `/brd-split` is named only for a row still `unallocated` — which a slice this route interviewed never holds, `/brd-interview` refusing an unallocated ledger, and which a `--sent` review of a slice never interviewed can find. The command's documentation page says the same.

### Fixed — `/brd-reconcile` named a route for an uncovered requirement that no command takes

A requirement the customer asked for that no `[BR#n]` covers was recorded under *what still needs a human* "naming the two real routes: an amendment logged against the defect log, or a fresh source document through `/product-workflows:brd-intake`". No command logs an amendment for it: `customer-amended` resolves a defect raised on a `[BR#n]` the inventory already holds, and such a requirement has none. **Population: every reconciliation whose customer asks for a requirement the package missed — on 3.6.x as well.** The record now names the one route a command takes — a revised source document, intaken over the root with `/brd-intake <PARENT-KEY> @<revised-file>`, whose read extracts the requirement as a new `[BR#n]`, at the cost its Phase 0 step 7 names — and otherwise leaves it to a person to take up with the customer; the documentation page says the same.

### Fixed — a `--sent` reconcile of a slice holding no register had nowhere to freeze an answer

`/brd-reconcile` Phase 0 step 2 said `--sent` admits a hand-authored package and *"does not make the route's earlier phases optional"*: a folder that never reached `/brd-interview` *"still has no `decisions.md` for a `[CD#n]` to be frozen against"*. Its step 7 and *Next steps* meanwhile provide for a `--sent` review of a slice never interviewed, and its Phase 5 assumed the register on file because `/brd-interview` writes it and `/brd-package` gates on it — neither of which a `--sent` run passes through. So nothing said what such a run does, and a `[CD#n]` answering a held `[C]` question on a slice interviewed before 3.7.0 over rounds that recorded no decision, which holds its questions and no register, had no file to be written into. **Population: every `--sent` run on a slice holding no `decisions.md` — one never interviewed, or one whose rounds, interviewed before 3.7.0, recorded no decision.** Such a slice is admitted, and Phase 5 now creates the register where none is on file, as its header line alone, whether or not it then freezes a record; `decision-register-format.md` §1 names `/brd-reconcile` among the commands that create the file. On a slice never interviewed, with no `[C]` question and no `[SR#n]` on file, a candidate can answer only an `[AS#n]` `/create-prd` recorded there, and any other is `unmatched` and goes to a human. Step 8 and the Phase 3 dispatch read the register and the question set where they are on file. `/brd-package`, which said a register on no ref means the interview's handoff was declined, now names the three commands that create one, and both documentation pages say the same. Phase 13's handoff declared `interview/round-<N>.md` and `interview/customer-questions.md` unconditionally, and on a slice never interviewed, which holds neither, `workflows-core:phase-handoff` §2.3 names a declared path nothing wrote as a declaration unaccounted for; it now declares both where on file, and the documentation page says the question set is updated where one is on file.

### Fixed — `/brd-reconcile` offered an effort proposal for decisions it had not frozen, on a slice with no PRD to price

Phase 14's advancing branch named `/product-workflows:prd-proposal <SLICE-KEY>` in prose, for the reason that *"until this run froze the customer's `[CD#n]`s there were none"*, so a proposal written now could cite them. A run that froze none — every candidate sent to a human, or a `--sent` review of a slice never interviewed whose answers matched no roundless `[AS#n]` — made the same offer for a reason untrue of it, and on a slice an earlier reconciliation had reached, decisions frozen before the run already stood, which the sentence denied. And `/prd-proposal` gates `prd.md` on main, stopping with `PRD_PROPOSAL_NEEDS_PRD` where the slice holds none — the ordinary state on this route, which authors the PRD after reconciliation — so the offer handed the operator a run that stops in its first phase. **Population: every `/brd-reconcile` run that reaches the advancing branch — on 3.6.x as well.** The offer is now named only where the run froze at least one `[CD#n]`, for the reason that holds — a customer decision no proposal written before the run could cite — and says that it prices the slice once its `prd.md` is on main, after `/product-workflows:create-prd <SLICE-KEY>` where it holds none yet; a run that froze none names no proposal.

### Fixed — `/brd-reconcile`'s sweeps named ids bare in another BRD's files

The propagation sweep's dispositions are written into a dependent BRD's `decisions.md`: a `reopened` record named *"this run's `[CD#n]` as its cause"*, and an `inherited-unchanged` or `reverted` one was recorded against *"the changed id"*, with nothing saying whose. A dependent's register numbers its own records and ships in the dependent's own package, so a bare `[CD#3]` there named the dependent's own `[CD#3]` where it held one — a wrong cause, which `bundle-packaging.md` §6.2's relation 1 then resolved green — and nothing where it did not, a dead citation the dependent's next package stops on with `BRD_PACKAGE_DEAD_CITATION` wherever the write is the plugin's own prose. The stale cross-reference sweep's `updated` outcome, correcting a sentence in the parent's or a sibling slice's artifact *"naming the `[CD#n]` that changed it"*, had the same defect. **Population: every propagation sweep that writes into a dependent BRD, and every `updated` correction written into another BRD's artifact — on 3.6.x as well.** Every id a sweep write names now carries the key of the BRD whose numbering it is — this slice's for a `[CD#n]`, `[VD#n]`, `[AS#n]` or `[SR#n]`, the parent's for a `[BR#n]` or `[DEF#n]` — always in prose, `<BRD-KEY> [CD#n]`, the one qualified spelling `bundle-packaging.md` §6.2 fixes, and at a place the sweep's own table names. A `reopened`, `reverted` or `withdrawn` write puts it in a closing paragraph appended to the dependent record's `argumentation`, opening `Reopened`, `Reverted` or `Withdrawn` and the date; an `inherited-unchanged` item writes nothing into the dependent's record, and its row, naming both records qualified, is in the reconciliation record. **Never `decision-register-format.md` §5's slash shape, `<BRD-KEY>/[CD#n]`, in a sweep write**: an earlier draft of this fix sent a structured field to that shape, but no sweep write names a changed id in one of a record's thirteen fields, and §6.2's relation 1 reads the slash shape as qualified only in the declared fields its table lists — `conditional_on` among them, which a `reverted` write restores to the value the record already held, in that field's own shape, naming the changed id only in its closing paragraph. `decision-register-format.md` §4 now says where **any** reopening records its cause — that closing paragraph, beneath the customer's quoted reason on a `[CD#n]` and never inside it, `bundle-packaging.md` §6.3 counting only the customer's quoted words as theirs — that what stands in `argumentation` stays, that **§4 now names one set — a record's decision fields, all thirteen §1 defines — with the per-field rule under it**, so a file needing to name the fields of a decision record cites that set instead of enumerating part of it; that a re-decision and a `reverted` write alike take afresh the seven of them a decision's own writing fills — `statement`, `options_considered`, `chosen`, `evidence`, `defects`, `conditional_on` and `status`, §1's set for a decision's own writing, `evidence` and `conditional_on` under §6 as well — and append their own closing paragraph after it, never over it or what stands above it. **Three more of the thirteen are settled beside that set rather than left to a reader**, because a register that keeps them stale is read wrongly by the commands downstream of it: `round` takes the round that produced the position now on record (the re-decision's own; on a reversion, the round of the position being restored, a propagation sweep being no round), `consumed_by` returns to `none` so `/create-prd`, `/create-ard` and `/specify` report the record as unconsumed at their own altitude while the artifact they wrote still holds the position that was replaced, and `altitude` stays, `id` and `settles` standing with it — all thirteen accounted for. `/brd-interview` carves the re-decision out of both places that said a decision carries every field §1 defines (its `[V]` phase and its register phase), and `/brd-package`'s round-set derivation now says what a round that only re-decided contributes to it — those records, by the `round` §4 moves — and why the earlier round the field moved off may fall out of the set, and that a cause from another BRD is named with that BRD's key; the sweep's `withdrawn` write now carries the reason §3 says a withdrawn record keeps, and its `reverted` row, which said the record's `chosen` **and `argumentation`** return to the earlier position — which on a record reopened and re-decided earlier in the same sweep would have erased the `Reopened` paragraph and the re-decision's reasoning — now restores the decision fields and appends. `/brd-interview`, which reopens decisions on a new grounding finding, now names that reopen — the `status` move, the closing paragraph and the re-decision that follows it, in the same run or a later one, which takes the record's decision fields afresh — as the second of two admitted changes to another run's record, beside a `[CDF#n]`'s re-disposition, where its *Resolve inputs* phase said there was exactly one, and said of the re-decision that it wrote three fields where its own *Put each `[V]` to the operator* and *The will-change rule* phases move `options_considered`, `evidence` and `conditional_on` too. The documentation page says the same. Where a reopening's cause went had never been said, on 3.6.x either.

### Fixed — the roles page said what a phase `/frames` charges means by a folder's progress, and put a slice's frame set in `prd-creation`

`docs/roles-and-phases.md` said being in *prd-creation* means the PRD does not yet have a merged specification or design, and that `/workflows-core:frames` lands there on a PRD folder or one of its Epics — while `workflows-core`'s `cost-emission.md` §7 charges a frame set by the kind its folder asserts, so a BRD-route slice, a `PRD-` folder asserting `kind: brd`, is `brd-to-prd`, and an Epic folder that already holds a merged specification or design is `prd-creation` all the same. The page now keeps the no-merged-specification meaning for `/idea` and `/create-prd`, says `/frames` lands there on a folder asserting `prd` or `epic` — an Epic folder under a slice included — where the phase marks product-definition spend and nothing about the folder's progress, and says under *brd-to-prd* that it lands there on a folder asserting `kind: brd`, a container or a slice, where the phase likewise says nothing about progress. That paragraph's *"rather than a PRD already existing for it"* was false on the route itself too — a later interview round, package or reconciliation runs on a slice that already holds its PRD — and now says so. **No run's behaviour changed. Population: every reader of the page.**

### Fixed — an accepted self-review finding could make a package unshippable

`/brd-package` part 9 quotes every self-review finding the operator disposes `accepted-risk` in the reviewer's own words, and the plugin-free scan stops the run on any `§`, command or agent name, and the citation-resolution check on any working filename, in the finished prompt. `brd-package-reviewer`'s findings routinely cited a plugin reference's section, a working filename or a command — in one measured run, 9 of 12 findings carried such a token — and nothing may reword an attack, so an accepted finding carrying one stopped the run with no remedy the text allowed. **Population: every `/brd-package` run on 3.6.x whose operator accepted a finding carrying such a token.** The reviewer now writes each finding's target, attack and what would settle it in words a customer can read without this plugin — the test it applies stated in words, a package document named by what it is, a passage of the customer's cited with its captured path — so an accepted finding passes both as quoted, and the command still never changes an attack's words.

### Fixed — a wikilinked file in a BRD was dropped without a trace

`/brd-intake` Phase 2 recognised three link forms — the inline markdown image or link, the reference-style definition and the HTML `<img src>` — and `[[` occurred in neither `commands/brd-intake.md` nor `references/brd-format.md`. A file the document linked as `[[notes]]` or embedded as `![[shot.png]]` was therefore not copied and not logged, which broke the phase's own rule that every link not copied is named, never dropped in silence. It is the likeliest shape to meet: Obsidian writes a pasted image as `![[Pasted image ….png]]` and saves it to the vault's attachment folder, usually outside the note's own folder. **Population: every BRD authored or converted in Obsidian that links a page or embeds an image that way.** The link walk now recognises all four forms (`references/linked-sources.md` §1), resolves a wikilink against its own file's directory — as written, then with `.md` added where the name does not already end in a markdown or image extension, so `[[appendix]]` beside `appendix.md` resolves in a customer's folder with no vault at all and `[[notes.v2]]` finds `notes.v2.md` — and only then by filename across the vault — the nearest ancestor of the document holding `.obsidian/` — and reports a name matching more than one file as `ambiguous`, with every candidate, choosing none — the link log names each candidate by its path relative to the vault's root, never by an absolute path that would write the operator's own directory layout into the specs repository.

**Re-running `/product-workflows:brd-intake <BRD-KEY> @<brd-file>` over the same folder captures them.** Nothing captured before is removed and the inventory's reconciliation keeps every existing id, so a row drawn from a file captured this time takes the next id after the highest in use; every defect already logged keeps its id too (below). **Ids are kept, and so is wording over an unchanged file**: a row the reconciliation matches keeps its id and, wherever the file its anchor points into is unchanged since the inventory's rows were last reconciled, its wording (below). The re-run is not free where the BRD has already been split: Phase 0 step 7 names every disposition the ledger holds and asks first, because a re-run whose read finds a requirement rewrites every ledger row to `unallocated` and `/brd-split`'s walk has to be taken again — that step lists what a re-run keeps and what it discards, and a read that finds none keeps the ledger as it stands (below).

### Fixed — a re-run of `/brd-intake` logged every defect a second time

`/brd-intake` Phase 4 gave each confirmed defect the next `[DEF#n]` and had no rule for a re-run, which re-extracts the inventory — and `brd-reader`, reading the same document, proposes the defects already logged all over again. Each was confirmed again under a new id. Measured on a byte-identical source, one re-run took a log of 15 `open` entries to 31, 13 of the new ones restating an old one on the same row and class, and left 13 of the old entries cited by no inventory row. The only other reading of the phase, numbering from `[DEF#1]` again, would have reused ids for different defects. It cited `brd-format.md` §3–§4 for *"ids are never reused or renumbered"*, and neither section said so. **Population: every `/brd-intake` re-run over a folder holding a defect log — on 3.6.x as well — and, with this release, every such duplicate of a defect in the five classes `/brd-interview` asks — `unsourced` is not among them — becoming a second customer question**, since `/brd-interview` now puts each such open defect to the customer where a slice owns it (above), and a re-minted id is one no slice has asked. **The worst case is a duplicate of an entry `/brd-reconcile` had already resolved**: the re-minted entry is `open`, so it asks the customer a question they have already answered. The wikilink fix above tells every Obsidian-authored BRD to re-run.

`brd-format.md` §4 now states that a `[DEF#n]` id is permanent — never reused, never renumbered, never deleted — as a `[BR#n]` is. On a re-run, Phase 4 matches each candidate against the log on file before asking, every row taken as Phase 3's id reconciliation numbers it: the same class raised on the same row, or, for a `conflict` or a `duplicate`, the same rows joined — in either direction, since a split can be raised from the whole naming each part or from each part naming the whole, so a candidate matches where entries on file of its class already join every pair of rows it joins. A match is that entry, or those entries — not asked again, ids, reasons and resolutions unchanged, so a defect `/brd-reconcile` resolved stays resolved. Only an unmatched candidate is walked, numbered after the highest id on file. Where several candidates and entries share one class and row they pair in order, and the order decides only which pairs: a candidate left over is walked with every entry on file of its class and row shown beside it, and the question says *Reject — not a defect* is the answer where it restates one; a `conflict` or `duplicate` only some of whose pairs entries on file join is walked with each entry of its class joining any of them shown beside it the same way, and, confirmed, is recorded for the pairs none of them joins and no other — each joined pair is a defect on file already, so confirming the whole candidate would log it twice, and rejecting it would drop the pairs nothing on file joins — its question naming both sets; and a group of one read's candidates, walked as one, is matched against the log the same way before it is walked, since its pairs — its row with each other row the group joins — need not be any of its members'. An entry this read did not propose again — no candidate of its class raised on its row, matched or not, so an entry the pairing left over still counts as re-raised; for a `conflict` or a `duplicate`, one no pair of whose rows a candidate of its class joins, since such an entry counts as re-raised wherever any pair it joins is, so one a candidate matched is never also reported not re-raised — is kept as it stands and reported as *not re-raised by this extraction*, and every row that cited it keeps citing it; a `conflict` or `duplicate` entry only some of whose pairs a candidate joins stays re-raised, and the report names beside it the pairs no candidate joined. **A log an earlier re-run already doubled is not repaired**: every id in it is permanent now, and which of two entries restating one defect to resolve, and how, is a person's call.

### Fixed — the four record files `/brd-intake` writes had no layout, and a verbatim requirement did not fit the one every run chose

`/brd-intake` Phase 2 wrote `brd/brd-link-log.md` "in the shape `brd-format.md` §1.1 fixes", and §1.1 listed what the log holds and fixed no shape; §2, §4 and `coverage-ledger-format.md` §2 fixed the fields of the inventory, the defect log and the ledger and no file around them. So each verification run invented a layout: one named the document in a label line above the title and another in the title itself, one gave an `ambiguous` link's candidates a column and another folded them into the reason, and each annotated its `files copied` count by hand because nothing said whether the document counts — while `brd-format.md` §1.1 had every reader of the name, a slice's inventory header and `/brd-package` Phase 0 among them, take it off the log's "opening line", whichever line a run had put first. And the markdown table every run chose cannot hold a requirement verbatim: a quoted row of the customer's own table carries `|` and a multi-line list item carries line breaks, which one run escaped as `\|` and `<br>` and nothing said to, changing the quote's bytes. **Population: every `/brd-intake` run, every `/brd-split` run that creates a slice, and every reader of the four files.** Each file now has one layout, fixed in its owner — `brd-format.md` §1.1 for the link log, §2 for the inventory, §4 for the defect log and `coverage-ledger-format.md` §2 for the ledger: frontmatter carrying `kind` and `key`, a fixed opening line where a reader needs one — the link log's `Source document: <basename>` — fixed count labels, and a table with named columns. The inventory's `kind: brd` is the folder's carrier, as before; the other three name their own document — `brd-link-log`, `brd-defect-log` and `coverage-ledger` — a kind `workflows-core:addressing` §4 passes over, so a ledger at a root BRD's top level is never read as the folder's identity (`workflows-core` 1.7.1's §4 now says the inventory is the only artifact there naming a folder kind). The link log's `files copied` leaves the document itself out, as Phase 1's count does. `brd-format.md` §2.3 fixes **one cell encoding for all four**, and for the figures file's *Annotations* table: a literal `|` is written `\|`, every backslash the customer wrote directly before one doubled first — so their own `\|` is written `\\\|` — and a line break `<br>`; "verbatim" means verbatim after decoding. **Every read parses and decodes by one rule, fixed there once** — a row splits on a delimiting `|`, one that opens the row or has a space directly before it, since every writer puts one space on each side of a cell, so a cell ending in the customer's own backslash — `C:\reports\` — never escapes the `|` that closes it — whatever it reads a cell for: to show it to the operator (`/brd-intake` Phase 4, `/brd-split`'s offer and walk), to work from it or hand it to an agent (`/brd-split`'s placement, `/prd-ground`'s claim list), or to compare it (`/brd-intake` Phase 3's match, relation 1, and `/brd-package`'s corpus parse, the ledger's included, and verbatim spans — `bundle-packaging.md` §2.1, §6.1, §6.3). A markdown link inside a quoted requirement is quotation: kept as written, never followed or repaired, though its target resolves to nothing from `brd/` — and `/brd-package`'s de-Obsidianising pass, which rewrites every wikilink the package wrote, leaves one quoted in an inventory or ledger `text` cell as written (`bundle-packaging.md` §2.1). A file written before this release is read as it stands, and rewritten in its layout by the next run that writes the whole file; a writer that edits a cell in place — `/brd-split`'s walk, `/brd-reconcile`'s resolutions and dispositions — leaves it in its own layout until then, and `/brd-split` encodes a cell of a pre-layout parent's inventory as it copies it into a slice. A pre-3.7.0 link log whose opening line names no single document under `brd/source/` makes its reader ask the operator rather than choose, and a pre-3.7.0 defect-log entry is read with the first row it was recorded against as its `raised on` and any other as its `names`. `/brd-split` writes each slice's inventory and ledger in the same layouts. Beside them, `brd-format.md` §4 said a `customer-amended` amendment "is held in the ledger beside the original", where no ledger column holds it; the row now names the returned review the amendment came from, which is where `/brd-reconcile` reads it, and that command's two sentences holding it "beside the original", and its documentation page's, now name the slice's returned review too — no run's behaviour changed.

### Fixed — a split became one defect per part, and a `conflict` or `duplicate` was listed on whichever rows a run chose

`brd-format.md` §2 recorded a split — one numbered item binding the delivery team to several obligations, inventoried as several rows — as **one** `duplicate` naming the sibling rows, while `brd-reader` put a `duplicate` candidate on every sibling and `/brd-intake` Phase 4 confirmed each: in two verification runs one three-way split became three defects, each naming the other two, and each an open defect `/brd-interview` would put to the customer as a question of its own. The split rows' `text` fitted neither of §2's forms, and the agent improvised, a blank line between lead-in and item for one sibling and a `…` line for the next. Phase 4 said to record a confirmed defect "against every `[BR#n]` it was raised on" without saying whether a `conflict`'s or `duplicate`'s counterpart row lists it too, fixed no order inside a class, which decided the numbering, and fixed the answer array for each candidate but not the question. **Population: every intake of a source holding an item with more than one obligation, every intake confirming a `conflict` or `duplicate` or more than one defect of a class, and every Phase 4 question.** `brd-reader` now proposes one `duplicate` per split, on the first row the split produced, naming every other row it produced, and Phase 4 confirms it once. **Every other `conflict` or `duplicate` is raised once too, from one end** (`brd-format.md` §3) — a part on the whole, a restatement or a clash on the lowest-numbered of its rows. `brd-reader` had no such rule, so a clash between two rows could be proposed from each, each confirmed, and the customer asked about one clash twice; Phase 4 now also groups, before walking, the candidates of one read and one class that share a pair of rows, directly or through another candidate of the group, and walks each group as one candidate — raised on the row §3 fixes, naming every other row the group joins — the other candidates folded into it, their reasons on an *Also raised* line, so a read that raises one relation from both ends, or among three or more rows as several candidates each naming some of the others, asks it once. **Population: every first intake whose document states such a relation.** A split row's `text` takes one fixed form, `<lead-in> … <item>` (`brd-format.md` §2), so siblings sharing one anchor differ in text; a split row an earlier release wrote in another shape — its lead-in, then a line break or a line holding an ellipsis alone (`…`, `[…]` or `...`), then its item behind a list marker — is rewritten into that form before a re-run's Phase 3 compares it, so it matches the new read's row rather than being minted again beside it (below); one whose lead-in or item itself spans more than one line cannot be split into the two and is not rewritten, and where the same-or-new question applies it is shown there beside the new read's row. A `conflict` or `duplicate` is listed in the `defects` column of the row it was raised on only, its counterparts named in the entry (§4) — the reading `/brd-reconcile` Phase 8 already took — and `/brd-interview`'s requirement-defect source, which said a log entry does not say which rows list its defect, now reads a defect's rows off its entry — the row it was raised on and every row it names — and decides ownership over all of them, lowest-numbered live row first: on the one-row listing alone, a split whose first row no slice builds was withheld as `no live row` while another slice built a sibling row. **Population: every split, restatement or clash whose raised-on row no slice builds while a row it names is built.** Ids are assigned when the walk ends, in the inventory order of the row raised on, then §3's class order, then the order `brd-reader` returned them in, and after them those raised from documentation, in the order the grounder returned them — never in the order the walk asked, which documentation grounding reorders. Each candidate's question now carries the same facts: its class, its row and that row's text, its reason, each counterpart a `conflict` or `duplicate` names, and, for a row drawn from an image, the image's path. **A log an earlier run already split three ways is not merged**: every id in it is permanent, and a re-run matches the one new candidate against those entries by the rows they join, none of the three then reported as not re-raised (above).

### Fixed — a re-run reworded requirements over an unchanged source, and filed a row it had only not re-read as one the source no longer contains

Phase 3 matched a returned row to one on file "by `source_anchor`, else by its `text`". The anchor half assumed one anchor names one row, while the rows one split produced share one — three in one verification run, each matching all three; the text half never said whether text must match exactly, or past a `<br>`; and a row matched by anchor silently took the new read's wording. Measured on a byte-identical source, a re-run rewrote the `text` of 10 of 14 inventory rows and their ledger rows, and in another a row lost *"finance approval completes it"*, the dropped words then minted as a new id. A row the read did not return was kept as one *this source no longer contains* and passed over by relation 1 even where the source had not changed, and nothing said whether it kept its `defects`. And an `EMPTY` read over an intaken BRD rewrote its inventory with no row while `brd/brd-defect-log.md` kept entries citing the dropped ids, so the next readable re-run numbered from `[BR#1]` and every kept entry named whichever requirement took its old id. **Population: every `/brd-intake` re-run over an intaken folder, on 3.6.x as well — and, for the defect log below, every first intake whose read found no requirement.** Phase 2 now records each file a re-run captures as unchanged, replaced or new (below). Phase 3 matches in two passes — by resolved anchor, one existing row to one returned row, and among rows sharing an anchor by text alone; then by text — comparing text decoded (`brd-format.md` §2.3) with every run of whitespace collapsed. A matched row keeps its wording wherever the file its anchor points into is unchanged since the inventory's rows were last reconciled, and takes the new wording, reported old → new, only where Phase 2 records that file replaced. **That is judged against a record the inventory now carries, never against the copy on disk**: its frontmatter's `document:` names the document the rows were read from, and its `captured:` map holds the SHA-256 of every captured file as the rows were last reconciled (`brd-format.md` §2). Judged against the copy, a run that stopped between Phase 2's copy and Phase 3's write, or a read that found no requirement, left a replaced copy that the next run found unchanged, and a revised requirement kept its old wording. The document is judged against the map's entry for the one `document:` names, whatever either is called — never against the document `brd/brd-link-log.md` opens by naming, which every Phase 2 rewrites, so a run that copied a renamed document and stopped before Phase 3 would have left the next run judging it new and keeping its predecessor's text — and a revised document sent under a new filename rewords the rows anchored in it. `/brd-split`, which names the document in a slice inventory's `source:`, and `/brd-package`, which names it in the bundle, read the same `document:` — the parent's on a slice — and not the log's opening line, which after such a stopped run names a document no row was read from; on an inventory carrying no `document:` — one written before 3.7.0 that no 3.7.0 intake has run over — each still reads the log (`brd-format.md` §1.1). **The first 3.7.0 intake over an inventory written before 3.7.0 that holds a row writes that inventory's `document:` in Phase 2, before it copies anything or rewrites the log**, naming the document as §1.1 tells a reader to — off the log it is about to replace, or by that section's rules for an older log or none — and adding that one line alone, `kind: brd` and `key:` unchanged, so the folder is never keyless; it writes no `captured:` map, which nothing on file could fill, and Phase 2 now tests for the map rather than for both fields. Before, a run that stopped before Phase 3 left such an inventory with no `document:` beside a log naming the document it copied, which every reader then took for the one the rows were read from; `brd-format.md` §1.1 stated that as a limit, and no longer does — what it still states is the same case left by an intake before 3.7.0, which no record tells apart. **Population: every 3.7.0 re-run over a BRD intaken before 3.7.0 whose inventory holds a row, and every `/brd-split` or `/brd-package` run after such a re-run stopped before Phase 3.** A file recorded replaced whose copy already stood with its bytes — an earlier run copied it and reconciled no row against it — is named so in the final report, so the operator sees why a file nothing was written to is called replaced. **A one-time migration comes with it**: an inventory written before this release carries neither record, so its first re-run on this release records every file whose copy already stands replaced, and the document whatever its name, and rewords every matched row to the new read's wording, once, each change reported old → new, and writes the map every later run judges against — **population: the first 3.7.0 re-run over each folder intaken before 3.7.0 that holds an inventory row**. Where a returned row is still unmatched over a file recorded unchanged, beside a row on file still unmatched under the same anchor, Phase 3 now asks the operator whether it is the same requirement — showing every row on file still unmatched under that anchor, a typed `[BR#n]` choosing among them — rather than minting a second id for one a read worded or cut differently, and every id a re-run mints is reported by id, never as a count. **Of the two measured cases, the second is only partly fixed, and is recorded as such**: `[BR#7]` now keeps its words over the unchanged file, but the read's row for *"finance approval completes it"* carries a different anchor, so no pass matches it and the same-or-new question, which asks only about rows sharing an anchor, never reaches it — it is still minted, as `[BR#14]`, and is now reported by id with its text, where the operator can see that it restates part of `[BR#7]`. **Population, for the record: every re-run after a run that copied a revised file and reconciled no row against it, and every re-run over a document revised under a new filename; for the question: every re-run over an unchanged file holding rows that share an anchor.** A row nothing matched is kept whole, `defects` included: as *not re-extracted by this read* where its anchor still resolves, which relation 1 still tests, and as one *this source no longer contains* only where it does not. An `EMPTY` read over an earlier intake's inventory keeps the inventory, its `document:` and `captured:` map included, the defect log, the ledger and each figures section's *Rows* line exactly as they stand and renumbers nothing — a *Rows* line naming kept rows, `illustrates` included, is not recomputed from a read that returned none (`brd-format.md` §1.2), its final report's ledger line resolves any `covered-by` row one hop, as `coverage-ledger-format.md` §6 fixes, and Phase 8 offers `/brd-split` only where a ledger row is still `unallocated`, since a bare run over a ledger its walk already settled moves nothing; on a first intake it still writes the inventory and the ledger with no row, and now the defect log too, which Phase 7 declared and nothing wrote. Phase 0 step 7's confirmation, skipped where every ledger row was still `unallocated` although the re-run rewrites the inventory, now fires wherever an earlier intake's inventory holds a row, and says what the re-run keeps — every id, every defect with its resolution, a row's wording over an unchanged file, an unchanged image's transcription — and what it may change, every ledger disposition among them. The remedies that name this re-run as the way to re-take an inventory — in `/create-prd`, `/create-ard`, `/specify`, `/epics` and `coverage-ledger-format.md` §5, and the `/create-prd` and `/prd-ground` documentation pages — said it rewrites the ledger with every row `unallocated`, which an `EMPTY` read does not do; each now says so wherever its read finds a requirement, citing that step for the rest.

### Fixed — `coverage-ledger-format` §3 said no command moves a row back to `unallocated`, beside the one that does

§3 said *no command ever moves a row back to `unallocated`*, and the root-level remedies in `/create-prd`, `/create-ard`, `/specify` and `/epics` quoted it — while each of the same remedies offers a `/brd-intake` re-run, which rewrites a source-owning BRD's ledger with every row `unallocated`, as it did on 3.6.x. §3 now names that re-run as the one exception: taken only on the operator's consent at its Phase 0 step 7, and never reaching a slice's ledger. The four quotes follow it; the reason those remedies give for a bare `/brd-split` being a no-op now says nothing *but that re-run* moves a terminal row back; the three sentences about a slice's non-empty `CREATE_PRD_BRD_NOT_ELIGIBLE` branch — in `/epics`, `/update-prd` and §5.2 — now say no command moves a *slice's* row back; the removal repair in `/brd-split`, its documentation page and §3.2, which said `unallocated` is a value no command may write, now says `/brd-split` never writes it onto a row carrying a fate, and the `/brd-split` re-cut's seeding sentence scopes the same claim to a slice's ledger; and `docs/brd-workflow.md`, which said no command returns a row to `unallocated`, now names the re-run that reopens every row at once, as does the 2.1.0 entry below that said the same. **No run's behaviour changed. Population: every reader of those sentences — an agent taking one as proof that no allocation can be re-taken.**

### Fixed — a revised document was written over the copy `brd-format.md` called immutable, and nothing said so

`brd-format.md` §1 said nothing under `brd/source/` is ever edited after intake; §1.1 called the document's re-copy on a re-run "additive"; and Phase 2 copied a revised document over `brd/source/<basename>` without comparing it or reporting that it had. **Population: every `/brd-intake` re-run over a revised document, or over one whose linked file at the same path changed.** §1 now states the rule both sections follow: `/brd-intake` Phase 2 alone writes under `brd/source/`, only ever a byte-for-byte copy; a re-run replaces a file's copy only where its bytes changed, git keeping the earlier one, leaves an unchanged file untouched and removes nothing; and the final report names every file it records replaced. A document revised under a new filename is copied at that name beside the earlier copy, which stays, and the final report names each slice whose inventory's `source:` still names an earlier document, whose rows keep resolving against it. `brd/source-external/` never replaces a copy at all: a changed file lands beside the old one under collision rule 2's name.

### Fixed — five smaller `/brd-intake` defects

- **A path typed before the key became the key.** Phase 0 step 1 took the first token that is neither a flag nor a flag's value, so `@<brd-file>` typed first stopped the run with `BRD_INTAKE_NEEDS_KEY`. It now passes over a token opening with `@`, which is step 2's source wherever it stands. **Population: every run whose operator types the path first.**
- **"Kebab-cased" did not say what becomes of punctuation.** Step 7's slug now lowercases the first heading, turns every run of characters outside `[a-z0-9]` into one `-` and trims both ends — `Acme reporting — business requirements` gives `acme-reporting-business-requirements` — falling back to the filename by the same rule, and to `brd` where both leave nothing. **Population: every new BRD whose first heading carries anything but letters, digits, spaces and hyphens.**
- **A declined handoff had no outcome line, and the final report asked for a note defined nowhere.** Phase 7 named an outcome line for its first choice only; the other two now emit `workflows-core:phase-handoff` §4.1's *Declined by the user* line with the advisory clause the array promised. The final report's "`brd` prefix note" is gone. **Population: every run whose handoff was declined, and every final report.**
- **`<how to cut it>` had no value.** Phase 8's `/brd-split` offer now says it is printed as it stands, a placeholder the operator fills with their own slicing instruction. **Population: every run offering it.**
- **`Proceed with <folder>` did not say name or path.** It is the folder's name. **Population: every run.**

### Fixed — a new BRD folder was keyless from its first copy until its inventory was written

`workflows-core:addressing` §4 requires the command that creates a folder to write a keyed artifact into it **in the same act**, so a folder is never keyless. `/brd-intake` Phase 2 created the BRD folder by copying the customer's document into it, and nothing in that folder carried `kind:` and `key:` until Phase 3 wrote `brd/brd-inventory.md` — so every stop between them (a failed copy, a `figure-reader` batch that could not run, an interrupted session) left a folder `resolve-address` could not identify, and a re-run over it resolved `absent`. Phase 2 now **keys the folder first**: where the folder holds no `brd/brd-inventory.md` — always so for a new one — it writes that file's header, `kind: brd` and `key: <BRD-KEY>` between `---` lines and no row, before anything is copied, and Phase 3 fills in the rows below it. A run that stops in between leaves a header-only inventory — as a Phase 3 `NOT_FOUND` stop and a completed first intake whose read was `EMPTY` do — and Phase 3 now says an inventory holding no row is **no prior inventory**, whatever left it so: a re-run numbers as a first intake does, every first-intake rule applying. Phase 3's `EMPTY` branch said to write "an empty `brd/brd-inventory.md`", which read as a zero-byte file would un-key the folder again; it now writes the header alone. `workflows-core` 1.7.1's `addressing` §4 names `brd/brd-inventory.md` as the carrier a root BRD is resolved by, which is what makes the header enough. **Population: every `/brd-intake` run that creates its folder**; on a folder already holding an inventory, Phase 2 leaves it as it stands.

### Fixed — `brd-format` showed the inventory header without its frontmatter delimiters, and gave a reason about `addressing` §4 that no longer holds

§2.1's header block listed `kind:`, `key:`, `parent:` and `source:` with no `---` lines, where the family's other artifact templates show their frontmatter inside the block. It now shows the delimiters and says the file opens with frontmatter; the paragraph under it says `workflows-core:addressing` §4 reads a source-owning BRD's identity off this file where no top-level file carries `key:` beside a `kind:` naming a folder kind, that `/brd-intake` writes the header, with no row, before it copies anything into a new folder (its Phase 2), and that a slice's inventory, which carries the pair too, is never the slice's carrier — its `brd-link.md` is. It had called the two fields *"how the folder asserts its own identity"* of every inventory, a slice's included. §1.2's bullet on `brd/brd-figures.md`'s frontmatter justified carrying `key:` and no `kind:` by §4 reading "the first artifact in it carrying both" with "nothing fixes which is first"; `workflows-core` 1.7.1 fixes the order and names the inventory as the one file under `brd/` it reads, so the bullet now gives that as the reason. **Population: every writer of an inventory's header — `/brd-intake` Phase 2, which writes a source-owning BRD's before anything is copied, and `/brd-split` Phase 3 step 4, which writes a slice's and cites §2.1 — and every reader of either section.**

### Fixed — `/brd-package` stopped on its own inventory's section anchors

The plugin-free scan's classes include *"any `§`"*, Phase 8 rule 7 runs that scan over every document in the finished bundle, and on 3.6.x its only exemption was the customer's source document. The inventory ships in every bundle, and a `source_anchor` locates a requirement in the customer's own numbering — `brd-format.md` §2.2 records that every anchor in the corpora it was measured on opens with a `§` and a section number. So read literally, rule 7 stopped every package whose inventory carries such an anchor, on text the plugin copied from the customer. **Population: every `/brd-package` run on 3.6.x over an inventory with one.** A customer-derived locator's leading section reference, in its fixed position — opening a `source_anchor`, or opening what follows the ` › ` after a captured file's path — is now no hit at all (`bundle-packaging.md` §6.3 rule 1), and the reader resolves it by opening the customer's file, which the bundle carries.

### Changed — `/brd-package` ships what the customer wrote, and reports rather than stops on it

The bundle now carries every markdown file `/brd-intake` captured, from `brd/source/` and `brd/source-external/`, byte for byte as the source document always was; `brd/brd-figures.md` where it exists, its frontmatter replaced by a note naming the customer's document; and every image the figures file holds a section for, save one a later intake no longer captured and no preserved row still anchors on, together with every image a captured file reaches through a wikilink or from outside its folder, found through the link log (`bundle-packaging.md` §1.1, §2.1). The manifest maps each captured file's path relative to `brd/` — the form an appendix or image anchor, a figures heading and an interview question naming an image all use — to its bundled filename. Each bundle document's filename now **begins** `<BRD-KEY>-`, the form the citation-resolution check already tested for; 3.6.x required only that the name carry the key, and a document carrying it elsewhere was one that check's filename relation skipped. The prompt's *documents to review* part names the manifest and then every other document `bundle-packaging.md` §1.1 admits, by those bundled filenames; 3.6.x filled it from the manifest, which a later phase writes.

**A plugin token in the customer's own words, or in a customer-derived locator, is now reported rather than stopped on**, by the plugin-free scan and the citation-resolution check alike. 3.6.x gave that treatment to the customer's source document alone. It now reaches every captured file; the parts of the figures file that transcribe the image's own words — its *Text*, the *Says* of its annotations, its *Flow*; the inventory's and the ledger's requirement text, where it quotes the customer; and every customer-derived locator — the customer's names for their own files, headings and links, in the positions `bundle-packaging.md` §6.3 recognises them in, including this package's own writing. Hits there are grouped, one line per file and token with a count, and the operator decides whether to ship: a `D3` in a customer's diagram or a `references/` folder in their layout had no repair but renaming their files, retitling their headings or making a transcription say something the image does not, and each of those falsifies the record. Every other hit stays a hard stop.

**Population: every package** — the report-not-stop treatment reaches the inventory's and the ledger's text and the anchors of any BRD — **and, for the larger bundle, every package over a BRD whose intake captured a markdown file beside the document or wrote a figures file.** **What it costs**: that larger bundle, carrying every captured page and image and the transcriptions, and a grouped report of hits inside the customer's own content that the operator rules on before the package ships, where 3.6.x either stopped or never looked. **An appendix the current document no longer links still ships**, with the images it embeds: a captured file is never removed from `brd/source/` or `brd/source-external/`, and nothing records which intake captured a markdown file, so the bundle cannot tell a page the latest intake read from one an earlier intake captured.

**One position is deliberately not relaxed.** A customer heading or section number quoted in this package's own prose **without its captured file's path** — `(see § 4.2)` — stays a hard stop, as every `§` in plugin-written text was on 3.6.x: a pathless `§` there cannot be told apart from a plugin citation. The repair is to write it as a path, `source/<basename> › § 4.2`, whose leading section reference is exempt.

### Added — `/brd-intake` reads what it captures

On 3.6.x the intake copied every file the document linked from its own folder and read none of them — `brd-reader` read the document alone — so a requirement stated only in an appendix or only in a screenshot yielded no `[BR#n]`, and no later command read those pictures either. Customers' BRDs routinely carry annotated screenshots of the current product, mockups, sample outputs the result must match, and diagrams. Now:

- **Phase 1 walks every link first, read-only** (`references/linked-sources.md` §4), before anything is written: it shows how many files of each kind — markdown, image, other — lie inside the document's folder and outside it, and every target it could not resolve. Where anything lies outside the folder it asks first — *Capture all*, *Only the document's own folder — capture nothing outside it*, or *Stop* — and where the taken set holds a file that is neither markdown nor an image it asks next, with *Stop and convert them first* recommended: `BRD_INTAKE_UNREAD_ATTACHMENTS` names each such file, since it would be copied and never read.
- **Files from outside the folder land in `brd/source-external/`**, at their basename under `idea-format.md`'s collision rule, immutable as `brd/source/` is and never replaced by a later intake, a changed file landing beside the old one under a new name; `brd/brd-link-log.md` gains a second table mapping every captured link that does not resolve as written — a wikilink, an outside file, an absolute path — to its copy, and never rewrites the customer's link.
- **Phase 2.5 has `figure-reader`, a new Opus agent, transcribe every captured image** — each label, header, field, legend and annotation verbatim, what each annotation points at, a diagram's flow, and one sentence on what the image depicts — at most 10 images per dispatch and 4 dispatches at once, with no cap on the total. It never reads the document that links the image, so a transcription cannot be bent toward the prose. The transcriptions go to `brd/brd-figures.md` (`brd-format.md` §1.2), the plugin's record of its reading beside the link log, and a later intake re-uses a section whose image's content hash still matches instead of reading the image again.
- **`brd-reader` reads the document, every captured markdown file and the transcriptions together.** A row can now anchor in an appendix (`<path> › <heading path or line range>`) or on an image (`<path> › "<element quoted verbatim>"` or `annotation <n>`), paths relative to `brd/`. An obligation only an image states, with no prose anywhere stating it, is proposed with an `ambiguity` candidate — *stated only in `<image>` — binding force unknown* — unless a passage linking the image makes it binding; an image contradicting a prose row is an ordinary `conflict`. Phase 4 puts an image-drawn candidate to the operator with its transcription and tells them to open the picture, since the transcription is the plugin's reading and the picture is what the candidate is about.
- **The coverage check reaches every captured file.** Every top-level section of each captured markdown file must hold a row or be accounted for, as the document's always had to, and every image must yield a row, illustrate one, or be accounted for; Phase 5 records which on each image's *Rows* line. A section holds a row where an anchor names it, or where it links an image or a captured markdown file that yields one — so a section whose only content is a link to an appendix is not put to the operator as empty. `brd-format.md` §2.2 now defines a top-level section: where one heading titles a file, the sections at the shallowest level beneath that title, so the ordinary shape of one `#` title over `##` sections is checked at `##` granularity rather than as one section that is always held — and every heading path, in an anchor or a figures file's *Linked from*, leaves that title out. **Population: every intake of a document one heading titles** — on 3.6.x the check ran over the document with the term undefined, and read literally could never fire on such a document — **and what it costs**: more Phase 3 coverage prompts, and different ones, since each `##` section holding no row is now put to the operator, while a section whose only content links an appendix that yields a row no longer is.

**What it costs.** Every image the intake takes is transcribed on Opus on first intake, ten to a dispatch, and the intake may ask one or two more questions at Phase 1. A BRD linking a PDF or a spreadsheet, which 3.6.x copied without comment and never read, now meets a question whose recommended answer stops the run until the file is converted, or until the operator records that it carries no obligation. **Transcription fidelity on real images is unmeasured** — low resolution, scribbled mark-up and where an arrow lands are caught, when they are caught, by Phase 4's operator and by the customer, whose review now confirms or corrects the transcriptions of their own images (`customer-review-schema.md` §4, section 4). A correction the transcription itself must carry comes back as a section-12 required change naming the figures file, and `/brd-reconcile` Phase 6 applies it there in place — to a section's *Text*, *Annotations*, *Flow*, *Illegible* or *Depicts*, never to the image, and never to an inventory row quoting the corrected element, which goes to the reconciliation record's *what still needs a human*. A corrected transcription is not corrected requirement text, so it closes no defect: one the row carries stays open for its own question. A later intake keeps the corrected section for as long as the image's bytes are unchanged. **A re-intake after such a correction mints a second `[BR#n]` for the image-drawn row**: the read quotes the corrected element, which no longer matches the earlier row, so the earlier row is kept as one this source no longer contains, and the new row's binding-force `ambiguity` is a new defect that may be asked again.

### Changed — `/idea` walks every link and asks rather than caps

`idea-reader` followed links two levels deep and read at most twelve markdown files, the source among them, and six images, reporting the rest `reason: cap` or `reason: depth`. Those bounds are gone. **Phase 1.5** runs the same walk `/brd-intake` runs, from a markdown source, and asks nothing where the walk stays inside all three old bounds; where it reaches past any of them — a markdown file deeper than two levels, more than twelve markdown files, or more than six images — it names every file past the bound and asks: *Read all*, *Only what the source links directly*, or *Stop*. A file left out is reported `excluded`. `figure-reader` then transcribes every taken image, in parallel, before `idea-reader` runs, and each image's digest `description` is the transcription's `depicts` sentence; `idea-reader` reads the source, exactly the pages the walk took, and the transcriptions, and follows no link and opens no image itself. A `[[name]]` resolves the way it does for `/brd-intake`, so two runs over one vault no longer resolve a bare name differently. **What it costs**: an image is now read on Opus rather than on `idea-reader`'s Sonnet tier, and Phase 4.5 vendors every page the walk took and every image `figure-reader` read, with no cap of its own, so a source linking a hub note can leave dozens of copies for its handoff to stage.

### Fixed — nine defects in `/idea` that its verification runs found

Nine defects the verification runs of this release met in `/idea`, each confirmed on the tree as it stood before the release began; none is introduced by it.

- **Phase 0 said it created a folder it had no name for.** It resolved the key and said `PRD-<KEY>-<slug>/` is "created there when absent", but the only slug a run holds is `idea-reader`'s `candidate_slug`, which arrives in Phase 2, and Phase 4 then wrote into "the folder Phase 0 resolved" — none, on `absent`. Phase 0 now creates nothing: on `absent` the folder is created by Phase 4's first write, as `PRD-<KEY>-<candidate_slug>/` with `idea.md` in it, and Phase 4 and Phase 5 name it as the folder resolved or created. The command's documentation page says so, no longer says a key that resolves to nothing stops the run, and names Phase 0 as the command does. Phase 4's `kind`/`key` step, which said this command creates the folder and so writes its only keyed file, now says so only where Phase 0 returned `absent`. **Population: every `/idea` run on a key that has no folder yet** — the ordinary first run; each verification run created the folder in Phase 4 on its own judgement.
- **A correct `<KEY> @<file>` invocation read as a path that resolves to nothing, and every prompt read its own key as idea text.** Phase 1 classified the argument with only the flags removed, so the Phase 0 key token and the `@` were still in it, and a literal reading lands on Case B's *Re-enter the path* on a run with nothing wrong. Phase 1 now removes the key token and every flag with its value, and a leading `@` marks a file whose path is the text after it — the path rule 1 tests and Phase 2 hands `idea-reader`, whose input now says so. **Population: every `/idea` run** — over a markdown source a literal reading lands on Case B, and on a prompt the key went into the raw idea text handed to `idea-reader`.
- **The `docs grounding:` line was shown before anything had resolved it.** Phase 1 showed it "as resolved", while the command's only `resolve-docs-grounding` call sat in Phase 2.5. Phase 1 now runs that procedure itself and shows the line from what it returns, so its one-time question about building the docs index comes before the link walk and the readers, as `workflows-core:docs-grounding` step 3.5 asks; Phase 2.5 dispatches the docs grounder on the state Phase 1 resolved and resolves nothing again. **Population: every `/idea` run** — as written, only under `--no-docs` was the line's value knowable at Phase 1.
- **Phase 2.5 said to dispatch "both grounding agents" in one response.** It dispatches one, the docs grounder; Phase 2.6 says in its own words why code grounding is kept out of Phase 2.5. The sentence now says so. `/create-prd` Phase 2.5 carried the same sentence, and beside it a BRD-route paragraph handing a `known_refs` input to no agent — both left behind when the prior-art finder was removed — and it now names the one agent and the inputs it takes. **No run's behaviour changed**: neither Phase 2.5 has a second agent to dispatch. **Population: every reader of either phase.**
- **`--docs <path>` was missing from the Flags paragraph**, which Phase 0 cited as "the flag list below" although it stands above; Phase 0 and Phase 1 both parse `--docs` as a flag that takes a value. The paragraph lists it and Phase 0 cites it as the paragraph above. **Population: every reader looking the flag up** — the parsing was already right.
- **A link in `idea.md` to the source file itself was never repointed at its copy.** Phase 4.5 copies the source into `attachments/`, but the rewrite map was keyed only from the digest's link arrays, which never hold the source, so a `## Signals & evidence` citation of the source brief kept pointing at the operator's disk beside its own copy — two of the three verification runs left one there, and the third repointed it only by treating the reference as a key the text never named. The map now has a row for the source, keyed by its `source_refs[].ref` exactly as the digest records it and valued at its `attachments/` copy, and Section 5 names that as the form to cite the source in. **Population: every `/idea` run over a markdown source whose brief links the source file**; a brief written before this release keeps that link as it stands.
- **Whether a linked page or image is an "ingested source" was left open.** `sources` listed "every ingested source", and Phase 4.5 recorded `vendored:` "beside each vendored entry's `ref:`", while `sources:` held the source alone. It is not one: `sources` names the one source each run ingests and, where it was copied, its `vendored:` path, and a linked page's or image's copy is recorded by the rewritten link in `idea.md` that points at it and, for an image, by its index row. **Population: every `/idea` run that vendored a linked page or image** — each verification run wrote the one entry, which is now the rule.
- **An index row's `Linked from` named a path on the operator's machine.** `idea-format.md` made it the image's digest `from`, an absolute path, while `workflows-core:grounding-format` §6.2's example showed a relative one. It now names the linking file by its vendored copy's path relative to the PRD folder (`attachments/p3.md`), its own path where it already sat inside the folder, or `—` where its copy failed; §6.2 owns the rule, changed in `workflows-core` 1.7.1. **Population: every `/idea` run that vendored an image**; a row an earlier run wrote keeps its absolute path, because §6.2 step 2 preserves every described row whose frame is still there verbatim.
- **`idea-reader` described `NOT_FOUND` as an invalid key, and could omit `signals`.** It takes no key: `NOT_FOUND` is a missing or unreadable source file, in the agent and in Phase 2. And `signals` joins the arrays returned as `[]` when empty and never omitted — one verification run's reader omitted it, and the rule as written allowed that. **Population: every `/idea` run whose source carries no demand signal**, and every reader of the `NOT_FOUND` line.

Three more stale lines sat beside the text these fixes changed, and are corrected with them: Phase 1 said two cases put the classification to the operator where only Case B is left, Case A having gone with the tracker export; `idea-format.md` Section 6 said a prior-art bullet takes "one of two shapes" directly above the paragraph saying there is one; and Phase 4 cited the `ambiguous` stop as `workflows-core:addressing` "§4 rule 5", a rule §4 never had — the stop is §3's, `resolve-key` step 5. **No run's behaviour changed. Population: every reader of any of them.**

**Two `provenance` values nothing produces are gone.** `idea-format.md`'s `sources` enum listed `rfe` and `doc-grounding`, `idea-reader` listed `rfe` among the hints it takes and the provenances it returns and kept an `rfe` split for product feedback, `/idea` Phase 1's future `--as` override named `rfe`, and `workflows-core:prd-format`'s propagated `sources` listed it with an "RFE key" ref, as `/create-prd`'s propagation note did. No command produces either value — Phase 1 classifies a prompt or a markdown file and `idea-reader` upgrades a markdown file to `community-post` or `prd` — so each list now carries only what a run writes, and `idea-reader`'s hint is the two values its caller computes. A value left in an existing `idea.md`, or propagated from one into a PRD, stays readable: nothing decides anything on it, and `idea-format.md` says so. **No run's behaviour changed. Population: every reader of those lists.**

### Fixed — `/idea` accepted any folder its key named, left `source_refs` unpinned, and grounded on themes nothing produced

Four `/idea` defects that predate this release:

- **`IDEA_NOT_AN_IDEA_FOLDER` is new.** Phase 0 treated any folder a key resolved to as the one to write the brief into, whatever it was. A key naming a `BRD-` container, a BRD-route slice (a `PRD-` folder carrying `brd-link.md`) or an `EPIC-` folder had an idea brief written where no PRD is ever seeded from one. Phase 0 now tests the folder itself, never the kind it asserts, since a slice asserts `kind: brd`: a `brd-link.md` naming a `parent:` at its top level first, then its prefix where the folder resolved through the prefixed glob, and positive evidence instead where it resolved through the legacy unprefixed fallback, whose name starts with the key, so a legacy folder keyed `BRD-12` is not taken for a BRD container by its name — a `coverage-ledger.md` or `brd/brd-inventory.md` for a BRD container, a resolved `kind: epic` for an Epic folder, as `workflows-core:addressing` §4.1 places a folder with no prefix. Every such folder stops the run with `IDEA_NOT_AN_IDEA_FOLDER`, naming the folder and what it is, before anything is read or written, and naming the run that does take that folder's work — `/create-prd <KEY>` for a slice; `/brd-split <KEY> "<how to cut it>"`, or `/create-prd <SLICE-KEY>` on a slice already carved, for a BRD container; `/update-prd <PRD-KEY>`, `<PRD-KEY>` being the key of the folder above, read off its carrier, or `/epics <KEY>` or `/specify <KEY>` for an Epic folder — beside a key of the idea's own for a genuinely separate idea. The paragraph after it, which said the key is checked against nothing, now says it is checked against no tracker and resolved only against the specs tree. **Population: every `/idea` run whose key already named a BRD, slice or Epic folder.**
- **`source_refs` is pinned.** `idea-reader`'s `source_refs` was an unbounded list of `path | KEY | url` refs, and nothing produces a key or a URL any more. It now holds exactly one entry for a markdown source, whose `ref` is the source's path exactly as the argument gave it, and none for a prompt. `idea-format.md`'s `sources` entry follows it: `{provenance, ref}` for a file, with `vendored` where the file was copied, and `{provenance: prompt}` with no `ref` for a prompt. `/idea`'s refine step writes that shape. `workflows-core:prd-format`'s propagated `sources` template now shows the `vendored` field and a prompt entry with no `ref`, and `/create-prd` propagates `vendored` with the rest of the entry where the idea it read is the `idea.md` in the PRD's own folder, and drops it otherwise: the path is relative to the folder holding that `idea.md`, so from any other folder it names nothing. **Population: every `/idea` run** — the ref that release 3.7.0's source-link rewrite keys on is now fixed rather than improvised.
- **The capability themes now have a producer.** Phase 2.5 handed the docs grounder the digest's `signals` as its `themes`, and those are demand bullets, empty for most sources. Phase 2.6 proposed repos from, and handed its scanners, "the `idea-reader` digest's themes", a field the digest does not have. Phase 2 now ends with the orchestrator naming 1–5 capability themes from the digest's `raw_context`, without changing `idea-reader`'s contract, and both phases use those. **Population: every `/idea` run with documentation grounding on, and every `--ground-code` run.**
- **Two smaller corrections.** Phase 0 said `--docs` and `--ground-code` "each consume the token after them", but `--ground-code` takes its value only on the conditions the Flags paragraph sets. The documentation page called the argument after the key "the single positional argument". Both now say what the command does. **No run's behaviour changed. Population: every reader of either.**

### Fixed — a declined `/idea` handoff sent the operator to a `/create-prd` that does not read the brief in contract

On `status: refined`, `/idea` Phase 5 presents `workflows-core:phase-handoff` §4.3's **gated — falling back** array, whose second option said the next phase *"reports the artifact as un-landed and proceeds from the resolved folder"*, and then recommended only `/create-prd <KEY>` with a merge clause. A declined handoff leaves `idea.md` on no ref, and `/create-prd <KEY>` on that state (row F) names the file and does not read it in contract: it goes on down its idea ladder — back to the brief only through its same-session rung or a path the operator types, otherwise to a PRD grilled from scratch. The draft branch already offered the form that reads such a file in place, `/create-prd <KEY> @<path>`; the refined branch, which leaves the same on-disk state after a decline, did not. It now says that on option 2 or 3 `handoff-to-main` does not run and §4.1's *Declined by the user* line is emitted, the run's emitter tail still running as it does after option 1, recommends `/create-prd <KEY> <merge-clause>` whichever option was taken, and **on a decline, or where §4.1's *Gate failed* line was emitted, offers `/create-prd <KEY> @<the absolute path of this idea.md>` beside it** — neither outcome committed anything — the route that does not wait for the files to land. The array's wording is `workflows-core` 1.7.1's to fix (its §4.3 now reads *"the next phase does not stop on this, but until this is on main it might not read your copy"*); `/idea` cites it, and the draft branch's own sentence about a bare `/create-prd <KEY>` — which said it *"grills the PRD from scratch, ignoring this file"*, where that command names the file and first reaches its same-session rung and its path prompt — now says what it does. `/create-ard` quotes the same array for an ARD handed off alone, and its copy moves with §4.3. The `/idea` documentation page's `/create-prd` line says the `@<path>` form is what reads an un-landed brief, and that a bare `/create-prd <KEY>` finds the brief once the handoff's pull request is merged. **Population: every run that presents the array — `/idea` on `status: refined` and `/create-ard` whose set is the ARD alone — and, for the `@<path>` route, every such `/idea` run whose handoff was declined or whose gate failed.**

### Fixed — four grill commands asked about the docs index after their work had started

`/create-prd`, `/update-prd`, `/create-ard` and `/specify` each showed the `docs grounding:` line in Phase 1 "in the form `workflows-core:docs-grounding` resolved", while the `resolve-docs-grounding` call that produces it ran later:

- `/create-prd` ran it in Phase 2.5 and `/update-prd` in Phase 2, so Phase 1 printed a result nothing had computed yet.
- `/create-ard` (Phase 3 step 5) and `/specify` (Phase 4) ran it only after their `code-scanner` dispatches.

`workflows-core:docs-grounding` step 3.5 asks its one-time question about building the docs index before any of the run's real work. In the last two commands that question therefore came after agents had run, and in all four the line could not be what it claimed to be.

Each command now runs `resolve-docs-grounding` in Phase 1, step 3.5's prompt included, and shows the line from what it returns. The later phase dispatches the docs grounder on that result and resolves nothing again. This is the same pattern `/idea` now follows, and the one `/brd-intake`, `/epics`, `/prd-ground` and `/release-notes` already did. `/update-prd`'s key step (Phase 0 step 1) also read "`--docs` each consume the token after them" of its one flag; it now reads "consumes the token after it".

**Population: every run of the four with documentation grounding in reach** — `$DOCS_PATH`, or its `/workspace/docs` default, a readable directory holding markdown, or a `--docs <path>` given. **Where `qmd` is installed and no collection covers that root**, `/create-ard` and `/specify` — the two whose ordering moved — now ask the index-build question before any agent is dispatched. The other runs see the line computed before it is shown, and nothing else changes.

### Fixed — seven commands cited a numbered section `workflows-core:docs-grounding` does not have

`/create-prd`, `/update-prd`, `/create-ard`, `/specify`, `/epics`, `/brd-intake` and `/prd-ground` each said `--docs <path>` is declared for every consumer by `workflows-core:docs-grounding` *"§1's *Flags first* rung"*, and `/create-ard`'s address step said the same section declares the flags' shape. That reference has no numbered sections: *Flags first* is step 1 of its *Procedure*. All eight sites now cite *Procedure* step 1 (*Flags first*); `docs-workflows` 1.2.2 corrects the same citation in `/release-notes` and in its repository-resolution reference. **No run's behaviour changed. Population: every reader following the citation.**

### Fixed — four commands placed a folder by the kind it asserts, and none placed a folder with no prefix

`/create-ard` Phase 0 step 1 opened its altitude list with *"The resolved kind decides the altitude"*, while a BRD-route slice's resolved kind is `brd` — `workflows-core:addressing` §4 reads it off the slice's `brd-link.md` — and the list beneath it decided by the folder's prefix, with no entry for a folder that has none: one §5's legacy fallback resolves, or an unprefixed folder an `@<path>` names. `/specify` said *"The kind decides the altitude"* in its step 1, both commands' one-address stops said *"The kind of the folder the address resolves to is what sets the altitude"*, and `/epics` cited `/specify`'s derivation as *"the resolved folder's kind decides the altitude"*. `workflows-core` 1.7.1's `addressing.md` §4.1 now owns the rule, and `/create-ard` and `/specify` cite it: the prefix places a folder — its name beginning `<KIND>-<the resolved key>-`, so a legacy folder whose key begins with a kind token is not taken for a prefixed one — and a folder with none is placed by positive evidence, the container test first: a resolved `kind: epic` is Epic-level; a resolved `kind: prd`, or a `brd-link.md` naming a `parent:`, PRD-level. `/create-ard` and `/specify` now place such a folder by it — a legacy slice by its `brd-link.md`, onto the BRD route — both stops say the folder's prefix sets the altitude, and `/epics` says its gate decides by the artifact the folder holds, which is what it always did — it places no folder by §4.1, and takes from it only what a prefix is (below). `/prd-ground` refused an `EPIC-` folder by its prefix alone, so a legacy Epic folder fell through to the interrupted-intake stop naming `/brd-intake`; it is now refused as an Epic folder. The container test — `coverage-ledger-format.md` §5.1, and the copies in `/create-ard`, `/specify`, `/epics`, `/prd-proposal` and `/brd-proposal` — was scoped to a folder §5's fallback resolved, so an unprefixed root BRD an `@<path>` named, which §3 resolves without the fallback, reached none of them; each now takes it. §5 says its `BRD-` prefix is §4.1's, so a legacy folder keyed `BRD-12` is not a container by its name. The four refusals that test a prefix themselves — `/create-prd`'s, `/epics`', `/prd-proposal`'s and `/brd-proposal`'s, the last refusing a `PRD-` slice — said only *"the directory prefix"*, which a literal reading takes a legacy folder's name to carry wherever its key begins with the kind token tested, so an idea-route folder keyed `BRD-12`, named `BRD-12-checkout/`, was refused as a container; each now defines the prefix as §4.1 does. `/create-prd`'s two sentences, the `/create-ard` and `/specify` documentation pages', and §5's prose and §5.2's table, which said a container refusal is decided on the folder's kind, now say its prefix; and the `/create-ard`, `/specify` and `/prd-ground` documentation pages state the placement. `dev-workflows` 4.1.2's `/ready`, `/design` and `/implement` take the same rule. **Population: every `/create-ard` or `/specify` run on a folder with no kind prefix, every `/prd-ground` run on a legacy Epic folder, every `/create-ard`, `/specify`, `/epics`, `/prd-proposal` or `/brd-proposal` run handed an unprefixed root BRD by `@<path>`, every run of those four refusals handed a legacy folder whose key begins with the kind token it tests — `/create-prd` by that key, the other three by an `@<path>` naming it, since their text already took a folder §5's fallback resolved as carrying no prefix — and every reader of the steps.**

### Fixed — `/specify` and `/create-ard` tested a `focus_key` they never defined

Both commands' Phase 0 step 1 carries `<EPIC>` — the resolved `EPIC-` folder's key, or `null` for a `PRD-` folder — while their later phases test and set `focus_key`, a name neither command defined anywhere, as `dev-workflows`' `/design` did until its 4.1.2. Each now defines it where step 1 carries `<EPIC>`: one value under two names, which `/specify`'s Phase 2 Step A picker may set on a `PRD-` address. **No run's behaviour changed where the name was read as `<EPIC>`. Population: every reader of the two commands' later phases.**

### Fixed — `/specify` derived its candidate repositories from one folder's implementation record

Phase 3 step 1 took repositories from *"the resolved folder's `implementation.md`"* — one file, the folder the address named. `/dev-workflows:implement` writes a record into the folder of the unit it implemented — from `dev-workflows` 4.1.2 an Epic's folder whichever way the Epic was chosen, which `workflows-core` 1.7.1's `implementation-format.md` §1 now states — so a run addressed to a PRD that chose an Epic in Step A read the PRD folder's record and not that Epic's, and a broad PRD-level spec read no Epic's at all, although its Step B reads every Epic folder. Step 1 now reads the records in the folders Step B read: the focus Epic's where `focus_key` is set, the PRD folder's and every `EPIC-` folder's where it is null. The documentation page says the same. **Population: every `/specify` run whose Epics hold an implementation record — a re-specification after `/implement` has run — on 3.6.x as well.** The list is a candidate set the operator confirms, so what was lost was a starting candidate, never a gate.

### Fixed — `/epics` looked up a re-refine run's ARD by the Epic's key

Phase 2.5 ran `workflows-core:ard-resolution` with `prd = key`, the run's own key, which on a re-refine run — an `EPIC-` address, Phase 0 step 1b — is the Epic's. That reference resolves `prd` to the folder whose `ard.md` it reads at PRD level, so a re-refine run read the Epic folder's own `ard.md`, where one exists, as the PRD's, and never the PRD's. It now passes `<PRD-KEY>`, the PRD folder's key step 1b derives, and its note on `epic: null` no longer says Epics do not exist yet, which a re-refine run contradicts. **Population: every `/epics` re-refine run whose PRD or Epic folder holds an ARD.**

### Fixed — `/create-prd` said a `rejected` row cites a defect, and nothing else

`/create-prd`'s `CREATE_PRD_BRD_NOT_ELIGIBLE` table said a `rejected` gate-set row "cites the `[DEF#n]` justifying it". A slice's ledger can now also hold `rejected: <SLICE-KEY>/[CD#n]`: the customer's withdrawal, which `/brd-reconcile` records (`coverage-ledger-format.md` §3). The row now names both, as §5's table it mirrors does. **No run's behaviour changed. Population: every `/create-prd` refusal on a slice whose rejected rows include a customer withdrawal.**

### Fixed — `/create-prd` said an unset `key` made a slice resolve as a BRD

`/create-prd`'s frontmatter step said that, with `key` unset, *"a folder whose only `kind:`+`key:` carrier is `brd-link.md` (`kind: brd`) resolves as a BRD rather than a PRD"* — implying that setting it makes the slice resolve as a PRD. `workflows-core` 1.7.1's `addressing` §4 fixes the order a folder's carrier is found in, and a slice's `brd-link.md` sorts before its `prd.md` whether or not `prd.md` carries a key, so a slice resolves as `brd` either way — the reading this plugin's container refusals already rest on. The sentence now names the defect that was real, the empty key `/document` and `/release-notes` build their commit scan from, and says the folder still resolves. **No run's behaviour changed. Population: every reader of `/create-prd` Phase 3's frontmatter step.**

### Fixed — `specification-format.md` said `specification.md`'s `kind:` and `key:` are how its folder asserts its identity

The frontmatter section said both fields *"are how the folder asserts its own identity"*, citing `workflows-core:addressing` §4. `workflows-core` 1.7.1's §4 now fixes which file carries a folder's identity — the first top-level file whose `kind:` is a folder kind (`brd`, `prd` or `epic`), else `brd/brd-inventory.md` — so `kind: specification`, which names the document and not the folder, is passed over, as it had to be for the resolution record to hold one of those three. The section now says `key:` records the folder's key so nothing parses one out of a directory name, `kind:` names the document, and §4 reads the folder's identity off the folder's carrier in its order — `brd-link.md` in a BRD-route slice, `idea.md` or `prd.md` in an idea-route PRD folder, `epic.md` in an Epic folder — never this file. **No run's behaviour changed. Population: every reader of the specification format's frontmatter section.**

### Changed — `brd-reader` runs on Opus

`brd-reader` was pinned to Sonnet on the grounds that *"extraction is mechanical"*. That holds for splitting requirements and is false for the half of its job that proposes defect candidates: a missed conflict, an ambiguity read as settled or a misread transcription yields no candidate, so Phase 4's human never sees it, and the failure is silent in exactly the direction that matters. It is now frontmatter-pinned to Opus, as `figure-reader` is, and both degrade to the best available model — recorded in the run's notes and final report — where Opus does not resolve. **A text-only BRD now costs more to intake than it did on 3.6.x.** **The Opus default for both agents is unmeasured**: it was taken on that asymmetric-cost argument, and no comparison of tiers on real customer documents and screenshots was run (spec §13).

### Fixed — `BRD_INTAKE_DANGLING_ANCHOR`'s remedy could not get past the stop

The stop told the operator to correct the anchors by hand and re-run, and in the same breath not to re-run `brd-reader` over the whole document *"which would renumber every row"*. The re-run it named does re-run `brd-reader` over the whole document, and nothing is renumbered — Phase 3's reconciliation keeps every existing id — but that reconciliation wrote the anchor `brd-reader` returned over the one the operator had just corrected, so a re-run met the same stop on the same row. A matched row now keeps its existing `source_anchor` where that one resolves and the returned one does not (Phase 3), and the stop names the re-run and says what it keeps. **Population: every intake that met this stop on 3.6.x and followed its remedy.**

### Changed — a `§` anchor that names no heading now stops the intake

`brd-format.md` §2.2 branch 1 resolved a leading section reference "directly", matching the number against nothing, so an anchor reading `§ 99` passed relation 1 in a file whose sections stop at 12. It now resolves only to the one heading carrying that number — opening its text, after an optional `§`, followed by a space, by a full stop and a space, or by nothing — so `§ 7` finds `§ 7 Retention`, `7. Retention` or `7 Retention` and never `7.1 Scope`. A number no heading carries, or more than one does, falls to branch 4, and `/brd-intake` stops with `BRD_INTAKE_DANGLING_ANCHOR`, whose remedy — correct the anchor by hand and re-run — keeps every id. **Population: every intake whose inventory holds a `§` anchor naming no heading of its file, or naming a number several headings carry — rows that passed on 3.6.x and now stop the run.**

### Fixed — `/brd-package` recommended the repository route for a bundle it had not pushed, and settled the route from an answer it had not yet taken

Phase 7 asked *"They pull the specs repository (Recommended)"* wherever the handoff's consent choice was accepted — a choice the *Handoff* phase presents two phases later — and took the archive route only on a decline. An accepted handoff that ends *Push failed* or *No remote* leaves the bundle on a commit only the packaging machine holds, and one that ends *Gate failed* committed nothing, so the recommended route sent the customer to pull a bundle that is not there; and no reading of the phases in order gave Phase 7 the answer it keyed on. Keying it on the `Phase handoff:` line instead, as an earlier draft of this fix did, cannot work either: the delivery note Phase 7 writes is one of the files that handoff commits, so the note cannot wait on its outcome. And even a pushed bundle is on a `brd/<KEY>-<slug>` branch — suffixed `-2` where that name is taken — until its pull request merges, so a customer pulling the repository's default branch does not find it there; 3.6.x recommended that route and said nothing of the condition, and this branch's own first fix then called the note *"true as written"* on any push, which is corrected here. **Population: every `/brd-package` run — on 3.6.x as well.** Phase 7 now takes the handoff's consent itself, testing `workflows-core:phase-handoff` §2.1's gate conditions, running its push-target probe and presenting its §4.3 choice array; the *Handoff* phase executes on that answer, handing `handoff-to-main` the probe's `remote` value, and does not ask again. Declining — the array's second or third option — declines the handoff and nothing else: the run continues on the archive route, and the *Handoff* phase, which described its first choice alone, now says what those two do — `handoff-to-main` does not run, and the final report carries `workflows-core:phase-handoff` §4.1's *Declined by the user* line with the **gated — stopping** clause the array's parenthetical promised. Phase 7 asks which route, recommending the repository one, only where the gate conditions hold, the probe found an `origin` remote and the operator consented; otherwise it takes the archive route without asking, and says why. The note names no branch. The note and its delivery-route item are now printed at *Next steps*, in that order and **before** the offer that asks whether to send it, since an operator who has not read the note cannot answer it: on the repository route that item names the branch and pull request the handoff pushed and says the note is true once that pull request merges — or, where the handoff pushed nothing, what must happen first, and, for every path the handoff declared and staged nothing for (§4.1's *Declaration unaccounted for* clause, an ignored file among its causes), that the note is not true for that file on any outcome, no merge putting on the branch what nothing staged. That item is the one place the condition is printed; the final report names the note and the item rather than repeating either, and the next-step offer's *Send it* waits on the same condition. The note is never rewritten. `bundle-packaging.md` §4 and §5 and the command's documentation page say the same.

### Fixed — `/brd-reconcile`'s handoff left the corrections it applied behind

`/brd-reconcile` Phase 6 corrects a live working document in place wherever a returned review's section 12 asks and no other rule owns the field — `slices.md`, a seed file, the inventory's or `brd-link.md`'s prose, a `depends-on:` added to `brd-link.md` — but its handoff's `deliverable_paths` named none of those files, and `workflows-core:phase-handoff` §2.3 stages only what is declared. Each correction stayed in the working tree while the pull request went out without it. The handoff now declares every artifact Phase 6 changed — the parent's `brd/brd-figures.md` among them, where a slice's run applies a transcription correction — and names each file beneath `customer-sent-<YYYYMMDD>/` literally where it named the directory, which §2.3 does not stage either. **Population: every `/brd-reconcile` run on 3.6.x that applied a section-12 row to a file the handoff did not otherwise name, and every `--sent` run whose first handoff was declined.** A correction left behind that way is still in the operator's working tree, uncommitted, and the specs-repo preflight of the next run in that repository reports it as a dirty path.

### Fixed — `/brd-reconcile`'s stale-reference sweep could rewrite the customer's own files

Phase 11 reads every markdown file under the parent's folder for prose still asserting a position the customer's answer moved, and may mark a hit `updated` and correct the sentence. Its structured-record table kept that off the ledger, the inventory, the register and the code-defect log, but not off `brd/source/` — the customer's own document, which Phase 6's class 3 says is never touched at all and `brd-format.md` §1 makes immutable. So a sentence of the customer's own that stated a position their answer later moved was one the sweep could rewrite, falsifying the record of what they sent. A hit anywhere under `brd/source/` or `brd/source-external/` is now `needs-a-human`, and the file is never edited; the figures file takes the same outcome. **Population: every `/brd-reconcile` run whose second search matches a sentence in the customer's own captured files — on 3.6.x the source document and any markdown file it linked from its own directory, which that version copied into `brd/source/` as well; this release adds every file captured from outside that directory.**

### Changed — `/brd-intake` asks about each uncovered section and image on its own, re-reads at most once, and remembers the answers

Phase 3's coverage check put every top-level section and image holding no row into one question — *Re-read* or *They hold no obligation* — and its recommendation stayed on *Re-read* for a prose section the reader keeps treating as context, so a re-read that changed nothing brought back the same question with the same recommendation. The only ways out were an untrue *they hold no obligation* for the whole set, or *Cancel*: nothing let the operator record that a section binds, and one answer stood for every section at once — verification run B5 gave `1. Background`, arguably binding, and `6. References`, pointers only, one shared record. And the answer was saved nowhere but the final report, so every re-run over an unchanged document asked it again. **Re-reading is now an action on the whole set, and accounting is per item.** One question for the set offers *Re-read* — recommended unless every item it names is one a re-read cannot change — *Account for each section and image*, and *Cancel*; it is put once per run, since a re-read is allowed once. Then each item still unaccounted for gets a question of its own, at most four to a call: *They hold no obligation* (recommended), *Quote the sentence that binds*, or *Cancel* — an image that was not read has nothing to quote and is offered the first and the last. A quote is asked for with the text it is checked against shown — the section's own text, or the image's transcription — one obligation to a quote and as many quotes as the item binds, the item's question coming back after each with *Finished* recommended. A span is checked with whitespace collapsed and nothing else normalised, and becomes a row whose text is the source's own, never the operator's typing, anchored at the section — at the lines it covers, where the section's heading path names no single heading — or on the image. A span the source does not hold is never written through, and never a dead end: the item's question is put again in the form it stands at, with the failed span shown, so that form's own way out stays in reach beside *Cancel* — *They hold no obligation* while the item holds no row, *Finished* once it holds one. A typed answer to any of these questions, the set question included, is acted on as the option it expresses or asked again. A re-read that finds no requirement replaces nothing — the first read's result stands, and the re-read is spent. Every account — a section's and an image's alike — is recorded in a new `accounts:` map in the inventory's frontmatter (`brd-format.md` §2), its keys and values double-quoted with `"` and `\` escaped, written in the same act as `captured:`. A re-run carries an account, rather than asking again, only where Phase 2 recorded the item's file — or the image itself — unchanged; every write of the map, the one relation 1's stop makes included, drops the account of every item in a file recorded replaced, so an answer given about a file's earlier bytes never stands beside a record of its new ones. An image's *Rows* line shows its account and is never read back as one. The final report names each account with its words, and says which were asked and which were carried. **Population: every intake whose inventory leaves a top-level section or an image without a row — most real BRDs, nine of fifteen sections on the package the check was measured on — and every re-run over one. Present before this branch**: 3.6.x's question, over sections alone, recommended *Re-read* unconditionally, gave the set one answer, and saved it nowhere.

### Fixed — a coverage re-read left the first read's candidates standing, and on a first intake numbered its new row last

A re-read re-dispatches `brd-reader` over the whole set, and Phase 3 said only that its rows are reconciled. Nothing said whether Phase 4 and Phase 5 then took the first read's defect candidates, `illustrates` lists and notes, or the re-read's. And the re-read was reconciled against the rows the first read had already written, so on a first intake a row only the re-read found was minted after every other id — verification run P2's `Background` became `[BR#15]` — against `brd-format.md` §2's reading-order numbering, while over a replaced file a row the re-read worded differently beside a first-read row under a shared anchor matched nothing and was minted a second time. **Phase 3 now holds a read's result and writes the inventory once**, after the coverage step settles, or just before relation 1's stop, so that stop's hand-correction remedy has a file to correct. A re-read's rows, candidates, `figures` entries and notes replace the first read's wholesale — save a re-read that finds no requirement, which replaces nothing — and it is reconciled exactly as the first read was, against the inventory on file: nothing is copied between the two reads, so every file stands for it as Phase 2 recorded it, and the text rule and the same-or-new question apply to it unchanged. On a first intake the rows are numbered once, after the last read and after any row a quoted sentence adds, in reading order; on a re-run every id on file is kept and each new row takes the next id after the highest. `brd-format.md` §2's numbering paragraph now states both cases. **Population: every intake whose operator answers the coverage question *Re-read*. Present before this branch** for the numbering and the unstated candidates — 3.6.x reconciled a re-read against the rows it had just written; the second mint over a replaced file arrived with this release's text rule.

### Fixed — six smaller defects in `/brd-intake`'s figures, anchors and notes

- **An earlier run's `[BR#n]`s reached `brd-reader` through the figures file.** Phase 3 handed the agent `brd/brd-figures.md` itself, whose re-used sections carry an earlier run's *Rows* line, and an id copied from one came back as the agent's own numbering, which Phase 3 then mapped onto a different row. It now hands over a copy with every *Rows* line removed — a first intake's too — made in a `command mktemp -d` directory and removed after the phase's last read, and `brd-reader` is told the line is no input. **Population: every re-run that re-uses a figures section whose *Rows* line names a row. New in this release.**
- **"The same element" was undefined for an image anchor.** Reconciliation's first pass matched anchors resolving to "the same section or element", and `brd-format.md` §2.2 said only whether an image anchor resolves. §2.2 now fixes which element one names: a *Text* region, or a cell of a table's rows, an annotation, or a *Flow* edge; `annotation <n>` names the n-th annotation; a quote names the one element holding it, and a quote found in more than one element names none. Two anchors on one image quoting the identical string match all the same. That way a row drawn from an image — the plugin's own words, reworded between reads — never gets a second id over an unchanged image, and its image-only `ambiguity` never gets a second entry. For the same-or-new question, a quote that names no single element counts as naming every element holding it. `brd-reader` quotes a span its own element alone holds, or writes `annotation <n>`, and the pass and the same-or-new question test by those rules. **Population: every re-run over a BRD with a row drawn from an image. New in this release.**
- **A correction made in a note shipped uncorrected.** Phase 5 filled each *Rows* line from the returned `illustrates` while the agent's own notes said a list was wrong, and `brd-format.md` §1.2 reserves *Rows* to `/brd-intake`, so the error could ship only as an accepted risk — verification run P2's `[SR#7]`. `brd-reader`'s return contract now says a note never amends a returned field and a correction is made in the field, and `/brd-intake` says it writes *Rows* from `illustrates` alone and prints the notes without parsing them. **Population: every run whose reader corrects a field in a note. New in this release.**
- **The notes were printed "before the first candidate", which never comes on a run with nothing to walk.** They are now printed once at Phase 4's start, whether or not a candidate is left — the standing read's, the last read whose result was not discarded — and in the final report; after an `EMPTY` read, which skips Phase 4, in the final report alone. **Population: every run left with no candidate to walk — a re-run whose every candidate matches an entry on file, and any run, a first intake included, whose read proposed no candidate at all. New in this release** — 3.6.x reported the notes in the final report only.
- **Phase 4's *Image* line did not say what of the transcription to show.** It read *"transcription below"*. The question now shows the section's *Depicts* sentence and, on an *Element* line, the element the row's anchor names — the *Text* region or cell, the annotation's *Says* and what it points at, or the *Flow* edge — and every element holding the quote where it names none, and nothing else of the transcription. **Population: every candidate on a row drawn from an image. New in this release.**
- **A new figures section's empty *Rows* line had no written form.** `brd-format.md` §1.2 and Phase 2.5 now write it `- **Rows:**`, with nothing after it, which is what a run that stops before Phase 5 leaves. **Population: every figures file read after such a run. New in this release.**

### Fixed — `/idea`'s question past the older bounds had no text, printed no counts, and counted "reached" three ways

Phase 1.5 printed its counts line only where the walk stayed inside the older bounds, so the ask branch left the final report's walk line with no source; it named the files past a bound in prose and gave the question no text; and its headline total counted *other* files, `Read all <n>`'s `<n>` did not, while *Read all* took them. The ask branch now prints the counts line too, puts a fixed question before the array — the bounds the walk crossed, then one line per file past them with each bound it is past, then the line saying what *Read all* takes, and, where the walk reached nothing below depth 1, the line saying the second option takes that same set — and defines `<n>` as the headline total less the source, *other* files included: exactly the set *Read all* takes beside the source, as the branch that asks nothing takes it, an *other* file still never opened. *Only what the source links directly* takes every file the source links itself, of any kind. **Population: every `/idea` run whose walk reaches past a bound — `<n>` read differently only where it also reached an *other* file. New in this release**, whose Phase 1.5 is new.

### Fixed — `/idea` restated its collision rule without the fourth rule, and taught two ways to cite an image

Phase 4.5 step 2 said it restated none of `idea-format.md`'s vendoring rules and then restated *The collision rule* in part: identical content reused "at the destination", where rule 1 compares against every file in the destination directory, and no rule 4, which refreshes the copy an earlier run made of an edited source rather than minting a `_NN` twin. A run following the step as written mints an orphan beside a stale copy `idea.md` still points at. Step 2 now cites the rule — all four rules, executed from `idea-format.md` — and the final report names a refreshed name beside a substituted one, as the phase's own outcome line already did. *The collision rule* itself put all four of its rules behind "where that name is already taken". Read literally, rule 1's comparison against the whole destination never ran for a file whose basename was free, so a renamed file whose bytes were already vendored was copied beside them as a twin. Rule 1 now applies to every file and rules 2–4 to a name already taken, as `/brd-intake` Phase 2 already applied it. `idea-format.md` Section 5 said a linked image is "cited by path", two paragraphs above the rule that it is cited by its written `target` or its resolved `path`; it now says so where it opens. **Population: every `/idea` re-run over a linked page or image whose bytes changed since its copy was made, every run vendoring a file whose bytes already sit in the destination under another name, and every brief citing an image. Present before this branch** — 3.6.x carried all three: at `4387959f` the collision rule opens "where that name is already taken:".

### Fixed — a slice's `source:` had two path bases, and its `claims:` list had no spelling

`brd-format.md` §2.1 wrote a slice inventory's `source:` relative to the parent's folder — `brd/source/<basename>` — while `/brd-split` Phase 3 step 4 said to name the document the parent's `document:` records, which is relative to `brd/`. §2.1 now owns one base, the parent's folder: `brd/` put before the `document:` value, written as a double-quoted YAML string, the base a 3.6.x slice already carries and the one `/create-prd`'s documentation page reads, and `/brd-split` cites it. None of `/brd-package`, `bundle-packaging.md` or `/prd-ground` reads a slice's `source:` — `/brd-package` names the document off the parent's `document:`, and the `source:` the bundle drops is the figures file's own field — so no reader changed. And nothing fixed how `brd-link.md`'s `claims:` list is spelled, though YAML reads an unquoted `[BR#1]` as a flow sequence holding `BR#1`, which matches no ledger row's `[BR#1]`. §2.1 now shows the file and fixes the list: each entry a double-quoted bracketed id, one to a line, `claims: []` when empty; a list written before in another spelling is read id by id and rewritten this way when `/brd-split` next writes it, and `/brd-split` cites the spelling and writes the whole list again in it wherever it adds to or withdraws from the list, so an older list is rewritten the first time the command touches it. **Population: every slice `/brd-split` writes. The `source:` disagreement is new in this release** — 3.6.x's `/brd-split` cited §2.1's header; **the unfixed `claims:` spelling was present before this branch.**

### Fixed — `/brd-intake`'s outside-files count, its re-read recommendation and the row a quote added — eight defects two live runs found

A fresh intake and a re-run over the same unchanged source were executed against the command's own text, and the redesign held: the coverage set question was put once, each per-item form carried three options, a wrong quote wrote nothing and re-put the item's question with the failed span beside it, the right one landed as `[BR#1]` in reading order, `accounts:` carried over the unchanged file, and the re-run kept all 15 ids with no text changed. Eight things in the text did not. **Phase 1's outside-files question printed the wrong number**: `Capture all <n>` counted every file the walk reached — 5 in the run — while the answer decides only the files outside the document's folder — 1 — so the number an operator needs was never printed. The option now names what it captures and `<n>` counts exactly that, with the run's other counts said to be the taken set's and the copied set's. **A row the coverage step added from a quoted span was reported *not re-extracted by this read* on every later run**, in the same words as a row the reader genuinely stopped returning — the two were indistinguishable in the final report, and the designed steady state read as a signal. The inventory now carries a `quoted:` list of those rows (`brd-format.md` §2) and each is reported in its own words, *added from a quoted span; no read has returned it*, leaving the other phrase to mean what it says. **`(Recommended)` on the set question turned on whether a section's content was "apart from its heading and the words introducing its links" only links** — a judgement about what words are for, which decided a live recommendation; the test is now applied to the section's own text, piece by piece, and an unclear one defaults to re-readable, since a wasted dispatch is cheaper than an account standing where a requirement would have been. **The re-run confirmation carried no `(Recommended)` marker and did not say the omission was deliberate**, unlike the zero-row list in the same command; it now says so and why. **A matched row whose two anchors both resolved and differed had no rule** — the case `brd-format.md` §2.2 makes reachable by matching `annotation 2` against a quote of its *Says* — and now goes to the same-or-new question, because a moved row and a re-anchored one look alike from there. **The per-item questions never said how their plain-text quote prompts interleave** when two items in one call both answer *Quote…*; they run one item at a time, in the call's order. **An image anchor quoting the empty string resolved against every image and named no element of any of them** — §1.2 writes an unlabelled mark's *Says* as `""`, and §2.2 resolved an anchor by verbatim appearance, so `<path> › ""` passed relation 1, the traceability stop, on a row pointing at nothing; §2.2 now refuses an empty quote outright, says that a form the format writes where content is absent is not content to quote, and fixes `annotation <n>` as what anchors a row drawn from an unlabelled mark, which `brd-reader`'s contract now writes. And **`brd-format.md` §1.2 told the figures file's `source:` to take the document's name from `brd/brd-link-log.md`**, which §1.1 forbids wherever the inventory carries `document:` — on a re-run over a document revised under a new filename the two disagreed; §1.2 now takes the name from the document the writing run itself copied into `brd/source/`, which Phase 2 holds, so the two no longer state two rules for one field. **One change rides with that eighth fix rather than being a ninth defect**: §1.2 now states a limit of its own notation — its *Flow* list holds edges and no grouping of them, so a diagram drawing one edge in two swimlanes is transcribed as two identical list items no anchor can tell apart — and `figure-reader` now names such a grouping, where it carries meaning, within the one sentence its `depicts` already is, that being the one field which describes the drawing rather than transcribing it. A limit stated without saying where the lost information goes leaves the only party still holding the diagram with nothing to do; this is the one change in the pair that alters what ships inside `brd/brd-figures.md`.

**Population: every `/brd-intake` run — the count on any run reaching a file outside the folder, the recommendation on any run whose coverage check names a section, the reported state on every re-run over a BRD whose coverage step ever took a quote, and the `source:` disagreement on any re-run over a document sent under a new filename; the `depicts` change lands on every diagram whose grouping carries meaning, of which the verification vault holds none — checked against run A6's transcription, whose one diagram is a left-to-right chain of four boxed steps with no grouping and no repeated edge. **The empty-quote refusal has no live population and is stated as a rule the format admitted rather than a run that hit it**: the verification vault holds the ingredient — `report.png` carries an unlabelled mark — but neither `brd-reader` nor an operator wrote such an anchor in either run, and no producer would; what was wrong was that the format resolved one. New in this release, all eight**: checked against `4387959f`, where `/brd-intake` runs no read-only link walk, its capture walking links transitively in Phase 2 and inside the document's own directory only — its Phase 1 asks nothing about files outside the document's folder, so there was no `<n>` to be wrong — its coverage step is one question over the whole set with no quote option, no `accounts:` map and no row it could add, and neither `brd/brd-figures.md` nor the §1.2 that formats it exists.

### Fixed — `/idea`'s walk printed three numbers over three different sets, and a declined handoff dropped its own outcome line

A live run over a 23-file vault — 6 pages and 4 images at depth 1, reaching depth 4 — put the phase's numbers beside each other for the first time. The counts line's headline counts the walk's visited set **with** the source; its by-depth tally counts the markdown **without** it, the source having no depth to be counted at; and the twelve-page bound orders the markdown **with** it. All three were individually exact and no two said which they were, so the run printed `depth 1: 6, depth 2: 6, depth 3: 2` — fourteen pages — above a bound of twelve that three files were past. Each number now names its set, and the bound says outright that it runs one ahead of the tally beside it. **`Read all <n>`'s `<n>` sits under the list of files past a bound and counts something else** — every file the walk reached beside the source, 22 against a five-line list — which the option now says rather than leaving an operator to count five things and answer about twenty-two. **The counts line had no stated form for a zero**: "anything unresolved … or `other`" reads as *whatever there was of those*, its one example prints all four clauses, and an omitted clause is indistinguishable from a forgotten one — every clause now prints on every run, `0` included. **The past-a-bound template offered one alternation slot and no separator** where its own prose asks for "every bound it is past", and three files in that run were past two; they are joined with `, ` in the template's left-to-right order. **`idea-format.md`'s rewrite map is keyed on `(target, from)`** while every link being rewritten sits in `idea.md`, which is no digest entry's `from`, so a literal pair-match would have rewritten nothing: matching is on the written target alone, the `from` half being what makes two entries two keys for rule 3's ambiguity case. And **the Final report asked for the `Phase handoff:` line "when the handoff ran"**, which on a decline — where `handoff-to-main` does not run — drops `workflows-core:phase-handoff` §4.1's *Declined by the user* line, the one line telling the operator their files are written and not on the default branch; it is now asked for by option: `handoff-to-main`'s line on the first, the *Declined by the user* line on either other, and **none of them on a `status: draft` run**, where Phase 5 never offers one at all — so there is no entry point run and no offer declined for §4.1 to have a line about, and asking for one unconditionally would have made a draft run assert a decision its operator was never given.

**Population: every `/idea` run whose walk reaches past a bound** — the three counts and the option label on all of them, the separator on any whose over-bound set holds a file past two bounds, and the zero form on any run at all. **The rewrite map's keying is every run that vendors a linked source**, and **the handoff line is every run whose operator declines**. **Status against `4387959f` is split, and was checked rather than assumed — two of these are older than this branch.** The three counts, the option label, the zero form and the separator are **new in this release**: that commit's `idea.md` has no `## Phase 1.5` at all (its headings run Phase 1 → Phase 2), so there is no walk, no counts line and no bound question to have been wrong. The **rewrite map's keying is present before this branch** — `git show 4387959f:plugins/product-workflows/references/idea-format.md` carries the identical key row at its line 317, *"the `target` as written, together with the `from` it was written in"*, above the same *"Match each link in `idea.md` on its target string"*, and that release already vendors sources in its own Phase 4.5. The **`Phase handoff:` condition is present before this branch** too: the same *"outcome line when the handoff ran"* stands at that commit's `idea.md:534`.

### Fixed — six more `/brd-intake` defects a second live pair found, a declaration that dropped five files on a re-run among them

A fresh intake and a re-run over the same unchanged source were executed against the command's own text, after the fixes the first pair bought. Everything those fixes were for held: the set question was put once, both per-item arrays went out in one call, a whitespace-only quote and a real sentence from the *wrong* section were each refused as a span that does not occur, the correct quote landed as `[BR#1]` on the `quoted:` list, `accounts:` carried an unchanged file's answer, and the re-run kept thirteen ids and reported the quoted row in its own words. Six other things did not. **Phase 7's `deliverable_paths` dropped the customer's document and four captured files on the re-run**: its generative clause declared "every file this run wrote … and every file Phase 2 re-used under collision rule 1", while the gloss beneath it declared every file captured into `brd/source/` or `brd/source-external/` — and a re-run over unchanged bytes *writes* none of them, collision rule 1 being a `source-external/` rule. The phase names the consequence itself: an undeclared file under those directories is staged by nothing and stays untracked for ever. One sentence now governs — every file under the BRD folder this run **captured or wrote**, captured covering a copy this run made, one collision rule 1 re-used, and one a re-run left untouched. **`brd/brd-figures.md`'s `source:` could not be followed on a first intake**: it deferred to the rule preferring the inventory's `document:`, which Phase 3 writes *after* Phase 2.5 writes the figures file, so the preferred source did not exist and the forbidden one was all there was. It is now taken from the document the writing run itself copied into `brd/source/`, which Phase 2 holds — never off the ladder §1.1 gives a *later* reader, which on a re-run over a document resent under a new filename would name the previous basename while the same run's Phase 3 writes the new one into `document:` minutes later; §1.1's list of readers that take the name from the inventory names the writer of a slice inventory's `source:` and `/brd-package`, and no longer the figures writer. **The link log's second table said it carries a row for every `[[wikilink]]`**, while its `Copy` column needs a copy to name and an `ambiguous` wikilink is captured by nothing: that table now holds only links whose target the capture took, and an uncaptured one keeps its row in the first table with its reason. **Phase 1's outside-files count asserted a number it cannot make** — "every file the second answer drops save one" — where that answer also drops any inside file reachable only through an outside one, of which there may be none, one or many; the fixture's set was empty. **The reconciliation now says when one of its two report wordings has no members**, since a class printed nowhere looks exactly like a class the run never looked at — which is the whole reason a quoted row and a genuinely dropped row are worded apart. And **a first intake now says what a quoted row costs the numbering, after the item's *Finished* answer**: ids are fixed once, in reading order, so a span quoted from an early passage takes an id one of the read's own rows would have had and moves every later one down — and since a first intake prints no `[BR#n]` before Phase 4, the operator is told not that a numbering they read has changed but that the ids they will meet are not the order they quoted in. **One change rides with these rather than being a seventh defect**: Phase 1's question about a linked file that is neither markdown nor an image recommends the option that ends the run, which two runners flagged independently — a scripted or careless operator taking the recommendation gets no BRD folder at all. The marker stays, stopping to convert being the safer default, but the reason now sits where the choice is made: such a file is copied and never read, so an obligation inside it would enter the folder unseen, and the recommended option ends the run having written nothing. The option itself says so — *this run ends here, having written nothing* — where it used to say *nothing has been written*, which reads as reassurance rather than as a cost.

**Population: the declaration on every re-run over a BRD whose files are unchanged** — which is the ordinary re-run, and the one defect here that loses something rather than confusing someone; the figures file's `source:` on every first intake; the second table on every capture holding an unresolvable wikilink; the count on every run reaching a file outside the document's folder; the empty-class line on every re-run whose unmatched rows are all of one kind; and the numbering notice on every first intake whose operator quotes a passage before the last requirement the read returned. **New in this release, all six** — checked at `4387959f`, whose `/brd-intake` has no `brd/brd-figures.md` and no `brd/source-external/` at all (neither string appears in that commit's command or its `brd-format.md`), no *Captured links that do not resolve as written* table, no outside-files question — an outside file is excluded there by rule rather than offered, so there was no count to be wrong — no `captured:` map, and no *not re-extracted by this read* wording for a class to be missing from. **The rider's subject is new there too**: neither the *other*-file question nor `BRD_INTAKE_UNREAD_ATTACHMENTS` exists at that commit — both grep to 0 in its `brd-intake.md`, which asks nothing about a linked file that is neither markdown nor an image.

### Fixed — every package stopped on — or shipped — the customer-review schema's own example identifier

`/brd-package` inlines `customer-review-schema.md` from its section 2 onward into the prompt's part 10, and that body's rule on citing identifiers gave its example as *"bracketed, like `[BR#4]` or `[CG#12]`"*. The prompt is itself a bundle document, so the citation-resolution check (`bundle-packaging.md` §6.2, relation 1) resolved both against the package's own records: each example stopped the package at *Assemble the bundle* rule 8 with `BRD_PACKAGE_DEAD_CITATION` wherever its id was absent from that package's corpus — the `[CG#12]` in every package holding fewer than twelve code-grounding findings, the `[BR#4]` in every slice whose inventory, which holds only the rows the slice claims in its parent's numbering, does not claim that row — on text no operator edit can repair because it is rendered from the reference on every run; and wherever the id was present it resolved, silently, to an unrelated record the customer then reads the rule as being about. A verification run of the package case end to end stopped exactly there. **Population: every `/brd-package` run — each one either stops or ships the rule naming an unrelated record — on 3.3.0, which added the example, through 3.6.1.** The example is now the placeholder form, `[BR#n]` or `[CG#n]` with `n` standing for the number. The schema's section 1 says an identifier with a number in it never appears in the file, even as an example, and why; `bundle-packaging.md` §6.2 says a placeholder names a class and no record, so relation 1 needs no exemption for the text the run copies in verbatim; and `render-schema` now verifies that the extracted body holds no identifier with a number in it, stopping with the new `BRD_PACKAGE_SCHEMA_EXAMPLE_ID` — a reference-integrity gap, which the capture-at-block invariant now names beside `BRD_PACKAGE_SCHEMA_BOUNDARY` and `BRD_PACKAGE_PROMPT_LEAK` — before an example number can reach a customer. `/brd-package` now binds every fixed sentence it renders — into the prompt, the delivery note, the manifest and the notes the de-Obsidianising pass leaves — to the same placeholder form, where §6.2 had asserted it with nothing in the command saying so. A sweep of every passage the prompt, the delivery note and the bundle render verbatim from a reference or template found no other.

### Fixed — a hit in the customer's own words asked the operator nothing

The plugin-free scan and the citation-resolution check report, rather than stop on, a hit inside verbatim customer content or a customer-derived locator, and `/brd-package` then said to *"let the operator decide whether to ship"* — with no question, no options and no stop, so a run either shipped without asking or improvised a question of its own. **Population: every `/brd-package` run whose scans report such a hit — on 3.6.x as well, where that treatment covered the customer's source document alone.** `bundle-packaging.md` §6.3 now owns the ruling: each pass that reports such a hit prints its grouped report and asks once — *Ship it* (recommended), or *Hold the package*, which stops with the new `BRD_PACKAGE_CUSTOMER_CONTENT_HELD` and hands nothing off. A hit an earlier ruling in the run already covered — keyed by its token and the file it sits in, the bundle's copy of the prompt counting as the prompt for those of its hits whose token that ruling covered, and one carrying a token it did not put like any other — is not asked about again, and the final report opens its grouped list with a `Customer content:` outcome line, or reads `Customer content: none`. The command's own statements that such a hit "reports rather than stops" now say that a *Hold* stops the run. `/brd-package` cites it from the prompt's, the note's and the bundle's scans, and the documentation page says the same.

### Fixed — two statements of what the prompt inlines from the review schema disagreed

`bundle-packaging.md` §1 said the review schema is *"inlined in full"*, while `/brd-package`'s `render-schema` entry point — which is what runs — takes everything from the schema's section 2 onward and says *"Never the whole file"*. **Population: a reader of `bundle-packaging.md`; no run behaved differently — on 3.6.x as well.** §1 now says the schema is inlined and never the whole file, and cites the boundary the schema declares and the entry point that takes it rather than stating what is taken.

### Fixed — the image path on a requirement-defect question had no line of its own

`/brd-interview` writes a held `[C]` question's defect on a line labelled `- **Requirement defect:** [DEF#n]` — the one line `/brd-reconcile` copies a `[CD#n]`'s `settles` from and every slice reads to know the defect is asked — and added *"where the defect sits on a row drawn from an image, that image's path relative to `brd/`"* without saying where it goes, so it could land on the line a reader copies an id from. **Population: every requirement-defect question about a row drawn from an image — new in 3.7.0, which added both the question source and image-drawn rows.** The defect line now carries the `[DEF#n]` and nothing else, and the image path goes on the next line, labelled exactly `- **Defect image:**`, including where *One question per row* appends a defect to an entry already on file. `/brd-package`'s part 7 now says what it renders from each held entry, that line among it, so the customer can find the picture through the manifest; `/brd-reconcile` still reads the id from the defect line alone. The documentation page says the same.

### Fixed — whether a bundled image takes the key prefix was unstated

*Assemble the bundle* rule 1 gave every bundle **document** a filename beginning `<BRD-KEY>-` and said nothing of the images the bundle also carries — the design frames the grounding cites and, from 3.7.0, the customer's captured images. **Population: every bundle carrying an image — on 3.6.x as well, for the frames.** Rule 1 now says every file the bundle carries takes the prefix, images included, and that an embedded image in a rendered document points at that name. Where two of the package's own images share a basename — frames in different frame sets, or a captured image under `brd/source/` and one under `brd/source-external/` — each takes the shortest run of its own trailing folder names that tells it apart, joined by `-`; the result is then tested against every other name the bundle carries — not merely against the images that shared a basename, since `design/login/01.png` disambiguated to `<BRD-KEY>-login-01.png` equals the plain name of an image whose own basename is `login-01.png` — and where two names still coincide the one whose path sorts later, byte-wise, takes `-2` before its extension, the test repeating until every name is unique. So the name is decided by the paths rather than the copy order. The manifest maps each captured file, image or markdown, to its name, and every other image whose name took a folder; `bundle-packaging.md` §1.1 says the same. `bundle-packaging.md` §7.1, which said §6's relation 3 already reaches a design finding's citation of a frame, now says nothing checks one — that relation reads a bare `<name>.md` token only — a false claim since 3.5.0.

### Fixed — the propagation sweep's citation pass never read the field holding the ids it sweeps for

`/brd-reconcile`'s changed-id set carries every `[DEF#n]` that gained a resolution, and `settles` is the one **structured** field of a decision record that holds a `[DEF#n]` (`decision-register-format.md` §1) — but the citation pass read `statement`, `argumentation`, `evidence` and `conditional_on` only, so a dependent BRD's `[CD#n]` that settles a defect this run resolved was never reached, and the position resting on it was never put to a disposition. **Population: every reconciliation whose dependent registers cite a changed id in one of the omitted fields — on 3.6.x as well, the list being as old as the pass.** Which fields the pass reads is now **derived from §1** rather than listed in the command, and the derivation reads **every** field §1 defines, skipping exactly two kinds: a field whose value §1 fixes to an identifier class — on every record kind it appears on, §7's assumption rows included — where the changed set carries no id of that class, and a field that names the record itself. **Both skips fall out of the rule rather than being written down beside it**, which is the property that matters for a pass whose failure mode is a silent miss: `defects` is skipped because it holds `[CDF#n]` ids on every record kind and the set carries no `[CDF#n]`, so the day it does the pass reads `defects` with nothing in the command changing; `id` is skipped because it names the record rather than citing another. The changed-id set's own definition now names every later reader — the citation pass's item matching, this field rule's class test, the stale cross-reference sweep's text search one phase on, and the reconciliation record — so none of them is invisible from where the set is settled, and it keeps no count of them, a census being what an editor checks against and the half of that paragraph that first went wrong. Everything else is read, so a field added to §1 is read on the day it is added rather than waiting for a reader to notice it — `settles` qualifies on the class test, and an `[AS#n]`'s `evidence`, which §7 makes the why-no-evidence **statement**, qualifies because §1 fixes it to no class there. **`chosen` is read on both record kinds** — on a `[CD#n]` it can be the customer's answer quoted verbatim where they answered outside the options put, which is exactly where a changed id is named in words nobody may edit, and on a `[VD#n]` it is the delivery team's own text — and a hit there is disposed like any other while no disposition corrects the text; the stale cross-reference sweep's `decisions.md` row, which already sends every field but `argumentation` to `needs-a-human`, is what refuses a correction there, for the field it is rather than for whose words it holds. The same enumeration-instead-of-citation defect is corrected in the two `/brd-reconcile` tables that govern writes into a record: both now cite the complete set `decision-register-format.md` §4 names — all thirteen fields, the stale-reference row excepting `argumentation` alone, which is the route's own prose and what an `updated` disposition is for.

### Fixed — the propagation sweep asked for a changed id that half its items do not have

`/brd-reconcile` reaches an item two ways: the citation pass finds one naming an id in the changed set, and the `conditional_on` pass finds one **by the field**, swept whether or not it cites a changed id. The disposition step nonetheless said to present *"the item, the changed id that reached it, and what changed about that id"*, and the `inherited-unchanged` row said to record *"the changed id that was considered"* — neither of which exists for a `conditional_on` position whose prerequisite decision this run did not move, leaving the operator a blank where the run owed them a fact. **Population: every reconciliation whose sweep reaches a `conditional_on` position this run's own changes never touched — narrow, and on 3.6.x as well, the two passes being as old as the sweep.** The step now presents what actually reached each item: the changed id and what changed about it for a citation-pass item, and the prerequisite decision the field names with whether this run moved it for a `conditional_on` one, saying so in as many words where it did not. The `inherited-unchanged` row records the same, and notes that such an item takes that disposition and no other — by the other three rows' own definitions, each recording a cause that **is** a changed id. Nothing downstream demanded a non-empty value: the reconciliation record already asks for each item's disposition and reason rather than for an id per item.

### Fixed — eleven text defects a live end-to-end run of the route hit, and a double outcome line

A run of `/brd-interview` then `/brd-reconcile` over a real root and slice hit eleven places where the text could not be followed as written, plus one contract breach. **Population: stated per item; all eleven predate this branch unless said otherwise.** `/brd-reconcile`: a customer who **explicitly declines to answer** — a case `customer-review-schema.md` §5 guarantees and the reader returns — had no disposition among the four, so *Confirm* would have frozen a non-answer as customer authority; it now takes *Reject*, which records the decline and leaves the question open. A `conflict` whose counterpart is dropped no longer claims *the obligation survives in the row kept*, which is false of two incompatible rows. And the command now says outright that which branch a conflict takes turns on the lowest-numbered row carrying the entry (`brd-format.md` §3) and must be reported with the pair. The stale sweep's exclusion table was **being read as the list** of what `updated` may not touch; the rule is now the class — any structured record, anything the customer wrote — with the table as worked cases, after a run found five more file kinds that belong to it. The `--sent` redundancy check no longer runs a gate whose `path` cannot be formed where the folder holds no prompt at all. The commercial next-step paragraph is scoped by its own condition rather than by the array it sits under, so a run that froze a `[CD#n]` and took `advance_ready: no` still prints it. And the two `handoff-to-main` calls this command makes now label their outcome lines and fix which one a `<merge-clause>` resolves from, the count itself being `workflows-core:phase-handoff` §4.1's to settle. `/brd-interview`: the NOT-PROVABLE re-tag route is reachable again, the question source and the re-tag now naming the different provenances each covers; step 9's input set reads *each slice under the parent, this one included*, where *sibling* made it empty on a one-slice parent; a held `[C]` entry's heading and delimiter are pinned as `## Round <N>, question <position>`, which is what lets its two readers bound an entry; the `[V]` picker no longer authors its own free-text escape, which `workflows-core:escalation-rules` §0 forbids; and it no longer reads as two entries per option. **One change rides with those eleven rather than being one of them**: `decision-register-format.md` gains **§1.1**, the serialisation counterpart to `workflows-core:grounding-format` §2.1 that the register lacked while two commands parse it by field and §4 writes fields in place.

### Fixed — eight defects an end-to-end `/brd-package` run hit, and the register's record boundary

A `/brd-package` run that reached completion — no stop at any phase — nonetheless shipped a package its own text had shaped wrongly. **Worst: `fixed` was offered on every finding** while the command mints no `[C]`, writes nothing into the interview's question set, changes no ledger disposition and owns no verified finding, so for an attack against any of those the disposition was offered and unavailable; the run fell to `accepted-risk`, and **11 of 17 accepted-risk findings were attacks the team agreed with and could not act on**, every one shipped to the customer as a risk it had chosen to take. **Population: every package whose review attacks an artifact this command may not change — on 3.6.x as well.** `fixed` is now admissible only against what this command may change, and the agreed-but-not-actionable case takes `accepted-risk` carrying a structured **`fixed-by:`** marker, whose value is a command or, where no command writes the named artifact, the person who must write it by hand. That marker stays inside the delivery organisation — the disposition's reason, the self-review, the final report — and is **never rendered**: part 9 forbids a command name in a finding's words and the plugin-free scan hard-stops on one over the finished prompt, so a clause naming the command would have stopped the package on the common path and the only way past would have been to strip it, reverting the fix. Part 9 instead renders one generated, plugin-free sentence **from the marker's presence and never from its value** — the team agrees, and it is recorded for repair outside this package rather than accepted as it stands. A **second-pass finding restating one already disposed** now takes the standing disposition and is rendered once, where both copies used to ship and the customer read six attacks twice. `render-schema` renumbers the schema body **under the part it is rendered into** rather than restarting at 1 inside a numbered outline; part 3 excludes the prompt itself, which §1.1's first row admits and which is the document the reviewer is reading; rule 4 removes **every** bundled document's frontmatter rather than the figures file's alone; and `bundle-packaging.md` §1.1 now disposes of a captured file that is neither markdown nor an image — not bundled, named in the manifest, its links rendered as the plain description §2 requires. **`/brd-package`'s own manifest rule did not carry the obligation.** Phase 8 rule 6 enumerated three things the manifest holds and stopped, so a run working it as the enumeration it reads like wrote neither the *captured and not bundled* line §1.1 requires nor the *what the reader may not be able to see* note §2.1 obliges for a byte-for-byte copy. Rule 6 now names both and says outright that what it enumerates is this command's contribution to the manifest, not its contents. **Population: every package carrying a captured file that is neither markdown nor an image, or a customer file holding an embedded image or a one-tool block** — reachable only from 3.7.0, the §1.1 disposition being this release's (`captured and not bundled` returns nothing at `4387959f`). `/brd-interview` pins that **a re-tagged question keeps its position**, which nothing had fixed, and `/brd-package` now reads a held `[C]` entry's round off the heading `/brd-interview` pins for it. **Keeping the position put two recorded states at one round-and-position, and nothing said which governs.** The round record is append-only, so a question re-tagged and then *deferred* carries a terminal disposition (*re-tagged*) and a holding state at the same number, while the readers that ask what state a question is in — the resume rule, the closure rule and `/brd-package`'s gate on the rounds — were each written in the singular. A reader keyed on the address found the question terminal, resumed past it, and shipped a package over a deferred question. The **last** state recorded at an address now governs and the earlier is history, the same rule the record's `Status:` line already carried. **Population: every round holding a re-tagged question that then reached a holding state** — reachable only from 3.7.0, since the keeps-its-number rule is this release's (`A re-tagged question keeps its number too` returns nothing at `4387959f`). **And `decision-register-format.md` §1.1's record boundary is a positive test** — a blank line followed by a line at block indentation — because §4 mandates an appended closing paragraph inside `argumentation`, so "no blank line inside a block" would have split every reopened record in two or folded the marker `bundle-packaging.md` §6.3 reads into the customer's quotation.

**The `fixed` narrowing above had landed in the disposition table and not in the two paragraphs that act on it**, and the half that was missing is the half a run executes: the re-dispatch paragraph still told the run that `fixed` corrects "a decision, a seed or a `[C]` question" — the three the table had just made inadmissible — so an orchestrator reading it edited `decisions.md`, a write `decision-register-format.md` §4 forbids and `/brd-reconcile` refuses when a **customer** asks for it. An orchestrator reading the table instead found `fixed` unreachable in practice, because three of the five artifacts it admits are written by later phases and the row said the correction lands "before the prompt is rendered", which is incoherent for the prompt. The row now says what *corrected* means for each half — the self-review and an `[SR#n]` record are corrected at the gate; the prompt, the delivery note and the bundle's rendered copies take the correction **recorded** there and carried out by the phase that writes them, which is also why no `fixed` is ever a write into a package document. The re-dispatch fires on **any** `fixed` whatever its artifact, and the corrections reach the second pass through the self-review in `prior_reviews`. **Population: every 3.6.x package run whose review attacked a register record, a held `[C]` entry, a ledger disposition or a verified finding** — on 3.6.1 the disposition table carried no admissibility rule at all and the re-dispatch paragraph sanctioned the register edit outright (`git show 4387959f:plugins/product-workflows/commands/brd-package.md`, the `fixed` row and the re-dispatch paragraph), while §4 and `/brd-reconcile`'s own refusal row were already on file there; the P3 run had five of seventeen findings targeting `decisions.md`.

**The narrowing's two halves were each an enumeration where a rule belonged**, and a live package run found two artifacts outside both: an entry in the requirement-defect log, and the inventory's coverage record. `fixed`'s exclusion is now the complement of the five artifacts this command writes, and the `fixed-by:` marker names the command that writes the artifact rather than choosing from a fixed list. Where an artifact has no command writer at all — a defect entry a grounding finding would settle, whose `resolved-by: <SLICE-KEY>/[CG#n]` the route admits and no command mints — the marker is still written and takes the person who must write it by hand as its value, the recorded reason saying that no command writes it, because a marker naming a command that will not write it reads as routed when nothing is. **That last clause rules on the value and not on whether a marker is written**, and it was read the other way by a live `[C]`-only run of the route before this release shipped: two findings the team had agreed with went to the customer unmarked, which part 9 renders as no sentence at all — indistinguishable from the eight risks that run had genuinely weighed and accepted. The value now has exactly two shapes, a command or a person, and part 9's one generated sentence says only what holds of both. **Population: every package whose review attacks an artifact the row's own enumeration did not name** — the run met two of twenty-one findings in that state. Six smaller ones ride with it: a reviewer's own words added to the wikilink-rewrite carve-out in both the rule and `bundle-packaging.md` §2.1; the render-boundary test matched wrap-insensitively, since the sentence it seeks is hard-wrapped in the file it seeks it in; *"any `fixed`"* and *"exactly once"* separated into which dispositions open a review and how many times one opens; the restated pair printing in whichever wording says more; the *Send it* option no longer naming a route an archive-route run is not on; and relation 3's scope stated once beside its three guards. **Population for the boundary test: every package run, on any tree** — it is a stop, and a line-anchored implementation of it fires on correct content every time.

### Fixed — stale claims the release sweep found

`/brd-intake` Phase 2 and two documentation pages said a file the intake did not capture was outside the record *permanently* — *"the only chance there is"*, *"gone for good"*. That stopped being true in this release, whose wikilink fix above rests on a re-run over the same folder capturing what an earlier one missed; each now says the file stays out of the record until the document is intaken again, since nothing but that copy phase ever writes under `brd/source/` or `brd/source-external/`. `/brd-intake`'s documentation page said an `EMPTY` read makes Phase 8 offer a re-run *"instead of offering `/prd-ground`"*; the command it leaves out is `/brd-split`, the ordinary next step and the one that refuses an empty inventory. And `/brd-package` Phase 0 said nothing in the run reads the customer's document or the defect log, while its assembly phase scans every copy of both; it now says no phase before assembly reads them, nor the figures file, which assembly also reads to choose which images ship.

**No run's behaviour changed.** Four of these sentences are documentation prose or a command's rationale, and the fifth is a `/brd-package` Phase 0 sentence that gates nothing. **Population: every reader of them** — an operator deciding whether a missed file can still be captured, or an agent executing either command in order — who was told something the tree does not do.

### Fixed — `/brd-intake` on a slice key destroyed every allocation `/brd-split` had recorded

Phase 0 step 7 stated the absolute — *"A slice is never a legitimate target here"* — and then discharged it with *"say so and confirm before Phase 2 copies anything into it"*: no `choices:` array, no stop id, against four `*_ROOT_LEVEL` stops elsewhere on this route and this command's own four Phase 0 stops. A slice that claims any row holds `brd/brd-inventory.md` with those rows in it, `/brd-split` having copied the parent's in, so this release's new re-run confirmation fired and was the only well-formed array on that path — an array whose keeps-and-changes list and whose disposition warning are both written for a source-owning root, and whose warning calls `covered-here` an *illegal root* disposition where on a slice it is the legitimate terminal one. Answering it *Re-run over this folder* sent Phase 2 to write `brd/source/` into a folder that holds none (`brd-format.md` §2.1), Phase 3 to renumber rows §2.1 says are never re-extracted, Phase 4 to write a `brd/brd-defect-log.md` shadowing the parent's `[DEF#n]` namespace, and Phase 5 to replace every `covered-here` the allocate-only walk had recorded with `unallocated`. Nothing short of git recovers that. Phase 6's whole *write the seeds at the root* rule also rested on a refusal step 7 did not make. On a **standing empty child** (`claims: []`, kept against a recorded `reason:`) the same run was worse: that confirmation is skipped silently where no inventory row stands, so Phase 2 wrote into the slice with no consent asked anywhere. Step 7 now **stops**, with `BRD_INTAKE_SLICE`, taken before the re-run confirmation is composed and keyed on `brd-link.md`'s `parent:` field rather than on rows, so it covers both states; the stop names the whole recovery route — re-intake the parent, re-take the dispositions with the root walk that run offers when it finishes, and only then re-allocate a slice the walk left a row `unallocated` in, once `/product-workflows:prd-ground` has ground that slice — and takes `<PARENT-KEY>` from the resolved folder's own `brd-link.md` `parent:` field rather than parsing one out of the folder name (`CLAUDE.md`, *Resolve an identifier against a known set*). Phase 9's list of Phase 0 stops that are operator halts and never `emit-block`s names it, and Phase 6 cites it by id. **Population: every `/brd-intake` run whose key resolves to a slice — on 3.6.x as well**, where the same step carried the same unspecified confirmation with no array behind it at all; what this release added was the array that made the destructive answer look like a considered one.

### Fixed — five defects in what `/brd-intake` records and reports about the customer's images

All five are **new in this release**, whose `brd/brd-figures.md` and its §1.2 are new.

**A row anchored on an image the run did not capture was reported two opposite ways in one run.** Phase 3 decided between *not re-extracted by this read* and *this source no longer contains* off the anchor, glossing the second with *"or its file is not in this run's capture"*. That is true of a linked markdown file and false of an image: `brd-format.md` §1.2 keeps a not-captured image's whole section — marker, *Read*, hash, transcription — and §2.2 resolves an image anchor against the transcription of any image the figures file records as read, so such a row resolves. Phase 5, following §1.2, then wrote `yields [BR#n]` on that same section in that same run. The clause is now scoped to the markdown file the anchor names, and Phase 3 states outright that an image anchor is never in the unresolvable state for want of a capture.

**An unreadable image was counted `reused` on every run after the first and never appeared in the *not read* report again.** §1.2 counts a re-used section under `reused` whatever its *Read* line says, and its *Content hash* rule does not read such an image again — which is right, and matches `/workflows-core:frames` refusing to retry a frame its describer could not read. The reporting was the defect: run 1 printed `read 2 · reused 0 · not read 1` and run 2 printed `read 0 · reused 3 · not read 0`, with nothing saying one of the three has no transcription and never will, while relation 3 carried its `accounts:` entry so the operator was asked nothing. Phase 2.5 step 1 now says that a `Read: no` section is re-used on the same hash test and not re-dispatched, and the Final report names **how many of the images this run captured hold no transcription at all** — every one that came back `read: false` and every re-used section whose *Read* line still records a failed read — each with the reason on record.

**An image whose binding content was an unlabelled mark could not be quoted at all.** The Phase 3 coverage flow wrote only `<path> › "<span>"`, while §2.2 refuses an empty image anchor outright and calls `annotation <n>` *"the only one that reaches a mark with nothing to quote"*, the form `brd-reader` is required to use for exactly that case. An operator looking at such an image was left with *They hold no obligation*, which is false, or *Cancel*. The quote prompt now also accepts an answer naming an annotation by its position, taken where that row exists and its *Says* is `""`, re-asked for the label's own words where it is not, and falling to the failed-span branch where the table has no such row; the row takes `<path> › annotation <n>` as its anchor and that row's *Points at* as its `text`, and goes on the inventory's `quoted:` list like any other.

**`brd/brd-figures.md`'s `source:` was written from a `document:` Phase 3 had not yet updated.** §1.1's ladder is written for a *later* reader, and Phase 2.5 is not one: on a re-run over a document resent under a new filename the ladder still named the previous basename, so the figures file disagreed with the `document:` written minutes later in the same run, and the ladder had no rung at all for what Phase 2.5 meets on a first intake. `source:` is now taken from the document the writing run itself copied, which Phase 2 holds; §1.1's list of readers that take the name from the inventory drops the figures file accordingly.

**`brd-reader`'s non-unique-quote escape was scoped to *Text*, leaving a duplicated diagram edge with no sanctioned output.** §1.2 transcribes a diagram that draws one edge in two places as two identical *Flow* items and rules a positional *Flow* form out on purpose, so quoting that edge whole — the remedy the agent named — does not make it unique, and the *"quote it all the same and say so in `notes`"* escape was written for two *Text* regions reading the same. The escape now covers any element kind that holds no content of its own, naming the duplicated-edge case.

### Fixed — two `/brd-intake` claims falsified by the step they cite and by the phase they sit in

**Step 7's third cost of an unprefixed folder was falsified by the step it cited.** It claimed `/brd-split` Phase 3 step 2 *"creates its slice at `specifications/BRD-<PARENT-KEY>-<parent-slug>/PRD-…`, a path that would not exist"*. That step says the opposite in bold, gives this exact case as its reason — *"the parent half of that path is the resolved folder's actual name, never a path re-derived from `<PARENT-KEY>`"* — and cites this step back, so the two were live contradictory instructions pointing at each other. The clause now states what `/brd-split` does: an unprefixed root still gets correctly nested `PRD-` children. The other two costs are unchanged and correct. **Population: every reader of Phase 0 step 7's unprefixed-folder paragraph — on 3.6.x as well**, where the same clause stood verbatim.

**A first intake's numbering warning claimed an operator had seen something no run prints.** *"the operator has just seen the read's numbering"* is false on a first intake: no `[BR#n]` is printed anywhere before Phase 4. The announcement was also made *"as the row is added"* while ids are fixed once at the end, so quoting a second span out of source order falsified the first announcement. It is now made after the item's *Finished* answer in the coverage step's third-form array — the first moment the run knows there is no next quote — and says what it can honestly say, that the ids the operator will meet are not the order they quoted in. **Population: every first intake in which the operator quotes a span — new in this release.**

### Fixed — `/idea`'s walk question named one set in its sentence and another in its label, offered two options that could take the same files, and left its crossed-bounds line unjoined

All three are **new in this release**, whose Phase 1.5 is new.

**The printed sentence and the option under it named different sets.** The fixed question ended *"Reading all takes every file the walk reached, not only the files listed here"* directly above `Read all <n>`, where `<n>` is the counts line's headline total **less the source**. On the fixture the operator read `23 files reached (source + 22)`, then a sentence asserting a set of 23, then a label reading 22, with the arithmetic reconciling them living only in the command body — the same defect class the sentence had been added to remove. The line now reads *"Reading all takes the source and every one of the `<n>` files the walk reached beside it"*, so the printed sentence and the label describe one set.

**On a flat source the second option took exactly what the first did, and the `(Recommended)` marker sat on the first.** The question fires when any of three bounds is crossed, and two of them — twelve pages counting the source, and six images — are crossable with every file at depth 1; *Only what the source links directly* takes depth 1. A source linking ten images, or a hub note linking twenty pages, therefore offered an operator trying to read less a set identical to *Read all*, with nothing saying so — and those images all go through `figure-reader` on Opus, all get copied, and all land as dirty OTHER paths that fire `specs-repo-git` §3.3's G1 on every later run in the session. `escalation-rules`' *When a choice list fires* makes a list whose answer is already determined a defect, and the array is fixed text presented verbatim, so the disclosure goes where it can: the question now prints one further line, where the walk reached nothing below depth 1, saying that the second option takes that same set.

**And the crossed-bounds line got the join clause its neighbour already had.** The line below it says its `·`-separated parts are *"joined with `", "` in that order"*; the line above had the identical shape and no join at all, so nothing fixed how a run renders two or three crossed bounds. It now carries the same clause.

### Fixed — three `/idea` claims about what a run leaves behind, and one count that named no set

**`Two routes reach that state` was falsified by this same file sixty lines later.** Phase 4.5's account of a run without a handoff named a decline and a `status: draft` run. Phase 5 names a third: a `status: refined` run whose handoff was **taken** and whose `phase-handoff` §2.1 gate then refused it, which commits nothing and leaves `idea.md` and every vendored path dirty exactly as a decline does. It is reachable rather than theoretical — §2.1 fails on `specs_git: blocked`, and Phase 0 carries that flag for the whole run on a §3.3 G0 detached HEAD. The paragraph now says three, and says why the other non-landing §4.1 rows are not further routes (*No remote* and *Push failed* both commit on a branch first; *Nothing to commit* changed nothing). **Population: every operator reading a run's own account of a suppressed leftover flush — on 3.6.x as well**, where the sentence stood verbatim.

**A bullet gave opposite instructions three lines apart about the frame-set index.** Phase 4.5 step 6 states the rule — the index is written whenever the set holds at least one frame, *"including on a run that copied no image into a set an earlier run populated"* — and then *"A bare-prompt run creates neither directory, writes no index"*. A bare-prompt run **is** a run that copied no image, so `/idea <KEY> "<more text>"` re-refining a brief whose folder already holds `design/idea-sources/` hit both halves; `grounding-format` §6.2 step 6 sides with the first, and step 5's drop-a-gone-row reconciliation is what skipping loses. The last sentence is now scoped to a folder holding no frame set, `idea-format.md`'s identical sentence with it, and Phase 5's *"passes `idea.md` alone"* names the one `index.md` such a run hands over. **Population: every bare-prompt `/idea` run over a folder an earlier run populated a frame set in — on 3.6.x as well**, where both sentences stood verbatim.

**`a frame it accounts for nowhere is always one it never looked at` is false for a reused copy.** §6.2's writer table gives `/idea` the images it **copied**, and the collision rule's rule 1 *reuses* a byte-identical file rather than copying it — which two other sentences in the same phase acknowledge. Where such an image has no row at all, the run writes `_no description on record_` while holding `figure-reader`'s `depicts` for it. The *always* is gone. **Population: narrow — a reused image with no index row, an earlier run's index write having failed or a person having dropped the file in — on 3.6.x as well.**

**The Final report's vendoring counts named no sets.** `15 files copied into attachments/` sat two sentences after `markdown by depth — depth 1: 6, depth 2: 6, depth 3: 2`, and a copy the collision rule *reused* is in `deliverable_paths` and in neither count and was reported nowhere — in the one run state that rule exists to serve. The report now says whether the source is among the `attachments/` copies, and names every reused copy beside both counts. **Population: every `/idea` run that vendors anything — on 3.6.x as well.**

### Fixed — `/idea`'s next-step offer printed its merge clause inside the command, so the one line it offers was not runnable

Phase 5's `status: refined` branch recommended `` `/product-workflows:create-prd <KEY> <merge-clause>` `` with the placeholder inside the command's own code span. Every value `workflows-core:next-phase-offer`'s resolution table gives that placeholder is a parenthesised English sentence, so a live run printed `/product-workflows:create-prd IDEA-6 (once this phase's artifacts are on the default branch)` as one code span — the line an operator copies whole, handing `/create-prd` a positional token of prose where it takes an address. No other adopter does that: `/product-workflows:specify` and `/dev-workflows:design` give the clause a span of its own, `/product-workflows:update-prd` writes both in plain text, and this command's own `status: draft` branch prints two runnable command lines. The clause now sits outside the span, and the paragraph says so and why. **Population: every `/idea` run that reaches `status: refined`** — every run whose brief left no open `[NEEDS CLARIFICATION]` marker, on either handoff answer, since the offer is printed whichever option was taken. **Present before this branch** — the same span stands at `idea.md:411` at `4387959f`.

### Fixed — `/create-prd` wrote three fields onto an assumption that the register format marks Not applicable

Phase 3's customer-authority branch instructed *"All thirteen fields except `round`"* for the `[AS#n]` it writes into `decisions.md`. `decision-register-format.md` §7 accounts for those thirteen one by one and marks three of them **Not applicable** on an assumption — `options_considered` (*"An assumption is an assertion, not a choice"*), `chosen` and `settles` (*"An assumption answers no question, so it settles no requirement defect"*) — while §1.1 omits a field that does not apply rather than writing it empty. `/brd-package` restates every open `[AS#n]` into the customer-facing prompt, so the customer was shown a record saying an option was weighed and picked where nothing was weighed at all, which is the confusion §7's own *"a record that argues for the assumption while never admitting it is one"* paragraph exists to prevent; `settles: []` additionally put a `[DEF#n]`-typed field on a record `/brd-reconcile` resolves defects from. The instruction now names the set rather than counting it — write every field §7's table admits, in the meaning and vocabulary it gives, and none it marks *Not applicable* — as the sibling rewrites in `create-ard.md` and `specify.md` already do, and says why a count in its place goes stale. **Population: every `[AS#n]` `/create-prd` has written on the BRD route — on 3.6.x as well**, where the same instruction read *"All twelve fields"* and put two of the three onto the record; this release's twelve → thirteen bump, made because `settles` joined §1's set, added the third — `settles` being precisely one of the fields §7 rules out. `/brd-interview`'s own register-writing phase named the count where it needed the set in the same way, and now names the set too: a block carries every field §1 defines on a `[VD#n]`, and on an `[AS#n]` every one §7 admits and none it marks *not applicable*. **Population for that one: none observable** — it is a wording claim in the command that writes `decisions.md`, pre-existing at `4387959f` as *"twelve"*, misdescribing three fields §7 already governs, and no run's output changes.

### Fixed — two documentation pages promised a file list `/brd-intake` Phase 1 does not print

`docs/commands/brd-intake.md` and `docs/brd-workflow.md` each said the run *"shows you every file the document links"*. Phase 1 shows counts by kind, inside the document's folder and outside it, and names by path only the outside files, the files that are neither markdown nor an image, and the targets it could not resolve — so a BRD whose links are all inside-directory prints no filename at all, and an operator reading either page expected a list before consenting to a capture. Both sentences are now derived from Phase 1's own first bullet. The same two pages gained the `BRD_INTAKE_SLICE` stop where each already described the slice case. **Population: every reader of either page — new in this release**, whose read-only Phase 1 walk is new.

### Fixed — seven readings a live run could not settle from the text

A run executed `/brd-intake` end to end against a synthetic BRD and recorded every place the text admitted two readings. **Phase 3's numbering sentence named a moment the run cannot identify**: it was to be said once the item's last quote was taken and expressly not as each row is added, but nothing tells a run that a quote is the last until the operator answers *Finished* in the third-form array, which the same paragraph places after the row and after that array. It is now said **after that answer**, with the reason. **The set question's carve-out said "the question"** where the per-item questions two paragraphs below also name an item; it is now scoped to the set question by name. **Phase 2.5 step 3 conflated two file sets**, asking for *Linked from* *"from the copied files Phase 1's walk found linking it"* — the walk is read-only over the customer's **originals** and precedes every copy, so no copy carries the link it found; it now says to map each linking original onto the path of the copy Phase 2 made of it, relative to `brd/`.

**`BRD_INTAKE_SLICE`'s own text gains three corrections.** It substituted two of its five slots and said nothing about the other three: it now substitutes the `@<brd-file>` **this invocation was given** — the run holds it, and it is telling the operator to re-type it against a different key — and states per slot which are substituted and which two stay literal and why (`"<how to cut it>"` is the operator's to write, as Phase 8 prints its own copy; `<SLICE-KEY>` names a slice the root walk has not run yet). Its `brd/` clause called the whole directory the parent's eight words before naming the slice's own inventory — a slice **does** hold a `brd/`, holding exactly `brd/brd-inventory.md` (`brd-format.md` §2.1) — so it now names the source and the records beside it instead. And the leg reading *"once that slice is ground"* was the one hop of the remedy naming no command; it now names `/product-workflows:prd-ground <SLICE-KEY>`.

**`brd-format.md` §1.2's frontmatter template put rationale inside two YAML value slots**, which a literal writer copies into the file it is writing: `key:`'s carried a clause and `source:`'s carried three sentences. Both move to the bullet list below the block. **And `source:` was the one path-valued field in the family whose quoting went unstated** — the inventory's `document:` and a slice inventory's `source:` are both double-quoted YAML strings (§2, §2.1) — so it is now stated as one, for the reason those two carry: a filename may hold a `:` or a leading character YAML reads as syntax. **Population: every `/brd-intake` run for the three command readings and the stop; every write of `brd/brd-figures.md` for the template — all new in this release**, whose Phase 3 coverage step, `BRD_INTAKE_SLICE` and figures file are all new.

### Fixed — a requirement defect that opened a round was then withheld as waiting on the round it had just opened

`/brd-interview`'s *Every round is closed* branch opens round `<highest + 1>` when a requirement defect becomes this BRD's to ask, naming that defect as the change that made the round askable. The round-1 test then ran over the round that branch had just resolved and said of exactly that defect that "while some round is still open it waits" — so round 2 opened holding nothing, the account line recorded the defect waiting on round 2, nothing closed round 2, and the next run resumed it and withheld the defect again. A `[C]` only the customer can settle was never asked, and the printed remedy named the round that was already open. The wait clause is now scoped to a round **this run did not open on the defect's account**: a round opened for the defect raises it, and only a resumed round makes it wait. **Population: every slice interviewed since this release whose every round is closed and on which a requirement defect later becomes its to ask — a sibling's `/brd-reconcile` moving a row to `rejected`, an intake re-run over a revised source, an allocation or a re-cut under the parent.** Reachable only from 3.7.0: the requirement-defect question source and its account line are this release's (`requirement-defect account line` returns nothing at `4387959f`).

### Fixed — the one line of a held `[C]` entry that three readers parse had no pinned spelling

*Hold every `[C]`* pins the round heading, `- **Altitude:**`, `- **Rejected row:**`, `- **Requirement defect:**` and `- **Defect image:**` exactly, and left *the findings that bear on it* as bare prose — while this release gave that line three readers. `/brd-package` part 5 chooses the claim to verify first "mechanically" from the findings held entries list, part 7 renders them into the customer prompt, and `/brd-reconcile` copies them into the answering `[CD#n]`'s `evidence`. A run that wrote the findings as a paragraph — which is what the exercise runs wrote — left `/brd-reconcile` parsing for a field it could not find and freezing `evidence: []`: a false statement about what the customer was shown, inside frozen customer authority, legal by §1 and undetectable downstream. The line is now `- **Findings:**`, carrying each `[CG#n]`/`[DG#n]` with its verdict or an explicit `none`, and the three readers name it. Both readers the pin serves and the `[CD#n]`'s `evidence` say what to do with an entry written before this release, whose findings are prose: the customer prompt renders them from that prose rather than rendering the question with no findings at all. **Population: every held `[C]` entry whose findings a run wrote as prose, and every `[CD#n]` frozen from one** — the `evidence`-copying reader and part 5's held-entry branch are this release's, so a 3.6.1 tree carries the unpinned line with no parser behind it; and since the pin ships in 3.7.0 and `product-workflows` is 3.6.1 at `4387959f`, every held `[C]` entry on every tree today is one written before it.

### Fixed — the propagation sweep offered an assumption a status the format cannot write

`/brd-reconcile`'s propagation sweep puts every `[AS#n]` in a dependent BRD in its swept set and forces each item to exactly one of four dispositions, the third of which writes `status: reopened`. `decision-register-format.md` §7 narrows an assumption's status vocabulary to `open`, `superseded` and `withdrawn` — `reopened` follows `decided`, which cannot apply to an assertion nobody chose — so an assumption disposed that way dropped out of every set that reads `open`, and `/brd-package` stopped carrying it into the customer prompt's *decisions the customer must make* and *where to attack us hardest* parts and out of its something-to-review count. The array is now presented without *Reopened* on an `[AS#n]` item, and the entry says what an assumption that must be re-taken takes instead. **The same paragraph bounds *Reverted***: §4 writes seven fields afresh on a re-decision and moves `round` with them, so the register holds only the position that replaced the earlier one — a reversion is argued from the record's append-only `argumentation`, and where that does not establish a field's prior value the disposition is unavailable and the item goes to *what still needs a human* rather than being written from invented values. §4 now states where a reversion reads the position it restores. **Population: every dependent BRD holding an `[AS#n]` or a re-decided position that names a changed id or carries `conditional_on`** — reachable only from 3.7.0: assumptions entered the swept set and the `reverted` row grew from restoring `chosen` and `argumentation` to restoring seven fields and `round` in this release (`every decision **and every [AS#n]** in every dependent` and `the seven a decision's own writing fills restored` both return nothing at `4387959f`).

### Fixed — two sentences this release's own changes left stale

`/brd-interview`'s next-step paragraph said *"both cited gates"* after this release made it cite three of `/brd-package`'s Phase 0 steps — the register-presence step and the two content gates — so the sentence counted the citations and named the gates. It now reads *both content gates*, agreeing with the sentence twenty lines above it; at `4387959f` exactly two steps were cited and the sentence was correct, so the staleness is 3.7.0's own. And `/brd-reconcile`'s two-option array for a candidate matching no question still read *"Reject — this is not a customer decision at all"* after the four-option array beside it gained *a declared refusal to answer included*; a customer who volunteers a refusal about something nobody asked lands on that array, so it now carries the same clause. **Population: every reader of the next-step paragraph for the first; every returned review carrying a volunteered refusal for the second** — that wording divergence is this release's, the four-option array having gained the clause here.

## [3.6.1] — 2026-09-18

### Fixed — `/epics` could write an Epic where nothing downstream can find it

`epics.md`'s own Invariants say *"ALWAYS write every Epic to `EPIC-<key>-<eslug>/epic.md`, refined and net-new alike — the folder carries the key, the filename carries the kind (never `<slug>.md`)"*. `epic-writer`'s write bullet **licensed exactly that filename**, on the premise that slug-named files are reserved for net-new Epics with no work-item ID yet — a state nothing reaches: `/epics` mints a key for every confirmed Epic in Phase 1, and this plugin mints no work-item ID and reads no tracker, so *every* net-new Epic qualified under that sentence. A run following it writes `<prd_dir>/<slug>.md`: no `EPIC-` subdirectory, no `kind:`/`key:` frontmatter — so `/epics`' own re-refine detection, `/ready`'s per-Epic inventory and the Epic picker `/specify`, `/design` and `/implement` share are all blind to it, and the requirement the Phase 6.1 gate was resolving lands where the pipeline cannot reach it. The bullet now forbids `<slug>.md` outright and says what a slug-named file costs downstream; the "in mode: both" sentence that described the generate flow as slug-named now says keyed and foldered, as that flow actually writes; and `/epics` Phase 6.1's instruction, its `choices:` option and the Phase 9 report group drop the term, the Net-new group being identified by its minted key exactly as the Refined group is.

### Fixed — the `epic.md` template omitted the frontmatter its own lead-in requires

The sentence introducing the template says `epic.md` carries `kind: epic` and `key:` frontmatter. The fenced block it introduces opened at `# <Epic title>` with no `---` delimiters and neither field, so a writer copying the block produced a file its own lead-in forbids — and every sibling artifact-format authority in this family (PRD, ARD, specification, design, idea, BRD, grounding) shows the frontmatter **inside** the block, which made this the one place in the family that did not. The two fields are load-bearing: `/epics` gates its re-refine path on `epic.md`'s own `kind: epic`, `/ready` Phase 2 reads each folder's `epic.md` for its title and its own key, and `workflows-core:addressing` §4 resolves a folder's kind off its carrier — the first top-level file, in byte-wise order of name, whose frontmatter holds `key:` and a `kind:` naming a folder kind (`brd`, `prd` or `epic`), the order `workflows-core` 1.7.1 fixed — which in an Epic folder is `epic.md`. Exactly the two fields the lead-in names are added, and no third.

### Fixed — a `/specify` open question was filed under the wrong heading by the renderer

Phase 5's register partition told the run to place every open gap as a `- [ ]` item under the relevant stage's `### Open questions` — generalising over all five stages at a fixed depth of three, and citing on the next line the very reference that now fixes **four** hashes for a user story and **five** for an acceptance criterion. Proven by rendering rather than by reading the parser: two otherwise identical specifications through `specification-to-html.py`, differing only in the depth of one story-level heading, put the question under *Open questions (user story)* at `####` and under *Open questions (AC)* at `###` — filed on the neighbouring criterion, because the renderer's dispatch tests depth alone and no branch fires for three hashes inside the stories section, leaving the target on the story's last acceptance criterion. `specification-format.md` now states both depths where an author reads them, and the command **names the depth's owner and enumerates no depth of its own** — a second copy of the depths is what produced this. The parser is deliberately not widened to accept `###`.

### Fixed — four smaller stale claims, three of them in text a run prints or follows

`/epics`' Phase 8 change-summary block, which a run composes and hands to `impl-maintenance`, still instructed it to list *"new Epics by slug"* and marked refined Epics as the key-identified ones in contrast. Every Epic has a key now, so the contrast no longer exists: the line takes the key and the parenthetical goes. `/idea`'s printed context-hygiene block gave one reason for having no resume pointer and no `/rename` label and cited the wrong section for it — §4 governs the label only, while §1 owns the pointer and gives `/idea`'s reason as its brief being handed off in the same run; the invariant a little further down the same file already separated the two correctly, and the printed block is brought onto it. And `/idea` Phase 2.6 step 3 pointed at a line number for a claim the file had since moved past — **the one same-file bare line self-citation in the tree**, replaced by an address that resolves by content, since this is a command body an agent executes in order and a run checking the premise read the wrong paragraph. `docs/reference/hooks.md`'s *"the other eleven"* commands is now thirteen, the figure having been right when this plugin shipped twelve.

### Known limit that ships with this release

**Eleven documentation pages describe only the successful handoff, not the ways a consented one still does not land.** `docs/commands/{brd-intake,brd-interview,brd-package,brd-proposal,brd-split,create-ard,create-prd,prd-ground,prd-proposal,specify,update-prd}.md` each carry one template sentence of the shape *"Behind Phase N's consent choice, these are committed, pushed, and a pull request opened against the specs repo's default branch"*. Consent is a real condition and the sentence states it, so this is not the unconditional claim the sibling `dev-workflows` pages carried and had corrected — but it is the **success row** of `workflows-core:phase-handoff` §4.1 and nothing else, and that table carries several outcomes besides it — *PR already existed*, *PR not opened*, *Push failed* and *No remote* among them, over and above the declined case the sentence's own condition already covers — the last of which is an ordinary state (a specs tree kept locally, or a clone whose remote was removed) in which the commit still happens, nothing is pushed, no pull request is opened, and the run reports the phase as **not handed off**. So a reader of one of these pages can consent expecting a pull request and correctly get none. **Nothing misbehaves**: each command's own run says which row it took, on its `Phase handoff:` outcome line, and prints every path it staged beneath it. What these eleven pages owe is the rest of that table, and the sweep is deferred to a later release rather than half-done here — the correction has to be made against §4.3's four consent classes, which cost different things on a decline, and a per-page rewrite that flattens them would replace one incomplete claim with a wrong one.

## [3.6.0] — 2026-09-18

### Fixed — `/brd-split` Phase 6 quoted the **stopping** consent array unconditionally, for a set that is advisory-only on a reachable path (1.0.0)

Phase 6 presented `workflows-core:phase-handoff` §4.3's **gated — stopping** array for every run. On Phase 0 step 10's third branch — `unallocated_zero` with at least one standing empty child, where Phases 2–4 are skipped and **Phase 4.5 runs alone** — an operator who answers that phase's picker with **keep** for every child leaves `deliverable_paths` at that child's `brd-link.md` and nothing else: Phase 4 never walked, and Phase 5 *"rewrites only what the removal changed"* so `slices.md` is untouched. Phase 6's own closing paragraph already said as much (*"a 'keep' that wrote or updated a `reason:` stages that child's `brd-link.md`, so it opens a pull request like any other run"*). §4.0's register classes `brd-link.md` **advisory** — read by BRD-route detection in `/create-prd`, `/create-ard` and `/specify`, by `/epics` step 1a and by this command's own Phase 0 step 5, and named in no §3.4 row — so the run told the operator that declining costs a downstream stop no command makes, and then, three paragraphs later, printed §4.1's **advisory** outcome clause against its own prompt: the precise contradiction §4.3 exists to forbid.

**The phase now selects between two arrays**, the shape `/create-ard` Phase 6 and `/implement` Phase 4.5 already use: **stopping** where the set holds any *slice-level* `coverage-ledger.md` or `brd/brd-inventory.md`, which `/prd-ground` gates; **advisory** where it holds neither. The second branch is exhaustive over the rest and is reachable three ways — the keep-only Phase 4.5-only run, the no-op path and a keep that changed nothing (both empty sets), and a `full` root run that carved nothing, whose whole set is a **root's** ledger and `slices.md`, both advisory in §4.0's register. **Why the previous round's producer walk missed it:** §4.1's new *"a producer whose set varies between runs resolves this per run, and two do"* is scoped to sets spanning the two **gated** halves, and is correct on that reading; `/brd-split`'s variation is one level up, gated-vs-advisory under §4.0's set rule. **Ungated** — check 12 reads arity only, and both arrays carry three options — verified by mutation.

### Fixed — the reader census was re-stated as **two** readers on six surfaces while §0 and this section's own heading say **three** (3.6.0)

The previous round replaced the unscoped *"the one reader a proposal has"* with a scoped head plus an **exhaustive** remainder — *"every other read is a later run of the producing command"* — which is one reader short of the census it was summarising. `references/proposal-format.md` §0, the entry below, and the tree all agree on the same four reads: `/brd-proposal` on each **included** slice's `proposal.md` (the one cross-folder read, and the only gated one), a later run of `/prd-proposal` and a later run of `/brd-proposal` each on their own folder's as §8's stability anchor, and `agents/proposal-reviewer.md`, which *"Refuse[s] to run without a readable `proposal.md` at the given path"* (`:62`) and is dispatched by `/prd-proposal` Phase 9 and `/brd-proposal` Phase 10 — **inside the run that wrote the file**, which is neither of the other two relations. One cross-folder reader, three further readers, and the heading above was right. Every surface now says that: `commands/prd-proposal.md`'s framing and its `## Final report` instruction, `commands/brd-proposal.md`'s framing and its `## Final report` instruction, `docs/roles-and-phases.md`'s `proposal` paragraph, `docs/commands/{prd,brd}-proposal.md`, `docs/reference/proposal-format.md`, and `CLAUDE.md`'s plugin paragraph.

**Two surfaces still carried the retired claim outright, and one of them instructs a run to print it.** `references/proposal-format.md` **§1** read *"The one command that does read a proposal is `/brd-proposal`"* — in the same file as, and directly under, the §0 the previous round had just made the census, so the corrected file contradicted itself, which `workflows-core:instruction-file-maintenance` calls a defect outright. `commands/prd-proposal.md`'s `## Final report` is worse in kind: it is not description but *"**Say plainly, at the end**, … the one command that does read one is the sibling umbrella"*, so a run following the file in order printed the retired sentence to the operator having read the scoped one at `:32`. §1 now scopes the head and points at §0 rather than restating it, with a line saying not to restate it — it is the sentence the six copies were taken from. Two further sites outside the previous round's list fell to the same probe: `commands/brd-proposal.md`'s *"the only reader of the umbrella this run writes"* and its own `## Final report` *"the one thing that ever reads an umbrella"*, both false of `proposal-reviewer`, which reads the umbrella in that same run.

### Fixed — "gates nothing" labels about the proposal pair, and `roles-and-phases.md` contradicting itself between its role bullet and its phase paragraph (3.3.3)

`docs/roles-and-phases.md`'s **PM** bullet called the two commands *"which gate nothing and which nothing waits on"*, while the same page's `proposal` paragraph says *"`/brd-proposal`'s gate on a slice's `proposal.md`"* is real. The previous round's residual deliberately left five compact labels standing on the test that *"the subject is the **role or phase group** rather than a command"*; this bullet names **the two commands** and is ordinary prose in a bullet with no width constraint, so it met neither half. `README.md`'s two role cells are the same failure with the row's own Commands column naming one command each. Both are now *"gates nothing on the build ladder"*, and the bullet names what each **does** gate — `/prd-proposal` the folder's `prd.md`, `/brd-proposal` each included slice's `proposal.md`.

**The `proposal` paragraph itself was falsified by the same walk and is corrected with it**, which no grep of the bullet's wording would have reached: *"the one phase here that no other phase waits on and that gates nothing **outside itself** — `/brd-proposal`'s gate on a slice's `proposal.md` is this same phase gating its own earlier run"* disposed of one of the pair's two input gates and not the other. `/prd-proposal` runs `require-on-main` on `prd.md` (`workflows-core:phase-handoff` §3.4, `PRD_PROPOSAL_NEEDS_PRD` / `PRD_PROPOSAL_PRD_NOT_HANDED_OFF`), which is the `prd-creation` phase behind it and squarely *outside itself*. The paragraph now says nothing it gates lies **downstream** of it and names both input gates. Two further compact labels went with them — `docs/workflow.md`'s mermaid subgraph label and its role table cell — and `commands/create-ard.md`'s next-step guidance lost an unqualified *"nothing waits on a proposal"* in the one place a `PRD-` folder it offers may be a slice an umbrella will wait on.

### Fixed — positional claims in this section's own records, measured against the blobs they describe

*"read `"It gates nothing"` **twelve lines under** a *Gates* section"* — `docs/commands/brd-proposal.md` has `## Gates` at `:70` and that bullet at `:79` at every revision where it read so, which is **nine**, or **seven** from the `"only hard refusal on readiness"` bullet the sentence quotes. *"`/specify` being affirmed as carrying the clause **three lines above**"* — in `a251768f`'s own blob the affirmation is at `:468` and the *"Neither option …"* sentence at `:477`, which is **nine**; the same pair sits fourteen apart at `226b1e4d`. A **third** distance on that same line, which no ruling named and which the line-number sweep found only because the fix grepped its own string: *"the `/specify` bullet **three lines above**"*, about the pre-fix tree, where the two sit at `:647` and `:661` — **fourteen**. And in the `/create-prd` rung-4 entry, *"ten lines apart"* / *"the paragraph ten lines above"* is **nine** from the sentence and **eleven** from the paragraph's first line in `6914b5b3`'s blob, so no reading gives ten. Every one is replaced by a relative reference rather than re-derived, because a line distance goes stale on the next edit above it — the disposition an earlier round already chose for *"sixteen lines above the gate"*. **Verified correct and left alone**, so they are not "fixed" next round: `workflows-core:CHANGELOG`'s *"`dependencies.md` undercounted the family…"* entry, whose *"nine lines above"* is 36 → 45 in `480ab946^`'s `dependencies.md`, and its *"`dependencies.md` says who declares `prose-style`"* entry, whose *"eleven lines later"* is 11 → 22 in `2ca15cf0^`'s — cited by heading rather than by line, since this entry's own ruling is that a line citation into another file goes stale on that file's next insertion, and the two `:115` / `:236` pointers first written here were already stale in the commit that published them — plus `commands/frames.md:287` and `workflows-core:prd-format:57`, both re-measured by the previous review.

### Fixed — "the one reader a proposal has" stood on six surfaces, beside the census that names three further readers (3.3.3, and 3.4.0 for the two in `CLAUDE.md`)

`references/proposal-format.md` §0 names the readers: the one command that reads **another folder's** proposal is `/brd-proposal`, and *"the other readers open only their own folder's"* — `/prd-proposal` and `/brd-proposal` each on a re-run, plus `proposal-reviewer` inside the run that wrote it. The unscoped form said there is one, and it survived on `docs/roles-and-phases.md`'s `proposal` paragraph, `commands/prd-proposal.md`'s framing and its §4.3 class justification, `docs/commands/prd-proposal.md`'s *What it does not do* bullet, and both copies in the repository's `CLAUDE.md`, which arrived a release later at **3.4.0** (`5076d2ca`) and are the reason the sweep scope is `plugins/` **plus** the root files. On `docs/roles-and-phases.md` the refutation was the *next sentence*, added in the same delta: *"The umbrella itself is read only by a later run of `/brd-proposal`"* establishes that a later run of the producing command is a reader, and `/prd-proposal` reads a slice's `proposal.md` on exactly that relation — Phase 0's revision note, Phase 6 step 6, and §8's stability rule, which carries a package's expected hours forward from it. Each head is now scoped the way `docs/commands/brd-proposal.md`'s twin already was, and says beside it that a later run of the producing command reads its own folder's. **`commands/prd-proposal.md`'s consent class is unaffected** and says so: the own-folder read is off the working tree and stops nothing, so a `PRD-` folder's `proposal.md` stays gated by `/brd-proposal`'s §3.4 row. *(Corrected in 3.8.0: `/brd-proposal` is the one command that reads another folder's proposal **as a proposal** — `/brd-reconcile`'s stale cross-reference sweep also reads any proposal under the parent BRD, as prose, and never edits one.)*

**Two exclusivity claims about *gating* fell to the same probe, both on `/brd-proposal`'s page.** Its *What it does not do* bullet lost the word `downstream` in this delta and read *"It gates nothing"* under a *Gates* section naming the command's *"only hard refusal on readiness"* — no line distance is stated, because one goes stale on the next edit above it; its opening paragraph carried the same claim unqualified from `10a72f2b` (2026-09-09, **3.3.3**). Both now read *"gates nothing on the build ladder"* and point at the input gate — the command body's own form. `docs/reference/proposal-format.md`'s twin needed no change: its subject is the **document**, not the command. **Ungated** — `check-docs.sh` check 9 is the only gate that reads a documentation page's prose *for its claims*, and it reads inventory **count** sentences alone (checks 1, 10, 13 and 15 read the same pages for links, marketplace tokens, vendor tokens and command listings, and check 14 reads every text file in the repository — docs pages included — for the quarantined organisation names; none of those is what this sentence carries) — verified by mutation.

### Fixed — `/idea` read two link forms where `/brd-intake` reads four, so a screenshot a source embedded as `<img src>` reached nothing and was reported nowhere (dev-workflows 3.20.0)

`agents/idea-reader.md` said *"Both syntaxes are followed — `[[wikilink]]` and standard markdown `[text](path.md)` alike"*, and `grep -cE '<img|reference-style|\[label\]:'` returned **0** on that file and on `commands/idea.md` alike. So an idea source carrying a screenshot as `<img src="shot.png">`, or naming a page or an image in a reference-style definition `[label]: target`, had that file reached by nothing: not followed by the traversal, not read by the image pass, not enumerated into `links_other`, and not named in `wikilinks_not_followed` or `wikilinks_broken` — the state *"a link nothing copied and nothing reported is indistinguishable from a link that was never there"* forbids, which is the same silence `/brd-intake` Phase 2 removed for a customer's BRD. **3.20.0 widened the set from one form to two**, adding standard markdown because *"a source written outside a vault uses the second form"*; that argument covers the other two exactly as well, and they were never added. A new `### The link forms` section now fixes the set in one place for every pass below it — the `[[wikilink]]` plus `/brd-intake` Phase 2's list, taken from there unchanged and restated in full rather than re-invented — and states that the form decides only *that* something is a link while the extension decides which pass takes it, so `<img src="shot.png">` is read exactly as `![shot](shot.png)` is.

**The caps and the reporting vocabulary are untouched, which is the whole of the trade.** `/idea` reads for a grill under 12-file and 6-image caps while `/brd-intake` preserves an immutable record; only *which forms are recognised* moves, and no form gains an array, a `reason` or a cap of its own. Verified against a fixture source linking an image beside it, one in a subdirectory, one above it, an absolute path, a URL and a missing file, plus a page reachable only through a reference-style definition and a `.pdf` named only there. Before: two pages and three images, **nine further targets found by nothing**. After: the appendix followed at depth 1 and its own frame reached at depth 2, three further images read, the `.pdf` in `links_other`, the URL and both missing targets in `wikilinks_broken`, and the frame past the six-image cap listed with `read: false` and `reason: cap` — every new form disposed of through vocabulary that already existed.

**Four surfaces stated the old coverage and are rewritten rather than appended to** — `idea-reader`'s `description` and its `## Bounding` depth bullet, `/idea`'s own `description` and Phase 2 prose, `docs/commands/idea.md`'s *What it reads*, and `docs/reference/agents.md`'s row. Phase 4.5's rewrite list keeps its four forms and now says why it is the shorter one: it matches links in `idea.md`, which Phase 4 authored and into which every link this phase writes is standard markdown. **A fifth sentence fell to the same sweep**, and its history is three releases rather than one: `commands/create-prd.md` Phase 2 called the bound *"two wikilink levels"*, which was **true when it was written** — `7fa6fa75`, 2026-09-01, `dev-workflows` **3.16.0**, whose `idea-reader` traversed *"`[[...]]` wikilinks … up to two levels deep"* and nothing else. 3.20.0 (`107cc7c8`, the same day) falsified it as a **count** by widening the traversal to two forms, and `idea-reader`'s own vocabulary rule — reserve "wikilink" for the `[[...]]` syntax and say "link" everywhere else in `/idea`'s chain — then forbade it outright as **wording**, entering a day later at `5225e0d8`, `dev-workflows` **3.24.1** (2026-09-02). The draft of this entry dated that rule to 3.20.0; `git show 107cc7c8:plugins/dev-workflows/agents/idea-reader.md` carries no such rule, which is why provenance here is read out of a blob rather than off a nearby version number. **Ungated** — nothing in `scripts/` reads an agent's traversal rules — verified by mutation.

### Fixed — `/create-ard`'s BRD-route offer swapped the command out of an option and left its role label, lead-in and merge clause behind (dev-workflows 3.13.0, before the split)

Phase 7's BRD-route array shipped one literal option — *"Hand to a Product Engineer — `/product-workflows:epics <SLICE-KEY>` (PE) `<merge-clause>`"* — under an instruction resolving **the command alone** by a test: `/epics` where the slice holds an authored `prd.md`, otherwise `/product-workflows:create-prd <SLICE-KEY>`, `/product-workflows:brd-split` against the slice or its parent, or no second option at all. Measured across the tree, `/create-prd` carries **(PM)** at every other offer site (`commands/brd-reconcile.md` Phase 14's prose list and its array, `commands/create-ard.md`'s own idea-route bullet, `workflows-core:next-phase-offer`'s routing graph) and `/brd-split` carries **(PM)** too (`next-phase-offer`'s BRD-route node); `/epics` carries **(PE)**. So on three of the four substitution branches the run printed a PM command under a Product-Engineer lead-in and a `(PE)` label — a false role attribution to the operator, in an array `workflows-core:escalation-rules` requires be presented verbatim. **The clause was in the same position and wrong in both directions**: `/create-prd` gates only `idea.md` and reads no ARD, and `/brd-split <PARENT-KEY>` is a root run in `split_mode: full` that skips the grounding gate entirely — neither waits on anything this run writes — while `/brd-split <SLICE-KEY>` runs `allocate-only` and does gate this slice's `grounding/code-grounding.md`, which this run's own `deliverable_paths` stages for the `consumed_by: ARD` writes. **The fix writes the five arrays out**, one per branch of the test, each option carrying the lead-in, role label and clause of the command *it* names — the same repair `/prd-ground` Phase 10 took one round earlier, and the shape this command already used for its other two routes. Three options per array except the drop branch's two, inside `AskUserQuestion`'s 2–4 (check 12), no authored "Other", and `(Recommended)` on `/product-workflows:specify <SLICE-KEY>`, which is reachable on every branch.

**Two sentences beside it were falsified by the same paragraph and are corrected with it**, neither reachable by grepping the option's wording. The `/epics` bullet closed *"No option here gates anything this run produced, so none carries a merge clause"* — contradicted by both options of its own array and by the `/specify` bullet above it (*"so the wait is real and the clause is required"*); `workflows-core:ard-resolution` names `/epics` among the **five** of its six consumers that **stop** on `status: unmerged` — `/ready` is the sixth and is excepted by name in that same file, recording it as a readiness finding capping the verdict at `PARTIAL` — so its option's clause was right and the sentence denying it was not. That contradiction is older than the substitution rule: `c17f8118` (2026-09-01, **dev-workflows 3.13.0**) wrote *"`/epics <ADDRESS>` is offered unconditionally … It gates nothing this run produced, so it carries no merge clause"* above an array that already carried the placeholder on it; `a251768f` the same day extended it to the replacement option beside it (*"**Neither option** gates anything this run produced, so **neither** carries a merge clause"* — two of that array's three, `/specify` being affirmed as carrying the clause at the close of the bullet immediately above); and `4a7cc6f0` (**dev-workflows 3.14.0**) then made it the universal this fix removes. The bullet's lead-in carried the second: *"only one of the three usual options can be reached with one \[slice key]"*, while the array beneath it named two of them — `/specify` and `/epics` both take the slice key, and it is `/dev-workflows:design` alone that a slice key cannot reach. The closing paragraph's *"the wait it names is real for every command named above"* is scoped to the options that carry the clause, since two commands named above now correctly carry none.

**Ungated in both directions, and measured rather than assumed.** `check-docs.sh` check 11's family glob is `/product-workflows:brd-*` and `/product-workflows:prd-*` (`next-phase-offer`'s scope paragraph), so `/create-ard` is inside the `<merge-clause>` *convention* — that paragraph names its *Next-step offer (adaptive)* phase among the six converted pipeline offers — and outside the *gate* that enforces it; check 12 sees only arity, which the fix keeps legal on every branch. Verified by mutation.

### Fixed — the `/brd-proposal` docs page's replacement exclusivity claim was still stronger than the agent it describes (3.6.0)

`658b1b21` replaced one over-claim (*"Only its Coverage check tells the two apart"*) with a narrower one that is still false: *"Two checks tell the two apart … **Nothing else about it differs by folder kind.**"* The same commit taught `agents/proposal-reviewer.md` the clause that falsifies it — *"A rule a check **delegates** to still carries its own scope: check 6 re-grades against `references/proposal-format.md` §5, which grades no umbrella off its ladder at all"* — and §5 confirms it: *"an umbrella's tier is **not graded off this ladder at all**: it is the minimum of its included slices' tiers (§14)."* Check 6's behaviour therefore does differ by folder kind, and a reader of the page concludes the reviewer re-grades an umbrella against a ladder that reads `prd.md`, `ard.md` and `specification.md` — none of which a `BRD-` container holds — which would file a tier BLOCKER against every umbrella claiming tier ≥ 2. Both surfaces now carry the agent's own scoping: nothing else in that agent *narrows a check* to an umbrella, though a rule a check delegates to keeps its own scope. **The command body carried the same sentence and is corrected with the page**, from `10a72f2b` (2026-09-09, 3.3.3); the page's own form entered in `658b1b21` at **3.6.0**, in the commit that also wrote the agent clause refuting it. **Ungated** — no check opens an agent's hard rules, and none reads a `Gates` bullet's exclusivity claim: `check-docs.sh` check 9 is the only gate that reads a documentation page's prose *for its claims*, and it reads inventory **count** sentences alone (checks 1, 10, 13 and 15 read the same pages for links, marketplace tokens, vendor tokens and command listings, and check 14 reads every text file in the repository — docs pages included — for the quarantined organisation names; none of those is what this sentence carries) — verified by mutation.

### Fixed — `/create-prd`'s capture-at-block invariant named a stop the command cannot emit (dev-workflows 3.5.0, before the split)

Phase 7's invariant closed *"`CREATE_PRD_NEEDS_KEY` and **`CREATE_PRD_TWO_SEEDS`** are the same."* `grep -rn 'TWO_SEEDS' .` over the whole repository returned that one line and nothing else — after the fix it returns this entry alone, which quotes the name in order to retire it: there is no emission site, and no state that could reach one — the stop fired when `--from-prd` and `--from-brd` were given together, and `/create-prd` has had exactly one value-taking seed flag since `--from-brd` was retired. Phase 7 is an executed instruction list, so this named a stop id a run can never produce and left a dangling member for anyone auditing the command's stop inventory against the tree. **Dangling from the commit that removed its cause:** `7088dff1` (2026-08-31, *"retire issue_type, `--from-brd`, and the two-grammar rule"*, `dev-workflows` **3.5.0**) deleted the emission block and rewrote this very sentence in the same diff, carrying both `CREATE_PRD_BRD_NOT_FOUND` and `CREATE_PRD_TWO_SEEDS` across unchanged; the first was later retired from the sentence and recorded as retired at the head of the file, the second was not. The name is dropped — the sentence reads correctly without it. **Ungated**: `check-id-grammar.sh` matches dash-separated requirement IDs and `check-docs.sh` opens no stop ids; verified by mutation.

### Fixed — `/brd-proposal` told the operator that nothing reads the file its own re-run reads (3.3.3)

Phase 11 presented `workflows-core:phase-handoff` §4.3's **`unread`** consent array — *"Just write the files — I'll handle git (nothing downstream reads this, so no command stops on it)"* — and its justification recorded a search that never looked in the run's own folder: *"no command of the build ladder reads a proposal, `/prd-proposal` reads only the profile and its own folder, and the one command that reads another folder's proposal — this one — reads a **slice's**, never an umbrella's."* Every clause is about *other* folders. **This command reads its own umbrella on every re-run:** Phase 0 step 5 notes whether `proposal.md` already stands in the resolved folder, and Phase 6 step 9 *"Apply §8's stability rule on a re-run, against the prior revision Phase 0 noted"* — the anchor `references/proposal-format.md` §8 makes the thing a moved figure owes a named cause against. §4.0's own test settles the class: *"no §3.4 row, **but** a command reads the artifact and never gates on it"* is **advisory**, and its `customer-review-<YYYYMMDD>.md` row is advisory on exactly this relation — a later run of the same command. The array is now the advisory one (*"no command stops on this; what reads it reads your working copy"*), which is also what is true: declining the handoff leaves the anchor on the working copy, where the next run reads it. The `<downstream-clause>` §4.1 prints follows the class automatically and needed no edit here.

**Three sibling sentences stated the retired claim and are corrected with it**, none of them reachable by grepping the array's wording: the command's own framing (*"nothing reads the umbrella this run writes"*), its `## Final report` instruction (*"nothing — this command included — reads an umbrella"*), and `docs/commands/brd-proposal.md`'s *What it does not do* bullet, which carried the strongest form of all — *"It gates nothing downstream, and **nothing reads what it writes**"* — against a reference twin that was already correctly qualified (`docs/reference/proposal-format.md`: *"It gates nothing, and nothing on the build ladder waits on it"*). `docs/roles-and-phases.md`'s `proposal` phase paragraph carried a fourth (*"and nothing reads the umbrella itself"*). **The census the class was derived from is corrected too**, since it is the recurrence vector: `references/proposal-format.md`'s reader list named *"two other readers … inside the pair"* and omitted `/brd-proposal`'s own-folder read — the one this defect turned on — and it now names both commands, the Phase 6 read and the `proposal.md`-authoring phase that archives after it — **Phase 7 in `/prd-proposal`, Phase 8 in `/brd-proposal`**, since the two do not share the number and `/prd-proposal`'s Phase 8 authors the *brief* — and why the umbrella is `advisory`.

**False the day it was written, in the same commit:** `10a72f2b` (2026-09-09, **3.3.3**) created `/brd-proposal` with Phase 0 step 5 and the §8 stability rule already in it, selected the `unread` array in the same file, and added the §4.0 row asserting *"The umbrella is read by nothing"* — a row whose own last clause then described the read (*"the anchor a later re-run diffs against is the canonical `proposal.md` it reads before archiving it"*). **Ungated** — no gate in `scripts/` opens a §4.3 array's parenthetical; check 12 sees its arity, which does not change — verified by mutation.

### Fixed — `/prd-ground` Phase 10's `/brd-split` withhold test ordered an edit to an array the family presents verbatim (1.1.0)

The `route: brd` branch shipped **one** literal `choices:` array, hardcoding `(Recommended — allocate-only, so no child is created)` with no placeholder in it, and above that array an instruction to replace the recommended option wherever the run recorded a frame set `skipped: no index`: *"do not offer `/brd-split` as Recommended … Offer the repair the stop itself names, **in the same position**"*. There is no third thing a run can do with those two sentences. `workflows-core:escalation-rules`' *Choice lists are presented verbatim* reads *"Its options, their order, their wording, and the `(Recommended)` marker are not the orchestrator's to change"* and, a few lines on, *"There is no permitted adjustment"*; `workflows-core:next-phase-offer` states it from the other side — *"A command that instead told the orchestrator to adjust the wording of an option would be contradicting that convention, which is why the variation lives in a placeholder and not in an instruction."* So a run reaching that state either edited a verbatim array or recommended `/brd-split` on a key `/brd-split`'s own Phase 0 step 7 test b refuses with `BRD_SPLIT_DESIGN_NOT_GROUND` — the exact stop the test was written to keep the operator out of. **The state is reachable**: Phase 5 gets `NO_INDEX` for a frame set, Phase 8 records `skipped: no index` in `## Frame sets covered`, and the test fires. **The fix writes the second array**, selected by the stated test, which is the mechanism this same phase's `route: idea` branch has used all along; the placeholder form `/brd-split` Phase 4's `<recommended>` uses is equally sanctioned (`escalation-rules`, *A marker the command resolves through a placeholder is not a violation of this rule*), and two literal arrays is what this phase already reads like. The withheld array recommends `/workflows-core:frames <BRD-KEY>` and names `/brd-split` in none of its options, because neither half of the repair the stop itself prescribes — write the index, then re-ground with `--no-code` — has run yet; the prose above both arrays still names `/brd-split` and where it sits in the route, which is `next-phase-offer`'s universal minimum for a route an array does not carry. Three options per array, inside `AskUserQuestion`'s 2–4 (check 12), no authored "Other", and each `(Recommended)` marker on an option that can actually be taken.

**Two sentences beside it were falsified by the same paragraph and are corrected with it.** The justification *"The offer's wording is deliberately 'its grounding is complete and verified' rather than the older 'now that every finding carries a verifier outcome'"* described **no array in the tree** — `grep -rn` over `plugins/` returned that one line and nothing else; after the fix the only occurrences are the replacement sentence, which quotes one phrase as retired history, and this entry. Both phrases are real history: `e7aac79f` (2026-08-30) wrote *"Split the BRD once every finding carries a verifier outcome"*, `e86c5ecd` replaced it with *"now that its grounding is complete and verified"*, and `a1109d82` (2026-09-06) retired the root-level branch that array belonged to while keeping the sentence that described it; a later pass then updated the count inside that very sentence (*three* → *four* tests) without re-reading the clause beside it. The rule it carries survives, restated against the arrays that ship: a marker naming one of `/brd-split`'s four tests promises a pass the offer cannot deliver, so each marker says what the run it recommends will *do*. And `docs/commands/prd-ground.md` said the offer *"always names `/brd-split`"* while the command said *"unless the test below withholds it"*; the page now carries the exception in one clause. Two sentences below the arrays that spoke of "the offer" in the singular are scoped to the array that carries `/brd-split`, since the other one does not.

**Unfollowable from the day it was written, at 1.1.0:** `e86c5ecd` (2026-09-06) authored *"in the same position"* beside an array that already hardcoded `(Recommended)` with no placeholder, and `escalation-rules`' verbatim rule already stood. **Ungated** — check 12 reads a `choices:` array's arity and never the prose that selects it — verified by mutation.

### Fixed — `/prd-ground` Phase 10 branched on a grounding verdict that does not exist (3.0.0)

The idea route's next-step offer selected between its two `choices:` arrays on a claim coming back **`SUPPORTED`**, at seven sites in the command and one on its documentation page. `SUPPORTED` is not a grounding verdict: `workflows-core:grounding-format` §3 is closed at **exactly six** — `CONFIRMED`, `AMENDED`, `REWRITTEN`, `FALSE-FRIEND`, `NOT-PROVABLE`, `SUPERSEDED` — and its §2 field table says a finding's `verdict` is *"exactly one of the six values in §3"*. **The consequence is a route no run could reach:** the *"At least one claim `SUPPORTED`"* array is selected by a predicate no finding can satisfy, so the `(Recommended)` *"Revise the PRD first — `/update-prd`"* option was unreachable and the no-claim array fired on every run, including the runs the recommendation exists for. The verdict the paragraph meant is named by its own stated reason — *"a PRD asking for something the code already does"* — which in §3's vocabulary is `CONFIRMED`, *"the premise holds, with evidence"*, and it is the only one of the six that says so — `AMENDED` says partly true, `REWRITTEN` materially wrong, `FALSE-FRIEND` a decoy, `NOT-PROVABLE` unsettleable from the repository, and `SUPERSEDED` a finding a later one replaced. `REWRITTEN` in particular is ruled out rather than merely passed over, because §3 names it and `NOT-PROVABLE` as the verdicts an absent mechanism lands on — and this command's own final report already reports an all-absence run as *greenfield*, the case that needs no PRD revision at all, so a set including `REWRITTEN` would have recommended `/update-prd` on exactly the run that headline exists to close. The correction is a vocabulary one, applied at all eight sites, and the paragraph now carries §3's definition so the token is not re-invented. **The `<N>` the array prints is defined with it, and a raw tally would have been the wrong operand:** it is a count of **distinct requirement claims** — the requirement id read off each `CONFIRMED` finding's own `claim` field, de-duplicated — and it excludes Phase 3's baseline `[CG#n]`, which is `CONFIRMED` by construction and written one per resolved repository, on the property `workflows-core:grounding-format` §4.1 rule 1 already states and already excludes baselines from its own report for: a baseline finding's `claim` is not a requirement premise. Counting the run's whole `CONFIRMED` tally instead would have replaced an unreachable branch with one that fires on **every** idea-route run, greenfield included — the run this command's own final report calls greenfield. Neither Phase 9's `body_facts` nor the final report supplies that de-duplicated quantity (both state finding counts by verdict), so the paragraph says outright to count it from the findings Phase 8 wrote. **Dangling from the day it was written:** six of the seven command sites entered in `e816544c` (2026-09-08), and the seventh — the frontmatter `description` — together with the documentation site in `59b303e0`, the commit that cut **3.0.0**; §3 already read "Exactly six" at both. **Ungated** — check 12 reads these two arrays for arity and never for what the prose above them tests — verified by mutation.

### Fixed — `proposal-reviewer`'s catch-all severity rule stopped one section short of the contract it governs (3.4.0)

Process step 4 read *"a violation of an absolute rule **§1–§13** states is a **BLOCKER**"*. A bare `§N` in this file means `references/proposal-format.md`'s §N — the file says so itself (*"This file cites it by section number throughout and never restates it"*), and its own checks are "check N", never "§N". That contract carries **§1–§14**: `## 14. The umbrella — what `/brd-proposal` adds over a slice's own proposal` is its last heading. §14 states absolute rules no check 1–11 assigns a severity to — the umbrella's per-slice rows are *"read out of that slice's own `proposal.md` and never re-derived"*, it *"does not restate a slice's `[ED#n]` driver table"*, *"Ranges are summed, and stated as summed"*, and its tier *"is the minimum of its included slices' tiers"* — so under the range as written they fell into the only remaining bucket, RECOMMENDATION, and an umbrella that re-derived a slice's hours or claimed a tier above the minimum would have been reported as advisory. `workflows-core:finding-triage`'s BLOCKER escalation never fires on that. **The range is replaced by a citation rather than widened to §14**, so the next section added to the contract does not reopen this. The same step's enumeration *"(2, 6, 7)"* of checks naming both severities goes with it: only check 2 names both (a no-reason band deviation is a RECOMMENDATION, everything else a BLOCKER), and checks 6 and 7 name BLOCKER alone — the clause *"where a check names both, follow the rule stated there"* is what the step actually needs and is true of any check. **True the day it was written and falsified by the commit that taught this file to cite §14:** `2037b4a2` created the agent when the contract's last heading was `## 13.`, and `10a72f2b` added `## 14. The umbrella` **and this agent's two §14 citations** without widening the range. **Ungated**, verified by mutation.

### Fixed — `proposal-reviewer`'s header said only check 9 distinguishes a slice run from an umbrella run (3.4.0)

The header read *"the same review, over a `PRD-` slice's own proposal or the `BRD-` umbrella's, and **only check 9 tells the two apart**"*, and the Hard rules repeated it as *"reviewed by checks 1, **2**, 3, 4, 5, 6, 7, 8, 10 and 11 exactly as a `BRD-` umbrella's is; only check 9 differs by folder kind"*. Check 2's final bullet is umbrella-conditional and says so on check 9's own test — *"(Umbrella runs only — the same folder-kind test as check 9.)"* — and it is the only relation in the file that catches a programme total which merely sums its slice rows, the thing §14 calls *"indistinguishable from an arithmetic error"*. The header is scoping guidance the agent reads before any check, so an agent told the review is *the same* except for check 9 has a stated reason to work check 2 in its slice form on an umbrella. Both sentences drop the count rather than enumerate around it: the header now says a check that tests the folder kind says so where it applies, and the Hard rule names check 2's bullet as the one place this file narrows a check beyond check 9 — and, separately, that a rule a check **delegates** to carries its own scope, which is how check 6's re-grade meets §5's *"an umbrella's tier is not graded off this ladder at all"*. `commands/brd-proposal.md`'s own dispatch paragraph already named both checks and was the one site that had it right. **True the day it was written:** `2037b4a2` wrote the header when check 9 genuinely was the only folder-kind-conditional check; `60f27877` added check 2's bullet and left both exclusivity claims standing. **Ungated**, verified by mutation.

### Fixed — the `$DOCS_PATH` consumption-mode partition gave `/epics` a mode it does not use (1.0.0)

`docs/reference/environment.md` enumerated the six commands that resolve no docs grounding, leaving eight consumers, and then offered two modes for them — *"grill-rank in the authoring/interview commands, lead-only in `/prd-ground`"*. There are three. `workflows-core:docs-grounding` names **`writer-attach`** for `/epics` and `/release-notes`, grill-rank for `/idea`, `/create-prd`, `/update-prd`, `/create-ard`, `/specify` and `/brd-intake`, and lead-only for `/prd-ground`; `CLAUDE.md` states the same split. `/epics` is an authoring command by any reading — `epic-writer` is *"the sole author of the Epic drafts"* — so the omitted member is covered by the phrase that names the partition's first half, while `/epics` runs no grill at all and has nothing for ranking to reorder. The line now names all three modes and the six commands the first covers. Nowhere else on this page or in `docs/getting-started.md` is writer-attach named. **False the day it was written:** `4ec4a374` (2026-09-06) built this documentation tree with the two-mode clause already in it, under a `plugin.json` reading **1.0.0** (the plugin was still named `pm-workflows`; the rename carried the version across) — and at that commit `epics.md` was already in it and `workflows-core:docs-grounding` already named `writer-attach` for `/epics`. The non-consumer enumeration beside it has been rewritten twice since; the partition was never re-read.

### Fixed — `/prd-ground` Phase 10 sourced its count from a Phase 0 step that reports a different one (3.0.0)

The next-step offer's `SUPPORTED`-claims branch read *"**Phase 8i's** report carried the count"*, and both halves were wrong. There is no Phase 8i: the command's `## Phase` headings are 0, 1, 2, 3, 4, 4.5, 5, 6, 7, 8, 9, 10 and 11, and `8i` is a **step of Phase 0** — the one that reads the claim list from `prd.md` on `route: idea`. Every one of the file's six other mentions calls it that (*"step 8i"*, or *"step 8 (or 8i)"*); this was the only one that promoted it to a phase. **And the step it named carries the wrong number.** Step 8i reports how many requirement rows were *excluded* from grounding — its own words are *"11 of 19 requirement rows ground; 8 excluded — 3 `[UC#n]`, 5 `[SM#n]`/`[SMC#n]`"* — and it runs in Phase 0, before Phase 5 grounds anything and Phase 7 verifies it, so it cannot hold a count of how any claim came back. The `<N>` Phase 10's own array prints is the verdict tally instead: Phase 9's `body_facts` sentence already carries it, and the Final report states it for `[CG#n]` and `[DG#n]` separately. The clause now names that. **Correcting the phase token alone was not enough, and that is recorded rather than quietly re-cut:** an earlier pass on this branch changed `Phase 8i` → `step 8i` and left the referent, which is the whole-sentence rule this release's changelog discipline exists for. **Dangling from the day it was written:** the sentence entered in `e816544c` (2026-09-08) and shipped at **3.0.0**, where `grep '^## Phase'` already listed the same thirteen headings and step 8i already reported only the exclusion count. **Ungated**, verified by mutation.

### Fixed — the deleted vault was still a live place in four `product-workflows` sentences (1.0.0)

`references/bundle-packaging.md` §2 opened *"The working documents live in a vault and use its syntax"*, and `references/idea-format.md`, *Vendored sources*, says the opposite of the same tree: *"`$SPECS_PATH` is a git repository, read on a forge's web view and in ordinary editors; it is **not** an Obsidian vault. Nothing there resolves `[[name]]`, and a forge renders it as literal text."* Both cannot be true, and the tree settles it — `$VAULT_PATH` was deleted in increment D and appears **nowhere** in the four family plugins, the repo-root `README.md` or `CLAUDE.md`; the full env-var census over that scope returns `$CLAUDE_PLUGIN_ROOT`, `$SPECS_PATH`, `$REPOS_PATH`, `$DOCS_PATH`, `$GIT_USER_INITIALS`, the two guideline paths and the cost-price variable, and no vault variable of any spelling. **The section's work is right and is untouched**: the artifacts genuinely do carry `[[KEY]]` wikilinks, which is what the de-Obsidianising pass rewrites for a reader outside a vault. Only the premise changes, to what the tree holds — they live in the specs repository, which is a git repository and not a vault, and carry wikilink syntax anyway, `[[KEY]]` being the specs tree's traceability form and the customer's own source document arriving with whatever its author wrote. Two definite articles pointing back at the deleted premise (*"resolves to nothing outside **the** vault"*, *"a reader outside **the** vault"*) become indefinite; every sentence about a reader outside a vault is otherwise left exactly as it stood, because that reasoning is the whole point of the rendered copy.

**`/epics`' invariant carried the same premise without the word**, and its provenance shows how: `6914b5b3` (2026-08-31, `dev-workflows` 3.9.0, *"increment D, deleting $VAULT_PATH"*) rewrote *"`/epics` writes into an Obsidian vault, where a wikilink is the native idiom **and resolves**"* into *"`/epics` writes markdown that Obsidian and IntelliJ both render, **where a wikilink resolves**"* — the store was removed from the sentence and its property kept. The invariant's instruction is unchanged (`[[KEY]]` wikilinks stay) and only its reason is corrected: they are the specs tree's required traceability form, not a link that has to resolve. The matching clause in `workflows-core:doc-structure-conventions` §1, which the same commit edited the same way, is corrected in that plugin.

**`/brd-package`'s two never-write lists named the vault as a fourth place a run must not touch** — *"NEVER touches a code repo, a docs repo, **the vault**, or the current working directory"* and *"NEVER writes into a code/docs repo, **the vault**, or the current working directory"*. `workflows-core:specs-repo-git` §1 *Scope* fixes the triple (*"a code repo, a docs repo, or the current working directory, where it is not the specs repository"*) and some twenty sibling sites across the family carry exactly it; these two were the outliers. The token is dropped; nothing else in either sentence moves.

**Ungated**, verified by mutation: with all four sentences restored verbatim the nine-gate CI chain exits **0**.

### Fixed — `/create-prd` treated its `idea.md` rung 4 as retired and as a live picker, in one step of one ladder (1.0.0)

Phase 0 step 3's ladder numbers rung 4 *"(retired — the discover rung searched a personal store for a stray `idea.md`. `/idea` writes into the resolved folder now, so rung 1 finds it.)"*, while the BRD-route paragraph above it justified skipping the whole ladder by saying *"**rungs 3 and 4** would then offer an idea from some other initiative — a picker over stray `idea.md` files is exactly the offer that names something this run has no business reading."* Two live contradictory instructions about one structure, inside a command body an agent executes in order. **The ladder itself settles it twice over**: rung 4 is the tombstone, and rung 1's own fall-through text says an unhandled-off `idea.md` *"would otherwise be passed over in silence all the way to rung 5's grill-from-scratch"* — a sentence that is only true if nothing between rungs 1 and 5 can find a file. **The enumeration is what changes, not the reasoning**: rung 3 is a real source of a stray `idea.md` — a same-session `/idea` output path, offered for confirmation — so the paragraph still has its reason for skipping the ladder, now resting on the one rung that can supply one. The "picker over stray files" phrasing went with rung 4, which is the only rung that ever was a picker. `docs/commands/create-prd.md`'s *"a rung-3 or rung-4 picker"* transcribed the same claim and is corrected with it; that page's own list of remaining rungs already named two, not three. **Born of a retirement that did not sweep its own file:** the sentence entered in `b2c93124` (2026-08-30) when both rungs were live, and `6914b5b3` (2026-08-31, `dev-workflows` 3.9.0, *"increment D, deleting $VAULT_PATH"*) retired rung 4 — the personal store — without touching the step-3 lead-in above it (no line distance is stated: measured against `6914b5b3`'s blob it is nine lines from the sentence and eleven from the paragraph's first line, which is why the number was wrong at ten and would go stale at either); `git show 6914b5b3:plugins/dev-workflows/commands/create-prd.md` carries both lines. It reached this plugin unchanged with the extraction, at **1.0.0**. **Ungated**, verified by mutation: with both sentences restored verbatim the nine-gate CI chain exits **0**.

### Fixed — `/prd-ground` cited a "four-part stop contract" that exists nowhere in the tree (1.0.0)

The `REPO_MISSING` / `FRAME_SET_MISSING` / `NO_INDEX` / `STALE_INDEX` branch read *"Stop, naming the finding and the path the agent reported — and, **per the four-part stop contract**, the command that resolves it"*. There is no four-part stop contract to consult: `grep -rn 'stop contract' plugins/*/references/` returns nothing at all — that is the relation which decided it, and it is the one to re-run. (The wider `grep -rn 'stop contract' plugins CLAUDE.md README.md` this entry first cited does **not** return a single line: it returns changelog hits too, this entry among them and `dev-workflows`' own record of the commit that introduced the clause, which was already in the tree on the day the claim was written. A recipe that returns a wrong answer is worse than a stale count, because the next reader trusts what it returns.), and the only *four-part* shape in the corpus is `workflows-core:specs-repo-git` §5's **guard notice** contract, which governs the specs-repo guards G0/G1/G2 rather than a command's stop. A run following the line is sent to an authority it cannot find. **The pointer is cut, not repointed** — naming §5 would assert that a `/prd-ground` stop is a specs-repo guard notice, which it is not — and every instruction in the sentence survives untouched: it still names the finding, the path the agent reported, and the command that resolves it. **Dangling from the day it was written:** the clause entered in `107cc7c8` (2026-09-01) as `dev-workflows` 3.20.0's `commands/brd-ground.md`, and `git grep 'four-part stop' 107cc7c8` at that same commit returns only the line itself; it reached this plugin unchanged with the extraction, at **1.0.0**. **Ungated**, verified by mutation: with the clause restored verbatim the nine-gate CI chain exits **0**. *(Corrected in 3.8.0: a four-part stop contract did exist when this was written — `workflows-core:phase-handoff` §4.4 *Stop contract*, which the case-sensitive recipe above misses; run it as `grep -rni`. §4.4 takes `specs-repo-git` §5's four parts and binds `require-on-main` stops only, not the agent-reported `REPO_MISSING` / `FRAME_SET_MISSING` / `NO_INDEX` / `STALE_INDEX` return this branch stops on, so the cut stands — on scope, not on absence: the pointer named an authority that does not govern this stop, not one that cannot be found.)*

### Fixed — `docs/commands/epics.md` counted another plugin's command wrong, ranked its own wrong, and undercounted its dispatches (3.5.0)

*"`/epics` has 20 `## Phase` headings — the second-most in the plugin, after `/document`'s 37"* was false in both halves. `/document` has **34**, not 37 — `grep -c '^## Phase' plugins/docs-workflows/commands/document.md`, which is also what `docs-workflows`' own `docs/commands/document.md` has said since the re-derivation that produced the 34, so the tree carried two live contradictory statements of one number. And `/epics`' 20 is the **most** in this plugin, not the second-most: the next is `/brd-reconcile` at 16, and `/document` ships from another plugin altogether, so the comparison the sentence made was with the family and not with the plugin it named. The cross-plugin number is **cut rather than corrected**, because a count owned by another plugin's command is a count this page cannot keep true: the re-derivation fixed the page that owns `/document` and left this one, which only cites it, standing. What the sentence exists to say — that `/epics` is large — survives in the half this page can verify against its own tree.

Beside it, *"Six subagents are dispatched"* enumerated six and omitted `prose-style:prose-style-checker`, which Phase 6.2 dispatches unconditionally. That is not a convention here: the same page names it twenty lines below as a non-gating quality pass, and the sibling pages for `/create-prd` and `/update-prd` both count it inside their own number. Seven named agents are dispatched, re-derived by intersecting the family's agent inventory (`plugins/*/agents/*.md` basenames) with `commands/epics.md` and reading every hit — `docs-style-checker` appears there too, only ever in the rule never to run it. The omitted dispatch is the one that crosses into the `prose-style` dependency, which is exactly what a reader asks this sentence.

### Fixed — `docs/commands/epics.md` cited a line of `/epics` that carries a different gate (3.5.0)

The optional PRD-level `specification.md` bullet cited *"Phase 2.6, `commands/epics.md:180`"*. Phase 2.6 begins at `:340` and executes its `require-on-main` at `:353`; `:180` is a Phase 0 coverage-ledger stop — a different phase, a different gate, and the one place `/epics` reads a ledger. The citation is inherited from `origin/main`, where the line held the same text, so it has never resolved. The line number is cut rather than renumbered, per this repository's own convention: the phase name does not go stale on the next edit above it. It was the only citation into a file of this tree, outside a changelog, that did not resolve — the other two, `changelog-owners-reminder.sh:7` and `cost-prices.yaml:22`, both do.

### Fixed — three HARD model gates tested the environment where they meant the session, and so missed the state they were written for (3.5.0)

`/create-ard`, `/prd-proposal` and `/brd-proposal` each open with *"require an Opus session"* and then stop on `opus_available` being false. Those are not the same condition, and `workflows-core:model-routing/classification` §2 keeps them apart deliberately: `opus_available` is *"true if a §2 Opus model resolved"* — what the `task` tool can reach — while the session's own tier is the separate `current_model` field of the same block, which every one of these three records beside it. Reading the field as the session's tier broke the gate in both directions at once.

**It missed the state it exists for.** On a Sonnet session with Opus reachable — `opus_available: true` — the gate did not fire at all, so a `SIGNIFICANT`/`HIGH-RISK` run authored an ARD, or a customer-facing estimate, inline on Sonnet with no advisory of any kind; `/create-ard`'s own line reserves the soft advisory for `SIMPLE`/`MODERATE`, and the two proposal commands, floored at `SIGNIFICANT`, carry no advisory line at all — while §9.1's third bullet requires one at SIGNIFICANT/HIGH-RISK wherever the orchestrator authors inline, which all three do. `/create-ard` declares its gate *"(like `/design`)"*, and `/design` tests `current_model`; the copy dropped the condition.

**And where it did fire, its recommendation was impossible.** `opus_available: false` is the one state the gate reached, and in it the `(Recommended)` option was *"I'll relaunch … on Opus"* — a relaunch onto a chain the run had just established the environment does not carry, which §9.3 forbids.

All three now test `current_model`, as `/design` does, and drop the relaunch option where `opus_available` is also false, leaving proceed-on-the-floor or cancel. `docs/reference/model-routing.md` described the retired condition for all three in one paragraph and is corrected with them. The semantics are stated once, in `workflows-core` 1.6.0's §9.3, and cited here rather than restated three times.

### Fixed — `docs/getting-started.md` told a reader a renamed clone is lost to `/create-ard`, which matches no name at all (3.5.0)

Its `REPOS_PATH` section said `/create-ard` and `/idea --ground-code` *"list top-level directories under `$REPOS_PATH` and match on their **basenames**, so a repo renamed on disk is not found by those two unless the rename is also reflected there"*. Neither command resolves a repository by name on that path: both `ls` one level down, propose a candidate set and confirm it with the operator (`create-ard.md`'s *Cheap discovery* step, `idea.md` Phase 2.6's *Resolve the repo set* step in its bare form), so a renamed clone appears under its new name and is offered like any other. What the sentence names instead is `/idea`'s **explicit** `--ground-code <repo>,<repo>` argument, whose parts must each match a top-level basename or the token is read as idea text instead — that one really does break on a rename. It is not the only basename match in `/idea`: Phase 2.6's **OFF branch** step matches idea-text tokens against `$REPOS_PATH` basenames too and goes silent on a rename the same way, but the page scopes itself to `--ground-code`, so it correctly leaves that one out. A reader debugging *"why was my clone not offered?"* was sent to a remedy that changes nothing; the real causes are the directory not being where the listing looks, or `$REPOS_PATH` resolving elsewhere. Found by sweeping what this release's `docs-workflows` sibling had just corrected — the identical overshoot, one plugin over, and older.

### Fixed — six `/epics` dispatches carried no model tier, against the command's own invariant (3.5.0)

`/epics` says twice that every subagent dispatch pins its §9 chain — as a role→chain map at Phase 1.5 and as an ALWAYS invariant naming `doc-fixer` among the mechanical steps — and `docs/reference/agents.md` states the same universal for every agent carrying no frontmatter pin. Phase 7's BLOCK branch and its "Manual fix notes" resolution dispatched `doc-fixer` bare, and Phase 8 spawned its four maintenance agents — three `general-purpose`, one `workflows-core:impl-maintenance` — bare. All six now pin the §2.1 Sonnet chain, and both the `detection_model` comment and the invariant name the Phase 8 maintenance agents they had left out of their consumer lists.

*"Nothing — the pipeline reads and writes one markdown tree and calls no external service."* **Neither half survives**, and the first correction of this entry cut only the second: a consented handoff opens a pull request through `gh` (`workflows-core:phase-handoff` §2.6) and `docs-grounder` retrieves through the `qmd` CLI, while *one markdown tree* is wrong in two further directions — `$DOCS_PATH` is a second markdown tree the grounding commands read, and the clones under `$REPOS_PATH` are no markdown tree at all. The same over-claim was retired from `workflows-core:dependencies` in 1.6.0 and this is the copy one file away. The answer is now `gh`, when present, with the two read-only sources named beside it — matching the sibling `dev-workflows` page, which answered the identical question differently.

### Fixed — `/specify`'s page said its repo candidates come from PR URLs (3.5.0)

`docs/commands/specify.md` had the candidate repos *"auto-derived from the PRD's capability themes and linked PR URLs"*, and its BRD-route note contrasted `grounding/baselines.md` with *"PRD themes and PR URLs, which this route has none of"*. The command's own Phase 3 step 1 says the opposite in as many words — it builds the list from the Phase 2 capability themes and the resolved folder's `implementation.md` `repo:` entries, and *"There is no PR list to read: nothing here reads a tracker or a pull-request API"* — so the page and the command it documents were two live contradictory instructions. The page now names the implementation record.

### Fixed — `/prd-ground`'s baseline gate skipped a path `git status --porcelain` quotes (3.5.0)

Phase 1's baseline-integrity step 3 compares each porcelain-reported entry's working-tree line count against `git show <sha>:<path> | wc -l`. `--porcelain` quotes a path carrying a space, a `"`, a `\` or a non-ASCII byte, and `git show <sha>:"<quoted path>"` resolves to nothing — which step 3 reads as "exists nowhere at the pin", its own not-a-signal case. So a dirty tracked path with a space passed a gate whose whole purpose is that every `file:line` in the package cites an identifiable snapshot. The path is now read from `git -C "<repo>" status --porcelain -z`; `workflows-core:grounding-format` §4 step 3 is the authority and states why, and the readable three-command block is unchanged because `/brd-package` Part 4 hands it to the customer's reviewer to run by hand. `/brd-split`'s note on why a folder-shaped declaration stages no removal names the same `-z` form `workflows-core:phase-handoff` §2.3 now reads.

### Fixed — `/brd-intake` captures the files the customer's BRD links

Phase 2 copied `@<brd-file>` byte-for-byte into `brd/source/<basename>` and copied nothing else, and 3.5.0 shipped that. A customer's BRD routinely carries screenshots, diagrams and appendices beside it; after intake every one of those links resolved to nothing, and `brd/source/` is the immutable record every `[BR#n]` anchors into (`references/brd-format.md` §1, D11) — nothing under it is ever written again, so what intake did not capture was outside the record permanently. The run meanwhile reported a faithful verbatim copy, which is what made the loss silent. Phase 2 now copies, byte-for-byte and whatever the type, **every file the document links from its own directory**, to the same relative path under `brd/source/`, so the copied text's links resolve exactly as the customer's did with no edit to the text. The forms covered are the ones a customer's markdown uses: markdown inline images and links, reference-style definitions, and HTML `<img src>`. Each target is resolved against **the directory of the file the link sits in** and normalised as text, and is copied only where it carries no URI scheme, does not begin with `/`, lands inside the source document's own directory, and names a readable file there — that containment test being what keeps every copy inside `brd/source/` rather than above it. The capture then repeats over each markdown file it copied, to a fixed point, so a linked appendix's own links do not reproduce the loss one level down.

The test was a syntactic *"contains no `..` segment"* while this entry was first written, and it was wrong the moment the transitive pass was added beside it: `../images/flow.png`, written in a copied `appendix/notes.md`, resolves **inside** the boundary and its literal path from the copy stays inside `brd/source/`. Measured on a fixture of the ordinary export shape — a document, an appendix one directory down, and a shared `images/` folder beside it — the syntactic rule refused six links whose targets were all in scope, lost `appendix/diagram.png` outright, and wrote `outside the source directory` into the log about files the copy in fact holds; the containment rule refuses only the four that genuinely are a URL, an absolute path, an escaping `../`, or a missing file, and the no-edit invariant holds under it because the copy mirrors the source tree's own layout.

**What could not be captured is named rather than dropped.** A new `brd/brd-link-log.md` carries the source document's basename, the run's counts, and one row per uncopied link — the target as written, the file it sits in, and one of four reasons (above the source document's own directory, an absolute path, a URL, unreadable) — and the final report says the same. It is written on every run, including one that captured everything, so its counts are the positive record that the capture ran. It is the **plugin's** record, not the customer's, which is why it sits in `brd/` beside `brd-inventory.md` and `brd-defect-log.md` and never under `brd/source/`, where every byte is the customer's own. `references/brd-format.md` gains §1.1 as the authority for all of it, Phase 7's `deliverable_paths` enumerates the copied files and the log, and `brd-reader` is told the linked files sit beside the source so a link is not a dead end — it still reads only `source_path`.

One consequence is worth stating because a reader will meet it: `brd/source/` can now hold more than one markdown file, so **which file in it is the customer's document is read, never guessed** — `brd/brd-link-log.md`'s opening line names it, and a BRD intaken before that log existed holds exactly one file there. `brd-format.md` §2.1 says so where it fixes the `source:` header a slice's inventory carries, which is the one place that name is re-derived *(at this release; 3.7.0 moved the rule into `brd-format.md` §1.1, which has two readers of the name — that `source:` header and `/brd-package` naming the document in the bundle — and reads it from the inventory's `document:` before the link log's opening line)*. What a screenshot *means* to the requirements — a `[BR#n]` anchored to an image, the defect walk reading one, grounding against one, a package returning one — is not in this release; this is capture and fidelity only.

**Why this is 3.6.0 and not 3.5.1.** The run writes files it did not write before — every in-directory linked file, and the link log — so the artifact set a consumer sees is larger, which is added behaviour at the plugin level rather than a repair that leaves the output unchanged.

### Fixed — the workflow diagram renders again

`docs/workflow.md`'s mermaid diagram failed to render on GitHub — `Parse error on line 42 … got 'SQS'` — and had since 3.0.0, when the `/prd-ground` edges were drawn. Five edge labels carried a bracketed requirement ID (`[BR#n]`, `[CG#n]/[DG#n]`, `[AC#n]/[FR#n]`) unquoted, and inside a mermaid edge label `[` opens a node shape. Each label is now quoted (`-->|"…"|`). The diagram parses under mermaid 10.9.8, 11.17.2 and 12.0.0, and renders. No gate checked mermaid syntax, which is how it shipped; the repository's CI now runs `scripts/mermaid/check-mermaid.mjs`, which parses every diagram in every tracked markdown file outside its own fixture tree, `scripts/fixtures/mermaid/`, on each push.

### Changed — a count the `docs-workflows` cold-start increment moved

`commands/create-prd.md` Phase 0 called this command *"the only one of the **twenty-six** `commit-artifacts` callers running no preflight"* — true in 3.5.0, and moved to twenty-eight by `docs-workflows`' `/docs-init` and `/docs-brand`. It is a runtime instruction an installed user reads, not a documentation page. The count was doing no work in a sentence that is a historical note about a past defect, so it is **removed rather than corrected** — the claim it makes is about being the only one, not about how many there are.

### Fixed — `bundle-packaging.md` no longer miscounts `specs-repo-git.md` §2.1

`references/bundle-packaging.md` said `specs-repo-git.md` §2.1 *"bounds staging to three path shapes, all of them under `dev-workflows/**`"*. §2.1 named six in `workflows-core` 1.5.0 — three directory shapes and three single files, the files sitting beside a feature folder's artifacts rather than under `dev-workflows/**` — and names nine in 1.6.0, its fourth directory shape, `documentation/<docs-repo-slug>/…`, not under a feature folder at all. Like the count above, it is read at run time. Restated as "a fixed, enumerated set of path shapes", which is what the paragraph's argument actually needs — the point is that a bundle matches **none** of them, not how many there are.

### Fixed — the documentation route's gaps draft is named as it is written

`references/code-defect-log-format.md` cites `workflows-core:source-truth` §7.5 as the family's sibling code-defect record and named its draft `implementation-gaps.md`, the name §7.5 itself carried. `docs-workflows` writes it as `<KEY>-implementation-gaps.md`, which is what §7.5 now says too, so a reader sent to the documentation route looks for the file that exists.

### Fixed — the grounding agents read the repository they were handed

A subagent's Bash tool starts every call in the session's directory — where `/prd-ground` stands — and a `cd` does not persist between calls. `code-grounder` wrote its three read primitives bare (`git show <commit>:<path>`, `git grep -n <pattern> <commit>`, `git ls-tree -r --name-only <commit>`) beside a citation of `workflows-core:read-only-repos` §4, where they carry `-C`; run from the session's directory they exit 128, the pinned commit not being there. `grounding-verifier` named `baseline-integrity`'s three steps as bare `rev-parse HEAD`, `diff --stat` and `status --porcelain`, which would compare the pin against the session's `HEAD`. Both now write `git -C "<repo_path>"`, and each is told why and to give `Grep` and `Glob` the handed root as their path. Where a call runs in the repository instead, it is a `(builtin cd "<repo_path>" >/dev/null && …)` subshell, so a `cd` function or alias of the user's, which the Bash tool's shell carries, neither runs in its place nor prints into what the agent reads.

### Fixed — `/epics`' claims file template ends in `XXXXXX`

`/epics` named the file it hands `epic-reviewer` as `claims_file` with `mktemp -t dw-epics-claims-XXXX.md`, a template BusyBox's `mktemp` — the one Alpine ships — rejects with *mktemp: Invalid argument*, since it takes a template only where it ends in six `X`s (BusyBox 1.36.1; GNU's accepts it). It is now `dw-epics-claims-XXXXXX`, which GNU, BusyBox and BSD `mktemp` all accept.

### Fixed — `/create-prd` no longer says `/release-notes` asks for the category label

Phase 3's frontmatter step, and the command page, did not ask for `release_notes_category`, on the ground that `/release-notes` "infers and confirms `change_type` and `release_notes_category` in its own grill". That command uses the label verbatim where the PRD carries one and otherwise omits it, never inferring or asking; so a PRD whose release notes should carry a label must carry the field. Both now say that the field is optional, that `/release-notes` infers `change_type` and confirms it only where the inference is uncertain, and that it takes the release from `--version` or its grill, never from the PRD's `release_versions`. The step still asks for none of the three, and still writes whichever the operator volunteers.

### Fixed — `/update-prd` no longer names the import it retired

Phase 0 step 4 says the base PRD is the resolved folder's own `prd.md`, and that the ladder which read an imported copy, stopped when none existed and offered a refresh past three days is gone. Yet Phase 7 still listed `not-imported` among the environment halts that must never raise a plugin-feedback block, and asked `impl-maintenance` for "import/freshness friction" among the run's key events, so both the reader and the maintenance handoff were told to expect a halt and a check the command no longer has. The invariant now names the halts it does have — a key whose folder does not resolve, or holds no `prd.md` — and the handoff asks for BLOCK reviews and unresolved clarifications.

### Fixed — no command says it never commits in a working directory that is the specs repository

`/idea`, `/specify`, `/epics`, and the BRD route's `/brd-intake`, `/brd-split`, `/prd-ground`, `/brd-interview`, `/brd-package` and `/brd-reconcile` said their terminal `commit-artifacts` step "NEVER touches" the current working directory, and `/epics`' invariants that it never commits "anything in the current working directory". Where the session stands inside `$SPECS_PATH`, that step commits and pushes in the working directory's own repository. Each now says so only where the working directory is not the specs repository.

### Fixed — no command says it never writes into a working directory that is the specs repository

All fourteen commands said their final phase "NEVER writes into" the current working directory — `/specify` and `/epics` said it of their feedback phase too, and `/epics` of its follow-up phase — while the `/prd-proposal` and `/brd-proposal` pages said each writes "nothing in your current working directory" and the session-cost page that the plugin "never writes into your current working directory". Where the session stands inside `$SPECS_PATH`, those phases write their feedback, follow-up, cost and resume files into the working directory's own repository. Each now makes the claim only where the working directory is not the specs repository. The session-feedback page's sentence, which speaks of a run where nothing resolves and so nothing is written, holds as written.

### Fixed — a run removes the temp files it made

`/epics` wrote `epic-writer`'s handoff file at Phase 6 and, on a BLOCK verdict, `doc-fixer`'s Fix Report at Phase 7, both to `mktemp` paths outside every repository and outside the specs tree, and nothing removed either: every run left them under the system's temporary directory, where no later phase and no later run looks. Phase 8 now removes both — `command rm -f -- "<path>"`, so an `rm -i` or `rm -I` alias of the user's cannot answer the prompt from an empty standard input and leave the file — and a run that stops before Phase 8 removes what it had made.

### Fixed — `/epics` creates its temp files with `command mktemp`

`epic-writer`'s handoff file (Phase 6) and `doc-fixer`'s Fix Report (Phase 7) were named with a bare `mktemp`, which the Bash tool's own shell resolves through any alias or shell function of that name before `mktemp` runs — so the path the run writes to and hands on is whatever that printed. Both now run `command mktemp`, the rule `dev-workflows:context-management` states.

### Fixed — `/create-prd` says when `/release-notes` settles `change_type`

The Phase 3 frontmatter step and the command's page both said `/release-notes` infers a type "where the PRD carries no `change_type`". It also infers one for a value that is present but not routable — `not applicable`, or `Bug fix` on a deprecating change (`docs-workflows:release-note-types` §7) — so both now say "no **routable** `change_type`".

## [3.5.0] — 2026-09-09

Five open defects from a live-engagement defect register, found running the family across two
customer engagements.

### Changed — both grounders are pinned to Opus

`code-grounder` and `design-grounder` carried no model pin and `/prd-ground` dispatched them on the
Sonnet detection chain. **Grounding adjudicates** — it decides whether a claim is true of a commit —
and detection is what `code-scanner` does. The measurement: blind re-derivation of a Sonnet-ground
corpus found **18 of 18 code-citing findings defective** (nine `contradict`, seven `extend`, no
`agree` at all), with a second slice near 50% verdict error. An Opus-ground corpus of 314 findings
still moved 85%, but mostly by *omission* rather than error — so the tier is a real and separable
cause and not the whole cause, which is why the pin ships beside `control` rather than instead of it.

Both are now frontmatter-pinned, like every reviewer. `workflows-core:docs-grounder` deliberately is
not: it **retrieves**, and a missed lead costs a lead. `/prd-ground` reports `ground_tier` on every
run, not only a degraded one, because a reader cannot otherwise tell an Opus corpus from a degraded
one and the two are not interchangeable evidence.

The standing cost objection is answered rather than dismissed: cheap and wrong grounding is the more
expensive option, since a corpus in which every code-citing finding is defective has negative value
and the cost is deferred and multiplied through verification, reconciliation and the human reading
the result. Where the spend is unacceptable on a given run, make the pin conditional on
classification rather than reverting it.

### Added — every absence claim carries a positive control

Both grounders emit `control` (`workflows-core:grounding-format` §2.2) wherever a finding asserts an
absence, and `code-grounder` gains step 4a and a hard rule for it. `design-grounder` carries it on
**class 2** — a requirement asks for a field no frame shows — which is an absence over a frame set
and fails the same way: the field may be there and this reading may not be one that finds it. Classes
1 and 3 resolve against the `inventory` the caller handed the agent, which is a lookup rather than a
search and so has nothing to control for; class 4's code half is the cited `[CG#n]`'s own search.

`grounding-verifier` is handed the field and **runs** the control rather than reading it, returning
`control_outcome`. It settles **owed-ness first**, by §2.2's closed-set rule: a negative over a set
the caller handed in is a lookup, not a search, so classes 1 and 3 owe no control and class 4's code
half belongs to the cited `[CG#n]`. Only class 2 — a negative over the frame set, which is read —
owes one. A `missing` control forces `contradict`; a `failed` one does too, **except** on a finding
already reading `NOT-PROVABLE` with that failed control recorded, which is what §2.2 tells a writer to
do and which the verifier is merely reproducing.

### Fixed — a class-4 `[DG#n]` is no longer left standing on a citation that moved

`/prd-ground` Phase 7 sweeps every class-4 finding whose cited `[CG#n]` this run rewrote and
re-derives the pair — **reading the `[DG#n]` set from `design-grounding.md` rather than from what the
run happens to hold**, since a `--no-design` run produces no `[DG#n]` at all while still rewriting
`[CG#n]`, and a sweep over held findings would report "none" on precisely the run that created the
staleness; Phase 8 supersedes class-4 findings alongside the `[CG#n]` that took them there
on a `--rebaseline` pass. Previously a design finding could keep a verifier outcome earned against a
version of its citation that no longer existed, with the id still resolving and the claim ids still
matching — undetectable by a reader who follows the citation.

### Added — a customer-answerable question from `/create-prd` reaches the customer

PRD authoring surfaces questions nothing before it could have — a scope boundary the requirement text
never drew, a rule the acceptance criteria need and nobody stated. Some are settled only by an
authority the customer holds, and they landed in `prd.md` under `## Assumptions & open questions`,
**which the customer never receives**. Every later reader then met them as flat statements in a
document full of grounded ones: read as settled, built on, argued from, while the one party who could
have corrected them in a sentence never saw the file.

The route back already existed and nothing pointed at it. `/create-prd` now triages each surviving
gap by `interview-tagging.md` §2's test — what kind of thing would settle it — and writes a
customer-authority one as an `[AS#n]` in `decisions.md`, which `/brd-package` surfaces (every open
one, twice) and `/brd-reconcile` supersedes with the answering `[CD#n]`. **Writing the record is not
asking the customer anything**: no `[CD#n]` is written here and none may be (D14).

`decision-register-format.md` §7 gains the second writer and the rule that **such a record omits
`round` entirely**. Giving it the last closed round's number would claim it was in front of whoever
answered that round; giving it any invented value is worse, because `round` is **read**, not just
displayed — `/brd-package` derives the set of rounds a BRD has from the distinct `round` values across
its records and then requires an `interview/round-<N>.md` for each, so a value no round record answers
to would make the slice permanently unpackageable. That command's derivation now says explicitly that
a record carrying no round contributes nothing to the set, and that this is the only reason a record
legitimately omits the field.

`/create-prd`'s third write guarantee is narrowed and the narrowing itemised rather than made
silently: it protected a settled decision from a grill answer, and it now says so over *existing*
records, leaving this command free to create one new `[AS#n]` — never a `[VD#n]`, never a `[CD#n]`.
Ids continue from the highest on file and a gap already recorded is not recorded twice, because the
sanctioned fresh-PRD re-run puts this command over the same folder more than once.

### Added — two checks over what a run restates, both measured against real packages

Two verification gaps the register named, built the second time against assembled artifacts rather
than against the format documents. The first attempt at each was cut before release because both
fired on **correct** trees; every relation below is narrower than its obvious form, and each
narrowing is a false positive somebody would otherwise have met.

**`/brd-package` — `bundle-packaging.md` §7, `set-resolution`.** The manifest, the delivery note and
several prompt parts each restate a set held elsewhere, and nothing compared them. Three relations,
comparing **membership** and never counts, since two sets of the same size with different members
read as correct. What measurement changed: the review-scope part renders its source as **prose in the
customer's vocabulary**, not identifiers, so relation 1 reaches only the parts that genuinely
enumerate identified records; a manifest writes filenames **with or without the extension** — both
conventions occur across builds of one package — and names images only sometimes, so relation 2
matches basenames tolerantly, excludes the manifest and non-review markers, and leaves images to §6;
and a delivery note **abbreviates** its commits, so relation 3 tests each as a **prefix** of a
baseline rather than for equality. An equality test reports every correct note as carrying no pin.

**`/brd-intake` — `brd-format.md` §2.2.** A section the read skipped and one that genuinely holds no
obligation look identical in an inventory. Two relations, both derived from the `source_anchor`
column already written: every anchor resolves to a section the source has, and every **top-level
section** either holds a row or is accounted for. **No agent returns a new field and nothing new is
stored** — the first design added one and persisted it nowhere. Granularity is the finding: real BRDs
carry fifty to sixty headings under about fifteen top-level sections, nine of which legitimately hold
nothing, so the operator answers nine questions rather than fifty. On a real package the sections
holding no row included the user stories and the acceptance tests — the pair worth asking about.

### Added — a recorded review verdict names the version it was taken against

`/create-prd`, `/update-prd`, `/create-ard`, `/specify`, `/epics`, `/prd-proposal` and
`/brd-proposal` cite the new `workflows-core:escalation-rules` rule. The one-fix-cycle cap assumes a
fix only removes defects; three live runs saw the fix introduce something the re-review then found
with the budget already spent, leaving a `PASS` on record beside a file the `PASS` never saw.

## [3.4.0] — 2026-09-08

### Added — two effort-proposal commands, and the format they author against

- **`/prd-proposal <ADDRESS>`** authors a customer-facing effort proposal for one `PRD-` folder — an
  idea-route PRD or a BRD-route slice, since a `PRD-` folder is a `PRD-` folder either way. It writes
  `proposal.md` and, at tier 2 and above, `proposal-brief.md`: work packages clustered by delivery
  seam, hours by package and role, and a range whose width is computed bottom-up from per-package
  confidence. Flags: `--no-brief`, `--profile`, `--baseline <path>`, `--redo`.
- **`/brd-proposal <ADDRESS>`** rolls a `BRD-` container's slice proposals into one programme
  umbrella — one row per included slice, read out of that slice's own proposal and never re-derived;
  the cross-slice effort that exists in no slice; and a coverage statement computed from the root
  `coverage-ledger.md`, with the remainder enumerated by identifier. Its natural altitude is the
  root, which inverts the rest of the BRD route: it refuses a slice, where `/prd-ground`,
  `/brd-interview`, `/brd-package` and `/brd-reconcile` each refuse a root. The roll-up is not a sum:
  each of its three adjustments — umbrella effort, de-duplication where two slices cite the same
  finding identifier, and peak concurrency rather than summed FTE — is named in the document rather
  than absorbed into a total.
- **`references/proposal-format.md`** is the authority both commands author against and
  `proposal-reviewer` checks: the section sets of both artifacts, the `[WP#n]` work-package and
  `[ED#n]` estimate-driver namespaces, the readiness tiers, the confidence grades and their default
  bands, the closed evidence set, and — in §14 — what the umbrella adds over a slice's own proposal.
  `docs/reference/proposal-format.md` documents the same subsystem from the reader's side.

### Added — readiness is graded, never gated

- **Neither command requires an ARD or a specification.** What those artifacts change is the
  **readiness tier** printed in the header beside the date — 1 · Indicative (the PRD alone),
  2 · Grounded (verified grounding plus a settled decision register), 3 · Architected (`ard.md`),
  4 · Specified (`specification.md`). Grading replaces the gate that would otherwise have stood here,
  which is the whole answer to when a requirement set becomes estimable. The only hard refusal on
  readiness is each command's own `require-on-main` gate on its input.
- **The tier is a ceiling on confidence and never sets it.** Tier 1 caps every package at Low, tier 2
  at Medium, tier 4 at High, and tier 3 at High only for a package an `[AD#n]` covers; confidence
  itself is computed from the evidence each package actually has, and evidence can only push a
  package lower. A tier-1 proposal is still a real document — it simply says outright that its cost
  drivers are not known.

### Added — what a cost driver may cite, and what happens when it cites nothing

- **A closed set of three evidence classes**, each resolving to something on disk an independent
  reader can open: a verified grounding finding (`[CG#n]`/`[DG#n]`, carrying its verifier outcome,
  cited with the `file:line` the finding records), a frozen decision (`[VD#n]`/`[CD#n]`), or a
  confirmed code defect (`[CDF#n]`). The narrowing is per driver: one making a claim about the code
  cites the first class, because a decision cannot evidence a statement about a repository.
- **A driver citing nothing from that set does not render at all**, and the run names every candidate
  it dropped. The value of the rule is that it is mechanically checkable rather than a matter of
  authorial care.

### Added — the defect-remediation package, and why it is never a lever

- **Where the folder records an unrepaired code defect, its repair becomes its own `[WP#n]`
  automatically**, swept from three sources and unioned: every `[CDF#n]` in `code-defect-log.md`
  whose disposition is `open`, `in-scope` or `conditional` (no confirmation needed — a standing entry
  is one somebody already adjudicated), a verified grounding finding whose own text records a defect,
  and an `[SR#n]` self-review finding in the packaged bundle dispositioned `accepted-risk` or
  `escalated-to-customer`. The last two need operator confirmation, because neither is a defect
  *register*.
- **The first source names the three dispositions it admits rather than filtering on *unresolved*.**
  That vocabulary has no value meaning resolved, so a filter phrased that way would admit all five.
  The two it leaves out are left out for opposite reasons: `withdrawn` means the intent basis was
  wrong and there was never a defect, while `out-of-scope` **is** unrepaired and is excluded anyway,
  because its repair is recorded and deliberately not this engagement's work — neither may be priced
  into a mandatory scope the customer is then forbidden to decline.
- **It never renders into the scope-lever or priced-options table.** Where it cannot fit the delivery
  window, that is disclosed as a schedule fact. Asking a customer to authorise deferring a defect the
  vendor's own work found would return that deferral carrying the customer's authority on a question
  the vendor's policy has already answered.

### Added — the rationale brief, and why it is withheld below tier 2

- **`proposal-brief.md` is derived from the same resolved data set as the proposal**, never
  re-authored from it, and its spine is the driver argument: the naive baseline, why the number is
  not that, and what the largest share of the estimate is owed to. It also carries the corrections
  this revision owes the customer, each deliberately-unpriced item with the gate that will price it,
  and what is needed before week 1.
- **It does not render below tier 2 irrespective of `--no-brief`**, because below tier 2 that spine
  does not exist. A two-page pre-read explaining why a number is large, written when the reasons are
  unknown, is the one artifact this format must not produce. The final report says which of the two
  reasons applied.

### Added — `proposal-reviewer`, the review gate both commands dispatch

- Opus-pinned by frontmatter with no override, dispatched with both artifact paths, the profile, the
  resolved tier and the anchor revision where one exists. Its findings are triaged by the
  orchestrator before anything is edited, per `workflows-core:finding-triage`.
- **Its arithmetic check is the reason the agent exists**, and it is the one check that cannot be
  delegated to judgement: it re-adds every column and every row of the `[WP#n]` × role grid — the
  Expected column first, then Low and High **independently**, because the Low and High columns each
  summing to the stated range is the relation a reader is least likely to re-add and exactly where a
  silent error survives — checks that every package's range brackets its own expected figure, and
  compares every band against its confidence grade's default within the one-percentage-point
  tolerance whole-hour rounding needs.

### Added — the no-money rule

- **Neither artifact carries money at all** — no rate, no currency symbol, no monetary total for
  human hours, at any tier and under any flag. Rates are contractual and belong in a document this
  pipeline does not produce, and a git-committed rate card is a disclosure waiting to happen. Note
  the collision this rule exists to prevent: `workflows-core:cost-emission` already records a
  quantity called **cost**, and it is USD of model spend for a run. The two are unrelated, and no
  sentence in either artifact lets a reader take one for the other.

### Added — what these commands do *not* do, stated because it is the property most likely to be misread

- **Neither command gates anything downstream, and nothing on the build ladder waits on either.** No
  command of that ladder reads a proposal: `/create-ard`, `/specify`, `/epics`, and the `dev-workflows`
  commands below them each resolve the same folder and neither know nor care whether it holds one.
  No readiness tier withholds permission to begin work. **The one reader is the sibling umbrella
  `/brd-proposal`**, which runs `require-on-main` on each included slice's `proposal.md` in order to
  roll it up — a gate that stays inside the pair, on a second proposal rather than a phase of the
  build. *(Corrected in 3.8.0: `/brd-proposal` is the one command that reads **another folder's**
  proposal as a proposal — `/brd-reconcile`'s stale cross-reference sweep reads one only as prose;
  a later run of the producing command reads its own folder's as a re-estimate anchor,
  and `proposal-reviewer` reads it inside the run that wrote it.)*
- **Neither resolves documentation grounding**, and neither takes `--no-docs`. An estimate's inputs
  are the specs tree and the profile; a documentation page bears on how a feature is described rather
  than on what it costs to build. There is no flag to turn off and no `docs grounding:` line in
  either report.
- **Neither opens a code repository.** Every commit a grounding finding cites was pinned by
  `/prd-ground`, and these commands read the finding rather than the repository.

### Changed — a `/brd-*` glob that stopped meaning "the BRD-to-PRD route"

`/brd-proposal` matches `/brd-*` without being a phase of that route, so every claim written as a
glob over the family had to be re-read against it. `coverage-ledger-format.md` §6 said *"every
`/brd-*` command's final report ends with"* the ledger line; `/brd-proposal` prints no such line —
it reads a container's ledger only to compute the coverage statement inside the document it writes.
§6 now states the route as the test and records why the glob is not, and the three pages that cited
it (`docs/commands/brd-intake.md`, `docs/commands/prd-ground.md`, `docs/roles-and-phases.md`) follow
it. `commands/create-prd.md`'s refusal cites the same convention by route rather than by glob, and
`commands/brd-intake.md`'s opening no longer describes what "the `/brd-*` commands between them"
do. The prefix claims are untouched and stay glob-shaped: `brd` genuinely is the branch prefix every
`/brd-*` command shares, `/brd-proposal` included.

## [3.3.3] — 2026-09-08

### Fixed

- **`/brd-reconcile` reaches a dependent BRD's `code-defect-log.md`.** A `[CDF#n]` declares its
  dependency by a field — `blocked_on: <BRD-KEY>/<decision-id>` — exactly as a decision does with
  `conditional_on`, but the propagation sweep walks decisions and `[AS#n]` only, and the stale
  cross-reference sweep stopped at the parent's folder. Inside the parent the log was always swept;
  outside it, a defect blocked on a decision that had just moved was reached by neither. The stale
  sweep's **first** search — the literal-id one — now also runs over the propagation sweep's
  already-resolved dependent set. No new traversal and no new outcome: a hit keeps `needs-a-human`,
  because every disposition on that log is the operator's.
- **The write surface deliberately did not move with it.** Outside the parent's folder the only
  outcomes are `still-true` and `needs-a-human`, never `updated`, and the second search — the prose
  one — does not follow at all: it cannot be reduced to a pattern and needs a reader who knows what
  the old position claimed. Two claims moved in the same change rather than being left to go stale:
  the sweep's opening now says the parent root bounds everything except search 1, and the guard's
  *"why this sweep's root being the parent's folder is safe"* became **writing root**.

## [3.3.2] — 2026-09-08

### Removed

- **`/create-prd` no longer captures `relevant_for_release_notes`**, and no longer asks to confirm a `no`.
  It asked a question with one answer — every PRD is relevant for release notes — so the only value it
  could carry that changed anything was one nobody should write.

## [3.3.1] — 2026-09-08

### Fixed

- **`/create-ard` and `/specify` no longer fall back to a key-globbed PRD filename at the `require-on-main`
  gate.** The fallback matched nothing on a current tree, and on the pre-rename tree it was written for it
  also matched that tree's ARD (`<KEY>_ARD.md`), so the PRD gate could gate the wrong artifact. Both now
  gate `prd.md`, the only name the plugin writes — correct on a current tree, and `absent` on a pre-rename
  one, which is what `workflows-core:addressing` §5 now tells that operator to fix. *(Corrected in
  3.8.0: `prd.md` is the only canonical PRD name the plugin writes — `/update-prd` also writes
  archived snapshots under `revisions/`, never read as a PRD.)*
- **`/create-prd` step 6 no longer claims a pre-rename `<KEY>_<slug>.md` is identified by `kind: prd`.**
  That field arrived in the same change as the rename, so no pre-rename file can carry it and the branch
  could never recognise the file it had found.

## [3.3.0] — 2026-09-08

### Added — `code-defect-log.md`, a route-local register for what `/prd-ground` finds broken in the code

`/prd-ground` spends its whole effort reading code at pinned commits, and routinely establishes that the code is broken — an active regression, a missing index the code assumes, a write path that never sets a column — and the route had nowhere to put that. `references/brd-format.md` §4's `[DEF#n]` log is for **requirement** defects only, so the fact landed in a decision's `argumentation`, and two instances in one shipped register asserted a defect *"is recorded"* while nothing held one, surviving drafting, the round record, and a first adversarial review.

The new `references/code-defect-log-format.md` fixes the `[CDF#n]` record: `id`, `statement`, `behaviour` (exactly one **verified** `[CG#n]` in this BRD's own `grounding/code-grounding.md`), `intent`, `intent_basis` (a `file:line`/document pointer, or the literal `operator-judgment` followed by the reasoning), `disposition`, and `blocked_on` (required only when `disposition: conditional`). Five dispositions, mirroring `decision-register-format` §3: `open`, `in-scope`, `out-of-scope`, `conditional`, `withdrawn`. **There is no `fixed` disposition** — nothing on the BRD-to-PRD route builds anything and no command can observe a repair; `withdrawn` means the intent basis turned out to be wrong, not that the bug got fixed, and using it for a repaired defect would put a false statement in the log.

`/brd-interview` is the **only** writer, at two points that already exist — Phase 6 raises an entry, held for the register phase exactly as a `[VD#n]` is; Phase 9 writes `code-defect-log.md` alongside `decisions.md` and the round record; Phase 10 adds the path to `deliverable_paths`. `/prd-ground`, which does the actual code reading, is deliberately **not** a writer, for a cost reason rather than a preference: its Phase 7 verifies every finding through `grounding-verifier`, and a defect entry emitted there would need its own verification contract and a change to the `code-grounder` agent; an entry raised at interview time cites a finding that is already verified and adds only the intent basis, which is the operator's judgment either way.

### Added — `defects:`, a twelfth field on the decision register

`decision-register-format.md` §1 gains `defects:`, listing the `[CDF#n]` ids a `[VD#n]`, `[CD#n]` or `[AS#n]` turns on, omitted when absent. It is deliberately **not** `evidence`: §6's will-change rule inspects the `evidence` list alone, firing when every finding in it carries `horizon: will-change`, and a `[CDF#n]` mixed into that list would silently change what the rule fires on. §7's per-field accounting grows from eleven rows to twelve to match, so a twelfth field is never one an author has to settle for themselves.

### Added — the log ships in the customer bundle

`bundle-packaging.md` §1.1's allow-list gains `code-defect-log.md`, and three dispositions map onto three prompt parts that already exist — `in-scope` to part 6 *Review scope*, because a defect disposed `in-scope` **is** the delivery boundary: the repair has to happen inside this PRD's scope or the feature cannot be delivered; `conditional` to part 8 *What could still move*, beside the `conditional_on` positions that part already carries; `out-of-scope` to part 11 *What this session cannot settle*. Shipping it also buys §3's evidence rule a mechanical check for free: `behaviour: [CG#12]` now sits in a bundle document, so it resolves against the partition's own shipped `grounding/code-grounding.md`, and an entry citing a finding that does not exist stops the packaging run with `BRD_PACKAGE_DEAD_CITATION`. Every entry ships, including `out-of-scope` and `withdrawn` ones — but seeing is not deciding: the operator settles every disposition exactly as before, a code defect never becomes an `interview-tagging.md` `[V]` or `[C]` question, and a customer who disagrees with one pushes back through the returned review, which `/brd-reconcile` already reads.

### Added — `brd-package-reviewer`'s sixth hunt class

Class 6, *"an argumentation that asserts a defect nothing holds"*: for every `[VD#n]`, `[CD#n]` and `[AS#n]`, the reviewer reads `argumentation` for a claim that a code defect is recorded, raised, or known, and checks the record's own `defects` list — a claim with nothing behind it is a finding. This is the class that catches the exact failure the design started from. It stands in for the prose-trigger check the design's §8 considered and **deliberately did not build**: the tree carries no corpus of real registers to measure a phrase-matching check against — the two `argumentation:` examples in the whole tree are both illustrative, both inside `decision-register-format.md` itself — so an unmeasurable pattern was left to the reviewer's judgment instead of shipped as a static check. What did ship at write time is a structural offer rather than a pattern: `/brd-interview` Phase 6 offers to raise a `[CDF#n]` exactly when a decision's `evidence` holds a `REWRITTEN`, `AMENDED` or `FALSE-FRIEND` finding — the three verdicts meaning grounding found the code does something other than claimed — reading the trigger off the record rather than out of prose.

## [3.2.0] — 2026-09-08

### Fixed — the package told every reviewer to extract an archive, including the ones who pull the repository

`bundle-packaging.md` §5 has always said the committed bundle serves **both** delivery routes — a customer with repository access pulls it, everyone else gets an archive command. Nothing recorded which route a given package was taking, so every other site assumed the archive — across the command, the reference and the documentation. Two of them were not statements about delivery at all but *justifications* that happened to name an attachment: rule 4's argument for filenames over paths, and the delivery note's 200-word ceiling. Both rules are right on either route; only their reasons were half-stated, and rule 4's mattered — naming only the archive case read as though a committed bundle could safely be addressed by path, which is the one reading that breaks rule 1 for the route now recommended.

The sharpest was in the customer's own prompt. Part 1's input table listed *the archive command* among what the prompt is filled from, and its OS note opened *"extract the archive to a real folder before pointing anything at it"* — so a customer who pulled the specs repository received a shell command for an archive nobody sent them, and an instruction naming a file they did not have, in the one document whose entire job is to be followable by somebody with no context and no plugin. That is the failure the de-Obsidianising pass exists to prevent — an instruction that looks actionable, is not, and gives the reader no way to tell which — reached by a different route.

**The prompt now names no delivery route at all, and the delivery note names the actual one.** The two have different readers, and that is what settles which may assume anything. The note is a covering letter to a named customer whose situation the operator knows; the prompt is handed on — to a colleague, to an agent, to whoever actually does the review — so a prompt that names a route is wrong for some of its readers about the first thing it tells them. The archive command is gone from the prompt entirely: assembling an archive is a delivery-team action, and a reviewer who was sent one has already had it done for them.

**The route is settled once, at the delivery note, and half of it is derived rather than asked.** The repository route exists only where the *Handoff* phase's consent choice was accepted — a bundle on no ref is a bundle nobody can pull — so where the handoff was declined the run takes the archive route without asking and says why. Where it was accepted the run asks, recommending the repository route. On that route no archive command is produced, and the note carries the repository, the committed `bundle-<YYYYMMDD>/` directory by path, and the instruction to open the prompt there and paste it.

**One constraint that survives the change and is worth stating, because it looks like an inconsistency:** even on the repository route the *prompt* must not name the specs-repo path. `bundle-packaging.md`'s own rule 1 holds that a path is correct exactly once, in the directory layout one machine had — so the note carries the path and the prompt carries filename search. The note tells the reviewer where to stand; the prompt works once they are standing there.

The Final report now names the route and why, on both branches, so a reader cannot mistake an absent archive command for a step that failed.

## [3.1.0] — 2026-09-08

### Added — `bundle-packaging.md` §6, a citation-resolution check over the assembled bundle

The plugin-free scan (§1) deliberately exempts identifiers — `[BR#n]`, `[CG#n]`, `[DG#n]`,
`[VD#n]`, `[AS#n]` and `[SR#n]` are how a returned review cites the package's own claims without
minting identifiers of its own — but nothing then checked that they land. `/brd-package` Phase 8
now runs a second pass over every document in the finished bundle, testing three relations: every
identifier reference resolves inside its own source package's corpus for its class, unless it
carries the owning BRD key at the point of use — in the prose form `<BRD-KEY> [CG#7]`, or inside a
structured field whose format another authority fixes and which that authority defines to name
another BRD's record, such as the register's own `conditional_on: <BRD-KEY>/<decision-id>`; those
fields are derived from the authorities that own them, not listed in §6, and the check reads them
rather than refusing them; a class-4 `[DG#n]`'s `cites` resolves within the
same partition and names the same requirement as the citing finding's own `claim` (the correctness
half of `workflows-core:grounding-format` §6.3's rule, added there in 1.3.2); and a bare
`<name>.md` token names a document actually present in the bundle.

Two exemptions, both principled rather than convenient. `[SR#n]` is exempt entirely — the
self-review file it would resolve against is excluded from the bundle by rule, and the `[SR#n]`
content a customer may see reaches them filtered through the prompt, never through the file
itself, so without this exemption the check would fire on every package the command ever builds.
And a hit inside the customer's own source document reports rather than stops, for the identical
reason the plugin-free scan already treats that file that way: it is copied byte for byte and
immutable by rule, so a hard stop would make that BRD permanently unpackageable.

Three stops: `BRD_PACKAGE_DEAD_CITATION` for a reference that resolves to nothing;
`BRD_PACKAGE_CITATION_MISMATCH` for one that resolves, but to a finding about the wrong
requirement; and `BRD_PACKAGE_CORPUS_UNREADABLE` for a corpus file that holds record-shaped
content and still parses to zero ids of its class, so a parse failure is never reported as an
absence. A corpus holding no record-shaped content is a legitimately **empty** corpus and passes —
a `design-grounding.md` written as a short note because design grounding was skipped, and a defect
log whose walk confirmed nothing, are both ordinary and neither is a parse failure.

**The honest consequence: relation 2 will refuse bundles that ship today.** A parent BRD's
verified findings, hand-narrowed onto a slice, is common enough that the first run against an
existing slice may stop on a mismatched `[CG#n]` citation. The repair is by hand, because the
plugin has no supported mechanism for narrowing a parent's findings to a slice's claimed subset —
that gap is a separate, already-tracked item, and this check catches a broken citation regardless
of how it got there, which is the point of checking at delivery rather than at authoring.

**One limit the design accepted.** An unkeyed bundle document — one that reached the bundle
without the `<BRD-KEY>`-carrying filename `commands/brd-package.md` rule 1 requires — is reported
rather than guessed at: §6 has no partition to place it in, so it names the document and stops
with `BRD_PACKAGE_DEAD_CITATION` rather than assigning it to a corpus by inference.

This is a minor bump, not a patch: the check can refuse a bundle a 3.0.0 run would have shipped,
which is a behaviour change a user will meet.

## [3.0.0] — 2026-09-08

### Changed (breaking) — `/brd-ground` renamed to `/prd-ground`

Every fully-qualified invocation, docs page, and cross-plugin citation must use
`/product-workflows:prd-ground`; `/brd-ground` no longer exists. The rename is taken now because
nothing has published — `product-workflows` does not exist on `origin/main` at all — so it costs
installed users nothing extra, and after a release it would have been a breaking change against a
name people had learned. It is called out as breaking anyway: a removed command name is breaking
however unpublished the plugin is, and the version is the one place a reader looks to find out.

**The rule that bounds it, so a future reader does not rename three more commands on the slice
argument.** `brd-` names the **route**, not the folder kind. Four of the six route commands refuse a
root — `/prd-ground` (`PRD_GROUND_ROOT_LEVEL`), `/brd-interview`, `/brd-package` and
`/brd-reconcile`, each with its own `*_ROOT_LEVEL` stop — so "runs on a slice" is the wrong test for
which one renames: `/brd-intake` and `/brd-split` are route commands that genuinely run at root, and
interviewing, packaging and reconciling exist only because a customer handed over a BRD — the slice
they run on is a slice *of* one, and none of them will ever run anywhere else. `/prd-ground` is the
only one of the six that **leaves the route**: after this release it runs on an idea-route PRD folder
with no BRD anywhere in its ancestry, where `brd-` was not merely imprecise but false. That is what
earns it the new name, and nothing else in the six-command route is touched — `code-grounder`,
`design-grounder`, `grounding-verifier`, `grounding/`, `code-grounding.md`, `design-grounding.md`,
`baselines.md`, `brd-link.md`, `coverage-ledger.md`, `slices.md`, `brd/` and `brd-reader` all keep
their names, the first six because they were already route-neutral and the rest because they *are*
BRD-route artifacts.

### Added — `/prd-ground` now serves the idea route too, optionally and ungated

Idea-route grounding runs after `/create-prd`, once merged, on the same PRD folder — never on a
root, and never on a resolved `EPIC-` folder (`PRD_GROUND_EPIC_LEVEL`, new). The route is detected
from the resolved folder, never declared: a `PRD-` folder carrying `brd-link.md` is still the BRD
route; a `PRD-` folder without one is the idea route. The claim list is built from the PRD's own
`[AC#n]` and `[FR#n]` rows, plus a `[US#n]` whose story carries neither — `[UC#n]`, `[SM#n]` and
`[SMC#n]` are excluded, and the run reports the count and the excluded prefixes both before the repo
prompt and in the Final report, so a clean run is never read as a fully-ground PRD. A PRD with no
resulting claim stops with `PRD_GROUND_NO_CLAIMS`, naming `/update-prd` as the fix and never
`/create-prd`, which would rewrite the PRD rather than add acceptance criteria to it. `--depends-on`
is refused outright on this route (`PRD_GROUND_NO_PREREQUISITES`): a `will-change` horizon needs a
decision register to freeze a prerequisite's decision in, and the idea route has none, so every
finding on it is `current`. Five new stops altogether: `PRD_GROUND_EPIC_LEVEL`,
`PRD_GROUND_NEEDS_PRD`, `PRD_GROUND_PRD_NOT_HANDED_OFF`, `PRD_GROUND_NO_CLAIMS`, and
`PRD_GROUND_NO_PREREQUISITES`. The branch prefix on this route is `prd/`, shared with `/create-prd`
and `/update-prd`; the next-step offer names `/create-ard` and `/specify`, with `/update-prd` named
first, marked `(Recommended)`, wherever a claim came back `SUPPORTED`. Grounding stays optional here
and nothing gates on it — the run's own Final report says outright when every claim came back a
verified absence, so a PRD that is greenfield against the resolved repositories reads as one finding
rather than a wall of absences, and a second run over the same folder is exactly what that headline
exists to make unnecessary.

### Added — design grounding ships on the idea route in the same release

A class 1, 2 or 3 `[DG#n]` is settled from the frame set and the requirement text alone; a class 4
cites a `[CG#n]` and inherits its commit. The frames are `/idea`'s own source images, vendored into
`design/idea-sources/` with their mandatory index — a class-1 finding here reads *this mockup shows a
screen no `[AC#n]` asks for*, a reconciliation available before `/create-ard` and on no other route.
A folder with no `design/` at all is the common case, and `grounding/design-grounding.md` is still
written on every run, carrying the `## Frame sets covered` census — absent always means the file is
not there, never that the pass was declined.

### Changed — `/create-ard` and `/specify` read grounding wherever the resolved folder holds it, and seed their scans from it

Both commands already knew how to read `grounding/code-grounding.md` and `grounding/design-grounding.md`
and to stamp `consumed_by` back onto what they drew on — that reading was gated on `brd-link.md`
being present. The gate is gone: wherever the resolved folder holds either file, on either route, its
findings are read, stamped `consumed_by: ARD` or `consumed_by: specification`, and — new in this
release — used to **seed** each command's own theme extraction before it falls back to the
PRD/Epic-derived themes it always used. A `[CG#n]`/`[DG#n]` whose verdict says a capability is absent
is a theme worth scanning; one whose verdict says it is present names the code that already
implements it, directing the scan at it instead of searching blind. Neither command's own
`code-scanner` fan-out is replaced, made conditional, or put behind a flag — the two answer different
questions, and a folder with no grounding derives its themes exactly as before this release.

### Added — `/update-prd` reads grounding and gives `consumed_by: PRD` its first writer onto a grounding finding

`/update-prd` now discovers `grounding/code-grounding.md` and `grounding/design-grounding.md` in the
resolved folder (all optional, read-only, never gating — the same posture as its existing `ard.md`
and `specification.md` reads) and carries their findings into the grill with the same **grill-rank**
consumption the documentation digest already uses. Where it draws on a finding to change the PRD, it
sets `consumed_by: PRD` on that finding — the same write `/create-ard` and `/specify` already make at
their own altitudes. This is the **first time `PRD` has been written onto a grounding finding
record**: `/create-prd` already writes `consumed_by: PRD`, but only onto a `decisions.md` decision
record on the BRD route, never inside a grounding file, since it reads no `grounding/` file on either
route.

## [2.2.0] — 2026-09-07

### Fixed — `/brd-ground` never checked a verifier's outcome against the verdict it re-derived

Phase 7 now reconciles the two before acting on either. `grounding-verifier` returns `own_verdict` on every outcome, and an `agree` or `extend` carrying a verdict that differs from the finding's is a return contradicting itself — the outcome is normalised to `contradict`, the finding is rewritten to the re-derivation as that branch already does, and the normalisation is recorded and reported in the Final report's verifier tally, with an explicit "none" where nothing was normalised so a clean run reads as checked rather than as unchecked. `unprovable` is never normalised.

### Fixed — Phase 8 could write the verifier's return fields into the finding record

Phase 8 now writes §2's fields plus `outcome` and `notes` **and nothing else**, per `workflows-core:grounding-format` §2.1's newly-closed field set. `own_verdict`, `own_evidence` and the verifier's re-derivation `commit` are return fields Phase 7 has already acted on; a block carrying `own_verdict` beside `verdict` states two verdicts at once and every downstream reader is free to quote whichever half suits.

### Added — `/brd-split` and `/brd-interview` refuse a malformed finding block

A fourth test in `/brd-split` Phase 0 step 7 (`BRD_SPLIT_MALFORMED_FINDING`) and a third in `/brd-interview` Phase 0 step 7 (`BRD_INTERVIEW_MALFORMED_FINDING`): every `[CG#n]`/`[DG#n]` block's keys are tested against the closed field set, and any other key stops the run naming the finding, the key, and the hand repair. **The existing outcome test cannot see this** — such a block carries an `outcome`, so it passes on presence while the disagreement travels into a slice's allocation, or into a `[VD#n]` frozen against whichever half the run read and then put in front of a customer. That is the same "a relation testing a property of what exists cannot catch what should not exist" shape as BRD-1, and the reason this is a relation of its own rather than a stricter count.

The stop names the hand edit rather than `--rebaseline`, which would re-derive an entire verified corpus to delete a line no command should have written. `/brd-package` gains nothing: it has no findings gate at all, and a `--rebaseline` that moves the findings forces a new interview round through the check above.

## [2.1.0] — 2026-09-07

### Added — the sibling re-cut: a slice may hand a row it has refused to build to a sibling

Slice-first grounding carves a slice before it is ground, so *"this slice is larger than one deliverable"* is a normal discovery. The route's only answer was deferral: the slice's own walk sent the rows it would not build to `deferred-to: <itself>`, where they stayed its live obligation. That was enough for *build less now* and not enough for *two independently deliverable slices*, because the blocker sat on the **parent** — its row for the delegated requirement read `covered-by: <that slice>`, `/brd-split` walked only `unallocated` rows, and no command returned one row to `unallocated` — only a `/brd-intake` re-run, which reopened every row at once. A future sibling could never claim it.

`/brd-split` on a fully-allocated **root** carrying an `<instruction>` now performs the re-cut. Phase 0 step 9a builds a **re-cut candidate set** — every `[BR#n]` whose row on this ledger reads `covered-by: <A>` while A's own ledger reads `deferred-to: <A>`, two ledgers already agreeing that nobody is building it — and computes the eligible receivers; step 10 tests that set **before** the no-op, so a run that can move a row is no longer swallowed by the no-op it otherwise looks identical to. Phase 1.5 reads the instruction over that set instead of over an empty unallocated one, Phase 2 proposes groups and **fixes a receiver per group**, and Phase 4's new **Step 2R** offers each move one row at a time — showing the donor, both dispositions quoted from the two ledgers, the receiver, and every decision in the donor's register whose evidence touches the row. Accepting writes `covered-by: <B-KEY>` on the parent's row and then on the donor's, in that order. Phase 4's bulk offer gained a third firing condition for the uniform case; Step 3's reconcile widened its input set so the donor's claim and copied inventory row are withdrawn and the receiver's three files are written; Phase 4.5 now recomputes emptiness *after* the walk and repairs any `covered-by` key left pointing at a folder it removes; and `slices.md` gained a re-cut block and a removal block, because the ledger records neither.

**The precondition is the design, not a guard on it.** `deferred-to: <A>` is A stating in its own ledger that it is not building this, so the parent re-points against a refusal the row's owner wrote down and never over a live commitment. A row A still intends to build reads `covered-here` and cannot be moved. The receiver must be a sibling under the same parent that has **not been interviewed** — no `decisions.md` holding a `[VD#n]` or `[CD#n]`, no `interview/round-*.md` — because a register that exists and holds decisions is closed to added scope; a slice the run carves itself qualifies by construction. Nothing travels with the row: the donor's findings and decisions stay where they are and are never edited, and the receiver re-derives against the same pins, since a finding carried in from an earlier run is unverified by definition.

**What this does not relax.** No row returns to `unallocated` — every write replaces one terminal disposition with another — so the allocation gate is never reopened, and a receiver removed later takes the row to `deferred-to: <PARENT-KEY>` rather than back to the donor or back to `unallocated`. The one-level nesting cap is untouched: a re-cut carves a **sibling**, never a child. What relaxes is only the weaker rule that `/brd-split` never re-allocates a row already carrying a fate, and it relaxes against the owner's own recorded refusal and against nothing else. `references/coverage-ledger-format.md` §3.2 is the authority for all of it.

**Minor version, not a major one.** Nothing that worked before stops working. There is no new flag and no new argument: the re-cut reuses the `<instruction>` the route already made mandatory for carving a root, on the one run where it was otherwise free — and on that exact run the previous behaviour was to parse it, discard it and report it unused, naming the path that swallowed it. A bare `/brd-split <PARENT-KEY>` on a fully-allocated parent is still the same no-op; the ordinary walk, the `allocate-only` walk, the four resolutions at each level and every existing stop are unchanged. The one behaviour a user could have depended on — that an instruction there did nothing — is one nobody could have depended on for anything.

**A stop was designed for this and then retired, which a reader tracking the design will look for.** `BRD_SPLIT_RECUT_NO_RECEIVER` was to fire where a non-empty candidate set met no receiver. It does not exist and no code path reaches it: Phase 3 can always key a new slice, so the state in which nothing could *ever* receive a row is unreachable — and the state that *is* reachable, every proposed target declined with no eligible child standing, is an operator's answer rather than an error. This route already refuses to call one a failure. Step 2R simply has nothing to offer, every candidate is reported left with its donor, and the run finishes normally.

## [2.0.0] — 2026-09-07

### Changed — grounding, the interview, packaging and reconciliation move to the slice; a root now refuses all four

`/brd-ground`, `/brd-interview`, `/brd-package` and `/brd-reconcile` used to accept a BRD key at either level. They now refuse a resolved root outright, in Phase 0 before any other gate — `BRD_GROUND_ROOT_LEVEL`, `BRD_INTERVIEW_ROOT_LEVEL`, `BRD_PACKAGE_ROOT_LEVEL`, `BRD_RECONCILE_ROOT_LEVEL` — each naming `/brd-split <BRD-KEY> "<how to cut it>"` as the remedy.

**This breaks the route for anyone running it at root level.** The six `/brd-*` commands carried this two-level model since they shipped in `dev-workflows` at `v3.24.1`, an ancestor of this release, so the model has been in users' hands the whole time the route has existed; a run that worked against a root in 1.1.0 now stops at the first gate. The design's decision was to refuse and detect rather than offer a compatibility path: where root-level grounding, decisions, interview records, package artifacts or a reconciliation record already exist from the earlier model, the refusal names their exact paths and says plainly that they are **left in place and read by nothing** — nothing migrates them and nothing deletes them.

**Major version, not another minor one.** `product-workflows` 1.0.0 and 1.1.0 both shipped this route as an installable plugin someone could pin against, unlike an internal step of a multi-part split — so unlike a case where no intermediate version was ever separately consumed, a run that worked against a root in either of those versions is exactly what this stops. Semver's own contract is that an incompatible change is a major version, not a minor one carrying a bold warning in its own prose; `2.0.0` is the number that tells an installer what this entry would otherwise have to say by hand.

Twelve documentation files, including both of the route's mermaid diagrams, were brought into line with the same fact, and the level-conditional branches only a refused root could still reach — "at either level," offers that fanned a root out into its slices, a next-step offer recommending a command that would now refuse the key it was handed — were removed rather than left as dead prose a future reader would have to re-verify is dead.

### Changed — `/brd-split` on a root now requires a slicing instruction

A root is never ground, so nothing exists yet to cluster candidate slices from. The `<instruction>` argument, previously optional at both levels, is now **mandatory on a root whose ledger still holds an `unallocated` row** — `BRD_SPLIT_NEEDS_INSTRUCTION` if absent — and stays optional on a slice, where it only seeds the allocation walk's per-row recommendation. The stop is taken late in Phase 0, once the ledger has been read, so a root run that proposes nothing still runs without one: that is the run three other stops on this route name as the way to resolve a child left standing while claiming nothing, and it carves no slice.

The route now runs intake → split (root, instruction required) → ground (slice) → split (the same slice, `allocate-only`) → interview → package → reconcile. `/brd-split` runs twice, with grounding sitting between the two runs rather than before the first. Route ordinals ("the second command...") were removed from all six command bodies and from the surrounding documentation, since counting a fixed position stopped meaning anything once a command occupies two different steps of its own route.

### Fixed — the PRD-eligibility test's consumer list undercounted itself by half

`coverage-ledger-format.md` §5.1's positive test for "this unprefixed folder is a BRD container" is the same test `/create-prd`, `/create-ard`, `/specify` and `/epics` already shared. The four newly-refusing `/brd-*` commands cite it too now, rather than each restating the rule inline — which is what keeps a fifth divergent copy from shipping the way a fourth once did. §5.1's own consumer list grew from four to eight to say so.

## [1.1.0] — 2026-09-06

### Fixed — three more gates that a count could satisfy with nothing

`/brd-reconcile` froze zero `[CD#n]`, swept nothing and reported success on a digest the reader had failed to parse: its three gates all count *undisposed* items, so an empty digest satisfied them vacuously. `BRD_RECONCILE_EMPTY_DIGEST` reads the verdict written in section 2, which the schema fixes to three values: `approved` beside three empty sets is a customer who agreed and is recorded as the approval it is; any other verdict, or a section 2 that is absent or gives none, is a review contradicting itself or a parse that failed.

`/brd-package` step 7's *"every question in every round"* is a universal, true over no rounds at all. The rounds a BRD has are now **derived from `decisions.md`'s `round` fields** rather than from the `interview/` listing, and each one is gated with `require-on-main`; any on no ref stop together with `BRD_PACKAGE_ROUNDS_NOT_ON_MAIN`. Deriving the set is what makes a partial merge visible — enumerating the directory finds the rounds that landed and never learns a third was owed, which is the case the gate exists for. Step 8 gained `BRD_PACKAGE_NOT_INTERVIEWED`: it had reported a BRD with no `[VD#n]` as **finished**, which is right for one that was interviewed and settled and wrong for one that never was.

`--docs <path>` was declared by `workflows-core:docs-grounding` for all nine of its consumers and parsed by `/idea` alone; the other eight took `--no-docs` and nothing else, so an operator whose documentation is not at `$DOCS_PATH` could only turn grounding off. Implemented in all eight, each stripping the flag and its value together before anything counts positional tokens.

**`/create-ard` and `/specify` needed a flag-parsing step before they could accept either flag**, and finding that out is the more useful half: neither had one, and both refuse a second positional token outright — so the `--no-docs` both of them have documented since they shipped would have tripped `CREATE_ARD_ONE_ADDRESS` / `SPECIFY_ONE_ADDRESS` and stopped the run. The flag was named in two Usage lines and parsed nowhere.

`/epics` gained the `--no-docs` / `--docs` parsing step it had never declared, and its "Provide manual fix notes" escalation option gained the resolution instruction it never had — every other option in that array had one.

### Added — `/brd-ground --no-code`, so a missing design pass can be added without re-deriving the code findings

`--no-code` is a run mode, not a step skip: `grounding/code-grounding.md` is read-only for the whole run, the run produces no `[CG#n]`, and every finding already on file keeps its verdict, evidence and verifier outcome — never renumbered, never re-verified, never rewritten. Repositories are still resolved and pinned, because a class-4 `[DG#n]` is pinned to the commit of the `[CG#n]` it cites.

It exists because there was no way to reach the state without it. A BRD found to have exported frame sets and no design grounding could only be repaired by a full re-run, which on the engagement that reported this would have put **278 verified findings** back through derivation. `--no-design` had existed since the route shipped; `--no-code` never had.

Documentation grounding and the derivation matrix are off under this mode — both are written into the file it holds read-only — and the run refuses it outright alongside `--no-design`, alongside `--rebaseline`, alongside an explicit `--derivation-matrix`, or against a BRD with no verified code grounding to build on.

### Added — `/brd-reconcile --sent`, so an out-of-band customer review can be reconciled at all

The reconcile gate required a `customer-review-prompt-<YYYYMMDD>.md` that `/brd-package` had built and handed off. A review answering a package authored by hand, or sent before the route existed, could therefore never become a `[CD#n]` by any route — and no re-run of `/brd-package` could produce the missing artifact, since it will not rewrite a dated bundle and a fresh one is a *different* document from the one the customer answered.

What that gate protects is that a quotation can be checked against a committed copy of the document it came from. `--sent <path>` supplies that copy from the other direction: the operator names what was actually sent, each path is copied verbatim into `customer-sent-<YYYYMMDD>/` and committed beside the review before anything reads either. The invariant holds; only its provenance changes, and the run records which of the two it worked from in the reconciliation record and the final report. It replaces the package gate and nothing else, and is refused where a handed-off package already exists.

### Fixed — `/brd-split` passed a BRD whose designs had never been ground

The gate counted findings carrying no verifier outcome, so **zero findings satisfied it vacuously**: a BRD with two indexed frame sets and no `grounding/design-grounding.md` at all sailed through, and its slices could reach build with their designs never reconciled. A count tests a property of the records that exist; what was wrong was the records that did not.

Two presence relations now run before the count, each failing when its own side comes up empty. A `code-grounding.md` on main recording no `[CG#n]` stops with `BRD_SPLIT_NO_FINDINGS`. A **design/** subdirectory covered by no entry in `design-grounding.md`'s new frame-set list stops with `BRD_SPLIT_DESIGN_NOT_GROUND`, which names `/brd-ground <KEY> --no-code` as the repair. A set the operator explicitly skipped with `--no-design` passes and is recorded in `slices.md` as a limit on what the split could check. The design test executes `require-on-main` against `design-grounding.md` rather than reading the worktree — `--no-code` hands that file off in its own commit, so the single-commit implication the code gate relies on does not reach it, and without the gate the repair this very stop recommends could satisfy it with an unmerged file. A BRD ground before this release has no frame-set census and is passed with a recorded note rather than sent into a re-derivation it does not need.

**`/brd-interview` carried the identical vacuous count and is fixed with it** — `BRD_INTERVIEW_NO_FINDINGS`. It matters more there than in `/brd-split`: with zero findings that command answers every `[G]` from an empty evidence set and freezes `[VD#n]` against nothing. It takes no design relation, deliberately — a frame set with no `[DG#n]` yields no question to answer wrongly, and `/brd-split` gates the same BRD before any slice reaches build. `/brd-ground` Phase 8 now writes that frame-set list — every subdirectory on disk, covered or not — which is what makes the relation checkable rather than inferred.

### Fixed — `/epics` dispatched `doc-fixer` without its plugin namespace

The **BLOCK** branch said "invoke `doc-fixer`" with no `subagent_type`. `doc-fixer` ships from `workflows-core`, so the unqualified name resolves to nothing. Every other dispatch in the file was already qualified; this branch was the one that carried its instruction in prose rather than in a dispatch block.

## [1.0.0] — 2026-09-06

### Added — the product-definition half of `dev-workflows`, extracted into its own plugin

Install it with `claude plugin install product-workflows@ihudak-plugins`. Updating the marketplace alone does **not** install it: `dev-workflows` does not declare it, because no `dev-workflows` run loads anything from it.

Twelve commands move here from `dev-workflows` 3.27.0, and each keeps its bare name once this plugin is installed — only the namespaced form changes, from `/dev-workflows:<command>` to `/product-workflows:<command>`:

- the idea → PRD → ARD → specification ladder: `/idea`, `/create-prd`, `/update-prd`, `/create-ard`, `/specify`
- the Epic breakdown: `/epics`
- the six-command BRD-to-PRD route: `/brd-intake`, `/brd-ground`, `/brd-split`, `/brd-interview`, `/brd-package`, `/brd-reconcile`

Twelve agents and nine reference files move with them. The partition is exact: no agent and no reference is shared with the commands that stayed, so every `${CLAUDE_PLUGIN_ROOT}` citation among the moved files points at a file that moved alongside it.

### Dependencies

Two, both declared, both host-resolved at install:

- **`workflows-core`** — the shared reference corpus every command here loads in its first phase. An unsatisfied dependency disables the plugin rather than letting it half-run, which is the intended behaviour: there is no degraded mode to fall back to.
- **`prose-style`** — `prose-style-checker` is `/epics`'s primary style checker, not a fallback. Declaring it removes the absent case entirely; the "skipped gracefully when not installed" branches that shipped while it was an optional companion are retired, because for `/epics` "skipped gracefully" meant no style check at all.

`dev-workflows` is **not** a dependency in either direction. This plugin's spine hands `specification.md` to `/dev-workflows:design`, but it installs and runs without it, against a specs tree someone else's engineering work will eventually fill in.

### Hooks

One `UserPromptSubmit` hook, which preloads `$SPECS_PATH` and `$REPOS_PATH` context for `/epics`. It matches both the bare and the plugin-qualified form; the family's three preload hooks accept only their own plugin's prefix and cover disjoint command sets, so no single prompt can match two.
