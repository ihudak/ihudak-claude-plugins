# Workflow overview

Every command shown here.

```mermaid
flowchart TD
    a["/alpha"] --> b["/alpha-two"]
```

The prose below the diagram is deliberately not where a command is allowed to appear alone.
