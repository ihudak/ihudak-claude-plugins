# 2026-08-12 — `/idea` code grounding + choice-gate polish — verification table

Task 13 closeout. No test framework — every "check" is a shell command run against the tree and its
actual output recorded verbatim. Repo root for all commands unless stated: `/workspace/ihudak-claude-plugins`,
cwd `plugins/dev-workflows/` where shown. Shell pipes (`|`) inside command cells are written as literal
words ("then") where table syntax would otherwise require escaping every pipe; every command was still
run verbatim as a real shell pipeline during verification.

46 requirement rows (`R1`–`R39` plus `R9a`/`R9b`/`R9c`, `R15a`, `R20a`/`R20b`, `R26a`), then the four
cross-cutting checks named in the task-13 brief.

## Requirement rows

| Req | Statement | Command | Expected | Actual | Verdict |
|---|---|---|---|---|---|
| R1 | `## When a choice list fires` section exists in `escalation-rules.md`, titled exactly that. | grep -c '^## When a choice list fires$' references/escalation-rules.md | 1 | 1 | PASS |
| R2 | The section closes with the same binding clause the other two plugin-wide rules carry. | grep -c 'This rule binds every command in the plugin' references/escalation-rules.md | 3 | 3 | PASS |
| R3 | The rule does not restate option wording/order/marker rules — only governs whether a list is shown. | grep -n 'This rule governs only' references/escalation-rules.md | text present, scoped to "whether a list is presented" only | line 65: "This rule governs only *whether* a list is presented. Option wording, option order, and the `(Recommended)` marker are governed by the two rules above and are untouched by it." | PASS |
| R4 | Phase 1 gains conditional List A (unresolved `issue_type`), verbatim options. | grep -A2 '^**A — the key resolved' commands/idea.md | choices: ["Read this as a vi — an existing Value Increment (Recommended)", "Read this as an rfe — product feedback", "Cancel", "Other… (describe)"] | present verbatim | PASS |
| R5 | Phase 1 gains new List B (path-like arg, no resolved file), `(Recommended)` on re-enter. | grep -A2 '^**B — the argument is path-like' commands/idea.md | choices: ["Re-enter the path (Recommended)", "Read the argument as a prompt — the literal text is the idea", "Cancel", "Other… (describe)"] | present verbatim | PASS |
| R6 | Every other case gets an inline confirmation (no `choices:`), citing the firing rule, `--as` parenthetical retained. | awk '/^## Phase 1/,/^## Phase 2 /' commands/idea.md then grep -c 'When a choice list fires' | 1 | 1 | PASS |
| R7 | Sweep the other 20 commands for the vacuous-confirmation shape (confirm/confirmation lead-in, no if/when/unless/on). | for f in commands/*.md; do grep -n -B4 'choices:' "$f" then grep -iE 'confirm\|confirmation' then grep -viE 'if\|when\|unless\|on <ident>' ; done | detector runs, hits recorded | 21 raw lines across 9 files (create-ard.md, design.md, docs-profile.md, document.md, epics.md, ready.md, specify.md, statusline.md, update-vi.md), resolving to 16 distinct gates (2 false positives, 14 real) | PASS |
| R8 | A `determined` hit is fixed (named condition or inline form); a `varies` hit is left alone, unannotated. | manual read of each of the 16 gates in docs/superpowers/plans/2026-08-12-choice-gate-sweep.md | 0 files changed if all 16 are varies/false-positive | all 14 real hits classified varies, 2 false positives (no attached choices: block); 0 files modified | PASS |
| R9 | If the sweep finds nothing beyond `/idea`, that is recorded as the honest outcome, not hidden. | grep -n 'sweep found nothing beyond' docs/superpowers/plans/2026-08-12-choice-gate-sweep.md | outcome statement present | "The sweep found nothing beyond `/idea`." present in the Outcome section | PASS |
| R9a | `## Repo missing (after resolution)` no longer byte-identical to `## Dirty working tree` above it. | grep -A1 '^## Repo missing (after resolution)$' references/escalation-rules.md then grep -c 'Stash changes' | 0 | 0 | PASS |
| R9b | Replacement list mirrors `Repo unresolved (zero matches)`, adds "Specify a different absolute path", no `(Recommended)`. | sed -n '/^## Repo missing (after resolution)/,/^## Dirty/p' references/escalation-rules.md | choices: ["Skip this repo", "I'll clone it — wait", "Specify a different absolute path for this repo", "Cancel", "Other… (describe)"], no (Recommended) | present verbatim, no (Recommended) marker | PASS |
| R9c | Six commands cite the rule by name after R16 (create-ard, document, epics, release-notes, specify, idea); /implement joins as a 7th in Task 8. | grep -rl 'Repo missing (after resolution)' commands/ then wc -l | 7 (6 + /implement) | 7 (create-ard.md, document.md, epics.md, release-notes.md, specify.md, idea.md, implement.md) | PASS |
| R10 | `/idea` accepts `--ground-code [<repo>[,<repo>…]]`; bare derives, with value scans exactly those. | grep -n 'ground-code [<repo>[,<repo>…]]' commands/idea.md | flag documented with bare/value split | Flags paragraph: "bare it derives the repo set, with a value it scans exactly those repos" | PASS |
| R11 | `--ground-code` (+ value) added to the Phase 1 flag-strip list. | awk '/^## Phase 1/,/^## Phase 2 /' commands/idea.md then grep -c 'ground-code' | 1 | 1 (in the strip-list parenthetical) | PASS |
| R12 | OFF branch prints at most one line, proceeds without waiting (inline confirmation, not a gate). | grep -n 're-run with --ground-code to verify it against the code' commands/idea.md | exactly one nudge line | line 169, single occurrence | PASS |
| R13 | Detection is tokenised, exact-match only, against git-repo basenames under $REPOS_PATH, excluding $DOCS_PATH/$SPECS_PATH/$VAULT_PATH. | grep -n 'Exact token match only' commands/idea.md | rule stated with the three exclusions | line 166: exact-token, git-repo-only, excludes $DOCS_PATH, $SPECS_PATH, $VAULT_PATH | PASS |
| R14 | No auto-trigger — detection alone never starts a scan. | grep -n 'no auto-trigger' commands/idea.md | statement present | "There is no auto-trigger: grounding is a fan-out across every confirmed repo plus a second seeded round, and starts only on the user's explicit flag." | PASS |
| R15 | Bare derivation follows /create-ard's pattern: cheap discovery, propose, one consolidated gate, fan out capped at 4. | sed -n '140,155p' commands/idea.md | discovery/propose/gate/fan-out-cap-4 all present | all four steps present; gate list matches design R15.3 verbatim; fan-out "batches of up to 4 concurrent agents per Agent message" | PASS |
| R15a | Empty proposal — the R15.3 gate is not shown; escalates via the existing "No repos derivable — /epics" rule instead. | grep -n 'Empty proposal — do not show that list' commands/idea.md | present, cites the no-repos-derivable rule | present, cites escalation-rules.md "No repos derivable — /epics" | PASS |
| R16 | A repo named on the flag/proposal that is not mounted uses the (fixed) Repo missing (after resolution) list — never invented, never dropped. | grep -n 'not mounted is handled by the' commands/idea.md | present | line 154: "a repo that is not mounted is handled by the `Repo missing (after resolution)` rule in the same file — never invented, never silently dropped" | PASS |
| R17 | A repo the user drops is named at Phase 5/Final report with its themes left unverified — never silently disappears. | grep -n 'carried to Phase 5 by name' commands/idea.md ; awk '/^## Phase 5/,/^### Context hygiene/' commands/idea.md then grep -c 'descoped or unmounted' | both present | Phase 2.6: "carried to Phase 5 by name, with the themes it would have grounded left unverified"; Phase 5 restates it | PASS |
| R18 | Code grounding is a new Phase 2.6, after 2.5, not folded into it; Phase 2.5 stays byte-identical. | grep -n '^## Phase' commands/idea.md ; git diff HEAD -- commands/idea.md then grep '^-' then grep -c 'dispatch-docs-grounder or dispatch-prior-art-finder or single response' | Phase 2.6 between 2.5 and 3; 0 deleted lines inside 2.5 | Phase list: …2, 2.5, 2.6, 3…; 0 deletions matching those markers | PASS |
| R19 | Phase 2.6 has exactly two branches: ON (gate → round 1 → conditional round 2) and OFF (R12/R13 detection). | grep -n 'Runs only when --ground-code was given, or OFF branch' commands/idea.md | both branch markers present | "Runs only when `--ground-code` was given; otherwise take the OFF branch…" + "**OFF branch** (no `--ground-code`)" | PASS |
| R20 | Every code-scanner status handled via the escalation-rules.md list already carrying it (REPO_MISSING, DIRTY_TREE, REFRESH_BLOCKED, stale/diverged read-only). | for s in REPO_MISSING DIRTY_TREE REFRESH_BLOCKED read_only; do awk '/^## Phase 2.6/,/^## Phase 3/' commands/idea.md then grep -c "$s"; done | 1 each | 1, 1, 1, 1 | PASS |
| R20a | Audit finding: /implement Phase 1.7 (pre-fix) only promised to "surface" DIRTY_TREE/REFRESH_BLOCKED, named no choice list, and never named REPO_MISSING at all. | git show 85763fd:plugins/dev-workflows/commands/implement.md then grep -c 'REPO_MISSING' ; same then grep -c 'is surfaced, not hidden' | 0 REPO_MISSING mentions; 1 "surfaced, not hidden" (no list) | 0; 1 — matches R20a's characterization exactly | PASS |
| R20b | /implement Phase 1.7 gains a REPO_MISSING branch citing Repo missing (after resolution); DIRTY_TREE/REFRESH_BLOCKED point at named lists; prep.read_only clause untouched; this is /implement's only edit. | awk '/^## Phase 1.7/,/^## Phase 1.8/' commands/implement.md then grep -c 'REPO_MISSING' ; same then grep -c 'Repo missing (after resolution)' ; grep -c 'is surfaced, not hidden' commands/implement.md ; grep -c '8.5' commands/implement.md | 1; 1; 0; 0 | 1; 1; 0; 0 | PASS |
| R21 | Classification stays MODERATE; Phase 0 gains a clause explaining why --ground-code doesn't floor at SIGNIFICANT. | grep -n 'classification: MODERATE' commands/idea.md ; tr newline space < commands/idea.md then grep -o 'A --ground-code run does...' | MODERATE retained; clause present | classification: MODERATE  # idea refinement is typically MODERATE; clause present citing §1.1 and §8.3 | PASS |
| R22 | model-routing/classification.md gains §8.5 after §8.4, before §9. | grep -o '^### 8.[0-9]' references/model-routing/classification.md ; awk placement check for 8.5 before ## 9. | 8.1 8.2 8.3 8.4 8.5; OK | 8.1 8.2 8.3 8.4 8.5; OK | PASS |
| R23 | §8.5 defines "inconclusive": round-1 classification partial/absent/error, OR two scanners' gap_summary each naming the other's repo/layer. | grep -n -A3 '**Inconclusive** describes' references/model-routing/classification.md | both conditions present | both conditions present verbatim | PASS |
| R24 | Round 2 fires when round 1 left ≥1 evidence anchor; same agent, capability_themes holds one question, search_hints seeded from round-1 evidence. | grep -n 'capability_themes holds exactly one question' references/model-routing/classification.md | present | present, with search_hints.paths/.symbols/.keywords seeded from round 1's evidence[].path/.symbols/.lines | PASS |
| R25 | Round 2 capped at 4 dispatches, one round only, no round 3; unresolved theme reported, never guessed at. | grep -n 'capped at 4 dispatches and is one round only' references/model-routing/classification.md | present | present, "There is no round 3. A theme still inconclusive after round 2 is reported unresolved — never guessed at." | PASS |
| R26 | §8.5 is opt-in; /idea named first/only consumer; /implement, /epics, /create-ard, /specify, /design explicitly unaffected. | grep -n 'first and only current consumer' references/model-routing/classification.md | present, names the 5 unaffected commands | present: "`/idea` (Phase 2.6) is its first and only current consumer. `/implement`, `/epics`, `/create-ard`, `/specify`, and `/design` run §8.2 alone and are unaffected by this section." | PASS |
| R26a | /implement is the candidate second consumer but is NOT wired here; recorded as a feedback entry for a later round. | grep -n 'implement-broad-then-narrow-candidate' $SPECS_PATH/dev-workflows-feedback/2026-08-12.md | feedback entry exists | entry 2026-08-12-implement-broad-then-narrow-candidate created (deliverable 2, this task) | PASS |
| R27 | §8.5 records why — a broad prompt lets each scanner defer to the layer it did not scan. | sed -n '/^### 8.5/,/^**Round 1**/p' references/model-routing/classification.md | opening paragraph states the reason | "A broad prompt lets each scanner defer to the layer it did not scan… Naming a verified anchor removes the escape." | PASS |
| R28 | code-scanner's handoff contract gains optional lines: [<n>]; the agent populates it from grep hits. | grep -c 'lines:   [<1-based line numbers>]' references/handoff/code-scanner.md ; grep -c 'Record the 1-based line numbers' agents/code-scanner.md | 1; 1 | 1; 1 | PASS |
| R29 | Field is additive/optional; the other 4 callers (/epics, /implement, /create-ard, /specify, /design) are not edited and ignore it. | grep -c 'evidence.lines is optional' references/handoff/code-scanner.md ; git diff --name-only main...HEAD -- commands/ then grep -Ec 'epics.md or create-ard.md or specify.md or design.md' | 1; 0 | 1; 0 | PASS |
| R30 | Phase 3 grill: code findings are facts, not questions — they answer gaps and are cited, not re-asked. | awk '/^## Phase 3/,/^## Phase 4/' commands/idea.md then grep -c 'facts, not questions' | 1 | 1 | PASS |
| R31 | Exception: a premise-contradicting finding becomes a ranked challenge, competes (never adds) for a slot, capped at 2. | awk '/^## Phase 3/,/^## Phase 4/' commands/idea.md then grep -c 'At most 2 such challenges' | 1 | 1 | PASS |
| R32 | idea-format.md gains Section 7 ## Feasibility grounding; old 7→8, old 8→9; every cross-reference to the old numbers still resolves. | grep -o '^## Section [0-9]*' references/idea-format.md (join numbers) ; grep -rn 'idea-format' --include=*.md plugins/dev-workflows then grep -c 'Section [789]' (cwd plugins/dev-workflows/) | 1 2 3 4 5 6 7 8 9; 0 stale citations in plugins/dev-workflows/ | 1 2 3 4 5 6 7 8 9; 1 — CHANGELOG.md:13 read "renumbers the former Section 6 `Open questions & assumptions` to 8 and Section 7 `Candidate success signal` to 9", off by one against references/idea-format.md at branch base 85763fd (base: Section 6 = Prior art, Section 7 = Open questions & assumptions, Section 8 = Candidate success signal — confirmed via git show 85763fd:plugins/dev-workflows/references/idea-format.md) | FAIL (found and corrected in this task — see note below) |
| R33 | Section 7 optional with the same discipline as ## Prior art: written only when grounding ran AND found ≥1 finding; omitted, not "nothing found", otherwise. | grep -n 'Write it when code grounding ran' references/idea-format.md | present | present: "Write it when code grounding ran *and* returned at least one finding; omit it entirely otherwise" | PASS |
| R34 | Section head records <repo>@<scanned_ref> per grounded repo, from code-scanner's prep.scanned_ref. | grep -n "taken from code-scanner's prep.scanned_ref" references/idea-format.md | present | present in Section 7's second paragraph | PASS |
| R35 | Three optional slots: What exists / What's missing / Reframing (one line, only on premise contradiction). | grep -n 'What exists\|What.s missing\|Reframing' references/idea-format.md | all three present as bullets | all three present | PASS |
| R36 | Every bullet cites <repo>/<path>:<line> (first lines entry) or <repo>/<path> with no lines; uncited bullet not written. | grep -n 'A bullet with no citation is not written' references/idea-format.md | present | present | PASS |
| R37 | Nothing speculative in Section 7 — an unresolved theme is [NEEDS CLARIFICATION] in Section 8, never a hedged bullet. | grep -n 'Nothing speculative goes here' references/idea-format.md | present, points to Section 8 | present: "…never a hedged bullet here", citing "Open questions & assumptions (Section 8)" | PASS |
| R38 | Section 5 gains a closing rule: code findings never go there — demand evidence only. | grep -c 'Code findings never go here' references/idea-format.md | 1 | 1 | PASS |
| R39 | Phase 5 + Final report gain: grounded repos + scanned_refs, descoped/unmounted repos, inconclusive themes, and the Reframing line (reported first). | awk '/^## Phase 5/,/^### Context hygiene/' commands/idea.md then grep -c 'scanned_ref' ; awk '/^## Final report/,0' commands/idea.md then grep -c 'code grounding: off' | 1; 1 | 1; 1 | PASS |

## Cross-cutting checks (task-13 brief, Step 1)

| # | Check | Command | Expected | Actual | Verdict |
|---|---|---|---|---|---|
| C1 | Residue — every statement about /idea's phase list still true. | grep -rn 'Phase 2.5' plugins/dev-workflows/ then grep -v 'commands/idea.md' (repo root) | hits reviewed by hand; none a stale claim about /idea's own phase list | 20 hits, all in create-vi.md, design.md, specify.md, epics.md, ready.md, design-reviewer.md, spec-reviewer.md, CHANGELOG.md — every one names that command's own, unrelated Phase 2.5 (ARD resolution / grounding), not /idea's. No stale claim about /idea's phase list found. | PASS |
| C2 | Residue — no leftover "eight sections" / "8 sections" claim about idea-format.md. | grep -rn 'idea-format' plugins/dev-workflows/ then grep -c 'eight sections or 8 sections' (repo root) | 0 | 0 | PASS |
| C3 | No silent rebinding — /epics, /create-ard, /specify, /design untouched by this sub-project. | git diff --name-only main...HEAD -- plugins/dev-workflows/commands/ then grep -E 'epics or create-ard or specify or design' then wc -l (repo root) | 0 | 0 | PASS |
| C4 | Three-repo manifest + frontmatter sanity — every marketplace.json/plugin.json in all three repos parses as JSON. | for each of the 3 repos: find -name marketplace.json -o -name plugin.json, then python3 -c json.load(...) for each | no BAD JSON lines | no BAD JSON lines printed | PASS |

## Supplementary: port integrity (mgd, copilot) — not individually R-numbered, verified for completeness

- **mgd content-verbatim**, scoped to `plugins/dev-workflows/`: `diff -rq --exclude=.git /workspace/mgd-claude-plugins/plugins/dev-workflows /workspace/ihudak-claude-plugins/plugins/dev-workflows` — differs in exactly the identity set (`plugin.json`, `CHANGELOG.md` — header suffix `(ported from...)` only, a pre-existing convention, `LICENSE`, `README.md`, `references/dependencies.md`). No 7th unexplained file. PASS.
- **copilot dialect leaks, scoped to the port commit's added lines** (per the task-13 brief's caveat that a tree-wide scan over-matches file paths and slash-separated prose lists): `git diff bb400fc a72df54 -- dev-workflows/ | grep '^+' | grep -v '^+++'` → 209 added lines; `subagent_type` 0, `CLAUDE_PLUGIN_ROOT` 0, `Sonnet chain` 0, `Agent message` 0, slash-form command refs 8 raw hits — all 8 inspected by hand and are false positives (`idea/create-vi/update-vi` prose list ×2, `skills/_shared/idea-format.md` file path ×2, no phrase where `/idea` etc. is used as a command reference). **0 real leaks.** The plan's own tree-wide checks (Task 12 Step 3) additionally show 2 pre-existing `CLAUDE_PLUGIN_ROOT` hits (`CHANGELOG.md:908-909`, describing the dialect rule itself) and 1 pre-existing `Sonnet chain` hit (`skills/docs-profile/SKILL.md:255`, from commit `9c56c84`, 2026-07-13) — both confirmed via `git blame`/`git diff bb400fc a72df54` to predate this port and be outside its diff. PASS.
- **copilot intent parity** (Task 12 Step 4, all `>=` expectations met): `When a choice list fires` ×1, `Feasibility grounding` ×3, `8.5 Broad, then narrow` ×1, `lines:` ×1, `ground-code` ×7, `Repo missing (after resolution)` ×1, Section list `1 2 3 4 5 6 7 8 9`. PASS.

## Finding: the changelog defect (deliverable 3)

`CHANGELOG.md:13` in **all three repos** (canonical, mgd, copilot) originally read:

> "renumbers the former Section 6 `Open questions & assumptions` to 8 and Section 7 `Candidate success signal` to 9"

Verified against `git show 85763fd:plugins/dev-workflows/references/idea-format.md` (the branch base):
Section 6 was and remains `Prior art (optional)` — it did not move. The former **Section 7** was
`Open questions & assumptions` → renumbered to 8. The former **Section 8** was `Candidate success
signal` → renumbered to 9. Each section number in the clause was off by one. Corrected in all three
repos to:

> "renumbers the former Section 7 `Open questions & assumptions` to 8 and Section 8 `Candidate success signal` to 9"

No other text in any of the three entries was changed.
