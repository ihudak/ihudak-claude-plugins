# Workflow

`docs-workflows` will carry the documentation half of the `dev-workflows` pipeline. It ships no commands yet — this page holds a placeholder diagram until later tasks in the marketplace split move commands, agents, and references into this plugin.

```mermaid
flowchart TD
    core["docs-workflows (empty skeleton)"]
```

See the [documentation index](README.md) for everything else.
