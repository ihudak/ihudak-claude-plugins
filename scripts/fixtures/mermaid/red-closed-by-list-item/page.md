# Red: a broken diagram whose fence the next list item closes

- ```mermaid
  flowchart TD
      a -->|see [BR#n]| b
- the next item closes the fence, so the failure is the diagram, not a missing fence
