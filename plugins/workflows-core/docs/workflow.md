# Workflow

`workflows-core` is the shared foundation the `dev-workflows` plugin family draws on. It ships no commands yet — this page holds a placeholder diagram until later tasks in the marketplace split move commands, agents, and references into this plugin.

```mermaid
flowchart TD
    core["workflows-core (empty skeleton)"]
```

See the [documentation index](README.md) for everything else.
