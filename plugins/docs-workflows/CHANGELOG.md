# Changelog

All notable changes to the **docs-workflows** plugin are recorded here.
Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Versions follow semver at the plugin level.

## [1.2.0] — 2026-09-10

### Fixed — `diff-summarizer`'s pull-request half had no producer and is retired (1.1.3)

`pr_refs` was an input nothing in the tree could build. `/document` Phase 5 passes `repo_path`, `repo_url_slug`, `refs`, `context`, `keys_hierarchy` and `refresh`; `/release-notes` Phase 5 passes the first three. Neither builds a `pr_refs` element, and neither can: producing one means resolving a pull-request URL, which both commands' own zero-API-calls invariant forbids. Everything gated on it was therefore unreachable machinery — the host-routing table, the `gh` GitHub resolver, Strategies 1–3 (all keyed on `pr_id`), the URL parse notes, the `status: MERGED | OPEN | DECLINED | UNKNOWN` field, the note on non-MERGED PRs, and the `host: other` unsupported-host disposition — sitting two lines below the agent's own sentence that *"nothing in this plugin reads a tracker or a pull-request API any more."* Retired from `agents/diff-summarizer.md` and `references/handoff/diff-summarizer.md` together, so the two state one contract, and the agent's user-visible `description` — *"Host-aware resolver — uses the gh CLI for GitHub when available"* — now says what it does.

What survives is what a `refs` element reaches: the direct `git diff <branch_to>...<branch_from>`, and Strategy 4, renamed **Key-commit fallback**, which is keyed on `keys_hierarchy` — an input `/document` does pass — and is the only way a squash-merged ref, whose branch is gone and whose sha was rewritten, is recovered at all. Also retired, as reachable only through `pr_refs`: `per_pr.pr_id` / `per_pr.url` and their `unresolved_prs` twins, which could only ever be null; the `resolved_via` values naming a retired strategy; and `unresolved_prs.candidates`, whose one documented producer was Strategy 4's single-key sub-branch keyed on `source_item`, a tracker field that appears nowhere under `plugins/`. `/document`'s all-unresolved gate loses its *"Review candidates one by one"* option with it — an option that could only ever present an empty list. `NO_PRS_RESOLVED`, `per_pr` and `unresolved_prs` keep their names: both callers match on the first, and renaming an identifier across callers buys nothing here.

### Fixed — `/release-notes` and six reader-facing surfaces described a run that resolves pull-request URLs (1.1.3)

`release-notes.md`'s invariant read *"ZERO external API calls — PR URLs are identifiers only; all resolution is local `git`"*, describing an input that command has never had: Phase 3 builds its refs from `implementation.md` and a `git log --grep` scan, and Phase 5 passes `refs[]`. Its Phase 1 sentence about *"the `pr_refs` elements that have a `status` field"* named a shape that no longer exists. Five documentation pages carried the same vocabulary — `docs/workflow.md` had keyed `/document` *"resolve PR URLs to local clones"*, `docs/getting-started.md` had it resolve *"each pull-request URL"* under `$REPOS_PATH`, `docs/reference/agents.md` described the agent as *"host-aware"*, `docs/reference/environment.md` had `$REPOS_PATH` *"read fresh by each run that resolves PR diffs"*, and `docs/commands/release-notes.md` explained what happens to *"a PR URL that appears anywhere in the inputs"*. Each now states what the run resolves: the repositories its implementation record and commit scan name, matched by `git remote get-url origin` slug. The sixth is the worst in kind and sits outside every plugin, in the marketplace's own `README.md` Prerequisites: it told the user `gh` + `gh auth login` *"enables reading GitHub PR diffs (`/document`, `/release-notes`); without it those commands fall back to local-git strategies"* — an install-and-authenticate instruction sold on a promise that was empty, since local git is the only path either command has ever taken. It now names what `gh` actually buys: the pull request `/document` offers to create, and the one every phase handoff opens in the specs repo.

### Fixed — `/document`'s `### Refs in scope` block could print an unresolved element twice (1.1.3)

`diff-summarizer` declares `per_pr` as one entry per **input** element, with `unresolved` among its `resolved_via` values, and `unresolved_prs` as the unresolved elements with their reasons — so an element that resolved to nothing is in both lists, and a report block reading *"one line per `per_pr` element … then one per `unresolved_prs` element"* prints it once from each. Settled from the command's side, which is the side that renders: the first shape is now *one line per **resolved** `per_pr` element*, and the block says an element that resolved to nothing is printed once, from the second list. The agent's contract is unchanged.

### Fixed — three agents attributed claims to PR URLs the summaries never carry (1.1.3)

`doc-planner` step 2 told the planner to record *"which `diff-summarizer` PR URLs back it up"*, its checklist schema typed `sources: [<key | PR URL>, ...]`, and its NEVER rule required every source to be *"a key in the `folder_read` or a PR URL in `diff_summaries`"* — a traceability requirement no run could satisfy, since a `diff_summaries` entry is identified by its `ref`. `doc-writer` step 6 sent *"per-claim attribution to resolved keys and PR URLs"* into its return payload, for the same absent field. `doc-reviewer`'s Factual-correctness dimension checked claims against *"the PR diff summaries"* by the same wrong name. All three now name the `ref`; `doc-writer`'s and `doc-reviewer`'s bans on writing a PR URL into a rendered page are untouched, because a prohibition holds whether or not the run has one.

### Fixed — `/document`'s Final Report asked for three things the run cannot produce (1.1.3)

Phase 9's `### PRs in scope` block instructed the run to print, per element, a **PR URL**, a `status` of `MERGED | OPEN | DECLINED | UNKNOWN`, and a `resolved_via` drawn from a six-value list. None of the three survives contact with what the run holds. `diff-summarizer`'s Output declares no per-element `status` at all — its `status` is per repo — so the field had no source; a keyed run builds `refs[]` from `implementation.md` and the commit scan, for which that agent sets `url: null`; and the `resolved_via` list omitted `local_ref`, the one value a `refs` element can ever carry. The same file's Phase 4 step 1 already stated flatly that *"there is no `pull_requests[]` to filter and no PR `status` to filter on"* — two live contradictory instructions in one command body, which `workflows-core:instruction-file-maintenance` names as a defect in itself. The block is now `### Refs in scope`, its fields taken from that agent's Output and nothing else: `ref` (or `url` on an element carrying one), the full seven-value `resolved_via`, the change counts, and a second line shape for an `unresolved_prs` entry with its reason.

### Fixed — the rest of `/document`'s pull-request vocabulary (1.1.3)

The command resolves no pull request: it resolves the implementation record's **repo slugs** to clones and summarises the **refs** that record names. Thirteen sites still said otherwise and are corrected against what the run does — the frontmatter `description` and the opening summary (*"resolves PR URLs to local git repos"*), the refresh choice and its rationale, the `diff-summarizer` dispatch prompt (*"Summarise this repo's PRs"*), the `NO_PRS_RESOLVED` handling and the all-unresolved aggregate gate, the maintenance key-events line, the `### Repos analysed` counts, the traceability rule, and the two direct-mode boundary sentences. `NO_PRS_RESOLVED` keeps its name — it is the agent's own declared return status — and every remaining "PR" in the file is the run's **own** pull request (the Phase 8.5 draft, the profile PR, a repo's pre-PR checklist), which is correct. `workflows-core:escalation-rules`' *Repo unresolved (zero matches) — /document* array offered *"Skip and continue without its PRs"* and now names refs; four sentences on the command's own documentation page follow, including its zero-direct-API-calls paragraph, which described a `gh` resolution path a keyed run cannot reach.

**Phase 4 step 6 is brought to the same standard rather than left inert.** It told the run what to do *"where a run was additionally given a genuine PR URL as `pr_refs` enrichment"* — a state nothing produces, since Phase 5 passes `refs[]` only. It now says that outright: `diff-summarizer`'s host routing, `gh` resolver and `host: other` disposition are that agent's contract for a caller holding a pull-request URL, and never a branch this run takes.

### Fixed — `/release-notes` called its grounding source "merged PR diffs" at four sites (1.1.3)

Both halves of the phrase over-claim. There is no pull request: Phase 3 builds `refs[]` from `implementation.md` and a `git log --grep` scan, and the sentence two lines under the choice already says so (*"No URL, no host classification, no `gh` requirement"*). And **merged** is falsified by the record itself — `workflows-core:implementation-format` §1 keeps `pushed: false` entries, never pushed and so never merged, which `/document` Phase 4 step 1 confirms is in scope. It told the operator the grounding was narrower than it is. The four sites — the frontmatter `description`, the opening summary, the Phase 1 `choices:` option and the sentence that keys off it — now name the refs' diffs. The option's arity is unchanged at three, and the sentence under it no longer quotes option text at all, so a later rewording cannot desynchronise the pair.

### Fixed — `diff-summarizer`'s handoff declared a `model_routing:` input nothing sends (1.1.3)

Measured on both halves before cutting it. No caller emits one: `/document` Phase 5 and `/release-notes` Phase 5 each pin the tier with `model:` on the dispatch and pass `repo_path` / `repo_url_slug` / `refs` / `context` / `keys_hierarchy` / `refresh` and nothing else. And no field of the block is read: the agent body names none of `classification`, `opus_available`, `planning_model` or `gate_tests_on_review`, and its own `description` already says the tier is *"assigned by the caller per the model-routing policy (no fixed pin)"*. Dropped, exactly as `pr_refs` was and for the same reason, with one sentence in its place so it is not re-added. `workflows-core` 1.6.0 drops the identical declaration from `code-scanner`'s handoff and narrows the §4 rule that licensed both.

### Added — the cold-start scaffold: `/docs-init`, `/docs-brand`, `/docs-serve`

Three commands answer the question the plugin previously could not: *there are no docs and no docs repo — what should exist?* Every existing command assumed documentation already existed, and documented a delta against it.

- **`/docs-init`** scaffolds a documentation repository from nothing: a product-shaped Material for MkDocs page skeleton with a stub in every section, **two builds over one content root** (public and internal), a nav generated from frontmatter `order`, Vale with a project vocabulary seeded with the product name and the scaffold's own stub vocabulary, a CI workflow that runs both builds and the two output-level visibility gates, a `.gitignore` for the build outputs, the synced Vale packages and `/docs-serve`'s state file — **created, or merged into**: an existing `.gitignore`, such as a hosting template's, gains only the lines it lacks and never loses, reorders or rewrites one of its own, and, immediately before the commit, every path the run wrote — the branding phase's logo and stylesheet included — is tested against it: a path a project line ignores is left uncommitted, never force-added, every config reference to it is removed so the committed `mkdocs.yml` names no missing file, and both are reported — and the `.dev-workflows/docs-profile.yml` that `/document`, `/docs-brand` and `/docs-serve` read (`/release-notes` reads none). It refuses to scaffold over a repository that already carries a docs signal — an in-repo `docs-profile.yml` among them, so a profiled repository built with any generator is refused — and points at `/docs-profile` instead; it also refuses a target below a git work tree's top level — an absent one included, tested through the nearest directory up its path that exists, since a `git init` there would nest a second repository inside the first — because the profile it writes lives at a work tree's top level. It branches before it writes, verifies the scaffold builds and lints, and finishes on a **drafted** pull request it never pushes. Its lint applies **the same Vale exit criterion its CI workflow applies, over the same configuration** — it runs Vale on the repository's configuration alone, with the machine's global configuration and any `VALE_CONFIG_PATH` set aside, which is what CI's bare `vale docs/` reads on a GitHub-hosted runner, and its `vale sync` runs the same way, so the packages it installs into the repository's `styles/` are the ones the repository's `.vale.ini` names — so a local pass on the scaffold's files is the verdict CI's first run reaches on them, and the stubs pass: they raise no error-level alert against the seeded vocabulary. What can still make the two differ `scaffold-tree.md` §7 names: a scaffold path a project `.gitignore` line keeps out of the commit, a newer Vale release or synced package on the runner, and a runner of the project's own that carries a Vale configuration of its own. The source-repo set it confirms is used for that run only; no profile field records it until `/docs-audit` needs one.
- **`/docs-brand`** extracts a logo and a rough primary/accent colour pair from the product's own code — a fixed precedence over a Tailwind config, CSS custom properties, a MUI theme, a web-app manifest and SCSS/LESS variables — prints every extracted value with its file and line before applying anything, checks the pair against the WCAG 2.2 threshold pair, and copies assets into the site's `docs_dir` (`docs/assets/` on the scaffold's layout) rather than linking back into the code repo. It runs standalone, or `--inline` from `/docs-init`, which folds its diff and its contrast finding into that command's own review and pull request: a rebrand never requires re-scaffolding the site. **`--inline` never aborts the scaffold**: any stop or Cancel returns `no branding applied: <reason>` with an empty diff, and `/docs-init` continues as if `--no-brand`, recording the reason — an API or CLI product with no frontend reaches `DOCS_BRAND_NOTHING_TO_APPLY` routinely. It validates the **effective** theme, following `INHERIT`, so the inheriting `mkdocs.internal.yml` the scaffold writes is accepted rather than refused, and it writes `theme` and `extra_css` only into the config that inherits nothing — never a second theme block into the inheriting one — and the `logo`/`favicon` keys only when it applies a logo. A standalone run runs the same commit-time ignore test before it commits, so a docs repository whose `.gitignore` keeps `*.svg` or `*.css` out never receives a config naming a file its commit lacks. It brands a Material site where it sits, a monorepo's `site/` below its repository's top level included: it finds the configs through the profile's `builds[]`, whose `config` paths are relative to the top level, or, where the profile records none, in the resolved directory; follows each `INHERIT` from the directory of the config that names it; and writes the stylesheet and copies the assets under each config's own `docs_dir`, resolved against that config's directory as MkDocs resolves it, while git runs at the top level.
- **`/docs-serve`** starts, stops or checks the docs site's dev server for any profiled repo and reports a URL that actually opens from the host. It never starts a second server on a port that already answers, falls forward to the next free port on a collision and says so, and `--build` runs the profile's build command and exits. **A port counts as free only where nothing answers on it and nothing is listening on it** — a listener bound to an address `localhost` does not reach answers nothing — read as `render-verification.md` §2 defines free, for the render check's pre-boot probe too; whether any other socket standing on a port refuses a bind is in no table, so a start that exits before its port answers is read as a collision wherever something is listening on that port, or its log names an address-in-use failure — both asked, since a connection's local end standing on the port is no listener and each tool words that failure its own way, some of them naming no port (`EADDRINUSE`, *Address already in use*, *Port &lt;n&gt; is already in use*). **The port reaches a server only through the `{port}` token in its recorded command**, which every run replaces with the port it serves on — the configured one, `--port <n>`, or the one a collision moved it to — and which is the only part of a command ever rewritten; the same substitution identifies a moved server by its command line. A command without the token is **fixed-port**: it binds its own port, so a collision on it is reported with what holds the port (`DOCS_SERVE_PORT_HELD`) instead of falling forward, `--port` on it stops (`DOCS_SERVE_FIXED_PORT`), and no pre-scan looks above its port for a copy nothing could have moved there. Before it starts a server it checks the command's tool, as `toolchain-preflight.md` §2 defines one. For `pnpm`, `npm` or `yarn` it also checks the dependencies installed beside the lockfile: `node_modules/`, or the `.pnp.cjs` a Yarn Plug'n'Play install keeps instead. Where either is missing it stops with `DOCS_SERVE_NO_SERVER_TOOL`, rather than wait out the readiness timeout for a server that could never start. It starts a server in a process group of its own, as `/document`'s render check boots one, and a command that exits before its port answers — `mkdocs` with a theme that is not installed, a script its package does not have — ends the readiness wait as soon as that group is gone: the exit is reported with the last lines of the command's log, and nothing is recorded, rather than the timeout being waited out and the server recorded as one that might still come up. Its pid record is **keyed by port**, because a port holds one server and the profile `/docs-init` writes serves one space twice (public and internal), and each entry names the server it started — its space, and its **visibility** where the profile tags one. A lookup made through the server selection — every serve run's already-running check, and `--status`/`--stop` with `--internal` — **filters on the recorded server before matching a port**, so a server a collision moved onto another server's configured port is never reported as that one: a public server on the internal server's port is never taken for the internal one, `--stop --internal` never stops it, and one space's server is never taken for the next space's. With no record to consult, **a server is identified by evidence, never by the port it answers on**: the process the socket table names for the port — first its checkout, the git top level of its working directory compared as a whole path, so the checkout test never takes a `git worktree` nested inside the main checkout for it (outside any git work tree, where no worktree can nest, the docs directory or any directory below it, each path read as `builtin cd "<dir>" >/dev/null && builtin pwd -P` prints it, so a `cd` or a `pwd` of the user's cannot print into it), then the command line it or a process it descends from was started with — or, for a process in this checkout whose command line names no server, where the profile's builds carry different site names, the home page's whole site name, which never overrides a server the command line names. **The process is asked before the page**: a process in this checkout started with one of the profile's commands is that server whether or not its page names the site, so a client-rendered site whose page names nothing — a VitePress dev server's shell carries an empty `<title>` — is re-adopted rather than walked past and started a second time, and whether a page names the site decides anything only where the evidence names no server; anywhere else it only helps name what holds the port. A command started through `npx` is matched by the `npm exec` command line `npx` rewrites itself to. A recorded command carrying quotes is matched too: the comparison removes every `'` and `"` from both sides and collapses each run of whitespace, since the shell strips the quotes before the server sees its arguments and the Bash tool's wrapper writes each `'` as `'"'"'`. Where one command line carries several recorded commands and they nest (`pnpm start` inside `pnpm start:internal`), the longest names the server, since a process started with a shorter one never carries the longer one's text; two unrelated commands on one command line name none. A recorded command that begins `cd <dir> &&` or with a `VAR=value` assignment is matched by what follows them, as `toolchain-preflight.md` §2 finds a command's tool: the shell consumes both, so a server's own command line carries neither, whether it was typed by hand or started by this command. **It reads which process holds a port, and that process's working directory, parents and command line, from the socket and process tables, and never guesses without them.** On Linux it reads the kernel's own tables under `/proc`: `/proc/net/tcp` and `tcp6`, and each process's `fd` links, `stat`, `cmdline` and `cwd`. They need nothing installed and no root for the user's own processes, so it runs as it is in a slim container that ships neither `lsof` nor `ss` and drops root before anything could be installed. Elsewhere it reads them with `lsof` and `ps`, which macOS ships. `render-verification.md` §2 defines each read once, and `/document`'s render check reads the tables the same way. Each read runs its utilities as `command <name>`, and the socket read's `ls` as `QUOTING_STYLE=literal command ls`, since the Bash tool's shell carries the user's aliases and exported environment: an `ls` alias carrying `-F`, `--color=always`, `-Q` or `-L`, or an exported `QUOTING_STYLE` that quotes, would leave the read naming no process for a port `lsof` and `ss` name a listener on. It stops before it probes, starts, stops or records anything where `curl` or `bash` is missing, or, off Linux, `lsof` or `ps` (`DOCS_SERVE_NO_PROBE`, `DOCS_SERVE_NO_BASH`, `DOCS_SERVE_NO_SOCKET_TOOL`, `DOCS_SERVE_NO_PS`), each tested as a binary rather than by a bare `command -v`, which an alias or a shell function of its name passes, and prints every missing tool's message at once — save a missing `bash`, reported on its own, since that test runs through `bash` and would report every other tool missing with it: without a socket table nothing can say which process holds a port, so every identity given to one would be a guess. `--build` needs none of them. A port whose process the socket table does not show — one another user runs, say — names no server, whatever its page says. A server established as another checkout's — its process named by the socket table, its working directory in another checkout — is never re-adopted or recorded on that evidence, so one checkout's `--stop` never ends another's server; a member of the process group this run started is the exception, recorded whatever its checkout — a command's `cd` into a repository nested below the docs directory leaves its server standing in one — because it is this run's own server, as the membership rule below says. The selected server is re-adopted wherever it is found, on every profile — at the configured port, on the collision walk, and, before anything starts, on the ports a collision would have moved a `{port}`-carrying command to, whether the configured port has come free since or is still held, since a moved copy can sit beyond the first free port — and another of the repository's servers is passed over. Once the server it started answers, a process in the process group it started is that server, whatever its command line says, and is recorded as such: npm rewrites its own command line as it runs, dropping its options and the `--` before the script's, and two servers whose commands differ only by a leading `cd <dir> &&` leave the same command line behind, so in those cases no command line could say it. Anything else that answers there is recorded only where the already-running check would re-adopt it as the selected server, its listener this checkout's — the same server another run in this checkout started a moment earlier, whose record the port key then replaces; where another checkout starting at the same moment, another of the repository's servers or a foreign process won the port instead, it records nothing and names what holds it. Only where nothing names the server and its page names the site does a one-server profile re-adopt it by that page, and then only where the socket table places its process in this checkout; anywhere else — a multi-server profile, or a process the table does not show or cannot place — it is reported unconfirmed, starting nothing and naming the process to stop by hand or, where the command carries `{port}`, `--port <n>`, which runs no such pre-scan. The recorded pid is always the listener's, never a wrapper's, so `--stop` never ends a wrapper and leaves its server serving, and it reports a server stopped only once no process on its port is identified as one of the repository's servers and, where nothing there is identified at all, the port no longer answers as this docs site. Where the process group it signals still runs after `SIGKILL` — or, for an entry that records no group of its own, the process it signals still holds the port — `--stop` says so and keeps the entry, so a second `--stop` can signal it again. Every re-adopted server is recorded with its listener's pid; an entry is recorded without one in two cases only — a server that had not answered when the readiness wait ran out, and a started server's port whose process the socket table does not show, another user's or a container's published port. A server it started is also recorded with the id of the process group it started it in and the start time of that group's leader, and `--stop` ends it through that group, wrappers and all — a timed-out server before it binds, too, so a slow server never comes up unrecorded after `--stop`, whatever its command line carries — but only while the group's leader still leads it with that start time, since a group id is reused as a pid is, and a process that takes a reused one started later; `--status` reports such a server that has not yet bound its port as starting — whatever pid its entry recorded, the group being the evidence there as it is for a living entry, so a server caught between one listener and the next is reported as coming back rather than as stale — and a serve run counts it as running, reporting it still starting and starting no second copy, while another server's serve run counts that port as taken, at its configured port and on its collision walk, rather than start there from under it; `--status` gives its entry the listener's pid once a process in that group holds the port. Its entry also records the log its start wrote under the temporary directory, which `--stop` deletes wherever it removes the entry, stopped or stale — only ever a regular file named `dw-serve-…`, whatever the state file names — so a long-running server's log is not left to grow once its record is gone. A run leaves no log that no entry names, save one interrupted between starting a server and recording it: a run that records nothing for what it started deletes its log itself, and a write that replaces an entry takes over that entry's log where the replaced entry's process group is still its own and holds the listener the new entry names, and deletes it otherwise. An entry recorded without a pid and without such a group is judged by what now holds its port: where the already-running check would re-adopt it as the entry's own server, the entry takes the listener's pid, and `--stop` ends it like any other; where another checkout's server, another of the repository's servers or a process that is not this docs site holds the port, or nothing answers there, the entry is stale, and `--stop` removes it without signalling anything; only where nothing identifies what answers, and it answers as this docs site, does the entry stay without a pid, and `--stop` cannot end it and leaves it in place. A recorded pid counts as living only while the socket table still names it on its port, so a pid number reused after a container restart is never reported as the running server and never signalled; an entry that records a process group of its own counts as living, too, while any member of that group holds its port, whatever pid it recorded — so a server that re-spawns its listener under a new pid in the same group is still found by `--status` and by a serve run, as `--stop` already found it — and it takes that member's pid. With `--status` or `--stop`, a bare `--port <n>` names the server bound to `<n>` — the entry recorded at that port, never one a collision moved off it — so `--stop --port <n>` stops that server without a prompt. On a profile that sets `dev_servers.concurrent: false`, it starts nothing while another of the repository's servers is known to be running — a living recorded entry, one recorded and still starting, or one its own port checks identified, which on such a profile take in every other server's configured port — and stops with `DOCS_SERVE_NOT_CONCURRENT`, naming that server and how to stop it. The state file stays out of `git status`, a copy someone committed excepted, which git reports until `git rm --cached`: where no rule ignores it, the command appends that one path to the repository's local `.git/info/exclude`, never committed, so a clean-tree gate in the family does not meet it as an untracked file, in a repository `/docs-init` scaffolded or any other. `--internal` on a profile that tags a public server but no internal one — a `/docs-init --public-only` scaffold — stops with `DOCS_SERVE_NO_INTERNAL_BUILD` instead of serving or building the public site — under `--build`, with a message naming the missing `builds[]` entry. It writes no documentation and no artefact — the one command in this plugin that writes neither — which is why it runs no `specs-preflight`, no `commit-artifacts`, no review gate and no maintenance phase; `/docs-profile`, which writes a profile, runs none of those four either, and its page, which called it the one command here that emits no cost entry, now names `/docs-serve` beside it.

### Added — `docs-scaffold-reviewer` (D25)

A new Opus-pinned agent gates both scaffolding commands. `/docs-init` and `/docs-brand` write `mkdocs.yml`, a CI workflow, `.vale.ini`, a generated nav and theme CSS — code, reviewed as code (D17) — but the plugin's declared dependencies are `workflows-core` and `prose-style`, and `code-review` and `review-fixer` ship from `dev-workflows`, so nothing guaranteed they were installed: **a miss would have removed the gate silently rather than degraded a feature**. It was also the wrong reviewer on its merits — `code-review` carries a spec-conformance dimension and a captured test baseline, and a scaffold has neither. The fixer disposition is *orchestrator applies*, behind `workflows-core:finding-triage`, so there is no re-review cycle: the orchestrator's direct edit is the fix, applied against the same finding it answers. Its Vale dimension proves that the written `.vale.ini` is the configuration Vale reads and that it loads: it looks beside it for a `.vale`, `_vale` or `vale.ini`, which Vale would read in its place, then lints one written page from the directory holding it on the repository's configuration alone — `unset VALE_CONFIG_PATH && vale --no-global`, the form `toolchain-preflight.md` §2 gives for the scaffold's `.vale.ini`, which sets `StylesPath` — and reads a runtime error as the finding, since that run loads every style a `BasedOnStyles` line names, while `vale ls-config` exits 0 on a style that does not resolve (Vale 3.21).

### Added — four references under `references/docs-workflow/`

- `scaffold-tree.md` — the scaffold's directory tree and stubs, both `mkdocs.yml` configs with their "must be identical / may differ" table, `.gitignore` with its create-or-merge rule, `.vale.ini` with its seeded vocabulary, and the one Vale exit criterion the scaffold's own verification and its CI both apply, over the same configuration: the verification runs Vale on the repository's configuration alone (`vale --no-global`, `VALE_CONFIG_PATH` cleared), which is what CI's bare `vale docs/` reads on a GitHub-hosted runner. The criterion is Vale's exit code, which an error-level alert or a configuration error fails, which a flag such as `--no-exit` or `--filter` can move, and which `MinAlertLevel` — a threshold on what is *reported* — cannot.
- `visibility.md` — the two-build model over one content root, both traps (the dev server is not the build — what `mkdocs serve` shows depends on the MkDocs version and the exclusion key, and none shows a snippet leak — and snippets cross the boundary invisibly), the two **output-level** gates, which assert on built HTML rather than on source paths or contributor discipline, the marker-comment convention, and the CI workflow the scaffold writes — whose image size budget tolerates an image directory that does not exist yet.
- `contrast.md` (D24) — the WCAG 2.2 **SC 1.4.3** (4.5:1 body text, 3:1 large text) and **SC 1.4.11** (3:1 UI boundaries) threshold pair and the relative-luminance formula, stated here rather than loaded from `guideline-reviewers`' `accessibility.md`, which sits in a plugin nothing declares and is therefore unreachable. That file is cited as further reading and loaded by nothing.
- `repo-resolution.md` — one docs-repo resolution ladder in two forms: **signal-positive** (`resolve-docs-repo`, for a repo that exists) and **signal-inverted** (`resolve-scaffold-target`, for a repo to create). Same variable, same `${DOCS_PATH:-/workspace/docs}` default, opposite predicate (D23) — an implementer who copies the adopting ladder verbatim gets the scaffold exactly backwards. Both test one signal set, which counts a Vale configuration under any of the five names Vale reads and an in-repo `.dev-workflows/docs-profile.yml` as signals: a profiled repository with no other marker is found by the adopting form and refused by the creating one.

The plugin `description` now counts reference **files** — eighteen, sixteen of them pages and two, `default-owners.txt` and `docs-profile.default.yml`, read as data — where 1.1.3's counted its twelve reference pages.

### Changed — the docs profile records what the scaffold produces

`docs-profile-schema.md` and `docs-profile.default.yml` gain `generator` (informational only — every invocation still goes through `commands.*`, which is what keeps the generator choice reversible), `builds[]` for a repo whose one content root renders into more than one output, `dev_servers.servers[].public_base_url` (a command inside a container cannot infer the host's published port mapping, so it reports this instead of guessing) and `dev_servers.servers[].visibility`. `dev_servers.servers[].command` may carry the literal token `{port}`: every consumer that runs the command substitutes the port it serves on and never runs the token unsubstituted, nothing else in a command is rewritten, and a command without it is fixed-port — its port is its own, and no consumer can move it. A server command that binds only loopback is a profile defect, which `/docs-serve` reports rather than patches. So is one that detaches its server (`setsid`, `nohup … &`, a trailing `&`, `docker run -d`), because both consumers that start a server track it through the process group they start it in, which `setsid` or a container takes a server out of, and `/docs-serve` records a pid only for a listener in its own checkout, which a server detached into a container is not. A foreground `docker run -p …` or `docker compose up` keeps to that rule, and the schema says `/docs-serve` cannot identify the server it starts — Docker's forwarding holds its port, not the command's process — and that such a server is stopped with Docker. `/docs-init` writes the token into both of its server commands (`mkdocs serve -f <config> -a 0.0.0.0:{port}`). `/docs-profile` writes it only in place of a literal port a detected command already carries, never inventing a flag or an argument-forwarding form, and its report lists every fixed-port server it wrote; the worked example's `pnpm cloud:start` stays fixed-port. `images.policy` becomes an enum — `in-repo` (the scaffold default) / `object-store` / `cdn` — replacing a free-text sentence, with every rule that sentence carried preserved under the policy it describes, beside `images.root`, `max_bytes`, `public_prefix` and `internal_prefix`. `dev_servers.concurrent` states what its absence means — no one-at-a-time limit, which is what `/docs-init` writes by writing no key, its two servers running side by side — and `/docs-serve` honours `false`; `dev_servers.readiness_timeout_seconds` names both of its readers, `/document`'s render check and `/docs-serve`'s readiness poll. The schema also advises recording, where the operator can, a server command whose tool keeps the command line it was given — `node_modules/.bin/<tool> …` — since `/docs-serve` identifies a server by that command line. And a server command binds the port it is given, or fails: one that moves itself to another port when its own is busy is a profile defect, like a loopback-only bind, and where a tool's command line cannot pin the port, its configuration must: VitePress passes `--strictPort`, and an Astro site, whose `astro dev` takes no such flag and moves off a busy port with one, sets Vite's `server.strictPort` under `vite` in its `astro.config` — without either, Vite takes the next free port, where nothing looks for the server and `/docs-serve` can start a second copy beside it. A tool can also detach its own server: Astro 7 does wherever it detects an AI coding agent, Claude Code among them, and the schema names the opt-out Astro documents, `ASTRO_DEV_BACKGROUND=0` ahead of the command, which keeps it in the foreground — and, since Astro 7.2 detaches `astro preview` the same way, that command's own, `ASTRO_PREVIEW_BACKGROUND=0`. It also says that Astro runs one dev server per project root: a second `astro dev` for a root whose first server still runs exits at once, *Another astro dev server is already running*, so a profile that serves one Astro root twice needs `--ignore-lock` on one of its two commands, never `--force`, which stops the first. `astro preview` locks its root the same way — a second exits with *Another astro preview server is already running* — and takes the same `--ignore-lock`. `/docs-profile` records a detected command as it stands and adds no flag, so its report now lists the server commands it can tell break one of these field rules — a loopback-only bind, a detaching command, `astro dev` or `astro preview` without its variable among them, and a VitePress or Astro command that moves off a busy port unpinned — each with the rule it breaks and how to fix it, or its tool's configuration, by hand. VitePress and Astro are the two tools whose pinning it checks: whether a command running another tool moves off a busy port, and what would pin it, is that tool's own behaviour, which it has no verified form to judge, so the report neither lists nor clears such a command. Run inline from `/document`, which skips that report, it hands both lists back, the fixed-port servers and these defects, and `/document` shows them.

`frontmatter-guidelines.md` gains a **reserved-keys** section (D18): `type`, `audience`, `visibility` and `unit`, reserved by this family on any docs repo it scaffolds or writes into. `docs-frontmatter`'s ownership of the schema is unchanged.

### Changed — `/document` direct mode files its bookkeeping per docs repo (D19)

A direct-mode run takes no address, so it ordinarily resolves no PRD folder, and its cost entry went to the pending file and its feedback unfiled to the specs-repo root — both reconciling against nothing, forever, which is the problem D19 names. It now takes the same rung `/docs-init` and a standalone `/docs-brand` take: both land under `$SPECS_PATH/documentation/<docs-repo-slug>/dev-workflows/`, the slug being `workflows-core:specs-repo-git` §2.1's name for the write target the run resolved, whether or not that target is a git work tree. Every direct-mode run resolves a write target, so none parks an entry in pending any more; a run whose key does resolve a PRD folder still files under it. Keyed mode is unchanged. Pending and unfiled entries left by earlier direct-mode runs stay where they are.

### Changed — `/document`'s render check boots the server that publishes each page

Phase 6.5's dev-server smoke-check booted `dev_servers.servers[<space>]`, one server per space. The profile `/docs-init` writes records **two** servers for its one space, a public one and an internal one, so on that profile the index names no single server and an agent would have to pick: picking the public server would check every internal-only affected page on a server whose build excludes it, and picking the internal one would never boot the public build at all. The server is now chosen **per affected page** (`render-verification.md` §2). A space with one server checks every page on it, as before. A space whose two servers are tagged `public` and `internal` checks a page on the public server unless the public build excludes it (`visibility.md` §1 decides which pages those are), and then on the internal one, booting them one at a time, public first. Any other space that several servers share is skipped with its reason and falls to the manual table, since a guessed server is a false result either way. Each server's `{port}` is replaced by its configured port before it boots.

### Changed — `/document`'s build check runs every build `builds[]` records

Phase 6.5's gating build check ran `commands.per_space.<space>.build`, else `commands.build`, and the profile `/docs-init` writes records `commands.build` as the public build alone — so under that rule its internal build would never run, an internal-only page with a broken link would pass the check, and only CI would catch it on the pull request. Where the profile records `builds[]`, the check now runs **every** entry, the precedence `/docs-serve --build` uses; otherwise it runs the per-space or flat command exactly as before. Each build is recorded on its own under its `id`, so a failure names the build that failed, and `doc-fixer` is handed that build's own output. The toolchain preflight derives its tools from `builds[]` too.

### Changed — `/document`'s render check stops each server by its process group

The smoke-check recorded "the process id" of each server it booted and killed it before booting the next, without saying which process. Started the way the Bash tool starts a command, that is a wrapper shell, and an `npm` or `pnpm` script adds another: signalling either leaves the server serving, so the next server booted beside it. A server that had not bound its port when the readiness poll timed out was worse: killing its wrapper left it running, and it bound after the check had moved on, outliving the run. It now boots each server **in a process group of its own** — `command bash -c 'set -m; (cd <docs_repo_path> && <command>) > <log> 2>&1 & echo $!'`, where job control makes the printed pid the id of a group the command's wrappers and children inherit — and stops it by **signalling that group**, under the same explicit `command bash -c`, so job control, `$!` and `kill -- -<pgid>` are bash's whatever shell the Bash tool uses — Claude Code runs it in bash or in zsh, zsh on a default macOS — and a `bash` function of the user's never answers for the group, `SIGTERM` and then `SIGKILL`, after its pages and after a readiness timeout alike. **The next server boots only once two things are confirmed**: the group is gone, and the port is quiet. A group counts as gone where **the process table** names no member of it or where every member it names is a zombie: a zombie runs nothing and holds no port, and under a parent that never reaps — a container whose PID 1 is `sleep infinity`, with no init — a signal test alone would find the group running for good and end the check after its first server. `kill -0` settles only the other half — exit 0, so a member is alive and signallable, so the group is not gone — because its failure has two causes that mean opposite things: no such group, and a group this user may signal no member of, which `kill(2)` refuses with `EPERM` and bash reports with the same exit 1. Reading that failure as a gone group reports a live server as ended: measured against four live root-owned groups on this host, where `kill -0` exits 1 while the table names a live, non-zombie member of each. The table names another user's processes too, on Linux from `/proc` and elsewhere from `ps`, so it answers where the signal cannot. Where the port still answers, the port's **listener** is signalled as a second stop — `/docs-serve`'s definition of one, without its checkout test (this run started the server) or its state-file test (this command keeps none). Where either still cannot be confirmed, no further server boots, the record names the port or process group left running, its command and its log, and every page not yet checked falls to the manual table; a server confirmed stopped has its log removed, so the check leaves behind no log but one its record names. A server job control gave no group of its own is stopped by its pid and its listener instead, and a readiness timeout on it ends the check, since nothing can tell whether it will bind late. Every read of the process or socket table (the group a boot made, its members' states, a listener and its parents) comes from `/proc` on Linux, which needs nothing installed, and from `ps` and `lsof` elsewhere (`render-verification.md` §2), each utility run as `command <name>` so an alias or shell function the Bash tool's shell carries cannot change what the read parses. A missing `lsof` alone never ends the check. A probe before each boot keeps the check from booting onto a port something else already holds, and that process is never signalled. **The readiness poll ends as soon as that group is gone**: at every interval at which its request gets no response it tests the group, and a server whose command exits before its port answers — a theme that is not installed, a configuration it cannot load, a script its package lacks — is reported with the last lines of its log, its pages fall to the manual table, and the check goes on at once, where 1.1.3's poll ran until HTTP 200 or the whole readiness timeout, 120 seconds by default, for a server that had already exited.

### Changed — a 404 in `/document`'s render check has one disposition

Two live instructions disagreed: Phase 6.5's outcomes treated a 404 on an affected page as a render defect and sent it to `doc-fixer`, while the route note said a wrong route that 404s simply leaves the page on the manual table. A 404 is now always surfaced as ❌ with its URL, the page stays on the manual table, and it is never a content failure by itself: the route is derived best-effort, so a 404 cannot tell a wrong route from a missing page — and the per-page server choice above keeps internal-only pages off the public server, the systematic source of false ones — while the build check, which runs every build that compiles an affected page, owns compile failures. It counts toward `render_smoke_check`'s `findings`, on a `RAN` row. A 5xx stays a render defect and a content failure, as before, and is now recorded `FAILED` on `render_smoke_check`'s row, where 1.1.3's ledger gave that gate no `FAILED` outcome.

### Changed — `/docs-profile` resolves through the shared ladder

Its Phase 0 took the first token of `$ARGUMENTS`, else the current working directory — which in a container means it profiles wherever the shell happens to be rather than where the docs clone is mounted. It now executes `resolve-docs-repo` and reports which rung answered. Its signal-less confirmation survives as a distinct question — whether to *write a profile* for a repo showing none of the usual signals — and cites `repo-resolution.md` §3's signal set rather than re-deriving it. Its own list had lacked `mkdocs.yml`, `docusaurus.config.js`, `antora.yml` and `DOCUMENTATION-GUIDELINES.md`, all of them in `/document`'s, so a repository `/document` had found by one of those and handed to inline profiling was asked "Profile it anyway?". It tests the directory the ladder resolved and the git top level, where the profile lives, and a signal in either counts.

### Fixed

A `/docs-profile` refresh could delete what its scan does not detect. It drafted the new profile from detection alone — `package.json` scripts, `*/_content` roots and a handful of conventions — and left out any field detection did not settle, so a field the scan does not look for, or one an operator had added by hand, was missing from the draft, and the refresh diff, recommending "overwrite changed fields", could read it as a change to nothing. A profile `/docs-init` writes would lose nearly everything that way, its `builds[]`, `generator`, `dev_servers`, `commands` and `images` among them: a MkDocs scaffold has no `package.json` script or `*/_content` root for detection to read, and detection looks for `builds[]` and `generator` not at all. A refresh now builds the new profile from the existing one: it proposes a change only for a field detection produced a value for, or for a value the schema no longer accepts, carries every other field forward verbatim and names it in the diff as *kept, not detected*, never treats detection finding nothing as a proposal to delete, and never empties `spaces[]`. Detection itself is unchanged. The image policy is one of the fields it detects, so it is proposed through the diff like the rest — and an existing free-text `images.policy`, which the new enum does not accept, is listed as non-conforming and replaced, asked for where detection found none, never carried forward into a file that must conform. And a `{port}` token an operator added by hand, as the schema advises, is not a difference — where it stands in place of the port a command names, or in arguments forwarded after the detected command, as in `pnpm docs:start -- --port {port}`: a detected command equal to the recorded one with the token replaced by the port, or to the recorded one without those forwarded arguments, keeps the recorded one and lists it in the diff as kept, so a refresh never proposes removing the token.

`/docs-profile`'s refresh offered "Keep existing, write nothing" without saying where the run went next. Only a bootstrap was sent on to Phase 5, and Phase 5 runs unconditionally, so a literal reading of the choice cut a branch, could stash the operator's work, and ended on an empty commit. Choosing it now ends the run with the Phase 6 report reading "kept — nothing written": no branch, no stash offer, no commit. Run inline, it hands `/document` no branch and no commit, and `/document` records the profile as an in-repo one, never a generated one. `/document` now looks for the profile where profiling does (below), so its inline run starts from no profile and does not reach this choice at all. A refresh whose diff lists no change now ends the same way without asking, and reports the profile up to date: it used to ask anyway, and "Apply the diff" then cut a branch whose commit failed with nothing to commit, leaving a branch with no commit of its own.

`/docs-profile --inline` switched onto a `dev-workflows/docs-profile-bootstrap` branch an earlier, interrupted `/document` run had left, and `/document` then renamed it into the docs branch — which forked where the leftover had, so it lacked every commit the base had gained since, the docs site itself among them where the site came later. An inline run now stops instead with `DOCS_PROFILE_BOOTSTRAP_BRANCH_EXISTS` — at its Phase 0, before its scan, its Opus synthesis or any question, since none of them can let the run finish — naming the branch, its base and the commit it forked from, and asks for it to be deleted or merged before a re-run; `/document` stops with that error. A standalone run still resumes its own existing profile branch.

**A branch based on the default branch is based on its name.** `/document` Phase 6.2 and `/docs-profile` Phase 5 each resolved the base with `git symbolic-ref --short refs/remotes/origin/HEAD` and ran `git switch <base>` on what it printed — `origin/main`, which `git switch` refuses with exit 128 — so the base was never switched to or pulled, and a run that carried on cut its branch from wherever HEAD stood. `/document` Phase 6.2 even said the command returns `main` or `master`. Both now cite `workflows-core:read-only-repos` §3's **A switch takes the name** rule, as `/docs-brand` Phase 7 and `/docs-init` Phase 2.5, new here, do too: `<base>` is the branch's name, `main`, with the chain's `origin/` removed, and each states its own fallback where the chain finds nothing — `/document` its `remote set-head origin --auto` retry, and all four a local `main`, then `master`, switched to without a pull, else HEAD. The two reads keep the ref and say why: `/document`'s in-repo-profile guard and the fork point `DOCS_PROFILE_BOOTSTRAP_BRANCH_EXISTS` names both run before the base is pulled, so each reads the `origin/` ref the pull would bring the base up to. `diff-summarizer`'s pull, which switched the same way, cites the same rule and keeps its `set-head` retry as its own step.

Two statements were corrected. `/document` Phase 6.2 ended "all git operations are local" although its first step fetches; it now says nothing is pushed or sent, that the first step's reads of the remote only settle the base, and that every write is local. The references page named three consumers of `docs-profiles/` — `/docs-profile`, `/document` and the `docs-frontmatter` skill — and missed the three agents and the hook that also read it; it now gives the `grep` that derives the set, and names that recipe as the authority.

**The docs profile has one home: the git work-tree top level of the resolved docs repository** (`docs-profile-schema.md`, *Where the profile lives*). Every path it records is relative to that top level, every command it records runs from it, and `/docs-serve`'s state file sits beside it. `/docs-profile` read and wrote the profile at the top level, while `/document` looked for it in the directory its ladder resolved — which its `$DOCS_PATH` rung took as given — so the two disagreed wherever that directory sat below its top level: a Docusaurus `website/` in a monorepo, a `site/` beside the code. On that layout every `/document` run re-profiled; a run whose profiling wrote at the top level waited for a file in the site directory that was never written; its finish found the profile commit with `git log --diff-filter=A`, which named the commit that first added the file, and squashed six commits — unrelated code among them — into the docs commit; and its branch setup ran git with no `-C`, renaming whatever branch HEAD was on. Now `/document` takes the resolved directory up to its top level before it looks for the profile; `/docs-profile --inline` hands back the branch it cut and the commit it made, and the finish renames that branch by name and squashes onto that commit; every git call in the branch setup, the docs commit and the finish runs as `git -C <docs_repo_path>`; and `/docs-profile` writes each path relative to the top level and each command in a form that runs from it. The three commands new here keep the same home: `/docs-serve` reads the profile, runs its commands and keeps its state at the top level, `/docs-brand` reads the profile there, and `/docs-init` refuses a target below a work tree's top level (above). Nothing migrates: the top level is where `/docs-profile` has always written the profile.

`/document` direct mode's style check linted the repository the shell was standing in. Its Phase 0 resolves `repo_root` from the edit target and says never to anchor on the working directory, yet Phase 3.5 handed `docs-style-checker` the working directory's git root and skipped itself where the working directory was not a git repository — so a run invoked on another repository checked the wrong one, the very failure Phase 0 names. Phase 3.5 now hands over the `repo_root` Phase 0 resolved and skips where the resolved target is not a git repository, and the same holds for Phase 4's `git diff --stat`, the project root its maintenance agent is given, and the report's `git status` advice.

**A smoke check without `curl` says so at once.** `/document`'s render check named no tool for its readiness poll and its page requests, and nothing checked for one — the toolchain preflight never asked for `curl` — so on a host without `curl` the poll could not run, and every server the check booted waited out the whole readiness timeout, 120 seconds by default, before its pages fell to the manual table. The check now confirms `bash` and `curl`, and, off Linux, `ps`, before its first boot and, where any is missing, boots nothing, sends every page to the manual table, and records `render_smoke_check` as `DEGRADED` with the tool named; a probe that exits 127 part-way through ends the check the same way, since 127 means `curl` could not run, which is neither an answer nor silence. The toolchain preflight requires the same tools wherever the profile records a dev server. **The row is `DEGRADED`, never `UNAVAILABLE`**, because the ledger reserves `UNAVAILABLE` for a gate where neither the primary nor a fallback ran, and this gate's registered fallback, the manual pages-to-visit table, needs no tool and is always produced.

**`build_check` runs its fallback before it offers a skip.** The ledger registers the dev-server boot as `build_check`'s fallback, and `UNAVAILABLE` means that neither the primary nor its fallback ran; yet `/document`'s build step answered a missing build tool with the ledger's conversion prompt at once, without asking whether the boot could still run — so a build written `uv run mkdocs build` beside a server written `mkdocs serve`, on a host without `uv`, was offered "Proceed without this check", and a `DEGRADED` gate was recorded as skipped. Step 1 now tests the fallback first: where `bash`, `curl` (and, off Linux, `ps`) and the tool of one of the failed build's own servers — the servers that publish what it compiles: for a `builds[]` entry the server whose `visibility` pairs with it, for a space's build that space's servers, for the flat build every server the smoke check boots — are all present, a package manager with no installed dependencies counting as missing, it asks nothing and continues to the smoke check, whose boot of those servers is the build proof, and the ledger records `build_check` `DEGRADED` — naming the build that did not run and why, and what CI still builds — once one of them comes up, whatever kept the build from running, a missing `.docstack` shim included. Another build's server is no proof: on a profile whose two spaces build and serve with different tools, the other space's server compiles none of a failed build's pages. Only where the fallback cannot run, or runs and brings none of those servers up, is the gate `UNAVAILABLE` and converted; a decision the toolchain preflight already took on that missing tool still stands in place of the prompt, and declining the smoke check still declines the build check — which the smoke check's offer now says beside its list whenever the boot is a build's only proof. `RAN` now requires every resolved build to have run, since a run in which one build ran and another's tool was missing is `DEGRADED` once the boot proves the rest. `render-verification.md` §1 no longer says the boot is the build proof only for a repo with no build command.

**Every `DEGRADED` row carries a `ci_still_checks` line, and none claims a CI check the repository does not run.** `gate-ledger.md` §6 makes an empty `ci_still_checks` a `doc-reviewer` BLOCKER, yet `render_smoke_check`'s `DEGRADED` row prescribed no such line at all, so every run in which a space fell back to the manual table drew a `doc-reviewer` BLOCKER on that row. The rows that did prescribe one made a CI claim whatever the repository's CI runs: `build_check`'s wrote "the repo's build runs on the PR in CI", and `style_check`'s, where a repo linter was detected and failed, "`<the repo's own linter>` runs on the PR in CI" — so in a repository with a `.vale.ini` and no CI at all, the Phase 9 table and the warning under it told the user CI would check what nothing checks. Each of the three now names the build or the linter CI runs on the pull request, or says that none runs, with its reason; §6 and `doc-reviewer` both say that such a line, with its reason, is filled rather than empty — only an absent or blank field is — and that a line naming CI names only what the repository's CI runs. `style_check`'s does so in both modes; the warning under the Phase 9 table says that nothing will check what such a line says nothing checks, and neither the toolchain preflight's example consequence line nor the getting-started page promises a CI pass the repository may not run. Direct mode's report followed its gate table with a warning "naming what CI will check that this run did not" wherever a row was `DEGRADED` or `SKIPPED_BY_USER`, but a skipped row records no `ci_still_checks` at all, so a style check the user skipped was reported as one CI would catch even in a repository whose CI runs no linter. The warning now names a CI check only where a row's `ci_still_checks` records one, says that nothing will where that line says none runs, and for a skipped row names the gate the user skipped and no CI check in its place; `gate-ledger.md` §2, which said a `DEGRADED` row records "what CI will still check" and the report names it, now adds "or that nothing will". And `docs-style-checker` called its `primary_attempts` list "the caller's only evidence for what CI will check that this run did not"; it records the rungs this run's ladder tried and how each ended, which says what this run did not check, and the caller fills `ci_still_checks` from it only together with what the repository's CI runs.

The workflow page said `/release-notes` writes its draft "to a persistent destination you choose"; the command's Phase 1 derives the destination instead, `release-notes.md` in the resolved PRD folder, appended under the release version and the change type's section. The page now lists that file among the drafts under `$SPECS_PATH` that the terminal step commits, and the command page's second worked example no longer speaks of a "destination question".

**`/release-notes` appends its draft, and never offers to overwrite the file it appends to.** Phase 1 derives one destination, `release-notes.md` in the resolved PRD folder, and files each run's Summary into it as a section, while Phase 8 still wrote to one of `file:<path>`, `stdout` or `skip` and, finding the file present — as it is on every run after the first — asked "Overwrite", "Write to <path>.new", "Print to screen instead" or "Skip". "Overwrite" destroyed every section earlier runs had filed, and the other three kept this run's Summary out of the file Phase 1 names. Phase 8 now appends: it adds the version heading and the Change Type's section where the file lacks them, puts the draft at the end of that section, rewrites, reorders and removes nothing already in the file, and asks nothing. Phase 7, which wrote the draft to its destination before the style check, now checks a scratch copy, which it removes with `command rm -f --` once it has read it back, so neither the checker nor the fixer sees the sections earlier runs filed, and hands `prose-style-checker` the specs repository as `repo_root` (prose-style 0.4.0), so the copy is checked under the house-style overlay the specs repository keeps, as 1.1.3 checked the file there, rather than under the rules of whatever repository the session stands in. The report's style line names the rule set the checker applied. An older `prose-style`'s checker ignores `repo_root` and takes another repository's rules with nothing to say so, so Phase 7 reads back the checker's echo of `repo_root`, which 0.4.0 returns on every run: where it is missing or names another repository, the report records the style check `DEGRADED`, saying the specs repository's rules may not have applied and that `prose-style` needs updating. `getting-started.md`'s update block now updates `prose-style` too. `release-notes-writer` was told, twice, to set its `destination` to "the matching file" from the section map, whose return schema names a section; it now says section.

**`/release-notes` says its draft is committed, as it is.** 1.1.3's Phase 11 said the run "NEVER commits the deliverable (the release-notes draft is a plain file for manual publication; the terminal step above commits only the bounded session-artifact paths in `$SPECS_PATH`)", and its invariant, like the marketplace's `CLAUDE.md`, promised "no commit of the draft". Yet `workflows-core:specs-repo-git` §2.1 lists `release-notes.md` under a specs folder among the paths the terminal `commit-artifacts` step stages — "the /release-notes draft" — and the command's own Phase 1 said that step commits it, which it did on every run that wrote the draft there, while the workflow page put the draft outside `$SPECS_PATH` altogether — "to a persistent destination you choose", beside a `$SPECS_PATH` holding "session bookkeeping only". So one command body told the operator both things about the same file, and the page a third. Phase 11, the invariant and `CLAUDE.md` now say what holds: the terminal step commits and pushes `release-notes.md` in the specs repository, with the rest of the run's artifacts and editing nothing in it; no step opens a branch or a pull request for it, and nothing commits it in a docs, code or vault repository.

`/release-notes`' report said where the Change Type came from as `(source: <imported | inferred>)`. Nothing is imported now — Phase 3 reads the PRD's own `change_type` — so a Change Type the PRD carried was reported with a provenance the run no longer has. The line now reads `(source: <PRD | inferred>)`.

**`release-notes.md` has one outline: the version at `#`, its sections at `##`, a draft's title at `###`.** 1.1.3 gave the file two. `release-note-types.md` §1 and `/release-notes` Phase 1 named the sections `## Breaking changes`, `## Feature updates` and `## Fixes`, "under" a heading for the release version, and §1 filed a declined version under `## Unreleased` — the sections' own level — while Phase 1's example put `### Feature updates` under `## 1.24.0`, and a titled draft opens with its own `### <feature title>` (§3). So a section was either a sibling of its version heading or at the level of its drafts' titles, and neither nests: a version's part of the file would end at its first section, or a section at its first draft. The section names and the draft's shape stay as they were; the version heading moves one level above the sections, `# <version>` and `# Unreleased`, which §1 now states with the reason, Phase 1's example now shows, and Phase 8's append now uses to find where a version's part and a section end.

**`/document` no longer offers a `release/*` base that nothing picks.** Phase 6.2 said "If the user picked a `release/*` branch earlier in Phase 1, use that instead", and `finish-and-handoff.md` §1 named the base "(main/master/release)". Phase 1 has never offered that pick, so the condition could not hold, but both sentences asserted a choice that does not exist. Both are gone; no pick is added.

**A proxy in the environment no longer answers for a local port.** With `http_proxy` or `ALL_PROXY` set and no `no_proxy` naming `localhost`, curl sends a request for `localhost` to the proxy, which answers it, so `/document`'s render check read the proxy's replies as its servers': a proxy's error kept the readiness poll waiting out its timeout, and the space fell to the manual table with its server up, while a proxy's `200`, where it gave one, passed a page no server had served. Every request the family makes to a local server now carries `--noproxy '*'`: the render check's probe (`render-verification.md` §2, which `/docs-serve` cites), its readiness poll and its page requests, now written as one exact command that follows a redirect (`mkdocs serve` answers a page's route without its trailing slash with a 302), and `/docs-serve`'s home-page fetch.

`/docs-profile`'s Phase 1 justified invoking the `model-routing` skill on the ground that "slash-command bodies cannot expand `${CLAUDE_PLUGIN_ROOT}`". That was verified false in a live run. The reason that survives is the one that was always doing the work: `${CLAUDE_PLUGIN_ROOT}` resolves to the *reading* plugin, so this plugin cannot read a `workflows-core` skill or reference by path whatever a command body can expand.

`/document` keyed mode adopts a docs repository whose only marker is an in-repo `.dev-workflows/docs-profile.yml` — its Phase 0 rungs (a.5) and (b) accept the profile in a signal's place — but its write-context classification counted step 2's signals alone, so that repository came out `non_docs_repo` with no confirmation ever asked, and the Phase 6.3 row for `non_docs_repo` assumes one. The classification now counts the in-repo profile too, so such a repository is a `docs_repo` and every later phase treats it as one. It is the population the shared signal set's new profile entry serves, which is how it was found.

**An agent handed a repository names it in every command.** A subagent's Bash tool starts every call in the session's directory — where the dispatching command stands — and a `cd` does not persist between calls. `diff-summarizer` wrote its Refresh step (`status --porcelain`, `fetch`, `remote set-head`, `switch`, `pull --ff-only`), Strategies 1–4 and the `gh` resolver's `cat-file`, `fetch` and `diff` bare, so each ran in the session's repository rather than the clone it was handed: the dirty-tree check and the fetch touched the wrong tree, the strategies searched it for the pull request's commits, and with `refresh.pull: true` the switch and pull targeted it — where the session stood in the docs repository mid inline-profiling, one that could move it off its bootstrap branch, so that Phase 6.3's commit landed on `main`. Every one is now `git -C "<repo_path>"`. The `gh` resolver's fallback for a refused direct-SHA fetch was `gh pr checkout`, which creates a local branch and checks it out — against the agent's own rules never to create a branch and never to move HEAD while `refresh.pull` is false, its default; it now fetches the pull request's head into a remote-tracking ref of its own, `refs/remotes/origin/pr/<n>`, which nothing checks out. `docs-style-checker` runs every rung as a subshell from the directory it names (next paragraph), `(builtin cd "<dir>" >/dev/null && …)`, so a `cd` function or alias of the user's neither runs nor prints into the output it parses, and `doc-writer`'s `Bash` use — copying a user-provided screenshot to its `dest` or `staging` path — says it copies between the absolute paths the checklist records. That use had been scoped to a `local` copy alone, although a screenshot deferred for manual upload is staged the same way, a binary copy into a directory that may not exist yet; the scope now names both destinations and the directory it creates, and it runs `command mkdir -p --` and `command cp --`, so a `cp -i` alias of the user's never asks a question nothing answers and leaves the old file in place.

**`/document` finds a site's own configuration.** Where `docs_repo_path` was a monorepo's git top level — keyed mode's rungs (a) and (b) took the top level wherever it carried a signal, a root `package.json` `docs:*` script or a `_snippets/` directory anywhere below it among them, and direct mode always took its edit target's top level — `docs-style-checker` tested `.vale.ini`, `package.json` and the markdownlint and remark configs at that top level alone, and the toolchain preflight's source 2 tested its config signals there too, so a site keeping its `.vale.ini` in `website/` was never linted by Vale — `style_check` recorded the false "no repo-level linter is configured" — and the preflight never required `vale`. Keyed mode's Phase 0 now takes whatever directory its ladder resolves up to the top level, which is right for the profile and its commands (above), so the site is carried beside it. `docs-style-checker` takes an optional `site_root`, looks there and then in each directory above it up to `repo_root`, the nearest first — the order Vale's own search takes, so a `$DOCS_PATH` naming `website/docs` finds `website/.vale.ini` — and runs each rung from the directory where it found its configuration; the profile's per-space `lint` commands still run from `repo_root`. The preflight checks the site directory and each one above it too, and `/document` passes the site it resolved to both, in either mode: keyed mode its `docs_repo_resolved`, and direct mode — which takes its edit target's git top level and had looked nowhere else — the first directory on the way up from the target that holds a docs signal, where that lies below the top level. Inline profiling is handed `docs_repo_resolved` too, and `/docs-profile`'s own Phase 0 takes that up to the same top level.

**`docs-style-checker` runs Vale without the machine's global configuration or `VALE_CONFIG_PATH`.** Vale merges the user's global configuration — `~/.config/vale/.vale.ini` on Linux — under the repository's, so a rule enabled or turned off only on the machine running the check changed what `style_check` reported: findings the repository's rules never raise, or, measured with Vale 3.21, silence on an error-level alert a clean CI runner raises. `VALE_CONFIG_PATH`, where the environment sets it, replaced the repository's configuration outright. Rung 1 now sets both aside, as a clean runner has neither, in the form `toolchain-preflight.md` §2 now defines once for every Vale run in the plugin, which the configuration decides: where it sets `StylesPath`, `unset VALE_CONFIG_PATH && vale --no-global …`; where it sets none, `unset VALE_CONFIG_PATH` and Vale run with `XDG_CONFIG_HOME` pointed at a fresh, empty directory, which hides the global file and nothing else. The form's `cd` is `builtin cd` with its output discarded, and its `mktemp`, `vale` and `rm` run as `command <name>`, `--` ending `rm`'s options, because the Bash tool's shell carries the user's aliases and shell functions: measured with Vale 3.21 under bash and zsh, a `cd` function that prints put its output ahead of Vale's, a `vale` alias adding `--no-exit` turned Vale's exit 1 into 0, and an `rm -i` alias left the temporary directory behind with its prompt in the output, answered no from the empty standard input. `builtin`, not `command`, for the `cd`: that shell is bash or zsh, and zsh runs no builtin through `command`, only a `cd` found on `PATH` — Linux has none, so `command cd` there is a command not found, and macOS has `/usr/bin/cd`, which changes only its own child process's directory, so `command cd` there exits 0 and Vale runs wherever the session stands. `--no-global` alone would not do: it also drops Vale's default StylesPath, where a configuration that sets no `StylesPath` keeps its synced packages and custom styles — a layout Vale documents as valid — so on such a repository it stops with `E100 … style '<name>' does not exist on StylesPath` where 1.1.3's rung 1 linted, while on a configuration that sets its own `StylesPath` the default path a clean runner has holds nothing and this machine's would only add its own styles. The agent's rationale said that wrapping the repository's tooling "guarantees the local check matches what CI will run on the PR"; it now says what holds — the local check applies the repository's own rules, and CI may still report otherwise, since it may pin another Vale release, sync other package versions, lint other files or pass flags of its own. Where the configuration sets no `StylesPath`, the default StylesPath that form keeps is one directory for every such project on the machine, and Vale reads with it the package configuration the latest `vale sync` into it wrote, another project's included — so such a run can still differ from a clean runner's until the repository is synced again, the limit `toolchain-preflight.md` §2 states; no check syncs to cure it.

**Vale is found by every name it reads.** Vale reads its configuration from five file names — `.vale`, `_vale`, `vale.ini`, `.vale.ini` and `_vale.ini`, the first of them in that order within one directory, a nearer directory before a farther one — while `docs-style-checker`'s first rung and the toolchain preflight's source 2 looked for `.vale.ini` alone, and so did `/document`'s docs-signal set, its Phase 0 test of whether a directory is a docs repository at all. A site configured through `_vale.ini` therefore recorded "no repo-level linter is configured", the preflight never required `vale`, and a site whose only marker it was carried no docs signal for `/document`, although Vale lints such a site as readily as any other. `toolchain-preflight.md` §2 now names the five, once, and all three look for all of them.

**A command's tool is defined once, and a leading `cd` is never it.** The toolchain preflight took a command's first token as its tool, so a command written `cd website && pnpm docs:build` — the form this release's `/docs-profile` records for a script below the top level, and one an operator writes by hand — named `cd`, a shell builtin `command -v` finds everywhere. On a host without `pnpm`, a profile whose commands all read that way passed the preflight silently, and the missing tool surfaced only after the documentation was written: at Phase 6.5 Step 1's conversion prompt for the build, and in Step 2, which booted each such server, watched it exit at once, and waited out the whole readiness timeout for it. `toolchain-preflight.md` §2 now defines a command's tool as its first token that is neither part of a leading `cd <dir> &&` nor a leading `VAR=value`, every check of a command's tool cites it — `render-verification.md`, `/document` Phase 6.5's Steps 1 and 2, and `/docs-serve` — and source 2 also looks for a lockfile in the directory a leading `cd` names. A tool containing `/` — `node_modules/.bin/vitepress` — needs no rule of its own, now that §3 asks every tool from the directory the command runs from, the top level or the leading `cd <dir>` under it: `command -v` resolves such a name against the directory it runs in, which was the session's, so from anywhere else it reported a present tool missing and the preflight prompted on a healthy container. And Step 2 checks each server's own command's tool the same way before it boots that server, a package manager with no installed dependencies counting as missing, as `/docs-serve` does before it starts one: a missing tool boots nothing for that server, sends its pages to the manual table and records `render_smoke_check` `DEGRADED`, so the check never waits out the readiness timeout for a server whose tool it knows to be missing.

**A tool check asks the shell, and the directory, that will run the tool.** 1.1.3's toolchain preflight tested every tool with `command -v <binary>` in the Bash tool's own shell, from wherever the session happened to stand. That shell carries the user's aliases and shell functions and runs them, so the test is right for a tool a gate runs there — the tool of a build or lint command, a lockfile's package manager, `markdownlint` and `remark` among them — and `toolchain-preflight.md` §3 keeps a plain `command -v` for those, run in that same shell. It is wrong for a tool the plugin runs where neither reaches, and this release runs more of them that way: every call on a process group goes through `command bash -c`, a dev server starts inside one, the port probes and page requests run `command curl`, the process reads run `command ps` and `command lsof`, and every Vale run calls `command vale`, where 1.1.3's `docs-style-checker` ran a bare `vale`. §3 asks each of those of the same child shell the start gets — `command bash -c 'builtin cd "<dir>" >/dev/null && command -v "<tool>"'` — which expands no alias of the user's and takes no shell function of theirs save one they exported, which the start would run too. So a `vale`, a `curl` or a dev server's tool that only an alias or an unexported function provides is reported missing before anything is written, rather than failing at its gate — and a `vale` that only an alias provides, which 1.1.3's first lint rung ran, no longer runs that rung, which the preflight now says. **Both tests run from the directory the command runs from** — the repository's top level, or the directory a leading `cd <dir>` names under it — where 1.1.3 asked from the session's: that is what resolves a tool a profile records as a path, `node_modules/.bin/vitepress`, the form the schema recommends for VitePress, since `command -v` resolves a name containing `/` against the directory it runs in. An alias that resolves to something that is not there passes, exactly as it passes in the shell that will expand it, and fails at the gate, which records it as it records any environmental failure; the preflight catches what it can see from where the command will run, and does not prove the end of an alias chain exists. A tool both kinds run — a package manager whose builds run in the Bash tool's shell and whose dev servers start inside `command bash -c` — takes both tests, and where they disagree the `toolchain` block carries a row for each, each row carrying the gates the commands that take **that** test power. Measured with bash 5.2 and zsh 5.8 as the Bash tool's shell, every form run as the Bash tool runs one and every tool checked against the gate that would run it: an alias-provided `mkdocs` in five shapes — an absolute path, `~/…`, `$HOME/…`, one led by `NO_COLOR=1`, and a chain through a second alias — and a function-provided one, where the own-shell test and the build it gates agree on every one; an alias-provided, a function-provided, an exported-function-provided and an absent `mkdocs`, where the child test and the boot that child shell makes agree on every one; a real `node_modules/.bin/vitepress`, which both tests find and the boot runs from a session directory that is not the repository's top level; and `command bash` and `builtin cd` holding against a `bash` alias, a `bash` function, a `cd` alias and a `cd` function. What the tests still cannot see is named in §3 where a reader meets them: a zsh **global** alias (`alias -g`) on `bash`, which is expanded in any position; an exported bash function named after a tool the run calls as `command <name>`, which reads present though `command <name>` bypasses it; a shadow on `command` itself, which defeats the prefix every read in this plugin rests on; and macOS's zsh 5.9, which is unchecked.

**The toolchain preflight predicts a gate's outcome from its fallback.** Its consequence line named each affected gate and "the outcome it will record" with no rule for choosing between `DEGRADED` and `UNAVAILABLE`, and its worked example predicted `UNAVAILABLE` for both `build_check` and `render_smoke_check` when a package manager is missing — although the ledger registers a fallback for each, the dev-server boot and the manual pages-to-visit table, and `UNAVAILABLE` means that neither the primary nor a fallback ran. The `toolchain` block now carries `fallback_for` beside `required_by` — `bash`, `curl`, `ps` off Linux, and a dev-server command's tool each name `build_check`, being the tools of the boot that stands in for a build — and a gate is predicted `DEGRADED` where its registered fallback still runs without the missing tool, `UNAVAILABLE` where it does not. `build_check`'s fallback is decided per build, by the test Phase 6.5 Step 1 makes: a build whose tool is missing keeps it only where `bash`, `curl`, `ps` off Linux, and the tool of one of that build's own servers are present, a package manager whose dependencies are not installed counting as missing (`node_modules/` absent, or for Yarn Plug'n'Play the `.pnp.cjs` absent) — so the prediction never counts a build as proved by another space's server, nor by a boot with no `curl` to probe it. A profile that records no build command at all is decided the same way, as one build that will not run whose own servers are every server, since the boot is then `build_check`'s only proof and the run records the gate `UNAVAILABLE` wherever that boot cannot run; `gate-ledger.md`'s registry says such a profile still meets the gate's precondition. The worked example now predicts `render_smoke_check` `DEGRADED` and keeps `build_check` `UNAVAILABLE`, since the boot that stands in for the build needs the same package manager. It is a prediction, and `toolchain-preflight.md` §5 calls it one and says what it assumes: no page is written at Phase 0, so it counts every server of a build, where Step 1 counts only those Step 2 boots for an affected page — and a build whose tool is missing and none of whose servers publishes an affected page records the decision taken at the preflight's prompt, where the line predicted `DEGRADED`. And the installed-dependencies signal counts a Yarn Plug'n'Play install: it looked for `node_modules/` beside a lockfile and nothing else, so a Plug'n'Play docs repository — a `yarn.lock` with a `.pnp.cjs` beside it and no `node_modules/`, Yarn 2 and later's default — was reported with its dependencies missing, every gate its package manager powers was named in the consequence line, and a healthy run was prompted to cancel. Beside a `yarn.lock`, a `.pnp.cjs` now counts as the installed dependencies it is.

**A gate row has one answer.** `gate-ledger.md` defined `UNAVAILABLE` twice, as "nothing ran and no fallback exists" in §2 and "neither the primary nor the fallback ran" in §5; both now say the latter, §5 citing §2. §2 now says what a gate records when one part `FAILED` and another only `DEGRADED` — `FAILED`, since that is what feeds the fix loop, with the degraded part in `not_run` and `ci_still_checks` — and §3's schema says `user_decision` is kept on a pre-seeded row whatever its outcome, not on `SKIPPED_BY_USER` rows alone. `/document`'s Ledger (final) gave two answers for which decision a skipped `build_check` quotes when the preflight had pre-seeded it and the user then chose Skip at Step 2; the Step 2 choice is the one that removed the build proof, and is now what the row quotes, as `render_smoke_check`'s does on a Skip. §2 also ranks `FAILED` above a decision to proceed without another part — a §5 conversion, the preflight's decision kept in its place, or a Skip that declined a build's only remaining proof — where Ledger (final) had called a row Step 1 converted "final", so a sibling build's content failure could sit behind `SKIPPED_BY_USER`. Such a row is `FAILED`, records the declined part in `not_run` and `ci_still_checks`, and keeps the decision in `user_decision`; §5, the schema and the Phase 9 table say so, and §6's checks — and `doc-reviewer`'s — apply to those fields wherever §2 requires them, a `FAILED` row with a degraded or declined part included, where they had checked `DEGRADED` rows alone. Phase 6.5's opening now says which row quotes a Step 2 Skip, and when, as Ledger (final) records it.

**`/document` commits only what lies in the docs repository.** Phase 6.3 committed every path in `doc-writer`'s `files_written`, which include the implementation-gaps draft in the PRD folder and any screenshot staged for manual upload. `git add` refuses a path outside the repository and then stages nothing at all, so on any run with a gaps draft or a deferred screenshot the documentation was never committed. Phase 6.3 now stages only the paths under the docs repository, and Phase 8.5's squash and `finish-and-handoff.md` §2 name the paths outside it — those two, and Phase 8's feedback file under `$SPECS_PATH` — as ones they never stage. What the run leaves in the resolved PRD folder is `$SPECS_PATH`'s: `workflows-core` 1.6.0's `specs-repo-git.md` §2.1 now names `pr-draft.md` and `<KEY>-implementation-gaps.md`, so the terminal `commit-artifacts` step commits both, where each had been left untracked and fired every later run's dirty-tree advisory. The run commits a screenshot staged for manual upload nowhere, it being a copy kept only until the operator uploads it, so Phase 6.3 now keeps each one it stages under `$SPECS_PATH` out of that repository's `git status`, with one anchored line in its local exclude file, and names in the report any path that line did not hide. `doc-planner`, `doc-writer` and the command's own invariant described that staging directory as one the operator named — "never inside a repo", in the planner's words, and "host-mounted" in the writer's — while Phase 1 sets it by default to the resolved PRD folder's screenshot subfolder, inside the specs repository, and asks the operator for one only where it finds no PRD folder; all three now describe it as Phase 1 resolves it, and the planner no longer says it is null wherever no screenshot was provided, since Phase 1 resolves it on every run.

**`/document`'s profile guard reads the base Phase 6.2 cuts from.** The in-repo-profile-not-on-base guard resolved its base with `symbolic-ref` and a `main`, then `master` fallback, while Phase 6.2 retried after `remote set-head origin --auto`. On a docs repository created locally and pushed, whose remote default is `develop` and whose `origin/HEAD` is unset, the guard read `main` and warned that the profile was missing from a base Phase 6.2 then did not use — or, with the profile on `main` only, passed while Phase 6.2 resolved a `develop` that lacks it. The guard now resolves `<base>` exactly as Phase 6.2 does, then reads `origin/<base>`. A dangling `origin/HEAD` — left naming a branch the remote deleted, after it renamed its default and the clone fetched with `--prune` — no longer resolves as the base: `read-only-repos` §3 counts rung 1 only where its ref exists, and Phase 6.2's `set-head` retry, and `diff-summarizer`'s, now run wherever rung 1 fails, not only where `origin/HEAD` is unset.

Phase 8.5 told the run to write the pull-request draft to the resolved PRD folder "(`ignore: legacy find $x -maxdepth 5 -type d -name "<KEY>*"`; ask if none)", the husk of a `$VAULT_PATH` lookup the vault's removal left behind. It now says "(ask if none)", as `finish-and-handoff.md` §5 already did. The command's invariants named the two PRD-folder drafts `implementation-gaps.md` and `<KEY>-pr-draft.md`, while the phases write `<KEY>-implementation-gaps.md` and `pr-draft.md`; the invariant now names what the phases write. The same invariant ended "— never anywhere else", while Phase 8.6 applies an accepted proposal to whatever file it names, `~/.claude/CLAUDE.md` and `~/.claude/memory/` among them, and the run also writes its temporary files, its `$SPECS_PATH` bookkeeping and the session-cost checkpoint — so an orchestrator holding to it would refuse a proposal the operator had accepted. It now lists every write the run makes outside the docs repository and its remote, Phase 8.6's only behind that phase's own consent.

**An ambiguous image policy is settled with the user before the writer runs.** Where a target's sibling pages carry mixed image references, or none, `doc-planner` returns `image_policy: ambiguous` with neither a `dest` nor a `staging` path. Three instructions disagreed about what came next: the planner said the writer asks the user at Phase 6.3, `doc-writer` — a subagent, which cannot ask — returned `BLOCKED` and said the orchestrator must resolve it before dispatch, and `/document` had no step that did, while its Phase 6.1 and its Phase 9 report cited a "Stage for manual upload" choice at Phase 6.3 that no phase offered. So a new page in a section whose pages carry no image, given a screenshot, reached Phase 6.3's generic `BLOCKED` loop, which re-dispatched with a free-text answer and still no path to copy the screenshot to; and since the writer blocked on any `ambiguous` target, such a page blocked the writer even with no screenshot to place. Phase 5.7 now asks, for each `ambiguous` target with a screenshot planned for it and before the checklist is approved, whether to copy the screenshots into the repository beside the page, stage them for manual upload, or leave them off the page, and hands the chosen policy to the planner's one re-invocation as `image_policy_resolution`, so the planner plans the paths as it would for a detected policy. The planner and the writer cite that step. A target with no screenshot is left `ambiguous`, and the writer places no screenshot on it; Phase 6.1 and the Phase 9 report cite the new step's "Stage for manual upload" choice. The planner's re-invocation carries `gap_resolution`, the user's answers to its "ask user" gaps, which its input contract did not list; the contract now lists it, with its shape, and the planner plans from each answer. A gap the user declines has no entry there, so the re-invocation returns it again; Phase 5.7 and the planner now say the orchestrator keeps the `"mark TODO in draft"` fallback it chose on the decline and does not ask about that gap again, and that a gap the re-invocation newly returns as "ask user" is marked TODO without being asked, since no re-invocation remains to take its answer. That re-invocation is made once, so a target it newly returns ambiguous with a screenshot is not asked about at Phase 5.7: `doc-writer`'s entry validation returns `BLOCKED` naming it, and Phase 6.3 puts that gap to the operator with Phase 5.7's own question, then plans each screenshot's path as the planner would — `dest` in the directory the target's sibling pages keep local images in, where the planner found one, else beside the page; or `staging`, with the upload note the planner wrote, its upload settled as Phase 6.1 settles one — or removes the screenshots, before its one re-dispatch; it makes that rewrite in the checklist `doc-reviewer` is handed as well as in the writer's handoff, so the reviewer judges the pages against the plan as the loop left it — a screenshot left off a page is no gap in coverage, and one copied or staged is checked as any other is. For that the planner records, on an `ambiguous` target, the directory its sample's local image references show and each screenshot's upload note. Phase 5.7, `doc-planner`, `doc-writer` and Phase 6.3 now say the same: the orchestrator settles every `ambiguous` target with a screenshot before the writer places anything, the planner is re-invoked at most once, where an answer needs it, and Phase 6.3's loop settles what that one re-invocation newly returns.

**`doc-reviewer` requires a TODO placeholder only on a screenshot deferred for manual upload.** Its Screenshots dimension required, on every `image_policy: cdn_upload_required` target, a TODO placeholder in the markdown and the staged file listed under Phase 9's `### Screenshots to upload manually`, while Phase 6.1 recommends "Upload now — I'll paste the CDN links", on which `doc-writer` writes the real CDN URL and stages and lists nothing — and Phase 7's brief carried neither the Phase 6.1 decision nor the URLs, so the reviewer could not tell the two paths apart and was instructed to flag every correctly uploaded image. Phase 7 now hands it `cdn_upload_resolutions`, one entry per such screenshot, `uploaded` with its CDN URL or `deferred`, and the dimension checks each against its own: an uploaded image by its real URL, with no placeholder and no manual-upload listing, and a deferred one by its placeholder and listing, as before. It never infers the resolution from the page, since a placeholder left on an uploaded image, or a URL on a deferred one, is what the check exists to find. `doc-planner`'s definition of the policy made the same claim of every such screenshot — staged outside the repository and listed for manual upload — and now says each is referenced by the CDN URL pasted at Phase 6.1, or, where that upload is deferred, staged and listed.

**Every `mktemp` template ends in `XXXXXX`.** `/document`'s handoff file and its claims file were named with `dw-<KEY>-XXXX.yml` and `dw-doc-claims-XXXX.md`, templates BusyBox's `mktemp` — the one Alpine ships — rejects with *mktemp: Invalid argument*, since it takes a template only where it ends in six `X`s (BusyBox 1.36.1; GNU's accepts both). They now read `dw-<KEY>-XXXXXX` and `dw-doc-claims-XXXXXX`, and the templates this release added end the same way: `/release-notes`' scratch copy, the render check's log and `/docs-serve`'s.

**A `not applicable` Change Type is drafted, as every run is, and every file says so.** 1.1.3's `release-note-types.md` §1 mapped `not applicable` to "no note is authored; the command's Phase 2 gate stops the run", and §7 and `release-notes-writer` named "the command's relevance gate" as what stops such a run. That gate had already been retired — `/release-notes` has no worthiness gate, and nothing reads `relevant_for_release_notes` — so a PRD authored with `change_type: not applicable` expected no note and got one: the writer infers the type as for an absent value (§7's second rung) and the draft is appended under the section the inference picks. §1, §7 and the writer now say that, and the command page names `not applicable` among the values that do not route. `/release-notes` Phase 6 said its Change Type confirmation "fires ONLY when `change_type` was null", which the writer's gap for a low-confidence inference of a value that does not route falsified; it now fires only where the type was inferred with low confidence — the value null, or one of §7's two values that do not route.

**An absent category label is omitted, and `release-note-types.md` §7 says so.** 1.1.3's §7 told `release-notes-writer` to infer an absent `release_notes_category` from the work's subject area and confirm it in the grill, while the writer's own step, citing the same §7, omitted the label — never inferring it, guessing it or raising a gap — as the command and its page did; the writer held two contradictory instructions, and `/create-prd` did not ask for the field, saying `/release-notes` would, which it never did. §7 now says what runs: absent, the draft carries no category line; its closing paragraph, which called every absent field a question, now ends on the fields' history — nothing returns them, so the PRD is the only place either can be authored — and restates neither rule; and its second Change Type rung and the writer's handoff reference, which said an inferred type is always confirmed, now say where the inference is low-confidence, which is when the writer raises the gap and the command asks. `/release-notes` Phase 3 carried the same sentence in 1.1.3 — the PRD "the only place either can come from" — and is narrowed with it: a Change Type the PRD does not carry is inferred (§7's second rung), so the PRD is the only place either can be *authored*, not the only place either arrives from. §3's titled render shape states the same omission rule and stated it in the retired taxonomy's vocabulary — *"when no Solution label is available"*, a term defined nowhere in the file — and now names the field §7 keys it on, `release_notes_category`.

**§3's render template names the field, not a retired taxonomy's levels.** 1.1.3's §3 rendered the category label as `**Category:** <Solution | Capability>`, the two level names of the taxonomy the import used to supply, while §7 of the same file keyed the label on the PRD's own `release_notes_category`, used verbatim, and `release-notes-writer`'s own template rendered `**Category:** <category_label>`. The template now names the field, as the writer's does.

**`/release-notes` says it never reads the PRD's `release_versions`.** 1.1.3's Phase 3 took the release from `--version` or its grill and said it "is not parsed from anything, because nothing supplies it", while `/create-prd` writes `release_versions` wherever the operator volunteers one; and the command page and `release-note-types.md` §6 both called the version "a separate field the PM sets" — §6 adding that no version belongs "anywhere", which the `#` heading the draft is filed under falsified. Phase 3 now says the PRD's `release_versions`, where `/create-prd` wrote one, is not read, and the page and §6 both that the version is the `#` heading the draft is filed under, §6's prohibition narrowed to the prose. What the run does with a version is unchanged.

**No run says it commits nothing in a working directory that is the specs repository.** 1.1.3's `/release-notes` said its terminal step "NEVER touches a code repo, or the current working directory", its invariant promised "no commit of the draft or of anything in a docs/code repo or the current working directory", and `/document`'s direct-mode invariant said it never commits "anything else in a docs/code repo or the current working directory". Where the session stands inside `$SPECS_PATH`, the terminal step commits and pushes in the working directory's own repository. Each now makes the claim only where the working directory is not the specs repository.

**No run says it writes nothing into a working directory that is the specs repository.** 1.1.3's `/document` said, in both modes, that its feedback, follow-up and final phases "NEVER write into the docs repo or the current working directory"; `/release-notes` said the same of those phases, and "NEVER write into a docs repo, a code repo, or the current working directory" of its draft; and the session-cost page said the plugin "never writes into your current working directory". Where the session stands inside `$SPECS_PATH`, those phases, and the draft's `release-notes.md` in the resolved PRD folder, write into the working directory's own repository. Each now makes the claim only where the working directory is not the specs repository.

**A run removes the temp files it made.** 1.1.3's `/document` (keyed mode) wrote `doc-writer`'s handoff file at Phase 6.3 and, on a BLOCK verdict, `doc-fixer`'s Fix Report at Phase 7, both to `mktemp` paths outside every repository and outside the specs tree, and nothing removed either: every run left them under the system's temporary directory, where no later phase and no later run looks. Phase 8 now removes both — `command rm -f -- "<path>"`, so an `rm -i` or `rm -I` alias of the user's cannot answer the prompt from an empty standard input and leave the file — and a run that stops before Phase 8 removes what it had made. A server log the render check writes already went this way.

**`/document` keyed mode says which repository it writes into.** Its opening sentence said the command "writes the output to the current working directory (a product docs repository)", which its own Phase 0 has not meant for several releases: step 2's ladder takes the working directory only where that directory carries a docs signal, and otherwise answers with `${DOCS_PATH:-/workspace/docs}` (rung a.5), a repository found under `${REPOS_PATH:-/workspace}` (rung b), or cwd or a path the operator confirms (rung c) — and step 2's closing paragraph then takes whichever it answered with to its git top level. A run started anywhere writes where the ladder answers. The sentence now names the ladder, and says that the working directory is one of its rungs rather than its destination.

### Fixed — `/document` and `/release-notes` create their temp files with `command mktemp`

`/document`'s `doc-writer` handoff file (Phase 6.3) and `doc-fixer` Fix Report (Phase 7), and `/release-notes`' scratch copy of the draft (Phase 7), were named with a bare `mktemp`, which the Bash tool's own shell resolves through any alias or shell function of that name before `mktemp` runs — so the path the run writes to and hands on is whatever that printed. All three now run `command mktemp`, as the smoke-check's own log already did.

### Fixed — a stray article in `/release-notes` Phase 2

*"before Phase 3's the folder read"* now reads *"before Phase 3's folder read"*, in a command body an agent executes in order.

### Fixed — four sentences that described a neighbouring phase loosely

`doc-planner` said `/document` Phase 5.7 settles "each such target" of its first return, where that step settles only a target whose `screenshots:` is non-empty, and that Phase 9 lists a staged screenshot's `upload_note`, where Phase 9 lists it only on the **Defer** path. `doc-reviewer`'s Inputs list did not declare `render_verification`, which Phase 7 passes it, though every other input in that dispatch was declared. And `/release-notes` Phase 7's "read the scratch file back … and remove it" sat under the auto-fix branch, so a report-only run could read the removal as that branch's, against its own invariant; the removal is now its own sentence and says it happens on every path, and the invariant, which keyed it to that same read-back, says so too.

### Fixed — the `release-notes-writer` handoff says when `change_type` is inferred

Its `change_type` field comment said "an absent value is inferred", where the agent's own hard rule and §7 rung 1 also infer for a present value that does not route. The comment now says "absent or not routable".

### Fixed — what ends `/document`'s render smoke-check is defined once and cited

The list of what ends the whole smoke-check, rather than one space's part of it, was written out wherever it was needed and went stale at one site after another. Two were still wrong: `/document`'s `render_smoke_check` ledger row missed the ending a listener the probe cannot reach adds, and it and the Phase 9 report template both told a run to name "the port or process group left running and its command" for the one ending that boots no command and writes no log — so the row either misdescribed the ending or invented a command, under a `doc-reviewer` dimension that grades it. `render-verification.md` §2 now carries **The endings** — the four, what each records, and what that record names — and every other site cites it: §5, `/document` Step 2, its Outcomes, that ledger row, its `doc-reviewer` dispatch and its Phase 9 template.

### Fixed — `release-notes-writer` calls the label by its own name

1.1.3's step 6 named the titled shapes' first line the **Context label**, the last live use of the retired `{{#context}}` macro's vocabulary under `plugins/` outside a changelog. Every other site in that agent calls it the category label — its own step 2, its rendered `**Category:**` line, its hard rules and its frontmatter — as do `release-note-types.md` §7, the command and its page. The bullet now reads **Category label**; it named step 2's value and step 7 showed the render, so no run behaved differently.

### Fixed — `/release-notes`' command page opens on a sentence that parses

1.1.3's `docs/commands/release-notes.md` opened *"…for the PM to publish wherever their release-notes field."*, a clause a de-vendoring edit truncated: it lost its verb along with the tracker field it named, and no check in `scripts/` validates a command page's opening sentence. It now ends *"wherever their release notes are published"*, the formulation the same page's **What it produces** section, the command's own opening paragraph and its frontmatter already use.

### Fixed — `/release-notes`' page describes the diff-grounding fork the command actually has

1.1.3's `docs/commands/release-notes.md` said the Phase 1 choice *"decides the folder read's `depth` before Phase 3 even runs (`prd-only` when off, `full` when on, so PR links are collected), and only Phase 4's repo resolution and Phase 5's `diff-summarizer` batches are actually skipped on the 'off' path."* Three claims, none of them the command's: `/release-notes` carries no `depth` argument and no `prd-only` or `full` value — Phase 3 reads the resolved folder inline, and `references/handoff/release-notes-writer.md` describes `folder_read` as *"the YAML the orchestrator assembled by reading the resolved folder"*, with no parameter to it — no phase collects a PR link, Phase 3 saying so where it builds the refs (*"No URL, no host classification, no `gh` requirement"*), and Phase 3's own diff-source step is skipped on the "off" path too, which is what its **only** falsified. All three are residue of the retired ticket-reader dispatch. The sentence now says what the fork decides: whether Phase 3's diff-source step runs at all, and Phase 4 and Phase 5 with it, the PRD read running either way.

### Fixed — what `/release-notes` refuses, on its page and in `CLAUDE.md`

Both said *"the run's one refusal is `RELEASE_NOTES_NEEDS_KEY`, on an address that does not resolve"*, which is wrong in both halves and contradicted the same page nineteen lines above it. `RELEASE_NOTES_NEEDS_KEY` fires on **no positional address at all**; an address that does not resolve is a different stop — the shared re-enter/cancel escalation — an ambiguous one a third, naming every match, and Phase 3 adds a fourth for a folder holding no PRD. Both sites now enumerate what Phase 0 and Phase 3 do. The point the sentence was making — that no *content* state refuses a draft — is unchanged and stands beside it.

### Fixed — `/document` no longer asks for a PR-status filter nothing reads either

The same defect its sibling carried, in the command beside it. Phase 1 asked which pull-request statuses to include — *MERGED only* / *All PRs (MERGED + OPEN + DECLINED)* / *Specific list* — and Phase 2 carried the answer into the plan as *"PR filter (MERGED only / all / specific)"*, where it stopped. Nothing read it, and Phase 4 step 1 says so in as many words: *"There is no `pull_requests[]` to filter and no PR `status` to filter on"* — the record of what was implemented is `implementation.md` and the `git log --grep` scan beside it, neither of which carries a status. Phase 5's dispatch passes `repo_path`, `repo_url_slug`, `refs`, `context`, `keys_hierarchy` and `refresh`, and never `pr_refs`, the only `diff-summarizer` input with a `status` field. It is removed rather than given a reader, for the reason its sibling's removal records: building a producer for it would mean resolving a pull-request URL, which this command's own zero-API-calls invariant forbids. The command page's example, which listed the question among the three Phase 1 asks, now names the refresh policy in its place. Phase 5's one conditional about a `pr_refs` element a run *was additionally given* is left standing: it is an inert conditional about `diff-summarizer`'s own input contract, and it costs the operator nothing, where a question costs an answer.

### Fixed — `/document` says which slugs its clone matching uses

Phase 1's repos-search-base bullet told the user that clones are located *"by matching their `git remote` against each PR's repo slug"*. Phase 4 matches each in-scope `repo` slug the implementation record and the commit scan named, there being no pull request in the resolution at all. Both the bullet and the command page's *What it needs* entry now say that, and the page's example and its direct-mode paragraph say **repo** resolution where they said PR resolution.

### Fixed — `/document` Phase 3 has one lead-in, not the same one twice

The sibling of the `/release-notes` fix below, found by the same reading and left unfixed by it. Two consecutive paragraphs both opened **"Read the resolved folder directly"** — one parenthesising the depth, the other naming what to read — in a phase an agent executes in order. They are one paragraph now, carrying both.

### Fixed — `/release-notes` Phase 3 has one lead-in, not the same one twice

Two consecutive paragraphs opened **"Read the resolved folder directly."** with different continuations, in a phase an agent executes in order. They are one paragraph now, carrying both facts: `prd.md` for the product content, and `implementation.md` beside it when diff grounding is on.

### Fixed — `/release-notes` no longer asks for a PR-status filter nothing reads

1.1.3's Phase 1 asked, whenever diff grounding was turned on, which pull-request statuses to include — *MERGED only* / *All PRs* / *Specific list*, the last of which prompts again for the list. No phase read the answer. Phase 3 builds its refs from `implementation.md` and a `git log --grep` scan, neither of which carries a status; Phase 4 resolves the slugs those two named; Phase 5 passes `repo_path`, `repo_url_slug` and `refs[]`, and never `pr_refs`, the only `diff-summarizer` input with a `status` field. The question is residue of the retired pull-request import path and is removed, rather than given a reader, because nothing in the run can produce a pull-request status to filter on. The same bullet told the user clones are matched against *"each PR's repo slug"*, where Phase 4 matches the slugs the implementation record and the commit scan named; it now says that.

### Fixed — a `refs[]` element has one shape, taken from `diff-summarizer`'s own Inputs

1.1.3's `/release-notes` and `/document` each stated the `refs[]` element shape twice and disagreed with themselves. The building step said `{repo_path, branch_from, branch_to}` and attributed that shape to the agent's Inputs; the dispatch five paragraphs later said `{branch_from, branch_to, title}`. `diff-summarizer`'s Inputs — and `references/handoff/diff-summarizer.md`, which both cite — declare `repo_path` as a **top-level** input and a `refs` element as `branch_from` / `branch_to` / `title`, so the earlier sentence was wrong on both counts and mis-attributed its shape to the authority it named. A run following it would have put the top-level input inside every element and dropped `title`. Both building steps now carry the declared shape and say where `repo_path` is passed instead.

### Fixed — `/release-notes`' Change-Type picker offers each route once

1.1.3's Phase 6 built the low-confidence Change-Type array as the proposal *plus* all three canonical routes, so whichever route was proposed was offered twice: a `Breaking change` proposal rendered option 1 and option 3 as the same Change Type under the same section, a `New technology support` proposal duplicated the Feature-update option, and a `Bug fix` proposal the Fix one. The sentence under the array asserted the opposite — *"the first option carries the proposal, so it never duplicates another"* — and cited `workflows-core:escalation-rules`' verbatim rule, so a run was both told the duplicate could not arise and forbidden from removing it. That sentence had replaced the instruction which used to handle the duplicate, *"Drop the option that duplicates the recommended one."*, in the `"Other… (describe)"` cleanup; the permitted adjustment was correctly removed and the array was never re-authored. The array now carries the three routes alone, each exactly once, and the question above it names the proposal and asks the user to confirm it — where a recommendation the array does not carry belongs. `check-docs.sh` check 12 gates an array's arity and its authored "Other", never whether two of its options resolve to the same answer, so nothing in `scripts/` saw this.

## [1.1.3] — 2026-09-09

### Added — a recorded review verdict names the version it was taken against

`/document` (keyed mode) cites the new `A recorded verdict names the version it was taken against` rule in
`workflows-core:escalation-rules`. The one-fix-cycle-plus-one-re-review cap assumes a fix cycle only
removes defects; on three live runs it **introduced** something the re-review then found, with the
budget already spent — so the run either shipped a known defect or fixed it and left the final text
unreviewed. Both end with a `PASS` on record beside a file the `PASS` never saw.

It now states what the verdict covers, and where any edit followed it, the final report says so and
names the edits. Where none did, it says that too, so a clean run reads as checked rather than as
unreported.

## [1.1.2] — 2026-09-08

### Removed

- **`/release-notes` has no worthiness gate.** Phase 2's `relevant_for_release_notes` check, its
  `RELEASE_NOTES_NOT_RELEVANT` stop and that stop's override are gone, and the phase is now
  *Plan + approval*.
  It asked a question with one answer — every PRD is relevant for release notes — so the only value it
  could carry that changed anything was one nobody should write. The run's one refusal is
  `RELEASE_NOTES_NEEDS_KEY`, on an address that does not resolve.

### Fixed

- **The command page no longer documents the gate's own retired implementation.** It said Phase 2 read
  the flag *"straight from the imported PRD frontmatter — never from the authored specs draft"* — the
  instruction the command reversed when the import was cut, which had made the stop unreachable. The
  page was not swept at the time; the paragraph is now gone with the gate it described.

## [1.1.1] — 2026-09-06

### Fixed — two more bare `doc-fixer` dispatches in `document.md`

`doc-fixer` ships from `workflows-core`, so an unqualified name resolves to nothing. An earlier sweep namespaced two dispatch sites in this file; **two of the same shape survived it** — the **BLOCK** branch and the "Manual fix notes" one-shot pass — because both carry their instruction in prose rather than in a dispatch block, where the qualified `subagent_type` lives. Both now name `workflows-core:doc-fixer`. The same shape was found and fixed in `product-workflows`'s `/epics`.

The sentence introducing the two modes also claimed they "share the same `docs-style-checker` / `doc-fixer` agents", which reads as one plugin's pair. They ship from different plugins, and it now says so.

### Documentation

`docs/reference/session-cost.md` now states the claim-namespace discipline: a deferred claim resolves only onto a command of this family, matched against a manifest of its own `<plugin>:<command>` names, and a command from another marketplace ends the open window without ever being claimable. No page in any plugin had said this.

## [1.1.0] — 2026-09-06

### Fixed — the plugin-qualified form now preloads context, where before it matched no hook

`hooks/preload-context.sh` matched `^/(document|release-notes)` only, so `/docs-workflows:document <KEY>` — the disambiguating form, and the form this plugin's own documentation teaches — injected nothing. It now matches an optional `docs-workflows:` prefix as well, and `/docs-workflows:document PRODUCT-1234` preloads specs context exactly as `/document PRODUCT-1234` does.

This matters more than a convenience for `/release-notes`: its bare form resolves to a Claude Code built-in of the same name, so the qualified form is the only one that reaches this plugin — and until now that form matched no hook at all.

The widening was made in one coordinated change across every plugin in the family that ships a `UserPromptSubmit` hook, because widening them independently is how double-injection returns. Each script accepts only its **own** plugin's name as the optional prefix, and the three command sets are disjoint, so no single prompt can match two plugins' hooks.

## [1.0.0] — 2026-09-05

### Added — the documentation half of `dev-workflows`, extracted into its own plugin

Install it with `claude plugin install docs-workflows@ihudak-plugins`. Updating the marketplace alone does **not** install it: `dev-workflows` does not declare it, because no `dev-workflows` run loads anything from it. Three commands move here from `dev-workflows` 3.26.0, and each keeps its bare name once this plugin is installed — only the namespaced form changes:

| Command | Was | Is now |
|---|---|---|
| `/document` | `/dev-workflows:document` | `/docs-workflows:document` |
| `/docs-profile` | `/dev-workflows:docs-profile` | `/docs-workflows:docs-profile` |
| `/release-notes` | `/dev-workflows:release-notes` | `/docs-workflows:release-notes` |

**Seven agents** come with them — `diff-summarizer`, `doc-location-finder`, `doc-planner`, `doc-reviewer`, `doc-writer`, `docs-style-checker` and `release-notes-writer` — dispatched as `docs-workflows:<agent>`. An agent crosses a plugin boundary for free, so every dispatch site in every plugin simply names the new namespace.

**Fourteen reference files and one bundled skill** come with them too. Twelve of the fourteen are markdown pages: `gate-ledger.md`, `repo-verification-gates.md`, `toolchain-preflight.md`, `release-note-types.md`, `finish-and-handoff.md`, the five `docs-profiles/` authoring guides, and the two `handoff/` agent contracts. The other two are data rather than prose — `default-owners.txt` and `docs-profile.default.yml` — read by `/document`, by the `docs-frontmatter` skill and by the changelog/owners hook. The `docs-frontmatter` skill ships here now; it was `dev-workflows`'s only bundled skill and that plugin now has none.

**Two hooks.** `preload-context.sh` injects specs and repo context on a `/document` or `/release-notes` prompt — it is one half of a split, `dev-workflows` keeping the half that matches `/implement`, `/epics`, `/vuln` and `/upgrade`; the two alternations are a disjoint partition of the original six, so no prompt makes both fire. `changelog-owners-reminder` moved whole rather than being duplicated, because it resolves `default-owners.txt` and `docs-profile.default.yml` under its own `${CLAUDE_PLUGIN_ROOT}` and those files are here now — left behind it would have swallowed the miss and exited 0, losing the owners check and the built-in default profile with no signal at all.

**Two declared dependencies: `workflows-core` and `prose-style`.** The first is structural — `${CLAUDE_PLUGIN_ROOT}` resolves to the *reading* plugin, so the shared reference corpus is reached through the `workflows-core:reference` loader skill rather than by path, and every command and agent here that cites one carries a preamble saying so. The second is the one **behaviour change** in the move, sanctioned as decision S12 of the split design: an unsatisfied dependency disables a plugin rather than letting it half-run, so the branches that skipped or degraded a style check when `prose-style` was absent were unreachable and are deleted. `docs-style-checker` now always runs its complementary `prose-style-checker` pass and can no longer return `NOT_CONFIGURED`; `/release-notes`'s Phase 7 style gate skips only on the user's own answer.

### Fixed — a style-check outcome no rule ever assigned

The same work found a **pre-existing** live defect, and it is worth separating from the deletion above because the deletion did not cause it. The style-check outcome mapping had two rules for a missing primary linter: `DEGRADED` when every detected rung *failed*, and `NOT_CONFIGURED` when no rung was detected **and** `prose-style` was absent. Those do not meet. A repository with no linter of its own and `prose-style` installed — the common case — landed in neither, so the `style_check` gate had no outcome to record at all. Removing the `NOT_CONFIGURED` branch did not expose the hole; it removed the last rule that mentioned the case at all, making it unavoidable. Both rules now name "detected and failed, or never detected" explicitly — the `DEGRADED` rule and the `ERROR` → `UNAVAILABLE` rule, in `/document` and in `docs-style-checker`'s own `status` definitions alike.

One consequence to know about if you read the agent's output: `complementary_linter: none` has **inverted** meaning. It used to say "no complementary pass ran; the primary carried the gate", which mapped to `RAN`. It now says "`prose-style-checker` *was* the primary", which maps to `DEGRADED`. Same token, opposite ledger consequence, in the field `/document` derives the outcome from. Nothing else consumes it.

Everything else is behaviour-preserving. This is the third increment of the marketplace split designed in `docs/superpowers/specs/2026-09-02-marketplace-split-design.md`; the first extracted `guideline-reviewers`, the second `workflows-core`. `dev-workflows` 3.27.0's changelog records the same move from its side.
