---
paths:
  - "plugins/product-workflows/**"
---

# product-workflows — invariants, workflow map, agent callers

Loaded when a file under `plugins/product-workflows/` is read. Repo-wide rules are in `CLAUDE.md`; evidence is in `docs/maintainers/rationale.md`.
