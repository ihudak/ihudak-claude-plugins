# CLAUDE.md split — verification record

Branch `iv-gu/claude-md-split`, base `d5f3034b`, plan `docs/superpowers/plans/2026-09-23-claude-md-split.md`, spec `docs/superpowers/specs/2026-09-22-claude-md-split-design.md`. Written last, after Task 11's final commit `68e5cf01`, per `CLAUDE.md` § Editing discipline, and updated by the fix wave. The sizes, the census counts and the resolutions were re-derived on the fix-wave tree; other figures are on `68e5cf01` where marked. Every figure was re-derived by the command named beside it; none is copied from the plan.

**Status: census defects resolved.** The rule census found five defects, D1–D5, listed with their resolutions under [Defects found by the census](#defects-found-by-the-census). The fix wave (commit `CLAUDE.md split: resolve census defects and deferred minors`) resolved all five, and with them the open deferred minors, D6–D10, under [Deferred minors](#deferred-minors). The re-run census reports 11 `DUP` spans, none of them a defect. One deviation stays accepted and recorded: some rationale sections are not strictly verbatim (tier-3 framing only).

## Sizes

All sizes are Python `len(text)` of the UTF-8-decoded file (`python3 -c "print(len(open(f,encoding='utf-8').read()))"`), never bytes.

Before: `CLAUDE.md` at `d5f3034b` = **189,969** characters, one file (`git show d5f3034b:CLAUDE.md`, and `$WS/base-CLAUDE.md`, both measured).

After:

| File | Characters | Tier | Loaded |
|---|---:|---|---|
| `CLAUDE.md` | 35,674 | 1 | every session |
| `.claude/rules/brd-route.md` | 16,946 | 2 | on a matching Read (`paths:`) |
| `.claude/rules/dev-workflows-tests.md` | 7,570 | 2 | on a matching Read (`paths:`) |
| `.claude/rules/dev-workflows.md` | 15,127 | 2 | on a matching Read (`paths:`) |
| `.claude/rules/docs-grounding.md` | 5,722 | 2 | on a matching Read (`paths:`) |
| `.claude/rules/docs-serve.md` | 8,033 | 2 | on a matching Read (`paths:`) |
| `.claude/rules/docs-workflows.md` | 19,908 | 2 | on a matching Read (`paths:`) |
| `.claude/rules/gates.md` | 19,400 | 2 | on a matching Read (`paths:`) |
| `.claude/rules/product-workflows.md` | 12,278 | 2 | on a matching Read (`paths:`) |
| `.claude/rules/release-notes.md` | 5,500 | 2 | on a matching Read (`paths:`) |
| `.claude/rules/workflows-core-git.md` | 13,368 | 2 | on a matching Read (`paths:`) |
| `.claude/rules/workflows-core.md` | 12,130 | 2 | on a matching Read (`paths:`) |
| `docs/maintainers/rationale.md` | 46,519 | 3 | never |
| **Total** | **218,175** | | |

Every session now loads 35,674 characters where it loaded 189,969 (18.8%). The eleven rules files hold 135,982 characters between them, each under the 20,000 warning (largest `docs-workflows.md`); `CLAUDE.md` is under the 36,000 warning and the 40,000 cap. The total grew by 28,206 characters, from frontmatter, file preambles, why-links, rationale headings and the stand-alone rewording of moved evidence (not itemised).

## Final accounting

| Measure | Command | Result |
|---|---|---|
| Move-table rows | `tail -n +2 "$WS/move-table.tsv" \| wc -l` | 185 (every non-heading block of the base inventory) |
| Rows with no destination | `awk -F'\t' 'NR>1 && $7==""' "$WS/move-table.tsv" \| wc -l` | 0 |
| `vendor-token-ok:` marked lines | `grep -rn --exclude=CHANGELOG.md 'vendor-token-ok:' plugins CLAUDE.md .claude/rules docs/maintainers \| wc -l` | 16 lines across 8 files |

Marker census per file (`grep -rc` over the same scope): `workflows-core/references/branch-naming.md` 4, `dev-workflows/commands/vuln.md` 3, `workflows-core/references/prd-format.md` 2, `dev-workflows/docs/commands/vuln.md` 1, `dev-workflows/docs/getting-started.md` 1, `CLAUDE.md` 2, `docs/maintainers/rationale.md` 2, `.claude/rules/gates.md` 1. That matches `scripts/check-docs.sh`'s check-13 header (*16 marked lines across 8 files*, kinds 8 + 2 + 1 + 5) and `.claude/rules/gates.md` § Check 13. Wrap-insensitive counts in both files: `14 marked lines across 6 files` 0, `15 marked lines across 8 files` 0, `16 marked lines across 8 files` 1. No figure needed changing in this task; Task 4 and its fix round had already moved it (14/6 → 15/8 → 16/8).

## Rule census

`$WS/census.py`, as the plan gives it: every bold span of the base `CLAUDE.md` of 12 characters or more, whitespace-collapsed, located in tier 1 (`CLAUDE.md`), tier 2 (`.claude/rules/*.md`) or else tier 3 (`docs/maintainers/rationale.md`). Run: `python3 "$WS/census.py" "$WS/base-CLAUDE.md" > "$WS/census.tsv"`.

| Outcome | Spans |
|---|---:|
| Found verbatim in exactly one tier-1/2 file | 140 |
| Found in more than one tier-1/2 file (`DUP`) | 11 |
| Found only in the rationale | 27 |
| Found nowhere verbatim (`MISSING`) | 55 |
| **Total distinct spans** | **233** |

Single-tier spans by file: `.claude/rules/brd-route.md` 16, `.claude/rules/dev-workflows-tests.md` 18, `.claude/rules/dev-workflows.md` 10, `.claude/rules/docs-grounding.md` 1, `.claude/rules/docs-workflows.md` 8, `.claude/rules/gates.md` 29, `.claude/rules/product-workflows.md` 5, `.claude/rules/release-notes.md` 2, `.claude/rules/workflows-core-git.md` 7, `.claude/rules/workflows-core.md` 1, `CLAUDE.md` 43.

### `DUP` adjudications

A `DUP` span is one the census found in two or more tier-1/2 files. A shared phrase is fine; a rule stated twice is a defect. Line numbers are on the fix-wave commit (`CLAUDE.md split: resolve census defects and deferred minors`). Before the fix wave the census had 13 `DUP` spans, three of them defects (D2, D3, D4); the fix wave removed two (`no marker and no exception`, `new or changed behaviour`), and the third (``or `RUN_FAILED` ``) is re-adjudicated below as a shared phrase. None of the 11 that remain is a defect.

| Span | Files | Verdict |
|---|---|---|
| `single source of truth` | `CLAUDE.md` and six rules files | Shared phrase: each authority paragraph opens with it. |
| `orchestrator` | `CLAUDE.md`, `dev-workflows.md`, `docs-workflows.md`, `workflows-core.md` | Shared word. |
| `implementation` | `CLAUDE.md` and seven rules files | Shared word. |
| `write-target` | `docs-workflows.md` (the `/document` keyed map line), `docs-grounding.md` (`/docs-init`'s ladder) | Shared term, two different commands. |
| `Zero direct API calls` | `docs-workflows.md` (`/document` keyed and `/epics`), `release-notes.md` (`/release-notes`) | Parallel invariants of different commands; each states its own reason. Not a restatement. |
| `single response` | `docs-workflows.md` (the docs and epics fan-outs), `dev-workflows.md` (`/implement`'s fan-out) | Different fan-outs. |
| `survivors only` | `dev-workflows.md` (`code-review` → `review-fixer`), `docs-workflows.md` (`doc-reviewer` → `doc-fixer`) | Parallel invariants of different reviewer/fixer pairs, both present in base. |
| `emitter tail` | `workflows-core-git.md` (the definition), `docs-workflows.md` (`/docs-brand --inline` runs none of it) | A defined term used once elsewhere. |
| `the run already holds` | `CLAUDE.md:95` (the resolve-against-a-known-set rule), `workflows-core.md:34` (`implementation-format`'s commit scan searches for tokens the run already holds) | The tier-2 sentence describes what one reference does in the rule's terms; it does not restate the rule. Both are in base. |
| `reviewer-fed dispatch only` | `workflows-core.md:38` (what `finding-triage` owns), `docs-workflows.md:85` (the `/document` keyed and `/epics` invariant) | An authority summary and its application to the two flows that dispatch `doc-fixer` twice. Both are in base. The two files do not load together (`workflows-core/**` against `docs-workflows/**` plus the `/epics` files). Judged the same authority-and-application pattern as `survivors only`, not a defect. The controller may rule otherwise. |
| ``or `RUN_FAILED` `` | `dev-workflows.md:64` (B099, the Phase 3.5 verification invariant), `dev-workflows-tests.md:21` (B179, the Pre-Phase 3.5 skip) | Was **Defect D3**, resolved in the fix wave. The skip's two provenances are now stated once, in `dev-workflows-tests.md:21`; `dev-workflows.md:64` points there. What still matches is the phrase in the verification invariant's own clause — which completions Phase 3.5 reaches, the gate being the recorded decision and never the baseline's `Framework` field — a rule `dev-workflows-tests.md` does not state. Shared phrase, not a restatement. |

### `MISSING` adjudications

A `MISSING` span is not in any tier verbatim. Each one is either a deletion row with its ground or a defect. For every span below, the operative content was located in other words at the place named. The usual reason a span misses is that the move rewrote it: a pointer resolved (`this file` → `CLAUDE.md`), an ordinal list renumbered (*Fifth, …* → item 5), the bold span recapitalised, or the lead-in reworded. None is a lost rule. One (#6) exposed defect D1, and one (#4) exposed defect D5.

| # | Base span (start) | Where the operative content is now |
|---:|---|---|
| 1 | It declares two dependencies, `workflows-core` and `prose-style` | `CLAUDE.md:35`: "It depends on `workflows-core` and `prose-style`, with no absent case for the second (S12)". |
| 2 | It too declares `workflows-core` and `prose-style` as dependencies | `CLAUDE.md:37` and `docs-workflows.md` § Plugin facts ("declares … as dependencies"). |
| 3 | This sentence used to call the dispatch a fallback gated on the rungs failing | History, moved to rationale#prose-style-roles with its pointer resolved. The operative rule is at `CLAUDE.md:37` (the checker dispatches `prose-style-checker` on every run) and, since the fix wave (D6), its three roles only at `docs-workflows.md:25`. |
| 4 | `/docs-serve` is the one command here that is a pure utility | `docs-serve.md:15` (the pure-utility rule, whole). `CLAUDE.md:37` keeps only the caller-list clause that binds other files. The move exposed **D5**, resolved in the fix wave. |
| 5 | This file previously claimed the opposite | History, moved to rationale#plugin-root-expansion as "`CLAUDE.md` previously claimed the opposite". The rule is `CLAUDE.md:47`. |
| 6 | Refinements the sibling re-cut paid for, and the rounds after it … | The evidence lead-in, moved to rationale#claim-expiry-sweep. The move exposed **D1**, resolved in the fix wave. |
| 7 | a phrase hit dispositions the whole paragraph, not the matched sentence | `CLAUDE.md:98` (refinement 1, recapitalised). |
| 8 | the phrase sweep is backed by an end-to-end read of every phase … | `CLAUDE.md:99` (refinement 2). |
| 9 | run an exclusivity probe as its own axis | `CLAUDE.md:100` (refinement 3). |
| 10 | Those five phrases are a starting set and not the vocabulary | `CLAUDE.md:100`: "The five are a starting set, not the vocabulary". |
| 11 | scope the sweep to `plugins/` plus the repo-root `README.md` and this file … | `CLAUDE.md:101` (refinement 4). The pointer is resolved, and the scope is widened to `.claude/rules/` and `docs/maintainers/` to follow the moved text. |
| 12 | And `plugins/` includes each plugin's `CHANGELOG.md` … | `CLAUDE.md:101`: "every `CHANGELOG.md` included" plus the ad-hoc-resolver clause. The measurement is in rationale#refinement-4. |
| 13 | Fifth, a correction fires the sweep … | `CLAUDE.md:102` (refinement 5). |
| 14 | Read the second half of that with the Sixth and the Eighth … | A cross-reference lead-in. What it pointed at is refinement 6's "never any one site's wording, neither the correction's nor the fixed site's" (`CLAUDE.md:103`). The cost evidence is in rationale#refinement-5. |
| 15 | never on any one site's wording — not the phrasing your correction introduces … | `CLAUDE.md:103`, compressed with both clauses kept. |
| 16 | Sixth, sweep the claim's *subject* … | `CLAUDE.md:103` (refinement 6). |
| 17 | Seventh, a sweep is not finished until the literal string is counted … | `CLAUDE.md:104` (refinement 7). |
| 18 | A replacement and an addition are not counted the same way | `CLAUDE.md:104` (the addition-count rule). The replacement/addition contrast is in rationale#refinement-7. |
| 19 | The two measurements behind that rule disagree … | Evidence, moved to rationale#refinement-7 as "behind the addition-count rule". The operative consequence ("a property, not a character count") is `CLAUDE.md:104`'s "enough context that the post-edit text cannot contain it". |
| 20 | Wrap-insensitively is load-bearing, not a nicety | `CLAUDE.md:104`: "Wrap-insensitive: collapse whitespace across the whole file and across the search string …". The measurement is in rationale#refinement-7. |
| 21 | Eighth, the Fifth's trigger has a sharper case … | `CLAUDE.md:105` (refinement 8). |
| 22 | The copies are not only where the rule executes. | `CLAUDE.md:105`: "the copies include the `docs/` page describing the behaviour, where a stale copy is the one **acted on**". |
| 23 | Which is why one noun across every copy is worth a little precision | `CLAUDE.md:105`: "use one noun across every copy, since the next sweep matches on it". The reason is in rationale#refinement-8. |
| 24 | That is a rule for what you write, not a licence for how you search. | `CLAUDE.md:105`: "but never search by that noun instead of the subject". |
| 25 | No case count is stated here, deliberately | `gates.md:19`: "**No case count is stated in `CLAUDE.md` or the rules files, deliberately.**" (pointer resolved). |
| 26 | Nothing gates any number written in this file. | `CLAUDE.md:92` (pointer resolved to `CLAUDE.md` or `.claude/rules/`). |
| 27 | The case is this file's own | Evidence, moved to rationale#number-gating as "The case is `CLAUDE.md`'s own". The rule is `CLAUDE.md:92`'s "count the thing and not your own rendering of it". |
| 28 | What check 11 excludes, stated because a reader who assumes otherwise … | `gates.md:65` (the three exclusions, in full). The reason clause is in rationale#check-11. |
| 29 | The widening census itself lives in exactly one place now … | `gates.md:67` ("now" dropped; pointer resolved). |
| 30 | Check 12 gates every `choices:` array's arity | `gates.md:71`: "Check 12 gates the arity rule in `CLAUDE.md` § Hard constraints". The arity itself is `CLAUDE.md:123`. |
| 31 | 14 marked lines across 6 files | `gates.md:81`: "16 marked lines across 8 files", re-derived (a stale figure fixed on the way; see Final accounting). |
| 32 | Every `choices:` array is an `AskUserQuestion` call, so the harness's schema is the authority … | `CLAUDE.md:123`. The tail clause "not the plugin's taste" is in rationale#choices-arity. |
| 33 | Two consequences outlive the cleanup. | Both consequences are stated at `CLAUDE.md:123` (free-text normalisation; the fifth option moves). The lead-in is in rationale#choices-arity. |
| 34 | A sentence is true in the context it was written in, and two kinds of edit break that … | `CLAUDE.md:106`. The dropped trailing qualifier ("whether it moved or was written that way") only widened the second kind, and the unqualified sentence covers both cases, so dropping it narrows nothing. |
| 35 | Widening the extent of a claim, a trigger or a condition puts every sentence around it … | `CLAUDE.md:107` (extent face): "every neighbouring sentence". |
| 36 | So enumerate the cases the new extent now reaches … | `CLAUDE.md:107`. |
| 37 | And the risk peaks where a reader would least expect it | Evidence, moved to rationale#sentence-context. |
| 38 | The first surface in full, because it is the clearest: | Evidence lead-in, moved to rationale#sentence-context ("The trigger surface in full"). |
| 39 | every effect the question states against the preconditions the new trigger does not carry | `CLAUDE.md:107`. |
| 40 | Measured here | Evidence marker, moved to rationale#sentence-context ("**Measured**, as an Important regression on its branch"). |
| 41 | The same shape has a prose surface … | Evidence, moved to rationale#sentence-context ("The rationale surface"). The operative check ("a rationale's neighbouring claims") is `CLAUDE.md:107`. |
| 42 | And where you do qualify, qualify by the class … | `CLAUDE.md:107`. |
| 43 | That guard serves both surfaces and is cheap | `CLAUDE.md:107`: "After adding an effect or a reason, re-read the whole paragraph it landed in …". The guard is kept for both surfaces (effect and reason). |
| 44 | The converse is a sentence that relies on context the reader may not carry … | `CLAUDE.md:108` (the pointer face). Why the guard inverts is in rationale#sentence-context. |
| 45 | Re-read such a sentence from where it lands … | `CLAUDE.md:108`. |
| 46 | One documented instance of each. | Evidence, moved to rationale#sentence-context. |
| 47 | Two instances, one check, so it stays a clause | Evidence, moved to rationale#sentence-context. The operative threshold (a third check makes it a separate rule) is `CLAUDE.md:109`. |
| 48 | Nothing here quotes `brd-intake.md` for what it says today, deliberately | Evidence about how the bullet's own instances were written, moved to rationale#sentence-context with its pointer resolved. |
| 49 | Measure the population a fix serves before designing the fix, not after. | `CLAUDE.md:111` (the full stop became " — as for a gate widening"). |
| 50 | `/docs-init`, `/docs-brand`, `/docs-serve` and `/docs-audit` resolve no docs grounding either … | `docs-grounding.md:33`, verbatim apart from the pointer: "recorded here" became "recorded in `.claude/rules/docs-grounding.md`". |
| 51 | 2–4 options, never an authored "Other" | `CLAUDE.md:123` (`minItems: 2, maxItems: 4` and no authored Other). B129 (verbatim presentation) moved to tier 1 by ruling. |
| 52 | Any kind gate must therefore test the artifact it actually cares about … | `CLAUDE.md:126` ("therefore" dropped, because the premise now follows the rule). |
| 53 | The `/plugins` interface updates installed plugins; the CLI's `marketplace update` does not. | `CLAUDE.md:195` and `:194`. Move-table B191: the second clause was dropped as a duplicate of B190, and the surviving copy was re-read. |
| 54 | Two other subcommands are worth knowing here | Deletion row B193: the lead-in went, both subcommands are at `CLAUDE.md:197`, and the `--help` verification rule survives at `:191`. |
| 55 | One key namespace, and the rule that policed the second is retired. | `CLAUDE.md:118` ("**One key namespace.**"). The retired rule is history in rationale#one-key-namespace, whose "What survives is the reason, not the rule" is the ground for not carrying it. |

### `rationale` adjudications

A `rationale` span is found only in tier 3. Each is evidence whose operative content tier 1 or tier 2 states in other words:

| Span (start) | Rationale section | Operative content |
|---|---|---|
| whole catalog | description-budget | `CLAUDE.md:116` (1024-character budget) |
| The count fell from ten when the tracker-reading agent … | id-grammar | `CLAUDE.md:117` (marker audited per file) |
| What survives is the reason, not the rule | one-key-namespace | `CLAUDE.md:118` (never widen the autolink detector) |
| 44 further matches | refinement-3 | `CLAUDE.md:100` (the five are a starting set) |
| 25 of the 67 defects … sited in a changelog | refinement-4 | `CLAUDE.md:101` (`CHANGELOG.md` included) |
| history keeps the retired form it shipped with | refinement-4 | `CLAUDE.md:101` (resolver filter), `:117` (history keeps the old form) |
| Reading the phase proves containment, not uniqueness | refinement-4 | `CLAUDE.md:101`: "that **proves containment, not uniqueness**" |
| Two instances of the miss itself … | refinement-8 | `CLAUDE.md:105` |
| one known site's wording | refinement-8 | `CLAUDE.md:103`, `:105` |
| And once prospectively … | refinement-8 | `CLAUDE.md:105` |
| Recorded once and not added to | refinement-8 | A rule about the rationale section itself, and it stays with that section |
| Copies vary in wording site by site … | refinement-8 | `CLAUDE.md:103` (sweep the subject) |
| per gated plugin | number-gating | `CLAUDE.md:92` |
| The narrow token set is evidence, not taste … | check-13 | `gates.md:83` (tracker names only, never the forge names) |
| 70 further sites, every one correct and none a defect | check-13 | `gates.md:83` |
| Its scope is every text file, not just markdown … | check-13 | `gates.md:81`, `:83` |
| All four of those figures live in … check-16 header … | check-16 | `gates.md:99` (the figures live in the check-16 header) |
| measured first | check-17 | `gates.md:105` (the structural precondition) |
| The recurrence is what bought it | check-18 | `gates.md:111` |
| Its first version used a hand-rolled fence scanner … | mermaid-gate | `gates.md` § mermaid (a real CommonMark lexer; the four fixtures) and `CLAUDE.md:122` |
| three were introduced by fixes to earlier findings | sentence-context | `CLAUDE.md:106`–`107` |
| Four surfaces, one move. | sentence-context | `CLAUDE.md:107`: "a trigger's cases, a condition's branches, a rationale's neighbouring claims" |
| report condition | sentence-context | `CLAUDE.md:107` |
| A third site kept the claim unqualified and was right to | sentence-context | `CLAUDE.md:107`: "Re-read, never qualify every match" |
| It arrived in a sentence written to repair an earlier finding | sentence-context | `CLAUDE.md:108` |
| from which the entire resolver was derivable, and had been derived. | measure-the-population | `CLAUDE.md:111`: "the fix belongs where that claim is written" |
| Match the execution phrase, not the bare name. | recipe-returns-wrong-answer | `CLAUDE.md:96` (the same sentence in lower case) |

### Defects found by the census

Five defects, all resolved in the fix-wave commit `CLAUDE.md split: resolve census defects and deferred minors`. The **Introduced** column separates defects the split created from duplication that was already in the base `CLAUDE.md`, which the split had moved verbatim without folding. Line numbers in **Defect** are as found on `68e5cf01`; those in **Resolution** are on the fix-wave commit.

| Id | Severity | Introduced | Defect | Resolution |
|---|---|---|---|---|
| D1 | Minor | Task 9 | rationale#claim-expiry-sweep (`docs/maintainers/rationale.md:173`) says the refinement list "carries no count of its own", but `CLAUDE.md:97` introduced the list with "Eight refinements:". The base list had ordinals (*First* … *Eighth*) and no count. | `CLAUDE.md:97` now reads "The refinements:"; the numbering 1–8 stays. The rationale sentence is true again and unchanged. Row B035. |
| D2 | Important | pre-existing (base B097 and B176), carried | `dev-workflows.md:62` said "`test-writer` writes tests for **new or changed behaviour** — mandatory for code changes", unbounded, while `dev-workflows-tests.md:14` states the same clause with its bound ("Only the first clause is bounded, and by exactly one state"). Both files load on `implement.md`. | `dev-workflows.md:62` is now a pointer: "The test-writing requirement — what `test-writer` must write, its one bound, and what a completion must name — is in `.claude/rules/dev-workflows-tests.md` § Test-writing requirement". The full, bounded statement stays only in `dev-workflows-tests.md:17`. Rows B097 and B176 record that the duplicate was in the base and was resolved here. |
| D3 | Important | pre-existing (base B099 and B179), carried | The Pre-Phase 3.5 skip's two provenances and their `clean_finish` consequences were stated in full in `dev-workflows.md:64` and `dev-workflows-tests.md:18`, with different reasons ("nothing was typed and nothing was declined" against "nobody consented to shipping unverified work"). | The full statement stays in `dev-workflows-tests.md:21`, the skip bullet, because the skip is test-writing behaviour, the bullet is where it is placed among the three kinds, and the file now loads on `references/code-handoff.md`, whose §2.9 is the `clean_finish` authority (D7). Both reasons are kept there in one sentence. `dev-workflows.md:64` keeps the bold lead and the steps 4–6 scoping, and points to it. Rows B099 and B179. |
| D4 | Minor | Task 4 fix round | `gates.md:89` (check 14) restated the identity-quarantine constraint ("quarantines the identity of the organisation …, has **no marker and no exception**") that `CLAUDE.md:121` states as a hard constraint. | `gates.md:89` now says check 14 "enforces the organisation-identity half of the identity quarantine, the rule in `CLAUDE.md` § Hard constraints, and it honours no marker", citing it as checks 10, 12 and 13 do, and keeps only the mechanism: the whole-repository scan and the base64 token list. Row B038. |
| D5 | Minor | Task 7 | `/docs-serve`'s pure-utility rule was stated in both tiers (`CLAUDE.md:37`, `docs-serve.md:15`), and `docs-serve.md:11` said the rule "is in `CLAUDE.md`". | `CLAUDE.md:37` keeps only the clause that binds other files: "`/docs-serve` writes no deliverable, so it is absent from every caller list, for that reason rather than by oversight; the rest of its pure-utility rule is in `.claude/rules/docs-serve.md`". `docs-serve.md:15` keeps the rule and points to `CLAUDE.md` § Active plugins for the caller-list clause; the pointer at `docs-serve.md:11` now names both homes correctly. Row B010. |

No lost rule was found: every `MISSING` span was located, and every one that the move deleted has a deletion ground in the move table.

## Move table

`$WS/move-table.tsv` rendered whole: one row per non-heading block of the base inventory (185), with the base line, block kind and size, the lead words, the destination, whether the block was split, and the notes each task recorded (pointer rewrites, deletion grounds, overlap adjudications). `|` in a cell is escaped. Figures in the notes are as the moving task measured them.

| block | line | kind | chars | section | lead | destination | split | notes |
|---|---|---|---|---|---|---|---|---|
| B002 | L3 | para | 57 | (top) | Guidance for Claude Code when working in this repository. | CLAUDE.md § (top) | no | kept in place |
| B004 | L7 | para | 358 | What this repo is | A public, open-source Claude Code plugin marketplace hosted at `github | CLAUDE.md § What this repo is | no | kept in place |
| B006 | L14 | fence | 777 | Structure | ``` .claude-plugin/marketplace.json   ← plugin catalog (do not reforma | CLAUDE.md § Structure | no | kept in place |
| B008 | L33 | para | 741 | Active plugins: dev-workflows, product-w | `plugins/dev-workflows/` provides five slash commands — `/design`, `/i | CLAUDE.md § Active plugins (≤400-char paragraph, 281 chars); .claude/rules/dev-workflows.md § Scope | yes: inventory sentence + "It depends only on `workflows-core`, and does not use `prose-style` at all." → CLAUDE.md; "Three extractions … none is coming back …" + the /epics clause → dev-workflows.md § Scope | Pointer "took content out of it" → "out of `dev-workflows`". Clause ": the one command that did (`/epics`) moved out with the rest" rewritten as its own sentence in dev-workflows.md ("The one command that did use `prose-style` (`/epics`) moved out with the rest.") because its antecedent sentence stays in CLAUDE.md. Counts re-verified on tree: 12 agents, 15 reference files (find -type f). B009's "(see above)" still points at this paragraph, which stays in CLAUDE.md. |
| B009 | L35 | para | 2617 | Active plugins: dev-workflows, product-w | `plugins/product-workflows/` provides fourteen slash commands — the id | CLAUDE.md § Active plugins (590-char paragraph); .claude/rules/product-workflows.md § Plugin facts | yes: inventory (14 commands: ladder, /epics, six-command route, two proposal commands; 14 subagents, 12 references, 1 hook, no skill, 3 env vars), the two dependencies with no absent case (S12), and "`dev-workflows` does not depend on `product-workflows`" → CLAUDE.md; route sequence with its "second /brd-split" clause and what the route does, the proposals clause with the full "gate nothing downstream" reader list verbatim, "`brd-` names the route…" and "Four of the six route commands refuse a root…" verbatim, and the S12 prose-style-checker clause → product-workflows.md § Plugin facts | Rewrites: first sentence split into a CLAUDE.md inventory sentence and two product-workflows.md sentences ("The six-command BRD-to-PRD route (…) grounds…", "The two effort-proposal commands … price … and **gate nothing downstream: …**."); "It declares two dependencies, `workflows-core` and `prose-style`**, and like `docs-workflows` it carries no absent case for the second (S12)" compressed to "It depends on `workflows-core` and `prose-style`, with no absent case for the second (S12)" to meet the brief's ≤600 budget (no constraint dropped; the docs-workflows comparison is not a rule); the S12 clause gets a lead "No absent case for `prose-style` (S12):" in product-workflows.md. DELETION: "and (see above) does not depend on `prose-style` either" — duplicate of B008's CLAUDE.md sentence "It depends only on `workflows-core`, and does not use `prose-style` at all." (re-read, still present). Counts re-verified on tree: 14 commands, 14 agents, 12 reference files (find -type f), 1 hook. Pointer "(see above)" dropped with the deleted clause. \| FIX ROUND 1: product-workflows.md paths add plugins/dev-workflows/commands/implement.md, which the clause "no command of the build ladder reads a proposal" binds. The S12 sentence in product-workflows.md now starts with the command list (lead "No absent case for `prose-style` (S12):" removed) so it does not restate the tier-1 rule's name. \| T8 FIX ROUND 1: the six route commands (`/brd-intake`, `/brd-split`, `/prd-ground`, `/brd-interview`, `/brd-package`, `/brd-reconcile`) named in the tier-1 paragraph, so every product command is assigned to its plugin in tier 1 (the B081 deletion ground needs it); product-workflows.md L21 keeps the route order and semantics, not the assignment. |
| B010 | L37 | para | 2954 | Active plugins: dev-workflows, product-w | `plugins/docs-workflows/` provides seven slash commands — `/document`, | CLAUDE.md § Active plugins; .claude/rules/docs-workflows.md § Plugin facts; .claude/rules/docs-serve.md § Run state; rationale#prose-style-roles | yes: command list + counts, deps + three-roles clause, the whole "Neither dev-workflows nor product-workflows depends…" sentence, and the /docs-serve pure-utility clause (no specs-preflight/commit-artifacts/review/maintenance, absent from caller lists) → CLAUDE.md; command descriptions (delta / cold-start trio / docs-audit), agent list, reference-count detail, bare-name + repo_root/DEGRADED dependency sentence, step-5 roles detail, "repo linter is conditional", README + step-5 authority → docs-workflows.md § Plugin facts; /docs-serve run-state parenthetical → docs-serve.md § Run state; "This sentence used to call the dispatch a fallback…" history → rationale prose-style-roles | RULING (tier test): tier-1 paragraph is 1093 chars, over the brief's ≤600, because the "Neither dev-workflows nor product-workflows depends on docs-workflows … load nothing from them" sentence binds edits to dev/product command files (tier 1), and the inventory counts are kept for parity with the three sibling Active-plugins paragraphs. Rewrites: the list sentence recast to name all seven commands in one clause; tier-1 roles clause is a summary ("dispatches prose-style-checker on every run, as the complementary, FALLBACK or SOLE style pass") of the verbatim rules-file sentence; rules-file sentences recast as standalone ("The cold-start trio is…", "`/docs-audit` enumerates…, crosses…, writes…", "The eleven subagents are…", "`docs-workflows` declares…"), no clause dropped. Pointers: "every caller list below" → "every caller list in `CLAUDE.md` and `.claude/rules/`" (referent moved); "the one command here" kept (still inside the docs-workflows paragraph); "This sentence used to call…" → rationale "The `CLAUDE.md` sentence stating `docs-workflows`' style-gate dependency used to call…". /docs-serve pure-utility run-state detail → docs-serve.md "…, which is why `CLAUDE.md` calls it a pure utility". FIX ROUND 1: number words "eleven" / "twenty-three" dropped from the docs-workflows.md Plugin facts sentences ("The subagents are…", "The reference files are twenty-one markdown pages…"), since tier 1 states the counts; one copy per number. TASK9-FIX1: the "dev-workflows offers /document…" why-clause → rationale#docs-workflows-not-a-dependency (why-link added to the kept "Neither … depends on docs-workflows" sentence); the /docs-serve pure-utility sentence → .claude/rules/docs-serve.md (its line 15 now states the no-preflight/no-commit-artifacts/no-review/no-maintenance rule and the caller-list absence verbatim; it previously pointed back at CLAUDE.md), with a one-clause pointer kept in CLAUDE.md, since the caller-list absence binds every rules file's caller lists. FIXWAVE (D5, D6): tier 1 now keeps only the clauses that bind other files — `/docs-serve` "writes no deliverable, so it is absent from every caller list, for that reason rather than by oversight" (pointer to docs-serve.md for the rest), and `docs-style-checker` "dispatches `prose-style-checker` on every run — roles in `.claude/rules/docs-workflows.md`". docs-serve.md § Run state keeps the full pure-utility rule and points to CLAUDE.md § Active plugins for the caller-list clause; its intro pointer, which said the rule was in CLAUDE.md, is corrected. |
| B011 | L39 | para | 1031 | Active plugins: dev-workflows, product-w | `plugins/workflows-core/` carries the family's shared foundation: twen | CLAUDE.md § Active plugins; .claude/rules/workflows-core.md § Plugin facts | yes: tier 1 (442 chars) = what core carries by kind, with the six family-meta commands named + the dependencies sentence compressed to "name it in dependencies; an unsatisfied dependency disables the plugin that named it rather than letting it half-run, and there is no degraded mode to fall back to"; Plugin facts = the full inventory sentence (29 refs, 5 agents named, 2 skills, scripts/, 6 commands named), the hooks sentence verbatim, and the host-installs / every-pipeline-command-loads-a-core-reference reason | Rewritten (tier-1 compression, brief-mandated ≤500): the first sentence now reads "`workflows-core` carries …" in Plugin facts; the dependencies sentence split between tiers with no restatement (tier 1 keeps the rule, tier 2 the reason). Pointers: "live here" → "live in `workflows-core`"; "declares this one" → "declares `workflows-core`". Counts re-derived: 29 files under references/ (28 .md + one data file), 5 agents, 2 skills, 6 commands, 2 hooks — all match. \| T8 FIX ROUND 1: the six commands named in tier 1 (command-to-plugin assignment is cross-cutting; over the brief's 500 chars, accepted by the controller); Plugin facts now cites "the six family-meta commands `CLAUDE.md` § Active plugins names" instead of repeating them; Plugin facts' reason sentence no longer restates "an unsatisfied dependency disables the plugin that named it" / "no degraded mode" — it keeps only the host-installs and first-phase-load reason. |
| B012 | L41 | para | 146 | Active plugins: dev-workflows, product-w | The live pipeline relies on a larger set of helper agents and workflow | CLAUDE.md § Active plugins | no | kept; Task 8's re-pointed workflow-map pointer kept. Pointer 'the taxonomy below' still true (taxonomy section follows). |
| B013 | L44 | para | 34 | Active plugins: dev-workflows, product-w | **Internal reference convention:** | CLAUDE.md § Active plugins › Internal reference convention | no | kept in place |
| B014 | L45 | item | 333 | Active plugins: dev-workflows, product-w | - Agents are invoked by `subagent_type` (`<plugin>:<agent>`, e.g. `dev | CLAUDE.md § Active plugins › Internal reference convention | no | kept in place |
| B015 | L46 | item | 579 | Active plugins: dev-workflows, product-w | - **A shared reference is cited `workflows-core:<name>` and loaded thr | CLAUDE.md § Active plugins › Internal reference convention | no | kept in place |
| B016 | L47 | item | 911 | Active plugins: dev-workflows, product-w | - Inside **agent** and **skill** bodies (and `hooks.json` / MCP / moni | CLAUDE.md § Active plugins › Internal reference convention; rationale#plugin-root-expansion | yes: "verified in a live run… all 33 literal tokens… absolute paths", the "substitution is specific… arrived literal in the same body" sentence, and "This file previously claimed the opposite… the claim is retired, the skill is not" → rationale (pointer "This file" → "`CLAUDE.md`"); tier 1 keeps the expand rule, the two literal variables, the real-location sentence and the skill-stays clause | Rewrites: "The substitution is specific to this variable…: … arrived **literal** in the same body" → "No other variable expands there: … arrive **literal**" ("the same body" pointed at the live run, which moved to rationale); "the claim is retired, the skill is not" → "The `model-routing` skill stays, although the claim once given as the reason it exists is retired." TASK9-FIX1: "The model-routing skill stays…" → rationale#plugin-root-expansion. |
| B017 | L48 | item | 445 | Active plugins: dev-workflows, product-w | - Slash **commands** that need their own plugin's bundled content invo | CLAUDE.md § Active plugins › Internal reference convention | no | kept in place |
| B018 | L51 | para | 172 | Active plugins: dev-workflows, product-w | **When editing any of these plugins:** update the files under `plugins | CLAUDE.md § Active plugins | no | kept in place |
| B020 | L56 | item | 62 | Adding a new plugin | 1. Create `plugins/<name>/` with `.claude-plugin/plugin.json`. | CLAUDE.md § Adding a new plugin | no | kept in place |
| B021 | L57 | item | 68 | Adding a new plugin | 2. Add content directories (`commands/`, `agents/`, `hooks/`, etc.). | CLAUDE.md § Adding a new plugin | no | kept in place |
| B022 | L58 | item | 84 | Adding a new plugin | 3. For hooks: create `hooks/hooks.json` using `${CLAUDE_PLUGIN_ROOT}` | CLAUDE.md § Adding a new plugin | no | kept in place |
| B023 | L59 | item | 85 | Adding a new plugin | 4. Register in `.claude-plugin/marketplace.json` with `"source": "./pl | CLAUDE.md § Adding a new plugin | no | kept in place |
| B024 | L60 | item | 82 | Adding a new plugin | 5. Commit and push to `main`. Claude Code picks up changes on next syn | CLAUDE.md § Adding a new plugin | no | kept in place |
| B026 | L64 | item | 108 | Conventions | - Agent `.md` files must start with YAML frontmatter (`---`) containin | CLAUDE.md § Conventions (new short list, directly after Adding a new plugin) | no | whole; old ## Conventions heading removed. B029 pointer "the Internal reference convention above" still true. |
| B027 | L65 | item | 58 | Conventions | - Hook scripts must exit 0 — they must never block Claude. | CLAUDE.md § Conventions (new short list, directly after Adding a new plugin) | no | whole; old ## Conventions heading removed. B029 pointer "the Internal reference convention above" still true. |
| B028 | L66 | item | 101 | Conventions | - `hooks.json` `matcher` field (for PostToolUse) goes at the entry lev | CLAUDE.md § Conventions (new short list, directly after Adding a new plugin) | no | whole; old ## Conventions heading removed. B029 pointer "the Internal reference convention above" still true. |
| B029 | L67 | item | 405 | Conventions | - Plugin content references its own bundled files via `${CLAUDE_PLUGIN | CLAUDE.md § Conventions (new short list, directly after Adding a new plugin) | no | whole; old ## Conventions heading removed. B029 pointer "the Internal reference convention above" still true. TASK9-FIX1: reduced to a pointer ("Bundled and shared files are reached only as the Internal reference convention above says."). Deletion ground: duplicate — every clause (own files via ${CLAUDE_PLUGIN_ROOT} or a resolving skill; shared via the loader, never by path; no hardcoded data/ paths) is in B014–B018, re-read. |
| B030 | L68 | item | 88 | Conventions | - MIT license applies to all plugins unless a plugin directory has its | CLAUDE.md § Conventions (new short list, directly after Adding a new plugin) | no | whole; old ## Conventions heading removed. B029 pointer "the Internal reference convention above" still true. |
| B031 | L69 | item | 821 | Conventions | - **A plugin `description` is a stable capability blurb, never a chang | CLAUDE.md § Hard constraints; rationale#description-budget | yes: rule sentences (blurb, 1024/900, whole-catalog, replace-never-append, CHANGELOG) → CLAUDE.md verbatim; 'Claude Code enforces no limit…' and '2788 chars…' → rationale | pointer 'this kept shipping' in rationale rewritten 'over-long blurbs kept shipping' (referent lost on move) TASK9-FIX1: "The limit is GitHub Copilot CLI's… fail to install or update." → rationale#description-budget. |
| B032 | L70 | item | 2430 | Conventions | - **Every requirement ID a plugin doc teaches is the bracketed `[PREFI | CLAUDE.md § Hard constraints; .claude/rules/gates.md § ID-grammar gate; rationale#id-grammar | yes: rule, Jira reason clause (with its vendor-token-ok marker), gate + run-before-push, CHANGELOG exclusion + id-grammar-ok marker + audited-per-file → CLAUDE.md; on-push/--selftest design, exit-code-not-enough (restated in present tense), validate-catalog --selftest, marker census kinds + recipe, spec-id-baseline → gates.md; SM-C deletion story, 'five such lines across five files' count, 'count fell from ten' → rationale | split sentence 'enforces it: run … before pushing, and it runs on every push…' at the comma: first half CLAUDE.md, second half gates.md (capitalised 'It runs'). 'now carries … for the same reason' kept in rationale, gates.md drops 'now'. Census recipe intro reworded 'Re-derive the census with' (was 're-derive it with'). Census count five/five verified unchanged on this tree; no as-of mark needed. Marker kept on the CLAUDE.md line, which still names the tracker. TASK9-FIX1: "A dash-separated ID has the shape of a Jira issue key… on export." → rationale#id-grammar (with its own vendor-token-ok marker); CLAUDE.md line's marker dropped (no tracker name remains). Census re-derived: 16 lines across 8 files, unchanged; check-docs.sh check-13 header per-kind text updated (the ID-grammar explanation now in rationale); gates.md figure unchanged. |
| B033 | L71 | item | 1325 | Conventions | - **One key namespace, and the rule that policed the second is retired | CLAUDE.md § Hard constraints; rationale#one-key-namespace | yes: "and the rule that policed the second is retired" (lead) + "The rule that stood here forbade widening… cannot occur." + "What survives is the reason, not the rule" → rationale (pointer "stood here" → "stood in `CLAUDE.md`"); rest compressed in tier 1 | Compressed: "Nothing is looked up anywhere, because there is no tracker…" → "; nothing is looked up anywhere"; "reads like a key validator and is not one — it is an autolink detector" → "is an autolink detector, not a key validator"; "Widening it would break it." → "never widen it"; "A user who keeps a tracker records its identity in…" → "A user's own tracker identity lives in…". Every constraint kept. vendor-token-ok marker kept on the line (names Jira). |
| B034 | L72 | item | 1274 | Conventions | - **Resolve an identifier against a known set; never parse one out of | CLAUDE.md § Editing discipline; rationale#resolve-against-a-known-set | yes: "All five sites of 3.3.0's…" and the "Candidates come from the set… reads PRODUCT-1234-2 out of…" sentence + "is a key nothing in the tree ever asserted" → rationale | and every match is a guess that… → "is a guess that…"; "is the worked example of the fix" → "is the worked example". |
| B035 | L73 | item | 15890 | Conventions | - **A note saying a feature or an edge does not ship is a claim with a | CLAUDE.md § Editing discipline (root + 8 numbered refinements); rationale#claim-expiry-sweep with ### refinement-1…8 | yes: root rule + one operative item per refinement → CLAUDE.md; every measured case, count, commit/round story, the 25/67 changelog measurement, the test-notify/test-baseliner measurements, the wrap-survival figures, sweep.py, the between-the-three story, and the three refinement-8 instances → rationale subsections verbatim (pointer rewrites only) | Compressed to operative clauses per brief. Scope (ref 4) now names CLAUDE.md, .claude/rules/ and docs/maintainers/ where the base said "this file". Ref 3 keeps the five phrases and the full noun list verbatim and names the verification record. Ordinal pointers "the Seventh/Sixth/Eighth/Fourth" → "refinement 7/6/8/4"; "the scope above" → "refinement 4's scope". Explanatory clauses cut from tier 1 (why a probe finds hidden claims; check 10 leaves the root README alone; changelog is most of what an entry is; containment-not-uniqueness explanation; replacement vs addition explanation; "write your own and keep the method"; copies-vary explanation) moved to the matching rationale subsection. Rationale pointers: "this file"/"this very document" → "`CLAUDE.md`"; "this rule exists for" → "refinement 7 exists for"; "this refinement" → "refinement 7/8" or "this rationale section". TASK9-FIX1: R4 CHANGELOG-quotation elaboration → rationale refinement-4 (base wording restored there), keeping "Check any ad-hoc resolver that copies the gates' CHANGELOG.md filter."; R5's subject clause merged into R6 ("never any one site's wording, neither the correction's nor the fixed site's"); R7's line-grep undercount clause → rationale refinement-7. M2: base wording restored in rationale claim-expiry (refinement-list sentence, "this list" → "the refinement list"), refinement-4 containment paragraph and /document mode sentence, refinement-7 last paragraph ("either", "rather than before it", "on the list"). FIXWAVE (D1): "Eight refinements:" → "The refinements:" — the base carried ordinals and no count, and rationale#claim-expiry-sweep says the list carries no count of its own; numbering kept. |
| B036 | L74 | item | 2504 | Conventions | - **Each plugin's human-facing documentation lives in `plugins/<name>/ | CLAUDE.md § Hard constraints; .claude/rules/gates.md § scripts/check-docs.sh; rationale#docs-tree; page totals → plugin rules files (Tasks 5–8) | yes: docs location, README-is-topics-not-facts, check-docs enforces + run-before-push → CLAUDE.md; PLUGIN_RELS iteration, on-push --selftest shape, no case count stated, grep -c recipe → gates.md; six-defects finding, case-count reason, 37-vs-39 history → rationale; page totals → Tasks 5–8 | PAGE TOTALS (verbatim, for Tasks 5–8 to place in each plugin's rules file): `dev-workflows` carries 20 pages — `docs/README.md` (the index), `getting-started.md`, `workflow.md` and `roles-and-phases.md`, 5 per-command pages under `docs/commands/`, and 11 reference pages under `docs/reference/` — with the plugin README reduced to a role-indexed pointer table; `product-workflows` carries 28 — the same four top-level pages plus `brd-workflow.md`, 14 command pages and 9 reference pages; `workflows-core` carries 16 — the same four top-level pages, 6 command pages and 6 reference pages; `docs-workflows` carries 20 — `README.md`, `getting-started.md`, `workflow.md` and `docs-workflow.md` (the route page, which states the whole procedure from an empty repository to a populated portal, manual steps written out), 7 command pages and 9 reference pages. \| Pointers: 'No case count is stated here' → 'in `CLAUDE.md` or the rules files'; 'a count here' → 'a count there' (then moved to rationale as 'a count'); 'needs no copy here' → 'needs no copy'; 'This sentence asserted 37' → 'The `CLAUDE.md` sentence that stated it asserted 37'. \| FIX ROUND 1: 'Adding a command, agent, reference file, hook, skill, or environment variable without its docs entry fails the build.' (from B038) moved from gates.md to the end of this block's CLAUDE.md hard constraint. |
| B037 | L75 | item | 730 | Conventions | - **Run the gates as one `&&` chain and read the chain's own exit code | CLAUDE.md § Running the gates | no | moved whole, verbatim, as a paragraph (bullet marker dropped). Pointer 'nothing needs copying here': referent still CLAUDE.md, unchanged. TASK9-FIX1: trailing-echo mechanism + "one round did five times" + "append nothing" → rationale#gate-chain-exit; replaced by "A trailing `echo \"EXIT=$?\"` makes the invocation's own status 0, so read the printed value." "so nothing needs copying here" dropped. |
| B038 | L76 | item | 22781 | Conventions | - **Nothing gates any number written in this file.** `check-docs.sh` c | CLAUDE.md § Editing discipline; .claude/rules/gates.md (### Check 1…19, ### No stop-routing check); rationale#number-gating, #check-10…#check-19, #stop-routing-no-check | yes: nothing-gates-a-number, check-9 note, counterpart/hand-held numbers, re-derive + prefer citation, count-the-thing → CLAUDE.md Editing discipline; 'Nineteen checks:' list split into ### Check 1–9 (one clause each), 'And no page…' → Check 10, 'And every choices: option…' + excludes + not-widened + census-lives-in-one-place → Check 11, 'Adding a command… fails the build' → gates.md check-docs intro, Check 10 word-boundary + identity quarantine → Check 10, check 12/13/14/15/16/17/18/19 operative sentences → their subsections, stop routing → ### No stop-routing check; evidence ([4]-[6] count-off-prose case; 38 failures; census history; three-live-arrays; stop-suffix measurements 7/11/14; check-13 13-sites/70-forge/17-files/marker-convention history/six-of-fourteen/older-filter story; check-14 zero-sites + added-after-review; check-15 demo + /frames; check-16 trade, three-form load-bearing detail, whole-plugin widening refusal, scope measurement, second-copy history; check-17 PS15 ledger, 3-for-3, verified-green; check-18 fire-on-both reason, 47050554 recurrence, earlier-widenings; check-19 hundred-files sweep, clear-text reason) → rationale | Pointers: 'written in this file' → 'in `CLAUDE.md` or `.claude/rules/`'; 'none is in CLAUDE.md' → '…or `.claude/rules/`'; 'this file's inventory numbers' → 'their'; 'prompts the edit here' → 'in `CLAUDE.md` or the rules file'; 'every other number here' → 'in them'; 'this gate's own check count' → '`check-docs.sh`'s own check count'; 'in the sections below' dropped (counts now span files); 'touch this file' → 'touch either'; 'The case is this file's own … from this file' → '`CLAUDE.md`'s own … from `CLAUDE.md`', 'this branch' → 'a branch'; 'the same file records it' → '`workflows-core:next-phase-offer` records it'; 'is not restated here' → 'in `CLAUDE.md` or the rules files'; 'this sentence used to carry' → 'The `CLAUDE.md` sentence stating the rule'; 'this paragraph's point' → 'the point'; 'this very sentence' → 'any line quoting the filter' (gates) / 'the `CLAUDE.md` sentence describing it' (rationale); 'this file's own resolve-against-a-known-set rule' → '`CLAUDE.md`'s own'; 'this file's checks 8 and 11' → 'checks 8 and 11'; 'the only check in this file' → 'in `check-docs.sh`'; 'whose one instance here' → dropped 'here'; 'this file's earlier widenings' → 'earlier widenings of `check-docs.sh`' (rationale); 'here, the organisation' → 'in this edition, the organisation'; 'this paragraph's own rule' → 'the rule stated with check 11'. STALE FIXES: check-13 scope sentence rewritten to Task 2's widened scope (CLAUDE.md, .claude/rules/*.md, docs/maintainers/*.md); 'The scope stops at the plugin' → 'at the plugin and the repo-root instruction tiers'; marker census recipe widened to `plugins CLAUDE.md .claude/rules docs/maintainers`; census figure 14/6 → re-derived 15 marked lines across 8 files (also in scripts/check-docs.sh check-13 header, with 'six of the fourteen' → 'fifteen' and instruction-file rationale kind 3 → 4, naming the gates.md line that matches the recipe without naming a vendor); check 1/2 subsections state Task 2's widened link/anchor scope and the inline-code-span hazard (new true sentence). REFUSED WIDENINGS AS RULES: check 11 (verbatim rule kept), check 13 token set 'not widened to the git-forge names; do not re-propose', check 16 'not widened to the whole plugin', check 18 'not widened to that form'. Reasons moved to rationale to hold gates.md ≤20k: forks reason, gate-a-fork-must-disable, forge-vs-tracker, check-13 leak argument, check-14 clear-text reason (gates keeps 'never in clear text'), check-15 hurry reason, check-17 runtime-impossible reason, check-18 fire-on-both reason, check-19 clear-text clause, check-11 'stated because…shipped defect'. The 'Nineteen checks' count word is not restated (the subsections carry it). vendor-token-ok marker on the B038 line (sanctioning [28]'s tracker names) travels to rationale#check-13's line holding [28]. \| FIX ROUND 1 (controller ruling, spec §4): (1) vendor-neutrality rule added to CLAUDE.md § Hard constraints as its own bullet (rewritten from the check-13 scope sentence: under `plugins/` or the instruction tiers, marker on the line or opening fence, CHANGELOG.md exempt, check 13 enforces; why → #check-13); the operative clause removed from gates.md check 13, which keeps scope, census and cannot-see. Census re-derived: 16 marked lines across 8 files (the new CLAUDE.md line writes the marker literal) — gates.md and check-docs.sh header updated ('six of the sixteen'; instruction-file rationale kind 4 → 5). (2) Identity quarantine added to CLAUDE.md § Hard constraints: the verbatim '**Identity quarantine:** …linking out' sentence joined by '; check 10 enforces it' (why → #check-10), plus a new sentence for check 14's half (organisation never named, no marker, no exception; why → #check-14); removed from gates.md check 10, which keeps token derivation, scope and word-boundary detail and now says 'The rule it enforces is in `CLAUDE.md` § Hard constraints'. (3) gates.md check 19 'encoded for check 14's reason' → 'encoded, never in clear text, for check 14's reason' + why-link #check-14. (5) 'Adding a command… fails the build' moved to B036's CLAUDE.md line. Checks 15 and 17 stay in gates.md: gated before push, and specific to editing scripts/ (edit-area-specific), so not tier 1. (6) gates.md check 12 opener → '**Check 12 gates the arity rule in `CLAUDE.md` § Hard constraints.**' (the tier-1 B040 line states the rule); parser and cannot-see detail kept. (7) 'Widen the scope, not the count, when the corpus moves.' moved next to the census recipe. (8) Rationale evidence restored to base wording: check-10 ¶1 verbatim except 'it' → 'the identity quarantine' (stitch: the two sentences reordered so 'Check 10 enforces…; before that check existed…' precedes 'The binding reason is forks'); check-10 ¶2 verbatim [14]; check-12 verbatim [17] with 'Its parser' → 'Check 12's parser'; check-15 verbatim [44]+[45] ([46] unchanged); check-16 ¶3 verbatim [52]-tail (clause from 'no file outside core cites…', capitalised 'No') + [54] + [55] with pointers 'this sentence' → 'the `CLAUDE.md` sentence describing check 16', 'this paragraph's own rule' → 'the rule stated with check 11'; check-17 last sentence restored verbatim [62] first clause ('That is 3-for-3 today' + as-of mark; stitch: ends at 'this decays.', the cannot-catch clause being operative in gates.md) and [63] verbatim with 'this file's checks 8 and 11' → 'checks 8 and 11'. TASK9-FIX1: check-9 description ("`check-docs.sh` check 9 gates seven sentences… held by hand alone.") → rationale#number-gating; "Re-derive all of them" → "Re-derive every number in them" (pointer). Identity quarantine: "which is why it carries the install commands inline instead of linking out" → rationale#check-10. FIXWAVE (D4): gates.md § Check 14 no longer restates the identity-quarantine rule; it cites "the rule in `CLAUDE.md` § Hard constraints" as checks 10, 12 and 13 do, and keeps the mechanism (honours no marker, whole-repository scan, base64 token list). |
| B039 | L77 | item | 3388 | Conventions | - **Every ```` ```mermaid ```` block in every tracked markdown file mu | CLAUDE.md § Hard constraints; .claude/rules/gates.md § Mermaid gate; rationale#mermaid-gate | yes: must-parse sentence (first clause) + quote-labels rule with example + enforced-by → CLAUDE.md; lexer/tracked-files/fixture exclusion, no-regex rule + fixtures, never runs old scanner, failure location by content, no-guess, unclosed fence, selftest shape, cannot-see, local run → gates.md; nothing-parsed-mermaid-until, product-workflows 3.0.0 incident, hand-rolled scanner history, replay-never-complete reason + second-version story, headless-render calibration → rationale | 'for each of those four' → 'for each of the four constructs a hand-rolled fence scanner got wrong' (referent moved to rationale). Failure-location reason replaced in gates.md by 'never by replaying mermaid's own rewrites of the text' (operative residue of the moved reason). 'calibrated against a real headless render of this tree' → 'of the repository's tree'. TASK9-FIX1: "because GitHub draws each one… where it does not" → rationale#mermaid-gate. |
| B040 | L78 | item | 1938 | Conventions | - **Every `choices:` array is an `AskUserQuestion` call, so the harnes | CLAUDE.md § Hard constraints; rationale#choices-arity | yes: schema-is-authority, 2–4 / no Other, five-option rejected, escalation-rules §0 + check 12, free-text normalised-or-re-asked in the four customer-authority pickers (D14), fifth option moves to prose (next-phase-offer overflow, epic-picker The cap) → CLAUDE.md; retired Other-convention history, census-in-check-12-header reason, 'Two consequences outlive the cleanup', six pickers used to omit → rationale | '§0 of that file now states the real one' → '`workflows-core:escalation-rules` §0 states the rule' (referent lost). 'First, the free-text option is now unconditional: … cannot do that any more — so a free-text answer…' → 'The free-text option is unconditional, so the four pickers … cannot omit it — a free-text answer…'. 'Second, a fifth option' → 'A fifth option'. 'cited rather than restated here' → 'in `CLAUDE.md`' (rationale). \| FIX ROUND 1: rationale 'saying the opposite' → 'saying the opposite of the harness schema (2–4 options, no authored Other)' (referent lost on move). TASK9-FIX1: evidence clauses ("not the plugin's taste"; "A five-option array is not a long prompt…"; "(the three that used to omit it, and /brd-reconcile's…)"; "which is where D14's protection now lives"; "(already that file's universal minimum)"; "the case no static check can see…") → rationale#choices-arity. B129 folded in as the bullet's last sentence. All operative clauses kept: 2–4, no authored Other, free text normalised or re-asked never written through, fifth option → prose / epic-picker cap. |
| B041 | L79 | item | 8145 | Conventions | - **A sentence is true in the context it was written in, and two kinds | CLAUDE.md § Editing discipline (root + extent face + pointer face + third-check rule); rationale#sentence-context | yes: root sentence + the operative guards → CLAUDE.md; the risk-peaks count, the four-surfaces detail, the /brd-intake trigger regression, the pass9a-1 rationale instance, d2cdba1a/9a5599a9, the brd-figures and 62c4a552/19ea31f3 pointer instances, and the "nothing here quotes brd-intake.md" audit note → rationale | Every guard kept: enumerate the new extent's cases; check every effect against the new trigger's preconditions; qualify by class not member; re-read never qualify every match; re-read the whole paragraph; pointer + omitted-subject re-read; the this <file-kind> tell (with "never a substitute"); third check → workflows-core reference. Pointers: "the root stays here" → "stays in `CLAUDE.md`"; "The Eighth sweep refinement above" → "Refinement 8 of the claim-expiry sweep"; "this bullet cites" kept (the bullet). "Measured here… on this branch" → "Measured… on its branch" in rationale; "The first surface in full" → "The trigger surface in full". TASK9-FIX1: "Refinement 8 covers the distant copies…" + "The risk peaks…" → rationale#sentence-context (base wording of the Eighth-refinement sentence); I1: "A face that needs a check of its own is a rule of its own" restored before the third-check clause; M3: "this bullet cites" → "the sentence-context bullet cites". |
| B042 | L80 | item | 439 | Conventions | - **A sub-project's verification record is written last** — after the | CLAUDE.md § Editing discipline; rationale#verification-record-last | yes: the 2026-08-07 sentences and "two of that round's wrong values propagated exactly that way" → rationale | — |
| B043 | L81 | item | 1558 | Conventions | - **Measure the population a fix serves before designing the fix, not | CLAUDE.md § Editing discipline; rationale#measure-the-population | yes: the pre-rename resolver story → rationale; "since a defect class assumed to be uniform routinely is not" and "so the rule is not over-applied" → rationale | Pointer: "This file already requires a gate widening to be measured" → "— as for a gate widening" (the widening rule lives in gates.md now). |
| B044 | L82 | item | 1304 | Conventions | - **A rule's drift risk is not proportional to how obvious the rule lo | CLAUDE.md § Editing discipline; rationale#drift-risk | yes: the prd-source-resolution story → rationale | — |
| B047 | L88 | para | 195 | Command, agent, and skill taxonomy | User-facing slash commands live under `commands/`. They own the end-to | CLAUDE.md § Command, agent, and skill taxonomy | no | kept in place (not in the brief table; stays tier 1) TASK9-FIX1: taxonomy reflowed from four hard-wrapped paragraphs under ### headings into four one-line bullets (Commands, Agents, Skills, Working rule); every clause kept, sentence shapes merged; ### headings removed. |
| B049 | L94 | para | 265 | Command, agent, and skill taxonomy | Helper agents live under `agents/`. They are Claude Code sub-agent sys | CLAUDE.md § Command, agent, and skill taxonomy | no | kept in place (not in the brief table; stays tier 1) TASK9-FIX1: taxonomy reflowed from four hard-wrapped paragraphs under ### headings into four one-line bullets (Commands, Agents, Skills, Working rule); every clause kept, sentence shapes merged; ### headings removed. |
| B051 | L101 | para | 267 | Command, agent, and skill taxonomy | Optional reusable guidance lives under `skills/`. A skill is neither a | CLAUDE.md § Command, agent, and skill taxonomy | no | kept in place (not in the brief table; stays tier 1) TASK9-FIX1: taxonomy reflowed from four hard-wrapped paragraphs under ### headings into four one-line bullets (Commands, Agents, Skills, Working rule); every clause kept, sentence shapes merged; ### headings removed. |
| B053 | L108 | para | 143 | Command, agent, and skill taxonomy | Commands orchestrate. Agents execute bounded tasks. Skills provide reu | CLAUDE.md § Command, agent, and skill taxonomy | no | kept in place (not in the brief table; stays tier 1) TASK9-FIX1: taxonomy reflowed from four hard-wrapped paragraphs under ### headings into four one-line bullets (Commands, Agents, Skills, Working rule); every clause kept, sentence shapes merged; ### headings removed. |
| B055 | L113 | para | 106 | Model routing reference | `plugins/workflows-core/references/model-routing/classification.md` is | CLAUDE.md § Model routing reference | no | kept whole |
| B056 | L116 | item | 88 | Model routing reference | - Task complexity classification (`SIMPLE` / `MODERATE` / `SIGNIFICANT | CLAUDE.md § Model routing reference | no | kept whole |
| B057 | L118 | item | 96 | Model routing reference | - The model fallback chain (Opus 5.5 → 5 → 4.8 → 4.7 → 4.6 → Sonnet 5 | CLAUDE.md § Model routing reference | no | kept whole |
| B058 | L119 | item | 42 | Model routing reference | - The mandatory Opus code-review checklist | CLAUDE.md § Model routing reference | no | kept whole |
| B059 | L120 | item | 75 | Model routing reference | - The `model_routing` YAML handoff block shared between commands and a | CLAUDE.md § Model routing reference | no | kept whole |
| B060 | L121 | item | 67 | Model routing reference | - The `phase: verify-resume` protocol for review-gated verification | CLAUDE.md § Model routing reference | no | kept whole |
| B061 | L122 | item | 376 | Model routing reference | - The large-input scan fan-out policy (§8): the input-shape trigger, t | CLAUDE.md § Model routing reference | no | kept whole TASK9-FIX1: "(its adopters are named in §8.5's own Opt-in paragraph, not copied here)" → rationale#model-routing-fan-out, why-link added. |
| B062 | L124 | para | 1448 | Model routing reference | Twenty-six commands invoke the `workflows-core:model-routing` skill an | .claude/rules/workflows-core.md § Model routing callers | no | Unwrapped to one line. Pointer: "must load and follow this file" → "must load and follow `plugins/workflows-core/references/model-routing/classification.md`" (the file named by the § Model routing reference block that stays in CLAUDE.md). "§4's list" left as is — §4 of that file, now named in the same sentence. Count re-derived: `grep -l 'workflows-core:model-routing' plugins/*/commands/*.md` = 26. |
| B064 | L137 | para | 585 | Source-truth reference | `plugins/workflows-core/references/source-truth.md` is the **single so | .claude/rules/workflows-core.md § Authorities | no | verbatim; tier test: describes the reference, whose rules bind its consumers at run time through the loader — index: Task 9 |
| B065 | L139 | para | 875 | Source-truth reference | `plugins/docs-workflows/references/release-note-types.md` is the **sin | .claude/rules/release-notes.md § Authority | no | RULING (Step 2 split): release-note-types authority goes with /release-notes to release-notes.md, whose paths: include references/release-note-types.md. |
| B066 | L141 | para | 2459 | Source-truth reference | `plugins/workflows-core/references/docs-grounding.md` is the **single | .claude/rules/docs-grounding.md § Authority | no | RULING (size + tier test): docs-grounding authority and its invariants split out of docs-workflows.md into a new docs-grounding.md (third split: docs-workflows.md was ~24k after the docs-serve and release-notes splits). paths: = workflows-core/references/docs-grounding.md, workflows-core/agents/docs-grounder.md, the nine consumer command files (idea, create-prd, update-prd, create-ard, specify, brd-intake, epics, release-notes, prd-ground) and the eleven commands the paragraph records as resolving none (document, brd-split, brd-interview, brd-package, brd-reconcile, prd-proposal, brd-proposal, docs-init, docs-brand, docs-serve, docs-audit), because its rules bind those files — the re-litigation guard binds docs-init/brand/serve/audit, "stated here and not in its own body" binds brd-split. One tier-2 copy loaded on every governed file, not tier 1 (Task 6 precedent for widened paths). docs-workflows.md's workflows-core/references/docs-grounding.md glob removed and its Task-3 ruling paragraph amended accordingly. Re-litigation guard kept operative with its reasons. Pointers: "stated here and **not** in its own body" → "stated in `.claude/rules/docs-grounding.md` and…"; "recorded here so it is not re-litigated" → "recorded in `.claude/rules/docs-grounding.md` so…". FIX ROUND 1: docs-workflows.md split paragraph "consumers in four plugins" → "three plugins" (workflows-core, product-workflows, docs-workflows). |
| B067 | L143 | para | 608 | Source-truth reference | `plugins/workflows-core/references/prose-formatting.md` is the **singl | .claude/rules/workflows-core.md § Authorities; rationale#recipe-returns-wrong-answer | yes: "this sentence named nine and missed /prd-proposal and /brd-proposal, which is why it now carries the recipe instead." → rationale (rewritten there to name the CLAUDE.md sentence); rest verbatim, "lists them;" → "lists them." + why-link | tier test: "never hard-wrap prose" binds plugin output, and the reference itself is loaded by every consumer — stays tier 2; index: Task 9 |
| B068 | L145 | para | 776 | Source-truth reference | `plugins/dev-workflows/references/bug-diagnosis.md` is the **single so | .claude/rules/dev-workflows.md § Authorities | no | whole paragraph, verbatim. index: Task 9 |
| B069 | L147 | para | 3397 | Source-truth reference | `plugins/workflows-core/references/specs-repo-git.md` is the **single | .claude/rules/workflows-core-git.md § Authorities; rationale#recipe-returns-wrong-answer | yes: "**Match the execution phrase, not the bare name.** The recipe that stood here … the next reader trusts what it returns." → rationale (pointer "stood here" → "stood in CLAUDE.md's specs-repo-git paragraph"); why-link after "…five of workflows-core's six)."; rest verbatim incl. the Hard rules sentence | Rule line for Task 9 (Editing discipline). Count re-derived: grep -l 'commit-artifacts` entry point' = 29; bare grep = 30 (the evidence still holds). Tier ruling: see Specs-repo git invariants rows — hard rules stay tier 2 in the git split, whose paths: widen to plugins/*/commands/*.md. |
| B070 | L149 | para | 1403 | Source-truth reference | `plugins/workflows-core/references/implementation-format.md` is the ** | .claude/rules/workflows-core.md § Authorities | no | verbatim; index: Task 9 |
| B071 | L151 | para | 2289 | Source-truth reference | `plugins/workflows-core/references/phase-handoff.md` is the **single s | .claude/rules/workflows-core-git.md § Authorities | no | verbatim. Counts re-derived by execution phrase `phase-handoff handoff-to-main` (15) / `phase-handoff require-on-main` (13): /idea and /frames execute handoff-to-main by a different phrase, /brd-interview gates via its require-on-main text — 17 / 14 stand. Overlaps B168 (inherits four rules, diverges on three); both kept — B168 adds that handoff-to-main opens a PR and says outright its widening is "not redundancy to strip". |
| B072 | L153 | para | 1936 | Source-truth reference | `plugins/dev-workflows/references/code-handoff.md` is the **single sou | .claude/rules/dev-workflows.md § Authorities | no | whole paragraph, verbatim. index: Task 9. The brief attributes the §2.8→heading citation history to B072, but B072 carries none; that history is in B086 and went to rationale#code-handoff-citation from there. §2.1–§2.12 numbers re-verified against code-handoff.md headings. Pointers "here the work…", "whereas here the run…" refer to the code repo within the paragraph — unchanged. |
| B073 | L155 | para | 1904 | Source-truth reference | `plugins/workflows-core/references/read-only-repos.md` is the **single | .claude/rules/workflows-core-git.md § Authorities | no | verbatim except pointer: "`/prd-ground` cites this file directly" → "cites `read-only-repos.md` directly" (in a rules file "this file" would read as the rules file) |
| B074 | L157 | para | 580 | Source-truth reference | `plugins/docs-workflows/references/gate-ledger.md` is the **single sou | .claude/rules/docs-workflows.md § Authorities | no | verbatim. B076 names /docs-serve but is not /docs-serve-only, so it stays in docs-workflows.md (docs-serve.md paths: include references/toolchain-preflight.md, so it does not need a copy). |
| B075 | L159 | para | 420 | Source-truth reference | `plugins/docs-workflows/references/repo-verification-gates.md` is the | .claude/rules/docs-workflows.md § Authorities | no | verbatim. B076 names /docs-serve but is not /docs-serve-only, so it stays in docs-workflows.md (docs-serve.md paths: include references/toolchain-preflight.md, so it does not need a copy). |
| B076 | L161 | para | 2364 | Source-truth reference | `plugins/docs-workflows/references/toolchain-preflight.md` is the **si | .claude/rules/docs-workflows.md § Authorities | no | verbatim. B076 names /docs-serve but is not /docs-serve-only, so it stays in docs-workflows.md (docs-serve.md paths: include references/toolchain-preflight.md, so it does not need a copy). |
| B077 | L163 | para | 654 | Source-truth reference | `plugins/workflows-core/references/doc-structure-conventions.md` is th | .claude/rules/workflows-core.md § Authorities | no | verbatim; index: Task 9 |
| B078 | L165 | para | 2670 | Source-truth reference | `plugins/workflows-core/references/finding-triage.md` is the **single | .claude/rules/workflows-core.md § Authorities; rationale#recipe-returns-wrong-answer | yes: " — this list omitted the two proposal commands until that grep was run against it." → rationale; rest verbatim + why-link | named here pointer: referent is the paragraph itself, unchanged. Tier test: the attachment rule binds commands in three plugins, but it is enforced by the reference each consumer loads; tier-1 index line: Task 9 |
| B079 | L167 | para | 679 | Source-truth reference | `plugins/workflows-core/references/instruction-file-maintenance.md` is | CLAUDE.md § Editing discipline | no | TIER OVERRIDE (brief said workflows-core.md): "binding on hand edits to this file" binds edits to CLAUDE.md and the rules files, which no paths: glob can scope (CLAUDE.md is always loaded, never "read") — tier 1. Moved whole as a bullet. Pointers: "binding on hand edits to this file" → "to `CLAUDE.md` and `.claude/rules/*.md`"; "this plugin's own `references/*.md`" → "the `references/*.md` of `workflows-core` and of every plugin that depends on it" (the reference's own scope sentence). Task 9: the Shared-authorities index entry for instruction-file-maintenance must point at § Editing discipline, not restate it. TASK9-FIX1: restructured to one compact bullet: authority + scope + consumer + four rules; "a rewrite that narrows a rule is a deletion and is itemised separately" dropped as duplicate of the drift-risk bullet's last sentence (re-read). New "Prose is executed" line (spec §4) added before it. |
| B081 | L171 | para | 984 | Workflow relationships across `dev-workf | Every command below ships from `dev-workflows` except: the fourteen pr | .claude/rules/workflows-core.md § Workflow map (last sentence only); deletion (the rest) | yes: last sentence → workflows-core.md § Workflow map preamble, rewritten to "In the `## Workflow map` blocks of `.claude/rules/*.md`, agents marked … like every other dispatch in the family." ("in this tree" → "in the family"; lead-in names where the markers now sit); the rest deleted | Deletion ground (re-read after T8 fix round 1): every command-to-plugin assignment survives in tier 1, CLAUDE.md § Active plugins — dev-workflows' five, product-workflows' fourteen (ladder, /epics, the six route commands, both proposal commands — all named), docs-workflows' seven and workflows-core's six are named there. The "four logging commands named at the foot of the map" are named there too (workflows-core's six), and their map note is in workflows-core.md § Workflow map. |
| B082 | L173 | fence | 33317 | Workflow relationships across `dev-workf | ``` /implement           → [require-on-main: in-scope specification.md | .claude/rules/dev-workflows.md; .claude/rules/product-workflows.md; .claude/rules/brd-route.md; .claude/rules/docs-workflows.md; .claude/rules/docs-serve.md; .claude/rules/release-notes.md; .claude/rules/workflows-core.md | yes | T5: /implement, /vuln, /upgrade, /design, /ready command lines + └── test-baseliner, test-writer, risk-planner, code-review, design-reviewer, interface-designer, readiness-reviewer → .claude/rules/dev-workflows.md § Workflow map (alignment kept; other dev agents — review-fixer, upgrade-*, vuln-* — have no tree line in the base) \| T6: /idea, /create-prd, /update-prd, /create-ard, /specify, /epics, /prd-proposal, /brd-proposal command lines + └── epic-reviewer, epic-writer, prd-reviewer, ard-reviewer, spec-reviewer, idea-reader, figure-reader, proposal-reviewer → .claude/rules/product-workflows.md § Workflow map; /brd-intake, /brd-split, /prd-ground, /brd-interview, /brd-package, the "⋯ the customer reviews the bundle off-platform…" line, /brd-reconcile and " BRD route (tail)" + └── brd-reader, code-grounder, design-grounder, grounding-verifier, brd-package-reviewer, customer-review-reader → .claude/rules/brd-route.md § Workflow map (Step 2 split). Lines whole, alignment kept; the tail line's §7.2 design-spec history (git show 62e791e8:…) kept operative. All 14 agents under plugins/product-workflows/agents/ have a tree line (ls-checked). \| T7: /document (direct), /docs-profile, /docs-init, /docs-brand, /docs-audit, /document (keyed) command lines + └── doc-reviewer, doc-location-finder, doc-planner, docs-style-checker, docs-scaffold-reviewer, docs-auditor, ia-planner, docs-audit-reviewer → .claude/rules/docs-workflows.md § Workflow map; /docs-serve line → .claude/rules/docs-serve.md § Workflow map; /release-notes line → .claude/rules/release-notes.md § Workflow map (Step 2 splits). Lines whole, alignment kept. diff-summarizer, doc-writer, release-notes-writer have no tree line in the base (ls-checked: 11 agents under plugins/docs-workflows/agents/). FIX ROUND 1: the brief gave docs-serve.md the glob plugins/docs-workflows/references/render-verification.md, which matches no file; the file is plugins/docs-workflows/references/docs-profiles/render-verification.md — glob corrected, and docs-serve.md's intro now names it `docs-workflows:docs-profiles/render-verification`. Every glob in docs-workflows.md, docs-serve.md, release-notes.md and docs-grounding.md verified to match ≥1 tracked file. \| T8: /frames (core) line + └── doc-fixer, code-scanner, frame-describer, docs-grounder, impl-maintenance (all 5 agents under plugins/workflows-core/agents/, ls-checked) → .claude/rules/workflows-core.md § Workflow map, lines whole. The "All twenty-nine in-scope commands additionally run specs-preflight…" line: OVERLAP WITH B093 (CLAUDE.md § Hard constraints) — its specs-preflight/commit-artifacts clause, phase list and "(workflows-core:specs-repo-git)" cite deleted as restatement (surviving copy B093, re-read); the rest moved out of the fence into a paragraph after it: "`/feedback`, `/prompt`, `/prompt-brainstorm`, and `/prompt-grill-me` run specs-preflight and commit-artifacts like every other command that writes into $SPECS_PATH (CLAUDE.md § Hard constraints), and have no line of their own in the workflow map because they are single-purpose logging commands rather than pipelines." + the cost-emission (§7/§13) and ceding (§4) sentences verbatim ("this map" → "the workflow map"). Fence emptied; fence and the ## Workflow relationships heading deleted. Step 2 script: checked 65, one line prints 0 — this trimmed line; its verbatim remainder ("All four emit…" to end) found in exactly one rules file (workflows-core.md). No base fence line unplaced. |
| B084 | L243 | para | 60 | Key invariants | Key invariants enforced by all three code-oriented commands: | .claude/rules/dev-workflows.md § Invariants (as ### sub-heading) | no | paragraph became the heading "### Key invariants enforced by all three code-oriented commands" |
| B085 | L245 | item | 209 | Key invariants | - Branch created before any file is touched (`feat/<slug>` or equivale | .claude/rules/dev-workflows.md § Invariants ### Key invariants enforced by all three code-oriented commands | no | verbatim |
| B086 | L246 | item | 1398 | Key invariants | - The work is COMMITTED on that branch before the run ends — prompt-fr | .claude/rules/dev-workflows.md § Invariants ### Key invariants enforced by all three code-oriented commands; rationale#code-handoff-citation | yes: bullet verbatim → dev-workflows.md up to "**cited by heading rather than by number**", closing ")" + why-link; ": it read §2.8 from 3.10.0 … to the wrong section" → rationale | rationale "above it" → "above that section" (referent lost on move). The "made symmetric because …" reason kept operative (it names the reverse case the rule covers). |
| B087 | L247 | item | 78 | Key invariants | - Opus review gate runs **before** tests for `SIGNIFICANT` / `HIGH-RIS | .claude/rules/dev-workflows.md § Invariants ### Key invariants enforced by all three code-oriented commands | no | verbatim |
| B088 | L248 | item | 386 | Key invariants | - `code-review`'s findings are triaged by the orchestrator before `rev | .claude/rules/dev-workflows.md § Invariants ### Key invariants enforced by all three code-oriented commands | no | verbatim |
| B089 | L249 | item | 83 | Key invariants | - `review-fixer` handles BLOCKER findings; only one `review-fixer` cyc | .claude/rules/dev-workflows.md § Invariants ### Key invariants enforced by all three code-oriented commands | no | verbatim |
| B090 | L250 | item | 526 | Key invariants | - Every file path these commands hand to `risk-planner`, `code-review` | .claude/rules/dev-workflows.md § Invariants ### Key invariants enforced by all three code-oriented commands | no | verbatim |
| B091 | L251 | item | 224 | Key invariants | - A reviewer handed `claims_file` reads it ONLY after every other dime | .claude/rules/dev-workflows.md § Invariants ### Key invariants enforced by all three code-oriented commands | no | verbatim |
| B092 | L252 | item | 95 | Key invariants | - `workflows-core:impl-maintenance` runs post-batch to update KB, `CLA | .claude/rules/dev-workflows.md § Invariants ### Key invariants enforced by all three code-oriented commands | no | verbatim |
| B093 | L253 | item | 457 | Key invariants | - Every command that writes into `$SPECS_PATH` runs `specs-preflight` | CLAUDE.md § Hard constraints | no | TIER TEST → tier 1: binds all 29 commands that write into $SPECS_PATH across all four plugins, not only the three code-oriented ones, so a dev-workflows rules file would not load for most editors it binds. Moved verbatim as the last Hard-constraints bullet. Overlap note for Tasks 8/9: B082's fence line "All twenty-nine in-scope commands additionally run `specs-preflight`…" and B173 (canonical terminal order) restate parts of it; whoever places those should cite this bullet rather than restate it. TASK9-FIX1: trailing period added. |
| B094 | L255 | para | 45 | Key invariants | Key invariants for `/implement` specifically: | .claude/rules/dev-workflows.md § Invariants (as ### sub-heading) | no | paragraph became the heading "### Key invariants for `/implement` specifically" |
| B095 | L257 | item | 407 | Key invariants | - Phase 4.6 (`finish-code-branch`) runs **after** Phase 4, never befor | .claude/rules/dev-workflows.md § Invariants ### Key invariants for `/implement` specifically | no | verbatim |
| B096 | L258 | item | 76 | Key invariants | - Test baseline captured **before** any source edits, using `test-base | .claude/rules/dev-workflows.md § Invariants ### Key invariants for `/implement` specifically | no | verbatim |
| B097 | L259 | item | 90 | Key invariants | - `test-writer` writes tests for **new or changed behaviour** — mandat | .claude/rules/dev-workflows.md § Invariants ### Key invariants for `/implement` specifically | no | verbatim FIXWAVE (D2): the duplicate was in the base — this /implement invariant and the test-writing section (B176) both stated the first clause, this one unbounded. Resolved here: the bullet is now a pointer to `.claude/rules/dev-workflows-tests.md` § Test-writing requirement, which keeps the one full, bounded statement. |
| B098 | L260 | item | 589 | Key invariants | - If no test framework is detected — or if every suite the capture act | .claude/rules/dev-workflows.md § Invariants ### Key invariants for `/implement` specifically | no | verbatim |
| B099 | L261 | item | 1703 | Key invariants | - Phase 3.5 verifies against the captured baseline before the workflow | .claude/rules/dev-workflows.md § Invariants ### Key invariants for `/implement` specifically; rationale#implement-phase-3-5 | yes: bullet → dev-workflows.md minus ' — which is the honest form of the "full test suite is verified" this line used to claim, and which three reachable completions denied' (replaced by ":") + why-link; that clause → rationale | pointer "this line used to claim" → "that invariant used to claim in `CLAUDE.md`" in rationale. FIXWAVE (D3): the skip's two provenances were stated in full here and in B179 — a duplicate already in the base. Resolved here: the full statement stays in dev-workflows-tests.md (B179), which loads with /implement, the test agents and `references/code-handoff.md` (whose §2.9 is the clean_finish authority); this bullet keeps the bold lead and points there. Its reason "nothing was typed and nothing was declined there" was folded into B179's sentence so neither wording is lost. |
| B100 | L262 | item | 218 | Key invariants | - Multi-source input (more than one repo, or any directory input — an | .claude/rules/dev-workflows.md § Invariants ### Key invariants for `/implement` specifically | no | verbatim |
| B101 | L263 | item | 190 | Key invariants | - The fan-out reads the resolved folder and runs per-repo `code-scanne | .claude/rules/dev-workflows.md § Invariants ### Key invariants for `/implement` specifically | no | verbatim |
| B102 | L264 | item | 490 | Key invariants | - A theme round 1 leaves inconclusive — `partial`/`absent`/`error`, or | .claude/rules/dev-workflows.md § Invariants ### Key invariants for `/implement` specifically | no | verbatim |
| B103 | L265 | item | 92 | Key invariants | - A referenced directory that is missing or unrecognized is surfaced, | .claude/rules/dev-workflows.md § Invariants ### Key invariants for `/implement` specifically | no | verbatim |
| B104 | L267 | para | 45 | Key invariants | Key invariants for `/document` (direct mode): | .claude/rules/docs-workflows.md § Invariants (### heading) | no | lead-in became the sub-heading "### Key invariants for `/document` (direct mode)". |
| B105 | L269 | item | 584 | Key invariants | - **No branch and no commit, ever** — not "not by default", and there | .claude/rules/docs-workflows.md § Invariants › /document (direct mode) | no | verbatim. Pointer "never the files edited here" — referent (direct mode) unchanged. |
| B106 | L270 | item | 85 | Key invariants | - **No `test-baseliner`, no `test-writer`, no `code-review`** — docs-o | .claude/rules/docs-workflows.md § Invariants › /document (direct mode) | no | verbatim. |
| B107 | L271 | item | 540 | Key invariants | - **No `doc-reviewer` gate** — direct mode is deliberately lightweight | .claude/rules/docs-workflows.md § Invariants › /document (direct mode) | no | verbatim. Pointers "no … Opus review here" (direct mode) and "the next edit above it" (generic) — referents unchanged. |
| B108 | L272 | item | 285 | Key invariants | - Style-check findings are fixed via `doc-fixer`; with no reviewer gat | .claude/rules/docs-workflows.md § Invariants › /document (direct mode) | no | verbatim. |
| B109 | L273 | item | 57 | Key invariants | - Mixed code + docs changes must use `/implement` instead | .claude/rules/docs-workflows.md § Invariants › /document (direct mode) | no | verbatim. |
| B110 | L275 | para | 57 | Key invariants | Key invariants for `/document` (keyed mode) and `/epics`: | .claude/rules/docs-workflows.md § Invariants (### heading) | no | lead-in became the sub-heading "### Key invariants for `/document` (keyed mode) and `/epics`" plus a one-line pointer to the tier-1 verbatim-presentation rule. Section kept whole in docs-workflows.md per the Task 3 ruling (paths: include the /epics files); the /epics map line stays in product-workflows.md (Task 6). |
| B111 | L277 | item | 96 | Key invariants | - `/document` (keyed mode) and `/epics` are distinct top-level command | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B112 | L278 | item | 711 | Key invariants | - **Zero direct API calls** — neither command ever calls a forge's RES | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B113 | L279 | item | 129 | Key invariants | - Parallel agent invocation: all diff summarizers (docs flow) or code | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B114 | L280 | item | 68 | Key invariants | - Branch setup happens **before** writing output files — never after | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B115 | L281 | item | 604 | Key invariants | - Branch policy: `/epics` never branches. `/document` classifies its w | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B116 | L282 | item | 82 | Key invariants | - `doc-location-finder` (docs flow) identifies write targets before wr | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B117 | L283 | item | 90 | Key invariants | - `doc-planner` (docs flow) synthesizes PRD content + diffs into a doc | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B118 | L284 | item | 493 | Key invariants | - `docs-style-checker` + `doc-fixer` lint prose after writing, before | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B119 | L285 | item | 213 | Key invariants | - For epics, `prose-style-checker` is the sole style checker and runs | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. Overlaps product-workflows.md's "/epics, /create-prd, and /update-prd all run prose-style-checker unconditionally…" (B009, Task 6) but states the /epics style-gate role in this shared section; kept per the Task 3 ruling that the section stays whole. |
| B120 | L286 | item | 187 | Key invariants | - Description-vs-source discrepancies are escalated in Phase 5.8 (neve | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B121 | L287 | item | 303 | Key invariants | - A bug-report draft (`<KEY>-implementation-gaps.md`) is written to th | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B122 | L288 | item | 641 | Key invariants | - Review gate is `doc-reviewer` (docs flow) or `epic-reviewer@Opus` (e | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B123 | L289 | item | 278 | Key invariants | - The re-review is handed the `doc-fixer` Fix Report as `claims_file`, | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B124 | L290 | item | 186 | Key invariants | - Sub-agents return `DIRTY_TREE` / `REFRESH_BLOCKED` when a **writable | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B125 | L291 | item | 394 | Key invariants | - Every written claim must be traceable to the originating key plus th | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B126 | L292 | item | 1267 | Key invariants | - Writes never touch `_archive/`, and neither command confines its wri | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B127 | L293 | item | 166 | Key invariants | - (docs flow) Phase 0 runs the toolchain preflight after profile resol | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B128 | L294 | item | 199 | Key invariants | - (docs flow) Every gate in the `gate-ledger.md` registry appends its | .claude/rules/docs-workflows.md § Invariants › /document (keyed) and /epics | no | verbatim. |
| B129 | L295 | item | 436 | Key invariants | - A phase's `choices:` array is presented verbatim — order, wording, a | CLAUDE.md § Hard constraints (new bullet after the choices-arity bullet) | no | RULING (tier test): the verbatim-presentation rule binds every command in the family (workflows-core:escalation-rules "Choice lists are presented verbatim": "This rule binds every command in the family"), so it is lifted to tier 1. The arity clause ("carries 2–4 options, never an authored Other") is dropped as a restatement of the tier-1 choices-arity bullet (surviving copy re-read: "`minItems: 2, maxItems: 4`, and … no 'Other' option"). Rewrite: "in every command of the family, not only `/document` and `/epics`" added; "The two rules are one rule" → "The two rules are one rule — verbatim presentation and the arity rule the previous bullet states:". STALE FIX: citation "`workflows-core:escalation-rules` §0" named the arity section; the verbatim rule is in that file's unnumbered "Choice lists are presented verbatim" section, now cited by heading. FIX ROUND 1: positional "the arity rule the previous bullet states" → "the arity rule in the bullet beginning **Every `choices:` array is an `AskUserQuestion` call**". TASK9-FIX1: folded into the B040 bullet as one sentence keeping order/wording/(Recommended) and "every command of the family"; "not only /document and /epics", "The two rules are one rule…" and the retired-convention clause → rationale#choices-arity. docs-workflows.md line 72's pointer ("presented verbatim binds every command and is in CLAUDE.md") still true. |
| B130 | L297 | para | 36 | Key invariants | Key invariants for `/release-notes`: | .claude/rules/release-notes.md § Invariants (### heading) | no | RULING (Step 2 split): /release-notes invariants, map line and B065 moved to release-notes.md with the brief's paths:. Lead-in became "### Key invariants for `/release-notes`". |
| B131 | L299 | item | 314 | Key invariants | - **Zero direct API calls** — the run has no forge URL to resolve in t | .claude/rules/release-notes.md § Invariants | no | verbatim. |
| B132 | L300 | item | 312 | Key invariants | - The draft is the **authored body only** — for a titled destination a | .claude/rules/release-notes.md § Invariants | no | verbatim. |
| B133 | L301 | item | 321 | Key invariants | - The **Category:** label IS the PRD's own `release_notes_category`, u | .claude/rules/release-notes.md § Invariants | no | verbatim. |
| B134 | L302 | item | 315 | Key invariants | - The Summary is shaped per its destination (breaking → present tense, | .claude/rules/release-notes.md § Invariants | no | verbatim. |
| B135 | L303 | item | 860 | Key invariants | - The run has **no worthiness gate**, and that is a retirement rather | .claude/rules/release-notes.md § Invariants; rationale#release-notes-worthiness | yes: "no worthiness gate…retirement rather than an omission", retired-from-prd-format / no command reads it / inert, and the refusal list → release-notes.md; "it once read … relevant_for_release_notes … one nobody should write" history → rationale | Rewrite: ": it once read …. It is retired from" → ": `relevant_for_release_notes` is retired from" (the pronoun lost its antecedent when the history moved). |
| B136 | L304 | item | 217 | Key invariants | - A deprecation carries a deprecation note in the Summary — end-of-lif | .claude/rules/release-notes.md § Invariants | no | verbatim. |
| B137 | L305 | item | 208 | Key invariants | - NEVER writes into a docs repo; the draft's one destination, `release | .claude/rules/release-notes.md § Invariants | no | verbatim. |
| B138 | L306 | item | 852 | Key invariants | - Light gate only — `prose-style-checker` (optional, skipped only on t | .claude/rules/release-notes.md § Invariants | no | verbatim. |
| B139 | L307 | item | 91 | Key invariants | - Diff grounding is opt-in; when on, it reuses `$REPOS_PATH` resolutio | .claude/rules/release-notes.md § Invariants | no | verbatim. |
| B140 | L309 | para | 114 | Key invariants | Key invariants for the PRD-creation flow (`/idea`, `/create-prd`, `/cr | .claude/rules/product-workflows.md § Invariants (as ### heading + lead paragraph) | yes: lead-in → heading "### Key invariants for the PRD-creation flow (`/idea`, `/create-prd`, `/create-ard`, `/specify`)"; new lead paragraph names where the /design, /ready, ARD-respect and phase-finished bullets live | Brief: /design and /ready dropped from the parenthetical because Task 5 moved their bullets to dev-workflows.md § Invariants ### /design and /ready; the lead paragraph says so. The ARD-respect (B149) and phase-finished (B158) bullets stay in CLAUDE.md for Task 9, now directly after the /release-notes list with no lead-in of their own (transient; Task 9 moves both to Hard constraints). The lead paragraph says they "are in `CLAUDE.md`" (true before and after Task 9). |
| B141 | L311 | item | 218 | Key invariants | - Each authoring command is gated by its own Opus reviewer (`prd-revie | .claude/rules/product-workflows.md § Invariants ### Key invariants for the PRD-creation flow | no | verbatim. TIER TEST: names /design (dev) — kept in tier 2; product-workflows.md paths add plugins/dev-workflows/commands/design.md and ready.md so the rule loads where it binds (design-reviewer, readiness-reviewer). |
| B142 | L312 | item | 352 | Key invariants | - Only `/idea`'s embedded grill is **bounded** (≤10 questions; `--deep | .claude/rules/product-workflows.md § Invariants ### Key invariants for the PRD-creation flow | no | verbatim. TIER TEST: names /design (dev) — kept in tier 2; product-workflows.md paths add plugins/dev-workflows/commands/design.md and ready.md so the rule loads where it binds; paths also add plugins/workflows-core/references/grilling-technique.md, the authority it cites for bounded vs relentless. |
| B143 | L313 | item | 324 | Key invariants | - PRD / ARD / `specification.md` / `design.md` are written under `$SPE | .claude/rules/product-workflows.md § Invariants ### Key invariants for the PRD-creation flow | no | verbatim. TIER TEST: binds workflows-core:addressing §2 (folder naming) and /design's design.md — kept in tier 2; paths add plugins/workflows-core/references/addressing.md and the dev design/ready command files. |
| B144 | L314 | item | 146 | Key invariants | - `/create-ard` grounds on mounted repos it discovers (`$REPOS_PATH` l | .claude/rules/product-workflows.md § Invariants ### Key invariants for the PRD-creation flow | no | verbatim |
| B145 | L315 | item | 726 | Key invariants | - `/design` Phase 5 offers a three-take `interface-designer` fan-out ( | .claude/rules/dev-workflows.md § Invariants ### /design and /ready | no | verbatim |
| B146 | L316 | item | 295 | Key invariants | - `/idea` Phase 5 hands `idea.md` off via `handoff-to-main` (`workflow | .claude/rules/product-workflows.md § Invariants ### Key invariants for the PRD-creation flow | no | verbatim |
| B147 | L317 | item | 257 | Key invariants | - `/create-prd <KEY>` derives `idea.md` in-contract from the resolved | .claude/rules/product-workflows.md § Invariants ### Key invariants for the PRD-creation flow | no | verbatim |
| B148 | L318 | item | 1039 | Key invariants | - `/ready` is **read-only about status** — it **derives the workflow p | .claude/rules/dev-workflows.md § Invariants ### /design and /ready | no | verbatim; "never reads a tracker" is a generic noun, no vendor marker needed |
| B149 | L319 | item | 217 | Key invariants | - `/create-ard`, `/design`, `/implement`, `/specify`, `/epics`, `/read | CLAUDE.md § Hard constraints | no | moved from the orphaned ## Key invariants (spans plugins); trailing period added. ## Key invariants heading removed. |
| B150 | L320 | item | 607 | Key invariants | - `/create-prd` DOES write `release_versions` / `change_type` / `relea | .claude/rules/product-workflows.md § Invariants ### Key invariants for the PRD-creation flow | no | verbatim; kept whole ("a reversal" and the tracker-dropdown reason are short and explain the field's authoring rule — kept operative per "when in doubt"); "tracker" is a generic noun, no vendor marker needed. TIER TEST: binds workflows-core:prd-format — paths add plugins/workflows-core/references/prd-format.md; the /release-notes side is stated by docs-workflows' B136 invariant. \| FIX ROUND 1: product-workflows.md paths add plugins/docs-workflows/commands/release-notes.md, which the clause "`/release-notes` reads `change_type` and `release_notes_category`…" binds. |
| B151 | L321 | item | 158 | Key invariants | - `/idea` takes one source — an inline prompt or an `@<path>` markdown | .claude/rules/product-workflows.md § Invariants ### Key invariants for the PRD-creation flow | no | verbatim; "tracker export" generic, no marker |
| B152 | L322 | item | 168 | Key invariants | - `/idea` Phase 4 has no write-path gate and no `prd_disposition`: the | .claude/rules/product-workflows.md § Invariants ### Key invariants for the PRD-creation flow | no | verbatim |
| B153 | L323 | item | 1535 | Key invariants | - **A slice folder is `PRD-`-prefixed and asserts `kind: brd`, and tha | CLAUDE.md § Hard constraints (the general kind-gate rule); .claude/rules/brd-route.md § Invariants (the rest) | yes: the bold rule "Any kind gate must therefore test the artifact it actually cares about, never the folder's asserted kind" → CLAUDE.md § Hard constraints as one line ("therefore" dropped; it gets a short reason clause and a pointer to brd-route.md); the rest of the bullet → brd-route.md, where the removed lead is replaced by "That is why a container refusal tests…" | verbatim. STEP 2 SPLIT (product-workflows.md would be ~28.9k): moved to brd-route.md per the brief. TIER TEST: binds workflows-core:addressing §4/§4.1 and every command carrying a *_BRD_NOT_SLICED kind gate — `grep -l '_BRD_NOT_SLICED: <' plugins/*/commands/*.md` returns create-prd, create-ard, specify, epics, prd-proposal and dev-workflows' design, ready, implement. Kept in tier 2 (a named, finite set of files) and brd-route.md's paths widened beyond the brief's to cover them: product commands create-prd/create-ard/specify/epics/prd-proposal, dev commands design/ready/implement, and workflows-core/references/addressing.md. The brief's globs alone would not load this rule on the very commands whose gates it governs. \| FIX ROUND 1: TIER TEST: the general rule binds any future kind gate anywhere, so it moves to tier 1 and is stated once. brd-route.md paths now cover plugins/product-workflows/** (the individual product-command globs are replaced; this adds idea.md, the "test the folder, never the kind it asserts" gate, and update-prd.md, the §5.1 positive-evidence gate) and add plugins/workflows-core/commands/frames.md (its positive-evidence kind test). The intro no longer claims the cross-plugin list is complete. FIXWAVE (D9): brd-route.md's intro now gives the recipe for finding kind-gated files outside product-workflows — `grep -l '_BRD_NOT_SLICED: <' plugins/*/commands/*.md` and `grep -lE 'kind it asserts\|asserted kind\|asserts .kind: brd\|asserting .brd' plugins/*/commands/*.md plugins/*/references/*.md`. Together they return the five files already listed plus `docs-workflows`' document.md and release-notes.md, which place a folder by prefix and were missing; both added to brd-route.md's paths:. |
| B154 | L324 | item | 1049 | Key invariants | - **PRD-eligibility is a folder-kind question now, not a ledger questi | .claude/rules/brd-route.md § Invariants | no | verbatim. Same tier ruling and widened paths as B153 ("all three refusals" = /create-prd, /create-ard, /epics; coverage-ledger-format is under product references/*; addressing §4.1 via the addressing.md path). \| FIX ROUND 1: brd-route.md paths widened to plugins/product-workflows/** plus workflows-core/commands/frames.md; update-prd.md's §5.1 positive-evidence container test is now covered. The general kind-gate sentence is in CLAUDE.md § Hard constraints (see B153). |
| B155 | L325 | item | 2256 | Key invariants | - **The sibling re-cut is the one place `/brd-split` re-allocates a ro | .claude/rules/brd-route.md § Invariants; docs/maintainers/rationale.md#brd-recut-readers | yes: bullet → brd-route.md minus "The one that did — `grep -l unallocated …` — returns six files and is wrong in both directions: … are all outside the glob.", + why-link after the last sentence; that sentence → rationale#brd-recut-readers | Pointers: "no recipe stands here" — "here" is the bullet itself, referent unchanged; "every write here" = the re-cut, unchanged. Rationale: "The one that did" → "That recipe", with a one-sentence framing lead naming it as the recipe that stood in the CLAUDE.md bullet. Count "six files" re-verified on tree (6), so no as-of mark. |
| B156 | L326 | item | 962 | Key invariants | - **The BRD route is detected, never declared.** `/create-prd`, `/crea | .claude/rules/brd-route.md § Invariants | no | verbatim. Binds /create-prd, /create-ard, /specify (brd-route.md paths list them) and prd-format.md's no-implementation-detail rule (prd-format.md is in product-workflows.md's paths; the rule is stated once, here). |
| B157 | L327 | item | 1235 | Key invariants | - **`workflows-core:addressing` §7's shared-fallback adoption is addit | CLAUDE.md § Hard constraints; docs/maintainers/rationale.md#addressing-fallback-totals | yes: bullet → CLAUDE.md § Hard constraints minus "The \"twelve files, eleven commands\" totals this bullet used to tell readers to re-derive went false twice — … so §7 now keeps none.", + why-link; that sentence → rationale#addressing-fallback-totals | TIER TEST → tier 1: every command that addresses a folder reaches the tree through resolve-address, in all four plugins (`grep -l resolve-address plugins/*/commands/*.md`), and "a regression here reaches /epics, /design and /ready"; a product rules file would not load for most of the editors it binds. Pointer "A regression here" — the fallback adoption, referent unchanged. Rationale "this bullet" → "the `workflows-core:addressing` §7 bullet in `CLAUDE.md`". The bold lead "it keeps no totals" still carries the operative conclusion of the moved evidence. TASK9-FIX1: "A regression here reaches /epics, /design and /ready…" → rationale#addressing-fallback-totals. |
| B158 | L328 | item | 318 | Key invariants | - A phase is not finished until its artifact is on the specs repo's de | CLAUDE.md § Hard constraints | no | moved from the orphaned ## Key invariants (spans plugins); trailing period added. |
| B159 | L330 | para | 47 | Key invariants | Key invariants for `$DOCS_PATH` docs grounding: | .claude/rules/docs-grounding.md § Invariants (### heading) | no | lead-in became "### Key invariants for `$DOCS_PATH` docs grounding". See B066 ruling. |
| B160 | L332 | item | 93 | Key invariants | - Read-only; never writes into `$DOCS_PATH`; advisory only — never a g | .claude/rules/docs-grounding.md § Invariants | no | verbatim. |
| B161 | L333 | item | 566 | Key invariants | - Default ON when `$DOCS_PATH` (`:-/workspace/docs`) is a readable dir | .claude/rules/docs-grounding.md § Invariants; rationale#docs-grounding-flags | yes: default-on rule, flag declaration "declared by the shared reference for all nine consumers and now parsed by all nine", /prd-ground override, silent skip → docs-grounding.md; "`/idea` was the only implementation until the other eight were given a flag-stripping rung…" → rationale | clause removed from inside the parenthetical at the "; " boundary; rest verbatim. |
| B162 | L334 | item | 319 | Key invariants | - Grill commands rank challenges into the Impact × Uncertainty gap lis | .claude/rules/docs-grounding.md § Invariants | no | verbatim. |
| B163 | L335 | item | 588 | Key invariants | - `docs-grounder` retrieves via `qmd` CLI (no skill installed) but onl | .claude/rules/docs-grounding.md § Invariants | no | verbatim. |
| B164 | L337 | para | 68 | Key invariants | Key invariants for specs-repo git (`workflows-core:specs-repo-git`): | .claude/rules/workflows-core-git.md § Invariants | no | Lead-in became the sub-heading "### Specs-repo git (`workflows-core:specs-repo-git`)", so "this reference" in B168 keeps its referent |
| B165 | L339 | item | 1647 | Key invariants | - Every git invocation is `git -C "$SPECS_PATH"` — the working directo | .claude/rules/workflows-core-git.md § Invariants ### Specs-repo git | no | verbatim. TIER RULING for B165–B171 (git -C never cd, enumeration staging / never git add -A at repo scope, -z porcelain, eight-prefix branches, never force-push etc., never fatal): they bind every command that runs git against the specs repo (all four plugins) — kept tier 2, and the git split's paths: widened to plugins/*/commands/*.md plus the git references (specs-repo-git, phase-handoff, read-only-repos, grounding-format, code-handoff), so the file loads on any edit those rules govern. Not lifted to tier 1: CLAUDE.md B093 already carries the tier-1 anchor, and lifting ~3k chars of hard rules would restate the authorities beside them. "this sentence states" — the bullet's own sentence, unchanged. Count "fifteen of the twenty-nine" not re-derived (criterion-based; held by hand). |
| B166 | L340 | item | 769 | Key invariants | - Staging is by enumeration over `git status --porcelain -z --untracke | .claude/rules/workflows-core-git.md § Invariants ### Specs-repo git | no | verbatim; the -z rule also binds phase-handoff §2.3, code-handoff §2.2 and grounding-format §4 — all three are in the file's paths: |
| B167 | L341 | item | 190 | Key invariants | - Only `^(idea\|prd\|ard\|spec\|design\|ready\|brd\|frames)/` branches are th | .claude/rules/workflows-core-git.md § Invariants ### Specs-repo git | no | verbatim |
| B168 | L342 | item | 618 | Key invariants | - `workflows-core:phase-handoff` extends this reference for a phase's | .claude/rules/workflows-core-git.md § Invariants ### Specs-repo git | no | verbatim |
| B169 | L343 | item | 232 | Key invariants | - **Detached HEAD is the one blocking state** — it sets `specs_git: bl | .claude/rules/workflows-core-git.md § Invariants ### Specs-repo git | no | verbatim |
| B170 | L344 | item | 133 | Key invariants | - Never force-push, never `branch -D`, never merge/rebase/reset, never | .claude/rules/workflows-core-git.md § Invariants ### Specs-repo git | no | verbatim |
| B171 | L345 | item | 63 | Key invariants | - Never fatal — every failure is reported and the run continues | .claude/rules/workflows-core-git.md § Invariants ### Specs-repo git | no | verbatim |
| B172 | L346 | item | 112 | Key invariants | - The artifact commit carries no `Co-Authored-By` trailer; each artifa | .claude/rules/workflows-core-git.md § Invariants ### Specs-repo git | no | verbatim |
| B173 | L347 | item | 816 | Key invariants | - The canonical terminal order is deliverable + handoff → feedback → f | .claude/rules/workflows-core-git.md § Invariants ### Specs-repo git | no | verbatim. Overlap with B093 (tier 1): B093 states "commit-artifacts as its last action, or immediately before a phase that cedes control"; B173 adds the emitter-tail order, the deliverable-side freedom and the reason + examples for the ceding exception — kept whole as the elaboration, not a restatement |
| B174 | L348 | item | 261 | Key invariants | - Exactly one `Specs repo:` outcome line per run, wherever `commit-art | .claude/rules/workflows-core-git.md § Invariants ### Specs-repo git | no | verbatim. Overlap with B093 ("reported once as a `Specs repo:` line"): B174 adds where the line prints on a ceding run and that a guard notice is repeated in full — kept whole |
| B176 | L352 | para | 3105 | Test-writing requirement for code change | Any `/implement` invocation that touches source code **must** write at | .claude/rules/dev-workflows-tests.md § Test-writing requirement; rationale#test-writing-requirement | yes: all sentences through "…DO-NOT-MERGE banner." → dev-workflows-tests.md (unwrapped to one line); "Changing the tree … not the operator's." → rationale; new operative refusal sentence "The first clause is not made unqualified by removing either escape … do not re-propose it without new evidence" + why-link added in its place | STEP 2 SPLIT: dev-workflows.md would be ~22.3k with this section; split to dev-workflows-tests.md with the brief's paths. Pointer "a second copy of it in this file" → "in these rules" (was CLAUDE.md). "No count here", "this section", "bullets below", "this section was built around" — referent is the moved section, unchanged. Rationale "that option" → the quoted option name; "first clause" → "first clause of the test-writing requirement". FIXWAVE (D2, D8): sole statement of the first clause now that B097 is a pointer (duplicate was in the base, resolved here). D8: the refusal sentence restored to the base's "both": "The first clause is not made unqualified: that would have to remove **both** escapes a user can choose — …; do not re-propose it without new evidence". |
| B177 | L393 | item | 106 | Test-writing requirement for code change | - Prefer unit tests; use integration or end-to-end tests only if that | .claude/rules/dev-workflows-tests.md | no | verbatim |
| B178 | L394 | item | 110 | Test-writing requirement for code change | - Tests must be meaningful (assert specific behaviour), deterministic, | .claude/rules/dev-workflows-tests.md | no | verbatim |
| B179 | L395 | item | 1075 | Test-writing requirement for code change | - **Pre-Phase 3.5's skip is where kind 1 and the first of kind 2's fou | .claude/rules/dev-workflows-tests.md | no | verbatim FIXWAVE (D3): now the one full statement of the skip's two provenances (duplicate with B099 was in the base, resolved here); B099's reason "nothing was typed and nothing was declined there" appended to the run's-record reason. |
| B180 | L396 | item | 1436 | Test-writing requirement for code change | - **Kind 2's third member is environmental and nobody picks it: a Phas | .claude/rules/dev-workflows-tests.md | no | verbatim; "three kinds above", "the bullet above's", "the bullet below", "the headline paragraph" — same section, referents unchanged |
| B181 | L397 | item | 1378 | Test-writing requirement for code change | - **Kind 2's fourth member is the silent one, and it is the reason tha | .claude/rules/dev-workflows-tests.md; rationale#test-writing-requirement | yes: bullet → dev-workflows-tests.md minus the "Measured on Mocha 12.0.2 … and after." sentence, + why-link; that sentence → rationale | The new test now reads as the test test-writer just wrote (named in the bold lead) — referent holds. "the third clause above" — same section, unchanged. |
| B182 | L398 | item | 66 | Test-writing requirement for code change | - Docs-only changes (`/document`) are exempt from this requirement | .claude/rules/dev-workflows-tests.md | no | verbatim |
| B184 | L402 | para | 165 | Updating installed plugins after editing | After editing files in this repo and pushing, update the affected plug | CLAUDE.md § Updating installed plugins after editing | no | kept TASK9-FIX1: § Where-the-rest Holds cells trimmed (table is new text, no block). |
| B185 | L406 | fence | 217 | Updating installed plugins after editing | ```bash claude plugin update dev-workflows@ihudak-plugins claude plugi | CLAUDE.md § Updating installed plugins after editing | no | kept |
| B186 | L413 | para | 173 | Updating installed plugins after editing | **`claude plugin update` requires a restart to apply** — the CLI says | CLAUDE.md § Updating installed plugins after editing | no | kept; paragraph → list item TASK9-FIX1: "An edit that did not land is very often…" → rationale#plugin-update-cli. |
| B187 | L415 | para | 445 | Updating installed plugins after editing | **There is no `claude plugin reinstall`.** This section named one for | CLAUDE.md § Updating installed plugins after editing; rationale#plugin-update-cli | yes: "This section named one… offers install and uninstall." → rationale | Pointer: "before writing it here — this file is read by agents" → "before writing it into `CLAUDE.md` or `.claude/rules/` — agents run what those files say". |
| B188 | L417 | para | 295 | Updating installed plugins after editing | **Update `prose-style` with `docs-workflows` past 1.1.3** — `claude pl | CLAUDE.md § Updating installed plugins after editing; rationale#plugin-update-cli | yes: "0.4.0 is the release whose checker applies the specs repository's house-style rules…" → rationale | Rewritten: "beside a `prose-style` older than 0.4.0, `/release-notes` records its style check `DEGRADED`". |
| B189 | L419 | para | 656 | Updating installed plugins after editing | **Update the plugin that holds the file you edited — not the one whose | CLAUDE.md § Updating installed plugins after editing | no | Compressed: the per-plugin inventory ("five shared agents", "fourteen agents", "twelve references") → kinds only (counts live in § Active plugins / rules files); "The symptom is the same either way and is the reason this is worth stating: the run picks up…" → "Otherwise the run picks up…". |
| B190 | L421 | para | 761 | Updating installed plugins after editing | **`claude plugin marketplace update <marketplace>` does NOT update ins | CLAUDE.md § Updating installed plugins after editing; rationale#plugin-update-cli | yes: the 1.0.0/1.1.0 measurement and "This file previously claimed the opposite…" → rationale | — |
| B191 | L423 | para | 574 | Updating installed plugins after editing | **The `/plugins` interface updates installed plugins; the CLI's `marke | CLAUDE.md § Updating installed plugins after editing; rationale#plugin-update-cli | yes: "verified live… 1.0.0 to 1.1.0" → rationale | Compressed; "the CLI's marketplace update does not" dropped as a duplicate of B190 (surviving copy re-read). "which is why the per-plugin update command is what this section leads with" → "so it uses the per-plugin update command". |
| B192 | L425 | para | 388 | Updating installed plugins after editing | **A renamed plugin blocks marketplace update entirely** — not just its | CLAUDE.md § Updating installed plugins after editing | no | kept; "Worth knowing before renaming one: the cost lands on every user…" → "and the cost lands on every user, so weigh it before renaming one". |
| B193 | L427 | para | 92 | Updating installed plugins after editing | **Two other subcommands are worth knowing here**, both verified from ` | CLAUDE.md § Updating installed plugins after editing | no | deletion of lead-in "Two other subcommands are worth knowing here, both verified from claude plugin --help:" — the two items follow directly as one bullet; the --help verification rule survives in B187. |
| B194 | L429 | item | 188 | Updating installed plugins after editing | - `claude plugin validate <path>` — validates a plugin or marketplace | CLAUDE.md § Updating installed plugins after editing | no | merged with B195 into one bullet; wording compressed, every clause kept. |
| B195 | L430 | item | 343 | Updating installed plugins after editing | - `claude plugin tag [path]` — creates a `{name}--v{version}` git tag | CLAUDE.md § Updating installed plugins after editing | no | merged with B194; "so a disagreement should never reach it — but the tag step refusing is the last place to catch one" → "— the last place to catch a disagreement". |
| B197 | L434 | para | 150 | Behavioral guardrails (Karpathy) — marke | These notes complement the user-scope Claude guidance. They add only t | CLAUDE.md § Behavioral guardrails (Karpathy) | no | kept whole |
| B198 | L437 | item | 263 | Behavioral guardrails (Karpathy) — marke | - **Goal-Driven Execution** maps directly onto the existing `test-base | CLAUDE.md § Behavioral guardrails (Karpathy) | no | kept whole |
| B199 | L438 | item | 354 | Behavioral guardrails (Karpathy) — marke | - **Surgical Changes** applies in both directions when you edit comman | CLAUDE.md § Behavioral guardrails (Karpathy) | no | kept whole |
| B201 | L442 | item | 62 | Git | - `origin` → `git@github.com:ihudak/ihudak-claude-plugins.git` | CLAUDE.md § Git | no | kept whole. Pointer "here"/"this tree" unchanged (this checkout). |
| B202 | L443 | item | 24 | Git | - Default branch: `main` | CLAUDE.md § Git | no | kept whole. Pointer "here"/"this tree" unchanged (this checkout). |
| B203 | L444 | item | 596 | Git | - **Verify the current branch immediately before every commit — `git b | CLAUDE.md § Git; rationale#verify-branch-before-commit | yes: "That is not hypothetical: one session branched away…" → rationale | New bullet added after B204: "Never bare git stash…" (brief). |
| B204 | L445 | item | 521 | Git | - **When work must land on `main` while this tree sits on another bran | CLAUDE.md § Git | no | kept whole. Pointer "here"/"this tree" unchanged (this checkout). TASK9-FIX1: "A duplicate commit on that branch…" → rationale#worktree-not-checkout, why-link added. |

## Loading probe

The controller ran the probe itself (a nested `claude -p`), on commit `68e5cf01`, from the worktree root. Command shape: `claude -p --model claude-sonnet-5 --allowedTools Read --output-format stream-json --verbose "<prompt>"`.

- **Precondition:** `grep -c 'sibling re-cut is the one place' CLAUDE.md` returns 0. The sentence is only in `.claude/rules/brd-route.md`.
- **Positive:** the model read only `plugins/product-workflows/commands/brd-split.md` (limit 5). It quoted "The sibling re-cut is the one place `/brd-split` re-allocates a row that already carries a fate by the owner's choice, and its precondition is the whole of…" and attributed the sentence to `.claude/rules/brd-route.md`. The only `file_path` read was `brd-split.md`.
- **Negative:** the model read only `README.md` (limit 5) and answered `NOT LOADED`. The only `file_path` read was `README.md`.

**Task 1 spike**, run before any text moved, used a throwaway probe rules file and the same two prompts. Positive: the model quoted the probe sentence after reading only `brd-split.md`. Negative: `NOT LOADED` after reading only `README.md`. So a `paths:` rules file loads on a matching Read and not otherwise. The spike model called the injected rule "suspicious", but it quoted it.

The raw transcripts are `probe-pos.jsonl`, `probe-neg.jsonl` (spike), `probe-real-pos.jsonl` and `probe-real-neg.jsonl` (real files), all in the git-ignored SDD workspace.

## Rulings

These are the ledger's rulings in ledger order, each with the ledger's own statement of what it costs if wrong.

| # | Task | Ruling | Cost if wrong |
|---:|---|---|---|
| 1 | pre-flight | `docs-workflows.md` carries the `/epics` and docs-grounding globs; splitting the shared bullets would otherwise make two copies (spec §4). | `/epics` rules load on `docs-workflows` edits too (small context cost). |
| 2 | pre-flight | A tier-2 file over 20k is split, not left warning: spec §7's warning signals regrowth. | More rules files than the spec's five (the tree now has eleven). |
| 3 | 2 | The plan miscounted Task 2's red cases (4+3 against the 6 the implementation has): the plan text is wrong and the behaviour is right. | None. |
| 4 | 3 | Inbound `this-repo` rows in this split's own plan (110) and spec (16) are its design records, not citations to re-point; Task 10 skips them. | A stale pointer in a planning doc. |
| 5 | 3 | Moved text must not carry a literal `](` inside inline code, because check 1 parses inline code spans as links. The base had 0 occurrences. | A red gate on a later task, caught there. |
| 6 | 4 | The plan's step-7 heredoc snippet was broken (stdin consumed by the heredoc) and was replaced with `$WS/opcheck.py`, which takes a sentences file. | None (a tool only). |
| 7 | 4 | The plan's Task 4 table omitted the vendor-neutrality and identity-quarantine tier-1 hard constraints that spec §4 requires; they were added in fix round 1. | None. |
| 8 | 5 | B093 (`specs-preflight` / `commit-artifacts` for all 29 writers) belongs in tier 1 by the tier test; Tasks 8 and 9 cite it rather than restating B173 or the fence's "All twenty-nine" clause. | A duplicate rule. |
| 9 | 6 | Widening `paths:` to the exact files a rule governs is within spec intent (a rule loads when its file is read). | Rules load on a few extra reads. |
| 10 | 7 | Accept `docs-grounding.md` as an extra split, by the same rule as the 20k splits the plan names. | One more rules file. |
| 11 | 7 | Nothing gated `paths:` globs, and one dead glob had already slipped through, so Task 11 adds a `validate-catalog.py` check that every glob matches at least one tracked file, with a selftest. | A small scope addition to Task 11. |
| 12 | 8 | Accept B079 in tier 1 (it binds edits to instruction files, which no path glob scopes), and let `workflows-core-git.md` load on every command edit rather than lifting about 3k of git rules into tier 1. | 13k of extra context on command edits. |
| 13 | 8 | The command-to-plugin assignment stays in tier 1 (naming core's six commands and product's commands), even past the 500-character brief budget. | About 150 characters. |
| 14 | 9 | `instruction-file-maintenance` stays in tier 1 as compact clauses, not a bare pointer, because the reference is not loaded when `CLAUDE.md` is edited. | About 400 characters. |
| 15 | 10 | Task 11 also (a) fails any rules file whose `paths:` glob matches nothing, or which has no `paths:` (it would load every session), with selftest pairs; and (b) in a separate commit, resolves the pre-existing spec-id-baseline drift after confirming the three IDs are legitimate. | A small scope growth of Task 11. |
| 16 | 11 | Accept Task 11's correction of the needles in two size-selftest cases: the plan's cases were unsatisfiable, and the multi-byte case still discriminates bytes from characters. | None. |

Rulings 2 and 10 are the splits made under the 20k rule. `dev-workflows.md` → `dev-workflows-tests.md` (Task 5); `product-workflows.md` → `brd-route.md` (Task 6); `docs-workflows.md` → `docs-serve.md`, `release-notes.md` and `docs-grounding.md` (Task 7); `workflows-core.md` → `workflows-core-git.md` (Task 8).

## Inbound citations

`$WS/inbound.tsv` classes 283 citations of `CLAUDE.md` found by `grep -rn 'CLAUDE\.md'` outside `.git`, `node_modules`, `.worktrees`, `.superpowers` and `CHANGELOG.md`: 166 `this-repo`, 66 `target-repo` (a target repository's own `CLAUDE.md`, not this file), 38 `history` (records under `docs/superpowers/`, left as history) and 13 `plugin-own`. Of the 166 `this-repo` rows, 126 are this split's own plan and spec (ruling 4). Task 10 (`a89fcec8`) re-checked the other 40 against the moved text.

**Re-pointed (8 citations in 5 files):**

| File | Old wording | New wording |
|---|---|---|
| `plugins/workflows-core/references/addressing.md` (defect-family sentence) | "is the defect family `CLAUDE.md` names" | "is the defect family this repository's `docs/maintainers/rationale.md` records under `resolve-against-a-known-set`" |
| `plugins/workflows-core/references/addressing.md` (standing-rule sentence) | "— `CLAUDE.md`'s standing rule —" | "— the standing rule `CLAUDE.md` states as *Resolve an identifier against a known set* —" |
| `scripts/check-docs.sh` (check-11 comment) | "CLAUDE.md has always said the family comes from the scope paragraph" | "The repo's instructions (now .claude/rules/gates.md, § Check 11) have always said …" |
| `scripts/check-docs.sh` (same paragraph) | "in the direction CLAUDE.md asks for" | "in the direction .claude/rules/gates.md asks for" |
| `scripts/check-docs.sh` (check-16 header) | "CLAUDE.md cites this comment instead of holding a second copy" | ".claude/rules/gates.md (§ Check 16) cites this comment …" |
| `scripts/spec-id-baseline.txt` (header, Regenerate and Verify) | scan root `plugins/ CLAUDE.md` | `plugins/ CLAUDE.md .claude/rules/ docs/maintainers/` |
| `.claude/rules/gates.md` (check 16) | a loose quotation of the resolve-against-a-known-set rule | the bullet's exact title |
| `docs/maintainers/rationale.md` (refinement-4) | "the file `CLAUDE.md` names as the authority for the offer convention …" | "the file `.claude/rules/gates.md` § Check 11 names as the authority …" |

The `addressing.md` edit released `workflows-core` 1.7.5 → **1.7.6** (`plugin.json`, `marketplace.json`, and a dated `## [1.7.6] — 2026-09-23` CHANGELOG section). No other plugin was edited.

**Left unchanged after verification:** `implementation-format.md` (the never-parse rule is still stated in `CLAUDE.md`), `session-cost.py` (the cache-layout sentence is unchanged from base), `brd-intake.md` (the *Resolve an identifier against a known set* bullet still stands), the Task 2 and Task 11 scope text in the three scripts, and the past-tense history in `check-docs.sh`'s check-13 header. Task 10's wrap-insensitive before/after counts took every re-pointed old string to 0. The one exception is a released `workflows-core/CHANGELOG.md` entry, left as history.

## The spec-id-baseline drift

`scripts/spec-id-baseline.txt` is the census tripwire for the spec/design numbered-ID namespace, and it is not in the CI chain. Task 10 found that its Verify command already printed a diff at base `d5f3034b`, before this branch touched anything: `[AC01]` 1→2, `[TCxx]` 16→17, `[U01]` 1→2. With the scan root widened, the diff was byte-identical to base's, so the split added no drift. Task 11 Part C (`68e5cf01`) traced each extra occurrence with `git blame` and `git show`:

| ID | Site | Commit | Verdict |
|---|---|---|---|
| `[U01]` | `plugins/product-workflows/commands/specify.md`, the seed-to-story paragraph | `b8710a3e` (2026-08-30, `--from-brd` on `/create-ard` and `/specify`) | Legitimate illustrative example ID |
| `[AC01]` | the same paragraph | `b8710a3e` | Legitimate |
| `[TCxx]` | `plugins/product-workflows/references/specification-format.md`, the test-case micro-format sentence | `9e5f04e1a` (2026-09-22, HTML renderer removed) | Legitimate |

The baseline was regenerated with its own Regenerate command, and a dated 2026-09-23 paragraph recording the trace was added above it. Verify now prints nothing. The resolution is data specific to this repository's history, not code.

## Deferred minors

These are the ledger's deferred minors, each with its state after the fix wave. The fix wave resolved every one that was open, numbered D6–D10 after the census defects. The not-strictly-verbatim rationale stays as an accepted, recorded deviation.

| Task | Minor | Disposition |
|---|---|---|
| 3 | Task 3's report calls the `docs-workflows.md` ruling paragraph "verbatim", but the paragraph says "this file" for `docs-workflows.md`. | Harmless: the referent is the file the sentence sits in. Closed, no change. |
| 3 | The plan says B001…B205; the actual range is B001…B204. | Plan text off by one; the move table has 185 rows, every block. Closed, no change. |
| 4 | Page totals lived only in move-table notes until Tasks 5–8. | Resolved: Tasks 5–8 each carried their plugin's totals. |
| 5 | `dev-workflows-tests.md`'s `paths:` misses `references/handoff/test-*.md`, `docs/reference/test-suite-detection.md` and `references/code-handoff.md`. | **Resolved (D7).** `dev-workflows-tests.md`'s `paths:` gains `plugins/dev-workflows/references/handoff/test-*.md`, `plugins/dev-workflows/docs/reference/test-suite-detection.md` and `plugins/dev-workflows/references/code-handoff.md`, each checked with `git ls-files` (the glob matches `handoff/test-baseliner.md`). The file's intro and `dev-workflows.md:10` name the new triggers. `validate-catalog.py` reports no dead glob. |
| 5 | Three rationale sections (`code-handoff-citation`, `implement-phase-3-5`, `test-writing-requirement`) have framing lead-ins and are not strictly verbatim. | **Accepted, recorded deviation: not strictly verbatim rationale.** Tier-3 framing only; no operative sentence is affected. |
| 5 | `dev-workflows-tests.md`'s refusal says "either escape" where the base says "both". | **Resolved (D8).** `dev-workflows-tests.md:17` now reads "The first clause is not made unqualified: that would have to remove **both** escapes a user can choose — …; do not re-propose it without new evidence". That matches the base and rationale#test-writing-requirement. |
| 6 | B149/B158 were orphaned under the `/release-notes` list in `CLAUDE.md` until Task 9 moved them. | Resolved: both are in `CLAUDE.md` § Hard constraints (lines 127–128). |
| 6 | `brd-route.md`'s intro has no grep recipe for finding future kind-gated files. | **Resolved (D9).** The intro now gives two greps. `grep -l '_BRD_NOT_SLICED: <' plugins/*/commands/*.md` returns `/design`, `/ready`, `/implement` and five product commands. `grep -lE 'kind it asserts\|asserted kind\|asserts .kind: brd\|asserting .brd' plugins/*/commands/*.md plugins/*/references/*.md` returns all five listed files outside `plugins/product-workflows/` (`design.md`, `ready.md`, `implement.md`, `frames.md`, `addressing.md`), plus `docs-workflows`' `document.md` and `release-notes.md`. Those two place a folder by prefix, never by its asserted kind, and were missing from `paths:`. Both were added, and the intro names them. |
| 7 | The three prose-style roles are stated in tier 1 (one clause) and tier 2 (detail). | **Resolved (D6).** `CLAUDE.md:37` is now a pointer: "`docs-style-checker` dispatches `prose-style-checker` on every run — roles in `.claude/rules/docs-workflows.md`". The roles are stated only in `docs-workflows.md`, in its authority paragraph (line 25) and its keyed-mode invariant (line 81). Nothing about the roles binds dev or product edits: `/epics`' unconditional run is in `docs-workflows.md:82`. D4 and D5 were resolved the same way. |
| 8 | B173/B174 partly overlap tier-1 B093 (ceding placement, one `Specs repo:` line). | Adjudicated as elaboration in the move table; closed. |
| 9 | Some moved rationale sentences are lightly reworded to stand alone. | **Accepted, recorded deviation: not strictly verbatim rationale.** Tier-3 framing. D1 was one such rewording that went wrong, and it is resolved. |
| 10 | Task 10's report cites two line numbers a few lines off (the check-16 header at about 2270; `CHANGELOG` :316). | Closed. The report is a workspace file, and this record cites by comment name instead. |
| 11 | `check_rules_paths`' empty-paths and malformed-line ERROR branches have no selftest case. | **Resolved (D10).** Three red cases were added, one per untested ERROR branch. Each needle is its branch's own message: the empty `paths:` list (`(the paths: list is empty)`), a list item that is not a `- ` line (`unexpected line in the paths: list`), and an empty entry (`an empty entry in the paths: list`). Each case was shown RED against a copy of the script with that branch stubbed out (`SELFTEST FAIL`, naming the case), then GREEN against the real script (`SELFTEST PASS`). |

## Port to the other editions

`scripts/check-docs.sh`'s body is byte-identical across three editions. None of the changes below is ported here; each is named by function or comment so the port can find it. `git log d5f3034b..HEAD -- <file>` lists the commits.

### `scripts/check-docs.sh`

- **Task 2 (`b8c47da5`)**
  - `check_links_and_anchors`: the scanned file list gains `CLAUDE.md`, `.claude/rules/*.md` and `docs/maintainers/*.md`, so why-links and rationale anchors are checked by checks 1 and 2.
  - `check_vendor_tokens`: the scan list gains the same three surfaces, replacing the single `CLAUDE.md` append.
  - Check-13 header comment: the `SCOPE` paragraph names the instruction tiers, and the census recipe reads `plugins CLAUDE.md .claude/rules docs/maintainers`.
  - `selftest`: nine new cases — check-13 red/green pairs for `.claude/rules/` and `docs/maintainers/`; check 1 on a broken link in `CLAUDE.md`, in a rules file and in `docs/maintainers/`; check 2 on a why-link to a missing rationale anchor, and its green twin from both `CLAUDE.md` and a rules file.
- **Task 4 (`c964da45`, `e10c0daf`)**, comment only: the check-13 header's *Sanctioned users* census went 14/6 → 15/8 → 16/8, and its *instruction-file rationale* kind grew to name the rationale's check-13 evidence, `gates.md`'s check-13 description and `CLAUDE.md`'s hard-constraint statement.
- **Task 9 (`481bab6c`, `c0c55c2b`)**, comment only: the same kind re-attributes the id-grammar explanation to `docs/maintainers/rationale.md` and the key-namespace rule to `CLAUDE.md`, and names the two marker lines that carry no vendor name.
- **Task 10 (`a89fcec8`)**, comment only: the check-11 comment above `check_merge_clause` (the anchored scope-paragraph derivation) and the check-16 header above `check_loader_contract` re-point from `CLAUDE.md` to `.claude/rules/gates.md` § Check 11 and § Check 16.
- **Task 11:** no change to `check-docs.sh` (`git log d5f3034b..HEAD -- scripts/check-docs.sh` lists no Task 11 commit).

Because the other editions' repositories carry no `.claude/rules/` or `docs/maintainers/`, the `for extra in …` loops in `check_links_and_anchors` and `check_vendor_tokens` are `[ -f ]`-guarded and are a no-op there. They port as-is.

### `scripts/check-id-grammar.sh` (Task 2, `b8c47da5`)

- `EXCLUDED_SUBTREES`: `docs` narrowed to `docs/superpowers`, so `docs/maintainers/` is gated.
- The exclusion comment above `PATTERN` says why.
- `--selftest`: a new red case (`[FR-1]` in `docs/maintainers/rationale.md`, exit 1) beside a green root (`[AC-1]` in `docs/superpowers/verification/`, still excluded).

### `scripts/validate-catalog.py` (Task 11, `368ad0d3`)

- New module constants `CLAUDE_MD_MAX` (40,000), `CLAUDE_MD_WARN` (36,000) and `RULES_FILE_WARN` (20,000), placed after `DESCRIPTION_WARN`.
- New `check_instruction_sizes(root)`: characters, not bytes; ERROR above the cap, WARN above each threshold.
- New `check_rules_paths(root)`: every `.claude/rules/*.md` needs frontmatter with a non-empty `paths:` list, and every glob in it must match at least one file.
- `validate_repo`: two new call sites, just before `if errors == 0 and warnings == 0:`.
- `_selftest`: `build()` gains the keyword-only `claude_md` and `rules` parameters, and 11 new cases were added (5 `CLAUDE.md`-size, 1 rules-size, 5 `check_rules_paths`). Two size needles were corrected per ruling 16.
- **Fix wave (D10):** three more `check_rules_paths` cases, with their comment, after the no-frontmatter case: an empty `paths:` list, a non-`- ` item line, and an empty entry. Port them with the function.
- Module docstring: two sentences (the instruction budget and the `paths:` gate).
- Not code: `CLAUDE.md` § Running the gates gained one sentence. Port it to any edition that carries its own `CLAUDE.md`/rules split.

### `scripts/spec-id-baseline.txt`

- **Task 10 (`a89fcec8`):** the scan root in the header and in the Regenerate and Verify commands becomes `plugins/ CLAUDE.md .claude/rules/ docs/maintainers/`. Port this wherever an edition carries the split, since without it the census silently loses the three IDs the split moved into `.claude/rules/dev-workflows.md`.
- **Task 11 Part C (`68e5cf01`):** regenerated data and a dated paragraph. This is specific to this repository's history; nothing to port.

## Gates

The full chain in `global-constraints.md`, run unpiped from the worktree root on the tree carrying this record (`git add -A` first), printed `GATES_EXIT=0`.

The gates that reach this file:
- Checks 14 and 19 scan the whole repository, so they read this record; check 19 reads it through `git ls-files -co`.
- The id-grammar gate excludes `docs/superpowers/`.
- Check 13 (vendor tokens) does not reach this record: its scope is `plugins/` plus the instruction tiers.
- Check 6, the 200-character table-cell limit, scopes each plugin's `docs/` and README plus the repo-root `README.md`, so it does not reach this record either. The move table's long cells are ungated by design.
