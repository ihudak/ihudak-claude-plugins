# Red: an unclosed fence that swallows the prose after it

```mermaid
flowchart TD
    a["one"] --> b["two"]

This paragraph was meant to follow the diagram, but the fence above is never closed, so mermaid is handed it as part of the diagram.
