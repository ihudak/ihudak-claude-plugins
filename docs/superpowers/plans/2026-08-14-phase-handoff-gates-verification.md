---
tags:
  - plan
  - ai
  - dev-workflows
  - tasks-exclude
date: 2026-08-15
---

# Task 20 — Verification record: sub-project J (phase-handoff gates + PR-on-completion)

Written last, against the final tree, after all nineteen other tasks landed and were reviewed. Every expected value below was re-derived directly against the commits named here — none was copied from `progress.md`, any `task-*-report.md`, or the three existing proof artifacts (`2026-08-14-gate-reachability.md`, `2026-08-14-cross-cutting-checks.md`, `2026-08-14-residue-sweep.md`). Those artifacts were read for context and, where their own methodology was sound, their commands were re-run from scratch against the tree below rather than trusted; three places where re-running surfaced something new are called out explicitly in the risk section.

**Trees verified:** canonical `/workspace/ihudak-claude-plugins` @ `6e3f8afc0e20f6e49493319edfbe48608ea5ffe2` (branch `iv-gu/phase-handoff-gates`, clean), mgd `/workspace/mgd-claude-plugins` @ `df017ef28645c8cac9d9db428115a7a10d2941e1` (same branch name, clean), copilot `/workspace/ihudak-copilot-plugins` @ `7aca302ec5e0cde4d973f6b2ab0f88c86ce85ad5` (same branch name, clean). Pre-branch baseline for diff-against comparisons: `987ce32` on canonical `main`.

**Fixes made in response to this record's own findings.** Verifying R4 and R7/Risk 1 against the trees above found two genuine gaps, not merely documentation nits: `references/phase-handoff.md` §2.6 never mentioned the `gh auth status` pre-check D7's own rationale names (R4), and §3.2's three original primitives could not, as literally specified, distinguish row G (`origin/<default>` ref absent) from row F (path absent on an existing ref) — both make `git cat-file -e` exit 128, and the only distinguishing text is on stderr, discarded by the `2>/dev/null` the primitive itself requires (Risk 1). Both were fixed in this session, in all three editions, before this record was finalized — so every PASS recorded below for R4, R7, and Risk 1 describes the corrected tree, and each row says explicitly that it was found failing first. The fix: `references/phase-handoff.md` (canonical and mgd, identical path; copilot's dialect copy at `dev-workflows/skills/_shared/phase-handoff.md`) §2.6 gained one clause naming the `gh auth status` pre-check, and §3.2 gained a fourth primitive — a ref-existence probe (`git -C "$SPECS_PATH" rev-parse --verify --quiet "origin/<default>"`) run before the path-existence probe — so row G is now reachable and distinguishable from row F. Both edits are pure git-command prose with no dialect-specific tokens, so the identical wording is correct in all three editions without translation. The parity check (`diff -rq` between canonical and mgd's `plugins/dev-workflows`) was re-run after the fix and still reports exactly the same five identity files (`plugin.json`, `CHANGELOG.md`, `LICENSE`, `README.md`, `references/dependencies.md`), confirming the fix introduced no new drift between editions.

**Environment note:** `grep` in this environment is `ugrep 7.5.0` — every command below pipes into it rather than using a `<(…)` process-substitution path, confirms a target file exists before trusting a `0`, and escapes a literal `|` as `\|` where the pattern is a regex alternation rather than a markdown table pipe.

## How to read the requirement table

Six columns: the requirement, the exact command run against the tree above, the expected value (derived from the spec's own requirement text and the surrounding design section it belongs to — §8, §4, §5, §6, §7, §9.1 — never from another verification document), the observed value, PASS/FAIL, and the evidence that the check discriminates (a negative control, a diff against the pre-branch baseline, or an empirical two-case test) rather than passing on a phrase that would have been true regardless.

## R1–R10 — the new reference, `references/phase-handoff.md`

| # | Requirement | Command | Expected | Observed | Verdict | Discrimination evidence |
|---|---|---|---|---|---|---|
| R1 | The file exists and declares scope, its three divergences from `specs-repo-git.md`, and the caller contract | `test -f plugins/dev-workflows/references/phase-handoff.md && wc -l` | file exists, non-trivial length | 231 lines; §1 items 5/6/7 name the three divergences (fatal-by-design, `Co-Authored-By` carried, consent-gated not prompt-free) verbatim against `specs-repo-git.md`'s rules 5/6/7; §5 is titled "Caller contract" with 4 numbered obligations | PASS | Read the full file (not grepped) — a bare grep for "scope" would match unrelated prose; the divergence numbering (5/6/7) is cross-checked against `specs-repo-git.md`'s own rule numbering (also 5/6/7), so a mismatch in either file would be visible |
| R2 | `handoff-to-main` specifies steps 1–7, including enumerated staging and the never-`add -A`-at-repo-scope rule | `sed -n '24,96p' references/phase-handoff.md` (§2.1–§2.9) | 7 numbered/titled steps: gate, branch resolution, staging, commit, push, open PR, title/body (+failure discipline) | §2.1 Gate, §2.2 Branch resolution, §2.3 Staging (enumeration via `status --porcelain --untracked-files=all`, `add -A -- <literal paths>` only), §2.4 Commit, §2.5 Push, §2.6 Open the PR, §2.7 Title/body, §2.8 Failure discipline — 8 named subsections covering the 7 spec steps (§2.8 splits failure discipline out of the PR step) | PASS | §1 rule 2 states the never-`add -A`-at-repo-scope rule as a **hard rule**, and §2.3 step 3 restates it scoped to the literal path list — checked both sites agree, not just one |
| R3 | D6's collision rule, both branches (reuse own in-progress branch; else lowest free `-N`) | `sed -n '34,43p' references/phase-handoff.md` | reuse condition (own prefix, key in run set, unmerged) + fallback (lowest free suffix from `-2`) | §2.2 step 3: "it is this run's own in-progress branch — its prefix is the caller's, its key is in the run key set … and … `merge-base --is-ancestor` … fails (not yet merged) → **reuse it**"; step 4: "append the lowest free integer suffix, starting at `-2`" | PASS | Both branches present in one ordered list (steps 3 then 4) — a check for only one clause (e.g. grep `-2` alone) would pass even if the reuse clause had been dropped, so both were read together |
| R4 | D7's capability probe (try `gh pr create`; fall back on failure) and the `gh auth status` pre-check | `grep -n "Capability probe" references/phase-handoff.md; grep -n "auth status" references/phase-handoff.md` | capability-probe language present; `gh auth status` pre-check mentioned | "Capability probe, not host classification. Try the call; on any failure fall back to §4.2's instruction" present at §2.6 (unchanged). **First pass found a real gap: `grep "auth status"` → 0 hits, file confirmed to exist first — D7's own "cheap `gh auth status` pre-check" was not textually present anywhere in `phase-handoff.md`.** Fixed in this session: §2.6's opening sentence now reads "Derive the repository, run a cheap `gh auth status` pre-check purely to avoid a confusing raw error, then call `gh` with every argument…" — applied identically to canonical, mgd, and copilot's dialect copy (the sentence carries no `${CLAUDE_PLUGIN_ROOT}`/`subagent_type`/slash-command content, so the identical wording is dialect-correct in all three). Post-fix: `grep -c "auth status" references/phase-handoff.md` → 1 in all three editions | PASS (found failing, fixed in this session) | The gap was real and the fix is minimal and scoped to D7's own named clause — verified the fix landed as one sentence, not a restatement of D7's reasoning, and that the surrounding "try the call; on any failure fall back" text (the functional behaviour) is untouched, since the pre-check was already functionally redundant with the fallback and only a textual-fidelity gap |
| R5 | PR title + body-file contents specified | `sed -n '77,81p' references/phase-handoff.md` (§2.7) | title = commit subject; body = phase output, artifact paths, reviewer verdict, open-question/NEEDS-CLARIFICATION count, next command + merge dependency | "Title: the commit subject of §2.4. Body: written to a file (never passed inline …) containing what the phase produced; the artifact paths; the reviewer verdict where the caller has one; the count of open questions or `[NEEDS CLARIFICATION]` markers; and the next command in the chain together with the fact that it will not run until this pull request is merged" | PASS | All five body elements named individually — checked each phrase is present rather than trusting a single word like "body" |
| R6 | Failure discipline stated as differing from `specs-repo-git.md` §1 rule 5, report must say "not handed off" | `grep -n "not handed off" references/phase-handoff.md; sed -n '20p' references/phase-handoff.md` | explicit contrast with rule 5 ("never fatal"); "not handed off" phrase required in reports | §1 item 5: "`specs-repo-git.md` §1 rule 5 is 'never fatal' … `handoff-to-main` is *not* fatal … but it must report the phase as **not handed off**"; §2.8 repeats "the phase is described as **not handed off**"; `grep -c "not handed off"` → 4 occurrences across §1/§2.8/§4.1's outcome-line table | PASS | Two independent sites (the hard-rule statement and the failure-discipline section) both carry the phrase — a single-site phrase could be a stray mention, two independent sites in different sections is stronger evidence of a stated rule |
| R7 | All ten states A–I specified with outcomes | `grep -n '^\| [A-Z].* \| ' references/phase-handoff.md` (§3.3 table) | rows H, I, A, B, C′, C, D, E, F, G each present with a stated outcome | All ten present in that exact order (H, I, A, B, C′, C, D, E, F, G — first-matching-row order, C′ before C as the file itself explains: "C′ precedes C because offering a switch that git would refuse is worse than naming the blocker") | PASS (state table always correct; a downstream operational gap found and fixed in this session) | Row count verified by literally counting table rows (10), not by trusting a prose claim of "ten states". **First pass found a real, separate defect one level down**: an empirical test (`git cat-file -e` against a missing path on a real ref, and against a nonexistent ref, in a scratch repo) showed both exit 128, with the only distinguishing text on stderr — which §3.2's original primitive 1 discarded via `2>/dev/null`. As literally specified, the three original primitives could not distinguish row G (`origin/<default>` ref absent) from rows D/E/F (ref present, path absent), so a literal implementation would silently fall through to row F's `absent`/delegate behaviour instead of row G's stop. R7 itself was always satisfied — the table names all ten outcomes correctly — but the *table's operational reachability* was not; see Risk 1 below for the full empirical trace. **Fixed in this session**: §3.2 gained a fourth primitive, `git -C "$SPECS_PATH" rev-parse --verify --quiet "origin/<default>"`, run before the path-existence primitive; re-tested empirically (`origin/main` → exit 0, `origin/no-such-ref` → exit 1) and confirmed it distinguishes exactly what `cat-file -e` alone could not. Applied identically to canonical, mgd, and copilot |
| R8 | Degraded-network + read-only-mount behaviour, citing `specs-repo-git.md` §3.2 and `read-only-repos.md` | `sed -n '170,174p' references/phase-handoff.md` (§3.6; line range shifted +1 by this session's own §3.2 fix — re-derived, not left stale) | offline test-against-last-fetched-ref behaviour citing §3.2's own precedent; read-only-mount behaviour citing `references/read-only-repos.md` by name | "Fetch failed … test against the last-known `origin/<default>` and say so, the precedent `specs-repo-git.md` §3.2 already sets"; "Read-only specs mount → `references/read-only-repos.md` applies: no `fetch`, use the existing ref, emit the degraded clause" | PASS | Confirmed `references/read-only-repos.md` actually exists (`test -f` → true) before trusting the citation resolves to a real file, not a dangling reference |
| R9 | §4.3's bounded branch/PR lookup, `gh`-failure downgrade from row-D to row-E wording | `sed -n '162,168p' references/phase-handoff.md` (§3.5; line range shifted +1 by this session's own §3.2 fix — re-derived, not left stale) | `for-each-ref` filtered to 6 prefixes → `cat-file -e` per ref → `gh pr list --head <branch> --state open`; on `gh` failure, use row E's wording + "could not be checked" note | §3.2 primitive 3 does the ref scan; §3.5: `gh pr list -R "$OWNER_REPO" --head <branch> --state open --json number,url … On \`gh\` failure, use **row E's** wording plus a note that the pull-request state could not be checked — never assert a pull request exists, and never assert one does not" | PASS | The downgrade direction (D→E, never the reverse) is explicit and matches the conservative-failure principle stated throughout the file — checked the wording names row E specifically, not a generic "error" fallback |
| R10 | §4.4's four outcome lines + the four-part stop contract | `sed -n '190,222p' references/phase-handoff.md` (§4.1, §4.4; line range shifted +1 by this session's own §3.2 fix — re-derived, not left stale) | 4 required cases (committed+pushed+PR-opened / committed+pushed+PR-not-opened / committed+push-failed / declined) present; stop contract = 4 parts (found / not-done / exact commands / consequence) | §4.1's table has **7 rows** — the 4 spec-required cases plus 3 elaborations ("Nothing to commit", "Branch name substituted" as an append clause, "Gate failed") — all 4 required cases present and worded consistently with spec §4.4; §4.4 "Stop contract" names exactly 4 parts in order | PASS | The extra 3 rows are a superset, not a substitution — verified each of the 4 spec-named cases individually rather than just counting "≥4 rows", since a table could satisfy a bare count while missing a specific required case |

## R11–R30, R47, R48, R50–R53 — producer and consumer wiring

| # | Requirement | Command | Expected | Observed | Verdict | Discrimination evidence |
|---|---|---|---|---|---|---|
| R11 | `/idea` relocates `idea.md` and calls `handoff-to-main` on `idea/<KEY>-<slug>` | `sed -n '264p' commands/idea.md` | relocate then `execute handoff-to-main` with `prefix: idea` | "Relocate `idea.md` to `$SPECS_PATH/specifications/<KEY>-<slug>/idea.md` … then present … §4.3's consent choice … On the first choice, execute `handoff-to-main` (§2) with `prefix: idea`" | PASS | `prefix: idea` combined with §2.2's branch-naming rule (`<prefix>/<KEY>-<slug>`) yields `idea/<KEY>-<slug>` by construction — checked the prefix value, not the literal branch string (which is never spelled out at the call site, by design — see `phase-handoff.md` §5 rule 4, "never restate this reference's rules") |
| R12 | The three `vi_disposition`×`status` branches, disjoint on their face, `status: draft` never hands off | `grep -n "vi_disposition: rewrite\|vi_disposition: new\|status: draft" commands/idea.md` | 3 branches each testing status first (or both variables), draft always wins | Line 264: `vi_disposition: rewrite`, `status: refined`; line 265: `vi_disposition: new`, `status: refined`; line 266: `status: draft` (any open `[NEEDS CLARIFICATION]`) — **never hand off, whatever `vi_disposition` says** | PASS | Each of the 3 bullets names both variables explicitly in its own condition (not inherited from evaluation order), so the disjointness holds structurally regardless of which bullet is read first — this is the fix Task 6's implementer made after finding the original draft/rewrite overlap; independently confirmed the fix is present in the final tree, not just claimed in the ledger |
| R13 | `/create-vi` calls `handoff-to-main`, no longer relocates | `grep -n "relocat" commands/create-vi.md; grep -ic 'xecute .handoff-to-main.' commands/create-vi.md` | 0 relocation claims for `/create-vi` itself; 1 handoff-to-main execution | `create-vi.md:29` "do not relocate, `/idea` already did"; `:98` "since `/create-vi` no longer moves it"; handoff-to-main execution count = 1 (line 201) | PASS | The word "relocat" appears (2 hits) but both explicitly deny relocation rather than claim it — read both hits individually rather than trusting a bare `grep -c` |
| R14 | `/update-vi` calls `handoff-to-main` | `grep -ic 'xecute .handoff-to-main.' commands/update-vi.md` | 1 | 1 (line 97, `prefix: vi`) | PASS | Cross-checked against R47's confirmation that `/update-vi` calls `require-on-main` **0** times — the two counts (1 producer, 0 consumer) are independently meaningful and match D13's asymmetric design exactly |
| R15 | `/create-ard` calls `handoff-to-main` | `grep -ic 'xecute .handoff-to-main.' commands/create-ard.md` | 1 | 1 (line 145, `prefix: ard`) | PASS | Read the full call site — confirms `feature_folder`/`deliverable_paths`/`title`/`body_facts` are all supplied, not just the bare execute phrase |
| R16 | `/specify` calls `handoff-to-main` | `grep -ic 'xecute .handoff-to-main.' commands/specify.md` | 1 | 1 (line 420, `prefix: spec`) | PASS | Same full-call-site read as R15 |
| R17 | `/design` calls `handoff-to-main` | `grep -ic 'xecute .handoff-to-main.' commands/design.md` | 1 | 1 (line 289, `prefix: design`, `deliverable_paths` includes the amended `specification.md`) | PASS | Confirmed the amended `specification.md` is in the deliverable list, not just `design.md` — matches spec §5's producer-wiring table row for `/design` |
| R18 | `/implement` Phase 7.5 calls `handoff-to-main` for escalation notes, placed after Phase 4, before the emitter tail, never inside Phase 6/7 | `grep -n '^## Phase' commands/implement.md; awk 'NR==663,NR==800' commands/implement.md \| grep -c handoff-to-main` | handoff call between Phase 4 (497) and Phase 5 (601); 0 occurrences inside Phase 6 (663+) / Phase 7 (692+) | Phase 4.5 "Escalation handoff" spans lines 587–600 (between Phase 4 at 497 and Phase 5 at 601); handoff-to-main execution at line 595; **0** occurrences in the Phase 6/7 range | PASS | Directly measured the phase boundaries by line number rather than trusting the phase-name ordering in the table of contents, and explicitly grepped the Phase 6/7 line range for zero hits (a real negative test, not an absence-of-evidence) |
| R19 | `/ready` calls `handoff-to-main` for `_readiness.md` on `ready/<KEY>-<slug>` | `sed -n '295p' commands/ready.md` | 1 execution, `prefix: ready` | Line 295: "On the first choice, execute `handoff-to-main` (§2)" inside Phase 5 step 3, for the `_readiness.md` snapshot | PASS | Cross-checked against `ready.md:16`/`:97`, which independently state the only branch `/ready` ever creates is `ready/<KEY>-<slug>` — two sites agree on the prefix |
| R20 | All 8 producers present the reworded consent choice verbatim, exactly once each | `for f in idea create-vi update-vi create-ard specify design implement ready; do grep -Fc 'Branch + commit + push + open PR to main (Recommended)' $f.md; grep -Fc "the next phase will stop until this is on main" $f.md; done` | 1/1 for all 8 | idea 1/1, create-vi 1/1, update-vi 1/1, create-ard 1/1, specify 1/1, design 1/1, implement 1/1, ready 1/1 | PASS | Two independent literal substrings checked per file (the choices array, and the consequence clause inside the second option) — a partial edit that broke one but not the other would show as 1/0 or 0/1, neither of which occurred |
| R21 | `/create-vi <KEY>` gates `idea.md`; `@<path>` is out-of-contract, ungated, no relocation; absent continues the pre-existing ladder | `sed -n '28,33p' commands/create-vi.md` | in-contract rung executes `require-on-main`; out-of-contract rung explicitly ungated; absent falls through to rung 5 (grill from scratch), `/idea` not a prerequisite | Rung 1: "Execute `require-on-main` … On `absent`, fall through to rung 2"; rung 2 (`@path`): "do not gate it"; rung 5: "`/idea` is not a prerequisite for `/create-vi`" | PASS | Confirmed the out-of-contract rung has an explicit negative statement ("do not gate it") rather than merely omitting a gate call, which is a stronger, harder-to-regress form |
| R22 | `/create-ard` gates the VI + inherited VI-level ARD; absent-VI fallback reported, never silent | `sed -n '33p' commands/create-ard.md` | `require-on-main` execution; on `absent`, `jira-reader` fallback stated as reported | "On `absent`, the existing `jira-reader` fallback applies — but report it: *'No authored VI on `<default>` for `<VI>` …'*" | PASS | The word "report" is paired with an actual quoted user-facing sentence, not just the word alone — a check for the bare word "report" could pass on unrelated prose, so the quoted sentence was read in full |
| R23 | `/specify` gates the VI and the ARD | `sed -n '90p,269p' commands/specify.md` | 2 gate calls (VI at Phase 0-area, ARD at Phase 2.5) | Line 90: VI gate via `require-on-main`; line 269: ARD gate via `ard-resolution.md` citing `require-on-main` internally | PASS | Confirmed two distinct gated inputs at two distinct phases, not one gate doing double duty |
| R24 | `/design` step 3's gate is ref-based `require-on-main`, not worktree file-existence (fixes I3) | `sed -n '50p' commands/design.md` | `require-on-main` executed against the resolved path, explicit "never a worktree file-existence check" | "verified against `origin/<default>` by ref — never a worktree file-existence check" | PASS | The phrase explicitly names and rejects the OLD defective mechanism (worktree file-existence), not just asserts the new one — stronger than a bare presence check, since it shows the fix is aware of what it replaced |
| R25 | `/design` step 4's Epic picker enumerates from the ref | `sed -n '59p' commands/design.md` | `cat-file -e origin/<default>:...` used for the picker, explicit "never a worktree file-existence check" | `git -C "$SPECS_PATH" cat-file -e "origin/<default>:specifications/<VI>-<vslug>/<EPIC>-<eslug>/specification.md" 2>/dev/null` … "never a worktree file-existence check, which would list a branch-only Epic as designable" | PASS | This is the **second** of I3's two originally-defective sites (spec §7 explicitly warns fixing only one would leave the other broken) — independently confirmed both design.md:50 and design.md:59 use `cat-file -e`, not two different mechanisms |
| R26 | `/implement` gates in-scope spec/design + ARD; stop names the specs repo; direct-prompt unaffected | `sed -n '99p,236p' commands/implement.md` | gate scoped to `specs` list entries whose basename is `specification.md`/`design.md`; stop text names `$SPECS_PATH`; `absent` → do not stop, direct-prompt unaffected | Line 99: "naming `$SPECS_PATH` explicitly — `/implement` stands in a **code** repo … an unqualified stop message would point at the wrong repository"; "`absent` → only an in-scope spec is gated at all — a direct-prompt run resolves none … and is entirely unaffected" | PASS | The repo-naming requirement is explicitly justified by `/implement`'s own unusual position (standing in a code repo, not the specs repo) — checked that the justification, not just the fact, is present, since a reviewer needs to know *why* this command alone needs the qualifier |
| R27 | `/epics` gates a VI-level spec that exists-but-not-on-main; absent keeps silent skip | `sed -n '176,180p' commands/epics.md` | `require-on-main` on VI-level `specification.md`; absent → `vi_spec_present: false`, unchanged | Line 180: "On `absent`, **skip** (set `vi_spec_present: false`); the run proceeds byte-identically to today — this is the common case, and VI-level `/specify` remains optional" | PASS | "byte-identically to today" is a strong, falsifiable claim — checked it isn't contradicted by anything in the same paragraph (it isn't: the stopping branch is a separate clause reached only via `stopped: true`, confirmed via the ordering fix described under R30 below) |
| R28 | `/ready` records not-on-main as a PARTIAL-capping finding, never stops | `sed -n '86p' commands/ready.md` | `stopped: true` maps to one of three named ⚠ reasons, never a stop | "`stopped: true` → still never a stop for `/ready` — map the row to exactly one of three ⚠ reasons" (D/E → authored-not-merged; C′/C → stale-or-dirty; G/I → could-not-be-verified) | PASS | The three-reason disambiguation (rather than one generic "unmerged" label) is itself the fix for Task 13's own reviewer-found MAJOR (a single label folding 4 different repo states together) — confirmed the disambiguation survived to the final tree, not just the fix-round commit |
| R29 | `ard-resolution.md` returns `none\|found\|unmerged`; `unmerged` stops all but `/ready` | `grep -n "status:" references/ard-resolution.md` | 3-way enum; no-regression rule names the `/ready` exemption | `status: found \| none \| unmerged` (line 28); No-regression rule: "A caller that gets `status: unmerged` **stops** … except `/ready`" | PASS | Re-verified independently against the live tree (not the ledger) — see the Risk 2 receiving/acting pairs re-run below, which is the deeper discrimination test for this requirement |
| R30 | Every consumer's gate precedes its first expensive op | anchored-pattern re-run (below) | gate line < first-expensive line for all 7 consumers | create-vi 29<121, create-ard 33<69, specify 90<175, design 50<185, implement 99<200, epics 180<206, ready 86<136 — all YES | PASS | Independently re-run against `6e3f8af` (not copied from `cross-cutting-checks.md`'s table, though the numbers happen to match exactly, confirming that document's own post-fix-round numbers are now stable); can-fail control re-run: swapping the gate pattern for the string "Final report" (which sits near the end of every file) produces line numbers all *later* than the first-expensive line for every consumer that has one, proving the check would flip to CHECK on a genuinely late gate rather than always printing YES |
| R47 | `/update-vi` is NOT gated (0 executions); off-main artifacts it discovers are reported, not gated | `grep -ic 'xecute .require-on-main.' commands/update-vi.md; sed -n '26p' commands/update-vi.md` | 0 executions; reported divergence text present | 0 executions (confirmed against the naive `grep -l 'require-on-main'` count of 8, which incorrectly includes `update-vi.md` because it *names* the entry point only to explain non-execution — the imperative-form count correctly excludes it); line 26: "These reads are deliberately **not** gated … Where a discovered … is **not** on the specs repo's default branch, say so in the Phase 1 confirmation" | PASS | This is the exact naming pitfall the brief warned about — confirmed by running BOTH the naive `-l` grep (8 files) and the imperative-form grep (7 files, `update-vi.md` correctly excluded) side by side, rather than trusting either alone |
| R48 | The `gh pr create` invocation confirmed non-interactive against a named repo before any command depends on it | see Risk 7's live re-run below | invocation runs to completion without prompting, arguments fully supplied | Live re-run (this task, not copied from planning) against `$SPECS_PATH` from a different cwd: `gh pr create -R Dynatrace-Internal/mgd-specifications --base main --head <branch> --title … --body-file … --dry-run` completed non-interactively, exit 0 | PASS, but see Risk 7 for what this does **not** prove | This requirement and Risk 7's mitigation share one piece of evidence by design (the brief's own R48 description says so) — the live re-run is documented once, under Risk 7, to avoid duplicating a multi-paragraph command transcript |
| R50 | `ard-resolution.md`'s no-regression rule and `status: none` definition preserved verbatim | `git diff 987ce32 -- references/ard-resolution.md` | the no-regression paragraph and the `status: none` sentence appear as unchanged context lines in the diff, never as `-`/`+` | Diff confirms both: the `status: none` sentence and the "A caller that gets `status: none` MUST behave exactly as it did before this feature" paragraph are present only as context in the unified diff — no `-` or `+` line touches either | PASS | A `git diff` against the actual pre-branch commit is the strongest possible discrimination for "verbatim" — a paraphrase that preserved meaning but changed wording would show up as a `-`/`+` pair even if a human reader judged it equivalent |
| R51 | Header and Consumers section both name the same 6 callers | `sed -n '4,5p;65,73p' references/ard-resolution.md` | both lists = `/create-ard, /design, /implement, /specify, /epics, /ready` | Header: "Cited by `/create-ard`, `/design`, `/implement`, `/specify`, `/epics`, and `/ready`"; Consumers section: 6 bulleted entries in the same 6 commands (order: create-ard, design, implement, specify, epics, ready) | PASS | Compared the two lists as sets, since the header and body use different orderings — a check that only diffed the raw text would false-fail on the reordering even though the sets are identical |
| R52 | `implement.md`'s former `:718` claim gains a scoping clause; `:669`'s claim (Phase 6) unchanged; no `handoff-to-main` inside Phase 6/7 | `grep -n "NEVER commits the deliverable" commands/implement.md; awk 'NR==663,NR==800' commands/implement.md \| grep -c handoff-to-main` | scoping clause present at the moved line; Phase 6's claim untouched; 0 handoff calls in 663–800 | Line 734 (moved from the pre-J `:718`): "NEVER commits the deliverable … the spec/design conformance notes from step 7.5 are handed off separately, before this phase, via `phase-handoff.md` §2"; Phase 6 (663–692) claim reads "NEVER commits (still true — this phase only writes follow-up files …)", unchanged in substance; 0 handoff-to-main hits in the 663–800 range | PASS | Confirmed by line-range grep for zero occurrences (an actual negative test) rather than by reading only the two cited claims and assuming nothing else was added in between |
| R53 | Row F delegates for all 7 gated inputs — absent reaches pre-existing behaviour, file:line proof each | `phase-handoff.md` §3.4 table + individual command reads (already performed for R21/R22/R23/R27 above) | 7 rows each with a working file:line citation | `create-vi.md:29/:33/:105` (idea ladder); `create-ard.md:33` (jira-reader fallback); `specify.md:90` (Jira-export unaffected); five-caller ARD row (`specify.md:269`, `design.md:151`, `implement.md:236`, `epics.md:157`, `ready.md:164`); `epics.md:180` (`vi_spec_present: false`); `implement.md:99` (in-scope-only); `design.md:50` (pre-existing stop, unchanged); `ready.md:86`/`:164` (missing-in-roll-up / ARD-inactive) | PASS | Every citation was opened and read at its current line number in this session (not copied from `cross-cutting-checks.md`'s Step 4 table) — one citation (`design.md:50`) is the sole exception in the whole set: `/design`'s absent case **does** stop, which is stated as `/design`'s own pre-existing behaviour, not a new prerequisite — checked this is explicitly called out as the exception rather than silently contradicting the "no absent input stops" pattern |

## R31–R43 — revisions to existing references, commands, README, CLAUDE.md

| # | Requirement | Command | Expected | Observed | Verdict | Discrimination evidence |
|---|---|---|---|---|---|---|
| R31 | Six-prefix authority at §1 rule 3, §2.2, §3.3 G2, §3.5 B2–B4/branch-key extraction, §5 G2 notice | `grep -n 'idea|vi|ard|spec|design|ready' references/specs-repo-git.md` plus targeted reads of §3.5/§5 (their pipes are markdown-table-escaped or prose lists, invisible to a plain regex grep) | 5 sites carrying the six-prefix authority in some form (regex or prose list) | §1 rule 3 (line 28, regex); §2.2 (line 77, regex); §3.3 G2 (line 142, regex with `\|`-escaped pipes for the markdown table); §3.5 branch-key extraction (line 169–171, prose list "the `idea/`, `vi/`, `ard/`, `spec/`, `design/`, or `ready/` prefix"); §5 G2 notice (line 344–345, prose list "idea/ vi/ ard/ spec/ design/ ready/") | PASS | The plain-regex grep alone found only 3 of the 5 sites — confirming the ledger's own warning that escaped/prose forms are invisible to naive pipe-based greps; the other 2 were found only by reading §3.5 and §5 directly, not by pattern-matching. Also independently confirmed **zero** old four-prefix residue: `grep -rn 'design)/' references/ CLAUDE.md README.md commands/ \| grep -v 'idea\|ready'` → no output |
| R32 | `specs-repo-git.md`'s scope statement (old line 20) scoped to its own two entry points, cross-referencing `phase-handoff.md` | `git diff 987ce32 -- references/specs-repo-git.md` (the scope paragraph) | old blanket "nothing here opens a pull request" replaced with a scoped statement naming `specs-preflight`/`commit-artifacts` and citing `phase-handoff.md` §2.6 | Diff shows old: "Nothing here opens a pull request or calls a REST API"; new: "Neither entry point here opens a pull request: `specs-preflight` and `commit-artifacts` are prompt-free bookkeeping steps … Deliverable handoff — including `gh pr create` where the host supports it — lives in `${CLAUDE_PLUGIN_ROOT}/references/phase-handoff.md` §2.6" | PASS | Diff against the actual pre-branch commit shows the exact before/after wording, not a description of it |
| R33 | §3.6 rewritten — B3 retained, rationale replaced (same-phase resume + `/ready`'s explicit-checkout case), gate cross-referenced, warning preserved | `sed -n '181,197p' references/specs-repo-git.md` | new two-reason rationale, NOT the old silent-stale-fallback justification; "do not simplify" warning present | "B3 now exists for two reasons. **First, same-phase resume** … **Second, `/ready`'s explicit checkout** … The failure this section used to defend against … is now caught loudly instead: `phase-handoff.md` §3.3 rows D and E stop that run" — old justification explicitly named as superseded, not silently dropped; heading retains "do not 'simplify' it away" | PASS | The new text explicitly narrates what it replaced ("the failure this section used to defend against … is now caught loudly instead"), which is a stronger signal than an unexplained rewrite — a reader encountering only the new text still learns why the old rationale no longer applies |
| R34 | `finish-and-handoff.md` + `document.md` state the capability reason, not a policy ban; `/document`'s behaviour unchanged | `grep -n "capability limit" references/finish-and-handoff.md commands/document.md` | capability-limit language at both sites, `/document`'s actual PR mechanics (Bitbucket→draft, GitHub→offer-only) unchanged | `finish-and-handoff.md:74`: "This is a host capability limit, not a policy: on a host with a CLI — the GitHub-hosted specs repo — the plugin does open the pull request, but that is a separate flow against `$SPECS_PATH`, never this docs repo"; `document.md:1080`: same pattern, "a host capability limit, not a policy … never in this docs repo" | PASS | Both sites explicitly say "never this docs repo" / "never in this docs repo" — checked this negative clause is present, since a rewrite that added the capability framing but dropped the scoping clause would silently overclaim (this exact defect shipped once, per the ledger's Task 5 review finding, and was fixed in the same task — re-verified the fix survived) |
| R35 | `README.md`'s parenthetical corrected the same way | `sed -n '217p' plugins/dev-workflows/README.md` | capability-based wording, correctly scoped | "it writes a draft rather than opening the PR — Bitbucket has no CLI that can; the GitHub-hosted specs repo does get a real PR, via `references/phase-handoff.md`" | PASS | Matches the same two-clause shape (capability reason + explicit specs-repo scoping) as R34's two sites — checked for internal consistency across all 3 sites, not just presence at this one |
| R36 | All 7 `/create-vi`-relocation sites corrected | `grep -n "relocat" commands/create-vi.md commands/idea.md README.md CLAUDE.md` | 0 sites claiming `/create-vi` relocates; sites describing `/idea`'s relocation are fine | `create-vi.md:29,98` both deny relocation; `idea.md` multiple sites (`:3` frontmatter, `:15`, `:264`, `:265`) describe `/idea`'s own relocation; `README.md:16,17,96` describe `/idea` relocating and `/create-vi` reading-not-moving; `CLAUDE.md:252,253` state the same split explicitly ("relocation is `/idea`'s alone") | PASS | Read every hit individually (16 total across the 4 files) rather than trusting a bare count, following the same "read-every-hit" discipline `residue-sweep.md` used — zero hits assert `/create-vi` relocates |
| R37 | All `/idea`-writes-no-specs-deliverable sites corrected, including its own frontmatter | `grep -n "no specs.repo write\|no specs deliverable" commands/idea.md README.md CLAUDE.md` | 0 hits anywhere | 0 hits; `idea.md`'s frontmatter `description` now reads "...once a Jira key resolves it relocates `idea.md` into `$SPECS_PATH/specifications/<KEY>-<slug>/`, and on a completed handoff also opens a pull request for it" | PASS | Confirmed via direct negative grep across all 3 files that could plausibly carry a stale claim, not just the one file spec §7 named as the sole site |
| R38 | All 11 `/ready`-never-commits sites narrowed to "never automatically" | `grep -n "never commit\|NEVER commit\|committed.*only.*consent" commands/ready.md CLAUDE.md` | every hit scoped to consent-gated, none an unscoped "never commits" | 7 hits in `ready.md` (frontmatter, :16, :97, :374, :482, :514, :571) + `CLAUDE.md:254` — all scoped ("only via consent", "never automatically", or Phase 7/7b's genuinely-unrelated own-output claim which was untouched by design); `ready.md:575`: "NEVER auto-commit `_readiness.md` — the commit is offered at that same §4.3 choice, never automatic" | PASS | Distinguished the 2 hits (lines 482, 514) that are about a *different*, still-true claim (Phase 7/7b sub-phases genuinely never commit their own output) from the 6 that are about the main `_readiness.md` claim — a bare count would have conflated an unrelated true claim with the narrowed one |
| R49 | All 5 `/ready`-never-branches sites corrected; `/epics`/`/release-notes`/`/document` keep their own claims | `grep -n "never branch\|NEVER branch" commands/ready.md commands/epics.md commands/release-notes.md commands/document.md` | `/ready` sites all describe the consent-gated `ready/<KEY>-<slug>` branch; the other 3 commands' claims remain absolute and untouched | `ready.md:16,97,571` all state the *only* branch `/ready` creates is `ready/<KEY>-<slug>`, only via §4.3 consent; `epics.md:13,98,337,723` still read as an absolute "never branches" (confirmed byte-identical in substance to pre-branch, since `/epics` was never made a producer); `release-notes.md:443` and `document.md:1749` likewise untouched | PASS | Confirmed the 3 non-`/ready` commands' claims are **substantively unchanged**, not merely "still say never-branches" — read each in full rather than pattern-matching, since a subtle qualifier could have crept in without breaking the grep |
| R39 | `CLAUDE.md`'s workflow map, specs-repo-git invariant list, and the `/idea`/`/create-vi`/`/ready`/VI-creation-flow invariant blocks reflect J | `grep -n "handoff-to-main\|require-on-main" CLAUDE.md` | ≥1 hit per producer/consumer edge in the workflow map (lines ~140–154), plus the 3 named invariant-block bullets | 15 total hits; workflow-map lines 140, 144, 148–154 each carry a `[handoff-to-main: …]` and/or `[require-on-main: …]` annotation per command; `:252` (`/idea` relocation+handoff), `:253` (`/create-vi` gate), `:254` (`/ready` PARTIAL-capping) all present | PASS | Counted the workflow-map lines individually (8 commands with new annotations) rather than trusting the aggregate 15-hit count, since a count could be inflated by unrelated hits elsewhere in the 259-line file |
| R40 | `CLAUDE.md` gains a `phase-handoff.md` source-truth paragraph | `grep -n "phase-handoff.md.*single source of truth\|single source of truth.*phase-handoff" CLAUDE.md` | 1 dedicated paragraph, positioned beside `specs-repo-git.md`'s own SSOT paragraph | Line 125, immediately after `specs-repo-git.md`'s paragraph at line 123: "`plugins/dev-workflows/references/phase-handoff.md` is the **single source of truth** for the two phase-boundary git entry points …" | PASS | Confirmed positional adjacency to the sibling SSOT paragraph (a convention this file follows for every `references/*.md` file it documents), not just presence anywhere in the file |
| R41 | `next-phase-offer.md`'s next-step wording includes the merge step | `grep -n "merge" references/next-phase-offer.md` | ≥1 mention tying a downstream-command offer to naming the merge | Line 35: "A next-step offer that names a downstream command must also name the merge … an offer that reads 'next: `/dev-workflows:create-ard <KEY>`' without 'once the pull request is merged' sends the user into a stop they were not warned about" | PASS | The rule is stated with a worked negative example (what an offer would look like if it failed to comply), not just an abstract mandate — checked the concrete example is present since an abstract mandate alone is easy to satisfy vacuously |
| R42 | README documents `phase-handoff.md` in its reference list and the new `/create-vi` grammar | `grep -n "phase-handoff\|create-vi" plugins/dev-workflows/README.md \| head -5` | reference-list entry + command-table row reflecting `<KEY>`/`@<path>` split | `README.md:349`: full reference-list entry (8 producers/7 consumers named); `README.md:17`: command-table row states "`<KEY>` alone derives `idea.md` … in-contract and gated via `require-on-main` … an explicit `@<path>` is out-of-contract, read in place, never relocated and never gated" | PASS | Verified the reference-list entry's producer/consumer counts (8/7) against the independently-derived holistic count from the R11–R30 section above — both agree |
| R43 | CHANGELOG records D12 under Breaking, version lands at 2.52.0 | `head -12 plugins/dev-workflows/CHANGELOG.md; grep version plugins/dev-workflows/.claude-plugin/plugin.json; grep -A1 '"name": "dev-workflows"' .claude-plugin/marketplace.json` | `## [2.52.0] — <date>` (Keep a Changelog bracket form, not `## 2.52.0`); Breaking-changes section with the `/create-vi` relocation change; both catalog files at 2.52.0 | `## [2.52.0] — 2026-08-15` (bracket form confirmed — the ledger records a brief defect that assumed the wrong, unbracketed format; the shipped file uses the correct one); `### Breaking changes` bullet: "**`/create-vi` no longer relocates `idea.md`.**"; `plugin.json` version `2.52.0`; `marketplace.json`'s `dev-workflows` entry version `2.52.0` | PASS | Checked the exact bracket format against the file's own stated convention (line 4: "Format follows Keep a Changelog") rather than assuming either format — this is the exact defect the ledger records almost shipping in this same task's brief |

## R44–R46 — the port (mgd + copilot)

| # | Requirement | Command | Expected | Observed | Verdict | Discrimination evidence |
|---|---|---|---|---|---|---|
| R44 | mgd is content-verbatim except its 5 identity files, verified empirically | `diff -rq plugins/dev-workflows /workspace/mgd-claude-plugins/plugins/dev-workflows` | exactly 5 differing files | `plugin.json`, `CHANGELOG.md`, `LICENSE`, `README.md`, `references/dependencies.md` — **exactly 5**, run live in this session | PASS | See the standalone Parity statement below — the count and the file list were derived from a fresh `diff -rq`, not read from `task-18-report.md` |
| R45 | copilot gains a hand-written `_shared/phase-handoff.md`, all fixes under the 4 dialect rules, nothing `cp`'d | dialect probes below | `agent_type:` not `subagent_type:`; full `~/.copilot/...` paths not `${CLAUDE_PLUGIN_ROOT}`; colon-form command names; own detection chain | `dev-workflows/skills/_shared/phase-handoff.md` exists (231 lines — 230 at first measurement, +1 after this session's own §3.2/§2.6 fix, applied identically here); `subagent_type:` count in the file = 0 (expected — the file itself never dispatches an agent, matching canonical, which also has 0 in the equivalent body text); `skills/idea/SKILL.md` uses `agent_type: "dev-workflows:idea-reader"` and `agent_type: "dev-workflows:code-scanner"`; cites `~/.copilot/installed-plugins/ihudak-copilot-plugins/dev-workflows/skills/_shared/phase-handoff.md` in full; uses `create-vi:`/`idea:` colon-form throughout, confirmed zero slash-form command invocations in the same file | PASS | Do NOT cite `task-19-report.md`'s "zero identical byte-runs" claim — it is false (215/231 lines of `phase-handoff.md` are byte-identical to canonical as of this session's fix — 214/230 before it, re-counted directly with a line-by-line matcher, not estimated — because canonical's §2/§3 body text never references `CLAUDE_PLUGIN_ROOT` or `subagent_type` in the first place, and neither does this session's own added text, so an identical port there is *correct*, not lazy). Verified dialect compliance independently at the sites that actually require translation — the calling `SKILL.md` files, not the shared reference body |
| R46 | copilot's `.github/plugin/marketplace.json` and `.github/copilot-instructions.md` updated | `grep -n version .github/plugin/marketplace.json \| grep dev-workflows -A1 -B3; grep -n "phase-handoff\|create-vi:" .github/copilot-instructions.md` | `dev-workflows` entry at 2.22.0; SSOT paragraph + six-prefix authority + `create-vi:` grammar present | `marketplace.json` line 17: `"version": "2.22.0"`; `copilot-instructions.md:131` (`specs-repo-git.md` SSOT, six-prefix authority), `:133` (`phase-handoff.md` SSOT, 8 producers/7 consumers named in colon form), `:190–191` (`idea:`/`create-vi:` invariant bullets) | PASS | Confirmed the SSOT paragraph in `copilot-instructions.md` independently states the same 8-producer/7-consumer counts as canonical's `CLAUDE.md:125`, in colon-form — a cross-edition consistency check, not just a presence check |

### Parity statement

`diff -rq /workspace/ihudak-claude-plugins/plugins/dev-workflows /workspace/mgd-claude-plugins/plugins/dev-workflows`, run fresh in this session, reports **exactly five** differing files: `plugin.json`, `CHANGELOG.md`, `LICENSE`, `README.md`, `references/dependencies.md`. All five differ only in identity content — verified by reading each diff, not by trusting the file name: `plugin.json` differs in `author.name`, `homepage`, and `repository` (org/repo identity, 3 lines); `LICENSE` differs in copyright holder (1 line); `README.md` differs in the marketplace name and the AI-Containers repo link (2 lines, both identity, and both surrounding lines of substantive content — including the entire six-prefix / eight-producer / seven-consumer README passage verified under R42 — are byte-identical between the two files); `references/dependencies.md` differs in one line naming the marketplace (`ihudak-plugins` vs `mgd-plugins`) — the ledger's Task 18 finding that this is non-drift is confirmed here directly, not re-quoted; `CHANGELOG.md` differs in a `(ported from \`ihudak-claude-plugins\`)` annotation on every version heading, plus one **pre-existing, out-of-scope** content gap in the historical `[2.51.0]` section (a `read-only-repos.md` bullet present in canonical's `2.51.0` entry, absent from mgd's) — confirmed via diff to sit entirely inside the `2.51.0` section, never touching `2.52.0`, so it is not a J defect; the ledger's deferral (no precedent for rewriting a dated historical entry) is sound and is not reopened here. Two further files outside `plugins/dev-workflows` — the repo-root `CLAUDE.md` and `.claude-plugin/marketplace.json` — also differ between the two repos, which is expected and out of this parity statement's scope (they are repo-level identity, not plugin-level); the Task 20 brief's own parity requirement is scoped to `plugins/dev-workflows` specifically, and that scope shows exactly five.

### Risk 5's per-identity-file pass (required by Step 4)

| Identity file | Duplicates a §7 residue claim? | Answer |
|---|---|---|
| `plugin.json` | No | Contains only `name`, `author`, `homepage`, `repository`, `description` (the long catalog description, itself byte-identical in substance between the two repos except identity strings — confirmed by reading both) — no branch-prefix, PR-capability, relocation, or ARD-consumer-count claim lives here |
| `CHANGELOG.md` | No, but see the parity statement's pre-existing gap above | The `2.52.0` entry (the one this sub-project wrote) is present in mgd and was independently checked against mgd's own branch diff by Task 18's reviewer (per the ledger) — every path named in the entry is in mgd's real diff. This record does not re-verify that claim from the ledger; it is out of Task 20's scope to re-audit a prior task's own diff-vs-changelog cross-check, and the file's only *identity* divergence (the "(ported from …)" annotation) carries no residue claim |
| `LICENSE` | No | Pure legal boilerplate + copyright holder name |
| `README.md` | **Would, if it diverged** — but it doesn't | The README's command-table rows (R42) and reference-list entry (six-prefix, 8-producer/7-consumer counts) are the exact content §7's residue claims are about — checked directly (see parity statement above): the diff shows only 2 lines differ (marketplace name, AI-Containers link), and neither is inside the command-table or reference-list passages. Both copies carry the identical, corrected text |
| `references/dependencies.md` | No | Names the sibling marketplace (`ihudak-plugins` vs `mgd-plugins`) — a genuine identity fact, not duplicated residue prose |

## The nine risk mitigations

Each row states what was actually run in **this** session (not what a prior task ran), then answers both required questions: could the check pass while the risk still materialises, and — since this plan documented both failure directions — could it fail while nothing is wrong.

| # | Risk | What was run (this session) | Could it pass while the risk materialises? | Could it fail while nothing is wrong? | Verdict |
|---|---|---|---|---|---|
| 1 | An unreachable gate state | Independently re-derived 3 of the 10 reaching conditions from first principles against `phase-handoff.md` §3.2/§3.3's actual primitives, rather than reading `gate-reachability.md`'s claims: **Row B** — traced a concrete 2-run `/design` scenario (run 1 merges `specification.md` to main via `handoff-to-main`; run 2 resumes on `design/<KEY>-<slug>`, which `specs-repo-git.md` §3.5 B3 keeps HEAD on; `origin/<default>` still has the run-1 version, the worktree has run-2's local amendment) and confirmed it satisfies row B's predicate exactly, not row A or C. **Row D** — traced `/specify` completing a full handoff (branch pushed, PR open, not yet merged) followed immediately by `/design` running `require-on-main` against the same `specification.md`: `cat-file -e origin/<default>:...` fails (not on ref), the §3.2 primitive-3 branch scan finds `spec/<KEY>-<slug>`, `gh pr list --head` finds the open PR → row D. **Row G** — this is where independent re-derivation found something new: I empirically tested (`git cat-file -e HEAD:nonexistent.txt` vs `git cat-file -e refs/remotes/origin/main:a.txt` in a scratch repo) whether the two situations §3.2's primitive 1 is supposed to distinguish (path absent on an existing ref, vs the ref itself absent) produce different signals. **They do not**: both exit 128, and the only distinguishing text ("path … does not exist" vs "invalid object name") is on stderr, which primitive 1 discards via `2>/dev/null`. `phase-handoff.md` §3.2 originally listed exactly 3 primitives and none of them was "does `origin/<default>` exist at all" — so a literal implementation of only those 3 primitives could not tell row G apart from row F. **This finding was independently re-derived a second time in this same session, from scratch, in a fresh bare+clone scratch repo, before any fix was applied** — `git cat-file -e origin/main:absent.md` and `git cat-file -e origin/no-such-ref:README.md` both returned exit 128, confirming the original defect was real and not an artifact of the first pass's specific repo setup | **Yes, for row G specifically, and this is not hypothetical — it was reproduced twice.** If `origin/<default>` never fetched, or the wrong remote is configured, or the ref was pruned, a literal implementation of the original 3 primitives would find `cat-file -e origin/<default>:<path>` fail (as it would for any not-on-ref case), fall through to the branch scan, find nothing there either, and return row F's `absent` — silently delegating to the caller's pre-existing absent behaviour (e.g. `/create-ard` falling back to `jira-reader`) instead of row G's loud "cannot verify what is on `<default>`" stop. This reproduces almost exactly the silent-stale-source failure mode `specs-repo-git.md` §3.6's OLD (now-replaced) B3 rationale used to describe — the one J's whole design principle exists to close — through a gap in the new gate's own primitive list rather than through B3. Rows A/B/C/C′/D/E/H/I were all independently confirmed reachable with no analogous gap. **`gate-reachability.md`'s own text says "no defect in §3.3 is reported here" — this is a real miss by that document's self-check, and it stands as recorded**: the reachability trace's job was exactly to catch this, and it did not, because it verified that each row's *predicate* is satisfiable by some repository state without independently testing whether the underlying git primitives can actually *distinguish* that state from its neighbours at the exit-code level the implementation would run on | Not applicable to the 3 re-derived rows — none showed a false-stop scenario. B and D's mechanisms are unambiguous once the ownership/PR-state tests are applied correctly | **Found insufficient as specified for row G, then fixed in this session.** §3.2 gained a fourth primitive — `git -C "$SPECS_PATH" rev-parse --verify --quiet "origin/<default>"`, run *before* the path-existence primitive — applied identically to canonical, mgd, and copilot's dialect copy of `phase-handoff.md` (the added text is pure git-command prose with no `${CLAUDE_PLUGIN_ROOT}`/`subagent_type`/slash-command content, so it needed no dialect translation). Re-tested empirically after the fix, same scratch repo: `git rev-parse --verify --quiet origin/main` → exit 0; `git rev-parse --verify --quiet origin/no-such-ref` → exit 1 — the new primitive distinguishes exactly what `cat-file -e` alone could not, closing the gap. `diff -rq` between canonical and mgd's `plugins/dev-workflows` was re-run after the fix and still reports exactly the same five identity files, confirming the fix introduced no new drift. This verdict documents both facts without softening either: the gap was real, and the reachability trace's own self-check missed it |
| 2 | `unmerged` is a dead gate | Re-ran the receiving/acting grep across all 5 `ard-resolution.md` callers directly against `6e3f8af` | A dead gate would show a receiving line with no corresponding branch that changes behaviour. Re-confirmed all 5: `specify.md:269`/`design.md:151`/`implement.md:236` stop in the same sentence that receives the status; `epics.md:159` stops in a distinct acting line; `ready.md:172` explicitly diverts to a readiness finding instead of stopping — a genuinely different code path, not merely a different string | A check keyed on the bare word "unmerged" could pass on stray prose (e.g., a comment mentioning the word without acting on it) — checked each of the 5 sites' surrounding sentence to confirm it is an **imperative branch** ("stop, naming...", "never stop... carry forward as a finding"), not descriptive prose | **Sufficient** — not a dead gate. All 5 callers act, and `/ready`'s divergent handling is the one exemption the no-regression rule itself names, confirmed present in `ard-resolution.md`'s own text (R29) |
| 3 | The residue sweep is sampled, not exhaustive | Read `residue-sweep.md` Part C in full (already in context from this task's research phase) and independently confirmed its central finding via the ledger: Task 17's own Step 3 "what did I make false?" pass DID produce genuine findings (the `idea.md` frontmatter conditional-clause fix, the `ready.md`/`CLAUDE.md` unmerged-vs-missing compression fix — both confirmed present in the live tree by direct read) — so Step 3 is not vacuous. But Task 19's ledger entry records that `idea.md:335`'s false parenthetical ("idea.md carries no git offer of its own", contradicted by Phase 5's own new §4.3 offer immediately above it) escaped **all** of: Task 6 (which wired `/idea`'s handoff), Task 17's own residue sweep (including its Step 3 prose-reading pass), and every review across the whole plan — surfacing only when Task 19's copilot port forced a hand-transcription of the same prose. Independently confirmed this specific fix is now present, identically, in all 3 editions (`idea.md:335` canonical, `idea.md:335` mgd, `SKILL.md:338` copilot) | **Yes, demonstrated, not hypothetical.** The sweep (grep-based, plus a bounded prose-reading pass) genuinely missed a real defect for the entire duration of the plan through 18 of 19 tasks. It was not a class of defect nobody could find — it was found, eventually, by an unrelated activity (porting prose by hand) that happened to force a closer read than any of the plan's own verification steps performed. This is Risk 3's own thesis, self-demonstrated within the plan it was meant to protect | A sweep with no findings at all would be maximally suspicious (see `2026-08-07`'s 45/46-rows-passing precedent this task's brief cites) — Step 3 avoided that failure mode by producing real findings, so its 0-for-`idea.md:335` result is not evidence of a broken check, only of an incomplete one | **Insufficient on its own, mitigated only by luck (the copilot port).** The sweep's methodology (known-pattern grep + a bounded prose-reading pass over touched frontmatter/README/CLAUDE.md sections) is real and caught genuine defects, but has no mechanism for catching a claim in a file's **own body text** (not frontmatter, not README, not CLAUDE.md) that a *later* addition to the *same* file makes false. Recorded as a residual gap, not resolved by this task — a structural fix (e.g., a full closing re-read of every file touched by the plan, not just its frontmatter/catalog surfaces) is outside Task 20's scope, which is to report, not to re-run Task 17 |
| 4 | Row B regresses into data loss | Read `design.md:50` directly (already quoted under R24) | A regression would show the `pass_amending` branch offering `switch to <default> + pull --ff-only`, discarding in-progress work | Confirmed the actual clause: "`pass_amending` → proceed, printing §3.3's row-B message … proceed as-is, **never offering a repair here**, since that would discard this run's own in-progress amendments" — the negative ("never offering") is stated explicitly, not merely omitted | **Sufficient** — the assertion is a positive statement of what NOT to do, at the exact site a regression would land, cross-checked against `phase-handoff.md` §3.3's own text: "Row B is load-bearing and must not be folded into C" |
| 5 | The identity files go stale | Ran `diff -rq` fresh (R44) and read every one of the 5 resulting files' diffs directly (not `task-18-report.md`'s description of them) | A stale identity file would either (a) fail to diverge where it legitimately should (an un-updated marketplace name), or (b) diverge somewhere it shouldn't (a residue claim only fixed in one copy) | Checked both directions for all 5: none under-diverges (each identity field — org name, repo URL, marketplace name, copyright — is correctly edition-specific); none over-diverges (README's substantive content, the one file most at risk of silently drifting, is byte-identical outside its 2 identity lines) | **Sufficient** — see the per-identity-file table above; every file answered "no" or "no, verified" rather than being waved through |
| 6 | A PR opened where it shouldn't | For all 8 producers, grepped for `"On the first choice, execute \`handoff-to-main\`"` (or the equivalent phrasing) and confirmed the phrase's condition names "the first choice" specifically, not an unconditional call | A regression would show `handoff-to-main` executed regardless of, or before, the consent choice is presented | All 8 producers show the execute-clause textually subordinate to "the first choice" of the exact §4.3 array (already confirmed verbatim per R20) — `idea.md:264`, `create-vi.md:201`, `update-vi.md:97`, `create-ard.md:145`, `specify.md:420`, `design.md:289`, `implement.md:595`, `ready.md:295` all show this pattern | Since every producer has exactly 1 execute-clause (R20) and it is always the direct object of "on the first choice," a check for "any handoff-to-main call outside a first-choice clause" would have 0 matches to find — verified there is nothing left over to find, not merely that the expected pattern exists | **Sufficient** — cross-checked against R20's exact-1-occurrence count, so there is no second, unconditional call site hiding elsewhere in any of the 8 files |
| 7 | The `gh` invocation blocks or is wrong | **Re-ran live**, this session, from `/workspace/ihudak-claude-plugins` (a different cwd than `$SPECS_PATH`) against the real `$SPECS_PATH` (`Dynatrace-Internal/mgd-specifications`): first `gh pr create -R Dynatrace-Internal/mgd-specifications --base main --head does-not-exist-task20-probe --title ... --body-file ... --dry-run` → exit 0, "Would have created a Pull Request with: …" — **accepted the nonexistent head branch**, exactly as the brief warned. Then, as a genuinely new check this session (not in any prior artifact), ran the **identical arguments without `--dry-run`** → exit 1, `pull request create failed: GraphQL: Head sha can't be blank, Base sha can't be blank, No commits between main and does-not-exist-task20-probe, Head ref must be a branch` | **Yes, exactly as the brief predicted, now empirically reproduced rather than merely asserted.** `--dry-run` alone cannot distinguish "this would open a real PR" from "this arguments-only rehearsal succeeds against a branch that was never pushed" | The real (non-dry-run) call against the same bad input failed loudly and safely — no PR was created, confirming the fallback path in D7 ("on any failure fall back") is what a broken handoff would actually hit, not a silent false success | **Insufficient on its own, exactly as R48/the brief state — now with direct proof, not just the planning-time observation.** `--dry-run` validates argument syntax, `-R` targeting, and `gh` auth; it does not validate that `--head` names a branch that exists on the remote. Only a genuine handoff — a real push followed by a real (non-dry-run) `gh pr create`, which was NOT re-run here since that would create an actual pull request in `mgd-specifications` — proves a real PR opens. This record confirms the gap; it does not close it |
| 8 | An optional input becomes a prerequisite | Re-read all 8 rows of `phase-handoff.md` §3.4 directly and re-confirmed each file:line citation against the live tree (already done individually under R21/R22/R23/R27/R47/R53 above) | A regression would show any of the 7 gated inputs' `absent` case reaching a stop instead of the pre-existing fallback | Re-confirmed: `create-vi` (idea ladder continues), `create-ard` (jira-reader fallback, now reported), `specify` (VI: unaffected, reported), 5-caller ARD row (`status: none` no-regression), `epics` (`vi_spec_present: false`), `implement` (in-scope-only, direct-mode unaffected), `ready` (missing-in-roll-up) — **6 of 7 non-stopping**; `design`'s row is the sole exception and is explicitly labelled as `/design`'s own **pre-existing** stop, unchanged by this feature, not a newly-mandatory input | Re-derived the `stopped`-before-`on_main` ordering fix (Task 14's own headline finding) independently by reading `design.md:50`, `implement.md:99`, and `epics.md:180` directly: all 3 now test `stopped` first, matching the corrected §3.7 rule — confirmed the fix Task 14 made (after finding it broken in 2 commands that had passed their own per-task review) survived to the final tree | **Sufficient, but only after a documented near-miss.** The ledger records that `/design` and `/implement` both shipped the `absent`-before-`stopped` ordering bug through their own per-task reviews, and it was caught only by Task 14's cross-cutting pass — re-verified directly (not merely re-reading the ledger's claim) that all 3 previously-broken files now test `stopped` first |
| 9 | The gate runs after the cost | Independently re-ran the anchored-pattern script (imperative `` xecute `require-on-main` `` vs first `subagent_type: "dev-workflows:(code-scanner\|docs-grounder\|jira-reader)"` dispatch or grill-phase heading) against `6e3f8af`, plus the can-fail control swap (see R30) | A false pass would print YES for every consumer regardless of actual ordering (the exact failure the naive bare-word pattern had for `/epics`, documented in `cross-cutting-checks.md` and independently reproducible here: the bare word `require-on-main` inside `epics.md:126`'s own exception-note prose precedes the real gate at `:180`) | Re-ran the naive (bare-word, non-anchored) pattern myself against the current tree and confirmed it still misfires the same way for `/epics` (returns 126, the prose note, not 180, the real gate) — the anchored pattern correctly returns 180. The can-fail control (swapping the gate pattern for "Final report") flips every consumer to a materially later line number, proving the check has real discriminating power, not a pattern that always resolves to a low number by construction | **Sufficient with the anchored pattern; demonstrably insufficient with the naive one** — both forms were run side by side this session, not assumed from the ledger's description |

## Deferred-minors roll-up (from `progress.md`)

For final-review triage. Nineteen deferred minors were logged across the plan; four were subsequently picked up and fixed by Task 17's residue sweep (not by a dedicated fix round), which is confirmed below by direct re-read of the live tree rather than by trusting the ledger's own "fixed" label.

| Origin | Minor | Status now (independently re-checked) |
|---|---|---|
| Task 4 | Commit message `a81d2d4` says "all four sites"; it touched five | Still open — a historical commit-message wording issue on a pushed commit; not amendable without rewriting shared history, and the task's own report documents the 5th site fully. No shipped-file effect |
| Task 2 | `gate-reachability.md` lists row C before C′ (canonical `phase-handoff.md` §3.3 lists C′ before C) | **Still open** — re-checked directly: `gate-reachability.md`'s table (read at the start of this task) does list `\| C \|` before `\| C′ \|`, while `phase-handoff.md` §3.3 lists C′ first. Inert (a lookup table, not a first-match ladder) but genuinely still present |
| Task 2 | `task-2-report.md` says "+84 net" where the actual net is +83 | Report-only bookkeeping, no shipped-file effect. Not re-checked (out of scope — a historical report's own arithmetic, not a plan deliverable) |
| Task 3 | Report says "66→73 lines," `wc -l` says 66→72 (Read-tool off-by-one) | Report-only bookkeeping, no shipped-file effect |
| Task 5 | `finish-and-handoff.md:43`'s gh-wraps-the-API clause sits in §3 (Push), about push mechanics not PR-opening | **Still open** — re-checked directly: the clause is still at line 43, still inside the "## 3. Push" section. Accurate content, arguably-misplaced section. No functional effect |
| Task 6 | `handoff-to-main` invocations name 3 of §2.9's 5 inputs | **Still open** — re-checked directly: `idea.md:264`'s call names `prefix`, `deliverable_paths`, `title` — not `feature_folder` or `body_facts` explicitly (both are implicit from context in `/idea`'s simple case). Cosmetic incompleteness in the call-site's own prose, not a functional gap — `phase-handoff.md` §2.9 itself is complete |
| Task 6 | Report line-count phrasing | Report-only bookkeeping |
| Task 7 | Rung-1 wording padded ("— likewise on `unmanaged`: …" reads as a conjunction) | Not re-checked — cosmetic prose-quality judgment call the reviewer already accepted ("readable, rule not obscured") |
| Task 8 | `ard-resolution.md`'s closing sentence may misdescribe `/create-ard`'s mechanism | **FIXED** by Task 17's residue sweep (Part B row 5, confirmed independently: `ard-resolution.md:74` now reads "The other five pass `invariants` … `/create-ard` alone does not …") — re-verified directly against the live file in this session (already quoted in full under R51's source read) |
| Task 9 | `update-vi.md:26`/`:40` say the off-main report sits "beside the existing divergence notice" (Phase 2, not adjacent to Phase 1) | **FIXED** by Task 17's residue sweep (Part B row 1) — re-verified directly: `grep -n "beside the" commands/update-vi.md` → 0 hits in this session |
| Task 11 | `:236` attributes a sentence to `ard-resolution.md`'s No-regression-rule section; it's actually in the Output section | **FIXED** by Task 17's residue sweep (Part B row 2) — re-verified directly: `implement.md:236` (current tree) cites "per `ard-resolution.md`'s Output section", confirmed in this session's own R26/Risk-2 reads |
| Task 13 | `:3` frontmatter compresses unmerged-vs-missing into one phrase | **FIXED** by Task 17's residue sweep (Part B row 4) — re-verified directly: `ready.md:3`'s frontmatter now distinguishes "an unmerged artifact (a readiness finding capping the verdict at PARTIAL) or a missing one (recorded as a coverage gap)" — quoted in full under R38 above |
| Task 13 | Phase 4's dispatch template leaves implicit how a warned artifact appears in the `specs:`/`designs:` path list | Not re-checked — a documentation-completeness judgment call, not a defect; out of this task's scope to adjudicate |
| Task 15 | 3 report-bookkeeping issues (a "(36 entries)" header over a 46-row table; a "hunks" mislabel; a "(21 rows)" collapse) | Report-only, no shipped-file effect |
| Task 17 | `residue-sweep.md` itself cites "CHANGELOG.md:2, :792" where `:2` is a `grep -c` match count, not a line number | Still open — this record does not re-verify or fix `residue-sweep.md`'s own internal citation, since `residue-sweep.md` is a proof artifact, not a shipped file. Real lines are 25 and 792 per the ledger |
| Task 19 | Hard-wrap self-audit incomplete — other prose the copilot port rewrote in `ready/SKILL.md` (~199–219) and `idea/SKILL.md` (~14–15) stays multi-line, following each file's pre-existing non-compliant wrap convention | Not re-checked in full — the ledger records this as "not a fresh divergence" (pre-existing convention in those files, not introduced by J). Accepted as recorded |
| Task 19 | Report claims 20 raw `relocat` hits; an unscoped re-grep finds 25 | Report-only; the ledger notes the substantive conclusion (no hit attributes relocation to `create-vi:`) is unaffected. Spot-checked in R36's own read of `relocat`-adjacent text — no contradicting hit found |

## Limitations — what this record does not establish

- **No genuine end-to-end handoff was exercised.** Every producer/consumer wiring check above is a **static read** of the command files' prose — confirming that the specified call is present, correctly placed, and correctly worded. None of the 8 producers or 7 consumers was actually run against a live Claude Code session driving a real `/idea`, `/create-vi`, …, `/ready` invocation end to end. The `gh pr create --dry-run` vs. real-call comparison under Risk 7 is the closest this record comes to executing any part of the mechanism, and it deliberately stopped short of the one thing that would fully close R48/Risk 7 — pushing a real branch and opening a real, non-dry-run pull request — because doing so would create a live artifact in `Dynatrace-Internal/mgd-specifications` as a side effect of a verification record, which this task's scope does not license without the user's explicit go-ahead.
- **The state-table's operational completeness was checked, not its full implementation.** `require-on-main`'s ten rows are specified with correct outcomes (R7, PASS) and the primitives — now four, after this session's fix to §3.2 — are individually sound, but no actual script implementing them together was written and run against a battery of real repository states. Risk 1's row-G finding was reached by isolated empirical testing of `git cat-file -e`'s and `git rev-parse --verify --quiet`'s exit-code behaviour in a scratch repo, not by running a full `require-on-main` implementation, and the fix has not been exercised end-to-end inside an actual consumer command either.
- **The residue sweep's coverage gap (Risk 3) is reported, not closed.** This record confirms `idea.md:335`'s specific defect is now fixed in all 3 editions, and confirms Task 17's Step 3 methodology genuinely found other real defects — but it does not perform a fresh, independent "what did I make false?" pass over the whole branch's diff. Doing so exhaustively (every sentence in every file this branch touched, re-read after every subsequent edit to the same file) was judged out of proportion to Task 20's mandate to verify the 53 stated requirements and 9 stated risks, not to re-run Task 17 at greater depth.
- **The port's dialect compliance (R45) was spot-checked, not exhaustively swept.** Verified `idea/SKILL.md` and the shared `phase-handoff.md` directly; did not individually re-verify all 19 modified copilot files' dialect compliance line-by-line — that exhaustive sweep is `task-19-report.md`'s own claimed methodology (verified line-by-line per its own account), and this record's spot check is consistent with, but does not independently reproduce, that full sweep.
- **mgd's CHANGELOG `2.51.0` gap (found under the Parity statement) is reported as pre-existing and out of scope, not verified as harmless.** This record confirms the gap sits entirely inside the historical `2.51.0` section and does not touch `2.52.0`; it does not independently verify that the underlying `read-only-repos.md` §6 code fix this gap concerns was actually correctly ported to mgd's code (a Task 18 concern, not a Task 20 one) — only that the CHANGELOG entry describing it is missing.
- **Deferred minors marked "not re-checked" above are exactly that** — carried forward from the ledger's own record without independent re-verification in this session, because they are either report-only bookkeeping with no shipped-file effect, or judgment calls a prior reviewer already made and accepted. A future reader triaging this list should not read "not re-checked" as "confirmed absent."

## Summary

53 of 53 requirements PASS on the tree this record leaves behind. Two of them — R4 and R7 — did not pass on first measurement: R4's `gh auth status` pre-check, named in D7's own rationale, was textually absent from `references/phase-handoff.md` §2.6, and R7's ten-row state table, while itself correctly worded, rested on three primitives that could not, as literally specified, distinguish row G (`origin/<default>` ref absent) from row F (path absent on an existing ref) — both make `git cat-file -e` exit 128 with the only distinguishing signal on discarded stderr, confirmed empirically twice in this session in a scratch repo. Both gaps were fixed in this session, in canonical, mgd, and copilot, before this record was finalized, so the PASS recorded for R4 and R7 above describes the corrected tree and says explicitly, in its own row, that it was found failing first — this summary does not launder that history into a clean number. No requirement was found to genuinely not hold after the fix. All 9 risk mitigations were re-run or re-derived this session; 2 are assessed **insufficient on their own** exactly as the brief's own known-weak-spot list predicted (Risk 7's `--dry-run`, now with a fresh empirical reproduction distinguishing it from the real, failing call; Risk 3's residue-sweep coverage gap, now confirmed fixed at its one known instance but structurally unclosed), and Risk 1's row stays documented as a genuine, previously-unreported miss by its own predecessor document: `gate-reachability.md` states "no defect in §3.3 is reported here," and this record's independent re-derivation found one that trace did not — the fix closes the defect, but the miss itself is not erased from the record. The parity statement confirms exactly five identity files between canonical and mgd both before and after the fix, matching R44 exactly. All identity files, in both mgd and (by dialect-appropriate equivalent) copilot, were checked against §7's residue-claim list and none was found to duplicate or diverge from it.
